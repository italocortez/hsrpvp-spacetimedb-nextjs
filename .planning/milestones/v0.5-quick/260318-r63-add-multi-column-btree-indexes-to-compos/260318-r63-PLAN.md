---
phase: quick
plan: 260318-r63
type: execute
wave: 1
depends_on: []
files_modified:
  # Step 1 — Table index additions (19 tables, TournamentParticipant already done)
  - spacetimedb/src/tables/calendarEventInvite.ts
  - spacetimedb/src/tables/characterStats.ts
  - spacetimedb/src/tables/costSetDraftCharacter.ts
  - spacetimedb/src/tables/costSetDraftLightcone.ts
  - spacetimedb/src/tables/costSetDraftSynergy.ts
  - spacetimedb/src/tables/groupStanding.ts
  - spacetimedb/src/tables/hsrAccountCharacter.ts
  - spacetimedb/src/tables/hsrAccountLightcone.ts
  - spacetimedb/src/tables/hsrCharacterArchetype.ts
  - spacetimedb/src/tables/hsrCharacterCost.ts
  - spacetimedb/src/tables/hsrLightconeCost.ts
  - spacetimedb/src/tables/lobbyMember.ts
  - spacetimedb/src/tables/matchResultGame.ts
  - spacetimedb/src/tables/matchResultParticipant.ts
  - spacetimedb/src/tables/mmrRating.ts
  - spacetimedb/src/tables/savedCalendar.ts
  - spacetimedb/src/tables/teamMember.ts
  - spacetimedb/src/tables/tournamentAssistant.ts
  - spacetimedb/src/tables/tournamentTeamRequest.ts
  # Step 2 — Reducer/helper filter+find migration (11 files)
  - spacetimedb/src/reducers/roster.ts
  - spacetimedb/src/reducers/rosterAdmin.ts
  - spacetimedb/src/reducers/tournamentRegistration.ts
  - spacetimedb/src/reducers/tournamentTeams.ts
  - spacetimedb/src/reducers/tournamentAdmin.ts
  - spacetimedb/src/reducers/refereeManagement.ts
  - spacetimedb/src/reducers/matchResultSubmission.ts
  - spacetimedb/src/reducers/bracketAdvancement.ts
  - spacetimedb/src/reducers/cursor.ts
  - spacetimedb/src/reducers/admin.ts
  - spacetimedb/src/helpers/tournamentHelpers.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All composite PK tables have a multi-column btree index covering all PK columns"
    - "No filter+find patterns remain for composite PK lookups in reducers or helpers"
    - "Module publishes successfully and 32/32 integration tests pass"
  artifacts:
    - path: "spacetimedb/src/tables/*.ts"
      provides: "Multi-column btree indexes on all 20 composite PK tables"
      contains: "by_"
  key_links:
    - from: "spacetimedb/src/reducers/*.ts"
      to: "spacetimedb/src/tables/*.ts"
      via: "multi-column btree accessor .filter([val1, val2])"
      pattern: "\\.filter\\(\\["
---

<objective>
Add multi-column btree indexes to all composite PK tables and migrate all filter+find patterns to use the new multi-column filter API.

Purpose: Eliminates O(n) JavaScript `.find()` scans after btree lookups by pushing the full composite key lookup into the SpacetimeDB index, making composite PK lookups O(log n) end-to-end.

Output: 19 table files updated with new indexes, 11 reducer/helper files migrated to `.filter([val1, val2])` pattern, module published and tests passing.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/skills/spacetimedb/SKILL.md — accessor types, multi-column btree index definition and usage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-column btree indexes to all composite PK tables</name>
  <files>
    spacetimedb/src/tables/calendarEventInvite.ts
    spacetimedb/src/tables/characterStats.ts
    spacetimedb/src/tables/costSetDraftCharacter.ts
    spacetimedb/src/tables/costSetDraftLightcone.ts
    spacetimedb/src/tables/costSetDraftSynergy.ts
    spacetimedb/src/tables/groupStanding.ts
    spacetimedb/src/tables/hsrAccountCharacter.ts
    spacetimedb/src/tables/hsrAccountLightcone.ts
    spacetimedb/src/tables/hsrCharacterArchetype.ts
    spacetimedb/src/tables/hsrCharacterCost.ts
    spacetimedb/src/tables/hsrLightconeCost.ts
    spacetimedb/src/tables/lobbyMember.ts
    spacetimedb/src/tables/matchResultGame.ts
    spacetimedb/src/tables/matchResultParticipant.ts
    spacetimedb/src/tables/mmrRating.ts
    spacetimedb/src/tables/savedCalendar.ts
    spacetimedb/src/tables/teamMember.ts
    spacetimedb/src/tables/tournamentAssistant.ts
    spacetimedb/src/tables/tournamentTeamRequest.ts
  </files>
  <action>
For each composite PK table below, add a multi-column btree index to the `indexes` array. The accessor name follows `by_{col1}_and_{col2}` in snake_case. The columns array must match the PK columns in the same order.

DO NOT touch `tournamentParticipant.ts` — it already has `by_tournament_and_user`.

Exact indexes to add per table:

| Table | Accessor | Columns |
|-------|----------|---------|
| calendarEventInvite | `by_event_and_invitee` | `['eventId', 'inviteeUserId']` |
| characterStats | `by_user_and_character` | `['userId', 'characterName']` |
| costSetDraftCharacter | `by_set_character_and_mode` | `['costSetId', 'characterName', 'gameMode']` |
| costSetDraftLightcone | `by_set_lightcone_and_mode` | `['costSetId', 'lightconeName', 'gameMode']` |
| costSetDraftSynergy | `by_set_source_target_and_mode` | `['costSetId', 'sourceName', 'targetName', 'gameMode']` |
| groupStanding | `by_tournament_group_and_team` | `['tournamentId', 'groupId', 'participantTeamId']` |
| hsrAccountCharacter | `by_account_and_character` | `['hsrAccountId', 'characterName']` |
| hsrAccountLightcone | `by_account_and_lightcone` | `['hsrAccountId', 'lightconeName']` |
| hsrCharacterArchetype | `by_character_and_archetype` | `['characterName', 'archetypeId']` |
| hsrCharacterCost | `by_character_mode_and_set` | `['characterName', 'gameMode', 'costSetId']` |
| hsrLightconeCost | `by_lightcone_mode_and_set` | `['lightconeName', 'gameMode', 'costSetId']` |
| lobbyMember | `by_lobby_and_user` | `['lobbyId', 'userId']` |
| matchResultGame | `by_result_and_game` | `['matchResultId', 'gameNumber']` |
| matchResultParticipant | `by_result_and_user` | `['matchResultId', 'userId']` |
| mmrRating | `by_user_and_mode` | `['userId', 'gameMode']` |
| savedCalendar | `by_user_and_target` | `['userId', 'targetUserId']` |
| teamMember | `by_team_and_user` | `['teamId', 'userId']` |
| tournamentAssistant | `by_tournament_and_user` | `['tournamentId', 'userId']` |
| tournamentTeamRequest | `by_team_and_user` | `['teamId', 'userId']` |

Each index is appended to the existing `indexes` array. Do not remove existing single-column indexes.
  </action>
  <verify>
    <automated>grep -r "by_" spacetimedb/src/tables/ --include="*.ts" | grep -c "algorithm: 'btree'"</automated>
  </verify>
  <done>All 19 table files have a new multi-column btree index appended. The count of `by_*` btree indexes across all table files is at least 20 (19 new + 1 existing on TournamentParticipant).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate filter+find patterns to multi-column filter in all reducers and helpers</name>
  <files>
    spacetimedb/src/reducers/roster.ts
    spacetimedb/src/reducers/rosterAdmin.ts
    spacetimedb/src/reducers/tournamentRegistration.ts
    spacetimedb/src/reducers/tournamentTeams.ts
    spacetimedb/src/reducers/tournamentAdmin.ts
    spacetimedb/src/reducers/refereeManagement.ts
    spacetimedb/src/reducers/matchResultSubmission.ts
    spacetimedb/src/reducers/bracketAdvancement.ts
    spacetimedb/src/reducers/cursor.ts
    spacetimedb/src/reducers/admin.ts
    spacetimedb/src/helpers/tournamentHelpers.ts
  </files>
  <action>
Replace every `[...ctx.db.Table.single_col_idx.filter(val1)].find(r => r.col2 === val2)` pattern with `[...ctx.db.Table.by_col1_and_col2.filter([val1, val2])][0]`.

The `[0]` produces the same result as `.find()` — a single row or `undefined`.

Exact replacements by file (table -> old accessor -> new accessor):

**roster.ts** (2 occurrences):
- Line ~170: `HsrAccountCharacter.hsr_account_id.filter(hsrAccountId)].find(row => row.characterName === item.characterName)` -> `HsrAccountCharacter.by_account_and_character.filter([hsrAccountId, item.characterName])][0]`
- Line ~253: `HsrAccountCharacter.hsr_account_id.filter(targetAccountId)].find(row => row.characterName === char.characterName)` -> `HsrAccountCharacter.by_account_and_character.filter([targetAccountId, char.characterName])][0]`

**rosterAdmin.ts** (6 occurrences):
- Line ~139: `HsrAccountCharacter.hsr_account_id.filter(hsrAccountId)].find(row => row.characterName === item.characterName)` -> `HsrAccountCharacter.by_account_and_character.filter([hsrAccountId, item.characterName])][0]`
- Line ~171: same pattern -> same replacement
- Line ~179: same pattern -> same replacement
- Line ~259: `HsrCharacterArchetype.character_name.filter(characterName)].find(row => row.archetypeId === archId)` -> `HsrCharacterArchetype.by_character_and_archetype.filter([characterName, archId])][0]`
- Line ~283: same pattern -> same replacement
- Line ~293: same pattern -> same replacement

**tournamentRegistration.ts** (6 occurrences — all TournamentParticipant):
- Lines ~27, ~117, ~149, ~175, ~204: `TournamentParticipant.tournament_id.filter(tournamentId)].find(...)` where `.find` checks `row.userId === X` -> `TournamentParticipant.by_tournament_and_user.filter([tournamentId, X])][0]`
- Line ~87: `HsrAccount.user_id.filter(user.id)].find((a: any) => a.isActive)` — SKIP this one. HsrAccount is NOT a composite PK table, this is filtering by a boolean column. Leave it unchanged.

**tournamentTeams.ts** (8 occurrences):
- TournamentParticipant lookups (lines ~36, ~96, ~156, ~216): `.tournament_id.filter(tid)].find(r => r.userId === uid)` -> `.by_tournament_and_user.filter([tid, uid])][0]`
- TournamentTeamRequest lookups (lines ~108, ~139, ~187): `.team_id.filter(teamId)].find(r => r.userId === uid)` -> `.by_team_and_user.filter([teamId, uid])][0]`
- TournamentTeam lookup (line ~48): `.captain_user_id.filter(user.id)].find(team => team.tournamentId === tournamentId)` — TournamentTeam is NOT a composite PK table (has autoInc PK). SKIP this one. Leave it unchanged.
- Line ~146: `TournamentParticipant.tournament_id.filter(team.tournamentId)].filter(...)` — This is `.filter().filter()`, NOT `.filter().find()`. SKIP. Leave unchanged.
- Line ~258: Same — this gets all members, not a single lookup. SKIP.

**tournamentAdmin.ts** (3 occurrences):
- Line ~24: `TournamentParticipant.tournament_id.filter(tournamentId)].find(r => r.userId === userId)` -> `.by_tournament_and_user.filter([tournamentId, userId])][0]`
- Line ~203: `TournamentAssistant.tournament_id.filter(tournamentId)].find(r => r.userId === userId)` -> `.by_tournament_and_user.filter([tournamentId, userId])][0]`
- Line ~262: same TournamentAssistant pattern -> same replacement

**refereeManagement.ts** (6 occurrences — all LobbyMember):
- Lines ~19, ~36, ~94, ~137, ~148, ~179, ~190: `LobbyMember.lobby_id.filter(lobbyId)].find(r => r.userId === X)` -> `LobbyMember.by_lobby_and_user.filter([lobbyId, X])][0]`
- Note: Line ~85 gets ALL members (no `.find()`), leave that unchanged.

**matchResultSubmission.ts** (2 occurrences):
- Line ~87: `LobbyMember.lobby_id.filter(matchResult.lobbyId)].find(r => r.userId === user.id)` -> `LobbyMember.by_lobby_and_user.filter([matchResult.lobbyId, user.id])][0]`
- Line ~105: `TournamentAssistant.tournament_id.filter(matchResult.tournamentId)].find(r => r.userId === user.id)` -> `TournamentAssistant.by_tournament_and_user.filter([matchResult.tournamentId, user.id])][0]`

**bracketAdvancement.ts** (1 occurrence):
- Line ~300: `TournamentParticipant.tournament_id.filter(matchResult.tournamentId)].find(r => r.userId === matchResult.winnerId)` -> `TournamentParticipant.by_tournament_and_user.filter([matchResult.tournamentId, matchResult.winnerId])][0]`

**cursor.ts** (1 occurrence):
- Line ~15: `LobbyMember.lobby_id.filter(lobbyId)].find(r => r.userId === mapping.userId)` -> `LobbyMember.by_lobby_and_user.filter([lobbyId, mapping.userId])][0]`

**admin.ts** (2 occurrences):
- Line ~198: `HsrCharacterArchetype.character_name.filter(key.characterName)].find(r => r.archetypeId === key.archetypeId)` -> `HsrCharacterArchetype.by_character_and_archetype.filter([key.characterName, key.archetypeId])][0]`
- Line ~212: `LobbyMember.lobby_id.filter(key.lobbyId)].find(r => r.userId === key.userId)` -> `LobbyMember.by_lobby_and_user.filter([key.lobbyId, key.userId])][0]`

**tournamentHelpers.ts** (1 occurrence):
- Line ~49: `TournamentAssistant.tournament_id.filter(tournamentId)].find(r => r.userId === user.id)` -> `TournamentAssistant.by_tournament_and_user.filter([tournamentId, user.id])][0]`

After all replacements, remove any leftover `?? null` suffixes that were on the old `.find()` calls — `[0]` already returns `undefined` when no match, which is falsy. `?? null` is harmless but unnecessary clutter.

Remove any comments referencing "Composite PK lookup: filter by indexed X, then match Y" — the new pattern is self-documenting.
  </action>
  <verify>
    <automated>grep -rn "\.filter([^)]*)\]\.find\|\.filter([^)]*)\]\n.*\.find" spacetimedb/src/reducers/ spacetimedb/src/helpers/ --include="*.ts" | grep -v "HsrAccount\.user_id" | grep -v "\.some(" | grep -v "captain_user_id" | wc -l</automated>
  </verify>
  <done>Zero filter+find patterns remain for composite PK lookups in reducers and helpers. Only non-composite-PK filter+find patterns (HsrAccount.user_id for isActive check, TournamentTeam.captain_user_id for tournament check) remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Publish module, generate bindings, and verify tests pass</name>
  <files>spacetimedb/dist/bundle.js</files>
  <action>
1. Publish the module: `spacetime publish hsrpvp-spacetimedb-nextjs-test1 -y --module-path spacetimedb`
   - If publish fails due to schema changes requiring `--clear-database`, add that flag.
   - Adding multi-column btree indexes to existing tables should NOT require --clear-database (it's an additive change).

2. Generate bindings: `spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb`

3. Update module-bindings reference: Read `src/module_bindings/types.ts` and `src/module_bindings/index.ts`, then update `.claude/skills/spacetimedb/references/module-bindings.md`.

4. Run integration tests: `npm run test:integration`
   - All 32 tests must pass.
   - If any test fails, diagnose the cause (likely a wrong column order in .filter([]) call) and fix it.
  </action>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <done>Module published successfully. Bindings regenerated. All 32/32 integration tests pass. Module-bindings reference updated.</done>
</task>

</tasks>

<verification>
1. `grep -rn "by_" spacetimedb/src/tables/ --include="*.ts" | grep "algorithm"` — shows 20+ multi-column btree indexes
2. `grep -rn "\.filter(" spacetimedb/src/reducers/ spacetimedb/src/helpers/ --include="*.ts" | grep "\.find("` — shows only non-composite-PK usages (HsrAccount isActive, TournamentTeam captain)
3. `npm run test:integration` — 32/32 pass
</verification>

<success_criteria>
- All 20 composite PK tables have multi-column btree indexes (19 new + 1 existing on TournamentParticipant)
- All filter+find patterns for composite PK lookups are replaced with .filter([val1, val2])[0]
- Module publishes without --clear-database
- All 32/32 integration tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260318-r63-add-multi-column-btree-indexes-to-compos/260318-r63-SUMMARY.md`
</output>
