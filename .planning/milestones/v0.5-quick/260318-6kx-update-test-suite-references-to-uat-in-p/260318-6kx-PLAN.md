# Quick Task 260318-6kx: Update test-suite references to uat in planning files

## Task 1: Search and update references

**Files:** `.planning/**/*.md`, `CLAUDE.md`
**Action:** Find all references to `test-suite` skill and update to `uat` where they are actionable pointers (not historical records)
**Verify:** `grep -r "skills/test-suite" .` returns no matches; `grep -r '`test-suite`' .` returns no matches
**Done:** All actionable references point to `uat`

## Result

- `.planning/STATE.md` line 59: Historical record — NOT modified (task description, not a skill pointer)
- `CLAUDE.md` line 25: Already updated by user (test-suite → uat)
- `CLAUDE.md` line 65: Already updated by user (skills/test-suite → skills/uat)
- No other actionable references found in `.planning/`
