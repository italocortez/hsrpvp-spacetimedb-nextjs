---
phase: quick-260401-7zs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notes/erd-mermaid.md
  - notes/erd.excalidraw
autonomous: false
requirements: [AUDIT-CORE-ENTITIES]
must_haves:
  truths:
    - "Every table column type in the ERD matches the actual SpacetimeDB table definition"
    - "Every FK cardinality (required vs optional) matches the actual code"
    - "Every cross-chunk relationship line referencing core entities exists in the mermaid"
    - "The Excalidraw diagram reflects the same corrections as the mermaid"
  artifacts:
    - path: "notes/erd-mermaid.md"
      provides: "Corrected mermaid ERD"
    - path: "notes/erd.excalidraw"
      provides: "Corrected Excalidraw ERD matching mermaid"
  key_links:
    - from: "notes/erd-mermaid.md"
      to: "spacetimedb/src/tables/*.ts"
      via: "Table definitions match ERD"
---

<objective>
Audit and fix the mermaid ERD (notes/erd-mermaid.md) Chunk 1 (Core Entities) and all cross-chunk relationships referencing core entity tables, then update the Excalidraw diagram to match.

Purpose: The ERD has accumulated cardinality errors, missing relationships, and type mismatches as the schema evolved through 9+ phases. This audit ensures the ERD is an accurate reference.
Output: Corrected erd-mermaid.md and erd.excalidraw files.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@notes/erd-mermaid.md
@spacetimedb/src/tables/user.ts
@spacetimedb/src/tables/userIdentity.ts
@spacetimedb/src/tables/serverIdentity.ts
@spacetimedb/src/tables/userDeletionJob.ts
@spacetimedb/src/tables/hsrCharacter.ts
@spacetimedb/src/tables/hsrLightcone.ts
@spacetimedb/src/tables/archetype.ts
@spacetimedb/src/tables/hsrCharacterArchetype.ts
@spacetimedb/src/tables/season.ts
@spacetimedb/src/tables/tournament.ts
@spacetimedb/src/tables/tournamentStandIn.ts
@spacetimedb/src/tables/matchResult.ts
@spacetimedb/src/tables/mmrHistory.ts
@spacetimedb/src/tables/userAchievement.ts
@spacetimedb/src/tables/matchResultGame.ts
@spacetimedb/src/tables/leaderboard.ts
@spacetimedb/src/tables/tournamentParticipant.ts
@spacetimedb/src/tables/costSetDraftSynergy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mermaid ERD cardinalities, types, and missing relationships</name>
  <files>notes/erd-mermaid.md</files>
  <action>
Apply all corrections found during the audit of core entity tables and their cross-chunk references. Read every table file referenced below to confirm each fix before applying.

**Cardinality Fixes (required vs optional mismatches):**

1. Line ~175: `tournament }o--|| season : "seasonId"` -> change to `tournament }o--o| season : "seasonId"` (seasonId is `t.u32().optional()` in tournament.ts)

2. Line ~188: `tournament_stand_in }o--o| user : "approvedByUserId"` -> change to `tournament_stand_in }o--|| user : "approvedByUserId"` (approvedByUserId is `t.u32()` required in tournamentStandIn.ts)

3. Line ~392: `match_result_record }o--|| user : "winnerUserId"` -> change to `match_result_record }o--o| user : "winnerUserId"` (winnerUserId is `t.u32().optional()` in matchResult.ts)

4. Line ~409: `mmr_history }o--|| season : "seasonId"` -> change to `mmr_history }o--o| season : "seasonId"` (seasonId is `t.u32().optional()` in mmrHistory.ts)

5. Line ~424: `user_achievement }o--o| user : "awardedById"` -> change to `user_achievement }o--|| user : "awardedById"` (awardedById is `t.u32()` required in userAchievement.ts)

6. Line ~179: `tournament_participant }o--|| hsr_account : "hsrAccountId"` -> change to `tournament_participant }o--o| hsr_account : "hsrAccountId"` (hsrAccountId is `t.u32().optional()` in tournamentParticipant.ts)

**Type Fixes:**

7. In match_result_game entity block (~line 283): change `u32 gameNumber PK` to `u8 gameNumber PK` (actual type is `t.u8()`)

8. In leaderboard entity block (~line 319): change `u32 rank PK` to `u16 rank PK` (actual type is `t.u16()`)

**Missing Relationship Lines:**

9. Add after the cost_set_draft_synergy relationships section (~line 119):
   ```
   cost_set_draft_synergy }o--|| hsr_character : "sourceName"
   cost_set_draft_synergy }o--|| hsr_character : "targetName"
   ```
   These are missing entirely. The cost_set_draft_synergy table has sourceName and targetName columns that reference hsr_character names.

**Verification approach:** After editing, grep the file for each changed line to confirm correctness.
  </action>
  <verify>
    <automated>cd "D:/GitsWork/hsrpvp-spacetimedb-nextjs" && grep -c "tournament }o--o| season" notes/erd-mermaid.md && grep -c "tournament_stand_in }o--|| user : \"approvedByUserId\"" notes/erd-mermaid.md && grep -c "match_result_record }o--o| user : \"winnerUserId\"" notes/erd-mermaid.md && grep -c "mmr_history }o--o| season" notes/erd-mermaid.md && grep -c "user_achievement }o--|| user : \"awardedById\"" notes/erd-mermaid.md && grep -c "tournament_participant }o--o| hsr_account" notes/erd-mermaid.md && grep -c "u8 gameNumber" notes/erd-mermaid.md && grep -c "u16 rank" notes/erd-mermaid.md && grep -c "cost_set_draft_synergy }o--|| hsr_character" notes/erd-mermaid.md && echo "ALL_CHECKS_PASSED"</automated>
  </verify>
  <done>All 9 corrections applied: 6 cardinality fixes, 2 type fixes, 2 missing relationship lines added. Each correction verified against the actual SpacetimeDB table definition file.</done>
</task>

<task type="auto">
  <name>Task 2: Update Excalidraw ERD to match mermaid corrections</name>
  <files>notes/erd.excalidraw</files>
  <action>
Load the `/draw` skill. Read the existing `notes/erd.excalidraw` file and the Excalidraw MCP `read_me` cheat sheet to understand the current diagram structure.

Use `mcp__excalidraw__query_elements` to inspect the current checkpoint or read the file to find:
- Arrow elements that represent the 6 cardinality-changed relationships
- Text/label elements on table boxes for match_result_game (gameNumber type) and leaderboard (rank type)
- Whether cost_set_draft_synergy -> hsr_character arrows exist

Then apply these corrections using the Excalidraw MCP tools:
1. Update arrow labels/styles for the 6 cardinality changes (required <-> optional)
2. Fix gameNumber type label from u32 to u8 in match_result_game box
3. Fix rank type label from u32 to u16 in leaderboard box
4. Add 2 new arrows from cost_set_draft_synergy to hsr_character for sourceName and targetName

Save the result to `notes/erd.excalidraw`.

Note: The Excalidraw diagram is large (60 tables). Focus changes ONLY on the affected elements. Do not redraw the entire diagram.
  </action>
  <verify>
    <automated>cd "D:/GitsWork/hsrpvp-spacetimedb-nextjs" && test -f notes/erd.excalidraw && node -e "const d=JSON.parse(require('fs').readFileSync('notes/erd.excalidraw','utf8')); console.log('elements:', d.elements.length); console.log('type:', d.type);" && echo "FILE_VALID"</automated>
  </verify>
  <done>Excalidraw ERD updated with all corrections matching the mermaid fixes: cardinality arrows corrected, type labels fixed, missing relationship arrows added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Corrected mermaid ERD and Excalidraw diagram with 9 fixes: 6 cardinality corrections (tournament.seasonId, tournament_stand_in.approvedByUserId, match_result_record.winnerUserId, mmr_history.seasonId, user_achievement.awardedById, tournament_participant.hsrAccountId), 2 type fixes (match_result_game.gameNumber u32->u8, leaderboard.rank u32->u16), and 2 missing relationship lines (cost_set_draft_synergy -> hsr_character for sourceName and targetName).</what-built>
  <how-to-verify>
    1. Open notes/erd-mermaid.md in VS Code and review the diff (9 changed locations)
    2. Paste the mermaid block into https://mermaid.live to verify it renders without errors
    3. Open http://localhost:4000 or the erd.excalidraw file to verify the Excalidraw diagram changes
    4. Spot-check 2-3 corrections against the actual table files in spacetimedb/src/tables/
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All 9 audit findings corrected in mermaid ERD
- Excalidraw diagram updated to match
- No mermaid syntax errors (renders in mermaid.live)
- No Excalidraw JSON parse errors
</verification>

<success_criteria>
- notes/erd-mermaid.md has correct cardinalities matching actual SpacetimeDB table definitions for all core entity cross-references
- notes/erd.excalidraw reflects the same corrections
- Zero discrepancies between ERD and code for Chunk 1 core entities and their cross-chunk references
</success_criteria>

<output>
After completion, leave files unstaged for user review per project git rules.
</output>
