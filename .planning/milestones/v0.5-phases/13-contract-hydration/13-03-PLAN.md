---
phase: 13-contract-hydration
plan: 03
type: execute
wave: 2
depends_on:
  - "13-02"
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "All 7 codebase docs reflect the current state of the codebase including Phase 12.3 and Phase 14 changes"
    - "STRUCTURE.md lists rosterMutations.ts in the helpers directory"
    - "TESTING.md shows 63 test files (not 58) and lists all 5 Phase 12.3 test files"
    - "ARCHITECTURE.md documents accountRatingSnapshot on MatchResultParticipant and requireOwnership on Tournament"
    - "CONVENTIONS.md documents the rosterMutations helper extraction pattern and D-G lobby guard pattern"
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Current architecture overview including Phase 12.3 schema additions"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Current known issues and tech debt"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Current coding conventions including helper extraction patterns"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Current external integrations"
    - path: ".planning/codebase/STACK.md"
      provides: "Current technology stack"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Current project file tree including new files"
    - path: ".planning/codebase/TESTING.md"
      provides: "Current test infrastructure with updated file counts"
  key_links:
    - from: ".planning/codebase/TESTING.md"
      to: "test/backend/**/*.test.ts"
      via: "Test file count matches filesystem"
      pattern: "63 test files"
    - from: ".planning/codebase/STRUCTURE.md"
      to: "spacetimedb/src/helpers/rosterMutations.ts"
      via: "New helper listed in file tree"
      pattern: "rosterMutations"
---

<objective>
Full regeneration of all 7 .planning/codebase/ documentation files from the current codebase state per D-12. These files are dated 2026-04-09 and miss Phase 12.3 (13 source file changes, 5 new test files) and Phase 14 (test harness changes) content.

Purpose: Codebase docs are the primary context source for planning agents in future phases. Stale docs cause agents to miss new patterns (rosterMutations helper, D-G lobby guards), new files, and updated counts.

Output: 7 regenerated codebase documentation files reflecting the complete current state.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate STACK.md, STRUCTURE.md, INTEGRATIONS.md, and CONCERNS.md</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/STACK.md (current state -- dated 2026-04-09, read to understand format then regenerate from scratch)
    - .planning/codebase/STRUCTURE.md (current state -- dated 2026-04-09, understand format then regenerate)
    - .planning/codebase/INTEGRATIONS.md (current state -- dated 2026-04-09)
    - .planning/codebase/CONCERNS.md (current state -- dated 2026-04-09)
    - package.json (for current dependency versions)
    - spacetimedb/src/index.ts (for current module exports including rosterMutations)
    - spacetimedb/src/helpers/ (ls directory to see all helper files including rosterMutations.ts)
    - test/backend/ (ls directory recursively to count test files -- should be 63)
  </read_first>
  <action>
Per D-12, fully regenerate these 4 files from scratch (not incremental update). Read the current versions for FORMAT reference only, then write new content from the actual codebase state.

**STACK.md:**
Regenerate the full technology stack inventory. Key updates vs 2026-04-09 version:
- SpacetimeDB SDK version should match current package.json (was 2.1.0 from Phase 12.2)
- Test file count: 63 (was 58)
- Include Phase 14's test infrastructure changes (withConfirmedReads, onApplied subscription readiness)
- Include `test/global-setup.ts` from Phase 14

**STRUCTURE.md:**
Regenerate the full project file tree. Key updates:
- Add `spacetimedb/src/helpers/rosterMutations.ts` (NEW in Phase 12.3, 105 lines, applyBatchUpsert/applyBatchRemove)
- Add 5 new test files from Phase 12.3:
  - `test/backend/match-results/mmr-snapshot.test.ts`
  - `test/backend/match-results/mmr-snapshot-betweengames.test.ts`
  - `test/backend/match-session/auto-pick-ownership-pool.test.ts`
  - `test/backend/roster/migrate-roster-rating.test.ts`
  - `test/backend/tournaments/tournament-ordering-guard.test.ts`
- Add `test/global-setup.ts` from Phase 14
- Total test files: 63 (verify with filesystem)
- Total helper files: verify count with filesystem (was ~26, now includes rosterMutations.ts)

**INTEGRATIONS.md:**
Regenerate external integrations. Check for any Phase 12.3/14 changes to external service usage. Likely minimal changes since Phase 12.3 was internal-only (no new external dependencies).

**CONCERNS.md:**
Regenerate known issues and tech debt. Key updates:
- The Phase 5 mmr-account-rating-source design bug (documented in `.planning/debug/phase-5-mmr-account-rating-source.md`) is now FIXED by Phase 12.3 -- should be removed from concerns or marked resolved
- Phase 14 withConfirmedReads changes may affect test reliability notes
- Check if iter() performance concerns have been addressed or if new ones emerged

Set all 4 files to `Last updated: 2026-04-12`.
  </action>
  <verify>
    <automated>head -3 .planning/codebase/STACK.md .planning/codebase/STRUCTURE.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - STACK.md contains "2026-04-12" as last updated date
    - STRUCTURE.md contains "rosterMutations.ts" in the file tree
    - STRUCTURE.md contains "mmr-snapshot.test.ts" in the test file listing
    - STRUCTURE.md contains "global-setup.ts"
    - INTEGRATIONS.md contains "2026-04-12" as last updated date
    - CONCERNS.md contains "2026-04-12" as last updated date
    - All 4 files exist and are non-empty (each > 50 lines)
  </acceptance_criteria>
  <done>4 codebase docs regenerated from scratch per D-12 with current project state including Phase 12.3 and Phase 14 changes.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate ARCHITECTURE.md, CONVENTIONS.md, and TESTING.md</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (current state -- understand format then regenerate)
    - .planning/codebase/CONVENTIONS.md (current state -- understand format then regenerate)
    - .planning/codebase/TESTING.md (current state -- understand format then regenerate)
    - spacetimedb/src/tables/matchResultParticipant.ts (for accountRatingSnapshot column)
    - spacetimedb/src/tables/tournament.ts (for requireOwnership column)
    - spacetimedb/src/helpers/rosterMutations.ts (new helper, read in full for convention documentation)
    - spacetimedb/src/reducers/roster.ts (D-G guard pattern for conventions documentation)
    - test/shared/connection.ts (for Phase 14 withConfirmedReads and onApplied patterns)
    - test/global-setup.ts (Phase 14 addition)
    - vitest.config.ts (current test configuration)
  </read_first>
  <action>
Per D-12, fully regenerate these 3 files from scratch. These are the most content-heavy codebase docs and have the most Phase 12.3 impact.

**ARCHITECTURE.md:**
Regenerate the architecture overview. Key updates:
- Document `accountRatingSnapshot: f64` column on `MatchResultParticipant` table (captures account rating at match start for ELO calculation)
- Document `requireOwnership: boolean` column on `Tournament` table (controls character ownership validation during draft)
- Document `rosterMutations.ts` as a new helper module for batch roster operations (applyBatchUpsert, applyBatchRemove)
- Document the D-G lobby guard pattern: roster mutation reducers check for active LobbyMemberAccount before allowing changes
- Document the D-H ordering guard: tournament match finalization restricted until tournament reaches terminal stage
- Include total table count: 67 tables
- Include total reducer export count: ~156

**CONVENTIONS.md:**
Regenerate coding conventions. Key new patterns from Phase 12.3:
- **Helper extraction pattern:** When multiple reducers share batch operation logic, extract to a `*Mutations.ts` helper (e.g., rosterMutations.ts holds applyBatchUpsert/applyBatchRemove used by both user and admin roster reducers)
- **D-G lobby guard pattern:** Before allowing roster mutations, check if caller has an active LobbyMemberAccount -- pattern: `LobbyMemberAccount.lobbyId.filter(lobbyId).find(lma => lma.userId === ctx.sender)`
- **Monotonic snapshot pattern:** When updating cached values (accountRatingSnapshot), only update if new value is >= existing (prevents gaming by downgrading)
- Phase 14's `withConfirmedReads(false)` convention for test connections
- Phase 14's `onApplied` callback pattern replacing fixed-timeout subscription sync

**TESTING.md:**
Regenerate test infrastructure documentation. Key updates:
- Test file count: 63 (was 58)
- List all 5 new Phase 12.3 test files with brief descriptions:
  - `mmr-snapshot.test.ts` -- accountRatingSnapshot capture at start_draft
  - `mmr-snapshot-betweengames.test.ts` -- monotonic snapshot hook during BetweenGames account changes
  - `auto-pick-ownership-pool.test.ts` -- timer_expiry_classic uses LMA pool
  - `migrate-roster-rating.test.ts` -- D-D-04 migrate_roster rating recompute + D-G guards
  - `tournament-ordering-guard.test.ts` -- D-H-01 ordering guard + process_tournament_mmr
- `test/global-setup.ts` from Phase 14
- Phase 14 test infrastructure: `withConfirmedReads(false)` on connection builder, `onApplied` subscription readiness pattern
- Update test count estimate (run `grep -r "it(" test/backend/ --include="*.test.ts" | wc -l` for current count or note the estimate)

Set all 3 files to `Last updated: 2026-04-12`.
  </action>
  <verify>
    <automated>head -3 .planning/codebase/ARCHITECTURE.md .planning/codebase/CONVENTIONS.md .planning/codebase/TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - ARCHITECTURE.md contains "accountRatingSnapshot"
    - ARCHITECTURE.md contains "requireOwnership"
    - ARCHITECTURE.md contains "67 tables" or "67" in a table count context
    - CONVENTIONS.md contains "rosterMutations" (helper extraction pattern)
    - CONVENTIONS.md contains "LobbyMemberAccount" (D-G guard pattern)
    - CONVENTIONS.md contains "withConfirmedReads" (Phase 14 convention)
    - TESTING.md contains "63" in a test file count context
    - TESTING.md contains "mmr-snapshot" in a test file listing
    - TESTING.md contains "global-setup" (Phase 14 addition)
    - TESTING.md contains "onApplied" (Phase 14 subscription pattern)
    - All 3 files have "2026-04-12" as last updated
    - All 3 files are non-empty (each > 100 lines)
  </acceptance_criteria>
  <done>3 codebase docs regenerated from scratch per D-12 with Phase 12.3 schema additions, new patterns, and Phase 14 test infrastructure changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- internal planning documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | I (Info Disclosure) | codebase docs | accept | Internal planning docs; no sensitive data (no env vars, secrets, or credentials) |
</threat_model>

<verification>
1. All 7 files have `Last updated: 2026-04-12` or `2026-04-12` date reference
2. `grep "rosterMutations" .planning/codebase/STRUCTURE.md .planning/codebase/CONVENTIONS.md` returns matches in both files
3. `grep "accountRatingSnapshot" .planning/codebase/ARCHITECTURE.md` returns a match
4. `grep "63" .planning/codebase/TESTING.md` returns a match in test file count context
5. `wc -l .planning/codebase/*.md` shows all 7 files are substantial (> 50 lines each)
</verification>

<success_criteria>
- All 7 codebase docs regenerated from scratch per D-12
- All reflect current codebase state including Phase 12.3 + Phase 14 changes
- rosterMutations.ts helper documented in STRUCTURE and CONVENTIONS
- accountRatingSnapshot and requireOwnership documented in ARCHITECTURE
- Phase 14 test infrastructure documented in TESTING and CONVENTIONS
- Test file count accurate (63)
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-03-SUMMARY.md`
</output>
