---
phase: 13-contract-hydration
plan: 04
type: execute
wave: 3
depends_on:
  - "13-02"
  - "13-03"
files_modified:
  - docs/FRONTEND-HANDOFF.md
  - notes/erd-mermaid.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "FRONTEND-HANDOFF.md shows 86 mapped requirements (not 83)"
    - "FRONTEND-HANDOFF.md shows 25 phases (not 20)"
    - "FRONTEND-HANDOFF.md shows ~156 reducers (not ~155)"
    - "FRONTEND-HANDOFF.md shows 63 test files (not 58)"
    - "ERD mermaid includes accountRatingSnapshot column on MatchResultParticipant"
    - "ERD mermaid includes requireOwnership column on Tournament"
  artifacts:
    - path: "docs/FRONTEND-HANDOFF.md"
      provides: "Accurate frontend handoff document with correct statistics"
    - path: "notes/erd-mermaid.md"
      provides: "Complete ERD with Phase 12.3 column additions"
  key_links:
    - from: "docs/FRONTEND-HANDOFF.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirement count must match"
      pattern: "86 mapped"
    - from: "notes/erd-mermaid.md"
      to: "spacetimedb/src/tables/matchResultParticipant.ts"
      via: "Column definitions sourced from actual table files"
      pattern: "accountRatingSnapshot"
---

<objective>
Rewrite FRONTEND-HANDOFF.md with correct statistics (per D-14, after all other docs are normalized) and update ERD mermaid with 2 missing Phase 12.3 columns (per D-15, D-16).

Purpose: FRONTEND-HANDOFF.md is the v1 frontend team's entry point to the backend. Stale numbers (83 vs 86 requirements, 20 vs 25 phases, 58 vs 63 test files) misrepresent backend completeness. The ERD is a quick-reference for schema questions during frontend development.

Output: Accurate FRONTEND-HANDOFF.md and complete ERD mermaid diagram.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md

Depends on Plans 02 and 03 being complete (architecture/contract docs normalized, codebase docs regenerated).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FRONTEND-HANDOFF.md with correct statistics</name>
  <files>docs/FRONTEND-HANDOFF.md</files>
  <read_first>
    - docs/FRONTEND-HANDOFF.md (full file -- understand current structure, identify all stale numbers)
    - .planning/REQUIREMENTS.md (for accurate requirement count: 89 total, 86 mapped, 3 OOS)
    - .planning/ROADMAP.md (for accurate phase count: 25 phases in progress table after Plan 01 fixes)
    - .planning/codebase/TESTING.md (for accurate test file count from Plan 03 regeneration)
    - .planning/codebase/ARCHITECTURE.md (for accurate table/reducer counts from Plan 03 regeneration)
  </read_first>
  <action>
Per D-14, rewrite FRONTEND-HANDOFF.md from scratch (not incremental update). Use the current version for STRUCTURE reference but write fresh content reflecting the finalized state after Plans 01-03.

**Stale numbers to correct (from research):**

| Field | Old Value | New Value | Source |
|-------|-----------|-----------|--------|
| Requirements mapped | 83 | 86 | REQUIREMENTS.md: "Mapped to phases: 86" |
| Requirements total | 83 | 89 | REQUIREMENTS.md: "v0.5 requirements: 89 total" |
| Phases | 20 | 25 | ROADMAP.md progress table row count |
| Reducer exports | ~155 | ~156 | grep verification during execution |
| Test files | 58 | 63 | filesystem count of test/backend/**/*.test.ts |
| Test count | 728 | TBD | Run `grep -rc "it(" test/backend/ --include="*.test.ts" | awk -F: '{s+=$2} END{print s}'` to get current count |

**During execution, verify these numbers:**
- Run `ls test/backend/**/*.test.ts 2>/dev/null | wc -l` or equivalent to confirm 63 test files
- Run `grep -rc "it(" test/backend/ --include="*.test.ts" | awk -F: '{s+=$2} END{print s}'` for current test count
- Run `grep -rc "export function\|export const.*Reducer\|reducer(" spacetimedb/src/reducers/ --include="*.ts" | awk -F: '{s+=$2} END{print s}'` for reducer count estimate

**Structure of rewritten FRONTEND-HANDOFF.md:**
Keep the same general structure as the current version but ensure ALL numeric claims are verified against the live codebase. Key sections:
1. Title: `# HSRPVP Backend -- Frontend Handoff`
2. `Last updated: 2026-04-12`
3. TL;DR with correct counts
4. Backend Summary (phases, requirements, tables, reducers, views, test files)
5. Feature Map (all 19 features with links to architecture.md + contract.md)
6. Getting Started / Developer Quick Start
7. Schema reference (link to ERD)
8. Testing reference (test file count, how to run)

Ensure the feature map includes all 19 feature directories and their documentation links. Do NOT include cost-tables contract (deferred per D-10).

Set `Last updated: 2026-04-12`.
  </action>
  <verify>
    <automated>grep -c "86" docs/FRONTEND-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/FRONTEND-HANDOFF.md contains "86" in a requirements context (86 mapped requirements)
    - docs/FRONTEND-HANDOFF.md contains "25" in a phases context (25 completed phases)
    - docs/FRONTEND-HANDOFF.md contains "63" in a test files context
    - docs/FRONTEND-HANDOFF.md contains "67" in a tables context (67 production tables)
    - docs/FRONTEND-HANDOFF.md contains "Last updated: 2026-04-12"
    - docs/FRONTEND-HANDOFF.md contains links to all 19 feature architecture.md files
    - docs/FRONTEND-HANDOFF.md does NOT contain "83 mapped" or "20 phases" or "58 test" (stale values)
    - docs/FRONTEND-HANDOFF.md file is > 200 lines (substantive rewrite, not a stub)
  </acceptance_criteria>
  <done>FRONTEND-HANDOFF.md fully rewritten with verified statistics from the current codebase, referencing all 19 normalized feature docs.</done>
</task>

<task type="auto">
  <name>Task 2: Update ERD mermaid with Phase 12.3 column additions</name>
  <files>notes/erd-mermaid.md</files>
  <read_first>
    - notes/erd-mermaid.md (full file -- find MatchResultParticipant and Tournament entities)
    - spacetimedb/src/tables/matchResultParticipant.ts (for exact accountRatingSnapshot column definition: f64)
    - spacetimedb/src/tables/tournament.ts (for exact requireOwnership column definition: boolean)
  </read_first>
  <action>
Update the ERD mermaid diagram with 2 missing columns from Phase 12.3 (per D-15):

**1. MatchResultParticipant entity:**
Add `accountRatingSnapshot: f64` column to the entity definition. This column stores the frozen account rating at match start time for ELO calculation.

Find the `MatchResultParticipant` entity block in the mermaid diagram and add the column in the appropriate position (after the existing columns, before the closing brace).

**2. Tournament entity:**
Add `requireOwnership: boolean` column to the entity definition. This column controls whether character ownership validation is enforced during draft.

Find the `Tournament` entity block in the mermaid diagram and add the column in the appropriate position.

**No new entities needed** -- Phase 12.3 added no new tables, only columns to existing tables.

Update the header comment to reflect `Last updated: 2026-04-12` and maintain the existing table count (67).

Per D-16, ensure both columns have appropriate cardinality annotations if they introduce or modify relationships (these are scalar columns, not FKs, so they likely don't change relationships -- but verify).
  </action>
  <verify>
    <automated>grep "accountRatingSnapshot\|requireOwnership" notes/erd-mermaid.md</automated>
  </verify>
  <acceptance_criteria>
    - notes/erd-mermaid.md contains "accountRatingSnapshot" in the MatchResultParticipant entity
    - notes/erd-mermaid.md contains "requireOwnership" in the Tournament entity
    - notes/erd-mermaid.md contains "2026-04-12" as the last updated date
    - notes/erd-mermaid.md still contains "67" tables (no tables were added)
    - notes/erd-mermaid.md contains valid mermaid erDiagram syntax (starts with `erDiagram`)
  </acceptance_criteria>
  <done>ERD mermaid diagram has both Phase 12.3 column additions with correct types and updated timestamp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | T (Tampering) | ERD diagram | accept | ERD is a reference diagram with no runtime impact; accuracy verified against source tables |
</threat_model>

<verification>
1. `grep "86" docs/FRONTEND-HANDOFF.md` returns a match in requirements context
2. `grep "accountRatingSnapshot" notes/erd-mermaid.md` returns a match
3. `grep "requireOwnership" notes/erd-mermaid.md` returns a match
4. `grep -c "2026-04-12" docs/FRONTEND-HANDOFF.md notes/erd-mermaid.md` returns matches for both files
</verification>

<success_criteria>
- FRONTEND-HANDOFF.md has all 5 stale numbers corrected and verified against live codebase
- ERD mermaid has both Phase 12.3 columns added to the correct entities
- Both files dated 2026-04-12
- No stale values remain in either file
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-04-SUMMARY.md`
</output>
