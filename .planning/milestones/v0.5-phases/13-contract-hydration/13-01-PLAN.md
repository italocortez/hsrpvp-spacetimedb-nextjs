---
phase: 13-contract-hydration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - test/backend/match-results/mmr-stats.test.ts
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "ROADMAP Phase list at top includes entries for Phases 12.1, 12.2, 12.3, 13, and 14"
    - "ROADMAP progress table has correct plan counts for Phase 10.5 (5/5) and includes Phase 14 row"
    - "ROADMAP execution order line includes all phases through 12.3"
    - "ROADMAP plan checkmarks are [x] for Phases 1, 2, 4, and 04.1 (matching all other completed phases)"
    - "mmr-stats.test.ts header comment mentions Phase 12.3 snapshot-backed MMR and D-G guard coverage"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected phase list, progress table, execution order, plan checkmarks"
    - path: "test/backend/match-results/mmr-stats.test.ts"
      provides: "Accurate header comment reflecting Phase 12.3 additions"
  key_links:
    - from: ".planning/ROADMAP.md progress table"
      to: ".planning/ROADMAP.md phase list"
      via: "Phase entries must match between both sections"
      pattern: "Phase 10\\.5|Phase 12\\.1|Phase 12\\.2|Phase 12\\.3|Phase 13|Phase 14"
---

<objective>
Fix ROADMAP.md structural issues (missing phase list entries, incorrect progress table rows, stale plan checkmarks) and update mmr-stats.test.ts header comment to reflect Phase 12.3 additions.

Purpose: The ROADMAP is the primary navigation document for planning agents. Missing phase entries and incorrect progress rows cause agents to misread project state. The test header comment is developer documentation that must match actual test content.

Output: Corrected ROADMAP.md and updated test header comment.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md phase list, progress table, execution order, and plan checkmarks</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file -- need to see both the phase list at lines 15-33 AND the progress table at lines 379-403)
  </read_first>
  <action>
Fix the following 4 categories of issues in ROADMAP.md (per D-17, D-18):

**1. Phase list at top (lines ~15-33):**
The numbered phase list after "Decimal phases appear between their surrounding integers in numeric order." is missing 5 entries. Add them in numeric order among the existing entries:

After Phase 12 line, add:
- `- [x] **Phase 12.1: Identity Garbage Collection** - Scheduled cleanup of stale UserIdentity rows based on lastSeenAt age (INSERTED) (completed 2026-04-08)`
- `- [x] **Phase 12.2: SDK Upgrade Audit** - Verify SpacetimeDB SDK 2.0.3 to 2.1.0 upgrade compatibility, export all views, audit SDK internals (INSERTED) (completed 2026-04-09)`
- `- [x] **Phase 12.3: MMR Rating Snapshot** - Freeze accountRating at match-record time, defense-in-depth lobby guards on roster mutation reducers (INSERTED) (completed 2026-04-11)`

After the Phase 12.3 line (or in the right position):
- `- [x] **Phase 13: Documentation Normalization** - Normalize all documentation to consistent structure, full hydration from codebase, regenerate codebase maps, rewrite FRONTEND-HANDOFF (completed 2026-04-12)`
- `- [x] **Phase 14: Test Harness Modernization** - Fix confirmed reads default, event-driven subscription readiness, helper dedup (completed 2026-04-12)`

**2. Progress table (lines ~379-403):**
- Change Phase 10.5 row from `1/1` to `5/5` (it had 5 plans)
- Add a row for Phase 14: `| 14. Test Harness Modernization | 2/2 | Complete | 2026-04-12 |`
- Update Phase 13 row date from `2026-04-09` to `2026-04-12` (rerun date) and plan count from `5/5` to `4/4` (rerun has 4 plans)

**3. Execution order line (line ~374):**
Verify it currently reads: `Phases execute in numeric order: 1 -> 2 -> 3 -> 4 -> 04.1 -> 5 -> 6 -> 06.1 -> 7 -> 8 -> 9 -> 10 -> 10.1 -> 10.3 -> 10.4 -> 10.5 -> 11 -> 12 -> 12.1 -> 12.2 -> 13 -> 14 -> 12.3`
If it does NOT include all phases through 12.3, update it to match. Based on the research, it should already be correct but verify.

**4. Plan checkmarks for completed phases:**
Phases 1, 2, 4, and 04.1 use `[ ]` for their plan items while all other completed phases use `[x]`. Change all `[ ]` plan items in these 4 phase sections to `[x]`:
- Phase 1 plans: `- [ ] 01-01-PLAN.md` and `- [ ] 01-02-PLAN.md` -> `- [x]`
- Phase 2 plans: `- [ ] 02-01-PLAN.md` and `- [ ] 02-02-PLAN.md` -> `- [x]`
- Phase 4 plans: `- [ ] 04-01-PLAN.md`, `- [ ] 04-02-PLAN.md`, `- [ ] 04-03-PLAN.md` -> `- [x]`
- Phase 04.1 plans: `- [ ] 04.1-01-PLAN.md`, `- [ ] 04.1-02-PLAN.md`, `- [ ] 04.1-03-PLAN.md` -> `- [x]`

Also update Phase 13 plan entries to reflect rerun (4 plans instead of 5):
```
Plans:
- [x] 13-01-PLAN.md -- ROADMAP fixes + test header update
- [x] 13-02-PLAN.md -- Architecture timestamp fixes + contract Phase 12.3 additions
- [x] 13-03-PLAN.md -- Codebase docs full regeneration (7 files)
- [x] 13-04-PLAN.md -- FRONTEND-HANDOFF rewrite + ERD update
```
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md contains "Phase 12.1: Identity Garbage Collection"
    - ROADMAP.md contains "Phase 12.2: SDK Upgrade Audit"
    - ROADMAP.md contains "Phase 12.3: MMR Rating Snapshot"
    - ROADMAP.md contains "Phase 14: Test Harness Modernization" in the phase list section (before line 40)
    - Progress table contains "10.5. Test Suite Stabilization | 5/5"
    - Progress table contains "14. Test Harness Modernization | 2/2 | Complete"
    - Phase 1 plans show `[x]` not `[ ]`
    - Phase 2 plans show `[x]` not `[ ]`
    - Phase 4 plans show `[x]` not `[ ]`
    - Phase 04.1 plans show `[x]` not `[ ]`
    - No `[ ]` plan entries exist for completed phases
    - Phase 13 plan list shows 4 entries (rerun structure)
  </acceptance_criteria>
  <done>ROADMAP.md phase list, progress table, execution order, and plan checkmarks are all consistent and accurate for the rerun state of the project.</done>
</task>

<task type="auto">
  <name>Task 2: Update mmr-stats.test.ts header comment for Phase 12.3 coverage</name>
  <files>test/backend/match-results/mmr-stats.test.ts</files>
  <read_first>
    - test/backend/match-results/mmr-stats.test.ts (first 30 lines to see current header, then search for Phase 12.3 describe blocks to understand what was added)
  </read_first>
  <action>
Update the JSDoc header comment (lines 1-19) to include the Phase 12.3 additions. The current header only covers the original Phase 5/6 test content. Phase 12.3 added a `describe('Phase 12.3: snapshot-backed MMR + D-G guard rejections')` block with tests for:
- accountRatingSnapshot capture at start_draft time
- processMatchMmr reading snapshot instead of live HsrAccount
- D-G lobby guard rejections (batch_upsert_characters, batch_remove_characters, set_active_hsr_account, migrate_roster rejected while caller has active LobbyMemberAccount)

Update the header to add these items after the existing bullet list, keeping the same style. Add a line like:
```
 * Phase 12.3 additions:
 * - Verify accountRatingSnapshot captured at start_draft (MMR-RACE-01)
 * - Verify processMatchMmr reads snapshot, not live account (MMR-RACE-02)
 * - Verify D-G lobby guards reject roster mutations during active match (ROST-GUARD-01)
```

Also update the "Coverage gaps" section to remove any items now covered by Phase 12.3 if applicable.
  </action>
  <verify>
    <automated>head -25 test/backend/match-results/mmr-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - mmr-stats.test.ts header comment contains "Phase 12.3" 
    - mmr-stats.test.ts header comment contains "accountRatingSnapshot"
    - mmr-stats.test.ts header comment contains "MMR-RACE-01" or "MMR-RACE"
    - mmr-stats.test.ts header comment contains "ROST-GUARD-01" or "D-G"
    - Header comment block closes with `*/` before line 30
  </acceptance_criteria>
  <done>mmr-stats.test.ts header accurately describes all test content including Phase 12.3 snapshot-backed MMR and D-G guard rejection tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- docs-only changes to markdown and a test file header comment.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T (Tampering) | ROADMAP.md | accept | Low risk -- internal planning doc, no runtime impact |
</threat_model>

<verification>
1. `grep "Phase 12.1" .planning/ROADMAP.md` returns a match in the phase list section
2. `grep "5/5" .planning/ROADMAP.md` returns a match for Phase 10.5 in the progress table
3. `grep -c "\[ \]" .planning/ROADMAP.md` returns 0 (no unchecked plan items for completed phases; note: future phases may have `[ ]` which is correct)
4. `head -25 test/backend/match-results/mmr-stats.test.ts` shows Phase 12.3 additions in header
</verification>

<success_criteria>
- ROADMAP.md is structurally consistent: phase list matches progress table, all completed phases have [x] plan checkmarks, execution order includes all phases
- mmr-stats.test.ts header accurately reflects all test content
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-01-SUMMARY.md`
</output>
