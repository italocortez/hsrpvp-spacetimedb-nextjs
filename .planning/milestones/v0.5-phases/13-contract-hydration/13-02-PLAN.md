---
phase: 13-contract-hydration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/match-results/architecture.md
  - docs/roster/architecture.md
  - docs/tournament/architecture.md
  - docs/match-session/architecture.md
  - docs/roster/contract.md
  - docs/match-results/contract.md
  - docs/tournament/contract.md
  - docs/match-session/contract.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "Architecture files match-results, roster, tournament have header timestamp matching footer (2026-04-12)"
    - "match-session/architecture.md documents auto-pick pool migration from HsrAccount.isActive to LobbyMemberAccount"
    - "roster/contract.md documents D-G lobby guards on set_active_hsr_account, batch_upsert_characters, batch_remove_characters, migrate_roster"
    - "match-results/contract.md documents accountRatingSnapshot capture and processMatchMmr snapshot-based read"
    - "tournament/contract.md documents D-H-01 finalize_match_result ordering guard and Tournament.requireOwnership"
    - "match-session/contract.md documents timer_expiry_classic auto-pick pool migration to LobbyMemberAccount"
  artifacts:
    - path: "docs/match-results/architecture.md"
      provides: "Header timestamp fix to 2026-04-12"
    - path: "docs/roster/architecture.md"
      provides: "Header timestamp fix to 2026-04-12"
    - path: "docs/tournament/architecture.md"
      provides: "Header timestamp fix to 2026-04-12"
    - path: "docs/match-session/architecture.md"
      provides: "Phase 12.3 auto-pick pool migration content"
    - path: "docs/roster/contract.md"
      provides: "D-G lobby guard documentation"
    - path: "docs/match-results/contract.md"
      provides: "MMR snapshot capture and read-path documentation"
    - path: "docs/tournament/contract.md"
      provides: "D-H ordering guard and requireOwnership documentation"
    - path: "docs/match-session/contract.md"
      provides: "Auto-pick pool migration documentation"
  key_links:
    - from: "docs/*/architecture.md header"
      to: "docs/*/architecture.md footer"
      via: "Last updated timestamp must match"
      pattern: "Last updated: 2026-04-12"
    - from: "docs/*/contract.md"
      to: "spacetimedb/src/reducers/*.ts"
      via: "Reducer documentation sourced from actual code per D-09"
      pattern: "accountRatingSnapshot|requireOwnership|LobbyMemberAccount"
---

<objective>
Fix timestamp mismatches on 3 architecture files and add Phase 12.3 content to 4 architecture + 4 contract files. This synchronizes the documentation surface with the 13 source files changed by Phase 12.3.

Purpose: Architecture and contract docs are the primary reference for frontend developers. Stale docs that miss D-G guards, snapshot patterns, and ordering guards will cause incorrect frontend implementations.

Output: 8 updated documentation files reflecting Phase 12.3 changes.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md

Reference files for architecture template:
@docs/roster/architecture.md (D-05 reference example)

Reference files for contract template:
@docs/roster/contract.md (D-11 reference example)

Contract behavior spec template:
@.claude/skills/uat/references/workflow-doc-template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix architecture timestamp mismatches and add match-session Phase 12.3 content</name>
  <files>docs/match-results/architecture.md, docs/roster/architecture.md, docs/tournament/architecture.md, docs/match-session/architecture.md</files>
  <read_first>
    - docs/match-results/architecture.md (full file -- header says 2026-04-09, footer says 2026-04-11; fix header to 2026-04-12)
    - docs/roster/architecture.md (full file -- same timestamp mismatch)
    - docs/tournament/architecture.md (full file -- same timestamp mismatch)
    - docs/match-session/architecture.md (full file -- needs Phase 12.3 auto-pick pool content)
    - spacetimedb/src/reducers/draftClassic.ts (search for timer_expiry_classic and LobbyMemberAccount to understand the auto-pick pool migration)
  </read_first>
  <action>
**For match-results/architecture.md, roster/architecture.md, tournament/architecture.md:**
- Change line 3 `Last updated: 2026-04-09` to `Last updated: 2026-04-12`
- Change footer `*Last updated: 2026-04-11*` to `*Last updated: 2026-04-12*`
- No content changes needed -- Phase 12.3 Plan 08 already added the content, just timestamps are mismatched

**For match-session/architecture.md:**
- Change line 3 `Last updated: 2026-04-09` to `Last updated: 2026-04-12`
- Update footer `*Last updated:*` line to `*Last updated: 2026-04-12*`
- Add a subsection under the relevant Reducer Flows section documenting the auto-pick pool migration in `timer_expiry_classic`:

In the section that describes `timer_expiry_classic` behavior, add documentation that Phase 12.3 changed the auto-pick pool from querying `HsrAccount.isActive` (global active account) to querying `LobbyMemberAccount` (per-match selected accounts). This means timer expiry auto-picks now respect the match-specific account selection rather than the player's globally active account.

Add to Phase History: `| Phase 12.3 | timer_expiry_classic auto-pick pool migrated from HsrAccount.isActive to LobbyMemberAccount per-match selection |`

Tag additions with `Phase 12.3 execution` provenance per CLAUDE.md rules.
  </action>
  <verify>
    <automated>head -3 docs/match-results/architecture.md docs/roster/architecture.md docs/tournament/architecture.md docs/match-session/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/match-results/architecture.md line 3 contains "Last updated: 2026-04-12"
    - docs/roster/architecture.md line 3 contains "Last updated: 2026-04-12"
    - docs/tournament/architecture.md line 3 contains "Last updated: 2026-04-12"
    - docs/match-session/architecture.md line 3 contains "Last updated: 2026-04-12"
    - docs/match-session/architecture.md contains "LobbyMemberAccount" (Phase 12.3 auto-pick pool reference)
    - docs/match-session/architecture.md Phase History section contains "Phase 12.3"
    - All 4 files have footer timestamp of 2026-04-12
  </acceptance_criteria>
  <done>All 4 architecture files have consistent header/footer timestamps and match-session includes Phase 12.3 auto-pick pool migration documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 12.3 content to 4 contract files</name>
  <files>docs/roster/contract.md, docs/match-results/contract.md, docs/tournament/contract.md, docs/match-session/contract.md</files>
  <read_first>
    - docs/roster/contract.md (full file -- reference example per D-11, needs D-G guard additions)
    - docs/match-results/contract.md (full file -- needs accountRatingSnapshot and monotonic hook additions)
    - docs/tournament/contract.md (full file -- needs D-H ordering guard and requireOwnership additions)
    - docs/match-session/contract.md (full file -- needs auto-pick pool migration additions)
    - spacetimedb/src/reducers/roster.ts (search for "LobbyMemberAccount" to find D-G guard implementation)
    - spacetimedb/src/helpers/rosterMutations.ts (NEW helper file, 105 lines -- read in full)
    - spacetimedb/src/helpers/finalizationHelpers.ts (search for "accountRatingSnapshot" to find processMatchMmr read-path)
    - spacetimedb/src/reducers/accountSelection.ts (search for "accountRatingSnapshot" to find monotonic snapshot hook)
    - spacetimedb/src/reducers/matchFinalization.ts (search for "requireOwnership" or "ordering" to find D-H guard)
    - spacetimedb/src/reducers/draftClassic.ts (search for "LobbyMemberAccount" for auto-pick pool)
    - spacetimedb/src/tables/tournament.ts (search for "requireOwnership" column definition)
  </read_first>
  <action>
Update 4 contract files with Phase 12.3 additions. Per D-09, read actual reducer source code for accurate documentation. Per D-08, use full reducer format (Purpose/Permission/Parameters/Flow/Expected State Changes/Error Cases). Per CLAUDE.md, tag additions with `Phase 12.3 execution` in Phase History.

**roster/contract.md:**
1. In the Reducers section, update documentation for these 4 reducers to add D-G lobby guard behavior:
   - `set_active_hsr_account`: Add error case "Caller has active LobbyMemberAccount -> reject with error identifying the conflicting lobby" (ROST-GUARD-01)
   - `batch_upsert_characters`: Same D-G guard error case
   - `batch_remove_characters`: Same D-G guard error case  
   - `migrate_roster`: Same D-G guard error case, also document the D-D-04 rating recompute fix
2. Document the new `rosterMutations.ts` helper file: `applyBatchUpsert` and `applyBatchRemove` delegation pattern (shared between user and admin reducers)
3. Add acceptance scenarios for D-G guards:
   - "Given a user with an active LobbyMemberAccount, when they call batch_upsert_characters, then the reducer rejects with an error message identifying the lobby"
   - Same pattern for batch_remove_characters, set_active_hsr_account, migrate_roster
4. Add Phase History entry: `| Phase 12.3 | D-G lobby guards on roster mutation reducers (ROST-GUARD-01); rosterMutations.ts helper extraction (D-D-01/02/03); migrate_roster rating recompute fix (D-D-04) |`

**match-results/contract.md:**
1. Document the `accountRatingSnapshot` capture in the `start_draft` flow (or wherever MatchResultParticipant insert is documented):
   - At MRP insert time, resolve player's LobbyMemberAccount to get selected hsrAccountId, read HsrAccount.accountRating, write to accountRatingSnapshot column
2. Document the `processMatchMmr` read-path change:
   - Now reads `participant.accountRatingSnapshot` directly instead of querying `HsrAccount` by userId + isActive filter
   - Both standalone-ranked path (runFinalization step 11) and tournament-batch path (process_tournament_mmr) use the same snapshot column
3. Document the `select_match_account` monotonic-upward snapshot hook:
   - When a player changes account selection during BetweenGames, the snapshot is updated only if the new account's rating is HIGHER than the existing snapshot (monotonic-upward -- prevents gaming by swapping to a lower-rated account mid-series)
4. Add acceptance scenarios:
   - "Given a player in a match, when processMatchMmr runs, then it reads accountRatingSnapshot from MatchResultParticipant (not live HsrAccount.accountRating)"
   - "Given a best-of-N series where a player changes account between games, when the new account has a higher rating, then the snapshot is updated to the higher value"
   - "Given a best-of-N series where a player changes to a lower-rated account, then the snapshot retains the original higher value"
5. Add Phase History entry: `| Phase 12.3 | accountRatingSnapshot capture at MRP insert (MMR-RACE-01); processMatchMmr snapshot read-path (MMR-RACE-02); select_match_account monotonic hook |`

**tournament/contract.md:**
1. Document the D-H-01 `finalize_match_result` ordering guard:
   - For tournament-controlled matches, finalization rejects unless the tournament has reached a terminal stage (Completed or Cancelled) OR the match's bracket position allows it
   - Purpose: prevent ELO processing before tournament batch MMR can run
2. Document `Tournament.requireOwnership` column:
   - Boolean column (default false) -- when true, character ownership validation during draft checks against player's enrolled HSR account roster
3. Add acceptance scenarios:
   - "Given a tournament match where the tournament is still InProgress, when finalize_match_result is called, then it rejects with ordering guard error"
   - "Given a tournament with requireOwnership=true, when a player tries to pick a character not in their HSR account, then the pick is rejected"
4. Add Phase History entry: `| Phase 12.3 | D-H-01 finalize_match_result ordering guard (MMR-RACE-02); Tournament.requireOwnership column |`

**match-session/contract.md:**
1. Document the `timer_expiry_classic` auto-pick pool change:
   - Previously: auto-pick used `HsrAccount` filtered by `isActive=true` for the player (global active account)
   - Now: auto-pick uses `LobbyMemberAccount` to get the player's per-match selected account IDs, then queries characters from those specific accounts
   - Why: With Phase 10.4's per-match account selection, the globally active account may differ from the match-selected account
2. Add acceptance scenario:
   - "Given a player whose globally active HSR account differs from their match-selected account, when timer_expiry_classic fires, then the auto-pick pool comes from the match-selected account (LobbyMemberAccount), not the globally active account"
3. Add Phase History entry: `| Phase 12.3 | timer_expiry_classic auto-pick pool migrated from HsrAccount.isActive to LobbyMemberAccount (D-I) |`
  </action>
  <verify>
    <automated>grep -l "Phase 12.3" docs/roster/contract.md docs/match-results/contract.md docs/tournament/contract.md docs/match-session/contract.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/roster/contract.md contains "LobbyMemberAccount" (D-G guard reference)
    - docs/roster/contract.md contains "ROST-GUARD-01" or "D-G" in Phase History
    - docs/roster/contract.md contains "rosterMutations" (helper extraction)
    - docs/match-results/contract.md contains "accountRatingSnapshot"
    - docs/match-results/contract.md contains "MMR-RACE-01" or "snapshot" in Phase History
    - docs/match-results/contract.md contains "monotonic" (select_match_account hook)
    - docs/tournament/contract.md contains "requireOwnership"
    - docs/tournament/contract.md contains "ordering guard" or "D-H"
    - docs/match-session/contract.md contains "LobbyMemberAccount" (auto-pick pool)
    - docs/match-session/contract.md Phase History contains "Phase 12.3"
    - All 4 contract files have "Phase 12.3" in their Phase History section
  </acceptance_criteria>
  <done>All 4 contract files document Phase 12.3 additions with full reducer format per D-08, sourced from actual code per D-09, and tagged with Phase 12.3 provenance in Phase History.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | I (Info Disclosure) | contract.md | accept | Contracts document public reducer behavior; no sensitive data exposed |
</threat_model>

<verification>
1. All 4 architecture files have `Last updated: 2026-04-12` on line 3
2. All 4 contract files contain "Phase 12.3" in their Phase History section
3. `grep "accountRatingSnapshot" docs/match-results/contract.md` returns a match
4. `grep "LobbyMemberAccount" docs/roster/contract.md` returns a match
5. `grep "requireOwnership" docs/tournament/contract.md` returns a match
</verification>

<success_criteria>
- 3 architecture files have consistent header/footer timestamps (2026-04-12)
- match-session/architecture.md has Phase 12.3 auto-pick pool content
- 4 contract files document all Phase 12.3 additions (D-G guards, snapshot pattern, ordering guard, auto-pick pool)
- All additions tagged with Phase 12.3 provenance in Phase History sections
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-02-SUMMARY.md`
</output>
