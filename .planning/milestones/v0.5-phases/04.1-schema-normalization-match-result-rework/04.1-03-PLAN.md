---
phase: 04.1-schema-normalization-match-result-rework
plan: 03
type: execute
wave: 3
depends_on: [04.1-02]
files_modified:
  - docs/match-results/architecture.md
  - docs/match-results/contract.md
  - docs/brackets/architecture.md
  - docs/brackets/contract.md
  - docs/mmr/architecture.md
  - docs/player-stats/architecture.md
  - docs/match-session/architecture.md
  - docs/tournament/architecture.md
  - docs/tournament/contract.md
  - .claude/skills/spacetimedb/SKILL.md
  - .planning/codebase/ARCH.md
autonomous: true
requirements: [NORM-06]

must_haves:
  truths:
    - "All documentation files reflect the new column names, table names, and enum names"
    - "No doc file references player1Id, player2Id, team1Confirmed, team2Confirmed, participantTeamId, or MatchResult enum"
    - "Skill file updated with teamId column mapping"
    - "Codebase ARCH.md references PlayerStat and PlayerCharacterStat (not old names)"
    - "Match result docs describe the ephemeral lifecycle (operational → finalize → delete)"
    - "Match result docs describe captain-based confirmation via MatchResultParticipant"
    - "MMR docs describe tournament batch vs casual immediate timing"
    - "Match session docs describe MatchParticipantHistory as the my-matches junction"
    - "Player stats docs describe 3-table composite-PK structure and incremental aggregation"
    - "Brackets contract describes MMR-free rollback during tournament"
  artifacts:
    - path: "docs/match-results/architecture.md"
      provides: "Updated MatchResultRecord, MatchResultParticipant, MatchResultGame table references PLUS Match Result Lifecycle, Confirmation Flow, Table Relationships, isTournamentControlled sections"
    - path: "docs/match-results/contract.md"
      provides: "Captain-based Score Confirmation, Match Finalization scenario, non-captain edge case"
    - path: "docs/brackets/architecture.md"
      provides: "Updated BracketMatch (team1Id/team2Id/winnerTeamId) and GroupStanding (teamId)"
    - path: "docs/brackets/contract.md"
      provides: "Updated rollback MMR guard behavior for tournament context"
    - path: "docs/mmr/architecture.md"
      provides: "Tournament vs Casual MMR Timing section, process_tournament_mmr flow, mmrProcessedAt gate"
    - path: "docs/player-stats/architecture.md"
      provides: "PlayerStat, PlayerCharacterStat, PlayerRelationship 3-table structure with composite PKs and aggregation pattern"
    - path: "docs/match-session/architecture.md"
      provides: "MatchParticipantHistory junction, updated archive flow, query pattern"
  key_links:
    - from: "docs/match-results/architecture.md"
      to: "spacetimedb/src/tables/matchResult.ts"
      via: "column name documentation"
      pattern: "winnerUserId|isTournamentControlled"
    - from: "docs/match-results/architecture.md"
      to: "docs/mmr/architecture.md"
      via: "lifecycle cross-reference"
      pattern: "mmrProcessedAt|finalize_match_result"
    - from: "docs/brackets/architecture.md"
      to: "spacetimedb/src/tables/bracketMatch.ts"
      via: "column name documentation"
      pattern: "team1Id|team2Id|winnerTeamId"
    - from: "docs/mmr/architecture.md"
      to: "docs/match-results/architecture.md"
      via: "tournament MMR timing cross-reference"
      pattern: "process_tournament_mmr|batch"
---

<objective>
Update all documentation files to reflect the schema changes from Plans 01 and 02, AND rewrite behavioral narratives, flow diagrams, and lifecycle descriptions to explain HOW the new tables work together. This ensures docs accurately describe both the schema AND the operational flows for future phases (especially Phase 5 which builds directly on match results, MMR, and finalization).

Purpose: Documentation accuracy prevents confusion in Phase 5+ when building on these tables. Beyond column renames, Phase 5 needs to understand the ephemeral lifecycle, captain confirmation flow, tournament vs casual MMR timing, and the MatchParticipantHistory junction pattern.
Output: All docs, skill file, and codebase map updated with new names AND new behavioral sections.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-schema-normalization-match-result-rework/04.1-CONTEXT.md
@.planning/phases/04.1-schema-normalization-match-result-rework/04.1-RESEARCH.md (Wave 6 section)
@.planning/phases/04.1-schema-normalization-match-result-rework/04.1-01-SUMMARY.md
@.planning/phases/04.1-schema-normalization-match-result-rework/04.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update match-results, brackets, and tournament docs (names + behavioral narratives)</name>
  <files>
    docs/match-results/architecture.md
    docs/match-results/contract.md
    docs/brackets/architecture.md
    docs/brackets/contract.md
    docs/tournament/architecture.md
    docs/tournament/contract.md
  </files>
  <read_first>
    docs/match-results/architecture.md
    docs/match-results/contract.md
    docs/brackets/architecture.md
    docs/brackets/contract.md
    docs/tournament/architecture.md
    docs/tournament/contract.md
    spacetimedb/src/tables/matchResult.ts (source of truth for current columns)
    spacetimedb/src/tables/matchResultParticipant.ts (source of truth)
    spacetimedb/src/tables/matchResultGame.ts (source of truth)
    spacetimedb/src/tables/bracketMatch.ts (source of truth)
    spacetimedb/src/tables/groupStanding.ts (source of truth)
  </read_first>
  <action>
## Part A: Column-name updates (find-replace)

**docs/match-results/architecture.md — Column renames in table reference:**
- MatchResultRecord table: Remove player1Id, player2Id, team1Confirmed, team2Confirmed from column list. Rename winnerId -> winnerUserId, isTournamentMatch -> isTournamentControlled, matchType description from "u8 (0=casual, 1=ranked, 2=tournament)" to "MatchType enum (Casual, Ranked, Tournament)". Remove player_1_id and player_2_id indexes.
- MatchResultParticipant table: Add isCaptain (bool) and isConfirmed (bool) to column list. Update description to note captain-based confirmation pattern.
- MatchResultGame table: Rename all player1*/player2* columns to teamBlue*/teamRed*. Change winnerId to winnerTeamSide (TeamLabel enum). Rename validatedById to validatedByUserId. Add column table if not present.

**docs/match-results/contract.md — Column name references:**
- Replace all "player1"/"player2" participant references with "MatchResultParticipant-based lookup" language
- Replace "winnerId" with "winnerUserId" in MatchResultRecord context

**docs/brackets/architecture.md — Column name updates:**
- Replace ALL instances of `participant1Id` with `team1Id`
- Replace ALL instances of `participant2Id` with `team2Id`
- Replace ALL instances of `winnerId` (in BracketMatch context) with `winnerTeamId`
- Replace ALL instances of `participantTeamId` with `teamId` (GroupStanding context)
- Update the BracketMatch table reference with new column names
- Update the GroupStanding table reference with new column names
- Update "MatchResultRecord.winnerId is a userId" to "MatchResultRecord.winnerUserId"

**docs/brackets/contract.md — Column name updates:**
- Replace ALL instances of `participant1Id` with `team1Id`
- Replace ALL instances of `participant2Id` with `team2Id`
- Replace ALL instances of `winnerId` (bracket context) with `winnerTeamId`
- Replace `participantTeamId` with `teamId`
- Update winnerId references on MatchResultRecord to winnerUserId

**docs/tournament/architecture.md:**
- Update submit_and_advance_bracket description to use winnerUserId -> winnerTeamId mapping language
- Replace any participant1Id/participant2Id references with team1Id/team2Id

**docs/tournament/contract.md:**
- Replace participantTeamId with teamId if referenced

## Part B: Behavioral narrative additions (NEW SECTIONS)

**docs/match-results/architecture.md — Add NEW section "Match Result Lifecycle" (after Status Lifecycle):**

Write a new section that explains the ephemeral nature of MatchResultRecord:
```
## Match Result Lifecycle

MatchResultRecord is operational and ephemeral — it exists only while the match result
is being confirmed, submitted, and processed. After finalization, the record is DELETED
and all permanent data lives in history tables.

### Finalization Paths

**Non-MMR path (MMR disabled on tournament/lobby):**
Validated → finalize_match_result → write MatchSessionHistory + MatchParticipantHistory + update PlayerStat/PlayerCharacterStat/PlayerRelationship → delete MatchResultRecord

**Casual/Ranked MMR path:**
Validated → process MMR immediately → stamp mmrProcessedAt → finalize_match_result → write history + stats → delete MatchResultRecord

**Tournament MMR path:**
Validated → match stays (tournament still in progress) → tournament ends or cancelled → process_tournament_mmr (batch all Validated matches) → stamp mmrProcessedAt on each → finalize_match_result per match → write history + stats → delete all MatchResultRecords

### Implementation Status
- finalize_match_result reducer: stub exists (Phase 04.1), implementation Phase 5
- process_tournament_mmr reducer: stub exists (Phase 04.1), implementation Phase 5
- mmrProcessedAt column: present on MatchResultRecord, used as finalization gate
```

**docs/match-results/architecture.md — REWRITE "Match Result Submission Flow" section:**

Replace the existing ASCII flow diagram. The new version must show captain-based confirmation via MatchResultParticipant instead of team1Confirmed/team2Confirmed:
```
## Match Result Submission Flow

  Each participant's captain fills scores per game (MatchResultGame rows)
          |
          v
  Captain A calls confirm_match_scores --> MatchResultParticipant.isConfirmed = true
          |                                  (looked up by isCaptain=true for their side)
          v
  Captain B calls confirm_match_scores --> MatchResultParticipant.isConfirmed = true
          |
          v
  ALL captains confirmed? (every MatchResultParticipant with isCaptain=true has isConfirmed=true)
          |
          v
  Referee calls submit_match_result --> status: Submitted
          |
          +---- Any participant can dispute once (dispute_match_result) --> status: Disputed
          |                                                                        |
          v                                                                        v
  status: Validated (TO/Mod validates)                             TO/Mod: override_match_result
          |                                                           (Validated or Rejected)
          v
  Finalization path triggered [Phase 5 scope — see Match Result Lifecycle]
  Bracket advancement triggered [Phase 4 scope]
```

Also update Key Rule #1 from "team1Confirmed AND team2Confirmed must both be true" to "All MatchResultParticipant rows with isCaptain=true must have isConfirmed=true".

**docs/match-results/architecture.md — Add NEW section "Table Relationships" (after Table Reference):**

Write a hub-and-spoke diagram showing how the three match result tables relate:
```
## Table Relationships

MatchResultRecord is the hub. MatchResultParticipant and MatchResultGame are siblings
that both FK to MatchResultRecord — they are NOT parent-child of each other.

  MatchResultParticipant (who played, which side, captain flag, confirmation)
          |
          |  FK: matchResultId → MatchResultRecord.id
          |
  MatchResultRecord (lifecycle status, bracket link, dispute, MMR guard, ephemeral)
          |
          |  FK: matchResultId → MatchResultRecord.id
          |
  MatchResultGame (per-game scores by team side, screenshots, validation)

Participants track WHO is in the match and their confirmation status.
Games track WHAT happened in each game of the series.
The Record tracks the overall lifecycle and administrative state.

All three are deleted together when finalize_match_result runs.
```

**docs/match-results/architecture.md — Add to "Match Types" section or new section "isTournamentControlled":**

Add explanation of the behavioral distinction:
```
### isTournamentControlled vs matchType

These two fields serve different purposes and both remain on MatchResultRecord:

- **matchType** (MatchType enum: Casual, Ranked, Tournament) — Client-facing filter category.
  Used for match history filtering, stat breakdowns, and UI display.

- **isTournamentControlled** (bool) — Behavioral flag that changes how the match operates:
  - When true: lobby inherits tournament configuration (game mode, draft mode, cost set, disconnect policy)
  - When true: TO and assistants (canValidateResults) gain override/submission authority
  - When true: settings are locked — players cannot change inherited config

A match can have matchType=Tournament but isTournamentControlled=false (e.g., a custom match
labeled "tournament" for organizational purposes without actual tournament integration).
```

**docs/match-results/contract.md — REWRITE "Score Confirmation" scenario:**
```
### Score Confirmation
**Given:** MatchResultRecord in Pending status, caller is a captain (MatchResultParticipant.isCaptain=true)
**When:** `confirm_match_scores(matchResultId)`
**Then:** Caller's MatchResultParticipant.isConfirmed set to true. In 1v1, both participants have isCaptain=true. In team formats, only the designated team captain confirms on behalf of their side.
```

**docs/match-results/contract.md — ADD new scenario "Match Finalization":**
```
### Match Finalization (Phase 5)
**Given:** MatchResultRecord in Validated status, MMR processed (or MMR disabled)
**When:** `finalize_match_result(matchResultId)` is called
**Then:** MatchSessionHistory row written with match outcome. MatchParticipantHistory rows written for each participant. PlayerStat and PlayerCharacterStat incremented. PlayerRelationship updated for ally/opponent pairs. MatchResultRecord, MatchResultParticipant, and MatchResultGame rows all deleted.

**Note:** This reducer is a stub in Phase 04.1. Full implementation in Phase 5. Documenting the contract here so Phase 5 has a behavioral spec to implement against.
```

**docs/match-results/contract.md — ADD edge case:**
Add to the Edge Cases table:
```
| Confirm by non-captain in team match | Throws "Only the team captain can confirm scores" |
```

**docs/match-results/contract.md — UPDATE Phase History table:**
Add these entries:
```
| MatchResultRecord becomes ephemeral (deleted after finalization) | Phase 04.1 execution | 2026-03-20 |
| Captain-based confirmation replaces team1Confirmed/team2Confirmed | Phase 04.1 execution | 2026-03-20 |
| isTournamentControlled replaces isTournamentMatch (behavioral flag) | Phase 04.1 execution | 2026-03-20 |
| Match Finalization scenario documented (Phase 5 contract) | Phase 04.1 execution | 2026-03-20 |
| winnerTeamSide (TeamLabel) replaces winnerId on MatchResultGame | Phase 04.1 execution | 2026-03-20 |
| teamBlue*/teamRed* replaces player1*/player2* on MatchResultGame | Phase 04.1 execution | 2026-03-20 |
```

**docs/brackets/contract.md — UPDATE rollback MMR guard behavior:**

Find the "Rollback Bracket Match" scenario and the edge case "rollback after MMR processed". Update the behavioral description:
- Current: Implies rollback is always blocked if MMR processed.
- New: During an active tournament, rollback is FREE because MMR has not been processed yet (tournament MMR is batched at tournament end). The mmrProcessedAt guard only blocks rollback AFTER the tournament ends and the MMR batch (process_tournament_mmr) has run. For casual/ranked matches, the guard applies immediately since MMR is processed per-match.

Update the edge case row:
```
| rollback after MMR processed | Throws "Cannot rollback: MMR has already been processed for this match." (Only applies after tournament ends + batch MMR runs, or immediately for casual/ranked) |
```

Add Phase History entry:
```
| Rollback during tournament is free — MMR not yet processed (batched at tournament end) | Phase 04.1 execution | 2026-03-20 |
```

**docs/brackets/contract.md — Tag all column renames with Phase 04.1 execution in Phase History:**
```
| participant1Id/participant2Id → team1Id/team2Id, winnerId → winnerTeamId | Phase 04.1 execution | 2026-03-20 |
| participantTeamId → teamId (GroupStanding) | Phase 04.1 execution | 2026-03-20 |
```

**docs/tournament/contract.md — Tag changes:**
```
| participantTeamId → teamId in GroupStanding references | Phase 04.1 execution | 2026-03-20 |
```
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && echo "=== OLD NAMES CHECK ===" && grep -rn "player1Id\|player2Id\|team1Confirmed\|team2Confirmed\|participant1Id\|participant2Id\|participantTeamId" docs/match-results/ docs/brackets/ docs/tournament/ 2>/dev/null | head -20 && echo "=== NEW NAMES CHECK ===" && grep -c "winnerUserId\|isTournamentControlled\|team1Id\|team2Id\|winnerTeamId\|teamId" docs/match-results/architecture.md docs/brackets/architecture.md && echo "=== BEHAVIORAL SECTIONS CHECK ===" && grep -c "Match Result Lifecycle" docs/match-results/architecture.md && grep -c "Finalization Paths" docs/match-results/architecture.md && grep -c "Table Relationships" docs/match-results/architecture.md && grep -c "isTournamentControlled vs matchType" docs/match-results/architecture.md && grep -c "isCaptain" docs/match-results/architecture.md && grep -c "Match Finalization" docs/match-results/contract.md && grep -c "non-captain" docs/match-results/contract.md && grep -c "Phase 04.1 execution" docs/match-results/contract.md docs/brackets/contract.md && echo "=== ROLLBACK GUARD CHECK ===" && grep -c "batched at tournament end\|batch MMR\|free.*tournament" docs/brackets/contract.md && echo "VERIFY_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - 0 occurrences of "player1Id", "player2Id", "team1Confirmed", "team2Confirmed" in docs/match-results/
    - 0 occurrences of "participant1Id", "participant2Id", "participantTeamId" in docs/brackets/
    - 0 occurrences of "participant1Id", "participant2Id" in docs/tournament/
    - docs/match-results/architecture.md contains "winnerUserId", "isTournamentControlled", "MatchType", "isCaptain", "isConfirmed"
    - docs/brackets/architecture.md contains "team1Id", "team2Id", "winnerTeamId", "teamId"
    - docs/match-results/architecture.md contains NEW section "Match Result Lifecycle" with Finalization Paths subsection
    - docs/match-results/architecture.md contains NEW section "Table Relationships" with hub-spoke diagram
    - docs/match-results/architecture.md contains "isTournamentControlled vs matchType" explanation
    - docs/match-results/architecture.md submission flow diagram uses captain/MatchResultParticipant language (not team1Confirmed)
    - docs/match-results/contract.md Score Confirmation scenario uses captain-based confirmation
    - docs/match-results/contract.md contains "Match Finalization" scenario (Phase 5 contract)
    - docs/match-results/contract.md edge cases include "non-captain" rejection
    - docs/match-results/contract.md Phase History has >= 4 entries tagged "Phase 04.1 execution"
    - docs/brackets/contract.md rollback scenario explains tournament-free rollback (MMR batched at end)
    - docs/brackets/contract.md Phase History has >= 2 entries tagged "Phase 04.1 execution"
  </acceptance_criteria>
  <done>Match results, brackets, and tournament docs updated with all new column names AND behavioral narratives for ephemeral lifecycle, captain confirmation, table relationships, isTournamentControlled semantics, match finalization contract, and tournament rollback guard</done>
</task>

<task type="auto">
  <name>Task 2: Update MMR, player-stats, match-session docs, skill file, and codebase map (names + behavioral narratives)</name>
  <files>
    docs/mmr/architecture.md
    docs/player-stats/architecture.md
    docs/match-session/architecture.md
    .claude/skills/spacetimedb/SKILL.md
    .planning/codebase/ARCH.md
  </files>
  <read_first>
    docs/mmr/architecture.md
    docs/player-stats/architecture.md
    docs/match-session/architecture.md
    .claude/skills/spacetimedb/SKILL.md (search for "participantTeamId" around line 337)
    .planning/codebase/ARCH.md (search for "PlayerStats" around line 45)
    spacetimedb/src/tables/mmrHistory.ts (source of truth)
    spacetimedb/src/tables/playerStats.ts (source of truth)
    spacetimedb/src/tables/characterStats.ts (source of truth)
    spacetimedb/src/tables/matchSessionHistory.ts (source of truth)
    spacetimedb/src/tables/matchSessionStepHistory.ts (source of truth)
    spacetimedb/src/tables/playerRelationship.ts (new table)
    spacetimedb/src/tables/matchParticipantHistory.ts (new table)
  </read_first>
  <action>
## Part A: Column-name updates (find-replace)

**docs/mmr/architecture.md:**
- Rename `matchResultId` to `matchHistoryId` in MmrHistory table reference
- Update FK description: "FK to MatchSessionHistory.id" (not MatchResultRecord.id)
- Update index: `match_result_id` -> `match_history_id`
- Update any flow references that describe MmrHistory linking to MatchResultRecord

**docs/player-stats/architecture.md — Full rewrite for 3 tables:**
- PlayerStats -> PlayerStat:
  - Table name: 'player_stat' (was 'player_stats')
  - PK: [userId, gameMode, draftMode] (was single userId)
  - New columns: gameMode (GameMode enum), draftMode (DraftMode enum)
  - Removed columns: bestAllyUserId, nemesisUserId (moved to PlayerRelationship)
  - Kept: matchesPlayed, wins, losses, draws, matchesSpectated
  - Indexes: by_user [userId], by_user_mode_draft [userId, gameMode, draftMode]

- CharacterStats -> PlayerCharacterStat:
  - Table name: 'player_character_stat' (was 'character_stats')
  - PK: [userId, characterName, gameMode, draftMode] (expanded from [userId, characterName])
  - New columns: gameMode (GameMode enum), draftMode (DraftMode enum)
  - Indexes: by_user [userId], by_user_and_character_mode_draft [userId, characterName, gameMode, draftMode]

- NEW: PlayerRelationship table reference:
  - Table name: 'player_relationship'
  - PK: [userId, otherUserId, gameMode, draftMode]
  - Columns: userId, otherUserId, gameMode, draftMode, matchesAsAlly, winsAsAlly, matchesAsOpponent, winsAsOpponent, audit cols
  - Purpose: Replaces bestAllyUserId/nemesisUserId. Client derives best ally (max winsAsAlly) and nemesis (max winsAsOpponent) from subscription.
  - Indexes: by_user [userId], by_user_and_other_mode_draft [userId, otherUserId, gameMode, draftMode]

**docs/match-session/architecture.md:**
- MatchSessionHistory:
  - id type: u32 autoInc (was string UUID)
  - Remove blueTeamMembers and redTeamMembers columns from table reference
  - Remove PlayerSnapshot struct reference
  - Rename result -> outcome, MatchResult -> MatchOutcome
  - Add note: "Player membership now tracked via MatchParticipantHistory junction table"

- MatchSessionStepHistory:
  - matchId -> matchHistoryId, type string -> u32
  - Update FK description: "FK to MatchSessionHistory.id (u32)"

- NEW: MatchParticipantHistory table reference:
  - Table name: 'match_participant_history'
  - PK: [userId, matchHistoryId]
  - Columns: userId, matchHistoryId (FK to MatchSessionHistory.id), teamSide (TeamLabel), audit cols
  - Purpose: Indexed "show me all matches user X played" query. Replaces non-indexable PlayerSnapshot arrays.
  - Indexes: by_user [userId], by_match_history [matchHistoryId], by_user_and_match [userId, matchHistoryId]

**SKILL.md:**
- Find the line with `participantTeamId` (around line 337) in the column mapping table
- Change `participantTeamId` to `teamId` and `participant_team_id` to `team_id`

**ARCH.md:**
- Line 45 area: Replace `PlayerStats, CharacterStats` with `PlayerStat, PlayerCharacterStat`
- Add `PlayerRelationship` and `MatchParticipantHistory` to the table counts in the appropriate sections
  - Match Results section: add MatchResultParticipant if not listed (it was added in Phase 4)
  - Player Stats section: `PlayerStat, PlayerCharacterStat, PlayerRelationship` (3 tables)
  - History section: add `MatchParticipantHistory`

## Part B: Behavioral narrative additions (NEW SECTIONS)

**docs/mmr/architecture.md — ADD NEW section "Tournament vs Casual MMR Timing" (after Flow):**

Write a new section explaining the timing difference:
```
## Tournament vs Casual MMR Timing

MMR processing timing differs based on match context:

### Casual and Ranked Matches
MMR is processed immediately when the match reaches Validated status.
The existing Flow section above describes this path. Once mmrProcessedAt is stamped,
finalize_match_result can safely delete the MatchResultRecord.

### Tournament Matches
MMR is NOT processed per-match. Tournament matches stay in Validated status with
mmrProcessedAt=null until the tournament ends (Completed or Cancelled stage).

When the tournament ends:
1. `process_tournament_mmr(tournamentId)` is called
2. Reducer iterates all MatchResultRecords with tournamentId and status=Validated and mmrProcessedAt=null
3. For each match: calculate ELO deltas, update MmrRating rows, insert MmrHistory rows
4. Stamp mmrProcessedAt on each processed MatchResultRecord
5. After batch completes, finalize_match_result can run on each record

This batching avoids volatile mid-tournament MMR swings from affecting seeding or
perceived fairness during the event.

### mmrProcessedAt as Finalization Gate
mmrProcessedAt serves as a two-purpose guard:
- **Before set:** MMR has not been applied — safe to rollback bracket matches (no MMR to reverse)
- **After set:** MMR has been applied — rollback requires MMR reversal (blocked in current implementation)
- **For finalization:** finalize_match_result checks mmrProcessedAt is set (or MMR disabled) before deleting

### Implementation Status
- process_tournament_mmr reducer: stub exists (Phase 04.1), implementation Phase 5
- finalize_match_result reducer: stub exists (Phase 04.1), implementation Phase 5
```

**docs/mmr/architecture.md — UPDATE MmrHistory FK in Tables section:**
The `matchResultId` line must also update its description text to explain it now points to MatchSessionHistory (permanent record) not MatchResultRecord (ephemeral):
```
matchHistoryId → MatchSessionHistory.id (permanent record, not ephemeral MatchResultRecord)
```

**docs/match-session/architecture.md — ADD NEW section "Match Participant History" (after the tables block):**

Write a new section explaining the junction table pattern:
```
## Match Participant History

MatchParticipantHistory is a junction table that replaces the non-indexable PlayerSnapshot
arrays (blueTeamMembers/redTeamMembers) that were previously embedded in MatchSessionHistory.

### Purpose
The primary query it serves is "show me all matches user X played" — a profile page query
that needs to be indexed by userId. With embedded arrays, this query required scanning every
MatchSessionHistory row. With the junction table, it's a direct index lookup.

### Archive Flow (Phase 5)
When a match is archived (MatchSession → MatchSessionHistory):
1. Write MatchSessionHistory row (match outcome, config snapshot, timestamps)
2. Write MatchParticipantHistory rows — one per participant, capturing userId, matchHistoryId, teamSide
3. Write MatchSessionStepHistory rows (draft replay data)
4. Clean up active session rows

### Query Pattern
To load a player's match history:
1. Subscribe to MatchParticipantHistory filtered by userId → get list of matchHistoryIds
2. Filter MatchSessionHistory by those matchHistoryIds → get match metadata
3. On demand: load MatchSessionStepHistory for a specific matchHistoryId → replay data

This is a fan-out pattern: one subscription gives you the IDs, then you load details as needed.
```

**docs/player-stats/architecture.md — ADD NEW sections after the table diagram rewrite:**

After rewriting the table diagram with the 3 new tables (Part A), add these behavioral sections:

```
## Composite PK Design

All three stat tables use composite primary keys that include gameMode and draftMode:
- PlayerStat: [userId, gameMode, draftMode] — one row per user per mode combination
- PlayerCharacterStat: [userId, characterName, gameMode, draftMode] — one row per character per mode
- PlayerRelationship: [userId, otherUserId, gameMode, draftMode] — one row per opponent pair per mode

This means a player who plays MoC/AllRandom and AS/CaptainDraft has SEPARATE stat rows for each.
The client can show per-mode breakdowns or aggregate across modes by summing.

## Incremental Aggregation Pattern

Stats are updated incrementally, not recomputed from scratch:

### On finalize_match_result:
- PlayerStat: increment matchesPlayed, wins OR losses OR draws for each participant
- PlayerCharacterStat: increment matchesPlayed, wins/losses for each character used
- PlayerRelationship: for each pair of participants — increment matchesAsAlly/winsAsAlly
  (if same team) or matchesAsOpponent/winsAsOpponent (if opposing team)

### On rollback (if implemented):
- Subtract the same increments. This is why individual match deltas are tracked —
  you need to know exactly what was added to reverse it.

### Derived Values (client-side)
- **Best Ally:** Sort PlayerRelationship by winsAsAlly/matchesAsAlly ratio (where matchesAsAlly >= threshold)
- **Nemesis:** Sort by winsAsOpponent (from OTHER player's perspective — the opponent who beat you most)
- **Win Rate:** wins / matchesPlayed (can be computed client-side or stored as f32)

These are NOT stored in the database. The client computes them from the raw counters.
This avoids recomputing relationship rankings on every match — the client just sorts.

## What Triggers Updates

Only `finalize_match_result` updates stat tables. Stats are NEVER updated by:
- submit_match_result (only changes status)
- confirm_match_scores (only sets confirmation flag)
- override_match_result (only changes status/winner)

This ensures stats always reflect finalized, verified match outcomes.
```
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && echo "=== MMR COLUMN CHECK ===" && grep -c "matchHistoryId" docs/mmr/architecture.md && grep -c "matchResultId" docs/mmr/architecture.md && echo "=== MMR BEHAVIORAL CHECK ===" && grep -c "Tournament vs Casual" docs/mmr/architecture.md && grep -c "process_tournament_mmr" docs/mmr/architecture.md && grep -c "mmrProcessedAt.*gate\|Finalization Gate" docs/mmr/architecture.md && echo "=== PLAYER STATS CHECK ===" && grep -c "PlayerStat" docs/player-stats/architecture.md && grep -c "PlayerRelationship" docs/player-stats/architecture.md && grep -c "Composite PK\|composite.*primary" docs/player-stats/architecture.md && grep -c "Incremental Aggregation" docs/player-stats/architecture.md && grep -c "finalize_match_result" docs/player-stats/architecture.md && echo "=== MATCH SESSION CHECK ===" && grep -c "MatchParticipantHistory" docs/match-session/architecture.md && grep -c "MatchOutcome" docs/match-session/architecture.md && grep -c "matchHistoryId" docs/match-session/architecture.md && grep -c "Archive Flow\|archive flow" docs/match-session/architecture.md && grep -c "Query Pattern\|query pattern" docs/match-session/architecture.md && echo "=== SKILL/ARCH CHECK ===" && grep "participantTeamId" .claude/skills/spacetimedb/SKILL.md | wc -l && grep "PlayerStats" .planning/codebase/ARCH.md | wc -l && echo "VERIFY_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - docs/mmr/architecture.md: contains "matchHistoryId", 0 occurrences of "matchResultId" as a column name (may appear in text describing the old FK)
    - docs/mmr/architecture.md: contains "Tournament vs Casual MMR Timing" section
    - docs/mmr/architecture.md: contains "process_tournament_mmr" description with batch flow
    - docs/mmr/architecture.md: contains "mmrProcessedAt as Finalization Gate" or equivalent section
    - docs/player-stats/architecture.md: contains "PlayerStat" (not "PlayerStats" as table name), contains "PlayerCharacterStat", contains "PlayerRelationship" with full table reference
    - docs/player-stats/architecture.md: contains "Composite PK" section explaining per-mode stat rows
    - docs/player-stats/architecture.md: contains "Incremental Aggregation" section with finalize/rollback pattern
    - docs/player-stats/architecture.md: contains "What Triggers Updates" section naming finalize_match_result as sole trigger
    - docs/match-session/architecture.md: contains "MatchParticipantHistory" table reference AND behavioral section
    - docs/match-session/architecture.md: contains "MatchOutcome" (not "MatchResult" as enum), 0 occurrences of "PlayerSnapshot", contains "matchHistoryId"
    - docs/match-session/architecture.md: contains archive flow description (write history + write participants + write steps + cleanup)
    - docs/match-session/architecture.md: contains query pattern (subscribe by userId → get matchHistoryIds → filter → load on demand)
    - .claude/skills/spacetimedb/SKILL.md: 0 occurrences of "participantTeamId" (replaced with "teamId")
    - .planning/codebase/ARCH.md: 0 occurrences of "PlayerStats" as a table name (replaced with "PlayerStat"), contains "PlayerCharacterStat"
  </acceptance_criteria>
  <done>All remaining docs, skill file, and codebase map updated with new schema names AND behavioral narratives for tournament MMR timing, mmrProcessedAt gate semantics, MatchParticipantHistory junction pattern, composite PK stats, incremental aggregation, and finalization triggers</done>
</task>

</tasks>

<verification>
After both tasks, run a comprehensive check:
```bash
# No old names in any doc
grep -rn "player1Id\|player2Id\|team1Confirmed\|team2Confirmed\|participant1Id\|participant2Id\|participantTeamId" docs/ .claude/skills/spacetimedb/SKILL.md .planning/codebase/ARCH.md
# Should return 0 results

# Behavioral sections exist
grep -l "Match Result Lifecycle" docs/match-results/architecture.md
grep -l "Table Relationships" docs/match-results/architecture.md
grep -l "isTournamentControlled vs matchType" docs/match-results/architecture.md
grep -l "Match Finalization" docs/match-results/contract.md
grep -l "Tournament vs Casual" docs/mmr/architecture.md
grep -l "Composite PK" docs/player-stats/architecture.md
grep -l "Incremental Aggregation" docs/player-stats/architecture.md
grep -l "Match Participant History" docs/match-session/architecture.md
# All should return matches
```
</verification>

<success_criteria>
- Zero references to old column names (player1Id, player2Id, team1Confirmed, team2Confirmed, participant1Id, participant2Id, participantTeamId) in any doc file
- All table references in docs match the actual table definitions in spacetimedb/src/tables/
- New tables (PlayerRelationship, MatchParticipantHistory) documented with behavioral context
- Renamed tables (PlayerStat, PlayerCharacterStat) documented with new names and composite PK explanation
- Match result lifecycle (ephemeral nature, 3 finalization paths) documented
- Captain-based confirmation flow documented (architecture + contract)
- Table relationships hub diagram documented
- isTournamentControlled behavioral semantics documented
- Match Finalization contract scenario documented (Phase 5 forward reference)
- Tournament vs casual MMR timing documented
- mmrProcessedAt as finalization gate documented
- MatchParticipantHistory junction pattern + query pattern documented
- Incremental aggregation pattern documented
- Bracket rollback MMR guard nuance (free during tournament) documented
- Skill file and codebase map updated
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-schema-normalization-match-result-rework/04.1-03-SUMMARY.md`
</output>
