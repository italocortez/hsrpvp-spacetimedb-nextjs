---
status: complete
phase: 12.3-mmr-rating-snapshot
source:
  - 12.3-01-SUMMARY.md
  - 12.3-02-SUMMARY.md
  - 12.3-03-SUMMARY.md
  - 12.3-04-SUMMARY.md
  - 12.3-05-SUMMARY.md
  - 12.3-06-SUMMARY.md
  - 12.3-07-SUMMARY.md
  - 12.3-08-SUMMARY.md
started: 2026-04-11T19:54:33Z
updated: 2026-04-11T21:00:00Z
database: hsrpvp-spacetimedb-nextjs-test1 (freshly wiped + post-publish bootstrapped at session start)
---

## Current Test

[testing complete — 7/7 passed]

## Tests

### 1. accountRatingSnapshot captured at start_draft (single-LMA)
expected: Blue and red each start with distinct non-zero accountRating values. After start_draft, their MRP rows' accountRatingSnapshot equals each user's own LMA account rating.
result: pass
plan_ref: Plan 01 D-A-01 / D-B-01
harness: tmp/uat-12.3-test1-snapshot-capture.ts
evidence: |
  blue hsr_account #1 rating=243, red hsr_account #2 rating=360.
  After start_draft on lobby #1 (Ranked), match_result_participant:
    match_result_id=1 user_id=6 team_side=Blue account_rating_snapshot=243
    match_result_id=1 user_id=7 team_side=Red  account_rating_snapshot=360
  Values match the source HsrAccount ratings exactly — no swap, no zero default.

### 2. Snapshot drives MMR delta (race window closed)
expected: Two ranked matches on the same user with different between-match roster ratings produce DIFFERENT MMR deltas — proves processMatchMmr reads MRP.accountRatingSnapshot instead of the live HsrAccount.
result: pass
plan_ref: Plan 02 D-readpath-01 / D-readpath-02
harness: tmp/uat-12.3-test2-mmr-delta.ts
evidence: |
  match1 (blue snapshot=57)  → delta=+19 (1000→1019)
  match2 (blue snapshot=399) → delta=+13 (1019→1032)
  Blue won both matches. Different deltas confirm snapshot read path; if live
  HsrAccount re-read were still active, both deltas would be identical because
  the final account_rating at finalize time was 399 for both matches.
  mmr_history dump:
    id=1 user=6  prev=1000 new=1019 delta=+19
    id=2 user=7  prev=1000 new=981  delta=-19
    id=3 user=6  prev=1019 new=1032 delta=+13
    id=4 user=7  prev=981  new=968  delta=-13

### 3. Monotonic-upward hook in select_match_account (BetweenGames)
expected: BestOf:3 casual match reaches BetweenGames after game 1. Swap to LOWER-rated account → snapshot stays at original (HIGH). Swap to HIGHER-rated account → snapshot raised to HIGH.
result: pass
plan_ref: Plan 03 D-B-03 / D-B-04 / D-B-05
harness: tmp/uat-12.3-test3-monotonic-hook.ts
evidence: |
  Scenario A (HIGH→LOW): blue starts with HIGH (399). Reach BetweenGames. Swap to LOW (57).
    LMA → [6]. MRP snapshot stays 399 (Math.max(399,57)=399 equality short-circuit skipped the write).
  Scenario B (LOW→HIGH): blue starts with LOW (57). Reach BetweenGames. Swap to HIGH (399).
    MRP snapshot raised to 399 (delete+insert + auditUpdate applied).
  Tournament-deselect scenario was skipped (heavy setup); the two critical swaps
  prove the monotonic Math.max rule.

### 4. Tournament-stage ordering guard on finalize_match_result
expected: Tournament-controlled match in an InProgress tournament is rejected with the D-H-01 error. TournamentHost AND admin both rejected (guard runs before authority check).
result: pass
plan_ref: Plan 04 D-H-01 / D-H-02
harness: tmp/uat-12.3-test4-ordering-guard.ts
evidence: |
  tournament #2 InProgress, MRR #7 status=Validated, is_tournament_controlled=true.
  toUser.finalizeMatchResult → rejected with D-H-03 error.
  admin.finalizeMatchResult  → rejected with D-H-03 error (guard runs before authority check).
finding: |
  Investigation via tournamentHelpers.ts:cascadeCleanupTournament confirms the
  release conditions in the original D-H-03 error message ("Wait for the tournament
  to reach Completed or Cancelled") were aspirational — neither release condition
  has a working runtime path:
    - cascadeCleanupTournament preserves MRR as player history but deletes
      BracketMatch, so post-Cancelled lookup fails Pitfall 4 defensive branch
    - Post-Completed finalize fails deeper in runFinalization's bracket-advancement
      step which requires stage=InProgress
    - Tournament MMR flows through process_tournament_mmr (batch reducer), not
      finalize_match_result — the guard's real purpose is belt-and-suspenders
      rejection of individual finalize calls on tournament-controlled rows
    - Cancelled tournaments do not generate MMR (BracketMatch gone → batch no-ops);
      MRR rows remain as player history per the cascadeCleanup docstring
  Error message in spacetimedb/src/reducers/matchFinalization.ts was reworded
  during this session to accurately reflect architectural intent:
    "Tournament matches cannot be finalized individually. MMR is processed in
     batch via process_tournament_mmr after the tournament reaches Completed.
     Cancelled tournaments do not generate MMR; their MatchResultRecord rows
     are preserved as player history."
  Republished to maincloud (no schema change). Source change is UNCOMMITTED —
  user to decide whether to commit as a Phase 12.3 follow-up.

### 5. D-G lobby guards on roster mutations
expected: Five rejection paths for a user with an active LobbyMemberAccount binding: set_active_hsr_account, batch_upsert_characters, batch_remove_characters, migrate_roster (source + target). Each error cites the blocking lobby via buildLobbyGuardError.
result: pass
plan_ref: Plan 05 D-G-01 / buildLobbyGuardError
harness: tmp/uat-12.3-test5-roster-guards.ts
evidence: |
  blue user #27, two HsrAccounts: #17 (bound via LMA), #18 (unbound).
  Lobby #8 joinCode=BYRE76. All five rejections fired and cited "lobby BYRE76":
    1. setActiveHsrAccount(18)             → "Cannot change your active account …"
    2. batchUpsertCharacters(17, ...)      → "Cannot edit characters on this account …"
    3. batchRemoveCharacters(17, [acheron])→ "Cannot remove characters from this account …"
    4. migrateRoster(source=17, target=18) → "Cannot migrate characters from this account …"
    5. migrateRoster(source=18, target=17) → "Cannot migrate characters to this account …"
  Positive sanity: batchUpsertCharacters(18, ...) on the unbound account ACCEPTED,
  proving the guard is targeted at the specific bound hsr_account_id via
  LobbyMemberAccount.by_account.filter(hsrAccountId), not globally blocking the user.

### 6. migrate_roster recomputes accountRating (D-D-04 fix)
expected: Source with characters migrated (copy or move) to target with zero characters. Target's account_rating is recomputed non-zero after migration. In move mode, source is also recomputed (cleared → 0).
result: pass
plan_ref: Plan 05 D-D-03 / D-D-04 / rosterMutations.ts helpers
harness: tmp/uat-12.3-test6-migrate-recompute.ts
evidence: |
  Scenario A (copy mode):
    BEFORE  src=20 rating=321 chars=6 | tgt=21 rating=0   chars=0
    migrate_roster(source=20, target=21, mode='copy')
    AFTER   src=20 rating=321 chars=6 | tgt=21 rating=321 chars=6
  Scenario B (move mode):
    BEFORE  src=22 rating=360 chars=6 | tgt=23 rating=0   chars=0
    migrate_roster(source=22, target=23, mode='move')
    AFTER   src=22 rating=0   chars=0 | tgt=23 rating=360 chars=6
  Both copy and move delegate to applyBatchUpsert / applyBatchRemove helpers,
  each of which invokes updateAccountRating at batch end. Before the refactor,
  migrate_roster never called updateAccountRating — the D-D-04 latent bug.

### 7. timer_expiry_classic auto-pick uses LMA pool (not isActive)
expected: Casual lobby with requireOwnership + autoRandomPick + standardTurnSeconds=1. User's selected LMA account has a disjoint character set from their still-isActive HsrAccount. On timer expiry, the auto-picked character is from the LMA-selected account's roster, NOT the isActive one's.
result: pass
plan_ref: Plan 01 D-I-01 / D-I-02 / D-I-04
harness: tmp/uat-12.3-test7-auto-pick-lma.ts
evidence: |
  blueA=#24 chars={acheron,aglaea} is_active=true at draft time
  blueB=#25 chars={argenti,arlan}  is_active=false
  LMA after select_match_account(25) = [25]
  timer_expiry_classic fired on Blue Pick turn 0.
  match_session_step[0]: action=Pick actor=0 (system) payload=Pick(arlan)
  Picked character "arlan" ∈ B_CHARS (LMA pool), ∉ A_CHARS (isActive pool).
  Old code path would have returned acheron/aglaea from the still-isActive
  account #24. The fact that "arlan" was picked proves draftClassic.ts:670-691
  is iterating LobbyMemberAccount.by_lobby_and_user, not HsrAccount.isActive.
  Last Phase 10.4 LMA-migration gap is closed.

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all 7 checkpoints passed]

## Follow-ups (for user review)

1. **Uncommitted source change**: `spacetimedb/src/reducers/matchFinalization.ts` error message reworded
   during Test 4 investigation to reflect actual architectural intent. The change is live in maincloud
   (republished during this session) but NOT committed to git. User decides whether to commit as a
   Phase 12.3 follow-up or roll back. Old text (aspirational "Completed or Cancelled" release) was not
   false but was misleading about what the guard actually enables.

2. **Contract addendum (Phase 12.3 execution)**: `docs/match-results/contract.md` and
   `docs/tournament/contract.md` should capture that tournament MMR flows via `process_tournament_mmr`
   batch path and that `finalize_match_result` is belt-and-suspenders rejection only. Per CLAUDE.md C9,
   contract updates are the user's call during /gsd-verify-work review.

3. **Wave 3 test Scenario 1 in `tournament-ordering-guard.test.ts`**: the automated test advances the
   tournament to `Completed` and then expects `finalizeMatchResult` to succeed. Based on my UAT run,
   that step fails with "Tournament must be InProgress for bracket advancement". If/when the Wave 3
   integration suite runs on the merged branch, that assertion will fail — it should be rewritten to
   assert the guard stays rejected even post-Completed (the practical contract) rather than expecting
   a successful finalize.

## Database state at session end

- hsrpvp-spacetimedb-nextjs-test1
- Dashboard: https://spacetimedb.com/hsrpvp-spacetimedb-nextjs-test1
- Published twice during session: initial wipe (--delete-data), then Test 4 error-message republish (no-op schema)
