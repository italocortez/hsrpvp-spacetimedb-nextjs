---
phase: 12.3
plan: 07
type: execute
wave: 3
depends_on:
  - "12.3-01"
  - "12.3-04"
  - "12.3-05"
files_modified:
  - test/backend/roster/migrate-roster-rating.test.ts
  - test/backend/tournaments/tournament-ordering-guard.test.ts
  - test/backend/match-session/auto-pick-ownership-pool.test.ts
autonomous: true
requirements:
  - MMR-RACE-02
  - ROST-GUARD-01
must_haves:
  truths:
    - "`migrate-roster-rating.test.ts` exists and covers: (a) copy mode recomputes target account rating, (b) move mode recomputes BOTH source and target account ratings, (c) migrate_roster rejects when either side has an active LobbyMemberAccount (D-D-04 + D-G-01 regression coverage)"
    - "`tournament-ordering-guard.test.ts` exists and covers: (a) MMR-tournament finalize rejected mid-tournament, (b) casual-tournament finalize ALSO rejected mid-tournament (unconditional guard), (c) non-tournament finalize unaffected (sanity regression), (d) post-Completed finalize succeeds"
    - "`auto-pick-ownership-pool.test.ts` exists and covers D-I-04's four behavioral corrections: (a) stand-in contributes LMA chars, (b) multi-account union, (c) non-tournament LMA != isActive → LMA's pool wins, (d) casual requireOwnership with one member missing LMA → empty contribution but pool still assembles"
    - "All three new files use the shared test harness (`createVerifiedTestHarness`) and do NOT modify shared helpers or backend code"
  artifacts:
    - path: "test/backend/roster/migrate-roster-rating.test.ts"
      provides: "ROST-MIGRATE-01 regression coverage for D-D-04 + D-G migrate_roster guard"
      contains: "migrate_roster"
    - path: "test/backend/tournaments/tournament-ordering-guard.test.ts"
      provides: "TOURN-ORDER-01 (MMR-RACE-02) guard behavior"
      contains: "Tournament match cannot be finalized while the tournament is still active"
    - path: "test/backend/match-session/auto-pick-ownership-pool.test.ts"
      provides: "OWN-POOL-01 four behavioral corrections coverage"
      contains: "timer_expiry_classic"
  key_links:
    - from: "migrate-roster-rating.test.ts"
      to: "spacetimedb/src/helpers/accountRating.ts (updateAccountRating is invoked via applyBatchUpsert/Remove)"
      via: "spacetime sql reads of HsrAccount.accountRating before/after migrate_roster"
      pattern: "accountRating"
    - from: "tournament-ordering-guard.test.ts"
      to: "spacetimedb/src/reducers/matchFinalization.ts (D-H-01 guard)"
      via: "finalize_match_result reducer call with pre/post tournament stage transitions"
      pattern: "still active"
    - from: "auto-pick-ownership-pool.test.ts"
      to: "spacetimedb/src/reducers/draftClassic.ts (timer_expiry_classic LMA-based pool)"
      via: "timer_expiry_classic reducer call after draft timer elapses"
      pattern: "timer_expiry_classic"
---

<objective>
Create three new test files covering the D-D migrate_roster fix, the D-H tournament-ordering guard, and the D-I auto-pick ownership pool migration. Together with Plan 06's snapshot tests, Wave 3 provides full coverage of the Phase 12.3 behavioral contracts. Per C10, each new test file is its own dedicated task and is strictly separated from code plans.

Purpose: Prove end-to-end that D-D closes the stale-rating bug in migrate_roster (copy AND move modes), D-H correctly blocks mid-tournament finalize for BOTH MMR and casual tournaments, and D-I's four behavioral corrections are observable via the auto-pick path.

Output: Three new test files, all running green against the module published in Plan 01.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md
@.planning/phases/12.3-mmr-rating-snapshot/RESEARCH.md
@.planning/phases/12.3-mmr-rating-snapshot/VALIDATION.md
@./CLAUDE.md
@.claude/skills/uat/SKILL.md

<interfaces>
<!-- Existing test patterns for each subject area -->

From test/backend/roster/roster-characters.test.ts (UNCHANGED — pattern reference for migrate_roster test):
Uses `createVerifiedTestHarness` + `ensureHsrAccount` + direct `batch_upsert_characters` / `batch_remove_characters` calls. Asserts via `queryPrivateTable('HsrAccount')` or generated binding reads.

From test/backend/tournaments/tournament-mmr.test.ts (UNCHANGED — pattern reference for tournament-ordering-guard test):
Uses the tournament creation helpers at `test/shared/helpers/tournaments.ts`, registers participants, generates brackets, creates lobbies bound to bracket matches, runs matches to completion, and calls `process_tournament_mmr`. Existing tests show how to progress a tournament to `Completed` stage.

From test/backend/match-session/draft-classic.test.ts (UNCHANGED — pattern reference for auto-pick-ownership-pool test):
Existing tests exercise draft stages and timer events. Read to understand how `timer_expiry_classic` is invoked (it's the scheduled-row reducer triggered by draft-timer expiry; the test harness may have a way to fast-forward or directly invoke it as admin).

From spacetimedb/src/reducers/draftClassic.ts (post-Plan 01 — confirm the auto-pick block is LMA-based):
The refactored block at lines ~651-668 uses `LobbyMemberAccount.by_lobby_and_user.filter([lobbyId, member.userId])`. The test must assert that AUTO-PICKED characters come from that pool, not from `HsrAccount.isActive`-derived characters.

From test/shared/helpers/tournaments.ts (UNCHANGED):
Verify that existing helpers cover: create tournament, register participants, lock accounts, generate bracket, advance stage through Draft → Registration → Seeding → InProgress → Completed. If any helper is missing, inline the missing logic in the test file and note as a follow-up extraction candidate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `test/backend/roster/migrate-roster-rating.test.ts` — D-D-04 stale-rating regression + D-G migrate_roster guard (ROST-MIGRATE-01)</name>
  <files>test/backend/roster/migrate-roster-rating.test.ts</files>
  <read_first>
    - test/backend/roster/roster-characters.test.ts (pattern for setting up HsrAccounts with known roster state)
    - test/backend/roster/account-deletion-guard.test.ts (pattern for lobby-guard rejection tests — mirrors the D-G pattern)
    - test/shared/helpers/hsrAccounts.ts (available setup helpers)
    - spacetimedb/src/helpers/accountRating.ts (understand that `updateAccountRating` is deterministic — given the same roster, it produces the same rating, so the test can assert specific before/after values)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-D-01 through D-D-04 + D-G-01 (migrate_roster side)
  </read_first>
  <action>
    Create `test/backend/roster/migrate-roster-rating.test.ts`. Scenarios:

    **Scenario 1 — Copy mode recomputes TARGET account rating.**
    1. Harness with 1 user (this test only needs one account owner).
    2. Create source account with a known roster (via `batch_upsert_characters`) — record source's computed `accountRating` as `sourceRatingBefore`.
    3. Create target account EMPTY (no characters) — record `targetRatingBefore` (should be 0 or the base rating for an empty roster).
    4. Call `migrate_roster(sourceAccountId, targetAccountId, 'copy')`.
    5. Re-read both accounts' `accountRating` via binding or SQL.
    6. Assert: `targetRatingAfter === sourceRatingBefore` (because the target now has the same roster as source).
    7. Assert: `sourceRatingAfter === sourceRatingBefore` (source roster unchanged in copy mode).
    8. This is a regression test for D-D-04 — pre-Phase-12.3, the target rating would NOT have been recomputed.

    **Scenario 2 — Move mode recomputes BOTH source and target account ratings.**
    1. Same setup as Scenario 1 (source with known roster, target empty).
    2. Call `migrate_roster(sourceAccountId, targetAccountId, 'move')`.
    3. Assert: `targetRatingAfter === sourceRatingBefore` (target now has the source's roster).
    4. Assert: `sourceRatingAfter` equals the empty-account baseline rating (source roster was emptied — its rating should reflect the empty state, which is lower than `sourceRatingBefore`).
    5. Assert: `sourceRatingAfter !== sourceRatingBefore` (proves source rating was ACTUALLY recomputed, not just left stale).

    **Scenario 3 — D-G-01 migrate_roster guard: source account in lobby → reject.**
    1. Create source + target accounts for blue.
    2. Create a lobby, blue joins, blue calls `select_match_account(lobbyId, sourceAccountId)`. Source is now bound to a live LobbyMemberAccount.
    3. Call `migrate_roster(sourceAccountId, targetAccountId, 'copy')`.
    4. Assert: rejects with `SenderError` whose message contains `'migrate characters from this account'`.
    5. Use `expectReducerError` from the shared harness.

    **Scenario 4 — D-G-01 migrate_roster guard: target account in lobby → reject.**
    1. Same setup but blue selects TARGET instead of source.
    2. Call `migrate_roster(sourceAccountId, targetAccountId, 'copy')`.
    3. Assert: rejects with message containing `'migrate characters to this account'`.

    **Scenario 5 — Empty source is a no-op (early return in migrate_roster wrapper).**
    1. Create source (empty) and target (empty) accounts.
    2. Call `migrate_roster(sourceAccountId, targetAccountId, 'copy')`.
    3. Assert: no error thrown, both ratings unchanged.

    **Test integrity:**
    - Cleanup accounts/lobbies in `afterEach`.
    - No shared-harness or backend edits.
    - Report failures; do not auto-fix.
  </action>
  <verify>
    <automated>test -f test/backend/roster/migrate-roster-rating.test.ts && grep -c "migrate_roster\|migrate_roster" test/backend/roster/migrate-roster-rating.test.ts | grep -qE "^[5-9]|^[1-9][0-9]" && grep -c "migrate characters from this account\|migrate characters to this account" test/backend/roster/migrate-roster-rating.test.ts | grep -qE "^[2-9]" && npx vitest run test/backend/roster/migrate-roster-rating.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/backend/roster/migrate-roster-rating.test.ts` exists
    - File contains scenarios for copy, move, guard (source in lobby), guard (target in lobby), empty source
    - File contains the exact substrings `migrate characters from this account` and `migrate characters to this account` (matching the D-G guard error messages from Plan 05)
    - File uses `expectReducerError` for the rejection assertions
    - File uses `createVerifiedTestHarness`
    - `npx vitest run test/backend/roster/migrate-roster-rating.test.ts` exits 0 OR failures reported verbatim
    - Only this file added; no backend/helper edits
  </acceptance_criteria>
  <done>New file covers D-D-04 regression + migrate_roster D-G guard tests; all scenarios pass or failures reported.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create `test/backend/tournaments/tournament-ordering-guard.test.ts` — D-H-01 guard behavior (TOURN-ORDER-01 / MMR-RACE-02)</name>
  <files>test/backend/tournaments/tournament-ordering-guard.test.ts</files>
  <read_first>
    - test/backend/tournaments/tournament-mmr.test.ts (reference for the full "create tournament → register → generate bracket → run match → process_tournament_mmr → finalize" flow)
    - test/backend/tournaments/tournament-stages.test.ts (reference for driving a tournament through its stage lifecycle — specifically how to transition to `Completed`)
    - test/shared/helpers/tournaments.ts (available helpers)
    - spacetimedb/src/reducers/matchFinalization.ts (post-Plan 04 — verify the exact error message string)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decision D-H-01 through D-H-05
  </read_first>
  <action>
    Create `test/backend/tournaments/tournament-ordering-guard.test.ts`. Scenarios:

    **Scenario 1 — MMR-tournament match rejects mid-tournament, accepts after Completed.**
    1. Create a tournament with `countTowardsMmr: true`, minimum participants, game mode set.
    2. Register participants, lock accounts, generate bracket.
    3. Advance tournament to `InProgress` stage.
    4. Pick the first bracket match; create its lobby via the standard flow, join, start draft, play to completion, referee validates the result (so `matchResult.status = Validated`).
    5. Before advancing the tournament stage further, call `finalize_match_result(matchResultId)`.
    6. Assert: throws `SenderError` containing `'Tournament match cannot be finalized while the tournament is still active'`.
    7. Then advance the tournament to `Completed` via the TO's stage transition reducer (or admin_cancel if Completed can't be reached in test time — see Scenario 2 for Cancelled variant).
    8. Call `finalize_match_result(matchResultId)` again.
    9. Assert: succeeds (no error, MRP rows deleted, MmrRating updated).

    Reference the existing `tournament-mmr.test.ts` flow for the setup shortcuts — that test runs the full tournament-mmr path and knows how to get to `Completed`.

    **Scenario 2 — Casual tournament (countTowardsMmr=false) ALSO rejects mid-tournament.**
    1. Same tournament flow but `countTowardsMmr: false`.
    2. Mid-tournament, attempt finalize → assert same rejection.
    3. Transition tournament to `Cancelled` (use `cancelTournament` — Phase 10.5 established this is a persistent terminal state).
    4. Call finalize → succeeds.
    5. Assert that `process_tournament_mmr` was NOT called (because `countTowardsMmr=false` means the tournament-batch MMR step was never a consumer of the MRP rows — finalize just proceeds with MRP delete).

    **Scenario 3 — Non-tournament match finalization unaffected (sanity regression).**
    1. Create a non-tournament ranked lobby.
    2. Run match to completion.
    3. Call finalize → succeeds immediately (no guard applies because `isTournamentControlled === false`).
    4. Assert: MRP rows deleted, MmrRating updated.

    **Scenario 4 (Pitfall 4 edge case) — tournament-controlled MRR with undefined bracketMatchId.**
    If the test harness allows creating an MRR with `isTournamentControlled: true` and `bracketMatchId: undefined` (e.g., via an admin path or direct SQL insert), attempt finalize → assert the same rejection occurs (Pitfall 4 defensive handling from Plan 04). If the harness cannot set up this edge case cleanly, skip with a `it.skip(...)` and a comment noting it's a defensive case tested at the grep level in Plan 04.

    **Test integrity:**
    - Cleanup tournaments/lobbies in afterEach.
    - Tournament cleanup may use `cancelTournament` + the helpers Phase 10.5 hardened.
    - Do not modify shared helpers or backend code.
  </action>
  <verify>
    <automated>test -f test/backend/tournaments/tournament-ordering-guard.test.ts && grep -c "Tournament match cannot be finalized while the tournament is still active" test/backend/tournaments/tournament-ordering-guard.test.ts | grep -qE "^[1-9]" && grep -c "countTowardsMmr" test/backend/tournaments/tournament-ordering-guard.test.ts | grep -qE "^[1-9]" && npx vitest run test/backend/tournaments/tournament-ordering-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/backend/tournaments/tournament-ordering-guard.test.ts` exists
    - File contains the exact substring `Tournament match cannot be finalized while the tournament is still active`
    - File contains scenarios for MMR-tournament, casual-tournament, non-tournament, and (optionally) the Pitfall 4 edge case
    - File uses `expectReducerError` for the rejection assertions
    - File uses `createVerifiedTestHarness` and tournament helpers from `test/shared/helpers/tournaments.ts`
    - `npx vitest run test/backend/tournaments/tournament-ordering-guard.test.ts` exits 0 OR failures reported verbatim
    - Only this file added
  </acceptance_criteria>
  <done>New file covers D-H-01 guard across MMR/casual/non-tournament match types; rejection before terminal stage and success after.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create `test/backend/match-session/auto-pick-ownership-pool.test.ts` — D-I-04 four behavioral corrections (OWN-POOL-01)</name>
  <files>test/backend/match-session/auto-pick-ownership-pool.test.ts</files>
  <read_first>
    - test/backend/match-session/draft-classic.test.ts (understand how existing tests simulate `timer_expiry_classic` — look for any fast-forward or direct-call helpers)
    - test/backend/match-session/post-draft.test.ts (reference for post-draft assertions)
    - test/shared/helpers/drafts.ts (draft-stage helpers)
    - spacetimedb/src/reducers/draftClassic.ts (post-Plan 01 — the refactored `timer_expiry_classic` block; confirm the test targets the exact code path)
    - spacetimedb/src/reducers/lobbyLifecycle.ts lines 323-338 (auto-LMA seed on join — this is what sets up the "isActive != selected LMA" scenario)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-I-01 through D-I-06 (four behavioral corrections in D-I-04)
    - .planning/phases/12.3-mmr-rating-snapshot/RESEARCH.md Pitfall 9 ("D-I refactor order matters vs. tests using `requireOwnership`")
  </read_first>
  <action>
    Create `test/backend/match-session/auto-pick-ownership-pool.test.ts`. Four scenarios matching D-I-04's four behavioral corrections.

    **Preamble — identify how to trigger `timer_expiry_classic`:**
    The reducer is invoked by the draft timer expiring. In the test harness this typically means either:
    - A direct test-only admin path to fast-forward the timer, OR
    - A very short timer configured on the lobby, waited out.
    Read `draft-classic.test.ts` to find the established pattern. If neither exists, the test must either mock the timer or directly invoke `timer_expiry_classic` as a scheduled-row reducer call if the SDK allows. If none of these are possible in the harness, document the gap and leave the scenario as `it.skip(...)` with a clear explanation — manual UAT owns the coverage.

    **Scenario 1 — Non-tournament: `isActive=A` but LMA=B → auto-pick pool has B's chars, not A's.**
    1. Create user blue with two HsrAccounts: A (character set `{Acheron, Jingliu}`) and B (character set `{Silver Wolf, Kafka, Blade}`). Set A as isActive.
    2. Create non-tournament lobby with `requireOwnership: true`. Blue joins → auto-LMA seed uses isActive=A.
    3. Blue calls `select_match_account(lobbyId, B.accountId)` — replaces LMA to B.
    4. Other team member joins, start draft.
    5. Advance draft to blue's pick turn. Trigger `timer_expiry_classic`.
    6. Query the resulting auto-pick. Assert: the picked character is one of `{Silver Wolf, Kafka, Blade}` (B's pool), NOT one of `{Acheron, Jingliu}` (A's pool which was the pre-phase wrong answer).
    7. Assert also that the `availablePool` for blue's team excludes A's characters entirely by inspecting the draft state or logs if available.

    **Scenario 2 — Multi-account tournament user: union across all selected LMA accounts.**
    1. Tournament setup with `maxAccountsPerPlayer: 2`. Blue has accounts C (chars `{Sparkle}`) and D (chars `{Ruan Mei}`).
    2. Blue locks both for the tournament, joins a tournament lobby, selects BOTH via `select_match_account`.
    3. Start draft, advance to blue's auto-pick turn.
    4. Trigger `timer_expiry_classic`.
    5. Assert: the picked character is one of `{Sparkle, Ruan Mei}` — proving the pool was the UNION of both selected accounts.

    **Scenario 3 — Stand-in contributes LMA chars (not global isActive).**
    1. Create a team with captain blue. Blue has isActive account A with chars `{Jing Yuan}`.
    2. Create the lobby, start with blue as captain, then blue leaves and is replaced by stand-in Carol (simulated via rejoin or the stand-in flow, if the harness supports it).
    3. Carol's isActive account has chars `{Firefly}`, but Carol selects a DIFFERENT account (account Z with chars `{Dr. Ratio}`) via `select_match_account`.
    4. Start draft (or resume if the lobby is already in-progress — this scenario depends on whether stand-ins can join after `start_draft`; if not, the scenario becomes "stand-in joins in Waiting stage with a non-isActive account selected").
    5. Advance draft to Carol's auto-pick turn.
    6. Assert: the picked character is `Dr. Ratio` (from Carol's selected LMA), NOT `Firefly` (from Carol's isActive) and NOT `Jing Yuan` (from blue's pre-stand-in isActive).

    **Scenario 4 — Casual requireOwnership with one team member missing LMA → that member contributes empty, pool still assembles from others.**
    1. Create a 2v2 casual lobby with `requireOwnership: true`. Note: start_draft rejects ranked/MMR-tournament matches with zero-LMA users at `draftClassic.ts:59-73`. For this scenario to be reachable, the match MUST be CASUAL (and the requireOwnership lobby setting + zero-LMA must not be rejected at start_draft — verify by reading `draftClassic.ts` lines 59-73 to confirm the rejection only applies to ranked/MMR).
    2. Set up blue-1 with LMA on account X (chars `{Bailu, Luocha}`). Set up blue-2 with NO HsrAccount at all (so no auto-seed — verify harness allows this).
    3. Start draft, advance to blue-1's auto-pick turn (or blue-2's if the draft order allows).
    4. Trigger `timer_expiry_classic`.
    5. Assert: picked character is one of `{Bailu, Luocha}` (from blue-1's LMA, because blue-2 contributes nothing).
    6. If blue-2's turn comes up: assert the pool is still drawn from the team's union — since blue-2 contributes empty, the pool equals blue-1's LMA characters. Assert the pick is a character from that set.

    **Pitfall 9 note:** Existing tests in `draft-classic.test.ts` and `post-draft.test.ts` may have been passing because of the OLD `isActive`-based auto-pick behavior. Run `npm run test:phase test/backend/match-session` BEFORE creating the new test file; if pre-existing tests fail post-Plan-01, REPORT those failures. Do NOT fix them (C10) — they may indicate the old tests encoded the bug.

    **Test integrity:**
    - Cleanup in afterEach.
    - No shared-helper or backend edits.
    - Report pre-existing test failures separately from the new test results.
  </action>
  <verify>
    <automated>test -f test/backend/match-session/auto-pick-ownership-pool.test.ts && grep -c "timer_expiry_classic\|requireOwnership" test/backend/match-session/auto-pick-ownership-pool.test.ts | grep -qE "^[2-9]|^[1-9][0-9]" && grep -c "LobbyMemberAccount\|select_match_account" test/backend/match-session/auto-pick-ownership-pool.test.ts | grep -qE "^[2-9]|^[1-9][0-9]" && npx vitest run test/backend/match-session/auto-pick-ownership-pool.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/backend/match-session/auto-pick-ownership-pool.test.ts` exists
    - File contains the four scenarios (or `it.skip` for any scenario the harness cannot support, with a clear comment explaining)
    - File references `timer_expiry_classic` or the test harness's timer-fast-forward mechanism
    - File references `select_match_account` to establish the LMA vs isActive divergence
    - File uses `createVerifiedTestHarness`
    - `npx vitest run test/backend/match-session/auto-pick-ownership-pool.test.ts` exits 0 OR failures reported verbatim
    - Pre-existing `draft-classic.test.ts` / `post-draft.test.ts` failures (if any) are REPORTED but NOT fixed (C10 + Pitfall 9)
    - Only the new file added; no backend/helper edits
  </acceptance_criteria>
  <done>New file covers D-I-04 four corrections; any scenario that cannot be set up is explicitly skipped with explanation; no pre-existing test files modified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.3-26 | Test integrity (pre-existing tests encoded the bug) | Pitfall 9: `draft-classic.test.ts` may have been passing only because of the OLD `isActive`-based auto-pick | mitigate | Task 3 explicitly forbids fixing pre-existing test failures. Failures are reported to the user; the user decides whether the old test encoded the bug (delete) or the new code broke an invariant (investigate). |
| T-12.3-27 | Test integrity (false green via stub timer) | `timer_expiry_classic` tests might pass only because the timer never actually fires in the harness | accept | Each scenario must assert a specific observable post-state (e.g., a picked character from a known-constrained set). If the assertion is present but the test passes without triggering the timer, the assertion fails. Best effort with the existing harness; manual UAT is the backstop. |
</threat_model>

<verification>
- Three file-scoped vitest runs in Tasks 1-3 exit 0 or failures reported
- Phase-level validation happens in Plan 08's `npm run test:all` task
</verification>

<success_criteria>
- `migrate-roster-rating.test.ts` covers D-D-04 + D-G migrate guard
- `tournament-ordering-guard.test.ts` covers D-H MMR/casual/non-tournament branches
- `auto-pick-ownership-pool.test.ts` covers D-I-04 four corrections (or documents skip reasons)
- No backend or shared-helper edits
- All file-scoped runs exit 0 or failures reported
</success_criteria>

<output>
Create `.planning/phases/12.3-mmr-rating-snapshot/12.3-07-SUMMARY.md` with the vitest run output for each file and a summary of any pre-existing test failures uncovered by Plan 01-05 that Plan 07 chose not to fix.
</output>
