---
phase: 12.3
plan: 06
type: execute
wave: 3
depends_on:
  - "12.3-01"
  - "12.3-02"
  - "12.3-03"
files_modified:
  - test/backend/match-results/mmr-snapshot.unit.test.ts
  - test/backend/match-results/mmr-snapshot-betweengames.test.ts
  - test/backend/match-results/mmr-stats.test.ts
autonomous: true
requirements:
  - MMR-RACE-01
  - MMR-RACE-02
must_haves:
  truths:
    - "`mmr-snapshot.unit.test.ts` exists and covers: (a) snapshot captured at start_draft = max across LMA rows for tournament path, (b) snapshot = single LMA account rating for non-tournament path, (c) snapshot defaults to 0 when a casual match participant has zero LMA rows"
    - "`mmr-snapshot-betweengames.test.ts` exists and covers: (a) monotonic-upward update when a player swaps HIGH→LOW between games (snapshot stays at HIGH), (b) monotonic update when player swaps LOW→HIGH (snapshot updates to HIGH), (c) deselect_match_account does NOT lower the snapshot"
    - "`mmr-stats.test.ts` is extended (not replaced) with three D-G race-condition integration scenarios proving post-capture mutations do NOT affect processed MMR deltas"
    - "All new tests use the shared `createVerifiedTestHarness` pattern from `test/shared/connection.ts` and do NOT modify the shared harness itself"
  artifacts:
    - path: "test/backend/match-results/mmr-snapshot.unit.test.ts"
      provides: "MMR-RACE-01 capture + default-0 coverage"
      contains: "accountRatingSnapshot"
    - path: "test/backend/match-results/mmr-snapshot-betweengames.test.ts"
      provides: "MMR-RACE-01 monotonic-upward across best-of-N"
      contains: "Math.max"
    - path: "test/backend/match-results/mmr-stats.test.ts"
      provides: "Race-condition regression coverage (extension of existing file)"
      contains: "race"
  key_links:
    - from: "test files"
      to: "spacetimedb/src/tables/matchResultParticipant.ts (accountRatingSnapshot column)"
      via: "spacetime sql queries or generated binding reads"
      pattern: "accountRatingSnapshot"
---

<objective>
Create two new test files and extend one existing test file to provide end-to-end coverage of Plan 01 capture, Plan 02 read-path swap, and Plan 03 monotonic-upward hook. This plan owns ALL test-file touches related to the MMR snapshot code paths and is strictly separated from the code plans per C10 (test files are only modified via explicit tasks, never as afterthoughts).

Purpose: Prove that the snapshot mechanic works end-to-end — capture, read-back, monotonic rule, default-0, and race-condition resistance against post-capture mutations.

Output: Three Wave-3 test files/extensions, all running green against the maincloud module published in Plan 01.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md
@.planning/phases/12.3-mmr-rating-snapshot/RESEARCH.md
@.planning/phases/12.3-mmr-rating-snapshot/VALIDATION.md
@./CLAUDE.md
@.claude/skills/uat/SKILL.md

<interfaces>
<!-- Existing test harness patterns the new files must follow — do NOT invent new primitives -->

From test/shared/connection.ts (UNCHANGED — Task 1/2/3 all use this):
`createVerifiedTestHarness(userCount: number)` — creates `userCount` verified users with SpacetimeDB connections, returns a `TestHarness` with `{ users, reducers, conn, cleanup }`. Used by `test/backend/match-results/mmr-stats.test.ts` at lines 21-28 as the canonical pattern.

From test/shared/helpers/hsrAccounts.ts (UNCHANGED):
`ensureHsrAccount(harness, userIndex, opts?)` — creates an HSR account for the indexed test user, returns `{ accountId, uid }`.

From test/shared/helpers/lobbies.ts (UNCHANGED):
`defaultLobbyArgs(overrides?)` — returns the args object for `create_lobby`. `joinLobby(harness, lobbyId, userIndexes[])`. `startDraftClassic(harness, lobbyId, hostIndex)`.

From test/backend/match-results/mmr-stats.test.ts (EXISTING — Task 3 extends):
Uses `createVerifiedTestHarness(3)` for host/blue/red, `ensureHsrAccount` for each, and the standard lobby creation → join → confirm → start_draft flow at lines 30-80. Tests are organized in `describe('Ranked MMR', () => { ... })` blocks.

From test/backend/roster/ subdirectory (UNCHANGED — Task 3 does NOT edit anything here):
The four D-G guard test coverage lives in Plan 07's `mmr-stats.test.ts` extension for consistency with the existing mmr-stats harness patterns.

Test running commands (from package.json — UNCHANGED):
- `npm run test:typecheck` — `tsc --noEmit`
- `npm run test:phase test/backend/match-results` — phase-scoped integration run
- `npm run test` — unit only
- `npm run test:all` — unit + integration
- `npx vitest run <file>` — single-file run with exit code

NEVER use `--watch` (C10 harness rule).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `test/backend/match-results/mmr-snapshot.unit.test.ts` — snapshot capture + default-0 + max aggregation coverage (MMR-RACE-01)</name>
  <files>test/backend/match-results/mmr-snapshot.unit.test.ts</files>
  <read_first>
    - test/backend/match-results/mmr-stats.test.ts lines 1-80 (harness pattern reference — `createVerifiedTestHarness`, `ensureHsrAccount`, standard lobby-creation flow)
    - test/shared/connection.ts (verify `createVerifiedTestHarness` and `queryPrivateTable` signatures)
    - test/shared/helpers/hsrAccounts.ts (verify `ensureHsrAccount` options — can set accountRating? If not, the test must compute it from the roster mutation helpers)
    - test/shared/helpers/lobbies.ts (verify `defaultLobbyArgs`, `createLobby`, `joinLobby`, `startDraftClassic`, and any account-selection helpers)
    - test/backend/roster/roster-characters.test.ts (reference for how existing tests mutate accountRating via batch_upsert_characters)
    - .planning/phases/12.3-mmr-rating-snapshot/RESEARCH.md Validation Architecture section (existing harness details)
    - .planning/phases/12.3-mmr-rating-snapshot/VALIDATION.md Wave 0 Gaps section (enumerated test files)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-A-01, D-B-01, D-B-02
    - spacetimedb/src/tables/matchResultParticipant.ts (post-Plan 01 — column exists)
  </read_first>
  <action>
    Create a new file `test/backend/match-results/mmr-snapshot.unit.test.ts`. This file is actually an INTEGRATION test (spins up a harness and exercises reducers) despite the `.unit.test.ts` suffix — the suffix in `VALIDATION.md` is the planner's naming convention for "scoped, fast, focused on snapshot capture semantics". If the project convention reserves `.unit.test.ts` for pure-math unit tests (like `elo-calculation.unit.test.ts`), use the extension `.test.ts` instead. Read `test/backend/match-results/account-rating.unit.test.ts` first to confirm the project convention before choosing the suffix.

    The file structure:

    ```typescript
    import { describe, it, expect, beforeEach, afterEach } from 'vitest';
    import { createVerifiedTestHarness, TestHarness } from '../../shared/connection';
    import { ensureHsrAccount } from '../../shared/helpers/hsrAccounts';
    import { defaultLobbyArgs /* + whatever helpers exist for create/join/start */ } from '../../shared/helpers/lobbies';
    // ... import whatever else matches existing patterns

    describe('Phase 12.3: MMR accountRatingSnapshot capture', () => {
        // Three top-level scenarios, each its own `it(...)` block:
    });
    ```

    **Scenario 1 — Non-tournament (single LMA):** snapshot equals that one account's rating.
    1. Create harness with 3 users (host, blue, red).
    2. For blue and red, create an HsrAccount with a KNOWN `accountRating` value (e.g., populate via `batch_upsert_characters` with a specific character+eidolon combo whose computed rating is predictable — read `test/backend/roster/roster-characters.test.ts` for the exact pattern OR use the existing `ensureHsrAccount` helper if it accepts a rating override).
    3. Create a lobby (non-tournament — `isTournamentControlled: false`), have blue + red join.
    4. Each player calls `select_match_account(lobbyId, theirAccountId)`.
    5. Call `start_draft(lobbyId)` as host.
    6. Query `MatchResultParticipant` rows via `queryPrivateTable` or the generated binding.
    7. Assert: `mrp.accountRatingSnapshot === blueAccount.accountRating` for the blue participant, same for red.
    8. Cleanup (use the harness's cleanup pattern — see the existing `afterEach` in `mmr-stats.test.ts`).

    **Scenario 2 — Tournament multi-account (max across LMA rows):** snapshot equals the HIGHEST rating among selected accounts.
    1. Harness with 3 users + a tournament admin. Reuse any `createTournamentArgs` / `create_tournament` helpers from `test/shared/helpers/tournaments.ts`.
    2. Create a tournament with `maxAccountsPerPlayer: 2`, `countTowardsMmr: true`.
    3. For blue, create TWO HsrAccounts: account A with accountRating = LOW value (e.g., around 50), account B with accountRating = HIGH value (e.g., around 500). Use the same batch_upsert pattern to drive the rating. Record both known values.
    4. Register blue for the tournament, lock BOTH accounts via the existing tournament account-locking flow.
    5. Generate the bracket, create a tournament lobby bound to a bracket match, join, have blue call `select_match_account` twice (once for A, once for B — additive in tournament path).
    6. Start draft.
    7. Query `MatchResultParticipant`. Assert: `mrp.accountRatingSnapshot === max(accountRatingA, accountRatingB)` which equals the HIGH value.

    **Scenario 3 — Casual no-LMA default-0:** snapshot = 0 when a player has zero LMA rows at capture time.
    1. Harness with 3 users.
    2. Create a CASUAL lobby (non-ranked, non-tournament — `matchType: Casual` and `isTournamentControlled: false`) without requiring LMA presence. Verify via `defaultLobbyArgs` or an override that allows this.
    3. blue and red join WITHOUT calling `select_match_account` (leave LMA empty). Ensure there's no pre-populated LMA from any auto-seed path — if `lobbyLifecycle.ts:323` auto-seeds from `isActive`, the test must create users that have NO HsrAccount at all (so there's no isActive account to seed from), OR delete the LMA rows after join using an admin path if one exists.
    4. Call `start_draft(lobbyId)`.
    5. Query MRP. Assert: `mrp.accountRatingSnapshot === 0` for both participants.

    **If Scenario 3 cannot be set up** (i.e., the auto-LMA-seed on lobby join at `lobbyLifecycle.ts:323-338` ALWAYS creates an LMA row for a joining user when they have an `isActive` account), then:
    - Either skip this scenario (`it.skip(...)`) with a comment explaining that auto-seed makes zero-LMA casual matches unreachable via user-facing reducers, OR
    - Set the scenario up with users that have no HsrAccount at all (no auto-seed because there's nothing to seed from).
    - Pick whichever approach matches the existing test suite's style. Do NOT modify `lobbyLifecycle.ts` to make the scenario reachable — that would be out of scope.

    **Test integrity rules:**
    - Every scenario must `await` all reducer calls.
    - Every scenario must cleanup its lobby/tournament in `afterEach` or an equivalent pattern (see `test/backend/match-results/mmr-stats.test.ts` for the established style — Phase 10.5 hardened the cleanup rules).
    - Do NOT import from `spacetimedb/src/**` — always go through the test harness which uses the generated `src/module_bindings/`.
    - Do NOT modify `test/shared/connection.ts` or any helper file. If you need a helper that doesn't exist, INLINE the logic in the test and note it in the commit message as a follow-up extraction candidate.
    - Do NOT modify the Plan 01/02/03 source files. Tests are verification — if a test fails, report the failure; do NOT "fix" the backend to make a test pass (C10 — failures are diagnostic).
  </action>
  <verify>
    <automated>test -f test/backend/match-results/mmr-snapshot.unit.test.ts && grep -c "accountRatingSnapshot" test/backend/match-results/mmr-snapshot.unit.test.ts | grep -qE "^[3-9]|^[1-9][0-9]" && npx vitest run test/backend/match-results/mmr-snapshot.unit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/backend/match-results/mmr-snapshot.unit.test.ts` (or `.test.ts` if the project convention requires it — document the choice in the commit message) exists
    - File contains at least 3 `describe` or `it` blocks covering the three scenarios above
    - File contains at least 3 references to `accountRatingSnapshot` (one per scenario assertion)
    - File does NOT import from `spacetimedb/src/**` — only from `test/shared/**` and vitest
    - File uses `createVerifiedTestHarness` (imported from `../../shared/connection`)
    - `npx vitest run test/backend/match-results/mmr-snapshot.unit.test.ts` exits 0 (or, if any test fails, the failure is REPORTED in the commit message verbatim; do NOT modify backend code to make it pass)
    - `git diff --stat` shows only this file added; no modifications to shared harness, helpers, or backend
  </acceptance_criteria>
  <done>New test file exists, runs green (or failures reported), covers the three capture scenarios.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create `test/backend/match-results/mmr-snapshot-betweengames.test.ts` — monotonic-upward hook across best-of-N series (MMR-RACE-01)</name>
  <files>test/backend/match-results/mmr-snapshot-betweengames.test.ts</files>
  <read_first>
    - test/backend/match-results/mmr-stats.test.ts (harness pattern for best-of-N series tests; check if a `bestOf` option exists in `defaultLobbyArgs` overrides — Phase 10.1 added it)
    - test/shared/helpers/drafts.ts (verify helpers for completing a draft and entering BetweenGames stage)
    - test/shared/helpers/scores.ts (helpers for submitting game results to progress a best-of-N series)
    - test/backend/match-results/mmr-snapshot.unit.test.ts (just created in Task 1 — reuse the rating-setup pattern)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-B-03, D-B-04, D-B-05
    - spacetimedb/src/reducers/accountSelection.ts (post-Plan 03 — verify the hook matches the test's expectations)
  </read_first>
  <action>
    Create a new file `test/backend/match-results/mmr-snapshot-betweengames.test.ts`. Three scenarios:

    **Scenario 1 — HIGH → LOW swap does NOT lower the snapshot:**
    1. Harness with users; blue has two accounts `blueHigh` (rating 500) and `blueLow` (rating 50). This scenario uses the NON-tournament replace path (simpler setup).
    2. Create a best-of-3 non-tournament ranked lobby. Blue selects `blueHigh`, red selects their account. Join, confirm, start draft.
    3. Query MRP — blue's snapshot = 500.
    4. Play game 1 (use existing draft+score helpers) until the lobby transitions to `BetweenGames`.
    5. Blue calls `select_match_account(lobbyId, blueLow.accountId)` — the non-tournament path deletes the old LMA and inserts the new one.
    6. Query MRP again — assert `blueMrp.accountRatingSnapshot === 500` (still HIGH, not LOW — the `Math.max` rule preserves the higher value).

    **Scenario 2 — LOW → HIGH swap DOES raise the snapshot:**
    1. Same setup, but blue initially selects `blueLow` (rating 50).
    2. Start draft → snapshot = 50.
    3. Play game 1 → BetweenGames.
    4. Blue calls `select_match_account(lobbyId, blueHigh.accountId)`.
    5. Query MRP — assert `blueMrp.accountRatingSnapshot === 500` (snapshot raised to the new higher value).

    **Scenario 3 — `deselect_match_account` does NOT modify the snapshot:**
    1. Use the tournament path (additive LMA) because non-tournament deselect would leave the user with zero LMA and the hook's short-circuit plus the `Math.max(existing, fresh)` rule means there's no fresh account to read — deselect is a no-op by design.
    2. Set up a tournament with `maxAccountsPerPlayer: 2`. Blue has accounts A (rating 100) and B (rating 300).
    3. Blue selects BOTH A and B in Waiting stage. Start draft → snapshot = `max(100, 300)` = 300.
    4. Play game 1 → BetweenGames.
    5. Blue calls `deselect_match_account(lobbyId, B.accountId)` — removes B from LMA.
    6. Query MRP — assert `blueMrp.accountRatingSnapshot === 300` (still the old max, because `deselect_match_account` does NOT run the hook per D-B-03).

    **Scenario 4 (optional / if harness supports it) — Stand-in no-op:** If the test harness supports simulating a stand-in joining after `start_draft`, add a scenario proving `select_match_account` called by the stand-in does NOT modify any MRP row (because the stand-in has no MRP row — hook short-circuits at `if (!existingMrp) return;`). If harness support is missing, document the skip and leave the coverage to manual UAT.

    **Same test integrity rules as Task 1:**
    - No modifications to shared helpers, backend, or other test files.
    - Cleanup in afterEach.
    - Report failures, do not auto-fix.
  </action>
  <verify>
    <automated>test -f test/backend/match-results/mmr-snapshot-betweengames.test.ts && grep -c "accountRatingSnapshot" test/backend/match-results/mmr-snapshot-betweengames.test.ts | grep -qE "^[3-9]|^[1-9][0-9]" && grep -c "BetweenGames\|select_match_account\|deselect_match_account" test/backend/match-results/mmr-snapshot-betweengames.test.ts | grep -qE "^[3-9]|^[1-9][0-9]" && npx vitest run test/backend/match-results/mmr-snapshot-betweengames.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/backend/match-results/mmr-snapshot-betweengames.test.ts` exists
    - File contains at least 3 scenarios (Scenarios 1, 2, 3 from the action block)
    - File contains at least 3 references to `accountRatingSnapshot`
    - File uses `select_match_account` in Scenarios 1 & 2 and `deselect_match_account` in Scenario 3
    - File uses `createVerifiedTestHarness`
    - `npx vitest run test/backend/match-results/mmr-snapshot-betweengames.test.ts` exits 0 OR the failures are reported verbatim in the commit message
    - Only this file added; no shared-harness / backend edits
  </acceptance_criteria>
  <done>New file covers HIGH→LOW, LOW→HIGH, and deselect scenarios; monotonic-upward rule verified end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend `test/backend/match-results/mmr-stats.test.ts` with three D-G race-condition integration tests proving post-capture mutations do NOT affect processed MMR deltas (MMR-RACE-02)</name>
  <files>test/backend/match-results/mmr-stats.test.ts</files>
  <read_first>
    - test/backend/match-results/mmr-stats.test.ts (full file — understand the existing Ranked MMR describe block, helper usage, afterEach cleanup contract)
    - spacetimedb/src/helpers/finalizationHelpers.ts lines 85-92 (post-Plan 02 — confirm the snapshot read is in place; the test assumptions rely on this)
    - spacetimedb/src/reducers/roster.ts (post-Plan 05 — note the D-G guards exist and will reject most mutation attempts unless the test first releases the LMA by leaving the lobby)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-E-01 (the three race vectors being tested) + D-G
  </read_first>
  <action>
    Extend the existing `test/backend/match-results/mmr-stats.test.ts` file. DO NOT rewrite existing tests. Add a new `describe` block at the end of the file:

    ```typescript
    describe('Phase 12.3: MMR race-condition regression (snapshot vs post-capture mutations)', () => {
        // Three scenarios below
    });
    ```

    **Scenario A — `set_active_hsr_account` race (D-E-01 vector 1):**
    This test documents that EVEN IF the D-G guard were bypassed (e.g., via an admin path), the snapshot from Plan 01 makes the system correct. Since the D-G guard rejects the mutation at the reducer layer, the test must simulate the race by:
    - Option 1 (preferred): Run a ranked match through to completion (start_draft, draft, scores, finalize). Verify the `MmrHistory` delta for blue. THEN the test is complete — no mutation needed, because the snapshot is already captured and the old bug's vector is closed at the read-path level (Plan 02).
    - Option 2 (harder): Leave the lobby, mutate `isActive`, re-enter (new lobby), and verify the new lobby's snapshot reflects the new isActive while the old lobby's snapshot is unchanged. This proves per-match isolation but is a harder test to write.

    Pick Option 1 — it's the minimal, direct proof that the snapshot flows through to MMR. Write as:
    1. Create two HsrAccounts for blue: `blueLow` (100) and `blueHigh` (800). Start with `blueHigh` as `isActive`.
    2. Create ranked non-tournament lobby, blue selects `blueHigh`, red selects their account. Start draft → snapshot = 800.
    3. Run the full draft + score + finalize flow.
    4. Read `MmrHistory` for blue — capture the delta.
    5. Reset state (new lobby), switch isActive to `blueLow` BEFORE joining the new lobby (no guard blocks this because blue is not in a lobby right now). Blue selects `blueLow`, runs a SECOND match to completion against the same red account.
    6. Read `MmrHistory` for blue on the second match — capture that delta.
    7. Assert: the deltas are DIFFERENT, proving the account rating genuinely influenced ELO. (This is a negative control — the fact that two matches with different account ratings produce different deltas proves the snapshot is being used; if the snapshot were stuck at 0 or at a constant, the deltas would be identical.)

    **Scenario B — `batch_upsert_characters` recompute race (D-E-01 vector 2):**
    Test that mutating the roster BEFORE the match does affect the snapshot (baseline) but mutating AFTER start_draft cannot affect a match that already finalized.
    1. Blue has an HsrAccount with a known starting roster / rating.
    2. Create match, start draft. Snapshot captured at start_draft value X.
    3. Attempt `batch_upsert_characters` on blue's account while IN the lobby — assert it REJECTS with the D-G-01 error message (`'edit characters on this account'`). This is a direct test of the D-G guard; it's OK to include here because the test file is mmr-stats (the D-G guards protect mmr-stats invariants).
    4. Leave the lobby (releasing the LMA). Now `batch_upsert_characters` is allowed.
    5. Run the lobby's finalize path — EXCEPT the lobby is gone because we left. This scenario needs careful sequencing. Alternative: run the full match to completion, then AFTER finalize (when MRP rows are deleted), mutate the roster — nothing to compare because MRP is gone.
    6. The simpler proof: Scenario A already shows the snapshot determines the delta. Scenario B can just be a targeted test of the D-G guard rejection (NEW describe: 'D-G roster guards' inside the same Phase 12.3 describe block):
       - `set_active_hsr_account` while in lobby → rejects with `'change your active account'`
       - `batch_upsert_characters` while in lobby → rejects with `'edit characters on this account'`
       - `batch_remove_characters` while in lobby → rejects with `'remove characters from this account'`
       - `migrate_roster` while in lobby → rejects with `'migrate characters from this account'` or `'migrate characters to this account'` depending on which side is bound

    **Scenario C — `migrate_roster` guard check (D-E-01 vector 2 via migration path):**
    Specific test of migrate_roster's guard: a user with an LMA row for account X cannot `migrate_roster` from X OR to X.

    **Concrete plan for Task 3:**

    Add the following top-level describe to `mmr-stats.test.ts`:

    ```typescript
    describe('Phase 12.3: snapshot-backed MMR + D-G guard rejections', () => {
        it('processes MMR using the captured snapshot, not live isActive (negative control — different account ratings produce different deltas)', async () => {
            // Implement Scenario A Option 1 (two-match comparison)
        });

        it('set_active_hsr_account rejects while caller has an active LobbyMemberAccount binding (D-G-01)', async () => {
            // Create lobby, blue selects account, attempt set_active_hsr_account on a different account
            // Expect SenderError containing 'change your active account'
        });

        it('batch_upsert_characters rejects while target account is bound to a live lobby (D-G-01)', async () => {
            // Similar pattern; expect error containing 'edit characters on this account'
        });

        it('batch_remove_characters rejects while target account is bound to a live lobby (D-G-01)', async () => {
            // Similar pattern; expect error containing 'remove characters from this account'
        });
    });
    ```

    The `migrate_roster` guard rejection tests and the migrate_roster rating-recompute (D-D-04) tests belong to Plan 07's dedicated `migrate-roster-rating.test.ts` file — do NOT duplicate them here.

    **Use `expectReducerError` from `test/shared/connection.ts`** for the rejection assertions (confirmed available per RESEARCH.md: "test/shared/connection.ts (createVerifiedTestHarness, hasServerToken, expectReducerError, queryPrivateTable, TestHarness type)"). Verify the error message substring matches the verb phrase exactly.

    **Do NOT:**
    - Rewrite or modify existing test blocks in `mmr-stats.test.ts` (only APPEND the new describe).
    - Create a `mmr-stats.test.ts.bak` or any other artifact.
    - Move tests between files.
    - Touch any backend source file.
  </action>
  <verify>
    <automated>grep -c "Phase 12.3" test/backend/match-results/mmr-stats.test.ts | grep -qE "^[1-9]" && grep -c "change your active account\|edit characters on this account\|remove characters from this account" test/backend/match-results/mmr-stats.test.ts | grep -qE "^[2-9]|^[1-9][0-9]" && grep -c "expectReducerError" test/backend/match-results/mmr-stats.test.ts | grep -qE "^[1-9]" && npx vitest run test/backend/match-results/mmr-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/backend/match-results/mmr-stats.test.ts` contains the new top-level describe block with `Phase 12.3` in its name
    - The file contains all three guard error substrings: `change your active account`, `edit characters on this account`, `remove characters from this account` (one per rejection test)
    - The file uses `expectReducerError` for the rejection assertions
    - The file contains a negative-control test proving different snapshot values produce different MMR deltas
    - Pre-existing tests in `mmr-stats.test.ts` are UNCHANGED (diff should show only additions — verify with `git diff test/backend/match-results/mmr-stats.test.ts` showing only `+` lines for the new describe block)
    - `npx vitest run test/backend/match-results/mmr-stats.test.ts` exits 0 OR failures reported verbatim in commit message
    - Only `mmr-stats.test.ts` modified in this task; no shared harness or backend edits
  </acceptance_criteria>
  <done>mmr-stats.test.ts is extended with the Phase 12.3 describe block; negative-control + three rejection tests present; no pre-existing tests modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — tests run in the harness sandbox and exercise the backend via generated client bindings. No new external surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.3-24 | Test integrity (false green) | A test passes only because the backend was silently modified to match the test's wrong assumption | mitigate | C10 enforcement — tests are diagnostic. This plan's tasks explicitly forbid modifying backend code to make tests pass. Failures are reported, not fixed. |
| T-12.3-25 | Test integrity (test pollution across files) | Shared database state across integration test files contaminates later runs | accept | Phase 10.5 hardened the test suite for exactly this concern (afterEach strict cleanup). New tests must follow the same pattern. If pollution is detected post-land, Phase 10.5's Plan 04 approach applies (bisect, harden cleanup). |
</threat_model>

<verification>
- Three test files/extensions created, each grep-verifiable
- `npx vitest run` per file exits 0, or failures are reported
- Full `npm run test:all` is deferred to Plan 08's phase-finalization task
</verification>

<success_criteria>
- New file `test/backend/match-results/mmr-snapshot.unit.test.ts` exists and covers 3 capture scenarios
- New file `test/backend/match-results/mmr-snapshot-betweengames.test.ts` exists and covers 3 monotonic scenarios
- `test/backend/match-results/mmr-stats.test.ts` extended with 4 new tests (1 negative-control + 3 guard rejections) without modifying pre-existing tests
- All file-scoped vitest runs exit 0 (or failures documented)
- No backend source files touched
- No shared helpers touched
</success_criteria>

<output>
Create `.planning/phases/12.3-mmr-rating-snapshot/12.3-06-SUMMARY.md` with the vitest run output for each file (pass/fail summary + any failure details for diagnosis).
</output>
