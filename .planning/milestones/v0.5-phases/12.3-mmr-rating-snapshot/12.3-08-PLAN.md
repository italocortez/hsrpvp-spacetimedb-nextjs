---
phase: 12.3
plan: 08
type: execute
wave: 4
depends_on:
  - "12.3-01"
  - "12.3-02"
  - "12.3-03"
  - "12.3-04"
  - "12.3-05"
  - "12.3-06"
  - "12.3-07"
files_modified:
  - docs/match-results/architecture.md
  - docs/roster/architecture.md
  - docs/tournament/architecture.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - MMR-RACE-01
  - MMR-RACE-02
  - ROST-GUARD-01
must_haves:
  truths:
    - "`docs/match-results/architecture.md` documents the `accountRatingSnapshot` column, the `start_draft` capture point, the monotonic-upward update hook in `select_match_account`, the read-path swap in `processMatchMmr`, and the D-H tournament-ordering guard on `finalize_match_result`"
    - "`docs/roster/architecture.md` documents the `rosterMutations.ts` helper extraction, the `applyBatchUpsert` / `applyBatchRemove` helpers, the D-D `migrate_roster` rating-recompute fix, and the four D-G lobby guards (set_active, batch_upsert, batch_remove, migrate_roster)"
    - "`docs/tournament/architecture.md` documents the `finalize_match_result` D-H ordering guard behavior (MMR batch + rollback window preservation) and cross-references match-results"
    - "`.planning/REQUIREMENTS.md` contains the three new requirement IDs (MMR-RACE-01, MMR-RACE-02, ROST-GUARD-01) under a new `MMR Race-Condition Fixes` subsection and the Traceability table maps them to Phase 12.3"
    - "`docs/*/contract.md` files are NOT modified by this plan (C9 — contract updates belong to /gsd-verify-work post-execution)"
    - "Full test suite (`npm run test:all`) was run once at the end of this plan; result recorded in the plan summary for phase sign-off"
  artifacts:
    - path: "docs/match-results/architecture.md"
      provides: "Architecture-layer documentation for the snapshot mechanism"
      contains: "accountRatingSnapshot"
    - path: "docs/roster/architecture.md"
      provides: "Architecture-layer documentation for the roster mutation helpers + D-G guards"
      contains: "applyBatchUpsert"
    - path: "docs/tournament/architecture.md"
      provides: "Architecture-layer documentation for finalize_match_result ordering guard"
      contains: "Tournament match cannot be finalized"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Three new requirement IDs + traceability entries"
      contains: "MMR-RACE-01"
  key_links: []
---

<objective>
Close out Phase 12.3 by (a) updating the three affected `docs/*/architecture.md` files to reflect the new code patterns (C8 mandates architecture-doc updates on every backend change), (b) backfilling `.planning/REQUIREMENTS.md` with the three Phase 12.3 requirement IDs that currently live only in ROADMAP.md, and (c) running the full test suite as a final phase-level verification gate. Contract docs (`docs/*/contract.md`) are INTENTIONALLY NOT modified here — they're updated post-execution during `/gsd-verify-work` per C9.

Purpose: Establish the architectural record of what changed, fix the REQUIREMENTS.md traceability gap, and prove the phase is cohesively green.

Output: Three architecture.md files updated with new content (no rewrites, targeted additions), REQUIREMENTS.md amended, full test suite green (or failures cataloged for the user's triage).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md
@.planning/phases/12.3-mmr-rating-snapshot/RESEARCH.md
@.planning/phases/12.3-mmr-rating-snapshot/12.3-01-SUMMARY.md
@.planning/phases/12.3-mmr-rating-snapshot/12.3-05-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update `docs/match-results/architecture.md` with Phase 12.3 MMR snapshot + finalize-ordering-guard additions (C8)</name>
  <files>docs/match-results/architecture.md</files>
  <read_first>
    - docs/match-results/architecture.md (full file — to understand the existing structure: Table Relationships → Reducer Flows → Phase History)
    - .planning/phases/12.3-mmr-rating-snapshot/12.3-01-SUMMARY.md, 12.3-02-SUMMARY.md, 12.3-03-SUMMARY.md, 12.3-04-SUMMARY.md (the exact code changes to document)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md (source of truth for decision rationale)
    - .claude/skills/uat/references/workflow-doc-template.md (to confirm the Phase History tag format is `Phase 12.3` not `Phase 12.3 execution`)
  </read_first>
  <action>
    Edit `docs/match-results/architecture.md`. Make targeted ADDITIONS only — do NOT rewrite existing sections.

    **Edit 1 — Table Relationships:** Update the `match_result_participant` entry in the tree diagram to note the new column:
    - Add a line in that table's column list showing `accountRatingSnapshot (f64, default 0)` with the comment: `// Phase 12.3: frozen HsrAccount.accountRating input for ELO modifier`.

    **Edit 2 — Reducer Flows section — new subsection "Phase 12.3: MMR accountRatingSnapshot lifecycle":**
    Add a new subsection between the existing "Match finalization flow" and "Phase History" sections. Content:

    ```markdown
    ### Phase 12.3: MMR accountRatingSnapshot lifecycle

    The account-rating input to `processMatchMmr` is frozen at match-record time on a
    `MatchResultParticipant.accountRatingSnapshot: f64` column (Phase 12.3 D-F-01).
    This eliminates three race vectors documented in Phase 12.3 CONTEXT.md D-E-01:

    1. `set_active_hsr_account` called post-capture
    2. `batch_upsert_characters` / `batch_remove_characters` called post-capture
    3. `process_tournament_mmr` re-reading `isActive` at tournament-end (potentially hours later)

    **Capture point** (D-A-01): `start_draft` at `draftClassic.ts` computes
    `max(HsrAccount.accountRating)` across all `LobbyMemberAccount` rows for
    `(lobbyId, userId)` at the moment the lobby transitions `Waiting -> Drafting`
    and writes the value in the same MRP insert loop. For casual matches with zero
    LMA rows the snapshot defaults to 0 (matches the pre-phase `?? 0` fallback).

    **Monotonic update** (D-B-03): `select_match_account` extends the snapshot upward
    between games in a best-of-N series via a post-insert hook:
    `accountRatingSnapshot = max(existing, newAccount.accountRating)`. `deselect_match_account`
    does NOT run the hook (removing from selection can never lower the max). The hook
    short-circuits when no `MatchResultRecord` exists (Waiting stage) or when the caller
    has no MRP row (stand-ins joining after `start_draft`).

    **Read-site** (D-readpath-01): `processMatchMmr` at `finalizationHelpers.ts` reads
    `participant.accountRatingSnapshot` directly. Both the standalone-ranked path
    (`runFinalization` step 11) and the tournament-batch path (`process_tournament_mmr`)
    call the same helper, so the read-path swap is path-independent by construction.

    **Tournament ordering guard** (D-H-01): `finalize_match_result` rejects
    tournament-controlled matches while the parent tournament is not `Completed` or
    `Cancelled`. Unconditional (`countTowardsMmr` is not consulted) because the guard
    protects both MMR batch processing AND bracket rollback capability. Cross-reference:
    see `docs/tournament/architecture.md`.

    **Public subscription impact** (C7): `MatchResultParticipant` is `public: true`,
    so `accountRatingSnapshot` ships to subscribed clients. One f64 per player per
    live match; bounded by match count × team size. Acceptable within the energy budget.
    ```

    **Edit 3 — Phase History table:** Add a row at the end:
    ```markdown
    | Phase 12.3 | Freeze account-rating input to ELO via MRP.accountRatingSnapshot, monotonic-upward hook in select_match_account, read-path swap in processMatchMmr, finalize_match_result tournament-ordering guard, timer_expiry_classic auto-pick LMA migration |
    ```

    **Do NOT:**
    - Modify `docs/match-results/contract.md`. C9 — contract updates happen POST-execution during `/gsd-verify-work`.
    - Update the Reducer reference table at the top (unless the file has a row-per-reducer table — if so, add a short note to the `finalize_match_result` row about the D-H guard and nothing more).
    - Rewrite existing sections.
    - Delete any content.
  </action>
  <verify>
    <automated>grep -c "accountRatingSnapshot" docs/match-results/architecture.md | grep -qE "^[3-9]|^[1-9][0-9]" && grep -c "Phase 12.3" docs/match-results/architecture.md | grep -qE "^[2-9]|^[1-9][0-9]" && test ! -f docs/match-results/contract.md.backup</automated>
  </verify>
  <acceptance_criteria>
    - `docs/match-results/architecture.md` contains at least 3 occurrences of `accountRatingSnapshot`
    - `docs/match-results/architecture.md` contains at least 2 occurrences of `Phase 12.3`
    - `docs/match-results/architecture.md` contains the subsection header `### Phase 12.3: MMR accountRatingSnapshot lifecycle` or similar
    - `docs/match-results/architecture.md` contains a new Phase History row for Phase 12.3
    - `docs/match-results/contract.md` is UNCHANGED — verify with `git diff docs/match-results/contract.md` showing zero output
    - No other doc file modified in this task
  </acceptance_criteria>
  <done>match-results architecture doc updated with snapshot + guard sections; contract.md untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update `docs/roster/architecture.md` with Phase 12.3 helper extraction + D-G guards (C8)</name>
  <files>docs/roster/architecture.md</files>
  <read_first>
    - docs/roster/architecture.md (full file)
    - .planning/phases/12.3-mmr-rating-snapshot/12.3-05-SUMMARY.md (the exact changes to document)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-D-01 through D-D-04 + D-G-01 through D-G-03
  </read_first>
  <action>
    Edit `docs/roster/architecture.md`. Targeted additions only.

    **Edit 1 — Reducer reference table (or Reducer Flows section) — Phase 12.3 additions:**
    - `batch_upsert_characters`: Add a note that the body is now a thin wrapper delegating to `applyBatchUpsert` from `spacetimedb/src/helpers/rosterMutations.ts`. Also note the new D-G-01 lobby guard (rejects when target account has an active LobbyMemberAccount).
    - `batch_remove_characters`: Same — wrapper + D-G guard.
    - `migrate_roster`: Note the D-D-03 helper-based implementation (calls `applyBatchUpsert` for copy mode + additionally `applyBatchRemove` for move mode), the D-D-04 latent bug fix (previously never recomputed accountRating on either account), and the D-G-01 guard on both source and target.
    - `set_active_hsr_account`: Add a new D-G-01 guard note (WIDE interpretation — rejects when target OR caller's currently-active account is bound to a live LobbyMemberAccount).

    **Edit 2 — New subsection "Phase 12.3: rosterMutations.ts helpers and lobby guards":**
    ```markdown
    ### Phase 12.3: rosterMutations.ts helpers and lobby guards

    New file `spacetimedb/src/helpers/rosterMutations.ts` exports two helpers that
    centralize the "mutate characters + recompute accountRating" sequence previously
    duplicated across `batch_upsert_characters`, `batch_remove_characters`, and
    `migrate_roster`:

    - `applyBatchUpsert(ctx, accountId, items, actingUserId)` — validates items against
      HsrCharacter + eidolon 0..6, upserts `HsrAccountCharacter` rows via composite-PK
      delete+insert with audit columns, then calls `updateAccountRating`.
    - `applyBatchRemove(ctx, accountId, names, actingUserId)` — validates all names
      exist, deletes them, then calls `updateAccountRating`.

    Both helpers take pre-validated inputs (Pitfall 5): the calling reducer owns
    auth and account-ownership checks; the helper owns content validation and the
    mutate+recompute sequence. `migrate_roster` previously did the mutations inline
    without calling `updateAccountRating`, leaving both source and target with stale
    ratings until another mutation triggered a recompute — D-D-04 closed this latent bug.

    **D-G-01 lobby guards** (defense-in-depth UX layer on top of the Phase 12.3 MMR snapshot):
    Four reducers reject when the caller has an active `LobbyMemberAccount` binding:
    - `set_active_hsr_account` — WIDE predicate: rejects when the target account OR
      any of the caller's currently-active accounts is bound to a lobby.
    - `batch_upsert_characters`, `batch_remove_characters` — NARROW predicate:
      rejects when the specific target `hsrAccountId` is bound to a lobby.
    - `migrate_roster` — rejects when EITHER source or target is bound to a lobby.

    All four reuse the existing `LobbyMemberAccount.by_account` index; no new index
    was introduced. Error messages cite the conflicting lobby's `joinCode` (fallback:
    lobby id). The guards exist for UX clarity — the snapshot from Phase 12.3's D-A
    capture already makes the MMR system correct even if every guard were bypassed
    by an admin reducer.
    ```

    **Edit 3 — Phase History table row:**
    ```markdown
    | Phase 12.3 | Helper extraction (rosterMutations.ts with applyBatchUpsert / applyBatchRemove), migrate_roster rating-recompute fix (D-D-04 latent bug), four D-G lobby guards on set_active / batch_upsert / batch_remove / migrate_roster |
    ```

    **Do NOT modify `docs/roster/contract.md`** (C9).
  </action>
  <verify>
    <automated>grep -c "applyBatchUpsert" docs/roster/architecture.md | grep -qE "^[1-9]" && grep -c "D-G\|rosterMutations" docs/roster/architecture.md | grep -qE "^[1-9]" && grep -c "Phase 12.3" docs/roster/architecture.md | grep -qE "^[2-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/roster/architecture.md` contains at least one `applyBatchUpsert` reference
    - `docs/roster/architecture.md` contains at least one `rosterMutations.ts` reference
    - `docs/roster/architecture.md` contains the `### Phase 12.3:` subsection header
    - `docs/roster/architecture.md` Phase History has a new Phase 12.3 row
    - `docs/roster/contract.md` is UNCHANGED (`git diff docs/roster/contract.md` is empty)
  </acceptance_criteria>
  <done>roster architecture doc updated; contract.md untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update `docs/tournament/architecture.md` with the D-H finalize-ordering-guard (C8) and backfill `.planning/REQUIREMENTS.md` with MMR-RACE-01/02 + ROST-GUARD-01</name>
  <files>docs/tournament/architecture.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/tournament/architecture.md (full file)
    - .planning/REQUIREMENTS.md (current body + Traceability table — confirm the three new IDs are not already present)
    - .planning/phases/12.3-mmr-rating-snapshot/12.3-04-SUMMARY.md (D-H guard details)
    - .planning/phases/12.3-mmr-rating-snapshot/CONTEXT.md decisions D-H-01 through D-H-05
  </read_first>
  <action>
    **Part A — `docs/tournament/architecture.md` (C8 addition).**

    Targeted additions only:

    **Edit A1 — Reducer reference:** Add a note on `finalize_match_result` (if the doc has a per-reducer table):
    - "Phase 12.3 (D-H-01): Rejects tournament-controlled matches while the parent tournament stage is not `Completed` or `Cancelled`. Unconditional (`countTowardsMmr` not consulted). Preserves MMR batch processing and bracket rollback capability."

    **Edit A2 — New subsection:**
    ```markdown
    ### Phase 12.3: finalize_match_result tournament-ordering guard

    A stage guard rejects tournament-controlled match finalization until the parent
    tournament reaches `Completed` or `Cancelled`. Without this guard, an admin
    finalizing an individual MMR-tournament match mid-tournament would silently lose
    the match's MMR input — `runFinalization` step 18 unconditionally deletes
    `MatchResultRecord` + `MatchResultParticipant` rows, which is exactly the data
    `process_tournament_mmr` reads at tournament-end.

    The guard is unconditional:
    - **MMR tournaments** (`countTowardsMmr: true`): blocked until terminal stage →
      `process_tournament_mmr` runs against surviving MRP rows → individual
      `finalize_match_result` calls succeed and clean up the ephemeral data.
    - **Casual tournaments** (`countTowardsMmr: false`): blocked until terminal stage
      → per-match `finalize_match_result` succeeds (no MMR step because
      `process_tournament_mmr` rejects with `countTowardsMmr=false`). The guard still
      applies to preserve bracket rollback capability, which applies to casual
      tournaments too.
    - **Non-tournament matches** (`isTournamentControlled: false`): unaffected;
      finalize freely once validated.

    Bracket rollback itself is NOT implemented in Phase 12.3 — the guard makes it
    possible by keeping ephemeral data alive until tournament end. Implementing
    "invalidate this bracket match and redo downstream" is a future phase.

    Tournament derivation (`BracketMatch → Tournament.tournamentId`) reuses the same
    path as the existing authority check at `matchFinalization.ts:39-50`. Defensive
    handling: an MRR with `isTournamentControlled: true` but missing `bracketMatchId`
    is rejected with the same error message (Pitfall 4 resolution).

    Cross-reference: see `docs/match-results/architecture.md` Phase 12.3 section for
    the full MMR snapshot lifecycle context.
    ```

    **Edit A3 — Phase History row:**
    ```markdown
    | Phase 12.3 | finalize_match_result tournament-ordering guard (D-H-01): unconditional rejection of tournament-controlled finalize until tournament stage is Completed or Cancelled, preserves MMR batch processing and bracket rollback window |
    ```

    **Part B — `.planning/REQUIREMENTS.md` backfill.**

    The three Phase 12.3 requirement IDs live in ROADMAP.md's Phase 12.3 header but NOT in REQUIREMENTS.md's body or traceability table. Fix this.

    **Edit B1 — Add a new subsection.** Locate the `### MMR System` section (currently lines 79-86 with MMR-01 through MMR-07). After the MMR-07 row, add a new subsection:

    ```markdown
    ### MMR Race-Condition Fixes

    - [x] **MMR-RACE-01**: ELO delta uses rating value at match start, not finalize time (snapshot captured inline at `start_draft` MRP insert loop; `processMatchMmr` reads `MatchResultParticipant.accountRatingSnapshot` directly)
    - [x] **MMR-RACE-02**: `processMatchMmr` path-independent across standalone-ranked (`runFinalization` step 11) and tournament-batch (`process_tournament_mmr`) execution; both paths read the same persisted snapshot
    ```

    **Edit B2 — Add ROST-GUARD-01 under the existing `### Roster Management` section.** After the ROST-08 row, add:

    ```markdown
    - [x] **ROST-GUARD-01** *(added Phase 12.3)*: `set_active_hsr_account`, `batch_upsert_characters`, `batch_remove_characters`, and `migrate_roster` reject with a clear error identifying the conflicting lobby when the caller has an active `LobbyMemberAccount` binding. UX defense-in-depth layered on top of the Phase 12.3 MMR snapshot.
    ```

    **Edit B3 — Extend the Traceability table.** Find the Traceability table (starts around line 182) and add three rows at the end:

    ```markdown
    | MMR-RACE-01 | Phase 12.3 | Complete |
    | MMR-RACE-02 | Phase 12.3 | Complete |
    | ROST-GUARD-01 | Phase 12.3 | Complete |
    ```

    **Edit B4 — Update the coverage footer.** The file currently ends with:
    ```
    **Coverage:**
    - v0.5 requirements: 86 total
    - Mapped to phases: 83
    - Out of scope: 3 (TEAM-01, TEAM-02, TEAM-03)
    - Unmapped: 0
    ```
    Update the counts:
    - v0.5 requirements: `89 total` (86 + 3 new)
    - Mapped to phases: `86` (83 + 3)
    Keep "Out of scope" and "Unmapped" unchanged.

    **Edit B5 — Update the footer date line:**
    ```
    *Last updated: 2026-04-11 — Added MMR-RACE-01, MMR-RACE-02, ROST-GUARD-01 (Phase 12.3 backfill)*
    ```
    Replace the existing `*Last updated: ...*` line with this.

    **Do NOT modify `docs/tournament/contract.md`** (C9).
  </action>
  <verify>
    <automated>grep -c "MMR-RACE-01" .planning/REQUIREMENTS.md | grep -qE "^[2-9]" && grep -c "MMR-RACE-02" .planning/REQUIREMENTS.md | grep -qE "^[2-9]" && grep -c "ROST-GUARD-01" .planning/REQUIREMENTS.md | grep -qE "^[2-9]" && grep -c "Phase 12.3" docs/tournament/architecture.md | grep -qE "^[2-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/tournament/architecture.md` contains at least 2 `Phase 12.3` references
    - `docs/tournament/architecture.md` contains the subsection header `### Phase 12.3: finalize_match_result tournament-ordering guard`
    - `docs/tournament/architecture.md` contains the cross-reference `docs/match-results/architecture.md`
    - `.planning/REQUIREMENTS.md` contains at least 2 occurrences each of `MMR-RACE-01`, `MMR-RACE-02`, `ROST-GUARD-01` (body subsection + traceability table row)
    - `.planning/REQUIREMENTS.md` Traceability table has three new rows mapping each ID to Phase 12.3
    - `.planning/REQUIREMENTS.md` coverage footer shows `89 total` and `86` mapped
    - `.planning/REQUIREMENTS.md` footer `Last updated` line references 2026-04-11 and mentions the backfill
    - `docs/tournament/contract.md` is UNCHANGED
    - `docs/match-results/contract.md`, `docs/roster/contract.md` are UNCHANGED
  </acceptance_criteria>
  <done>Tournament architecture doc updated; REQUIREMENTS.md backfilled; all three contract.md files untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Run the full test suite as a phase-level verification gate and record the results</name>
  <files>(none — this task only runs commands and collects output for the user to triage)</files>
  <action>Run npm run test:typecheck followed by npm run test:all. Capture the full output. For each failure, classify as test-logic / backend-bug / env-issue per the UAT skill. Do NOT modify any test file to make failures pass (C10). Report the results to the user and wait for approval before proceeding.</action>
  <verify><automated>npm run test:typecheck &amp;&amp; npm run test:all</automated></verify>
  <done>Full test suite run completed, results cataloged in the format specified by the UAT skill, any failures triaged and presented to the user, user has approved phase closure OR directed follow-up work.</done>
  <what-built>
    Tasks 1-3 updated three architecture.md files and backfilled REQUIREMENTS.md. All Phase 12.3 code changes have been committed across Plans 01-07. This task runs the full test suite as a final gate and records the pass/fail status for the user's phase sign-off.
  </what-built>
  <how-to-verify>
    **Step 1 — Run the full test suite:**
    ```bash
    npm run test:typecheck   # must exit 0
    npm run test:all         # unit + integration; this is the comprehensive gate
    ```

    **Step 2 — Capture the exit code and the test summary.** If any tests fail, run them individually and capture the failure output verbatim. Do NOT modify any test file to make a failing test pass (C10).

    **Step 3 — Classify each failure** using the categories from `.claude/skills/uat/SKILL.md`:
    - `test-logic` — test has a bug (wrong assertion, stale fixture, race condition)
    - `backend-bug` — backend behavior doesn't match the spec; test is correct
    - `env-issue` — connection/env/database problem

    Present the results in the format specified by the UAT skill:
    ```
    ## Test Run Results — Phase 12.3 close-out — <date>

    ### Failures (<count>)
    | Test | Spec Scenario | Failure | Category |
    |------|---------------|---------|----------|
    | ... | ... | expected vs actual | test-logic / backend-bug / env-issue |

    ### Errors (<count>)
    <stack traces / connection errors>

    ### Passing (<count>)
    <summary only>
    ```

    **Step 4 — Special attention to Pitfall 9 / C10:**
    If `test/backend/match-session/draft-classic.test.ts` or `post-draft.test.ts` contain failures that did NOT exist before Phase 12.3, they may have encoded the OLD isActive-based auto-pick behavior. Report each such failure EXPLICITLY with the note "possible pre-existing bug encoding — user review required". The user will triage.

    **Step 5 — Reply with:**
    1. The full test results block in the format above
    2. `git status` showing only the files explicitly listed in Plans 01-08 as modified (no accidental touches)
    3. A phase summary statement: either "ALL GREEN — ready for /gsd-verify-work" OR "<N> failures flagged for user triage, phase execution complete but verification pending"

    **Do NOT proceed with `git commit` of this plan's changes until the user approves the test results.** The user will decide whether any failing tests indicate a true regression that must be fixed before Phase 12.3 can be marked complete, or whether they're pre-existing bug encodings that deserve their own follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" after the user reviews the test results. If the user asks to fix failing tests, create a new task — do not silently modify tests.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.3-28 | Documentation drift | `docs/*/architecture.md` falls out of sync with code | mitigate | C8 mandates architecture-doc updates on every backend change. Tasks 1-3 explicitly document each Phase 12.3 change. Cross-references tie the three docs together. |
| T-12.3-29 | Traceability gap | Phase 12.3 requirement IDs live only in ROADMAP.md, making post-hoc audits incomplete | mitigate | Task 3 Part B backfills REQUIREMENTS.md. The Traceability table now maps every Phase 12.3 ID. |
| T-12.3-30 | Contract drift | `docs/*/contract.md` updated during execution | mitigate | C9 hard rule enforced in every task in this plan. Tasks 1-3 explicitly verify `git diff docs/*/contract.md` is empty. Contract updates go through `/gsd-verify-work` post-execution. |
</threat_model>

<verification>
- Grep-level invariants on each architecture.md edit
- Grep-level invariants on REQUIREMENTS.md backfill
- `npm run test:typecheck` exits 0
- `npm run test:all` full-suite result captured and triaged
- `git status` shows only the files explicitly listed in Plans 01-08
</verification>

<success_criteria>
- Three architecture.md files updated with Phase 12.3 additions
- REQUIREMENTS.md contains MMR-RACE-01, MMR-RACE-02, ROST-GUARD-01 in body + traceability table
- Coverage footer updated
- Three `docs/*/contract.md` files UNCHANGED (C9)
- Full test suite run recorded
- User approves test results before phase is marked complete
</success_criteria>

<output>
Create `.planning/phases/12.3-mmr-rating-snapshot/12.3-08-SUMMARY.md` with:
- Diff summaries of each architecture.md edit
- Diff of REQUIREMENTS.md
- Full test suite result (verbatim)
- Any failures flagged for user triage (categorized)
- Confirmation all three `docs/*/contract.md` files are UNCHANGED
- Phase completion status (GREEN vs PENDING-TRIAGE)
</output>
