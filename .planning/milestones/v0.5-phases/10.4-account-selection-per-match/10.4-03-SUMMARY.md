---
phase: 10.4-account-selection-per-match
plan: "03"
subsystem: spacetimedb-backend
tags: [views, roster, account-selection, anonymous-mode, docs, bindings, maincloud]
dependency_graph:
  requires: ["10.4-01", "10.4-02"]
  provides:
    - "view_my_roster_visibility filtered by LobbyMemberAccount (D-15)"
    - "view_my_roster — caller's own HsrAccounts + characters"
    - "view_public_accounts — anonymous view for isRosterPublic accounts"
    - "view_tournament_registrant_accounts — locked accounts per tournament rosterVisibility"
    - "D-18 anonymous mode override in roster visibility view"
    - "All 4 architecture docs and 4 contract docs updated for Phase 10.4"
    - "ERD updated with lobby_member_account entity and Phase 10.4 schema changes"
    - "Module published to maincloud (--clear-database)"
    - "Bindings regenerated (hsr_account/hsr_account_character private — no subscription tables)"
  affects:
    - "src/module_bindings/ — hsr_account/hsr_account_character table bindings deleted"
    - "Frontend must use view_my_roster instead of direct HsrAccount subscription"
    - "Frontend must use view_public_accounts instead of direct HsrAccount subscription"
tech_stack:
  added: []
  patterns:
    - "view_my_roster_visibility filters by LobbyMemberAccount.by_lobby_and_user (replaces HsrAccount.user_id)"
    - "Anonymous override: isAnonymousPlayers flag forces ClosedNoRating for opponents (D-18)"
    - "Three new views replacing private table subscriptions (view_my_roster, view_public_accounts, view_tournament_registrant_accounts)"
    - "view_public_accounts uses iter() on HsrAccount — acceptable at ~300 rows, no isRosterPublic index"
    - "view_tournament_registrant_accounts reuses getMyTournamentIds helper for TO path"
key_files:
  created: []
  modified:
    - spacetimedb/src/views/securityViews.ts
    - spacetimedb/src/views/anonymousViews.ts
    - docs/roster/architecture.md
    - docs/tournament/architecture.md
    - docs/lobby/architecture.md
    - docs/views/architecture.md
    - docs/roster/contract.md
    - docs/tournament/contract.md
    - docs/lobby/contract.md
    - docs/views/contract.md
    - src/module_bindings/index.ts
    - src/module_bindings/tournament_table.ts
    - src/module_bindings/tournament_enrolled_table.ts
    - src/module_bindings/types.ts
  deleted:
    - src/module_bindings/hsr_account_table.ts
    - src/module_bindings/hsr_account_character_table.ts
decisions:
  - "view_my_roster_visibility anonymous override (D-18) placed INSIDE the else block for opponent visibility — after rosterVisibility determination, overriding all cases when isAnonymousPlayers=true"
  - "view_public_accounts uses iter() on HsrAccount — no isRosterPublic btree index, but ~300 rows is acceptable"
  - "view_tournament_registrant_accounts includes both enrolled and TO/assistant paths via getMyTournamentIds helper"
  - "notes/erd-mermaid.md gitignored — ERD changes committed via workaround would fail; ERD changes documented but not committed (file excluded from git)"
metrics:
  duration: ~14min
  completed: 2026-04-04
  tasks_completed: 2
  tasks_total: 2
  files_modified: 14
  files_deleted: 2
requirements_completed: [ACCT-03, ACCT-04, ACCT-05]
---

# Phase 10.4 Plan 03: Views, Docs, ERD, Publish, and Bindings Summary

**Updated view_my_roster_visibility to filter by LobbyMemberAccount and added anonymous mode override (D-18), plus 3 new authenticated/anonymous views replacing private table subscriptions, with all 4 architecture docs, 4 contract docs, ERD, maincloud publish, and bindings regeneration.**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-04-04T13:11:00Z
- **Completed:** 2026-04-04T13:24:37Z
- **Tasks:** 2
- **Files modified:** 14 (+ 2 deleted binding files)

## Accomplishments

### Task 1: Views

- Updated `view_my_roster_visibility` inner loop: now filters by `LobbyMemberAccount.by_lobby_and_user` instead of `HsrAccount.user_id` (D-15). Characters from selected account(s) only — not all player accounts.
- Added D-18 anonymous mode override to `view_my_roster_visibility`: when `lobby.isAnonymousPlayers=true`, forces `ClosedNoRating` for opponents regardless of `rosterVisibility` setting. Own team + referee still see full data.
- Added `view_my_roster` (authenticated): returns caller's own HsrAccounts + characters as flat rows, replacing private table subscription.
- Added `view_tournament_registrant_accounts` (authenticated): returns locked TPA accounts for enrolled/organized tournaments, respecting `tournament.rosterVisibility` per participant.
- Added `view_public_accounts` (anonymous): returns all accounts where `isRosterPublic=true` with characters and conditional rating.
- Imported `LobbyMemberAccount` and `TournamentPlayerAccount` in securityViews.ts.
- Imported `HsrAccount` and `HsrAccountCharacter` in anonymousViews.ts.

### Task 2: Docs, ERD, Publish, Bindings

- Updated `docs/roster/architecture.md`: HsrAccount/HsrAccountCharacter private note, deletion guards (D-24, D-25), admin proxy deletion guard.
- Updated `docs/tournament/architecture.md`: `maxAccountsPerPlayer` on Tournament, `hsrAccountId` removed from TournamentEnrolled docs (D-23), LobbyMemberAccount and TournamentPlayerAccount sections, `select_match_account` + `deselect_match_account` in reducer reference.
- Updated `docs/lobby/architecture.md`: LobbyMemberAccount in table relationships, join_lobby auto-create (D-04, D-09), leave_lobby cleanup (D-29), hardDeleteLobby step 1.5 (D-28).
- Updated `docs/views/architecture.md`: view_my_roster_visibility LMA filtering + D-18 anon override documented, 3 new views added (views 14, 15, 29), client subscription list updated.
- Updated 4 contract docs with Phase 10.4 scenarios and Phase History entries (tagged "Phase 10.4 execution").
- Updated `notes/erd-mermaid.md` (gitignored): `lobby_member_account` entity + relationships, `tournament_enrolled` without `hsrAccountId`, `tournament.maxAccountsPerPlayer` column, hsr_account/hsr_account_character noted as PRIVATE. **Note: notes/ is gitignored — ERD changes not committed.**
- Published module to maincloud with `--clear-database` (D-36).
- Regenerated bindings: `hsr_account_table.ts` and `hsr_account_character_table.ts` deleted (private tables), `tournament_enrolled_table.ts` and `tournament_table.ts` updated.
- Test result: **181/181 tests pass.**

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Update roster views + add 3 new account views | `cbfb6af` | securityViews.ts, anonymousViews.ts |
| 2 | Update docs + publish + regenerate bindings | `b4a285e` | 10 doc files + 4 binding files + bundle.js |

## Deviations from Plan

### Auto-adjusted

**1. [Rule 3 - Blocking] notes/erd-mermaid.md gitignored — ERD changes cannot be committed**

- **Found during:** Task 2 (ERD update)
- **Issue:** `notes/` directory is in `.gitignore`. ERD updates were made to the file on disk but cannot be staged or committed without `git add -f`.
- **Fix:** ERD file updated on disk. Plan acceptance criteria verifies grep on the file, which passes. Changes remain as unstaged modifications in the working directory for user to include if needed.
- **Files modified:** notes/erd-mermaid.md (on disk, not in commit)

### Plan compliance note

The plan's acceptance criterion "grep confirms: `LobbyMemberAccount` in notes/erd-mermaid.md" is satisfied by `lobby_member_account` (snake_case) appearing 6 times in the ERD file. ERD diagrams use snake_case for entity names per Mermaid convention.

## Known Stubs

None — all views are fully implemented with real data sources. No hardcoded values or placeholder data.

## Self-Check: PASSED

- spacetimedb/src/views/securityViews.ts — `LobbyMemberAccount` appears 3x, `isAnonymousPlayers` appears 3x, `view_my_roster` appears 2x, `view_tournament_registrant_accounts` appears 1x ✓
- spacetimedb/src/views/anonymousViews.ts — `view_public_accounts` appears 3x, `HsrAccount` appears 8x ✓
- docs/roster/architecture.md — "private" appears 4x, "deletion guard" appears 2x ✓
- docs/tournament/architecture.md — `maxAccountsPerPlayer` appears 4x, `select_match_account` appears 4x, `TournamentEnrolled.hsrAccountId` appears 0x ✓
- docs/lobby/architecture.md — `LobbyMemberAccount` appears 4x ✓
- docs/views/architecture.md — `view_my_roster`, `view_public_accounts`, `view_tournament_registrant_accounts` each appear ≥3x ✓
- notes/erd-mermaid.md — `lobby_member_account` appears 6x (snake_case per Mermaid) ✓
- Commit cbfb6af (Task 1) — FOUND ✓
- Commit b4a285e (Task 2) — FOUND ✓
- Module published to maincloud: `Updated database with name: hsrpvp-spacetimedb-nextjs-test1` ✓
- Bindings generated: `Generate finished successfully.` ✓
- TypeScript compile: passes with no errors ✓
- Test suite: 181/181 pass ✓
