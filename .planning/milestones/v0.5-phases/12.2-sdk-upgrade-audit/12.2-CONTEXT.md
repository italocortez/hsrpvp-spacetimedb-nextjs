# Phase 12.2: SDK Upgrade Audit (2.0.3 → 2.1.0) - Context

**Gathered:** 2026-04-09 (updated)
**Status:** Ready for planning

<domain>
## Phase Boundary

Audit the SpacetimeDB SDK upgrade from 2.0.3 to 2.1.0. Export all views for proper registration, regenerate client bindings with view support, rework the frontend auth flow to use typed view data, disable confirmed reads, add error handling to touched reducer calls, and verify no regressions in existing tests or behavior.

</domain>

<decisions>
## Implementation Decisions

### View Export Migration
- **D-01:** Export all 31 views — 23 from `securityViews.ts` + 8 from `anonymousViews.ts`. One-time migration, no partial rollout.
- **D-02:** Export pattern: `export const view_xxx = spacetimedb.view(...)` (same as proven `view_my_profile` POC).
- **D-03:** Re-export all views from `index.ts` module entry point.
- **D-04:** Regenerate client bindings after export. Verify all 31 views appear in `tablesSchema` with typed accessors.
- **D-05:** Verify all views appear in `st_view` system table after publish.

### Auth Flow Rework
- **D-06:** Replace all raw SQL subscription strings with typed view bindings wherever possible. `conn.db.view_my_profile` instead of `SELECT * FROM view_my_profile`.
- **D-07:** Views are the data source for private fields (UserPrivate). Do NOT use `useTable` where view data is needed — views provide the merged User + UserPrivate data that tables can't.
- **D-08:** Keep the view-based subscription pattern for `view_my_profile` and `view_my_identity`. These are the primary data sources for auth state.
- **D-09:** Profile resolution strategy: read from typed `conn.db.view_my_profile` binding instead of `conn.db.User`. The 3-fallback resolution (localStorage cache, guest username, session name) stays but targets view data.
- **D-10:** Populate `discordId`/`discordUsername` on the frontend `User` type from `view_my_profile` view data (was always `undefined` before because view wasn't registering).

### Phase 12 Architecture Provenance (DO NOT DISCARD)

**Why views exist as the auth data layer (Phase 12 D-01/D-04):**
- `discordId`, `discordUsername`, `email` were moved from public `User` table to private `UserPrivate` table (`public: false`) for security. Clients cannot subscribe to private tables.
- `view_my_profile` is the ONLY way the frontend accesses private fields — it merges User + UserPrivate server-side, filtered to `ctx.sender` only.
- `hasDiscordLinked` boolean on User replaced `discordId` comparison for public badge display (Phase 12 D-02). The actual IDs live in UserPrivate.

**Why SQL subscriptions for views (Phase 12 Research, Pitfall 6):**
- Views require explicit SQL subscription (`SELECT * FROM view_xxx`). `useTable(tables.ViewName)` does NOT subscribe to views — it only works for tables.
- After exporting all views with typed bindings (this phase), verify whether `conn.db.view_xxx` provides a subscription mechanism. If typed bindings offer a subscription API, migrate to that. If not, keep SQL subscriptions but use typed accessors for reading data.

**Why 3-fallback profile resolution (Phase 12 Research):**
- Strategy 1 (PK lookup): Cached userId from localStorage — fast path for returning users
- Strategy 2 (index lookup): Guest username via identity hex — handles fresh guest connections with no cache
- Strategy 3 (index lookup): NextAuth session name — handles Discord-linked users where guest username doesn't match
- These aren't redundant. Each covers a different connection recovery path. Do not collapse them.

**Why UserIdentity is private (Phase 12 D-14):**
- UserIdentity maps SpacetimeDB identity → userId. Made private so other clients can't see who is mapped to what identity.
- Frontend switched from `useTable(tables.UserIdentity)` to `view_my_identity` SQL subscription.
- Test harness was impacted: `subscribeToAllTables()` excludes private tables, so tests must explicitly subscribe to `view_my_identity` (Phase 12 Research, Pitfall 3).

**Why ephemeral connection for Discord linking (Phase 12 D-09/D-10):**
- Client sends SpacetimeDB auth token (not identity hex) to API route. API creates short-lived DbConnection using that token, extracts server-verified identity from `onConnect`. Eliminates trusting client-supplied identity hex.
- WR-02: `onDisconnect` must call `reject()` to fail fast instead of hanging.

### Confirmed Reads
- **D-11:** Disable confirmed reads. Add `.withConfirmedReads(false)` to the connection builder in `app/providers.tsx`. Revert to pre-2.1.0 behavior to avoid latency increase.

### Error Handling
- **D-12:** Add `_then()` error callbacks to all frontend reducer call sites touched during the auth refactor. Not a full codebase audit — scoped to files already being modified.
- **D-13:** `_then()` pattern: log failure, surface error to user where appropriate. No retry logic unless the specific reducer warrants it.

### Reducer & Test Audit
- **D-14:** Run full backend test suite against SDK 2.1.0. Fix any failures.
- **D-15:** Proactively audit reducers for patterns that relied on 2.0.3-specific behavior (subscription semantics, isReady timing, event delivery order). Not just "fix what breaks" — actively look for 2.0.3 assumptions.
- **D-16:** Review v2.0.4/v2.0.5/v2.1.0 changelogs for breaking changes affecting our patterns. Cross-reference identified changes against codebase usage.

### Subscription Cleanup
- **D-17:** Remove dead `view_my_identity` SQL subscription if now accessible via typed binding after export.
- **D-18:** Audit all remaining SQL string subscriptions across the frontend. Replace with typed view bindings where a matching exported view exists.

### Claude's Discretion
- Internal helper organization during view export (batch vs individual exports)
- Exact error messages in `_then()` callbacks
- Order of view re-exports in `index.ts`
- Whether to group anonymous views separately in re-exports or mix with security views

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### View Definitions (need export)
- `spacetimedb/src/views/securityViews.ts` — 23 views (only view_my_profile currently exported)
- `spacetimedb/src/views/anonymousViews.ts` — 8 views (none exported, uses anonymousView() pattern)
- `spacetimedb/src/index.ts` — Module entry point (only 1 view re-exported, line 10)

### Frontend Auth
- `components/features/auth/hooks/useAuth.ts` — Main auth hook. Raw SQL subscriptions (lines 20-40), 3-fallback profile resolution (lines 97-142), table callbacks (lines 45-68), Discord linking (lines 158-245)
- `components/features/auth/types.ts` — User interface with discordId/discordUsername optional fields
- `components/features/auth/components/AuthProvider.tsx` — Context wrapper
- `app/providers.tsx` — Connection builder (add .withConfirmedReads(false) here)
- `lib/session-cookie.ts` — stdb_session cookie for SSR display name

### Generated Bindings
- `src/module_bindings/index.ts` — Generated module entry (currently only view_my_profile in tablesSchema)
- `src/module_bindings/view_my_profile_table.ts` — Generated view row type (POC)
- `src/module_bindings/types.ts` — MyProfileRow type

### Skill Reference
- `.claude/skills/spacetimedb/SKILL.md` — Subscription pitfalls, _then() error handling, useTable/useReducer patterns
- `.claude/skills/spacetimedb/references/api-guide.md` § 8 — React integration patterns

### Phase 12 Context (MUST READ — explains WHY auth is structured this way)
- `.planning/phases/12-auth-security-hardening/12-CONTEXT.md` — Original auth rework decisions (D-01 through D-18, CR-01, WR-01, WR-02)
- `.planning/phases/12-auth-security-hardening/12-RESEARCH.md` — Architecture patterns, pitfalls, impact audit. Explains WHY views are the auth data layer, WHY SQL subscriptions, WHY 3-fallback resolution. §Impact Audit Summary lists every file referencing discordId.
- `docs/auth/architecture.md` — Current auth architecture docs
- `docs/auth/contract.md` — Auth behavior specs

### Exploration Proof
- Commit `20ffbd3` — POC: exported view_my_profile, verified st_view registration, typed bindings generated, conn.db.view_my_profile accessible

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **view_my_profile POC pattern:** Proven export pattern in `securityViews.ts` line 217 — replicate for all 31 views
- **tablesSchema generation:** `spacetime generate` already handles views alongside tables — no custom codegen needed
- **AuthProvider context:** Existing provider tree in `app/providers.tsx` wraps SpacetimeDB connection — single place for `.withConfirmedReads(false)`

### Established Patterns
- **View definition:** `spacetimedb.view()` for authenticated views, `spacetimedb.anonymousView()` for public views
- **Subscription:** Currently raw SQL strings (`SELECT * FROM view_xxx`) — migrating to typed bindings
- **Profile resolution:** 3-strategy fallback (localStorage PK, guest username, session name) — keep but retarget to view data
- **Table callbacks:** `User.onInsert/onUpdate` with tag filtering (`Reducer` or `Transaction`) — may need to target view table after migration

### Integration Points
- **useAuth.ts ↔ view_my_profile:** Primary auth state source. Subscription must fire `onApplied` for `setProfileReady(true)`
- **useAuth.ts ↔ view_my_identity:** Identity mapping. Currently dead (not exported). Will work after export migration.
- **providers.tsx ↔ DbConnection.builder():** Connection config including confirmed reads setting
- **module_bindings/ ↔ frontend:** All typed view accessors flow from `spacetime generate` output

</code_context>

<specifics>
## Specific Ideas

- Views are the data source for private fields — don't use `useTable` where view data is needed
- The existing exploration commit (`20ffbd3`) proved the full pipeline: export → publish → generate → typed access
- Skill docs already corrected for v2.1.0 — no more "views have no bindings" stale guidance

</specifics>

<deferred>
## Deferred Ideas

- Replace manual `conn.db.User.onInsert/onUpdate` with `useTable` callbacks across the full app — future phase (not all data comes from tables)
- Replace `conn.reducers.loginAsGuest({})` with `useReducer` hook pattern — future phase
- Full codebase `_then()` error handling audit for all reducer calls outside auth — separate phase

None — discussion stayed within phase scope

</deferred>

---

*Phase: 12.2-sdk-upgrade-audit*
*Context gathered: 2026-04-09*
