# Phase 12.2: SDK Upgrade Audit (2.0.3 → 2.1.0) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-09
**Phase:** 12.2-sdk-upgrade-audit
**Areas discussed:** View export scope, Auth refactor depth, Confirmed reads default, Error handling scope, Reducer/test rework scope

---

## View Export Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Export all 31 (Recommended) | One-time migration. Every view gets typed bindings. Frontend can subscribe to any view without code changes later. | ✓ |
| Export only consumed views | Export ~5-6 views that useAuth.ts and existing pages actually subscribe to. Add others when frontend features need them. | |
| You decide | Claude picks the approach during planning based on effort assessment | |

**User's choice:** Export all 31
**Notes:** Clean one-time migration preferred over incremental approach.

---

## Auth Refactor Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal — typed profile only | Swap view_my_profile from raw SQL to typed binding. Keep existing onInsert/onUpdate callbacks, keep other SQL subscriptions as-is. | |
| Moderate — all subscriptions typed | Replace ALL raw SQL subscriptions with typed view bindings. Keep onInsert/onUpdate callback pattern but target view tables. | |
| Full refactor — useTable hooks | Adopt useTable/useReducer hook patterns from skill guide. Replace callbacks with reactive hooks. | |

**User's choice:** Custom — use typed view bindings everywhere possible, but don't use useTable where view data is needed. Views are the data source for private fields (UserPrivate), not the table directly.
**Notes:** User clarified that not everything can use useTable because some components need UserPrivate table data that comes from the view, not the table. The refactor consists of using view bindings now wherever possible.

---

## Confirmed Reads Default

| Option | Description | Selected |
|--------|-------------|----------|
| Keep ON (Recommended) | More reliable data delivery. Minimal latency increase at ~100 concurrent users. No code change needed. | |
| Disable it | Revert to pre-2.1.0 behavior. Add .withConfirmedReads(false) to connection builder in providers.tsx. | ✓ |
| You decide | Claude assesses during planning based on latency testing | |

**User's choice:** Disable it
**Notes:** User explicitly chose to disable despite recommendation. Latency sensitivity for game context.

---

## Error Handling Scope

| Option | Description | Selected |
|--------|-------------|----------|
| loginAsGuest + deleteGuestAccount only | The two auth reducer calls in useAuth.ts that currently fire-and-forget. Minimal, targeted. | |
| All reducer calls touched during auth refactor | Any reducer call site we're already modifying gets _then() added. Natural scope — no extra files to touch. | ✓ |
| You decide during planning | Claude adds _then() wherever it makes sense as part of the auth rework. | |

**User's choice:** All reducer calls touched during auth refactor
**Notes:** User needed clarification on what _then() is (client-side callback for reducer success/failure). Once understood, chose natural scope — add to anything already being modified.

---

## Reducer & Test Rework Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Run tests, fix only what breaks | Run full test suite against 2.1.0. Only touch reducers/tests that actually fail. No preemptive refactoring. | |
| Run tests + audit SDK-internal usage | Fix test failures AND audit reducers for patterns that relied on 2.0.3 behavior (subscription semantics, isReady timing). Proactive but scoped. | ✓ |
| Full audit with changelog review | Review all changelogs, cross-reference every breaking change against codebase, fix and refactor accordingly. | |

**User's choice:** Run tests + audit SDK-internal usage
**Notes:** Proactive approach — don't just wait for failures, actively look for 2.0.3-specific assumptions.

---

## Claude's Discretion

- Internal helper organization during view export
- Exact error messages in _then() callbacks
- Order of view re-exports in index.ts
- Whether to group anonymous views separately in re-exports

## Deferred Ideas

- Full codebase useTable migration — future phase
- useReducer hook pattern adoption — future phase
- Full codebase _then() error handling audit — separate phase
