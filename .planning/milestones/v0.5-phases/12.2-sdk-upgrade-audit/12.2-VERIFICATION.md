---
phase: 12.2-sdk-upgrade-audit
verified: 2026-04-09T12:00:00Z
status: human_needed
score: 9/10 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "All 3 reducer calls in auth have _then() error callbacks (D-12, D-13)"
    reason: "SDK 2.1.0 reducer calls return Promise<void> — _then() does not exist on the return value. Equivalent coverage achieved via .catch() on the promise. Human verification (Task 3 checkpoint) confirmed login works with no errors. Deviation is intentional and documented in 12.2-02-SUMMARY.md."
    accepted_by: "human (Task 3 checkpoint approved)"
    accepted_at: "2026-04-09"
human_verification:
  - test: "Verify login flow reads from view_my_profile and discordId/discordUsername are populated"
    expected: "Browser console shows '[useAuth] View hit: view_my_profile' on login. For Discord-linked users, discordId and discordUsername are non-null in the User context object."
    why_human: "Can't verify live view data population or Discord field presence without running the dev server and authenticating with a Discord-linked account."
  - test: "Confirm no perceived latency increase with confirmed reads disabled"
    expected: "Reducer calls (loginAsGuest, etc.) complete at pre-2.1.0 speed — no noticeable 200ms+ delay added."
    why_human: "Latency is a runtime/UX perception metric that can't be measured by static code inspection."
  - test: "Verify the 2 pre-existing flaky test failures in anonymous-labels.test.ts are not caused by view export changes"
    expected: "Failures are reproduce-able on the pre-Phase-12.2 codebase and are caused by state-bleed (missing afterAll guards), not by the export const changes."
    why_human: "Requires running the test suite and comparing failure signatures against pre-12.2 baseline. The SUMMARY claims these are pre-existing but this cannot be confirmed by static analysis alone."
---

# Phase 12.2: SDK Upgrade Audit Verification Report

**Phase Goal:** Verify that the SpacetimeDB SDK upgrade from 2.0.3 to 2.1.0 hasn't broken existing behavior — tests, subscription semantics, reducer calls, or frontend auth flow. Export all views so they register properly (only view_my_profile was proven; 23+ views remain). Confirm confirmed-reads default change doesn't degrade UX. Audit all code that relies on SDK internals or worked around SDK bugs that may now be fixed.
**Verified:** 2026-04-09T12:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 32 views (24 security + 8 anonymous) are exported and appear in st_view after publish | VERIFIED | `grep -c "export const view_"` returns 24 in securityViews.ts, 8 in anonymousViews.ts. SUMMARY reports `SELECT count(*) FROM st_view` returns 32. |
| 2 | spacetime generate produces typed view_xxx_table.ts files for all 32 views | VERIFIED | `ls src/module_bindings/ \| grep "^view_" \| wc -l` returns 32. All 32 file names confirmed present. |
| 3 | conn.db.view_xxx is accessible for every exported view in generated bindings | VERIFIED | `grep -c "view_my_lobbies\|view_my_lobby_chat\|view_admin_user_private\|view_tournament_registrant_accounts"` returns 12 hits in `src/module_bindings/index.ts` (multiple views present in tablesSchema). |
| 4 | Full test suite passes against SDK 2.1.0 with no regressions | PARTIAL | SUMMARY reports 521 passed, 8 skipped, 2 failures. The 2 failures in `anonymous-labels.test.ts` are claimed to be pre-existing state-bleed, not caused by this phase. Cannot confirm without human test run comparison. |
| 5 | No 2.0.3-specific behavior assumptions remain in the codebase | VERIFIED | No 2.0.3 version strings, SDK-specific workaround comments, or bypassed-isReady patterns found in components/. SDK changelog audit in SUMMARY reports clean. |
| 6 | Profile resolution reads from conn.db.view_my_profile.iter() as primary source (D-09) | VERIFIED | Line 117 of useAuth.ts: `const profileRows = [...conn.db.view_my_profile.iter()]`. Log string `[useAuth] View hit: view_my_profile` present at line 120. |
| 7 | discordId and discordUsername are populated on the frontend User type from view data (D-10) | VERIFIED (code path only) | Lines 104-105 of useAuth.ts map `discordId: user.discordId` and `discordUsername: user.discordUsername` from the profile row. Runtime population requires human verification with a Discord-linked account. |
| 8 | 3-fallback resolution remains for connection recovery: cached PK -> guest username -> session name (D-09) | VERIFIED | Lines 131, 145, 157 of useAuth.ts confirm `conn.db.User.id.find`, `conn.db.User.username.find` for guest, and `conn.db.User.username.find` for session name all preserved. |
| 9 | Confirmed reads are disabled -- no 200ms+ latency increase on reducer calls (D-11) | VERIFIED (code) | `app/providers.tsx` line 36: `.withConfirmedReads(false)` present in connection builder. Runtime latency perception requires human verification. |
| 10 | All 3 reducer calls in auth have _then() error callbacks (D-12, D-13) | PASSED (override) | Plan specified `._then()` with `ctx.event.status.tag === 'Failed'`, but SDK 2.1.0 returns `Promise<void>` — `_then()` does not exist. Implementation uses `.catch()` at lines 220, 313, 334 of useAuth.ts. Functionally equivalent for network-level errors. Human verification (Task 3 checkpoint) confirmed login works with no errors. Override applied. |
| 11 | SQL subscription for view_my_identity removed (D-17) | VERIFIED | `grep -n "view_my_identity" useAuth.ts` returns 0 matches. Only `'SELECT * FROM view_my_profile'` and `'SELECT * FROM user'` remain (lines 32, 38). |
| 12 | No raw SQL subscription strings remain where typed view bindings exist (D-18) | VERIFIED | `'SELECT * FROM user'` is retained intentionally (User is a table, not a view — needed for PK/username index lookups in fallback strategies). No other raw SQL strings found. |

**Score:** 9/10 truths verified (1 override applied, 1 partial pending human test run)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `spacetimedb/src/views/securityViews.ts` | 24 exported view constants | VERIFIED | `grep -c "export const view_"` = 24 |
| `spacetimedb/src/views/anonymousViews.ts` | 8 exported view constants | VERIFIED | `grep -c "export const view_"` = 8 |
| `spacetimedb/src/index.ts` | Re-exports for all 32 views | VERIFIED | `view_my_lobbies`, `view_my_tournament_group_standings`, `view_tournament_registrant_accounts`, `view_my_lobby_chat`, `view_public_accounts` all confirmed present. No `import './views/anonymousViews'` side-effect remains. |
| `src/module_bindings/index.ts` | Generated bindings with all 32 views in tablesSchema | VERIFIED | `view_my_lobbies`, `view_my_lobby_chat`, `view_tournament_registrant_accounts` confirmed present (multiple views confirmed in tablesSchema). |
| `src/module_bindings/view_*_table.ts` (32 files) | One typed binding file per view | VERIFIED | All 32 file names enumerated and confirmed. |
| `components/features/auth/hooks/useAuth.ts` | View-based auth profile resolution with error handling | VERIFIED | `conn.db.view_my_profile.iter()`, onInsert/onUpdate callbacks, removeOnInsert/removeOnUpdate cleanup, 3x `.catch()` on reducers — all confirmed present. |
| `app/providers.tsx` | Connection builder with confirmed reads disabled | VERIFIED | `.withConfirmedReads(false)` at line 36. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `spacetimedb/src/views/securityViews.ts` | `spacetimedb/src/index.ts` | named re-exports | WIRED | `export { view_my_lobbies, ..., view_tournament_registrant_accounts } from './views/securityViews'` confirmed in index.ts |
| `spacetimedb/src/views/anonymousViews.ts` | `spacetimedb/src/index.ts` | named re-exports | WIRED | `export { view_my_lobby_chat, ..., view_public_accounts } from './views/anonymousViews'` confirmed in index.ts |
| `spacetimedb/src/index.ts` | `src/module_bindings/index.ts` | spacetime generate codegen | WIRED | `view_my_lobbies` present in generated bindings; 32 `view_*_table.ts` files confirm full codegen ran |
| `components/features/auth/hooks/useAuth.ts` | `src/module_bindings/view_my_profile_table.ts` | conn.db.view_my_profile typed accessor | WIRED | `conn.db.view_my_profile.iter()` at line 117; `conn.db.view_my_profile.onInsert/onUpdate` at lines 59-60 |
| `app/providers.tsx` | `src/module_bindings/index.ts` | DbConnection.builder() configuration | WIRED | `.withConfirmedReads(false)` at providers.tsx line 36 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `useAuth.ts` | `profileRows` | `conn.db.view_my_profile.iter()` | Server-filtered view (ctx.sender) | FLOWING (code path verified; runtime requires human auth test) |
| `useAuth.ts` | `user` (fallback) | `conn.db.User.id.find()` / `conn.db.User.username.find()` | User table PK/index lookups | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — requires running dev server + authenticated session. Delegated to Human Verification Required section.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| SDK-COMPAT-01 | 12.2-01 | All existing tests pass on 2.1.0 | PARTIAL | 521 passed, 8 skipped, 2 claimed pre-existing failures. Human verification needed to confirm failures are not regressions. |
| SDK-COMPAT-02 | 12.2-01 | All views exported and registered in st_view | VERIFIED | 24 exports in securityViews.ts, 8 in anonymousViews.ts, full re-export in index.ts, SUMMARY confirms `count(*) FROM st_view = 32` |
| SDK-COMPAT-03 | 12.2-01 | View client bindings generated and accessible | VERIFIED | 32 `view_*_table.ts` files present; multiple views confirmed in `tablesSchema` of generated `index.ts` |
| SDK-COMPAT-04 | 12.2-02 | Frontend auth flow works with view data | HUMAN NEEDED | Code path verified (`view_my_profile.iter()` as primary source, `.withConfirmedReads(false)`, fallbacks preserved). Runtime auth flow approved by human in Task 3 checkpoint but verifier cannot independently confirm. |

**Note:** SDK-COMPAT requirement IDs do not appear in `.planning/REQUIREMENTS.md` — they are phase-specific requirements defined in `12.2-RESEARCH.md` and the ROADMAP `Requirements:` line, not part of the v0.5 traceability table. This is expected for SDK audit phases inserted mid-milestone.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No TODO/FIXME/HACK comments, placeholder returns, or empty implementations found in the modified files. No bare `spacetimedb.view(` or `spacetimedb.anonymousView(` calls without `export const` remain in securityViews.ts or anonymousViews.ts. No try/catch wrappers remain on reducer calls (replaced with `.catch()`).

### Human Verification Required

#### 1. Auth Flow: view_my_profile as Primary Source

**Test:** Start `npm run dev`, open http://localhost:3000, click LOGIN as a registered user
**Expected:** Browser DevTools console shows `[useAuth] View hit: view_my_profile → id=X username=Y` (not Strategy 1/2/3 fallback logs)
**Why human:** Requires live WebSocket connection and authenticated session to confirm view data flows from server

#### 2. Discord Fields Populated

**Test:** With a Discord-linked account, log in and inspect the User context via React DevTools or temporary `console.log(currentUser)`
**Expected:** `currentUser.discordId` and `currentUser.discordUsername` are non-null/non-undefined strings
**Why human:** Requires a Discord-linked account in the live database to produce non-empty view_my_profile rows with private fields

#### 3. Confirmed Reads Latency

**Test:** Compare perceived speed of loginAsGuest reducer call before and after the `.withConfirmedReads(false)` change (can use browser network tab timing or console timestamps)
**Expected:** No 200ms+ latency increase compared to pre-2.1.0 behavior; reducer callbacks arrive at pre-upgrade speed
**Why human:** Latency perception is a runtime metric; static analysis confirms the config is set but cannot measure actual timing

#### 4. Pre-existing Test Failures Confirmation

**Test:** Run `npx vitest run --config test/vitest.integration.config.ts --reporter verbose` and examine the 2 failures in `anonymous-labels.test.ts`
**Expected:** Failures show `SenderError: You are already in a lobby` — consistent with state-bleed from missing afterAll guards, not related to view export changes
**Why human:** Requires running the full 63-minute test suite and comparing failure signatures; cannot determine if failures are new regressions vs pre-existing from static analysis

### Gaps Summary

No blocking gaps found. All code artifacts are substantive and wired. The only outstanding items are human verification requirements that confirm runtime behavior:

1. **SDK-COMPAT-01 (tests):** 2 test failures in `anonymous-labels.test.ts` are claimed pre-existing by the SUMMARY. The claim is plausible (state-bleed described with specific cause), but requires a human to confirm by running the suite and examining failure details.

2. **SDK-COMPAT-04 (auth flow):** The human checkpoint (Task 3) was approved during execution, but the verifier cannot independently re-confirm the live auth flow. The code path is correct — this is a runtime confirmation need.

3. **_then() deviation (override applied):** The plan specified `._then()` error callbacks but SDK 2.1.0 requires `.catch()`. This is a legitimate API difference and the override is warranted. Human approval already occurred via Task 3 checkpoint.

---

_Verified: 2026-04-09T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
