---
phase: 09-mouse-tracking-chat-and-lobby-browser
plan: 04
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - spacetimedb/src/reducers/lobbySettings.ts
  - spacetimedb/src/reducers/lobbyPresets.ts
  - spacetimedb/src/reducers/lobbyLifecycle.ts
autonomous: true
requirements:
  - LBBY-01
  - LBBY-02

must_haves:
  truths:
    - "A host can update lobby settings during Waiting stage only"
    - "A player can move between Blue/Red/Spectator slots freely during Waiting"
    - "Moving teams auto-resets isConfirmed for the mover"
    - "A player can confirm/unconfirm ready status"
    - "start_draft requires all Blue+Red non-coach players to be confirmed"
    - "LobbyPreset CRUD works with correct permission hierarchy"
    - "System presets can be created at bootstrap"
    - "create_lobby accepts presetId and loads config from preset when provided"
  artifacts:
    - path: "spacetimedb/src/reducers/lobbySettings.ts"
      provides: "5 lobby setting reducers"
      exports: ["update_lobby_settings", "set_team_slot", "confirm_ready", "unconfirm_ready", "set_captain"]
    - path: "spacetimedb/src/reducers/lobbyPresets.ts"
      provides: "3 preset CRUD reducers"
      exports: ["create_lobby_preset", "update_lobby_preset", "delete_lobby_preset"]
    - path: "spacetimedb/src/reducers/lobbyLifecycle.ts"
      provides: "Updated create_lobby with presetId support"
      contains: "presetId"
  key_links:
    - from: "spacetimedb/src/reducers/lobbySettings.ts"
      to: "spacetimedb/src/helpers/lobbyHelpers.ts"
      via: "ensureStageIs, ensureLobbyMember"
      pattern: "ensureStageIs.*Waiting"
    - from: "spacetimedb/src/reducers/lobbyPresets.ts"
      to: "spacetimedb/src/helpers/ensurePermissions.ts"
      via: "isRoleAtLeast for permission hierarchy"
      pattern: "isRoleAtLeast"
    - from: "spacetimedb/src/reducers/lobbyLifecycle.ts"
      to: "spacetimedb/src/reducers/lobbyPresets.ts"
      via: "LobbyPreset lookup for preset-based lobby creation"
      pattern: "LobbyPreset.id.find"
---

<objective>
Lobby settings, team slot management, ready-up system, lobby preset CRUD, and presetId wiring into create_lobby.

Purpose: Enables the pre-draft lobby configuration flow — players join, pick teams, ready up, host configures settings and can use presets. Presets are usable at lobby creation time via presetId. Required before draft system (Plan 06) can implement start_draft validation.

Output: lobbySettings.ts (5 reducers), lobbyPresets.ts (3 reducers), updated lobbyLifecycle.ts (presetId on create_lobby).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-CONTEXT.md
@.planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-02-SUMMARY.md
@spacetimedb/src/tables/lobby.ts
@spacetimedb/src/tables/lobbyMember.ts
@spacetimedb/src/tables/lobbyPreset.ts
@spacetimedb/src/helpers/lobbyHelpers.ts
@spacetimedb/src/helpers/ensurePermissions.ts
@spacetimedb/src/helpers/auditColumns.ts
@spacetimedb/src/reducers/lobbyLifecycle.ts

<interfaces>
From spacetimedb/src/helpers/lobbyHelpers.ts:
- ensureLobbyMember(ctx, lobbyId, userId): LobbyMember row
- ensureHostOrAbove(ctx, lobby, user): void
- ensureStageIs(lobby, ...stages: string[]): void

From spacetimedb/src/helpers/ensurePermissions.ts:
- getAuthenticatedUser(ctx): User
- isRoleAtLeast(role, minRole): boolean

From spacetimedb/src/helpers/auditColumns.ts:
- auditInsert(ctx, userId), auditUpdate(ctx, existing, userId)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lobbySettings.ts with 5 reducers</name>
  <files>spacetimedb/src/reducers/lobbySettings.ts</files>
  <read_first>
    spacetimedb/src/tables/lobby.ts,
    spacetimedb/src/tables/lobbyMember.ts,
    spacetimedb/src/helpers/lobbyHelpers.ts,
    spacetimedb/src/helpers/ensurePermissions.ts,
    spacetimedb/src/helpers/auditColumns.ts,
    spacetimedb/src/reducers/lobbyLifecycle.ts,
    .planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-CONTEXT.md
  </read_first>
  <action>
**Create spacetimedb/src/reducers/lobbySettings.ts:**

**1. update_lobby_settings reducer (per D-28):**

Args: All mutable lobby config fields as optional-like pattern. Since SpacetimeDB doesn't support optional args well, accept all fields and let host pass current values for unchanged ones: `lobbyId: t.u32(), teamSize: t.u8(), draftMode: DraftMode, banMode: BanMode, gameMode: GameMode, matchType: MatchType, standardTurnSeconds: t.u32(), reserveBankSeconds: t.u32(), characterBudget: t.f32(), lightconeBudget: t.f32(), minimumBidRaise: t.f32(), rosterDiffAdvantage: t.f32(), rosterThreshold: t.f32(), underThresholdAdvantage: t.f32(), aboveThresholdPenalty: t.f32(), deathPenalty: t.f32(), isAnonymousPlayers: t.bool(), isAnonymousSpectators: t.bool(), rosterVisibility: RosterVisibility, requireOwnership: t.bool(), costSetId: t.u32(), disconnectPolicy: DisconnectPolicy, disconnectForfeitSeconds: t.u32(), allowMirrorPicks: t.bool(), autoRandomPick: t.bool(), refereeCanUndo: t.bool(), refereeCanPause: t.bool(), refereeCanSetCaptain: t.bool(), refereeCanKick: t.bool(), allowPlayerPause: t.bool(), teamBlueAlias: t.string(), teamRedAlias: t.string(), isPublic: t.bool(), password: t.string()`

Logic:
- `getAuthenticatedUser(ctx)`, find lobby
- `ensureHostOrAbove(ctx, lobby, user)`
- `ensureStageIs(lobby, 'Waiting')` — per D-28 settings locked once Drafting
- If lobby.isTournamentControlled: `throw new SenderError('Cannot change settings on a tournament-controlled lobby.')` per D-65
- Per D-42: If `matchType.tag === 'Ranked'`, force `allowMirrorPicks = false`
- Per D-23: If host is guest, force `matchType` to Casual and `rosterVisibility` to ClosedNoRating
- Update Lobby via `id.update()` with all new values + `lastActivityAt: ctx.timestamp` + audit
- Handle password: if `isPublic` changed to false and password provided, upsert LobbyPassword. If changed to true, delete LobbyPassword if exists.
- Reset all members' isConfirmed to false (settings changed = everyone must re-confirm): iterate `ctx.db.LobbyMember.lobby_id.filter(lobbyId)`, for each delete+insert with `isConfirmed: false`

**2. set_team_slot reducer (per D-27/D-28):**

Args: `lobbyId: t.u32(), targetUserId: t.u32(), teamSlot: TeamLabel`

Logic:
- `getAuthenticatedUser(ctx)`, find lobby
- `ensureStageIs(lobby, 'Waiting')` — per D-28 free movement only during Waiting
- If `targetUserId !== user.id`: `ensureHostOrAbove(ctx, lobby, user)` — only host/admin can move others
- If `targetUserId === user.id`: self-move allowed for any member
- Find target LobbyMember: `ensureLobbyMember(ctx, lobbyId, targetUserId)`
- Delete+insert LobbyMember with new `teamSlot` and `isConfirmed: false` (per D-28 moving auto-resets confirmation for the mover only)
- Update lobby `lastActivityAt`

**3. confirm_ready reducer (per D-29):**

Args: `lobbyId: t.u32()`

Logic:
- `getAuthenticatedUser(ctx)`, find lobby
- `ensureStageIs(lobby, 'Waiting')` — can only confirm during Waiting
- `ensureLobbyMember(ctx, lobbyId, user.id)` — get member
- Delete+insert LobbyMember with `isConfirmed: true`
- Update lobby `lastActivityAt`

**4. unconfirm_ready reducer (per D-28):**

Args: `lobbyId: t.u32()`

Logic:
- `getAuthenticatedUser(ctx)`, find lobby
- `ensureStageIs(lobby, 'Waiting')` — per D-28 "Unconfirm blocked during the 3-second countdown after start_draft is called" — but start_draft hasn't been called yet if stage is Waiting, so stage check suffices
- `ensureLobbyMember(ctx, lobbyId, user.id)` — get member
- Delete+insert LobbyMember with `isConfirmed: false`

**5. set_captain reducer (per D-30):**

Args: `lobbyId: t.u32(), targetUserId: t.u32()`

Logic:
- `getAuthenticatedUser(ctx)`, find lobby
- Permission: caller must be host (`lobby.hostUserId === user.id`) OR (referee with `refereeCanSetCaptain` per D-30 — check LobbyMember.isReferee && lobby.refereeCanSetCaptain) OR `isRoleAtLeast(user.role, 'Admin')`
- Find target member, ensure they are on a team (Blue or Red, not Spectator) and not a coach
- Find current captain on same team: iterate members on same teamSlot where isCaptain=true, set isCaptain=false via delete+insert
- Set target's isCaptain=true via delete+insert
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - spacetimedb/src/reducers/lobbySettings.ts exists and contains `update_lobby_settings`, `set_team_slot`, `confirm_ready`, `unconfirm_ready`, `set_captain`
    - update_lobby_settings contains `ensureStageIs(lobby, 'Waiting')` (D-28 lock)
    - update_lobby_settings contains `isTournamentControlled` check (D-65)
    - update_lobby_settings resets `isConfirmed: false` on all members
    - set_team_slot contains `isConfirmed: false` on moved member (D-28)
    - set_captain contains `refereeCanSetCaptain` check (D-30)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>5 lobby setting reducers: settings update (Waiting-only, D-28), team slot movement with auto-reset (D-27/D-28), ready-up confirm/unconfirm (D-29), captain assignment with permission check (D-30).</done>
</task>

<task type="auto">
  <name>Task 2: Create lobbyPresets.ts and wire presetId into create_lobby</name>
  <files>
    spacetimedb/src/reducers/lobbyPresets.ts,
    spacetimedb/src/reducers/lobbyLifecycle.ts
  </files>
  <read_first>
    spacetimedb/src/tables/lobbyPreset.ts,
    spacetimedb/src/helpers/ensurePermissions.ts,
    spacetimedb/src/helpers/auditColumns.ts,
    spacetimedb/src/reducers/lobbyLifecycle.ts,
    .planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-CONTEXT.md
  </read_first>
  <action>
**Create spacetimedb/src/reducers/lobbyPresets.ts:**

**1. create_lobby_preset reducer (per D-31b):**

Args: `name: t.string(), teamSize: t.u8(), draftMode: DraftMode, banMode: BanMode, gameMode: GameMode, matchType: MatchType, standardTurnSeconds: t.u32(), reserveBankSeconds: t.u32(), characterBudget: t.f32(), lightconeBudget: t.f32(), minimumBidRaise: t.f32(), rosterDiffAdvantage: t.f32(), rosterThreshold: t.f32(), underThresholdAdvantage: t.f32(), aboveThresholdPenalty: t.f32(), deathPenalty: t.f32(), isPublic: t.bool(), isAnonymousPlayers: t.bool(), isAnonymousSpectators: t.bool(), rosterVisibility: RosterVisibility, requireOwnership: t.bool(), costSetId: t.u32(), disconnectPolicy: DisconnectPolicy, disconnectForfeitSeconds: t.u32(), allowMirrorPicks: t.bool(), autoRandomPick: t.bool(), refereeCanUndo: t.bool(), refereeCanPause: t.bool(), refereeCanSetCaptain: t.bool(), refereeCanKick: t.bool(), allowPlayerPause: t.bool()`

Logic:
- `getAuthenticatedUser(ctx)` for auth
- Permission: `isRoleAtLeast(user.role, 'TournamentHost')` — per D-31b "Admins, moderators, and TOs can create presets."
- Validate name length: 1-50 characters
- Insert LobbyPreset: `{ id: 0, name, isSystemPreset: false, creatorUserId: user.id, ...all config fields, ...auditInsert(ctx, user.id) }`

**2. update_lobby_preset reducer:**

Args: `presetId: t.u32(), name: t.string(), ...all config fields same as create`

Logic:
- `getAuthenticatedUser(ctx)` for auth
- Find preset: `ctx.db.LobbyPreset.id.find(presetId)`, throw if not found
- Permission hierarchy per D-31b:
  - Admins can edit any preset including system presets
  - Moderators can edit their own, other moderator presets, and TO presets (NOT system presets)
  - TOs can only edit their own presets
  - Implementation: if `preset.isSystemPreset && !isRoleAtLeast(user.role, 'Admin')` throw. Else if `preset.creatorUserId !== user.id`: check caller is Admin (edit anything) or Moderator (edit moderator/TO presets — need to find preset creator's role)
- Update via `id.update()`: `{ ...preset, name, ...all config fields, ...auditUpdate(ctx, preset, user.id) }`

**3. delete_lobby_preset reducer:**

Args: `presetId: t.u32()`

Logic:
- `getAuthenticatedUser(ctx)` for auth
- Find preset, throw if not found
- Permission hierarchy per D-31b (same as update):
  - Admins can delete any preset including system presets
  - Moderators can delete their own, other moderator presets, and TO presets (NOT system presets)
  - TOs can only delete their own presets
- Delete: `ctx.db.LobbyPreset.id.delete(presetId)`

**Wire presetId into create_lobby in lobbyLifecycle.ts (per D-31b):**

Modify the `create_lobby` reducer in `spacetimedb/src/reducers/lobbyLifecycle.ts`:

1. Add `presetId: t.u32()` to the reducer args (use sentinel 0 = no preset).
2. At the top of the reducer body, BEFORE the Lobby insert, add preset-loading logic:
```typescript
// Per D-31b: Load config from preset when presetId > 0
let config = { teamSize, draftMode, banMode, gameMode, matchType, ... /* all config args */ };
if (presetId > 0) {
    const preset = ctx.db.LobbyPreset.id.find(presetId);
    if (!preset) throw new SenderError('Preset not found.');
    // Preset provides defaults; explicit args override if they differ from "empty/default" sentinel values.
    // For simplicity: preset values are used as the base, and all explicit args from the caller override.
    // Since SpacetimeDB reducers require all args, the caller always passes values.
    // The pattern is: client sends preset config values when using a preset, or custom values when not.
    // So just use the args directly — the CLIENT is responsible for populating args from the preset.
    // The backend simply validates the presetId exists (for audit/tracking purposes).
}
```
3. The key behavior: the client loads preset values into the create_lobby form, then sends them as args. The backend validates the presetId exists and records it. This avoids complex "merge" logic on the server. The presetId is for audit trail only — the actual config values come through as args.
4. Optionally store `presetId` on the Lobby row if there's a column for it (check lobby.ts). If not, the presetId is just validated and discarded (the config values are what matter).

**Publish module and regenerate bindings.**
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit && spacetime publish hsrpvp-spacetimedb-nextjs-test1 2>&1 | tail -5 && spacetime generate --lang typescript --out-dir ../src/module_bindings --project-path . 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - spacetimedb/src/reducers/lobbyPresets.ts exists and contains `create_lobby_preset`, `update_lobby_preset`, `delete_lobby_preset`
    - create_lobby_preset contains `isRoleAtLeast(user.role, 'TournamentHost')` permission check
    - update_lobby_preset contains `preset.isSystemPreset` check for admin-only system preset editing
    - delete_lobby_preset contains similar permission hierarchy check
    - lobbyLifecycle.ts create_lobby now accepts `presetId: t.u32()` arg (per D-31b)
    - lobbyLifecycle.ts create_lobby contains `LobbyPreset.id.find(presetId)` validation when presetId > 0
    - Module publishes without error
    - Bindings regenerated
  </acceptance_criteria>
  <done>3 preset CRUD reducers with D-31b permission hierarchy (Admin > Moderator > TO), system preset protection. create_lobby wired with presetId parameter for preset-based lobby creation. Publish and bindings complete.</done>
</task>

</tasks>

<verification>
- Module published to maincloud
- Bindings regenerated
- 8 new reducers in bindings (5 settings + 3 presets)
- create_lobby now accepts presetId arg
</verification>

<success_criteria>
- Settings mutable only during Waiting (D-28), locked for tournament lobbies (D-65)
- Free team slot movement with auto-confirm-reset (D-27/D-28)
- Ready-up system with confirm/unconfirm (D-29)
- Captain assignment with referee power config (D-30)
- Preset CRUD with correct permission hierarchy (D-31b)
- create_lobby accepts presetId and validates preset existence (D-31b)
</success_criteria>

<output>
After completion, create `.planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-04-SUMMARY.md`
</output>
