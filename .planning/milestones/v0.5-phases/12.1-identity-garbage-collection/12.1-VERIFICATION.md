---
phase: 12.1-identity-garbage-collection
verified: 2026-04-10T12:00:00Z
status: passed
score: 10/10 must-haves verified
re_verification: false
overrides_applied: 0
backfill_note: "Verification report produced retroactively on 2026-04-10. Phase 12.1 was executed 2026-04-08 and completed UAT 2026-04-09 (status: complete, 7/7 passed, 0 gaps). The formal VERIFICATION.md was skipped during urgent phase insertion; this report back-fills the gap against current code state."
---

# Phase 12.1: Identity Garbage Collection Verification Report

**Phase Goal:** Add scheduled and admin-triggered garbage collection for stale UserIdentity rows (device-to-user mappings), with shared GcResult audit infrastructure usable by all GC types.
**Verified:** 2026-04-10T12:00:00Z
**Status:** passed
**Re-verification:** No — initial verification (back-filled retroactively)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GcResult private audit table exists with gcType discriminator and audit columns | VERIFIED | `spacetimedb/src/tables/gcResult.ts` lines 8-24: `public: false`, columns `id` (u32 PK autoInc), `gcType` (string), `ranAt` (timestamp), `itemsScanned`/`itemsDeleted` (u32), `details` (string JSON), audit quartet. Registered in `schema.ts:109,227` under GC Audit section. |
| 2 | IdentityGcJob scheduled table follows LobbyGcJob mutable-binding pattern | VERIFIED | `spacetimedb/src/tables/identityGcJob.ts` lines 11-19: exports `_runIdentityGcReducer` mutable binding and `setRunIdentityGcReducer()` setter, table declared with `scheduled: () => _runIdentityGcReducer`. Registered in `schema.ts:114,232`. Mirrors lobbyGcJob.ts pattern exactly. |
| 3 | lastSeenAt bump in clientConnected after isOnline, before ban check | VERIFIED | `spacetimedb/src/index.ts` lines 107-112: `ctx.db.UserIdentity.identity.update({ ...mapping, lastSeenAt: ctx.timestamp, lastModifiedById, lastModifiedDate })` positioned after User.isOnline update (lines 100-104) and before provider ban check (line 115). D-01 comment confirms intent. |
| 4 | run_identity_gc scheduled reducer implements all 5 GC guards in priority order | VERIFIED | `spacetimedb/src/reducers/identityGc.ts` `performIdentityGc` helper lines 23-78: D-12 orphan delete first (lines 43-51), D-06 guest skip (line 54), D-08 online skip (line 57), D-07 preserve-newest via descending sort + `[_newest, ...rest]` destructure (lines 60-63), D-11 90-day TTL on rest (lines 66-74). IDENTITY_TTL_MICROS = `BigInt(90 * 24 * 60 * 60) * 1_000_000n` at line 11. Scheduled wrapper at lines 85-116 writes GcResult only when `itemsDeleted > 0` (line 93) and self-requeues 7 days out (lines 111-114). |
| 5 | admin_gc_identities reducer is Moderator-gated and always writes GcResult | VERIFIED | `spacetimedb/src/reducers/identityGc.ts` lines 122-145: `const user = ensureModerator(ctx)` at line 123, calls shared `performIdentityGc(ctx)` at line 126, writes GcResult unconditionally at lines 128-141 with `triggeredBy: 'admin'` detail and `auditInsert(ctx, user.id)`. Explicit "No self-requeue" comment at line 144 (Pitfall 6). |
| 6 | admin_gc_lobbies reducer shares performLobbyGc helper and is Moderator-gated | VERIFIED | `spacetimedb/src/reducers/lobbyGc.ts` lines 190-208: `ensureModerator(ctx)` gate, `performLobbyGc(ctx)` shared helper at line 194, unconditional GcResult write at lines 196-204, one-shot (no self-requeue, line 207). `performLobbyGc` helper extracted at lines 88-152 — called by both scheduled `run_lobby_gc` (line 162) and admin reducer. |
| 7 | All 4 new reducers exported from index.ts | VERIFIED | `spacetimedb/src/index.ts` line 81: `export { run_lobby_gc, admin_gc_lobbies, seed_lobby_gc_job } from './reducers/lobbyGc'`. Line 82: `export { run_identity_gc, admin_gc_identities, seed_identity_gc_job } from './reducers/identityGc'`. |
| 8 | post-publish.ts seeds IdentityGcJob via seedIdentityGcJob | VERIFIED | `scripts/post-publish.ts` lines 235-242: Step 6/7 calls `connection.reducers.seedIdentityGcJob({})` with idempotent error handling. (Steps bumped to /7 post-Phase-12.1 when seedLobbyGcJob was added as Step 7.) |
| 9 | seed_identity_gc_job is server-only and idempotent | VERIFIED | `spacetimedb/src/reducers/identityGc.ts` lines 153-173: `ctx.db.ServerIdentity.identity.find(ctx.sender)` check throws `SenderError('Forbidden: caller is not the registered server identity.')` at lines 155-158. Idempotent skip via `ctx.db.IdentityGcJob.iter()` length check at lines 161-165. Seeds first run 7 days out at lines 168-171. |
| 10 | UAT 7/7 passed on live environment | VERIFIED | `.planning/phases/12.1-identity-garbage-collection/12.1-UAT.md` `status: complete`, 7/7 passed, 0 issues: cold-start publish, IdentityGcJob seeded row, lastSeenAt bump on connect, admin_gc_identities success, admin_gc_lobbies success, GcResult audit trail with both types, auth guard rejection — all passed 2026-04-09. |

**Score:** 10/10 truths verified

### Deferred Items

None.

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `spacetimedb/src/tables/gcResult.ts` | Private audit table with gcType discriminator | VERIFIED | 25 lines, `public: false`, all expected columns present. Note: `durationMs` column mentioned in 12.1-01-SUMMARY was intentionally omitted — ctx.timestamp is pinned at reducer start so elapsed time cannot be measured (documented decision in SUMMARY key-decisions). Details column carries the per-type breakdown instead. |
| `spacetimedb/src/tables/identityGcJob.ts` | Scheduled table with mutable-binding pattern | VERIFIED | 19 lines, mirrors lobbyGcJob.ts. `scheduledId` (u64 PK autoInc) + `scheduledAt` (scheduleAt) columns. |
| `spacetimedb/src/schema.ts` | GcResult + IdentityGcJob registered | VERIFIED | Lines 109, 114 (imports), lines 227, 232 (registration under GC Audit + Scheduled Jobs sections). |
| `spacetimedb/src/index.ts` | lastSeenAt bump in clientConnected | VERIFIED | Lines 107-112 with D-01 comment. Positioned correctly between isOnline update and ban check. |
| `spacetimedb/src/helpers/userDeletionHelper.ts` | D-09 audit comment for UserPrivate cascade | VERIFIED | Lines 46-50: "D-09 audit (Phase 12.1): UserPrivate cascade verified complete -- hard-deleted here" comment present, followed by `ctx.db.UserPrivate.userId.delete(userId)` call. |
| `spacetimedb/src/reducers/identityGc.ts` | performIdentityGc helper + 3 reducers + setter wiring | VERIFIED | 177 lines. All D-05/D-06/D-07/D-08/D-11/D-12 guards implemented. Scheduled/admin/seed reducers all present. `setRunIdentityGcReducer(run_identity_gc)` at line 176. |
| `spacetimedb/src/reducers/lobbyGc.ts` | performLobbyGc shared helper + admin_gc_lobbies + GcResult write | VERIFIED | 237 lines. `performLobbyGc` helper at lines 88-152. `run_lobby_gc` writes GcResult conditionally (line 165), `admin_gc_lobbies` writes unconditionally (line 196). GC-04 audit comment for MatchResultRecord at lines 22-24. |
| `scripts/post-publish.ts` | Step seeding IdentityGcJob | VERIFIED | Step 6/7 at lines 235-242. |

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `spacetimedb/src/schema.ts` | `spacetimedb/src/tables/gcResult.ts` | `import { GcResult }` + registration | WIRED | Line 109 import, line 227 in schema object. |
| `spacetimedb/src/schema.ts` | `spacetimedb/src/tables/identityGcJob.ts` | `import { IdentityGcJob }` + registration | WIRED | Line 114 import, line 232 in schema object. |
| `spacetimedb/src/reducers/identityGc.ts` | `spacetimedb/src/tables/identityGcJob.ts` | `import { IdentityGcJob, setRunIdentityGcReducer }` | WIRED | Line 2 import. `setRunIdentityGcReducer(run_identity_gc)` called at line 176 to resolve the circular dependency. |
| `spacetimedb/src/reducers/identityGc.ts` | `ctx.db.GcResult.insert(...)` | Schema-driven handle | WIRED | Lines 94-105 (scheduled conditional), 128-141 (admin unconditional). `auditInsert(ctx, SYSTEM_USER_ID)` and `auditInsert(ctx, user.id)` respectively. |
| `spacetimedb/src/reducers/identityGc.ts` | `ctx.db.UserIdentity.identity.delete(...)` | Schema-driven handle | WIRED | Lines 45 (orphan), 69 (stale). Both increment counters and log via D-05 prefix. |
| `spacetimedb/src/reducers/identityGc.ts` | `ensureModerator` helper | `import { ensureModerator } from '../helpers/ensurePermissions'` | WIRED | Line 6 import. `const user = ensureModerator(ctx)` at line 123 in `admin_gc_identities`. Helper returns user directly (verified at ensurePermissions.ts:60-66). |
| `spacetimedb/src/reducers/lobbyGc.ts` | `ctx.db.GcResult.insert(...)` | Schema-driven handle | WIRED | Lines 166-174 (scheduled conditional), 196-204 (admin unconditional). |
| `spacetimedb/src/index.ts` | identityGc reducers | `export { run_identity_gc, admin_gc_identities, seed_identity_gc_job } from './reducers/identityGc'` | WIRED | Line 82 re-export. |
| `spacetimedb/src/index.ts` | lobbyGc reducers (updated) | `export { run_lobby_gc, admin_gc_lobbies, seed_lobby_gc_job } from './reducers/lobbyGc'` | WIRED | Line 81 re-export including the new `admin_gc_lobbies`. |
| `scripts/post-publish.ts` | seedIdentityGcJob reducer | `connection.reducers.seedIdentityGcJob({})` | WIRED | Line 238. Try/catch for idempotent safety. |
| `spacetimedb/src/index.ts` clientConnected | `ctx.db.UserIdentity.identity.update` | Direct state update with ctx.timestamp | WIRED | Lines 107-112. Uses manual audit fields (not auditUpdate helper) per D-01 decision because mapping PK is identity, not id. |
| `spacetimedb/src/reducers/server.ts` server_nuke_test_data | `ctx.db.GcResult` | `nuke(ctx.db.GcResult, 'GcResult')` | WIRED | Line 527 — GcResult included in test data wipe, confirming table is accessible from reducer context. |

## Data-Flow Trace (Level 4)

| Flow | Source → Sink | Produces Real Data | Status |
|------|---------------|--------------------|--------|
| Connect → lastSeenAt bump | `clientConnected` lifecycle → `UserIdentity.identity.update({ lastSeenAt: ctx.timestamp })` | Yes — server-authoritative timestamp written on every WebSocket connection | FLOWING |
| Scheduled sweep → GcResult row | `run_identity_gc` → `performIdentityGc(ctx)` iterates `UserIdentity` → `GcResult.insert` when deleted>0 → self-requeue | Yes — real iter() + real delete() + real audit row when deletions occur | FLOWING |
| Admin sweep → GcResult row | `admin_gc_identities` → `ensureModerator` gate → `performIdentityGc(ctx)` → unconditional `GcResult.insert` | Yes — always writes audit row regardless of delete count; UAT 4 confirmed | FLOWING |
| Lobby admin sweep → GcResult row | `admin_gc_lobbies` → `ensureModerator` gate → `performLobbyGc(ctx)` → unconditional `GcResult.insert` | Yes — UAT 5 confirmed live write | FLOWING |
| post-publish seed → IdentityGcJob row | `post-publish.ts` Step 6 → `seedIdentityGcJob` → `IdentityGcJob.insert({ scheduledAt: now + 7d })` | Yes — UAT 2 confirmed exactly one row with future scheduled_at | FLOWING |

End-to-end chain (connect → lastSeenAt bump → 7-day scheduled wake → performIdentityGc → GcResult audit) is fully wired. UAT live-environment test results (7/7 pass) confirm the flow produces observable database state at every hop.

## Behavioral Spot-Checks

Step 7b: SKIPPED — This phase targets SpacetimeDB module reducers which cannot be invoked without a live maincloud connection and pre-registered server identity. The behavioral equivalence is covered by the UAT session on 2026-04-09, which exercised each reducer against a freshly published module and verified all observable state via `spacetime sql` (see 12.1-UAT.md tests 1-7, all `result: pass`).

## Requirements Coverage

Note: Phase 12.1's `requirements-completed` frontmatter lists internal GC requirement IDs (GC-01, GC-02, GC-03, GC-04). These are phase-local requirements authored during plan research — they are not tracked in the v0.5 `REQUIREMENTS.md` traceability matrix (which covers SCHM through ARCH only). This matches the Phase 12 precedent where SEC-01 through SEC-05 were similarly phase-local.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| GC-01 | 12.1-01, 12.1-02 | GcResult audit table + identity GC scheduled reducer | SATISFIED | `gcResult.ts` table present (public: false). `run_identity_gc` implements weekly 90-day sweep with D-11 TTL and GcResult write on delete. UAT 4+6 confirmed audit rows emitted. |
| GC-02 | 12.1-01, 12.1-02 | IdentityGcJob scheduled table + GcResult audit row on every GC run | SATISFIED | `identityGcJob.ts` scheduled table present. Lobby GC and identity GC both write GcResult rows discriminated by gcType. Admin variants write unconditionally; scheduled variants write on delete>0. UAT 6 confirmed both 'identity' and 'lobby' rows visible in gc_result table. |
| GC-03 | 12.1-02 | Admin-triggered GC for both identity and lobby | SATISFIED | `admin_gc_identities` and `admin_gc_lobbies` both present, both `ensureModerator`-gated, both one-shot (no self-requeue). UAT 4+5 confirmed successful runs; UAT 7 confirmed non-moderator rejection with no audit row created. |
| GC-04 | 12.1-01, 12.1-02 | UserPrivate cascade completeness + MatchResultRecord orphan audit | SATISFIED | D-09 audit comment in `userDeletionHelper.ts:46` confirms UserPrivate hard-delete cascade. GC-04 audit comment in `lobbyGc.ts:22-24` confirms MatchResultRecord cleanup in `hardDeleteLobby` and all termination paths. No orphan gaps found. |

No REQUIREMENTS.md entries reference Phase 12.1 (all GC-xx IDs are phase-local), so there are no orphaned requirement IDs to report.

## UAT Cross-Reference

Live-environment smoke tests passed 2026-04-09 (see `.planning/phases/12.1-identity-garbage-collection/12.1-UAT.md`):

| # | Test | Result | Verified Truth(s) |
|---|------|--------|-------------------|
| 1 | Cold Start Smoke Test | PASS | post-publish.ts Step 6 completes without errors; module boots with new schema registered |
| 2 | Identity GC Job Seeded | PASS | `identity_gc_job` has exactly one row with future scheduled_at after post-publish — truth 8, 9 |
| 3 | lastSeenAt Bump on Connect | PASS | `user_identity.last_seen_at` updated to recent timestamp on connect — truth 3 |
| 4 | Admin Identity GC Runs | PASS | `admin_gc_identities` completes without crash, `gc_result` row with `gc_type='identity'` and recent `ran_at` — truth 5 |
| 5 | Admin Lobby GC Runs | PASS | `admin_gc_lobbies` completes without crash, `gc_result` row with `gc_type='lobby'` — truth 6 |
| 6 | GcResult Audit Trail | PASS | `gc_result` shows at least one row per GC type with valid counts — truths 1, 5, 6 |
| 7 | Admin GC Auth Guard | PASS | Non-moderator calls rejected; no audit row created for rejected calls — truths 5, 6 (T-12.1-04/05) |

Summary: total=7, passed=7, issues=0, pending=0, skipped=0, blocked=0.

## Threat Model Verification

No new trust boundaries introduced beyond what Plan 01 and Plan 02 threat models covered. All threats have concrete code mitigations verified against current state:

| Threat | Mitigation | Status |
|--------|-----------|--------|
| T-12.1-02: Client egress of GC audit data | `GcResult` table `public: false` | VERIFIED (`gcResult.ts:23`) |
| T-12.1-03: IdentityGcJob sensitive columns | Only `scheduledId` + `scheduledAt` columns, no PII | VERIFIED (`identityGcJob.ts:4-5`) |
| T-12.1-04: Non-moderator triggers admin_gc_identities | `ensureModerator(ctx)` gate | VERIFIED (`identityGc.ts:123`) — UAT 7 confirmed rejection |
| T-12.1-05: Non-moderator triggers admin_gc_lobbies | `ensureModerator(ctx)` gate | VERIFIED (`lobbyGc.ts:191`) — UAT 7 confirmed rejection |
| T-12.1-06: Non-server caller invokes seed_identity_gc_job | `ctx.db.ServerIdentity.identity.find(ctx.sender)` + `SenderError` | VERIFIED (`identityGc.ts:155-158`) |
| T-12.1-07: Active user identity loss (live user GC'd mid-use) | isGuest skip (D-06) + isOnline skip (D-08) + preserve-newest (D-07) | VERIFIED (`identityGc.ts:54, 57, 60-63`) |

No regressions detected in adjacent auth code paths. The `clientConnected` handler order is still correct: User isOnline update → UserIdentity lastSeenAt bump → provider ban check → soft-delete on banned match.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `spacetimedb/src/reducers/identityGc.ts` | 87 | Unused `arg` parameter in scheduled reducer signature | Info | Acceptable — the `arg` parameter is required by the scheduled-reducer contract (must accept the table row type) even when the reducer does not consume it. Pattern mirrors `run_lobby_gc`. |
| `spacetimedb/src/reducers/identityGc.ts` | 63 | `[_newest, ...rest]` destructure with underscore-prefixed unused binding | Info | Intentional — preserves newest identity per D-07 by excluding it from the TTL delete loop. The underscore prefix signals the deliberate "keep but don't touch" semantic. |

No blockers. No warnings. The SUMMARY-noted `durationMs` column is absent from `gcResult.ts`, matching the "always 0" decision documented in 12.1-01-SUMMARY key-decisions — the field was dropped rather than stored as a meaningless zero. This is a documentation-vs-code consistency issue only; the phase goal does not depend on wall-clock duration tracking.

## Human Verification Required

None. All ten observable truths are programmatically verifiable against current code state, and the UAT on 2026-04-09 already provided the live-environment evidence (cold start, scheduled job seeding, lastSeenAt bump, admin reducers, audit trail, auth guard). No outstanding items require human verification at back-fill time.

## Gaps Summary

No gaps. All ten observable truths verified against current code in `spacetimedb/src/`. All four requirements (GC-01 through GC-04) are SATISFIED with code evidence. All seven threat model items have verified mitigations. UAT completed 7/7 pass with 0 issues on 2026-04-09.

**Minor drift (informational only):** The `gc_result` table omits the `durationMs` column that was mentioned in 12.1-01-SUMMARY's schema description. This was explicitly called out in that SUMMARY's key-decisions section ("durationMs column always 0 — ctx.timestamp is pinned at reducer invocation start") and the field appears to have been dropped rather than stored as a perpetual zero. The phase goal is still fully satisfied because the `details` JSON column captures the per-type counts that actually matter for GC audit review.

**Verdict: PASSED.** Phase 12.1 delivers scheduled and admin-triggered identity garbage collection with a shared GcResult audit infrastructure. All reducers are correctly gated, correctly wired into the scheduled-table mutable-binding pattern, and confirmed functional against live SpacetimeDB state. Phase 12.1 is ready for v0.5 milestone closure from the verification perspective.

---

_Verified: 2026-04-10T12:00:00Z (back-filled)_
_Verifier: Claude (gsd-verifier)_
