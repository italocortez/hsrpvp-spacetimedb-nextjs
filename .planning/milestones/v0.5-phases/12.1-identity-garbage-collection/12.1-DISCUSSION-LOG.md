# Phase 12.1: Identity Garbage Collection - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-08
**Phase:** 12.1-identity-garbage-collection
**Areas discussed:** GC trigger & lifecycle, Scope & targeting, TTL & deletion behavior

---

## GC Trigger & Lifecycle

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, bump on connect | Every clientConnected updates identity's lastSeenAt. Accurate GC data, tiny write overhead. | ✓ |
| No, keep current behavior | lastSeenAt stays as-is (set only at identity creation). GC uses lastLoginAt instead. | |
| Bump on connect + disconnect | Track both lastSeenAt and lastDisconnectedAt for richer lifecycle data. | |

**User's choice:** Bump lastSeenAt on connect
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Admin reducer only | admin_gc_identities callable by Admin/Moderator. Simple, deterministic. | |
| SpacetimeDB scheduled reducer | Self-scheduling reducer on a timer. Fully autonomous. | |
| Both — admin + scheduled | Admin can trigger on-demand, plus scheduled reducer runs automatically. | ✓ |

**User's choice:** Both admin and scheduled

| Option | Description | Selected |
|--------|-------------|----------|
| Daily | Runs once every 24 hours. | |
| Weekly | Runs once every 7 days. Lower overhead. | ✓ |
| Configurable via admin | Admin sets interval via config table. | |

**User's choice:** Weekly schedule

| Option | Description | Selected |
|--------|-------------|----------|
| Runtime-configurable | IdentityGcConfig table following AccountRatingConfig pattern. | |
| Fixed at publish time | Hardcoded constants. Simpler, requires republish to change. | ✓ |

**User's choice:** Fixed at publish time

| Option | Description | Selected |
|--------|-------------|----------|
| All at once | Single reducer call processes all stale rows. | ✓ |
| Batched with cap | Max N identities per run. | |
| You decide | Claude picks based on data scale. | |

**User's choice:** All at once

| Option | Description | Selected |
|--------|-------------|----------|
| Console log only | Log each deleted identity. Visible in spacetime logs. | ✓ |
| Audit table | IdentityGcLog table for persistent history. | |
| Both | Console log + audit table. | |

**User's choice:** Console log only

---

## Scope & Targeting

| Option | Description | Selected |
|--------|-------------|----------|
| Guest identities only | Only clean up guest (unverified) user identities. | |
| Both guest and verified | Prune stale identities from ALL users. | |
| Guest users + orphaned identities | Guest identities AND orphans from deleted users. | |

**User's clarification:** Guest identities are 1:1 with users — deleting an identity means deleting the guest. Too destructive for automated GC. Should target verified users' stale device identities + orphans instead.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, verified + orphans only | GC prunes stale verified-user identities + orphans. Admins handle guests manually. | ✓ |
| Add guest soft-delete option | Same + admin_gc_guests reducer for guest cleanup. | |
| Verified + orphans + flagging | Auto GC + flag stale guests for admin review. | |

**User's choice:** Verified + orphans only

| Option | Description | Selected |
|--------|-------------|----------|
| Always keep newest identity | Never delete most recent identity per verified user. | ✓ |
| Delete all past TTL | If all identities stale, delete all. | |
| You decide | Claude picks safest approach. | |

**User's choice:** Always keep newest identity

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, full orphan sweep | GC also cleans orphaned UserPrivate rows. | |
| Identity orphans only | Only clean UserIdentity orphans. | |
| You decide | Claude determines. | |

**User's choice:** Handle UserPrivate orphans, but through the existing cascade delete paths (userDeletionHelper.ts), not in the GC reducer. Audit cascade comprehensively.

| Option | Description | Selected |
|--------|-------------|----------|
| Console log summary | Log summary line visible in spacetime logs. | |
| Return stats via GcResult table | Write GcRunResult row with counts and duration. | ✓ |

**User's choice:** GcResult table — but make it general-purpose for all GC types, private (`public: false`), with `details` as a plain string column for JSON-encoded breakdown.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, skip online users | Never prune identities from isOnline users. | ✓ |
| No, rely on TTL only | Prune regardless of isOnline. | |
| You decide | Claude picks safest approach. | |

**User's choice:** Skip online users

---

## TTL & Deletion Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| 30 days | Balances cleanup vs. casual users. | |
| 60 days | More conservative. | |
| 90 days | Very conservative. Only truly abandoned. | ✓ |
| 14 days | Aggressive. | |

**User's choice:** Initially selected 60 days, then bumped to 90 days after confirming no conflict with 30-day session cookie.

| Option | Description | Selected |
|--------|-------------|----------|
| Hardcoded constant | const IDENTITY_TTL_DAYS = 90. Change requires republish. | ✓ |
| Admin-configurable via table | IdentityGcConfig table with ttlDays column. | |

**User's choice:** Hardcoded constant

| Option | Description | Selected |
|--------|-------------|----------|
| Delete immediately | Orphans are invalid — clean on every run. | ✓ |
| Same 90-day TTL | Even orphans wait 90 days. | |
| You decide | Claude picks safest approach. | |

**User's choice:** Delete orphans immediately

**User's clarification (cookie conflict):** Asked whether 30-day stdb_session cookie expiry conflicts with 90-day identity TTL. Answer: No conflict. The cookie is display-name-only for SSR NavBar rendering. SpacetimeDB identity tokens (localStorage) don't expire. clientConnected bumps lastSeenAt regardless of cookie state.

---

## Claude's Discretion

- Scheduled reducer implementation pattern
- Helper function structure for GC logic
- GcResult details JSON schema per GC type
- Error handling if GC reducer fails mid-iteration

## Deferred Ideas

- Guest user GC — could be its own phase if manual admin deletion becomes tedious at scale
- Lobby/match session GC — future GC types reuse GcResult table
- Runtime-configurable GC settings — add config table if TTL tuning becomes frequent
