---
status: complete
phase: 12.1-identity-garbage-collection
source: [12.1-01-SUMMARY.md, 12.1-02-SUMMARY.md]
started: 2026-04-08T23:10:00Z
updated: 2026-04-09T00:15:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Publish with --clear-database, run post-publish.ts. All 6/6 steps complete (including new Step 6: seedIdentityGcJob). Server boots without errors.
result: pass

### 2. Identity GC Job Seeded
expected: After post-publish, `spacetime sql "SELECT * FROM identity_gc_job"` returns exactly one row with a future scheduled_at timestamp.
result: pass

### 3. lastSeenAt Bump on Connect
expected: After a client connects, `spacetime sql "SELECT identity, last_seen_at FROM user_identity"` shows lastSeenAt updated to a recent timestamp (not stuck at identity creation time).
result: pass

### 4. Admin Identity GC Runs
expected: Moderator calls `admin_gc_identities`. No crash. `spacetime sql "SELECT * FROM gc_result WHERE gc_type = 'identity'"` shows a new row with items_scanned >= 0, ran_at = recent timestamp.
result: pass

### 5. Admin Lobby GC Runs
expected: Moderator calls `admin_gc_lobbies`. No crash. `spacetime sql "SELECT * FROM gc_result WHERE gc_type = 'lobby'"` shows a new row with items_scanned >= 0, ran_at = recent timestamp.
result: pass

### 6. GcResult Audit Trail
expected: After tests 4+5, `spacetime sql "SELECT gc_type, items_scanned, items_deleted FROM gc_result"` shows at least two rows — one for 'identity', one for 'lobby'. Both have valid counts.
result: pass

### 7. Admin GC Auth Guard
expected: Non-moderator calling `admin_gc_identities` or `admin_gc_lobbies` is rejected with permission error. No GcResult row created for the rejected call.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
