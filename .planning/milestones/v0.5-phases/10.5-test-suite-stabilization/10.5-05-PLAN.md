---
phase: 10.5-test-suite-stabilization
plan: 05
type: execute
wave: 5
depends_on: [10.5-01, 10.5-02, 10.5-03, 10.5-04]
files_modified:
  - test/README.md
  - .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04]
requirements_addressed: [TEST-01, TEST-03, TEST-04]
must_haves:
  truths:
    - "test/README.md documents the final (post-fix) suite runtime baseline with today's date"
    - "10.5-AUDIT.md has a Lessons Learned section summarizing the audit's findings"
    - "test/README.md references the canonical reset sequence so future devs know how to recover"
    - "The phase is self-contained: someone reading 10.5-AUDIT.md alone understands what was fixed and why"
  artifacts:
    - path: "test/README.md"
      provides: "Final suite runtime baseline + canonical reset sequence documentation"
      contains: "Suite Runtime"
    - path: ".planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md"
      provides: "Complete phase artifact with Lessons Learned + final metrics + fix story"
      contains: "Lessons Learned"
  key_links:
    - from: "test/README.md Suite Runtime"
      to: "final post-fix measured runtime"
      via: "new row in the baseline table"
      pattern: "Post-stabilization.*Phase 10.5"
    - from: "10.5-AUDIT.md Lessons Learned"
      to: "patterns to propagate in future test phases"
      via: "bulleted list of insights"
      pattern: "## Lessons Learned"
---

<objective>
Finalize phase documentation: update `test/README.md` Suite Runtime with the post-fix baseline, complete `10.5-AUDIT.md` with a Lessons Learned section summarizing patterns and pitfalls, verify all phase deliverables exist, and close out with a clean `npm run test:all` green.

Purpose: This plan turns the audit artifact from a working document into a permanent phase record. Per D-06, test/README.md gets the updated baseline for future regression detection. Per D-07, AUDIT.md is the phase's historical artifact — it should be readable standalone. Per CONTEXT.md SC#5 + SC#6, the full-suite runtime must be documented and failure-mode classifications must persist.

Output: `test/README.md` with updated Suite Runtime table (2 rows: pre-fix baseline + post-fix final), `10.5-AUDIT.md` with Lessons Learned section, final commit closing the phase.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.5-test-suite-stabilization/10.5-CONTEXT.md
@.planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md
@test/README.md
@.planning/phases/10.5-test-suite-stabilization/10.5-04-SUMMARY.md

<interfaces>
<!-- Post-fix runtime measurement command -->

Final baseline capture:
```
time npm run test:all 2>&1 | tee /tmp/final-run.log
```
Extract wall-clock "real XmYs" line → that's the post-fix baseline.

Also extract from /tmp/final-run.log:
- Total file count (vitest summary line)
- Passed file count (should equal total; failed = 0)
- Any skipped tests (describe.skipIf blocks)

<!-- test/README.md post-fix update template -->

New row to ADD to the Suite Runtime table (existing table from Plan 01 Task 2):
```markdown
| Post-stabilization (Phase 10.5 final) | {today} | {X}m{Y}s | {N} files | 0 failures, {S} skipped — baseline after audit + cleanup refactor |
```

The existing table after Plan 01 had 1 row (Pre-stabilization). This plan ADDS a second row.

<!-- AUDIT.md Lessons Learned template -->

Section structure (REPLACE the TBD in AUDIT.md):
```markdown
## Lessons Learned

### Patterns That Worked
- **D-03 strict afterAll + resource tracking arrays** — cut orphan tournaments from 40 → {X}, lobbies from 51 → {Y}
- **Helper extraction (D-02)** — collapsed {N} inline copies into {M} shared files; caught drift (e.g., `bestOf` + `refereeControlsShelving` present in account-selection but missing from 19 other `defaultLobbyArgs` copies)
- **Delta-based assertions** — replacing `toBe(0)` with `toBe(beforeCount + delta)` made {K} files pollution-proof
- **D-11 minimum-data trimming** — removed {P} unused harnesses, dropped teamSize 3→1 in {Q} tests

### Patterns That Backfired
- {Fill in from Plan 04 experience — e.g., "assumed ensureHsrAccount iter() would work, HsrAccount is private post-10.4 — had to use try/catch reducer idempotence instead"}

### Per-Category Fix Count
- pollution (missing cleanup in polluter): {N}
- absolute-count (→ delta assertion): {N}
- ordering (→ self-seed): {N}
- seeding-gap (→ ensureX helper): {N}
- stale-assertion (10.1/10.4 rename): {N}
- over-seeded (D-11 trim): {N}
- awaiting-result (admin_void_match cleanup): {N}

### Recommendations for Future Phases
- Future tests SHOULD import from `test/shared/helpers/` — inline helpers are tech debt from day 1
- Delta-based assertions SHOULD be the default; `toBe(N)` absolute counts only when N truly cannot drift
- Every file creating dynamic resources MUST have `openedXxxIds: number[]` tracking array + `afterAll` cleanup loop
- `spacetime publish --clear-database` remains the hammer for full reset — use it between major refactors, not per-file

### Artifacts Produced
- `test/shared/helpers/` subdirectory with 8 domain files
- Updated `test/README.md` Suite Runtime section
- Phase audit at `.planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md`

### Open Items (Deferred per D-05)
- New integration tests for 10.1-10.4 reducers → `/gsd:add-tests` follow-up
- Query idiom extraction (myLobbies, lobbyMembers, etc.) if not completed in Plan 02
- {other items from SUMMARY files}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure final post-fix runtime + update test/README.md Suite Runtime</name>
  <files>test/README.md</files>
  <read_first>
    - test/README.md (EXISTING — has Suite Runtime section from Plan 01; we ADD a row to the table)
    - .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md (pre-fix baseline from Plan 01 must match existing test/README.md row)
    - .planning/phases/10.5-test-suite-stabilization/10.5-04-SUMMARY.md (final runtime from Plan 04 — use as reference, but RE-MEASURE here for authoritative final number)
  </read_first>
  <action>
    **Step A — Reset DB to clean state (authoritative measurement):**
    ```
    spacetime publish --module-path spacetimedb --server maincloud --clear-database -y
    spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb
    npx tsx test/shared/bootstrap.ts
    npx tsx test/shared/seed-data.ts
    ```

    **Step B — Run full suite with timing + capture:**
    ```
    time npm run test:all 2>&1 | tee .planning/phases/10.5-test-suite-stabilization/final-run.log
    ```

    **Step C — Extract:**
    - Wall-clock "real XmYs" from `time` output (or vitest Duration line)
    - Total integration file count (grep `(.*\.test\.ts)` count)
    - Passed count (vitest summary)
    - Failed count (must be 0 — if not, STOP and report)
    - Skipped count (describe.skipIf blocks)

    **Step D — ADD a new row to the Suite Runtime table in test/README.md:**

    Current table (after Plan 01 Task 2) looks like:
    ```markdown
    | Run | Date | Wall-Clock Time | File Count | Notes |
    |-----|------|-----------------|------------|-------|
    | Pre-stabilization (Phase 10.5 baseline) | 2026-04-05 | {BASELINE}m{s}s | {N} files | Captured before audit fixes — {F} failures |
    ```

    Add a second row BELOW the existing one:
    ```markdown
    | Post-stabilization (Phase 10.5 final) | {TODAY} | {X}m{Y}s | {N} files | 0 failures, {S} skipped — baseline after audit + cleanup refactor |
    ```

    Preserve the rest of the Suite Runtime section (Commands, Reset sequence) unchanged.

    **Step E — Commit:**
    ```
    git add test/README.md
    git commit -m "docs(10.5): update test README with post-stabilization runtime baseline"
    ```
  </action>
  <verify>
    <automated>grep -cE "Post-stabilization.*Phase 10\.5" test/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Post-stabilization" test/README.md` → 1
    - `grep -c "Pre-stabilization" test/README.md` → 1 (original row preserved)
    - `grep -cE "[0-9]+m[0-9]+s" test/README.md` → 2 or greater (both rows have measured times)
    - `grep -c "0 failures" test/README.md` → 1 (final row confirms green suite)
    - Final-run.log exists with exit code 0 at the end
    - `npm run test:all` exits 0
    - Commit created with message matching `docs\(10.5\): update test README`
  </acceptance_criteria>
  <done>
    test/README.md Suite Runtime table has 2 rows: pre-fix baseline (from Plan 01) + post-fix final (measured in this task). Both show real wall-clock times. Final row confirms 0 failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize 10.5-AUDIT.md with Lessons Learned + phase closure commit</name>
  <files>.planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md</files>
  <read_first>
    - .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md (EXISTING — Plan 01 + Plan 03 + Plan 04 populated sections)
    - .planning/phases/10.5-test-suite-stabilization/10.5-02-SUMMARY.md (helper extraction metrics)
    - .planning/phases/10.5-test-suite-stabilization/10.5-03-SUMMARY.md (cleanup effectiveness metrics)
    - .planning/phases/10.5-test-suite-stabilization/10.5-04-SUMMARY.md (per-category fix counts, per-fix patterns that worked/backfired)
    - .planning/phases/10.5-test-suite-stabilization/10.5-CONTEXT.md (D-02 through D-12 for "Recommendations for Future Phases")
  </read_first>
  <action>
    Replace the "## Lessons Learned" section's `TBD` content with a fully populated section based on the template in `<interfaces>`.

    **Step A — Gather metrics from SUMMARY files:**
    - Helper extraction counts: from 10.5-02-SUMMARY.md
    - Orphan count deltas: from AUDIT.md's own Cleanup Effectiveness Metrics table
    - Per-category fix counts: from 10.5-04-SUMMARY.md
    - Patterns that backfired: from 10.5-04-SUMMARY.md "original assumptions that were wrong"
    - Trimming counts: from 10.5-04-SUMMARY.md D-11 section

    **Step B — Write Lessons Learned** using the template structure in `<interfaces>`. Fill every `{placeholder}` with a real number or observation:

    - "Patterns That Worked" — real metrics, not vague praise
    - "Patterns That Backfired" — honest retrospective of assumptions that turned out wrong (may be empty if nothing surprised)
    - "Per-Category Fix Count" — count rows in File ↔ Polluter Matrix per category label
    - "Recommendations for Future Phases" — concrete rules, not platitudes
    - "Artifacts Produced" — bullet list of deliverables (helpers subdir, test/README section, AUDIT.md)
    - "Open Items (Deferred per D-05)" — items consciously left unfinished

    **Step C — Spot-check the other AUDIT.md sections are complete:**
    - Baseline Runtime: has real numbers ✓ (Plan 01)
    - Full-Suite Failure Inventory: populated ✓ (Plan 01)
    - Per-File Pass/Fail Matrix: all files listed, isolated column populated ✓ (Plan 04 Task 1)
    - Category Definitions: unchanged ✓ (Plan 01)
    - Cleanup Effectiveness Metrics: has Post-Plan-03 + Post-Plan-04 columns ✓ (Plans 03 + 04)
    - File ↔ Polluter Matrix: rows populated with commits ✓ (Plan 04 Task 2)
    - Lessons Learned: populated ✓ (THIS TASK)

    **Step D — Final sanity re-run:**
    ```
    npm run test:all
    ```
    Must exit 0. If it fails, something regressed between Plan 04 close and now — investigate before finalizing.

    **Step E — Verify phase success criteria are met:**

    SC#1: `spacetime publish --clear-database && npm run test:all` exits 0 → verify by running.
    SC#2: No leaked resources → `spacetime sql` query counts confirm.
    SC#3: Shared-state dependencies self-contained → verified by passing isolated runs (documented in AUDIT.md Per-File Matrix).
    SC#4: Helpers extracted → `grep -rc "async function promoteUser" test/backend/` returns 0.
    SC#5: Runtime baseline documented → `grep "Suite Runtime" test/README.md` returns match.
    SC#6: Failure-mode classification → AUDIT.md has Matrix with categories.

    Record SC status in a new "## Success Criteria Verification" section at the END of AUDIT.md:
    ```markdown
    ## Success Criteria Verification

    | SC# | Criterion | Status | Evidence |
    |-----|-----------|--------|----------|
    | 1 | test:all passes on fresh --clear-database | ✅ | npm run test:all exit 0 post-Plan-04 |
    | 2 | No stale rows in afterAll-creating files | ✅ | Cleanup Effectiveness Metrics shows {X} leaks → 0 |
    | 3 | Shared-state self-contained or documented | ✅ | All files pass in isolation (Per-File Matrix) |
    | 4 | Helpers extracted to test/shared/helpers/ | ✅ | grep "async function promoteUser" returns 0 |
    | 5 | Full suite runtime baseline documented | ✅ | test/README.md has post-stabilization row |
    | 6 | Failure reports classify isolation vs suite | ✅ | File ↔ Polluter Matrix has category column |
    ```

    **Step F — Commit AUDIT.md finalization:**
    ```
    git add .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md
    git commit -m "docs(10.5): finalize AUDIT with lessons learned + SC verification"
    ```
  </action>
  <verify>
    <automated>grep -q "## Lessons Learned" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md &amp;&amp; ! grep -q "^TBD$" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md &amp;&amp; grep -q "## Success Criteria Verification" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md &amp;&amp; npm run test:all</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Lessons Learned" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "^TBD" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 0 (no stale placeholders)
    - `grep -c "## Success Criteria Verification" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -cE "SC#[1-6]" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 6 or greater (all 6 SCs referenced)
    - `grep -c "### Patterns That Worked" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "### Per-Category Fix Count" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "### Recommendations for Future Phases" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `npm run test:all` exits 0
    - `grep -rc "async function promoteUser" test/backend/ --include="*.test.ts"` → 0 (SC#4)
    - `grep -q "Suite Runtime" test/README.md` → exit 0 (SC#5)
    - Final commit created with message matching `docs\(10.5\): finalize AUDIT`
  </acceptance_criteria>
  <done>
    AUDIT.md is a complete standalone phase artifact. Lessons Learned populated with real metrics. Success Criteria Verification table confirms all 6 SCs. Full suite green. Phase 10.5 documentation closed.
  </done>
</task>

</tasks>

<verification>
- test/README.md Suite Runtime has 2 rows (Pre-stabilization from Plan 01 + Post-stabilization from this plan), both with measured times
- 10.5-AUDIT.md is complete with all 8 sections populated (Baseline Runtime, Full-Suite Failure Inventory, Per-File Matrix, Category Definitions, Cleanup Effectiveness Metrics, File ↔ Polluter Matrix, Lessons Learned, Success Criteria Verification)
- No `TBD` placeholders remain in AUDIT.md
- All 6 roadmap Success Criteria verified in AUDIT.md with evidence
- `npm run test:all` exits 0 with 0 failures (final confirmation)
- Final commit closes the phase
</verification>

<success_criteria>
Plan 05 complete when AUDIT.md is a readable standalone phase artifact with metrics + lessons, test/README.md has final post-fix baseline, all 6 roadmap success criteria are verified with evidence, and the suite remains green. This closes Phase 10.5.
</success_criteria>

<output>
After completion, create `.planning/phases/10.5-test-suite-stabilization/10.5-05-SUMMARY.md` recording:
- Final post-fix runtime (X minutes Y seconds)
- Comparison to baseline (time delta: faster or slower?)
- Final orphan counts per table (should be effectively 0)
- All 6 SC verifications
- Closing reflection on what Phase 10.5 accomplished and any follow-up tickets recommended
</output>
