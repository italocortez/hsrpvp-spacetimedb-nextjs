---
phase: 10.5-test-suite-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md
  - test/README.md
autonomous: true
requirements: [TEST-01, TEST-04]
requirements_addressed: [TEST-01, TEST-04]
must_haves:
  truths:
    - "A fresh --clear-database + bootstrap + seed cycle completes without errors"
    - "Baseline npm run test:all runtime is measured and recorded"
    - "Every failing test file is captured with its failure signature"
    - "An AUDIT.md artifact exists at the phase directory with the failure inventory"
  artifacts:
    - path: ".planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md"
      provides: "Phase-scoped audit artifact — failure inventory, per-file pass/fail matrix, category hypotheses"
      contains: "Baseline Runtime, Failure Inventory, Per-File Matrix"
    - path: "test/README.md"
      provides: "Pre-fix baseline runtime reference entry"
      contains: "Suite Runtime"
  key_links:
    - from: "10.5-AUDIT.md"
      to: "npm run test:all output"
      via: "captured stdout/stderr with per-file statuses"
      pattern: "FAIL|PASS.*\\.test\\.ts"
    - from: "test/README.md Suite Runtime"
      to: "measured baseline value"
      via: "markdown section with date-stamped runtime"
      pattern: "## Suite Runtime"
---

<objective>
Reset the test DB to a known-fresh state, measure the current `npm run test:all` runtime + failure inventory, and produce the phase-scoped `10.5-AUDIT.md` artifact that anchors the rest of the phase's work.

Purpose: Every subsequent plan needs a single source of truth for "what is failing, what is green, what is the runtime baseline." Without this, Plans 02-04 are flying blind. Per D-07 the AUDIT.md artifact is phase-mandatory. Per D-06 the runtime baseline belongs in `test/README.md`.

Output: `.planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` (new file), `test/README.md` (Suite Runtime section added).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.5-test-suite-stabilization/10.5-CONTEXT.md
@.planning/phases/10.5-test-suite-stabilization/10.5-RESEARCH.md
@.planning/phases/10.5-test-suite-stabilization/10.5-VALIDATION.md
@test/README.md
@test/vitest.integration.config.ts
@test/shared/bootstrap.ts
@test/shared/seed-data.ts

<interfaces>
<!-- Reset sequence commands (from 10.5-RESEARCH.md "Canonical reset sequence") -->
<!-- These are the EXACT commands the executor must run. Do not paraphrase. -->

Reset sequence (all 5 steps, in order):
1. `spacetime publish --module-path spacetimedb --server maincloud --clear-database -y`
2. `spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb`
3. `npx tsx test/shared/bootstrap.ts`
4. `npx tsx test/shared/seed-data.ts`
5. `npm run test:all 2>&1 | tee .planning/phases/10.5-test-suite-stabilization/baseline.log`

Env vars that MUST be set before commands 3-5:
- `SPACETIMEDB_URI` (default `wss://maincloud.spacetimedb.com` if unset)
- `SPACETIMEDB_DB` (default `hsrpvp-spacetimedb-nextjs-test1` if unset)
- `SPACETIMEDB_SERVER_TOKEN` (written by step 3 into `.env.local`)

Vitest reporter flags:
- `--reporter=verbose` shows per-file PASS/FAIL lines
- `--reporter=json` writes machine-parseable output
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset test DB + capture baseline failure log</name>
  <files>.planning/phases/10.5-test-suite-stabilization/baseline.log</files>
  <read_first>
    - .planning/phases/10.5-test-suite-stabilization/10.5-CONTEXT.md (D-09, D-10, D-12 for reset strategy)
    - .planning/phases/10.5-test-suite-stabilization/10.5-RESEARCH.md § "Runtime State Inventory" (exact reset sequence)
    - test/shared/bootstrap.ts (what bootstrap does + what it writes to .env.local)
    - test/shared/seed-data.ts (what static data gets seeded)
    - test/vitest.integration.config.ts (worker count, fileParallelism, timeouts)
    - .env.local (to VERIFY SPACETIMEDB_SERVER_TOKEN persists — NEVER delete or modify this file)
  </read_first>
  <action>
    Execute the canonical reset + capture sequence exactly as follows (no deviation):

    Step 1 — Publish with clear-database:
    ```
    spacetime publish --module-path spacetimedb --server maincloud --clear-database -y
    ```
    Wait for "Module published" success message. If it fails, STOP and report — do not proceed.

    Step 2 — Regenerate client bindings:
    ```
    spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb
    ```

    Step 3 — Re-bootstrap server identity (writes SPACETIMEDB_SERVER_TOKEN to .env.local):
    ```
    npx tsx test/shared/bootstrap.ts
    ```
    After this completes, VERIFY `.env.local` contains a `SPACETIMEDB_SERVER_TOKEN=` line with a non-empty value. DO NOT delete `.env.local`. DO NOT commit `.env.local`.

    Step 4 — Re-seed static game data (characters + lightcones from test/data/*.json):
    ```
    npx tsx test/shared/seed-data.ts
    ```

    Step 5 — Measure baseline runtime + capture full failure log:
    ```
    time npm run test:all 2>&1 | tee .planning/phases/10.5-test-suite-stabilization/baseline.log
    ```
    Record:
    - Total wall-clock time (from `time` output) — e.g., "real 4m32.5s"
    - Total file count, passed count, failed count (from vitest summary line)
    - Per-file PASS/FAIL statuses (grep the log)

    DO NOT try to fix any failures in this task. Capture and log only.

    Note: baseline.log is a run artifact — do not commit it. Its contents feed Task 2's AUDIT.md.
  </action>
  <verify>
    <automated>test -s .planning/phases/10.5-test-suite-stabilization/baseline.log &amp;&amp; grep -E "(PASS|FAIL).*\.test\.ts" .planning/phases/10.5-test-suite-stabilization/baseline.log | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `test -s .planning/phases/10.5-test-suite-stabilization/baseline.log` → exit 0 (file exists and is non-empty)
    - `grep -c "SPACETIMEDB_SERVER_TOKEN" .env.local` → 1 or greater (token persisted after bootstrap)
    - `grep -cE "(PASS|FAIL).*\.test\.ts" .planning/phases/10.5-test-suite-stabilization/baseline.log` → 20 or greater (at least ~20 test files represented; full suite is ~41 integration files)
    - `grep -E "^real\s+" .planning/phases/10.5-test-suite-stabilization/baseline.log` or runtime marker captured (time output or vitest "Duration" line present)
    - `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT COUNT(*) AS c FROM hsr_character"` → returns a count > 0 (seeding succeeded)
    - `.env.local` file still exists and is unmodified (NEVER deleted per CLAUDE.md)
  </acceptance_criteria>
  <done>
    Fresh DB is published, bootstrap + seed complete, `baseline.log` captures the current failure inventory + runtime, and the raw data for Task 2's AUDIT.md is on disk.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 10.5-AUDIT.md failure inventory + add Suite Runtime to test/README.md</name>
  <files>
    .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md
    test/README.md
  </files>
  <read_first>
    - .planning/phases/10.5-test-suite-stabilization/baseline.log (generated in Task 1 — source of all failure data)
    - .planning/phases/10.5-test-suite-stabilization/10.5-RESEARCH.md § "Failure Mode Categories" (the 12 categorization labels)
    - .planning/phases/10.5-test-suite-stabilization/10.5-CONTEXT.md § D-07 (AUDIT.md required sections)
    - test/README.md (EXISTING content — do not replace, APPEND a new section only)
  </read_first>
  <action>
    **Part A — Create `.planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md`** with the following EXACT markdown structure:

    ```markdown
    # Phase 10.5 — Test Suite Audit Report

    **Baseline captured:** 2026-04-05 (or current date)
    **Reset state:** Fresh `spacetime publish --clear-database` + bootstrap + seed

    ## Baseline Runtime

    | Metric | Value |
    |--------|-------|
    | Wall-clock (full `npm run test:all`) | {X}m{Y}s |
    | Integration test files total | {N} |
    | Passed | {P} |
    | Failed | {F} |
    | Skipped | {S} |

    ## Full-Suite Failure Inventory (Pre-Fix)

    | File | Status | Failure Signature (first error line) | Initial Category Hypothesis |
    |------|--------|---------------------------------------|------------------------------|
    | test/backend/.../file1.test.ts | FAIL | "expected 0 to be 1" at line 168 | absolute count assertion |
    | test/backend/.../file2.test.ts | FAIL | "already exists" at line 45 | missing-cleanup / pollution |
    | ... | ... | ... | ... |

    ## Per-File Pass/Fail Matrix

    | Domain | File | In-Suite | (Isolated — filled during Plan 04) |
    |--------|------|----------|-------------------------------------|
    | achievements | achievement-auto-award.test.ts | FAIL | TBD |
    | achievements | achievement-management.test.ts | PASS | — |
    | ... full list of all 41 integration .test.ts files ... | | | |

    ## Category Definitions (per 10.5-RESEARCH.md)

    | Category | Description | Fix Pattern |
    |----------|-------------|-------------|
    | pollution | Test fails because earlier file left DB state | Add afterAll cleanup to the polluter |
    | missing-cleanup | Test file creates resources but does not clean up | Add openedLobbyIds[] + afterAll teardown |
    | absolute-count | Assertion uses toBe(0) or toBe(N) against counts that drift | Replace with beforeCount + delta pattern |
    | ordering | Test B expects artifact created by Test A | Make B self-contained per D-04 |
    | stale-assertion | Renamed column/enum from 10.1/10.4 | Update to current name |
    | seeding-gap | Test expects pre-seeded data that is not seeded globally | Self-seed per D-04/D-09 |
    | awaiting-result | Lobby stuck in AwaitingResult, needs admin_void_match | Add admin harness + admin_void_match cleanup |
    | over-seeded | Test inserts too many entities for scenario | Trim per D-11 |

    ## File ↔ Polluter Matrix (Filled during Plan 04)

    | Failing File | Earliest Polluter (bisected) | Root Cause | Fix Applied | Commit |
    |--------------|------------------------------|------------|-------------|--------|
    | TBD | TBD | TBD | TBD | TBD |

    ## Lessons Learned (Filled during Plan 05)

    TBD

    ---
    *Audit artifact for Phase 10.5: Test Suite Stabilization*
    *Canonical reset: `spacetime publish --clear-database` → bootstrap → seed → test:all*
    ```

    Populate the tables from `baseline.log`:
    - For the Baseline Runtime table: extract wall-clock time, file counts, pass/fail/skip counts.
    - For the Full-Suite Failure Inventory: one row per FAILED file, with the first error line from the log as "Failure Signature" and your best-guess category from the 8 defined categories.
    - For the Per-File Pass/Fail Matrix: one row for EVERY `.test.ts` integration file (exclude `.unit.test.ts`). Leave "Isolated" column as TBD — Plan 04 fills it.
    - For File ↔ Polluter Matrix: leave body empty, header row only — Plan 04 fills it.
    - For Lessons Learned: write "TBD" — Plan 05 fills it.

    **Part B — Add "Suite Runtime" section to `test/README.md`:**

    APPEND (do not replace) a new top-level section at the END of `test/README.md` with exactly this structure:

    ```markdown

    ## Suite Runtime

    Measured baseline for regression detection. Update this entry when the suite composition materially changes (new files, new fixtures, major refactors).

    | Run | Date | Wall-Clock Time | File Count | Notes |
    |-----|------|-----------------|------------|-------|
    | Pre-stabilization (Phase 10.5 baseline) | 2026-04-05 | {X}m{Y}s | {N} files | Captured before audit fixes — {F} failures |

    **Commands:**
    - Full suite: `npm run test:all`
    - Integration only: `npm run test:integration`
    - Single file: `npx vitest run --config test/vitest.integration.config.ts <path>`

    **Reset sequence** (when test DB gets corrupted):
    1. `spacetime publish --module-path spacetimedb --server maincloud --clear-database -y`
    2. `spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb`
    3. `npx tsx test/shared/bootstrap.ts`
    4. `npx tsx test/shared/seed-data.ts`
    5. `npm run test:all`
    ```

    Fill `{X}m{Y}s`, `{N}`, `{F}` with the actual values from `baseline.log`.

    **Commit (per D-08):**
    ```
    git add .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md test/README.md
    git commit -m "docs(10.5): capture baseline failure inventory + runtime"
    ```

    Note: per CLAUDE.md rules, test/README.md IS documentation (not test code), so committing is allowed. If user instructs otherwise during execution, leave staged.
  </action>
  <verify>
    <automated>test -f .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md &amp;&amp; grep -q "Baseline Runtime" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md &amp;&amp; grep -q "Suite Runtime" test/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → exit 0
    - `grep -c "## Baseline Runtime" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "## Full-Suite Failure Inventory" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "## Per-File Pass/Fail Matrix" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "## File ↔ Polluter Matrix" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 1
    - `grep -c "## Suite Runtime" test/README.md` → 1
    - `grep -cE "\.test\.ts" .planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` → 20 or greater (Per-File Matrix populated)
    - `grep -E "[0-9]+m[0-9]+s|[0-9]+\.[0-9]+s" test/README.md | grep -i runtime || grep -E "Wall-Clock" test/README.md` → shows measured time (non-placeholder)
  </acceptance_criteria>
  <done>
    AUDIT.md exists with all 6 required sections (Baseline Runtime, Failure Inventory, Per-File Matrix, Category Definitions, Polluter Matrix, Lessons Learned), Per-File Matrix is populated from baseline.log, test/README.md has the new Suite Runtime section with real measured values.
  </done>
</task>

</tasks>

<verification>
- AUDIT.md artifact exists at `.planning/phases/10.5-test-suite-stabilization/10.5-AUDIT.md` with all 6 required sections
- `grep -q "Suite Runtime" test/README.md` returns exit 0
- Per-File Matrix in AUDIT.md lists every integration `.test.ts` file under `test/backend/` (~41 files)
- Baseline runtime is a real measured value, not "TBD"
- No test files modified in this plan (pure artifact creation)
</verification>

<success_criteria>
Plan 01 complete when baseline.log capture succeeded, AUDIT.md populated with the failure inventory + per-file matrix + category taxonomy, and test/README.md has a Suite Runtime section with real measured values. This plan unblocks Plan 02 (helper extraction targets) and Plan 04 (bisect input).
</success_criteria>

<output>
After completion, create `.planning/phases/10.5-test-suite-stabilization/10.5-01-SUMMARY.md` recording:
- Baseline runtime (X minutes Y seconds)
- Pass/fail counts
- Top-3 suspected polluters from initial hypothesis
- Any env-var or publish-step surprises encountered
</output>
