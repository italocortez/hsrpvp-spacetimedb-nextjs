---
phase: 06.1-landing-page-migration
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - components/features/landing/components/HeroSection.tsx
  - components/features/landing/components/HeroSection.module.css
  - components/features/landing/components/FeaturesSection.tsx
  - components/features/landing/components/FeaturesSection.module.css
  - components/features/landing/components/ContactSection.tsx
  - components/features/landing/components/ContactSection.module.css
  - components/features/landing/components/FeatureCards.tsx
  - components/features/landing/components/FeatureCards.module.css
  - app/(landing-page)/page.tsx
  - app/(landing-page)/page.module.css
  - app/(landing-page)/layout.module.css
autonomous: false
requirements:
  - LP-SECTIONS
  - LP-PAGE

must_haves:
  truths:
    - "HeroSection displays video background with gradient overlays, HUD elements, and overlay text"
    - "FeaturesSection shows rotating featured card with auto-rotate timer and bottom carousel of other features"
    - "ContactSection displays creator, discord, and servers cards with server carousel"
    - "No CSP violations in browser console (icon-lucide replaced with inline SVGs)"
    - "Landing page renders full composition: NavBar (from root layout) + HeroSection + FeaturesSection + ContactSection"
    - "Old FeatureCards component deleted and not imported anywhere"
  artifacts:
    - path: "components/features/landing/components/HeroSection.tsx"
      provides: "Video hero with loader, overlays, and HUD"
      exports: ["HeroSection"]
      min_lines: 50
    - path: "components/features/landing/components/FeaturesSection.tsx"
      provides: "Feature showcase with auto-rotating carousel"
      exports: ["FeaturesSection"]
      min_lines: 100
    - path: "components/features/landing/components/ContactSection.tsx"
      provides: "Contact section with creator/discord/servers cards"
      exports: ["ContactSection"]
      min_lines: 80
    - path: "app/(landing-page)/page.tsx"
      provides: "Landing page composition"
      min_lines: 15
    - path: "app/(landing-page)/page.module.css"
      provides: "Landing page container styles"
      contains: "width: 100%"
  key_links:
    - from: "app/(landing-page)/page.tsx"
      to: "components/features/landing/components/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection"
    - from: "app/(landing-page)/page.tsx"
      to: "components/features/landing/components/FeaturesSection.tsx"
      via: "import and render"
      pattern: "import.*FeaturesSection"
    - from: "app/(landing-page)/page.tsx"
      to: "components/features/landing/components/ContactSection.tsx"
      via: "import and render"
      pattern: "import.*ContactSection"
    - from: "components/features/landing/components/HeroSection.tsx"
      to: "/recording/landing-page-loop.mov"
      via: "video src prop"
      pattern: "landing-page-loop"
---

<objective>
Migrate the three landing page sections (HeroSection, FeaturesSection, ContactSection), compose the landing page, and delete the old FeatureCards component. This completes the landing page visual migration.

Purpose: With the design tokens, fonts, and NavBar in place from Plan 01, this plan brings in the actual landing page content -- the video hero, feature showcase carousel, and contact section.
Output: Three new landing section components, rewritten landing page, old FeatureCards removed, visual checkpoint.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-landing-page-migration/06.1-CONTEXT.md
@.planning/phases/06.1-landing-page-migration/06.1-RESEARCH.md
@.planning/phases/06.1-landing-page-migration/06.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: NavBar is rendered by root layout, not by page.tsx -->
<!-- tokens.css provides all --bg-*, --text-*, --font-*, --color-* variables -->
<!-- page.tsx should NOT render NavBar (comes from root layout per Plan 01) -->

From components/features/landing/components/ (being replaced):
```typescript
// FeatureCards.tsx -- to be deleted, replaced by FeaturesSection
// FeatureCards.module.css -- to be deleted
```

Source components to migrate:
```typescript
// HeroSection: named export, props: { videoSrc: string, loaderBrand?: string, title?: string, subtitle?: string }
// FeaturesSection: named export, no props
// ContactSection: named export, no props, uses icon-lucide (MUST convert to inline SVG)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HeroSection, FeaturesSection, ContactSection and compose landing page</name>
  <files>
    components/features/landing/components/HeroSection.tsx,
    components/features/landing/components/HeroSection.module.css,
    components/features/landing/components/FeaturesSection.tsx,
    components/features/landing/components/FeaturesSection.module.css,
    components/features/landing/components/ContactSection.tsx,
    components/features/landing/components/ContactSection.module.css,
    components/features/landing/components/FeatureCards.tsx,
    components/features/landing/components/FeatureCards.module.css,
    app/(landing-page)/page.tsx,
    app/(landing-page)/page.module.css,
    app/(landing-page)/layout.module.css
  </files>
  <read_first>
    D:/AI Playground/ImportPecilToCode/src/components/HeroSection/HeroSection.tsx,
    D:/AI Playground/ImportPecilToCode/src/components/HeroSection/HeroSection.module.css,
    D:/AI Playground/ImportPecilToCode/src/components/FeaturesSection/FeaturesSection.tsx,
    D:/AI Playground/ImportPecilToCode/src/components/FeaturesSection/FeaturesSection.module.css,
    D:/AI Playground/ImportPecilToCode/src/components/ContactSection/ContactSection.tsx,
    D:/AI Playground/ImportPecilToCode/src/components/ContactSection/ContactSection.module.css,
    D:/AI Playground/ImportPecilToCode/src/app/page.tsx,
    D:/AI Playground/ImportPecilToCode/src/app/page.module.css,
    D:/GitsWork/hsrpvp-spacetimedb-nextjs/app/(landing-page)/page.tsx,
    D:/GitsWork/hsrpvp-spacetimedb-nextjs/app/(landing-page)/page.module.css,
    D:/GitsWork/hsrpvp-spacetimedb-nextjs/app/(landing-page)/layout.module.css,
    D:/GitsWork/hsrpvp-spacetimedb-nextjs/components/features/landing/components/FeatureCards.tsx
  </read_first>
  <action>
    SKILL REQUIRED: Load `/frontend-design` skill before writing any code.

    **1. Create HeroSection (per D-01, D-04, D-14)**
    Copy `D:/AI Playground/ImportPecilToCode/src/components/HeroSection/HeroSection.tsx` to `components/features/landing/components/HeroSection.tsx`.
    Copy `D:/AI Playground/ImportPecilToCode/src/components/HeroSection/HeroSection.module.css` to `components/features/landing/components/HeroSection.module.css`.

    Modifications to HeroSection.tsx:
    - Import path for CSS module: `./HeroSection.module.css` (already correct)
    - Add `'use client'` at top (already present in source)
    - Keep all source JSX and logic as-is -- video loading, loader animation, gradient overlays, HUD elements
    - Named export `HeroSection` (already matches source)

    **2. Create FeaturesSection (per D-01, D-04)**
    Copy `D:/AI Playground/ImportPecilToCode/src/components/FeaturesSection/FeaturesSection.tsx` to `components/features/landing/components/FeaturesSection.tsx`.
    Copy `D:/AI Playground/ImportPecilToCode/src/components/FeaturesSection/FeaturesSection.module.css` to `components/features/landing/components/FeaturesSection.module.css`.

    Modifications to FeaturesSection.tsx:
    - Import path for CSS module: `./FeaturesSection.module.css` (already correct)
    - `'use client'` already present in source
    - Keep all carousel logic, auto-rotate timer, scroll handling as-is
    - Named export `FeaturesSection` (already matches source)

    **3. Create ContactSection (per D-01, D-04) -- WITH icon-lucide conversion**
    Copy `D:/AI Playground/ImportPecilToCode/src/components/ContactSection/ContactSection.tsx` to `components/features/landing/components/ContactSection.tsx`.
    Copy `D:/AI Playground/ImportPecilToCode/src/components/ContactSection/ContactSection.module.css` to `components/features/landing/components/ContactSection.module.css`.

    CRITICAL: Convert icon-lucide usages to inline SVGs (CSP blocks CDN fonts -- Pitfall 2 from RESEARCH.md).

    In ContactSection.tsx, there are TWO places using `icon-lucide`:

    a) **Discord card copy button** (line 93): Replace:
    ```tsx
    <span className={`icon-lucide ${styles.copyIcon}`}>&#xe869;</span>
    ```
    with inline SVG (Lucide "copy" icon):
    ```tsx
    <svg className={styles.copyIcon} viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <rect width="14" height="14" x="8" y="8" rx="2" ry="2" />
      <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2" />
    </svg>
    ```

    b) **Server carousel icons** (line 131-134): Replace:
    ```tsx
    <span className={`icon-lucide ${styles.serverIcon}`} dangerouslySetInnerHTML={{ __html: server.iconCode }} />
    ```
    with per-server inline SVGs. Update the `SERVERS` array to use SVG elements instead of icon codes:

    Change `ServerInfo` interface to:
    ```typescript
    interface ServerInfo {
      name: string;
      subtitle?: string;
      icon: React.ReactNode;
      accentColor: string;
      bgColor: string;
    }
    ```

    Replace `iconCode` with `icon` JSX in the SERVERS array:
    ```typescript
    const SERVERS: ServerInfo[] = [
      {
        name: 'Solar System',
        subtitle: 'Official Tournament Server',
        icon: (
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
            <circle cx="12" cy="12" r="10" />
            <line x1="2" y1="12" x2="22" y2="12" />
            <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" />
          </svg>
        ),
        accentColor: '#34D399',
        bgColor: '#1A3D2E',
      },
      {
        name: 'Yujicord',
        icon: (
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
          </svg>
        ),
        accentColor: '#8B6CE8',
        bgColor: '#231E3D',
      },
      {
        name: 'Genius Society',
        icon: (
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
            <path d="M18 20V10" /><path d="M12 20V4" /><path d="M6 20v-6" />
          </svg>
        ),
        accentColor: '#F24E4E',
        bgColor: '#3D1E1E',
      },
    ];
    ```

    Then replace the `dangerouslySetInnerHTML` span with:
    ```tsx
    <span className={styles.serverIcon}>{server.icon}</span>
    ```

    Also update the ContactSection.module.css `.copyIcon` class to work with SVG instead of icon-font. If the source CSS uses `font-family: lucide` on `.copyIcon`, remove that and adjust sizing for SVG (width/height instead of font-size). The `.serverIcon` class similarly needs SVG-compatible styling (set `width`, `height`, `display: flex`, remove `font-family`).

    Add click-to-copy functionality to the Discord card copy button (wire `navigator.clipboard.writeText("nathyron")`).

    **4. Rewrite app/(landing-page)/page.tsx (per D-15)**
    Replace the entire file. The new page does NOT import NavBar (NavBar is in root layout). The new page does NOT import LoginForm or useAuthContext (the old welcome/auth section is replaced by the HeroSection).

    New content:
    ```typescript
    import styles from './page.module.css';
    import { HeroSection } from '@/components/features/landing/components/HeroSection';
    import { FeaturesSection } from '@/components/features/landing/components/FeaturesSection';
    import { ContactSection } from '@/components/features/landing/components/ContactSection';

    export default function LandingPage() {
      return (
        <div className={styles.page}>
          <HeroSection videoSrc="/recording/landing-page-loop.mov" />
          <FeaturesSection />
          <ContactSection />
        </div>
      );
    }
    ```

    Note: Use a div not main to avoid the global main CSS rule (even though Task 1 of Plan 01 removed the width constraint, using div is safer and matches source's intent). The page does NOT need 'use client' because it does not use any hooks itself -- the child components handle their own client state.

    **5. Rewrite app/(landing-page)/page.module.css (per D-04)**
    Replace with the source's page.module.css content. The key class is `.page`:
    ```css
    .page {
      width: 100%;
      min-height: 100vh;
      background: var(--bg-page);
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }
    ```
    Keep only the `.page` class -- the feature card styles in source page.module.css are unused since FeaturesSection has its own module.

    **6. Update app/(landing-page)/layout.module.css**
    Change the `background-color` from `rgb(17, 24, 39)` (old blue-gray) to `var(--bg-page)` (the void black from tokens.css, `#0A0A0B`) so the layout wrapper matches the landing page design.

    **7. Delete old FeatureCards (replaced by FeaturesSection)**
    Delete `components/features/landing/components/FeatureCards.tsx` and `components/features/landing/components/FeatureCards.module.css`.
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && test -f components/features/landing/components/HeroSection.tsx && test -f components/features/landing/components/FeaturesSection.tsx && test -f components/features/landing/components/ContactSection.tsx && ! test -f components/features/landing/components/FeatureCards.tsx && ! grep -q "icon-lucide" components/features/landing/components/ContactSection.tsx && grep -q "HeroSection" "app/(landing-page)/page.tsx" && grep -q "FeaturesSection" "app/(landing-page)/page.tsx" && grep -q "ContactSection" "app/(landing-page)/page.tsx" && ! grep -q "FeatureCards" "app/(landing-page)/page.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - components/features/landing/components/HeroSection.tsx exists and contains `export const HeroSection`
    - components/features/landing/components/HeroSection.module.css exists (240+ lines)
    - components/features/landing/components/FeaturesSection.tsx exists and contains `export const FeaturesSection`
    - components/features/landing/components/FeaturesSection.module.css exists (422+ lines)
    - components/features/landing/components/ContactSection.tsx exists and contains `export const ContactSection`
    - components/features/landing/components/ContactSection.tsx does NOT contain `icon-lucide`
    - components/features/landing/components/ContactSection.tsx does NOT contain `dangerouslySetInnerHTML`
    - components/features/landing/components/ContactSection.module.css exists (265+ lines)
    - components/features/landing/components/FeatureCards.tsx does NOT exist
    - components/features/landing/components/FeatureCards.module.css does NOT exist
    - app/(landing-page)/page.tsx imports HeroSection, FeaturesSection, ContactSection
    - app/(landing-page)/page.tsx does NOT import LoginForm or FeatureCards
    - app/(landing-page)/page.tsx does NOT import useAuthContext
    - app/(landing-page)/page.module.css contains `width: 100%` and `background: var(--bg-page)`
    - app/(landing-page)/layout.module.css contains `var(--bg-page)` not `rgb(17, 24, 39)`
  </acceptance_criteria>
  <done>Three landing sections migrated with icon-lucide converted to inline SVGs. Landing page composed. Old FeatureCards deleted. No CSP violations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete landing page</name>
  <files>app/(landing-page)/page.tsx</files>
  <action>
    Human verifies the complete landing page migration visually. What was built:

    1. NavBar at top (Logo + 5 nav items + gear + auth section) from root layout
    2. HeroSection with video background, gradient overlays, HUD elements, and "IPC BATTLEGROUNDS" text
    3. FeaturesSection with rotating featured card (auto-rotates every 15s) and bottom carousel of feature cards
    4. ContactSection with creator/discord/servers cards and server carousel (auto-rotates every 4s)

    Steps to verify:
    1. Run `npm run dev` to start the dev server
    2. Navigate to http://localhost:3001 (or the configured port)
    3. Verify the following:

    **NavBar:**
    - Logo (gradient diamond SVG) appears at left
    - 5 nav items visible: LOBBIES, TEAM BUILDER, COST TABLES, TOURNAMENTS, EVENTS
    - Click LOBBIES navigates to /lobby
    - Click COST TABLES navigates to /costs
    - TOURNAMENTS and EVENTS are non-functional
    - Right section shows "LOG IN" button (if not authenticated) or username (if authenticated)
    - Click LOG IN triggers guest login

    **HeroSection:**
    - Video plays in background (topaz animation loop)
    - Loader animation shows briefly while video loads
    - Gradient overlays at bottom and vignette visible
    - "HONKAI STAR RAIL // PVP" label, "IPC BATTLEGROUNDS" title, "DRAFT. BATTLE. DOMINATE." subtitle
    - HUD elements in corners (SYS://ONLINE, 24 FPS, LAT: 12ms)
    - "SCROLL TO EXPLORE" indicator at bottom

    **FeaturesSection:**
    - Featured card shows with large icon and description
    - Auto-rotates to next feature every 15 seconds
    - Bottom row shows "MORE FEATURES" with scrollable card carousel
    - Clicking a bottom card promotes it to featured

    **ContactSection:**
    - Three cards: CREATOR, DISCORD, SERVERS
    - Discord card has "nathyron" with copy button (click should copy to clipboard)
    - Server carousel auto-rotates between Solar System, Yujicord, Genius Society
    - Icons render as inline SVGs (NOT as missing/broken icon-font glyphs)

    **General:**
    - Open browser DevTools console: NO CSP violation errors
    - JetBrains Mono font renders on headings/labels (monospace appearance)
    - Color scheme matches dark theme with void black background (#0A0A0B)
    - Scrolling through all sections works smoothly

    Resume signal: Type "approved" or describe any visual/functional issues to fix.
  </action>
  <verify>Human visual inspection of landing page at localhost</verify>
  <done>User approves that landing page matches design expectations from source project. All sections render, animations work, no console errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. No `icon-lucide` references in any project file: `grep -r "icon-lucide" components/` returns zero matches
3. No `FeatureCards` references: `grep -r "FeatureCards" app/ components/` returns zero matches
4. No `Header` references: `grep -r "import.*Header.*globals/layout" app/ components/` returns zero matches
5. Existing test suite: `npm test` passes (no regressions from backend tests)
6. Landing page loads at localhost with all four sections visible
</verification>

<success_criteria>
- Landing page displays video hero, feature carousel, and contact section
- All animations work (video loop, feature auto-rotate, server carousel)
- ContactSection icons render via inline SVG (no CSP violations)
- Click-to-copy on Discord username works
- Nav items route correctly (LOBBIES, TEAM BUILDER, COST TABLES)
- Auth state reflected in NavBar
- Old Header and FeatureCards completely removed
- User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-landing-page-migration/06.1-02-SUMMARY.md`
</output>
