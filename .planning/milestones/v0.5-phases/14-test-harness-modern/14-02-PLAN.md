---
phase: 14-test-harness-modern
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - test/backend/anonymous-play/anonymous-labels.test.ts
  - test/backend/match-session/draft-control.test.ts
  - test/backend/chat/chat-messages.test.ts
  - test/backend/lobby/account-selection.test.ts
  - test/backend/lobby/lobby-lifecycle.test.ts
  - test/backend/lobby/lobby-settings.test.ts
  - test/backend/match-session/draft-classic.test.ts
  - test/backend/lobby/disconnect-leave-lobby.test.ts
  - test/backend/lobby/lobby-tournament.test.ts
  - test/backend/match-results/referee-coach.test.ts
  - test/backend/roster/account-deletion-guard.test.ts
  - test/README.md
autonomous: true
requirements:
  - TEST-MODERN-01
  - TEST-MODERN-04

must_haves:
  truths:
    - "No test file has a local latestLobby() function -- all use the shared import"
    - "No test file has a local myLobbies() function -- all use the shared import"
    - "No test file has a local lobbyMembers() function -- all use the shared import"
    - "No test file has a local lobbyBans() function -- all use the shared import"
    - "test/README.md Suite Runtime table has a new row with post-Phase-14 timing"
    - "Full test suite passes with 0 failures"
  artifacts:
    - path: "test/shared/helpers/queries.ts"
      provides: "Canonical shared query helpers (latestLobby, myLobbies, lobbyMembers, lobbyBans)"
      exports: ["myLobbies", "latestLobby", "lobbyMembers", "lobbyBans"]
    - path: "test/README.md"
      provides: "Updated Suite Runtime table"
      contains: "Phase 14"
  key_links:
    - from: "test/backend/anonymous-play/anonymous-labels.test.ts"
      to: "test/shared/helpers/queries.ts"
      via: "import { latestLobby } from"
      pattern: "import.*latestLobby.*from.*shared/helpers/queries"
    - from: "test/backend/match-session/draft-control.test.ts"
      to: "test/shared/helpers/queries.ts"
      via: "import { latestLobby } from"
      pattern: "import.*latestLobby.*from.*shared/helpers/queries"
---

<objective>
Replace all local duplicate query helpers (latestLobby, myLobbies, lobbyMembers, lobbyBans) in test files with imports from the shared `test/shared/helpers/queries.ts`, then run the full test suite and record the new runtime baseline.

Purpose: Eliminate duplicated test helper code (D-06, D-07 opportunistic sweep) and capture the runtime improvement from Plan 01's onApplied migration (D-08).

Output: Clean test files with shared imports, updated Suite Runtime table in test/README.md.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-test-harness-modern/14-CONTEXT.md
@.planning/phases/14-test-harness-modern/14-01-SUMMARY.md

<interfaces>
<!-- Shared query helpers that all test files should import from -->

From test/shared/helpers/queries.ts:
```typescript
import type { TestHarness } from '../connection';

export function myLobbies(h: TestHarness): Lobby[];
export function latestLobby(h: TestHarness): Lobby | undefined;
export function lobbyMembers(h: TestHarness, lobbyId: number): LobbyMember[];
export function lobbyBans(h: TestHarness, lobbyId: number): LobbyBan[];
```

Import path from test/backend/*/*.test.ts:
```typescript
import { latestLobby, myLobbies, lobbyMembers, lobbyBans } from '../../shared/helpers/queries';
// Or for deeper nesting:
import { latestLobby } from '../../../shared/helpers/queries';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace local duplicate helpers with shared imports (D-06, D-07)</name>
  <files>
    test/backend/anonymous-play/anonymous-labels.test.ts,
    test/backend/match-session/draft-control.test.ts,
    test/backend/chat/chat-messages.test.ts,
    test/backend/lobby/account-selection.test.ts,
    test/backend/lobby/lobby-lifecycle.test.ts,
    test/backend/lobby/lobby-settings.test.ts,
    test/backend/match-session/draft-classic.test.ts,
    test/backend/lobby/disconnect-leave-lobby.test.ts,
    test/backend/lobby/lobby-tournament.test.ts,
    test/backend/match-results/referee-coach.test.ts,
    test/backend/roster/account-deletion-guard.test.ts,
    test/shared/helpers/queries.ts
  </files>
  <read_first>
    test/shared/helpers/queries.ts,
    test/backend/anonymous-play/anonymous-labels.test.ts,
    test/backend/match-session/draft-control.test.ts,
    test/backend/chat/chat-messages.test.ts,
    test/backend/lobby/account-selection.test.ts,
    test/backend/lobby/lobby-lifecycle.test.ts,
    test/backend/lobby/lobby-settings.test.ts,
    test/backend/match-session/draft-classic.test.ts,
    test/backend/lobby/disconnect-leave-lobby.test.ts,
    test/backend/lobby/lobby-tournament.test.ts,
    test/backend/match-results/referee-coach.test.ts,
    test/backend/roster/account-deletion-guard.test.ts
  </read_first>
  <action>
For each test file listed below, remove the local helper function(s) and add an import from `test/shared/helpers/queries.ts`. Use the correct relative import path based on file depth.

**Replacement map (file -> remove local functions -> add import):**

1. **anonymous-labels.test.ts** (test/backend/anonymous-play/):
   - Remove: local `hostLobbies(h)` (line 31-33), local `latestLobby(h)` (line 35-37)
   - Add import: `import { myLobbies, latestLobby } from '../../shared/helpers/queries';`
   - Note: `hostLobbies` is semantically identical to `myLobbies` -- update all call sites from `hostLobbies(h)` to `myLobbies(h)`. Check usages beyond `latestLobby` itself (e.g., `waitForNewLobby` or direct `hostLobbies` calls).

2. **draft-control.test.ts** (test/backend/match-session/):
   - Remove: local `latestLobby(h)` (line 29-32)
   - Add import: `import { latestLobby } from '../../shared/helpers/queries';`

3. **chat-messages.test.ts** (test/backend/chat/):
   - Remove: local `myLobbies(h)` (line 27-29)
   - Add import: `import { myLobbies } from '../../shared/helpers/queries';`

4. **account-selection.test.ts** (test/backend/lobby/):
   - Remove: local `myLobbies(h)` (line 67-69), local `lobbyMembers(h, lobbyId)` (line 72-74)
   - Add import: `import { myLobbies, lobbyMembers } from '../../shared/helpers/queries';`

5. **lobby-lifecycle.test.ts** (test/backend/lobby/):
   - Remove: local `myLobbies(h)` (line 30-32), local `lobbyMembers(h, lobbyId)` (line 35-37), local `lobbyBans(h, lobbyId)` (line 40-42)
   - Add import: `import { myLobbies, lobbyMembers, lobbyBans } from '../../shared/helpers/queries';`

6. **lobby-settings.test.ts** (test/backend/lobby/):
   - Remove: local `myLobbies(h)` (line 26-28), local `lobbyMembers(h, lobbyId)` (line 30-32)
   - Add import: `import { myLobbies, lobbyMembers } from '../../shared/helpers/queries';`

7. **draft-classic.test.ts** (test/backend/match-session/):
   - Remove: local `myLobbies(h)` (line 30-32), local `lobbyMembers(h, lobbyId)` (line 35-37)
   - Add import: `import { myLobbies, lobbyMembers } from '../../shared/helpers/queries';`

8. **disconnect-leave-lobby.test.ts** (test/backend/lobby/):
   - Remove: local `lobbyMembers(h, lobbyId)` (line 79-81)
   - Add import: `import { lobbyMembers } from '../../shared/helpers/queries';`

9. **lobby-tournament.test.ts** (test/backend/lobby/):
   - Remove: local `myLobbies(h)` (line 27-29), local `lobbyMembers(h, lobbyId)` (line 32-34)
   - Add import: `import { myLobbies, lobbyMembers } from '../../shared/helpers/queries';`

10. **referee-coach.test.ts** (test/backend/match-results/):
    - Remove: local `lobbyMembers(h, lobbyId)` (line 25-27)
    - Add import: `import { lobbyMembers } from '../../shared/helpers/queries';`

11. **account-deletion-guard.test.ts** (test/backend/roster/):
    - Remove: local `myLobbies(h)` (line 37-39)
    - Add import: `import { myLobbies } from '../../shared/helpers/queries';`

**Import path convention:**
- Files in `test/backend/{feature}/` use: `../../shared/helpers/queries`
- The existing shared queries.ts uses relative import `../connection` for types

**Important:** Do NOT modify any test logic, assertions, or other helper functions that are unique to each file (like `waitForNewLobby` in anonymous-labels.test.ts). Only remove the helpers that are exact duplicates of queries.ts exports.

**After all replacements:** Run `grep -rn "function latestLobby\|function myLobbies\|function lobbyMembers\|function lobbyBans" test/backend/` to confirm zero remaining local duplicates.
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && grep -rn "function latestLobby\|function myLobbies\|function lobbyMembers\|function lobbyBans" test/backend/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "function latestLobby" test/backend/` returns 0 matches
    - `grep -rn "function myLobbies" test/backend/` returns 0 matches
    - `grep -rn "function lobbyMembers" test/backend/` returns 0 matches
    - `grep -rn "function lobbyBans" test/backend/` returns 0 matches
    - `grep -rn "function hostLobbies" test/backend/` returns 0 matches
    - `grep -rn "from.*shared/helpers/queries" test/backend/ | wc -l` returns at least 11 (one per modified file)
    - No test file has both a local helper AND an import from queries.ts for the same function
  </acceptance_criteria>
  <done>All local duplicate query helpers removed from test/backend/ files. All files import from test/shared/helpers/queries.ts. Zero local latestLobby/myLobbies/lobbyMembers/lobbyBans functions remain in test/backend/.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and record new runtime baseline (D-08, per TEST-MODERN-04)</name>
  <files>test/README.md</files>
  <read_first>
    test/README.md
  </read_first>
  <action>
Run the full test suite and record the new runtime baseline in test/README.md.

**Step 1:** Run full suite with timing:
```bash
npm run test:all 2>&1
```

Capture:
- Total wall-clock time
- Number of test files (integration + unit)
- Number of tests (pass/fail/skip counts)
- Any failures (should be 0)

**Step 2:** Add a new row to the Suite Runtime table in test/README.md (around line 68).

Format the new row exactly like existing rows:
```
| Post-harness-modernization (Phase 14) | 2026-04-09 | {time} | {file count} integration files ({test count} tests) + {unit files} unit files ({unit count} tests) | onApplied subscription readiness replaces 2s setTimeout; .withConfirmedReads(false) on all builders; duplicate helpers deduplicated to shared queries.ts |
```

Replace `{time}`, `{file count}`, `{test count}`, `{unit files}`, `{unit count}` with actual measured values.

**Step 3:** If the measured time is less than 54m38s (the current baseline), TEST-MODERN-04 is satisfied. If not, note the timing -- the requirement says "any measurable improvement" so even a few seconds faster counts. If somehow slower (unlikely given ~160s of setTimeout removal), document that finding.

**Important:** Do NOT modify any test files, fixtures, or configurations. This task is measurement-only plus the README update.
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && grep -c "Phase 14" test/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Phase 14" test/README.md` returns at least 1 match (new baseline row exists)
    - The Suite Runtime table in test/README.md has 3 rows (2 existing + 1 new)
    - The new row contains actual measured wall-clock time, file count, and test count
    - Full test suite completed with 0 failures (report any failures in the SUMMARY)
  </acceptance_criteria>
  <done>New runtime baseline recorded in test/README.md Suite Runtime table. Full suite passes with 0 failures. The measured time demonstrates improvement over the 54m38s baseline (or documents the actual result if not).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only modifies test helper imports and updates documentation. No trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | Shared queries.ts import path | accept | Import paths are relative within the test directory. No external or user-facing code affected. Incorrect imports would cause immediate TypeScript compilation errors caught by the test runner. |
</threat_model>

<verification>
1. `grep -rn "function latestLobby\|function myLobbies\|function lobbyMembers\|function lobbyBans\|function hostLobbies" test/backend/` returns 0 matches
2. `grep -rn "from.*shared/helpers/queries" test/backend/ | wc -l` returns >= 11
3. `grep "Phase 14" test/README.md` returns 1 match
4. Full test suite passes: `npm run test:all` exits 0
</verification>

<success_criteria>
- All local duplicate query helpers replaced with shared imports
- Full test suite passes with 0 failures
- New runtime baseline recorded in test/README.md
- Measured runtime is faster than 54m38s baseline
</success_criteria>

<output>
After completion, create `.planning/phases/14-test-harness-modern/14-02-SUMMARY.md`
</output>
