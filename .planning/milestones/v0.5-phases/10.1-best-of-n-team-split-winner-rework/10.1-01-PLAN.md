---
phase: 10.1-best-of-n-team-split-winner-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/types/enums.ts
  - spacetimedb/src/tables/groupStanding.ts
  - spacetimedb/src/tables/groupPhaseRecord.ts
  - spacetimedb/src/tables/tournamentParticipant.ts
  - spacetimedb/src/tables/tournamentEnrolled.ts
  - spacetimedb/src/tables/tournamentTeamMember.ts
  - spacetimedb/src/tables/lobby.ts
  - spacetimedb/src/tables/matchSession.ts
  - spacetimedb/src/tables/matchSessionStep.ts
  - spacetimedb/src/tables/matchSessionStepHistory.ts
  - spacetimedb/src/tables/matchResult.ts
  - spacetimedb/src/tables/matchSessionHistory.ts
  - spacetimedb/src/tables/bracketMatch.ts
  - spacetimedb/src/schema.ts
autonomous: true
requirements:
  - D-01
  - D-03
  - D-06
  - D-11
  - D-12
  - D-13
  - D-18
  - D-20
  - D-30
  - D-31
  - D-32
  - D-35
  - D-42
  - D-43

must_haves:
  truths:
    - "MatchEndReason enum exists with Completed/Draw/Concede variants; MatchOutcome enum is removed"
    - "LobbyStage enum has BetweenGames and Shelved variants"
    - "TournamentStage enum has CheckIn variant between Registration and Seeding"
    - "GroupPhaseRecord table exists (renamed from GroupStanding); old file deleted"
    - "TournamentEnrolled table exists with (tournamentId, userId) PK"
    - "TournamentTeamMember table exists with (teamId, userId) PK and tournamentId btree index"
    - "TournamentParticipant table file is deleted"
    - "MatchResultRecord has winnerTeamSide (TeamSide?) and matchEndReason (MatchEndReason?) instead of winnerUserId and matchOutcome"
    - "MatchResultRecord no longer has tournamentId column or tournament_id index"
    - "BracketMatch no longer has lobbyId column or lobby_id index"
    - "MatchResultRecord has bracket_match_id btree index on bracketMatchId"
    - "Lobby has bracket_match_id btree index on bracketMatchId (for reverse lookup from BracketMatch to Lobby in cancel_tournament cascade)"
    - "Lobby has bestOf (u8) and refereeControlsShelving (bool) columns"
    - "MatchSession has currentGameNumber, gamesWonBlue, gamesWonRed, seriesBestOf columns"
    - "MatchSessionStep and MatchSessionStepHistory have gameNumber (u8) column"
    - "MatchSessionStepHistory PK is [matchHistoryId, gameNumber, sequence]"
    - "schema.ts registers GroupPhaseRecord, TournamentEnrolled, TournamentTeamMember; removes GroupStanding, TournamentParticipant"
  artifacts:
    - path: "spacetimedb/src/types/enums.ts"
      provides: "MatchEndReason enum, LobbyStage +BetweenGames/Shelved, TournamentStage +CheckIn"
      contains: "MatchEndReason"
    - path: "spacetimedb/src/tables/groupPhaseRecord.ts"
      provides: "GroupPhaseRecord table definition"
      contains: "GroupPhaseRecord"
    - path: "spacetimedb/src/tables/tournamentEnrolled.ts"
      provides: "TournamentEnrolled table (replaces TournamentParticipant)"
      contains: "TournamentEnrolled"
    - path: "spacetimedb/src/tables/tournamentTeamMember.ts"
      provides: "TournamentTeamMember join table"
      contains: "TournamentTeamMember"
    - path: "spacetimedb/src/schema.ts"
      provides: "All new tables registered, old tables removed"
      contains: "TournamentEnrolled"
  key_links:
    - from: "spacetimedb/src/tables/matchResult.ts"
      to: "spacetimedb/src/types/enums.ts"
      via: "MatchEndReason import"
      pattern: "import.*MatchEndReason"
    - from: "spacetimedb/src/schema.ts"
      to: "spacetimedb/src/tables/tournamentEnrolled.ts"
      via: "schema registration"
      pattern: "TournamentEnrolled"
    - from: "spacetimedb/src/tables/lobby.ts"
      to: "spacetimedb/src/tables/bracketMatch.ts"
      via: "bracket_match_id index enables reverse lookup from BracketMatch to Lobby"
      pattern: "bracket_match_id"
---

<objective>
All schema changes for Phase 10.1: enum modifications (MatchEndReason replaces MatchOutcome, LobbyStage +2, TournamentStage +1), table renames (GroupStanding -> GroupPhaseRecord), table splits (TournamentParticipant -> TournamentEnrolled + TournamentTeamMember), column additions (bestOf, refereeControlsShelving, gameNumber, series tracking), column replacements (winnerTeamSide, matchEndReason), column removals (tournamentId from MatchResultRecord, lobbyId from BracketMatch), and new indexes (bracket_match_id on MatchResultRecord AND Lobby).

Purpose: All 6 sub-scopes require schema changes that must publish together with --clear-database. This plan creates the foundation that all subsequent plans build on.
Output: All table/enum definition files updated, schema.ts registrations updated. Module does NOT publish yet (reducers/helpers still reference old names).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-CONTEXT.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enum changes + table renames + new tables</name>
  <files>
    spacetimedb/src/types/enums.ts,
    spacetimedb/src/tables/groupPhaseRecord.ts,
    spacetimedb/src/tables/tournamentEnrolled.ts,
    spacetimedb/src/tables/tournamentTeamMember.ts
  </files>
  <read_first>
    spacetimedb/src/types/enums.ts,
    spacetimedb/src/tables/groupStanding.ts,
    spacetimedb/src/tables/tournamentParticipant.ts,
    spacetimedb/src/tables/tournamentTeam.ts
  </read_first>
  <action>
**Enum changes in enums.ts (per D-31, D-32, D-03, D-35):**
1. Rename `MatchOutcome` to `MatchEndReason`. Change variants from `{ BlueWins, RedWins, Draw, Concede }` to `{ Completed: t.unit(), Draw: t.unit(), Concede: t.unit() }`. Keep the export name as `MatchEndReason`.
2. Add `BetweenGames: t.unit()` and `Shelved: t.unit()` to `LobbyStage` enum, placed between `Scoring` and `AwaitingResult`.
3. Add `CheckIn: t.unit()` to `TournamentStage` enum, placed between `Registration` and `Seeding`.

**Rename GroupStanding -> GroupPhaseRecord (per D-01):**
1. Create `spacetimedb/src/tables/groupPhaseRecord.ts` by copying `groupStanding.ts` content.
2. In the new file: rename export `GroupStanding` -> `GroupPhaseRecord`, rename `groupStandingColumns` -> `groupPhaseRecordColumns`, change table name string from `'group_standing'` to `'group_phase_record'`. Keep all indexes (`tournament_id`, `by_tournament_group_and_team`) unchanged.
3. Delete `spacetimedb/src/tables/groupStanding.ts`.

**Create TournamentEnrolled (per D-20):**
Create `spacetimedb/src/tables/tournamentEnrolled.ts` with:
- PK: `['tournamentId', 'userId']`
- Columns: `tournamentId: t.u32()`, `userId: t.u32()`, `status: ParticipantStatus`, `anonymousAlias: t.string().optional()`, `isWaitlisted: t.bool()`, `allowRandomTeamAssignment: t.bool()`, `approvedByToAt: t.timestamp().optional()`, `hsrAccountId: t.u32().optional()`, plus standard audit columns (`createdAt`, `createdBy`, `updatedAt`, `updatedBy`).
- Indexes: `tournament_id` (btree on tournamentId), `user_id` (btree on userId), `by_tournament_and_user` (btree on [tournamentId, userId]).
- Import `ParticipantStatus` from enums. Follow exact same patterns as existing `tournamentParticipant.ts` for audit columns and index setup.

**Create TournamentTeamMember (per D-20):**
Create `spacetimedb/src/tables/tournamentTeamMember.ts` with:
- PK: `['teamId', 'userId']`
- Columns: `teamId: t.u32()`, `userId: t.u32()`, `tournamentId: t.u32()`, plus standard audit columns.
- Indexes: `team_id` (btree on teamId), `by_tournament_and_user` (btree on [tournamentId, userId]).
- Import audit column types from the same pattern used by other tables.

**Delete old file:**
Delete `spacetimedb/src/tables/tournamentParticipant.ts`.
  </action>
  <verify>
    <automated>grep -c "MatchEndReason" spacetimedb/src/types/enums.ts && grep -c "BetweenGames" spacetimedb/src/types/enums.ts && grep -c "CheckIn" spacetimedb/src/types/enums.ts && test -f spacetimedb/src/tables/groupPhaseRecord.ts && test -f spacetimedb/src/tables/tournamentEnrolled.ts && test -f spacetimedb/src/tables/tournamentTeamMember.ts && test ! -f spacetimedb/src/tables/groupStanding.ts && test ! -f spacetimedb/src/tables/tournamentParticipant.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - enums.ts contains `MatchEndReason` with exactly 3 variants: Completed, Draw, Concede
    - enums.ts does NOT contain `MatchOutcome` or `BlueWins` or `RedWins`
    - enums.ts LobbyStage contains `BetweenGames` and `Shelved` variants
    - enums.ts TournamentStage contains `CheckIn` between Registration and Seeding
    - groupPhaseRecord.ts exports `GroupPhaseRecord` with table name `'group_phase_record'`
    - groupStanding.ts is deleted
    - tournamentEnrolled.ts exports `TournamentEnrolled` with PK `['tournamentId', 'userId']`
    - tournamentEnrolled.ts has `by_tournament_and_user` btree index
    - tournamentTeamMember.ts exports `TournamentTeamMember` with PK `['teamId', 'userId']`
    - tournamentTeamMember.ts has `by_tournament_and_user` btree index on [tournamentId, userId]
    - tournamentTeamMember.ts has `team_id` btree index
    - tournamentParticipant.ts is deleted
  </acceptance_criteria>
  <done>All enum changes applied, GroupPhaseRecord renamed, TournamentEnrolled + TournamentTeamMember created, old files deleted</done>
</task>

<task type="auto">
  <name>Task 2: Column changes on existing tables + schema.ts registration</name>
  <files>
    spacetimedb/src/tables/lobby.ts,
    spacetimedb/src/tables/matchSession.ts,
    spacetimedb/src/tables/matchSessionStep.ts,
    spacetimedb/src/tables/matchSessionStepHistory.ts,
    spacetimedb/src/tables/matchResult.ts,
    spacetimedb/src/tables/matchSessionHistory.ts,
    spacetimedb/src/tables/bracketMatch.ts,
    spacetimedb/src/schema.ts
  </files>
  <read_first>
    spacetimedb/src/tables/lobby.ts,
    spacetimedb/src/tables/matchSession.ts,
    spacetimedb/src/tables/matchSessionStep.ts,
    spacetimedb/src/tables/matchSessionStepHistory.ts,
    spacetimedb/src/tables/matchResult.ts,
    spacetimedb/src/tables/matchSessionHistory.ts,
    spacetimedb/src/tables/bracketMatch.ts,
    spacetimedb/src/schema.ts
  </read_first>
  <action>
**Lobby table (per D-06, D-11, D-18):**
Add `bestOf: t.u8()` column. No default in table def (SpacetimeDB doesn't support column defaults; reducer sets default=1).
Add `refereeControlsShelving: t.bool()` column. Default true set in reducer, not in table def.
Add `bracket_match_id` btree index on `bracketMatchId` column. This enables efficient reverse lookup from BracketMatch to Lobby (used by cancel_tournament cascade to find shelved/BetweenGames lobbies linked to bracket matches — per D-18). Symmetric with the bracket_match_id index being added to MatchResultRecord.

**MatchSession table (per D-12):**
Add 4 columns: `currentGameNumber: t.u8()`, `gamesWonBlue: t.u8()`, `gamesWonRed: t.u8()`, `seriesBestOf: t.u8()`.

**MatchSessionStep table (per D-13):**
Add `gameNumber: t.u8()` column.

**MatchSessionStepHistory table (per D-13, Claude's discretion on PK):**
Add `gameNumber: t.u8()` column. Change PK from `['matchHistoryId', 'sequence']` to `['matchHistoryId', 'gameNumber', 'sequence']`. This prevents PK conflicts when game 1 step 1 and game 2 step 1 share the same matchHistoryId.

**MatchResultRecord table (per D-30, D-31, D-32, D-33, D-42):**
1. Replace `winnerUserId: t.u32().optional()` with `winnerTeamSide: TeamSide.optional()`. Add `import { TeamSide } from '../types/enums';` (if not already imported).
2. Replace `matchOutcome: MatchOutcome.optional()` with `matchEndReason: MatchEndReason.optional()`. Change import from `MatchOutcome` to `MatchEndReason`.
3. Remove `tournamentId: t.u32().optional()` column.
4. Remove `tournament_id` btree index from the indexes section.
5. Add `bracket_match_id` btree index on `bracketMatchId` column (for the new query path per RESEARCH recommendation).

**MatchSessionHistory table:**
Replace `outcome: MatchOutcome` with `outcome: MatchEndReason`. Update import from `MatchOutcome` to `MatchEndReason`.

**BracketMatch table (per D-43):**
1. Remove `lobbyId: t.u32().optional()` column.
2. Remove `lobby_id` btree index.

**schema.ts registration:**
1. Remove import of `GroupStanding` from `./tables/groupStanding`. Add import of `GroupPhaseRecord` from `./tables/groupPhaseRecord`.
2. Remove import of `TournamentParticipant` from `./tables/tournamentParticipant`. Add imports of `TournamentEnrolled` from `./tables/tournamentEnrolled` and `TournamentTeamMember` from `./tables/tournamentTeamMember`.
3. In the `schema({})` call: replace `GroupStanding` with `GroupPhaseRecord`, replace `TournamentParticipant` with `TournamentEnrolled` and `TournamentTeamMember`.
4. Update section comments: change "Bracket & Group" section to include `GroupPhaseRecord`. Add `TournamentTeamMember` next to `TournamentTeam` in "Tournament Teams" section. Move `TournamentEnrolled` to "Tournament System" section.
  </action>
  <verify>
    <automated>grep -c "winnerTeamSide" spacetimedb/src/tables/matchResult.ts && grep -c "matchEndReason" spacetimedb/src/tables/matchResult.ts && grep -c "bracket_match_id" spacetimedb/src/tables/matchResult.ts && grep -c "bestOf" spacetimedb/src/tables/lobby.ts && grep -c "refereeControlsShelving" spacetimedb/src/tables/lobby.ts && grep -c "bracket_match_id" spacetimedb/src/tables/lobby.ts && grep -c "currentGameNumber" spacetimedb/src/tables/matchSession.ts && grep -c "gameNumber" spacetimedb/src/tables/matchSessionStep.ts && grep -c "GroupPhaseRecord" spacetimedb/src/schema.ts && grep -c "TournamentEnrolled" spacetimedb/src/schema.ts && grep -c "TournamentTeamMember" spacetimedb/src/schema.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - matchResult.ts contains `winnerTeamSide` with type `TeamSide.optional()` (NOT `t.u32().optional()`)
    - matchResult.ts contains `matchEndReason` with type `MatchEndReason.optional()` (NOT `MatchOutcome`)
    - matchResult.ts does NOT contain `winnerUserId` or `matchOutcome` or `tournament_id` index or `tournamentId` column
    - matchResult.ts contains `bracket_match_id` btree index on bracketMatchId
    - matchSessionHistory.ts contains `outcome: MatchEndReason` (NOT MatchOutcome)
    - bracketMatch.ts does NOT contain `lobbyId` column or `lobby_id` index
    - lobby.ts contains `bestOf: t.u8()` and `refereeControlsShelving: t.bool()`
    - lobby.ts contains `bracket_match_id` btree index on bracketMatchId
    - matchSession.ts contains `currentGameNumber: t.u8()`, `gamesWonBlue: t.u8()`, `gamesWonRed: t.u8()`, `seriesBestOf: t.u8()`
    - matchSessionStep.ts contains `gameNumber: t.u8()`
    - matchSessionStepHistory.ts PK array is `['matchHistoryId', 'gameNumber', 'sequence']`
    - schema.ts imports and registers GroupPhaseRecord, TournamentEnrolled, TournamentTeamMember
    - schema.ts does NOT import or register GroupStanding or TournamentParticipant
  </acceptance_criteria>
  <done>All table columns updated, schema.ts registrations complete, all old references removed from table definitions</done>
</task>

</tasks>

<verification>
- All enum variants match D-32 (MatchEndReason: Completed/Draw/Concede), D-03 (LobbyStage +BetweenGames/Shelved), D-35 (TournamentStage +CheckIn)
- GroupPhaseRecord renamed from GroupStanding per D-01
- TournamentEnrolled has same columns as TournamentParticipant minus teamGroupId per D-20
- TournamentTeamMember has teamId, userId, tournamentId per D-20
- MatchResultRecord has winnerTeamSide (TeamSide?) per D-30, matchEndReason per D-31, no tournamentId per D-42
- BracketMatch has no lobbyId per D-43
- Lobby has refereeControlsShelving per D-06
- Lobby has bracket_match_id btree index for cancel_tournament reverse lookup per D-18
- Note: Module will NOT compile at this point -- reducers/helpers still reference old names. This is expected; Plans 02-04 fix those references.
</verification>

<success_criteria>
All 13 table/enum/schema files are modified correctly. Old files (groupStanding.ts, tournamentParticipant.ts) are deleted. New files (groupPhaseRecord.ts, tournamentEnrolled.ts, tournamentTeamMember.ts) exist with correct exports and indexes. Lobby table has bracket_match_id btree index.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-01-SUMMARY.md`
</output>
