---
phase: 10.1-best-of-n-team-split-winner-rework
plan: 06
type: execute
wave: 4
depends_on: ["10.1-05"]
files_modified:
  - test/backend/tournaments/tournament-registration.test.ts
  - test/backend/tournaments/tournament-teams.test.ts
  - test/backend/tournaments/tournament-admin.test.ts
  - test/backend/tournaments/tournament-cancel-cleanup.test.ts
  - test/backend/tournaments/tournament-stages.test.ts
  - test/backend/tournaments/tournament-stage-validation.unit.test.ts
  - test/backend/tournaments/tournament-mmr.test.ts
  - test/backend/brackets/bracket-advancement.test.ts
  - test/backend/brackets/group-to-elimination.test.ts
  - test/backend/match-results/match-lifecycle.test.ts
  - test/backend/match-results/mmr-stats.test.ts
  - test/backend/lobby/disconnect-concede.test.ts
  - test/backend/lobby/disconnect-admin-tools.test.ts
  - test/backend/lobby/disconnect-gc.test.ts
  - test/backend/season/tournament-player-account.test.ts
  - docs/tournament/architecture.md
  - docs/match-results/architecture.md
  - docs/match-session/architecture.md
  - docs/views/architecture.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-11
  - D-12
  - D-13
  - D-20
  - D-30
  - D-31
  - D-32
  - D-33
  - D-35
  - D-36
  - D-37
  - D-38
  - D-39
  - D-40
  - D-41
  - D-42
  - D-43
  - D-44

must_haves:
  truths:
    - "All test files compile and reference new table/field names"
    - "Module publishes to maincloud with --clear-database without errors"
    - "Client bindings generated successfully (spacetime generate)"
    - "Full test suite passes (npx vitest run)"
    - "Architecture docs reflect all Phase 10.1 changes"
  artifacts:
    - path: "test/backend/tournaments/tournament-teams.test.ts"
      provides: "Tournament team tests using TournamentTeamMember"
      contains: "TournamentTeamMember"
    - path: "docs/tournament/architecture.md"
      provides: "Updated tournament architecture with TournamentEnrolled + TournamentTeamMember"
      contains: "TournamentEnrolled"
    - path: "docs/match-results/architecture.md"
      provides: "Updated match results with winnerTeamSide + matchEndReason"
      contains: "winnerTeamSide"
  key_links:
    - from: "test/backend/tournaments/tournament-teams.test.ts"
      to: "spacetimedb/src/reducers/tournamentTeams.ts"
      via: "tests exercise TournamentTeamMember-based reducers"
      pattern: "TournamentTeamMember"
---

<objective>
Update all test files (15 files, 122+ occurrences), architecture docs (4 files), publish to maincloud with --clear-database, generate client bindings, and verify full test suite passes. Also update the behavior spec contract.md for tournament features.

Purpose: Complete the phase by making all tests pass against the new schema and documenting the changes.
Output: Green test suite, published module, generated bindings, updated docs.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-CONTEXT.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-RESEARCH.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-01-SUMMARY.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all test files for new schema</name>
  <files>
    test/backend/tournaments/tournament-registration.test.ts,
    test/backend/tournaments/tournament-teams.test.ts,
    test/backend/tournaments/tournament-admin.test.ts,
    test/backend/tournaments/tournament-cancel-cleanup.test.ts,
    test/backend/tournaments/tournament-stages.test.ts,
    test/backend/tournaments/tournament-stage-validation.unit.test.ts,
    test/backend/tournaments/tournament-mmr.test.ts,
    test/backend/brackets/bracket-advancement.test.ts,
    test/backend/brackets/group-to-elimination.test.ts,
    test/backend/match-results/match-lifecycle.test.ts,
    test/backend/match-results/mmr-stats.test.ts,
    test/backend/lobby/disconnect-concede.test.ts,
    test/backend/lobby/disconnect-admin-tools.test.ts,
    test/backend/lobby/disconnect-gc.test.ts,
    test/backend/season/tournament-player-account.test.ts
  </files>
  <read_first>
    test/backend/tournaments/tournament-registration.test.ts,
    test/backend/tournaments/tournament-teams.test.ts,
    test/backend/tournaments/tournament-admin.test.ts,
    test/backend/brackets/bracket-advancement.test.ts,
    test/backend/brackets/group-to-elimination.test.ts,
    test/backend/match-results/match-lifecycle.test.ts,
    test/backend/lobby/disconnect-concede.test.ts
  </read_first>
  <action>
For EACH test file, apply the following systematic replacements. Read each file first to understand its test patterns, then apply changes:

**Global replacements across all test files:**

1. **TournamentParticipant -> TournamentEnrolled:**
   - All `TournamentParticipant` table references -> `TournamentEnrolled`
   - All `ctx.db.TournamentParticipant.*` -> `ctx.db.TournamentEnrolled.*`
   - All `TournamentParticipant.by_tournament_and_user` -> `TournamentEnrolled.by_tournament_and_user`
   - Test data creation: remove `teamGroupId` from participant/enrolled inserts
   - Add `TournamentTeamMember` inserts where tests previously relied on `teamGroupId` being set

2. **teamGroupId -> TournamentTeamMember:**
   - Where tests set `teamGroupId` on a participant, instead insert a `TournamentTeamMember` row: `{ teamId, userId, tournamentId, ...auditInsert }`
   - Where tests read `participant.teamGroupId`, instead query `TournamentTeamMember.by_tournament_and_user.filter([tournamentId, userId])` and read `.teamId`
   - Remove all `teamGroupId !== undefined && teamGroupId !== 0` guard patterns

3. **GroupStanding -> GroupPhaseRecord:**
   - All `GroupStanding` references -> `GroupPhaseRecord`
   - All `ctx.db.GroupStanding.*` -> `ctx.db.GroupPhaseRecord.*`

4. **winnerUserId -> winnerTeamSide:**
   - In match result test data: replace `winnerUserId: someUserId` with `winnerTeamSide: { tag: 'Blue', value: {} }` (or Red)
   - In assertions: replace `expect(result.winnerUserId).toBe(userId)` with `expect(result.winnerTeamSide.tag).toBe('Blue')` (or 'Red')
   - For draws: `winnerTeamSide: undefined`

5. **matchOutcome -> matchEndReason:**
   - All `matchOutcome` field refs -> `matchEndReason`
   - All `MatchOutcome` type refs -> `MatchEndReason`
   - Replace `BlueWins`/`RedWins` with `Completed`; `Draw` stays `Draw`; `Concede` stays `Concede`

6. **tournamentId on MatchResultRecord:**
   - Remove `tournamentId` from all MatchResultRecord test inserts/expectations
   - Where tests assert `matchResult.tournamentId`, derive from bracketMatch instead

7. **lobbyId on BracketMatch:**
   - Remove `lobbyId` from all BracketMatch test inserts
   - Where tests set lobbyId on BracketMatch, remove that field

8. **MatchSession new columns:**
   - Add `currentGameNumber: 1, gamesWonBlue: 0, gamesWonRed: 0, seriesBestOf: 1` to MatchSession test inserts
   - Add `gameNumber: 1` to MatchSessionStep test inserts

**Per-file specific changes (high-count files):**

- **tournament-teams.test.ts (38 occurrences):** This file is the heaviest. Every test that checks team membership via `teamGroupId` on TournamentParticipant must instead verify TournamentTeamMember existence. Create/join/leave team operations now insert/delete TournamentTeamMember rows.

- **tournament-registration.test.ts (25 occurrences):** Registration now inserts TournamentEnrolled. Solo auto-team creates TournamentTeamMember. Withdrawal deletes TournamentTeamMember.

- **bracket-advancement.test.ts (12 occurrences):** Winner determination uses winnerTeamSide, not winnerUserId->participant lookup.

- **match-lifecycle.test.ts (9 occurrences):** MatchResultRecord test data needs winnerTeamSide, matchEndReason, no tournamentId.

After all replacements, run `npx vitest run --reporter=verbose` and fix any remaining compilation errors.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `TournamentParticipant`, `teamGroupId`, `GroupStanding`, `winnerUserId`, `matchOutcome`, `MatchOutcome`, `BlueWins`, `RedWins` across all test files
    - All test files contain the new names: `TournamentEnrolled`, `TournamentTeamMember`, `GroupPhaseRecord`, `winnerTeamSide`, `matchEndReason`, `MatchEndReason`
    - MatchResultRecord test data includes no `tournamentId` field
    - BracketMatch test data includes no `lobbyId` field
    - MatchSession test data includes `currentGameNumber`, `gamesWonBlue`, `gamesWonRed`, `seriesBestOf`
    - `npx vitest run` passes all tests
  </acceptance_criteria>
  <done>All 15 test files updated, full test suite green</done>
</task>

<task type="auto">
  <name>Task 2: Publish + bindings + docs update</name>
  <files>
    docs/tournament/architecture.md,
    docs/match-results/architecture.md,
    docs/match-session/architecture.md,
    docs/views/architecture.md
  </files>
  <read_first>
    docs/tournament/architecture.md,
    docs/match-results/architecture.md,
    docs/match-session/architecture.md,
    docs/views/architecture.md
  </read_first>
  <action>
**Step 1: Publish module to maincloud with --clear-database:**
```bash
cd spacetimedb && spacetime publish hsrpvp-spacetimedb-nextjs-test1 --clear-database
```
Fix any compilation errors that appear. Common issues:
- Missing imports (old table names still referenced somewhere)
- Type mismatches (MatchOutcome vs MatchEndReason)
- Missing fields in table inserts (new required columns)

**Step 2: Generate client bindings:**
```bash
spacetime generate --lang typescript --out-dir ../src/module_bindings --project-path .
```

**Step 3: Run full test suite:**
```bash
npx vitest run --reporter=verbose
```
All tests must pass. If any fail, diagnose and fix.

**Step 4: Update architecture docs:**

**docs/tournament/architecture.md:**
- Replace TournamentParticipant table docs with TournamentEnrolled + TournamentTeamMember
- Document captain-transfer on DQ/withdrawal (D-21, D-22)
- Document team destruction when last member leaves (D-24)
- Add TournamentStage.CheckIn to stage diagram
- Document check-in flow (D-35, D-36, D-37)
- Document Active status on InProgress (D-39)
- Document Eliminated status on final bracket loss (D-40)
- Remove teamGroupId from all column listings
- Update DQ flow to include captain-transfer and lobby-aware handling (D-25, D-26)
- Update cancel_tournament cascade to mention shelved lobby cleanup (D-18)

**docs/match-results/architecture.md:**
- Replace winnerUserId with winnerTeamSide (TeamSide?) documentation
- Replace MatchOutcome with MatchEndReason (Completed/Draw/Concede)
- Document the separation: winnerTeamSide = WHO won, matchEndReason = HOW it ended
- Remove tournamentId from MatchResultRecord column docs
- Add bracket_match_id btree index documentation
- Document derivation path: tournamentId from bracketMatch.tournamentId
- Update concede matrix section to use MatchEndReason

**docs/match-session/architecture.md:**
- Add best-of-N series documentation: currentGameNumber, gamesWonBlue, gamesWonRed, seriesBestOf
- Add gameNumber on MatchSessionStep and MatchSessionStepHistory
- Document MatchSessionStepHistory PK change to [matchHistoryId, gameNumber, sequence]
- Add BetweenGames and Shelved to stage diagram
- Document series flow: Scoring -> BetweenGames -> Drafting -> ... -> AwaitingResult
- Document shelve flow: BetweenGames -> Shelved -> BetweenGames
- Document advance_to_next_game, shelve_series, resume_series reducers
- Document step archival between games

**docs/views/architecture.md:**
- Replace any GroupStanding references with GroupPhaseRecord
- Replace any TournamentParticipant references with TournamentEnrolled
- Update view queries that reference old table names
  </action>
  <verify>
    <automated>grep -q "TournamentEnrolled" docs/tournament/architecture.md && grep -q "TournamentTeamMember" docs/tournament/architecture.md && grep -q "winnerTeamSide" docs/match-results/architecture.md && grep -q "MatchEndReason" docs/match-results/architecture.md && grep -q "BetweenGames" docs/match-session/architecture.md && grep -q "seriesBestOf" docs/match-session/architecture.md && ! grep -q "TournamentParticipant" docs/tournament/architecture.md && ! grep -q "winnerUserId" docs/match-results/architecture.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Module publishes to maincloud without errors
    - Client bindings generated successfully (new files in src/module_bindings/)
    - Full test suite passes (`npx vitest run` shows all green)
    - docs/tournament/architecture.md: documents TournamentEnrolled, TournamentTeamMember, captain-transfer, CheckIn stage, no TournamentParticipant references
    - docs/match-results/architecture.md: documents winnerTeamSide, matchEndReason, bracket_match_id index, no winnerUserId references
    - docs/match-session/architecture.md: documents best-of-N columns, BetweenGames/Shelved stages, series management reducers
    - docs/views/architecture.md: updated with GroupPhaseRecord and TournamentEnrolled
  </acceptance_criteria>
  <done>Module published, bindings generated, tests green, all architecture docs updated for Phase 10.1</done>
</task>

</tasks>

<verification>
- `spacetime publish` completes without error
- `npx vitest run --reporter=verbose` shows all tests passing
- `grep -rn "TournamentParticipant\|teamGroupId\|GroupStanding\|winnerUserId\|matchOutcome\|MatchOutcome\|BlueWins\|RedWins" test/` returns zero results
- Architecture docs contain new names and don't contain old names
</verification>

<success_criteria>
Phase 10.1 is complete: module published with --clear-database, all tests pass, client bindings generated, architecture docs fully updated. All 44 decisions (D-01 through D-44) are implemented and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-06-SUMMARY.md`
</output>
