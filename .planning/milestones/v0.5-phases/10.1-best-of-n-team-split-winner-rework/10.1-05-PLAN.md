---
phase: 10.1-best-of-n-team-split-winner-rework
plan: 05
type: execute
wave: 3
depends_on: ["10.1-02", "10.1-03", "10.1-04"]
files_modified:
  - spacetimedb/src/reducers/seriesManagement.ts
  - spacetimedb/src/reducers/tournamentCheckIn.ts
  - spacetimedb/src/reducers/lobbyGc.ts
  - spacetimedb/src/reducers/lobbyLifecycle.ts
  - spacetimedb/src/reducers/postDraft.ts
  - spacetimedb/src/reducers/lobbySettings.ts
  - spacetimedb/src/reducers/concede.ts
  - spacetimedb/src/reducers/tournamentManagement.ts
  - spacetimedb/src/helpers/tournamentHelpers.ts
  - spacetimedb/src/helpers/finalizationHelpers.ts
  - spacetimedb/src/reducers/tournamentLobby.ts
  - spacetimedb/src/index.ts
autonomous: true
requirements:
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-35
  - D-36
  - D-37
  - D-39
  - D-41

must_haves:
  truths:
    - "advance_to_next_game resets MatchSession in-place (turnIndex, sequence, budgets, pauses), deletes game N steps (finalization archives all steps at end of series), increments currentGameNumber, transitions BetweenGames -> Drafting"
    - "shelve_series transitions BetweenGames -> Shelved; resume_series transitions Shelved -> BetweenGames with unshelve gate (1+ player per team online)"
    - "GC skips Shelved lobbies; casual Shelved lobbies have 72h TTL; BetweenGames treated like active stages"
    - "Empty Shelved lobby does NOT auto-close on last member leave"
    - "Concede works in BetweenGames stage (surrenders entire series)"
    - "postDraft advance_stage: after Scoring, checks if series won (gamesWon >= ceil(seriesBestOf/2)); if won -> AwaitingResult, if not -> BetweenGames"
    - "check_in_tournament sets participant status to CheckedIn"
    - "advance_tournament_stage handles CheckIn stage: auto-removes unchecked-in, bulk sets Active on InProgress"
    - "cancel_tournament cascade hard-deletes shelved lobbies linked to bracket matches (uses Lobby.bracket_match_id index)"
    - "create_lobby supports bestOf param; create_tournament_lobby copies bestOf from BracketMatch"
    - "index.ts exports advance_to_next_game, shelve_series, resume_series from seriesManagement and check_in_tournament from tournamentCheckIn"
  artifacts:
    - path: "spacetimedb/src/reducers/seriesManagement.ts"
      provides: "advance_to_next_game, shelve_series, resume_series reducers"
      exports: ["advance_to_next_game", "shelve_series", "resume_series"]
    - path: "spacetimedb/src/reducers/tournamentCheckIn.ts"
      provides: "check_in_tournament reducer"
      exports: ["check_in_tournament"]
    - path: "spacetimedb/src/index.ts"
      provides: "Re-exports all new reducers so clients can call them"
      contains: "seriesManagement"
  key_links:
    - from: "spacetimedb/src/reducers/seriesManagement.ts"
      to: "spacetimedb/src/tables/matchSession.ts"
      via: "resets MatchSession between games"
      pattern: "currentGameNumber.*\\+.*1"
    - from: "spacetimedb/src/reducers/postDraft.ts"
      to: "spacetimedb/src/types/enums.ts"
      via: "BetweenGames stage transition"
      pattern: "BetweenGames"
    - from: "spacetimedb/src/reducers/tournamentManagement.ts"
      to: "spacetimedb/src/tables/tournamentEnrolled.ts"
      via: "bulk Active status on InProgress"
      pattern: "Active.*InProgress"
    - from: "spacetimedb/src/index.ts"
      to: "spacetimedb/src/reducers/seriesManagement.ts"
      via: "export re-export"
      pattern: "export.*seriesManagement"
    - from: "spacetimedb/src/index.ts"
      to: "spacetimedb/src/reducers/tournamentCheckIn.ts"
      via: "export re-export"
      pattern: "export.*tournamentCheckIn"
    - from: "spacetimedb/src/helpers/tournamentHelpers.ts"
      to: "spacetimedb/src/tables/lobby.ts"
      via: "bracket_match_id index for efficient reverse lookup"
      pattern: "bracket_match_id\\.filter"
---

<objective>
Implement all new functionality: best-of-N series management (advance_to_next_game, shelve_series, resume_series), tournament check-in (check_in_tournament), GC/lifecycle extensions for BetweenGames and Shelved stages, concede in BetweenGames, post-draft series-aware stage transitions, bestOf wiring, cancel_tournament shelved lobby cascade, and ParticipantStatus lifecycle (Active on InProgress, CheckIn stage transitions). Also exports all new reducers from index.ts so clients can reach them.

Purpose: This is where the new features come alive. All schema and mechanical rewiring from Plans 01-04 is complete; this plan adds the behavioral logic.
Output: 5 new reducers, 11 modified files with BetweenGames/Shelved/CheckIn awareness, index.ts updated with new exports.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-CONTEXT.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-RESEARCH.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-01-SUMMARY.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-02-SUMMARY.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-03-SUMMARY.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-04-SUMMARY.md

<interfaces>
<!-- Schemas from Plan 01 -->
LobbyStage: { Waiting, Drafting, Equipping, Scoring, BetweenGames, Shelved, AwaitingResult, Finished }
TournamentStage: { Draft, Registration, CheckIn, Seeding, InProgress, Completed, Cancelled }
Lobby: has bestOf (u8) column, bracket_match_id btree index on bracketMatchId
MatchSession: has currentGameNumber, gamesWonBlue, gamesWonRed, seriesBestOf (all u8)
MatchSessionStep: has gameNumber (u8)
MatchSessionStepHistory: PK [matchHistoryId, gameNumber, sequence]
TournamentEnrolled: PK [tournamentId, userId], status: ParticipantStatus

<!-- From Plan 04 -->
draftClassic.ts: already initializes currentGameNumber=1, gamesWonBlue=0, gamesWonRed=0, seriesBestOf, gameNumber on steps

<!-- Patterns -->
D-07 authority: host, TO, TO assistant, admin, mod. Referee if refereeControlsShelving=true.
Captain transfer: transferTournamentCaptain in tournamentHelpers.ts (from Plan 03)
Flag transfer: flagTransferHelpers.ts transferCaptain for lobby
Stage checks: concede_match currently uses ['Drafting', 'Equipping', 'Scoring'] as activeStages
GC: lobbyGc.ts handles stage-based cleanup with hardDeleteLobby

<!-- Step archival context -->
MatchSessionHistory is created ONLY during finalization (Step 7 of pipeline). It has an autoInc `id` PK, no lobbyId column. MatchSessionStepHistory rows reference MatchSessionHistory.id as matchHistoryId. During finalization, Step 8 archives all remaining MatchSessionStep rows to MatchSessionStepHistory and Step 18 deletes them. There is NO MatchSessionHistory row during BetweenGames — it only exists after finalization.

<!-- Index context -->
Lobby table now has bracket_match_id btree index (added in Plan 01). Use Lobby.bracket_match_id.filter(bracketMatchId) for efficient reverse lookup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Series management reducers + postDraft series logic + bestOf wiring + index.ts exports</name>
  <files>
    spacetimedb/src/reducers/seriesManagement.ts,
    spacetimedb/src/reducers/postDraft.ts,
    spacetimedb/src/reducers/lobbySettings.ts,
    spacetimedb/src/reducers/tournamentLobby.ts,
    spacetimedb/src/helpers/finalizationHelpers.ts,
    spacetimedb/src/index.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/postDraft.ts,
    spacetimedb/src/reducers/lobbySettings.ts,
    spacetimedb/src/reducers/tournamentLobby.ts,
    spacetimedb/src/helpers/finalizationHelpers.ts,
    spacetimedb/src/helpers/lobbyHelpers.ts,
    spacetimedb/src/helpers/ensurePermissions.ts,
    spacetimedb/src/tables/matchSession.ts,
    spacetimedb/src/tables/matchSessionStep.ts,
    spacetimedb/src/tables/matchSessionStepHistory.ts,
    spacetimedb/src/index.ts
  </read_first>
  <action>
**NEW FILE: reducers/seriesManagement.ts — per D-02, D-04, D-07, D-08, D-14, D-15:**

Create `spacetimedb/src/reducers/seriesManagement.ts` with 3 reducers:

1. `advance_to_next_game(ctx, lobbyId: u32)`:
   - **Permission:** D-07 authority — caller must be lobby host, TO, TO assistant, admin, or mod. If `lobby.refereeControlsShelving === true`, also allow referee (check LobbyMember.isReferee).
   - **Pre-conditions:** Lobby stage = `BetweenGames`. Series not won (`gamesWonBlue < ceil(seriesBestOf/2)` AND `gamesWonRed < ceil(seriesBestOf/2)`).
   - **Step 1: Delete current game's MatchSessionStep rows.** Find all MatchSessionStep rows for this lobby via `MatchSessionStep.lobby_id.filter(lobbyId)`. Delete all of them. Do NOT attempt to archive them to MatchSessionStepHistory here — there is no MatchSessionHistory row yet (it is only created during finalization). Finalization at end-of-series will handle all step archival. The inter-game steps are disposable for now; the important data (game scores) is already captured in MatchResultGame rows and gamesWonBlue/gamesWonRed on MatchSession.
   - **Step 2: Reset MatchSession.** Update MatchSession (same lobbyId PK): `turnIndex: 0`, clear draftSequence (set empty or reset), reset budgets from Lobby.configSnapshot if applicable, reset pause-related fields (`isPaused: false`, `pausedAt: undefined`, `pauseRemainingMs` reset to initial), increment `currentGameNumber` by 1.
   - **Step 3: Transition lobby.** Update Lobby stage from `BetweenGames` to `Drafting`.
   - Import necessary tables and helpers. Register reducer with SpacetimeDB.

2. `shelve_series(ctx, lobbyId: u32)`:
   - **Permission:** Same D-07 authority as advance_to_next_game.
   - **Pre-conditions:** Lobby stage = `BetweenGames`.
   - **Action:** Update Lobby stage to `Shelved` (per D-15).
   - Simple stage transition, no other state changes.

3. `resume_series(ctx, lobbyId: u32)`:
   - **Permission:** Same D-07 authority.
   - **Pre-conditions:** Lobby stage = `Shelved`. Unshelve gate (D-08): at least 1 player (non-coach, non-spectator) per team must be present (have a LobbyMember row with `voluntarilyLeft === false` or `voluntarilyLeft === undefined`). 3rd party referee NOT required (D-09).
   - **Action:** Update Lobby stage from `Shelved` to `BetweenGames` (per D-15). From BetweenGames, caller can then advance_to_next_game or shelve again.

**postDraft.ts — per D-14 (series-aware stage transition):**
In the `advance_stage` reducer (or wherever stage transitions from Scoring occur):
- After Scoring completes, check if this is a best-of-N series:
  ```typescript
  if (matchSession.seriesBestOf > 1) {
      const winsNeeded = Math.ceil(matchSession.seriesBestOf / 2);
      if (matchSession.gamesWonBlue >= winsNeeded || matchSession.gamesWonRed >= winsNeeded) {
          // Series won -> AwaitingResult (same as current path)
          updateLobbyStage(ctx, lobby, 'AwaitingResult');
      } else {
          // Series not won -> BetweenGames
          updateLobbyStage(ctx, lobby, 'BetweenGames');
      }
  } else {
      // bestOf=1 -> AwaitingResult (current behavior, unchanged)
      updateLobbyStage(ctx, lobby, 'AwaitingResult');
  }
  ```
- Also: after a game's scoring completes and BEFORE the stage transition, update `gamesWonBlue` or `gamesWonRed` on MatchSession based on the game result. The winner of the current game is determined from the `MatchResultGame` rows for the current gameNumber. Update via `MatchSession.lobbyId.update(...)`.

**lobbySettings.ts — per D-11:**
In `create_lobby` (or `update_lobby_settings`): 
- Accept `bestOf` param (u8). If `bestOf === 0` (sentinel), default to 1. Store on Lobby row.
- Add `refereeControlsShelving` to Lobby if not already present. Default `true` per D-06. If this column doesn't exist in the table definition yet, note that it was added in Plan 01 — check the Lobby table definition. If it wasn't added (oversight), add it to the Lobby table as a `t.bool()` with default true in the reducer.

**tournamentLobby.ts — per D-11:**
In `create_tournament_lobby`: copy `bestOf` from `BracketMatch.bestOf` to `Lobby.bestOf`.

**finalizationHelpers.ts — Step 8 gameNumber partitioning:**
In the step archival (Step 8 of the pipeline), when archiving MatchSessionStep rows to MatchSessionStepHistory:
- Include the `gameNumber` field from each MatchSessionStep row in the history insert.
- The PK of MatchSessionStepHistory is now `[matchHistoryId, gameNumber, sequence]`, so gameNumber MUST be set. For best-of-1 matches, all steps have gameNumber=1. For best-of-N, by the time finalization runs only the LAST game's steps remain (advance_to_next_game deletes previous games' steps between games). Set gameNumber from the step's gameNumber field.

**index.ts — export new reducers:**
Add the following export lines to `spacetimedb/src/index.ts`:
```typescript
export { advance_to_next_game, shelve_series, resume_series } from './reducers/seriesManagement';
export { check_in_tournament } from './reducers/tournamentCheckIn';
```
Place the seriesManagement export near the other lobby-related exports (after the concede line). Place the tournamentCheckIn export near the other tournament exports (after tournamentRegistration or tournamentTeams).
  </action>
  <verify>
    <automated>test -f spacetimedb/src/reducers/seriesManagement.ts && grep -q "advance_to_next_game" spacetimedb/src/reducers/seriesManagement.ts && grep -q "shelve_series" spacetimedb/src/reducers/seriesManagement.ts && grep -q "resume_series" spacetimedb/src/reducers/seriesManagement.ts && grep -q "BetweenGames" spacetimedb/src/reducers/postDraft.ts && grep -q "bestOf" spacetimedb/src/reducers/lobbySettings.ts && grep -q "gameNumber" spacetimedb/src/helpers/finalizationHelpers.ts && grep -q "seriesManagement" spacetimedb/src/index.ts && grep -q "tournamentCheckIn" spacetimedb/src/index.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - seriesManagement.ts exists with 3 exported reducers: advance_to_next_game, shelve_series, resume_series
    - advance_to_next_game: deletes MatchSessionStep rows for the lobby (does NOT archive to MatchSessionStepHistory — finalization handles that), resets MatchSession, increments currentGameNumber, transitions to Drafting
    - advance_to_next_game: validates D-07 authority (host/TO/assistant/admin/mod/referee-if-enabled)
    - shelve_series: transitions BetweenGames -> Shelved
    - resume_series: validates unshelve gate (1+ player per team), transitions Shelved -> BetweenGames
    - postDraft.ts: after Scoring, checks series win condition before choosing AwaitingResult vs BetweenGames
    - postDraft.ts: updates gamesWonBlue/gamesWonRed on game completion
    - lobbySettings.ts: create_lobby accepts bestOf param (default 1 if 0 sentinel)
    - tournamentLobby.ts: copies BracketMatch.bestOf to Lobby.bestOf
    - finalizationHelpers.ts: step 8 includes gameNumber in history inserts
    - index.ts: exports advance_to_next_game, shelve_series, resume_series from seriesManagement
    - index.ts: exports check_in_tournament from tournamentCheckIn
  </acceptance_criteria>
  <done>Series management reducers created, post-draft series logic wired, bestOf param flows from lobby/tournament to match, all new reducers exported from index.ts</done>
</task>

<task type="auto">
  <name>Task 2: GC + lifecycle + concede extensions + check-in + stage transitions</name>
  <files>
    spacetimedb/src/reducers/lobbyGc.ts,
    spacetimedb/src/reducers/lobbyLifecycle.ts,
    spacetimedb/src/reducers/concede.ts,
    spacetimedb/src/reducers/tournamentCheckIn.ts,
    spacetimedb/src/reducers/tournamentManagement.ts,
    spacetimedb/src/helpers/tournamentHelpers.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/lobbyGc.ts,
    spacetimedb/src/reducers/lobbyLifecycle.ts,
    spacetimedb/src/reducers/concede.ts,
    spacetimedb/src/reducers/tournamentManagement.ts,
    spacetimedb/src/helpers/tournamentHelpers.ts,
    spacetimedb/src/tables/tournamentEnrolled.ts
  </read_first>
  <action>
**lobbyGc.ts — per D-16, D-17:**

1. **Shelved lobby handling (D-16):** In the GC scheduled reducer, add explicit handling for Shelved lobbies:
   - Skip tournament Shelved lobbies entirely (they persist until cancel_tournament cascade).
   - For casual Shelved lobbies: check if `shelvedAt` exceeds 72 hours (72 * 60 * 60 * 1000 ms). If so, call `hardDeleteLobby`. 
   - NOTE: if there's no `shelvedAt` column on Lobby, use `updatedAt` as proxy (it's set when stage changes to Shelved).
   - Add `Shelved` to the stages that are NOT cleaned up by the normal idle-30-min logic.

2. **BetweenGames handling (D-17):** Treat `BetweenGames` like active stages (Drafting/Equipping/Scoring). Add it to the active stages array in GC so it gets the same "only delete if ALL members offline + idle > 30 min" treatment.

**lobbyLifecycle.ts — per D-10, D-19:**

1. `leave_lobby`: Add `BetweenGames` to the active stages list (where active match handling occurs).
2. **Shelved exemption (D-19):** When all members have left and the lobby stage is `Shelved`, do NOT trigger `hardDeleteLobby`. Members are expected to return. Add explicit check: `if (lobby.stage.tag === 'Shelved') return;` (or skip the auto-close logic for Shelved).

**concede.ts — per D-10:**

1. `concede_match`: Add `'BetweenGames'` to the `activeStages` array (currently `['Drafting', 'Equipping', 'Scoring']`). This allows players to concede the entire series between games.
2. `claim_forfeit`: Add `'BetweenGames'` to allowed stages.
3. `defer_match`: Add `'BetweenGames'` to allowed stages.
4. These are ADDITIVE changes on top of Plan 04's matchEndReason migration (Plan 04 already replaced matchOutcome/winnerUserId).

**NEW FILE: reducers/tournamentCheckIn.ts — per D-35, D-36:**

Create `spacetimedb/src/reducers/tournamentCheckIn.ts` with:

`check_in_tournament(ctx, tournamentId: u32)`:
- Find the User via `ensureAuthenticated(ctx)`.
- Find TournamentEnrolled via `by_tournament_and_user.filter([tournamentId, ctx.sender])` -> verify exists.
- Validate tournament stage = `CheckIn` (error if not in CheckIn stage).
- Validate enrolled status = `Registered` (error if already CheckedIn or other status).
- Update TournamentEnrolled status to `CheckedIn` (delete + re-insert for composite PK).

**tournamentManagement.ts — per D-35, D-37, D-39:**

1. `advance_tournament_stage`: 
   - **Registration -> CheckIn (D-37):** If tournament.checkInEnabled is true, the next stage after Registration is CheckIn (not Seeding). Add this conditional: if `tournament.checkInEnabled && currentStage === 'Registration'`, advance to `CheckIn`.
   - If `!tournament.checkInEnabled`, skip CheckIn and go directly to Seeding (current behavior).
   
   - **CheckIn -> Seeding (D-37):** On this transition, auto-remove participants who did NOT check in. Iterate all TournamentEnrolled for this tournament. Delete (via cascade) any with `status.tag === 'Registered'` (not CheckedIn). For each removed: also delete their TournamentTeamMember row if exists, and handle captain-transfer if they were captain. Teams with no members left after removal should be deleted.
   
   - **Seeding -> InProgress (D-39):** On this transition, bulk-update all enrolled participants with status `Registered` or `CheckedIn` to `Active`. Iterate `TournamentEnrolled.tournament_id.filter(tournamentId)`, for each with status Registered or CheckedIn, delete + re-insert with status `Active`.

**tournamentHelpers.ts — per D-18:**

1. `cascadeCleanupTournament`: Extend to find and hard-delete shelved/between-games lobbies linked to bracket matches:
   - Find all BracketMatch rows for tournament: `BracketMatch.tournament_id.filter(tournamentId)`.
   - For each bracketMatch, find the linked Lobby via `Lobby.bracket_match_id.filter(bracketMatch.id)` (uses the btree index added in Plan 01 — efficient lookup, NOT a full table scan). If a Lobby exists with that bracketMatchId and its stage is `Shelved` or `BetweenGames`, call `hardDeleteLobby(ctx, lobby)`.
   - Do this BEFORE deleting bracket match rows (lobby references bracketMatchId).

2. Update `STAGE_ORDER` array: Add `'CheckIn'` between `'Registration'` and `'Seeding'`.

3. Update `validateStageTransition`: Allow `Registration -> CheckIn` and `CheckIn -> Seeding` transitions.
  </action>
  <verify>
    <automated>grep -q "Shelved" spacetimedb/src/reducers/lobbyGc.ts && grep -q "BetweenGames" spacetimedb/src/reducers/lobbyGc.ts && grep -q "BetweenGames" spacetimedb/src/reducers/lobbyLifecycle.ts && grep -q "Shelved" spacetimedb/src/reducers/lobbyLifecycle.ts && grep -q "BetweenGames" spacetimedb/src/reducers/concede.ts && test -f spacetimedb/src/reducers/tournamentCheckIn.ts && grep -q "check_in_tournament" spacetimedb/src/reducers/tournamentCheckIn.ts && grep -q "CheckIn" spacetimedb/src/reducers/tournamentManagement.ts && grep -q "Active" spacetimedb/src/reducers/tournamentManagement.ts && grep -q "CheckIn" spacetimedb/src/helpers/tournamentHelpers.ts && grep -q "bracket_match_id" spacetimedb/src/helpers/tournamentHelpers.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lobbyGc.ts: Shelved lobbies skipped (tournament) or 72h TTL (casual); BetweenGames treated as active
    - lobbyLifecycle.ts: BetweenGames in active stages for leave_lobby; Shelved exemption for empty auto-close
    - concede.ts: BetweenGames in activeStages for concede_match, claim_forfeit, defer_match
    - tournamentCheckIn.ts: exists with check_in_tournament reducer that validates CheckIn stage and Registered status
    - tournamentManagement.ts: Registration -> CheckIn transition (if checkInEnabled)
    - tournamentManagement.ts: CheckIn -> Seeding auto-removes non-checked-in, handles captain-transfer for removed members
    - tournamentManagement.ts: Seeding -> InProgress bulk sets Active status on all eligible enrolled
    - tournamentHelpers.ts: STAGE_ORDER includes CheckIn; validateStageTransition updated
    - tournamentHelpers.ts: cascadeCleanupTournament uses Lobby.bracket_match_id.filter() (NOT full table scan) to find and hard-delete shelved/BetweenGames lobbies linked to bracket matches
  </acceptance_criteria>
  <done>GC/lifecycle/concede extended for BetweenGames/Shelved, check-in reducer created, tournament stage transitions handle CheckIn and Active status, cascade cleanup uses indexed lookup</done>
</task>

</tasks>

<verification>
- `grep -c "BetweenGames\|Shelved" spacetimedb/src/reducers/lobbyGc.ts` returns 3+
- `grep -c "BetweenGames" spacetimedb/src/reducers/concede.ts` returns 3+ (one per concede/claim/defer)
- `grep -c "CheckIn" spacetimedb/src/helpers/tournamentHelpers.ts` returns 2+ (STAGE_ORDER + validateStageTransition)
- `test -f spacetimedb/src/reducers/seriesManagement.ts && test -f spacetimedb/src/reducers/tournamentCheckIn.ts` passes
- `grep -c "seriesManagement\|tournamentCheckIn" spacetimedb/src/index.ts` returns 2 (both new modules exported)
- `grep -c "bracket_match_id" spacetimedb/src/helpers/tournamentHelpers.ts` returns 1+ (uses indexed lookup)
</verification>

<success_criteria>
All new features are implemented: 5 new reducers (advance_to_next_game, shelve_series, resume_series, check_in_tournament, and bestOf wiring), all existing reducers extended for BetweenGames/Shelved/CheckIn awareness, the full ParticipantStatus lifecycle (Registered -> CheckedIn -> Active -> Eliminated) is wired, all new reducers are exported from index.ts, and cancel_tournament cascade uses the Lobby.bracket_match_id btree index for efficient lookup.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-05-SUMMARY.md`
</output>
