---
phase: 10.1-best-of-n-team-split-winner-rework
plan: 04
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - spacetimedb/src/reducers/concede.ts
  - spacetimedb/src/reducers/adminMatchTools.ts
  - spacetimedb/src/reducers/matchResultSubmission.ts
  - spacetimedb/src/reducers/draftClassic.ts
  - spacetimedb/src/reducers/draftAuction.ts
  - spacetimedb/src/helpers/disconnectHelpers.ts
  - spacetimedb/src/reducers/matchFinalization.ts
  - spacetimedb/src/reducers/scoreEntry.ts
autonomous: true
requirements:
  - D-30
  - D-31
  - D-32
  - D-33
  - D-42
  - D-43
  - D-44

must_haves:
  truths:
    - "performConcede writes winnerTeamSide (not winnerUserId) and matchEndReason (not matchOutcome) on MatchResultRecord"
    - "admin_force_finalize uses winnerTeamSide and matchEndReason"
    - "submit_match_result converts winnerId param to winnerTeamSide via participant teamSide lookup"
    - "MatchResultRecord inserts in draftClassic/draftAuction set winnerTeamSide/matchEndReason to undefined (not winnerUserId)"
    - "MatchResultRecord inserts do NOT include tournamentId field"
    - "ensureMatchAlive checks matchEndReason (not matchOutcome)"
    - "process_tournament_mmr queries via BracketMatch -> MatchResultRecord.bracket_match_id (not tournament_id)"
  artifacts:
    - path: "spacetimedb/src/reducers/concede.ts"
      provides: "Concede reducers using winnerTeamSide + matchEndReason"
      contains: "winnerTeamSide"
    - path: "spacetimedb/src/reducers/adminMatchTools.ts"
      provides: "Admin tools using winnerTeamSide + matchEndReason"
      contains: "winnerTeamSide"
    - path: "spacetimedb/src/reducers/matchFinalization.ts"
      provides: "Tournament MMR via BracketMatch query path"
      contains: "bracket_match_id"
  key_links:
    - from: "spacetimedb/src/reducers/concede.ts"
      to: "spacetimedb/src/tables/matchResult.ts"
      via: "writes winnerTeamSide on concede"
      pattern: "winnerTeamSide.*Blue\\|Red"
    - from: "spacetimedb/src/reducers/matchFinalization.ts"
      to: "spacetimedb/src/tables/bracketMatch.ts"
      via: "queries BracketMatch.tournament_id for MMR processing"
      pattern: "BracketMatch\\.tournament_id\\.filter"
---

<objective>
Update all match-related reducers and helpers to use the new schema: winnerTeamSide replaces winnerUserId, matchEndReason replaces matchOutcome, and tournamentId is removed from MatchResultRecord inserts. Also rewrites the process_tournament_mmr query path (from direct tournament_id index to BracketMatch -> bracket_match_id derivation).

Purpose: These 8 files handle match creation, scoring, concede, admin tools, and MMR processing. They all write or read winnerUserId and matchOutcome, which are being replaced.
Output: All match reducer files use new schema types. process_tournament_mmr has new query strategy.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-CONTEXT.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-RESEARCH.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-01-SUMMARY.md

<interfaces>
<!-- Key schema contracts from Plan 01 -->

From matchResult.ts (after Plan 01):
- winnerTeamSide: TeamSide.optional() (Blue/Red/undefined for draw)
- matchEndReason: MatchEndReason.optional() (Completed/Draw/Concede)
- tournamentId column REMOVED
- bracket_match_id btree index on bracketMatchId ADDED

From enums.ts (after Plan 01):
- MatchEndReason: { Completed, Draw, Concede }
- ConcedeTrigger: { Disconnect, VoluntaryLeave, RefereeDecision } (unchanged)

Convention: team1 = Blue, team2 = Red in BracketMatch
- concede.ts line 65: winnerTeamSide === 'Blue' ? bracketMatch.team1Id : bracketMatch.team2Id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concede + admin tools + submit + disconnect helpers</name>
  <files>
    spacetimedb/src/reducers/concede.ts,
    spacetimedb/src/reducers/adminMatchTools.ts,
    spacetimedb/src/reducers/matchResultSubmission.ts,
    spacetimedb/src/helpers/disconnectHelpers.ts,
    spacetimedb/src/reducers/scoreEntry.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/concede.ts,
    spacetimedb/src/reducers/adminMatchTools.ts,
    spacetimedb/src/reducers/matchResultSubmission.ts,
    spacetimedb/src/helpers/disconnectHelpers.ts,
    spacetimedb/src/reducers/scoreEntry.ts
  </read_first>
  <action>
**concede.ts — Scopes D + F:**

1. Replace `MatchOutcome` import with `MatchEndReason`.
2. `performConcede` function: 
   - Replace `winnerUserId` with `winnerTeamSide` in MatchResultRecord insert/update. The losing team's side is already known (e.g., the conceding user's team side), so the winner is the opposite side: `winnerTeamSide: concedingTeamSide.tag === 'Blue' ? { tag: 'Red', value: {} } as any : { tag: 'Blue', value: {} } as any`.
   - Replace `matchOutcome: { tag: 'Concede', value: {} }` with `matchEndReason: { tag: 'Concede', value: {} }`.
   - Remove `tournamentId` from MatchResultRecord insert (D-42). Derive tournament context from `bracketMatch.tournamentId` where needed (the bracketMatchId is already on the match result).
3. `concede_match`: Replace `matchOutcome` with `matchEndReason` in any status checks.
4. `claim_forfeit`: Same replacements.
5. `defer_match`: Same replacements.

**adminMatchTools.ts — Scopes D + F:**

1. `admin_force_finalize`:
   - Replace `winnerUserId` determination with `winnerTeamSide`. Instead of finding the captain userId, determine the winning team side directly from the match context (e.g., which team had more points). Set `winnerTeamSide: { tag: 'Blue', value: {} }` or `{ tag: 'Red', value: {} }`.
   - Replace `matchOutcome` assignment: instead of `BlueWins`/`RedWins`, use `matchEndReason: { tag: 'Completed', value: {} }` for decisive results or `{ tag: 'Draw', value: {} }` for draws.
   - Remove `tournamentId` derivation from MatchResultRecord (it doesn't have the field anymore). When the reducer needs tournamentId for permission checks, derive from lobby or bracketMatch.
2. `admin_void_match`: Replace `matchOutcome` field with `matchEndReason`. Remove `tournamentId` from match result.
3. `admin_set_bracket_winner`: Replace `winnerUserId` with `winnerTeamSide`. The winning team side is determined from the winnerId param: look up which team the winnerId is on via BracketMatch team1Id/team2Id, then set the corresponding TeamSide.

**matchResultSubmission.ts — Scopes D + F:**
1. `submit_match_result`: The reducer receives `winnerId` as a u32 param. Convert to `winnerTeamSide`:
   - If `winnerId === 0` (sentinel for draw/no winner): set `winnerTeamSide: undefined`.
   - Otherwise: look up which team side the winning user is on by checking `MatchResultParticipant` rows for the match — find the participant with `userId === winnerId`, read their `teamSide.tag`, set `winnerTeamSide` to that TeamSide.
   - Replace `matchOutcome` field with `matchEndReason`: set to `{ tag: 'Completed', value: {} }` when there's a winner, `{ tag: 'Draw', value: {} }` for draw.
2. Remove `tournamentId` from any MatchResultRecord writes (D-42). If tournamentId is needed for access control in this reducer, derive from lobby.tournamentId or bracketMatch.
3. Replace `winnerUserId` field name in all MatchResultRecord updates/inserts.

**disconnectHelpers.ts — Scope D:**
`ensureMatchAlive`: Replace `matchOutcome?.tag === 'Concede'` with `matchEndReason?.tag === 'Concede'`. Also replace any `matchOutcome` field reads with `matchEndReason`.

**scoreEntry.ts (if it references matchOutcome or winnerUserId):**
Check for any `matchOutcome` or `winnerUserId` references and replace with `matchEndReason` and `winnerTeamSide` respectively.
  </action>
  <verify>
    <automated>! grep -q "winnerUserId" spacetimedb/src/reducers/concede.ts && ! grep -q "matchOutcome" spacetimedb/src/reducers/concede.ts && ! grep -q "MatchOutcome" spacetimedb/src/reducers/concede.ts && ! grep -q "winnerUserId" spacetimedb/src/reducers/adminMatchTools.ts && ! grep -q "matchOutcome" spacetimedb/src/reducers/adminMatchTools.ts && ! grep -q "winnerUserId" spacetimedb/src/reducers/matchResultSubmission.ts && ! grep -q "matchOutcome" spacetimedb/src/helpers/disconnectHelpers.ts && grep -q "winnerTeamSide" spacetimedb/src/reducers/concede.ts && grep -q "matchEndReason" spacetimedb/src/reducers/concede.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - concede.ts: zero occurrences of `winnerUserId`, `matchOutcome`, `MatchOutcome`; contains `winnerTeamSide` and `matchEndReason`
    - concede.ts: performConcede does not include `tournamentId` in MatchResultRecord insert
    - adminMatchTools.ts: zero occurrences of `winnerUserId`, `matchOutcome`, `MatchOutcome`; uses `winnerTeamSide` and `matchEndReason`
    - adminMatchTools.ts: admin_force_finalize uses `Completed`/`Draw` variants (not `BlueWins`/`RedWins`)
    - matchResultSubmission.ts: converts winnerId param to winnerTeamSide via participant teamSide lookup
    - matchResultSubmission.ts: no `tournamentId` in MatchResultRecord writes
    - disconnectHelpers.ts: uses `matchEndReason` (not `matchOutcome`) for concede check
    - scoreEntry.ts: no references to old field names
  </acceptance_criteria>
  <done>Concede, admin tools, match submission, and disconnect helpers all migrated to winnerTeamSide + matchEndReason</done>
</task>

<task type="auto">
  <name>Task 2: Draft reducers + match finalization (process_tournament_mmr query rewrite)</name>
  <files>
    spacetimedb/src/reducers/draftClassic.ts,
    spacetimedb/src/reducers/draftAuction.ts,
    spacetimedb/src/reducers/matchFinalization.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/draftClassic.ts,
    spacetimedb/src/reducers/draftAuction.ts,
    spacetimedb/src/reducers/matchFinalization.ts
  </read_first>
  <action>
**draftClassic.ts — Scopes B, D, F:**

1. In `start_draft` (or wherever MatchResultRecord is first inserted):
   - Replace `winnerUserId: undefined` with `winnerTeamSide: undefined` in the MatchResultRecord insert.
   - Replace `matchOutcome: undefined` with `matchEndReason: undefined`.
   - Remove `tournamentId` field from the insert (D-42). The match result links to the tournament via `bracketMatchId`.
   - Replace `MatchOutcome` import with `MatchEndReason` if used.

2. In `start_draft` MatchSession initialization (will be extended in Plan 05 for series tracking, but the columns need valid values NOW):
   - Since Plan 01 added `currentGameNumber`, `gamesWonBlue`, `gamesWonRed`, `seriesBestOf` to MatchSession, the existing `start_draft` must set these when creating the MatchSession row.
   - Set `currentGameNumber: 1`, `gamesWonBlue: 0`, `gamesWonRed: 0`, `seriesBestOf: lobby.bestOf || 1` (read bestOf from Lobby, default to 1 if 0/undefined).

3. In MatchSessionStep inserts: add `gameNumber: matchSession.currentGameNumber` (or `1` for the initial insert if MatchSession isn't created yet; align with the value set above).

**draftAuction.ts — Scopes B, D, F:**
Same changes as draftClassic.ts:
1. MatchResultRecord insert: `winnerTeamSide: undefined`, `matchEndReason: undefined`, remove `tournamentId`.
2. MatchSession initialization: set `currentGameNumber: 1`, `gamesWonBlue: 0`, `gamesWonRed: 0`, `seriesBestOf`.
3. MatchSessionStep inserts: add `gameNumber` field.

**matchFinalization.ts — Scope F (critical query path rewrite):**

1. `process_tournament_mmr` reducer:
   BEFORE: `const matchResults = [...ctx.db.MatchResultRecord.tournament_id.filter(tournamentId)]`
   AFTER: New query strategy:
   ```typescript
   // Step 1: Find all bracket matches for tournament
   const bracketMatches = [...ctx.db.BracketMatch.tournament_id.filter(tournamentId)];
   
   // Step 2: For each bracket match, find associated match result
   const matchResults = bracketMatches
       .map(bm => [...ctx.db.MatchResultRecord.bracket_match_id.filter(bm.id)][0])
       .filter(Boolean);
   ```
   This uses the new `bracket_match_id` btree index on MatchResultRecord added in Plan 01.

2. Replace any `matchOutcome` field reads with `matchEndReason` in this file.
3. Replace any `winnerUserId` references with `winnerTeamSide`.
4. `finalize_match_result`: if it reads `matchResult.tournamentId`, replace with derivation from `BracketMatch.id.find(matchResult.bracketMatchId)?.tournamentId`.
  </action>
  <verify>
    <automated>! grep -q "winnerUserId" spacetimedb/src/reducers/draftClassic.ts && ! grep -q "matchOutcome" spacetimedb/src/reducers/draftClassic.ts && ! grep -q "winnerUserId" spacetimedb/src/reducers/draftAuction.ts && grep -q "currentGameNumber" spacetimedb/src/reducers/draftClassic.ts && grep -q "seriesBestOf" spacetimedb/src/reducers/draftClassic.ts && grep -q "gameNumber" spacetimedb/src/reducers/draftClassic.ts && grep -q "bracket_match_id" spacetimedb/src/reducers/matchFinalization.ts && ! grep -q "tournament_id.filter" spacetimedb/src/reducers/matchFinalization.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - draftClassic.ts: MatchResultRecord insert uses `winnerTeamSide: undefined`, `matchEndReason: undefined`, no `tournamentId`
    - draftClassic.ts: MatchSession insert includes `currentGameNumber: 1`, `gamesWonBlue: 0`, `gamesWonRed: 0`, `seriesBestOf`
    - draftClassic.ts: MatchSessionStep inserts include `gameNumber` field
    - draftAuction.ts: same 3 changes as draftClassic
    - matchFinalization.ts: process_tournament_mmr uses `BracketMatch.tournament_id.filter` -> `MatchResultRecord.bracket_match_id.filter` query path
    - matchFinalization.ts: zero occurrences of `MatchResultRecord.tournament_id.filter` or `tournament_id.filter` on match results
    - matchFinalization.ts: all `matchOutcome` references replaced with `matchEndReason`
  </acceptance_criteria>
  <done>Draft reducers write new schema fields and series tracking columns; process_tournament_mmr uses new BracketMatch->MatchResultRecord query path</done>
</task>

</tasks>

<verification>
- `grep -rn "winnerUserId\|matchOutcome\|MatchOutcome" spacetimedb/src/reducers/concede.ts spacetimedb/src/reducers/adminMatchTools.ts spacetimedb/src/reducers/matchResultSubmission.ts spacetimedb/src/reducers/draftClassic.ts spacetimedb/src/reducers/draftAuction.ts spacetimedb/src/reducers/matchFinalization.ts spacetimedb/src/helpers/disconnectHelpers.ts` returns zero results
- `grep -c "bracket_match_id" spacetimedb/src/reducers/matchFinalization.ts` returns 1+
- `grep -c "currentGameNumber" spacetimedb/src/reducers/draftClassic.ts` returns 1+
</verification>

<success_criteria>
All 8 match-related files are migrated. No references to winnerUserId, matchOutcome, or MatchOutcome remain. process_tournament_mmr has working query path through BracketMatch. Draft reducers initialize series tracking columns and gameNumber on steps.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-04-SUMMARY.md`
</output>
