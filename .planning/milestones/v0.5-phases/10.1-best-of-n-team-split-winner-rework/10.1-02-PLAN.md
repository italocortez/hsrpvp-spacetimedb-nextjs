---
phase: 10.1-best-of-n-team-split-winner-rework
plan: 02
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - spacetimedb/src/helpers/finalizationHelpers.ts
  - spacetimedb/src/reducers/bracketAdvancement.ts
  - spacetimedb/src/helpers/bracketHelpers.ts
  - spacetimedb/src/reducers/bracketGeneration.ts
  - spacetimedb/src/helpers/bracketGeneration.ts
autonomous: true
requirements:
  - D-01
  - D-20
  - D-30
  - D-31
  - D-32
  - D-33
  - D-34
  - D-40
  - D-42
  - D-44

must_haves:
  truths:
    - "Finalization pipeline uses winnerTeamSide instead of winnerUserId for all winner determination (MMR, stats, relationships, character stats, bracket advance)"
    - "Finalization pipeline uses matchEndReason instead of matchOutcome (Completed/Draw/Concede variants)"
    - "Finalization pipeline derives tournamentId from bracketMatch when needed, not from MatchResultRecord.tournamentId"
    - "Bracket advancement reads winnerTeamSide.tag directly (Blue->team1Id, Red->team2Id) without participant lookup"
    - "All GroupStanding references in bracket helpers/reducers are renamed to GroupPhaseRecord"
    - "Bracket generation validates team member count via TournamentTeamMember instead of teamGroupId"
    - "Eliminated status set on final bracket loss (single elim = any loss, double elim = losers bracket loss)"
  artifacts:
    - path: "spacetimedb/src/helpers/finalizationHelpers.ts"
      provides: "19-step finalization pipeline with winnerTeamSide and matchEndReason"
      contains: "winnerTeamSide"
    - path: "spacetimedb/src/reducers/bracketAdvancement.ts"
      provides: "Bracket advancement with winnerTeamSide, GroupPhaseRecord, TournamentTeamMember"
      contains: "winnerTeamSide"
    - path: "spacetimedb/src/helpers/bracketHelpers.ts"
      provides: "GroupPhaseRecord helpers"
      contains: "GroupPhaseRecord"
  key_links:
    - from: "spacetimedb/src/helpers/finalizationHelpers.ts"
      to: "spacetimedb/src/tables/matchResult.ts"
      via: "reads matchResult.winnerTeamSide"
      pattern: "matchResult\\.winnerTeamSide"
    - from: "spacetimedb/src/reducers/bracketAdvancement.ts"
      to: "spacetimedb/src/tables/bracketMatch.ts"
      via: "maps winnerTeamSide to team1Id/team2Id"
      pattern: "winnerTeamSide.*tag.*Blue.*team1Id"
---

<objective>
Update the finalization pipeline (finalizationHelpers.ts) and bracket system (bracketAdvancement.ts, bracketHelpers.ts, bracketGeneration.ts) to use the new schema from Plan 01. This is the highest-impact code change in the phase: winnerTeamSide replaces winnerUserId in 6+ finalization steps, MatchEndReason replaces MatchOutcome, GroupPhaseRecord replaces GroupStanding, TournamentTeamMember replaces teamGroupId lookups, and tournamentId is derived from bracketMatch instead of read from MatchResultRecord.

Purpose: These 5 files are the core of the match lifecycle and bracket system. Updating them unblocks Plans 03-05.
Output: Finalization pipeline and bracket system fully migrated to new schema.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-CONTEXT.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-RESEARCH.md
@.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-01-SUMMARY.md

<interfaces>
<!-- Key schema contracts from Plan 01 that this plan depends on -->

From enums.ts (after Plan 01):
- MatchEndReason: { Completed, Draw, Concede } (replaces MatchOutcome)
- LobbyStage: { Waiting, Drafting, Equipping, Scoring, BetweenGames, Shelved, AwaitingResult, Finished }

From matchResult.ts (after Plan 01):
- winnerTeamSide: TeamSide.optional() (replaces winnerUserId: t.u32().optional())
- matchEndReason: MatchEndReason.optional() (replaces matchOutcome: MatchOutcome.optional())
- tournamentId column REMOVED
- bracket_match_id btree index on bracketMatchId ADDED

From bracketMatch.ts (after Plan 01):
- lobbyId column REMOVED
- tournamentId still exists on BracketMatch

From groupPhaseRecord.ts (replaces groupStanding.ts):
- Export: GroupPhaseRecord (was GroupStanding)

From tournamentTeamMember.ts (NEW):
- PK: [teamId, userId], indexes: team_id, by_tournament_and_user

Convention: team1 = Blue, team2 = Red in BracketMatch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalization pipeline migration (finalizationHelpers.ts)</name>
  <files>spacetimedb/src/helpers/finalizationHelpers.ts</files>
  <read_first>
    spacetimedb/src/helpers/finalizationHelpers.ts,
    spacetimedb/src/tables/matchResult.ts,
    spacetimedb/src/types/enums.ts
  </read_first>
  <action>
Read the full finalizationHelpers.ts file. This contains the 19-step `runFinalization` pipeline. Apply these changes:

**Import changes:**
- Replace `MatchOutcome` import with `MatchEndReason` from enums.
- Add `TournamentTeamMember` import (from tables/tournamentTeamMember).
- Replace `GroupStanding` import with `GroupPhaseRecord` (from tables/groupPhaseRecord) if present.
- Remove `TournamentParticipant` import if present.

**Step 6 (Match outcome determination) — per D-30, D-32, D-33:**
BEFORE: Complex logic reading `matchResult.winnerUserId`, looking up participant to determine team side, then assigning BlueWins/RedWins.
AFTER: Simplified logic:
```typescript
if (matchResult.winnerTeamSide === undefined) {
    matchEndReason = { tag: 'Draw', value: {} } as any;
} else {
    matchEndReason = { tag: 'Completed', value: {} } as any;
}
```
Rename the local variable from `matchOutcome` to `matchEndReason` everywhere in the function.
Update the MatchSessionHistory insert to use `matchEndReason` field and `MatchEndReason` type.

**Step 8 (Archive steps) — no change needed yet.** The gameNumber partitioning for best-of-N will be added in Plan 05. This step currently archives all steps regardless.

**Step 11 (MMR calculation):**
Replace `matchResult.winnerUserId` usage with `matchResult.winnerTeamSide`. The ELO calculation needs to determine if each participant won or lost:
- For each MatchResultParticipant, compare their `teamSide.tag` with `matchResult.winnerTeamSide?.tag` to determine win/loss.
- If `matchResult.winnerTeamSide === undefined`, treat as draw.
- Remove any `winnerUserId` comparisons.

**Step 13 (PlayerStat increments):**
Replace `matchResult.winnerUserId === participant.userId` check with `matchResult.winnerTeamSide?.tag === participant.teamSide.tag` check.

**Step 15 (PlayerRelationship pairs):**
Replace winner determination: instead of `matchResult.winnerUserId === userA`, use `matchResult.winnerTeamSide?.tag === participantA.teamSide.tag`.

**Step 16 (Character stats):**
Replace winner determination logic same as Step 13 — compare teamSide tags.

**Step 17 (Bracket advancement) — per D-42, D-44:**
Replace `matchResult.tournamentId` with a derivation: 
```typescript
const bracketMatch = matchResult.bracketMatchId !== undefined
    ? ctx.db.BracketMatch.id.find(matchResult.bracketMatchId)
    : undefined;
const tournamentId = bracketMatch?.tournamentId;
```
Replace any `TournamentParticipant` lookups with `TournamentTeamMember` lookups for bracket advancement.
For winner team determination in bracket advance: use `matchResult.winnerTeamSide?.tag === 'Blue' ? bracketMatch.team1Id : bracketMatch.team2Id` directly — NO participant lookup needed.

**revealTournamentHistory function (if present) — per D-42:**
Replace `MatchResultRecord.tournament_id.filter(tournamentId)` with: find BracketMatch rows via `BracketMatch.tournament_id.filter(tournamentId)`, then for each bracketMatch, find MatchResultRecord via `MatchResultRecord.bracket_match_id.filter(bracketMatch.id)`.

**All `matchOutcome` field references:**
Rename to `matchEndReason` everywhere in the file. Replace `MatchOutcome` type references with `MatchEndReason`.

**All `GroupStanding` references:**
Replace with `GroupPhaseRecord` — any `ctx.db.GroupStanding.*` becomes `ctx.db.GroupPhaseRecord.*`.

**Concede check in finalization matrix:**
Replace `matchResult.matchOutcome?.tag === 'Concede'` with `matchResult.matchEndReason?.tag === 'Concede'`.
  </action>
  <verify>
    <automated>grep -c "winnerTeamSide" spacetimedb/src/helpers/finalizationHelpers.ts && grep -c "matchEndReason" spacetimedb/src/helpers/finalizationHelpers.ts && ! grep -q "winnerUserId" spacetimedb/src/helpers/finalizationHelpers.ts && ! grep -q "matchOutcome" spacetimedb/src/helpers/finalizationHelpers.ts && ! grep -q "MatchOutcome" spacetimedb/src/helpers/finalizationHelpers.ts && ! grep -q "GroupStanding" spacetimedb/src/helpers/finalizationHelpers.ts && ! grep -q "tournamentId.*filter\|tournament_id.*filter" spacetimedb/src/helpers/finalizationHelpers.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File contains zero occurrences of `winnerUserId`, `matchOutcome` (field), `MatchOutcome` (type), `GroupStanding`, `TournamentParticipant`
    - File contains `winnerTeamSide` in winner determination logic
    - File contains `matchEndReason` for match end reason assignments
    - File derives tournamentId from `bracketMatch.tournamentId` (not from `matchResult.tournamentId`)
    - MatchSessionHistory insert uses `matchEndReason` field name and `MatchEndReason` type
    - Winner team ID for bracket advance uses `winnerTeamSide.tag === 'Blue' ? bracketMatch.team1Id : bracketMatch.team2Id` pattern
    - PlayerStat/PlayerRelationship/character stat winner checks compare teamSide tags (not userId)
  </acceptance_criteria>
  <done>Finalization pipeline fully migrated: winnerTeamSide, matchEndReason, tournamentId derivation, GroupPhaseRecord, TournamentTeamMember</done>
</task>

<task type="auto">
  <name>Task 2: Bracket advancement + helpers + generation migration</name>
  <files>
    spacetimedb/src/reducers/bracketAdvancement.ts,
    spacetimedb/src/helpers/bracketHelpers.ts,
    spacetimedb/src/reducers/bracketGeneration.ts,
    spacetimedb/src/helpers/bracketGeneration.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/bracketAdvancement.ts,
    spacetimedb/src/helpers/bracketHelpers.ts,
    spacetimedb/src/reducers/bracketGeneration.ts,
    spacetimedb/src/helpers/bracketGeneration.ts
  </read_first>
  <action>
**bracketHelpers.ts — Scope A (GroupPhaseRecord rename, 6 occurrences):**
1. Replace import of `GroupStanding` with `GroupPhaseRecord`.
2. Replace all `ctx.db.GroupStanding.*` calls with `ctx.db.GroupPhaseRecord.*` in `updateGroupStandings` and `sortGroupStandings` functions.
3. Update any type annotations referencing `GroupStanding` to `GroupPhaseRecord`.

**bracketAdvancement.ts — Scopes A, C, D, E:**

*Scope A (3 occurrences):*
Replace all `ctx.db.GroupStanding.*` with `ctx.db.GroupPhaseRecord.*` in `reverseGroupStandings`.

*Scope D (winnerTeamSide — major rewrite of submit_and_advance_bracket):*
In `submit_and_advance_bracket`:
- Remove the `TournamentParticipant.by_tournament_and_user` lookup that maps winnerUserId -> teamGroupId.
- Replace with direct mapping: `const winnerTeamId = matchResult.winnerTeamSide?.tag === 'Blue' ? bracketMatch.team1Id : bracketMatch.team2Id;`
- This eliminates the participant lookup entirely.
- Update all `winnerUserId` references to use `winnerTeamSide`.

In `rollback_bracket_match`:
- Replace `MatchResultRecord.tournament_id.filter()` with: find match result via `MatchResultRecord.bracket_match_id.filter(bracketMatchId)` to locate the match result for rollback.
- Update any `matchOutcome` references to `matchEndReason`.

*Scope C (TournamentParticipant -> TournamentTeamMember):*
- Remove `TournamentParticipant` import. Add `TournamentTeamMember` import.
- Any remaining participant lookups for team determination should use `TournamentTeamMember.by_tournament_and_user.filter([tournamentId, userId])` instead.

*Scope E (Eliminated status — per D-40):*
After bracket advancement resolves a match, determine if the loser is eliminated:
- Single elimination: any loss = Eliminated. After advancing winner, find the loser's team members via `TournamentTeamMember.team_id.filter(loserTeamId)`, then for each member's userId, update their `TournamentEnrolled` status to `Eliminated` (delete + re-insert pattern for composite PK).
- Double elimination: only eliminated when losing in the losers bracket (check `bracketMatch.bracketSide.tag === 'Losers'` and there's no `nextLoserMatchId`).
- Group phase: elimination determined post-group (not in this reducer — handled when group phase ends).
Add `TournamentEnrolled` import for this.

**bracketGeneration.ts — Scopes A, C:**
*Scope A (5 occurrences):*
Replace all `ctx.db.GroupStanding.insert/delete/primaryKey.delete` with `ctx.db.GroupPhaseRecord.*`. Update `insertGroupStandings` function to use `GroupPhaseRecord`.

*Scope C (1 site — team member count validation):*
Replace `TournamentParticipant` lookup for counting members per team. Use `TournamentTeamMember.team_id.filter(teamId)` to count members per team for validation in `validateRegistrationToSeeding` or similar validation.

**bracketGeneration.ts (helper file) — Scope A:**
Update JSDoc comment referencing "GroupStanding" to "GroupPhaseRecord".
  </action>
  <verify>
    <automated>! grep -q "GroupStanding" spacetimedb/src/helpers/bracketHelpers.ts && ! grep -q "GroupStanding" spacetimedb/src/reducers/bracketAdvancement.ts && ! grep -q "GroupStanding" spacetimedb/src/reducers/bracketGeneration.ts && ! grep -q "winnerUserId" spacetimedb/src/reducers/bracketAdvancement.ts && ! grep -q "TournamentParticipant" spacetimedb/src/reducers/bracketAdvancement.ts && ! grep -q "TournamentParticipant" spacetimedb/src/reducers/bracketGeneration.ts && grep -q "winnerTeamSide" spacetimedb/src/reducers/bracketAdvancement.ts && grep -q "GroupPhaseRecord" spacetimedb/src/helpers/bracketHelpers.ts && grep -q "Eliminated" spacetimedb/src/reducers/bracketAdvancement.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - bracketHelpers.ts: zero occurrences of `GroupStanding`, all replaced with `GroupPhaseRecord`
    - bracketAdvancement.ts: zero occurrences of `GroupStanding`, `winnerUserId`, `TournamentParticipant`, `matchOutcome`
    - bracketAdvancement.ts: contains `winnerTeamSide` and `GroupPhaseRecord` and `TournamentTeamMember`
    - bracketAdvancement.ts: submit_and_advance_bracket uses `winnerTeamSide.tag === 'Blue' ? bracketMatch.team1Id : bracketMatch.team2Id` pattern
    - bracketAdvancement.ts: rollback_bracket_match uses `bracket_match_id.filter` not `tournament_id.filter`
    - bracketAdvancement.ts: sets Eliminated status on loser's TournamentEnrolled when no nextLoserMatchId
    - bracketGeneration.ts: zero occurrences of `GroupStanding`, `TournamentParticipant`
    - bracketGeneration.ts: uses `TournamentTeamMember.team_id.filter` for member count validation
    - bracketGeneration.ts helper: JSDoc updated to reference GroupPhaseRecord
  </acceptance_criteria>
  <done>All bracket system files migrated: GroupPhaseRecord, winnerTeamSide, TournamentTeamMember, Eliminated status wiring</done>
</task>

</tasks>

<verification>
- `grep -rn "GroupStanding\|winnerUserId\|matchOutcome\|MatchOutcome\|TournamentParticipant" spacetimedb/src/helpers/finalizationHelpers.ts spacetimedb/src/reducers/bracketAdvancement.ts spacetimedb/src/helpers/bracketHelpers.ts spacetimedb/src/reducers/bracketGeneration.ts` returns zero results
- `grep -c "winnerTeamSide" spacetimedb/src/helpers/finalizationHelpers.ts` returns 5+
- `grep -c "GroupPhaseRecord" spacetimedb/src/helpers/bracketHelpers.ts` returns 6+
- `grep -c "Eliminated" spacetimedb/src/reducers/bracketAdvancement.ts` returns 1+
</verification>

<success_criteria>
The 5 highest-impact files (finalizationHelpers, bracketAdvancement, bracketHelpers, bracketGeneration reducer + helper) are fully migrated to use new schema types. No references to old names remain in these files.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-best-of-n-team-split-winner-rework/10.1-02-SUMMARY.md`
</output>
