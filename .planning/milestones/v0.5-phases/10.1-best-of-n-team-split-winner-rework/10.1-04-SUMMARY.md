---
phase: 10.1-best-of-n-team-split-winner-rework
plan: 04
subsystem: backend
tags: [spacetimedb, reducers, match-result, schema-migration, series-tracking]

# Dependency graph
requires:
  - phase: 10.1-best-of-n-team-split-winner-rework
    plan: 01
    provides: winnerTeamSide/matchEndReason schema, bracket_match_id index, MatchSession series columns, MatchSessionStep.gameNumber

provides:
  - concede.ts using winnerTeamSide + matchEndReason (Concede), no tournamentId in insert
  - adminMatchTools.ts using winnerTeamSide + matchEndReason (Completed/Draw)
  - matchResultSubmission.ts converting winnerId param to winnerTeamSide via participant lookup
  - disconnectHelpers.ts checking matchEndReason for concede guard
  - draftClassic.ts initializing series tracking columns on MatchSession, gameNumber on steps
  - draftAuction.ts gameNumber on all step inserts
  - matchFinalization.ts process_tournament_mmr via BracketMatch -> MatchResultRecord query path

affects:
  - 10.1-05 (finalization pipeline — reads winnerTeamSide/matchEndReason, series archival)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - tournamentId derivation: matchResult.bracketMatchId -> BracketMatch.tournamentId (replaces direct matchResult.tournamentId)
    - process_tournament_mmr two-step query: BracketMatch.tournament_id.filter -> MatchResultRecord.bracket_match_id.filter
    - winnerId param -> winnerTeamSide: look up MatchResultParticipant by userId, read teamSide.tag

key-files:
  created: []
  modified:
    - spacetimedb/src/reducers/concede.ts
    - spacetimedb/src/reducers/adminMatchTools.ts
    - spacetimedb/src/reducers/matchResultSubmission.ts
    - spacetimedb/src/helpers/disconnectHelpers.ts
    - spacetimedb/src/reducers/draftClassic.ts
    - spacetimedb/src/reducers/draftAuction.ts
    - spacetimedb/src/reducers/matchFinalization.ts

key-decisions:
  - "submit_match_result param renamed winnerId (was winnerUserId) — matches plan spec; converts to winnerTeamSide via MatchResultParticipant lookup"
  - "tournamentId access in submit_match_result and finalize_match_result derived from bracketMatch.tournamentId via bracketMatchId FK"
  - "draftAuction.ts has no MatchResultRecord/MatchSession inserts — only needs gameNumber on step inserts (start_draft in draftClassic handles both modes)"

# Metrics
duration: 6min
completed: 2026-04-04
---

# Phase 10.1 Plan 04: Match Reducer Migration Summary

**winnerTeamSide replaces winnerUserId, matchEndReason replaces matchOutcome in all match reducers; process_tournament_mmr rewired through BracketMatch; draft reducers initialize series tracking columns**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-04T02:28:00Z
- **Completed:** 2026-04-04T02:34:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- **concede.ts**: `performConcede` writes `winnerTeamSide` (Blue/Red enum value) + `matchEndReason: Concede`; `tournamentId` removed from MatchResultRecord insert; `defer_match` same cleanup
- **adminMatchTools.ts**: `admin_force_finalize` derives `winnerTeamSide` from `bracketMatch.team1Id/team2Id` comparison; uses `Completed`/`Draw` matchEndReason; no more `BlueWins`/`RedWins`
- **matchResultSubmission.ts**: `submit_match_result` converts `winnerId` u32 param to `winnerTeamSide` by looking up the winner's `MatchResultParticipant.teamSide`; tournament access derived from `bracketMatch.tournamentId`
- **disconnectHelpers.ts**: `ensureMatchAlive` guard checks `matchEndReason?.tag === 'Concede'` instead of `matchOutcome`
- **draftClassic.ts**: `start_draft` initializes `currentGameNumber:1`, `gamesWonBlue:0`, `gamesWonRed:0`, `seriesBestOf:lobby.bestOf||1` on MatchSession; MatchResultRecord insert uses `winnerTeamSide:undefined`, `matchEndReason:undefined`, no `tournamentId`; all 4 MatchSessionStep inserts include `gameNumber:session.currentGameNumber`
- **draftAuction.ts**: all 5 MatchSessionStep inserts (nominate, bid, AuctionSold in pass_bid, auto-nominate, auto-AuctionSold in timer_expiry) include `gameNumber:session.currentGameNumber`
- **matchFinalization.ts**: `process_tournament_mmr` replaces `MatchResultRecord.tournament_id.filter` with two-step query through `BracketMatch.tournament_id.filter` → `MatchResultRecord.bracket_match_id.filter`; `finalize_match_result` derives tournamentId from bracketMatch

## Task Commits

1. **Task 1: Concede + admin tools + submit + disconnect helpers** - `86a2203` (feat)
2. **Task 2: Draft reducers + match finalization** - `dec31d9` (feat)

## Files Modified

- `spacetimedb/src/reducers/concede.ts` — winnerTeamSide+matchEndReason(Concede), removed tournamentId, removed winnerUserId derivation block
- `spacetimedb/src/reducers/adminMatchTools.ts` — winnerTeamSide from bracketMatch team IDs, Completed/Draw matchEndReason, removed BlueWins/RedWins
- `spacetimedb/src/reducers/matchResultSubmission.ts` — param renamed winnerId, winnerTeamSide via participant lookup, tournamentId derived from bracketMatch
- `spacetimedb/src/helpers/disconnectHelpers.ts` — matchEndReason check in ensureMatchAlive
- `spacetimedb/src/reducers/draftClassic.ts` — series tracking on MatchSession, gameNumber on 4 step inserts, MatchResultRecord uses new fields
- `spacetimedb/src/reducers/draftAuction.ts` — gameNumber on 5 step inserts
- `spacetimedb/src/reducers/matchFinalization.ts` — BracketMatch->MatchResultRecord query path, tournamentId derivation

## Verification

```
grep -rn "winnerUserId|matchOutcome|MatchOutcome" [8 files] → 0 results (PASS)
grep -c "bracket_match_id" matchFinalization.ts → 2 (PASS)
grep -c "currentGameNumber" draftClassic.ts → 5 (PASS)
grep -c "MatchResultRecord.tournament_id.filter" matchFinalization.ts → 0 (PASS)
```

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None. All fields written with real values:
- `winnerTeamSide` set from actual team side derivation
- `matchEndReason` set from outcome type (Concede/Completed/Draw)
- `gameNumber` set from `session.currentGameNumber` (initialized to 1 at match start)
- `seriesBestOf` set from `lobby.bestOf || 1` (defaults to 1 for non-series matches)

## Self-Check: PASSED

- All 7 modified files exist on disk
- Both task commits verified in git log (86a2203, dec31d9)
- Zero occurrences of winnerUserId/matchOutcome/MatchOutcome in all 7 files
- bracket_match_id: 2 occurrences in matchFinalization.ts (BracketMatch query + MatchResultRecord query)
- currentGameNumber: 5 occurrences in draftClassic.ts (4 in step inserts + 1 in MatchSession insert)
