---
phase: 06-anonymous-play-and-player-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/types/enums.ts
  - spacetimedb/src/tables/playerStats.ts
  - spacetimedb/src/tables/characterStats.ts
  - spacetimedb/src/tables/playerRelationship.ts
  - spacetimedb/src/tables/mmrRating.ts
  - spacetimedb/src/tables/leaderboard.ts
  - spacetimedb/src/tables/lobby.ts
  - spacetimedb/src/tables/lobbyCursorEvent.ts
  - spacetimedb/src/tables/matchSessionStep.ts
  - spacetimedb/src/tables/matchSessionHistory.ts
  - spacetimedb/src/tables/matchSessionStepHistory.ts
  - spacetimedb/src/tables/matchParticipantHistory.ts
  - spacetimedb/src/tables/season.ts
  - spacetimedb/src/tables/globalCharacterStat.ts
  - spacetimedb/src/tables/tournamentPlayerAccount.ts
  - spacetimedb/src/tables/matchResultGameHistory.ts
  - spacetimedb/src/schema.ts
  - spacetimedb/src/index.ts
  - spacetimedb/src/reducers/seasonAdmin.ts
  - test/backend/match-results/elo-calculation.unit.test.ts
  - test/backend/match-results/account-rating.unit.test.ts
autonomous: true
requirements: [STAT-01, STAT-02, STAT-05, STAT-06, STAT-07, STAT-08, ANON-01, ANON-03, ANON-04]

must_haves:
  truths:
    - "PlayerStat has expanded PK with seasonId, matchType, teamSize columns"
    - "PlayerCharacterStat has expanded PK and 4 new ban/faced columns"
    - "PlayerRelationship has expanded PK with seasonId, matchType, teamSize columns"
    - "MmrRating PK includes seasonId (non-optional)"
    - "Leaderboard PK includes seasonId (non-optional)"
    - "Lobby has rosterVisibility enum column replacing isOpenRoster bool, plus requireOwnership bool"
    - "LobbyCursorEvent and MatchSessionStep have anonymousLabel column"
    - "Season table exists with admin CRUD reducers"
    - "GlobalCharacterStat table exists for community aggregates"
    - "TournamentPlayerAccount junction table exists"
    - "MatchResultGameHistory table mirrors MatchResultGame with matchHistoryId FK"
    - "MatchSessionStepHistory repurposed from JSON blob to individual step rows"
    - "MatchSessionHistory rosterBlue/rosterRed removed"
    - "MatchParticipantHistory has displayName column"
    - "Module publishes to maincloud with --clear-database"
  artifacts:
    - path: "spacetimedb/src/tables/season.ts"
      provides: "Season table definition"
      contains: "export const Season"
    - path: "spacetimedb/src/tables/globalCharacterStat.ts"
      provides: "GlobalCharacterStat table"
      contains: "export const GlobalCharacterStat"
    - path: "spacetimedb/src/tables/tournamentPlayerAccount.ts"
      provides: "TournamentPlayerAccount junction table"
      contains: "export const TournamentPlayerAccount"
    - path: "spacetimedb/src/tables/matchResultGameHistory.ts"
      provides: "MatchResultGameHistory table"
      contains: "export const MatchResultGameHistory"
    - path: "spacetimedb/src/reducers/seasonAdmin.ts"
      provides: "Season admin reducers"
      contains: "export const create_season"
  key_links:
    - from: "spacetimedb/src/tables/season.ts"
      to: "spacetimedb/src/schema.ts"
      via: "import and registration"
      pattern: "import.*Season.*from.*tables/season"
    - from: "spacetimedb/src/reducers/seasonAdmin.ts"
      to: "spacetimedb/src/index.ts"
      via: "export re-export"
      pattern: "export.*create_season.*from.*reducers/seasonAdmin"
---

<objective>
Schema foundation for Phase 6: PK expansions, new tables, column changes, and Season admin reducers.

Purpose: All table schema changes must land together in a single --clear-database publish. This establishes the data contracts that Plans 02 and 03 build upon.
Output: Updated table definitions, 4 new tables, Season admin reducers, module published to maincloud with --clear-database.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-anonymous-play-and-player-stats/06-CONTEXT.md

@spacetimedb/src/schema.ts
@spacetimedb/src/index.ts
@spacetimedb/src/types/enums.ts
@spacetimedb/src/tables/playerStats.ts
@spacetimedb/src/tables/characterStats.ts
@spacetimedb/src/tables/playerRelationship.ts
@spacetimedb/src/tables/mmrRating.ts
@spacetimedb/src/tables/leaderboard.ts
@spacetimedb/src/tables/lobby.ts
@spacetimedb/src/tables/lobbyCursorEvent.ts
@spacetimedb/src/tables/matchSessionStep.ts
@spacetimedb/src/tables/matchSessionHistory.ts
@spacetimedb/src/tables/matchSessionStepHistory.ts
@spacetimedb/src/tables/matchParticipantHistory.ts
@spacetimedb/src/tables/matchResultGame.ts
@spacetimedb/src/tables/tournament.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema changes — PK expansions, column modifications, 4 new tables</name>
  <files>
    spacetimedb/src/tables/playerStats.ts,
    spacetimedb/src/tables/characterStats.ts,
    spacetimedb/src/tables/playerRelationship.ts,
    spacetimedb/src/tables/mmrRating.ts,
    spacetimedb/src/tables/leaderboard.ts,
    spacetimedb/src/tables/lobby.ts,
    spacetimedb/src/tables/lobbyCursorEvent.ts,
    spacetimedb/src/tables/matchSessionStep.ts,
    spacetimedb/src/tables/matchSessionHistory.ts,
    spacetimedb/src/tables/matchSessionStepHistory.ts,
    spacetimedb/src/tables/matchParticipantHistory.ts,
    spacetimedb/src/tables/season.ts,
    spacetimedb/src/tables/globalCharacterStat.ts,
    spacetimedb/src/tables/tournamentPlayerAccount.ts,
    spacetimedb/src/tables/matchResultGameHistory.ts,
    spacetimedb/src/schema.ts
  </files>
  <read_first>
    spacetimedb/src/tables/playerStats.ts,
    spacetimedb/src/tables/characterStats.ts,
    spacetimedb/src/tables/playerRelationship.ts,
    spacetimedb/src/tables/mmrRating.ts,
    spacetimedb/src/tables/leaderboard.ts,
    spacetimedb/src/tables/lobby.ts,
    spacetimedb/src/tables/lobbyCursorEvent.ts,
    spacetimedb/src/tables/matchSessionStep.ts,
    spacetimedb/src/tables/matchSessionHistory.ts,
    spacetimedb/src/tables/matchSessionStepHistory.ts,
    spacetimedb/src/tables/matchParticipantHistory.ts,
    spacetimedb/src/tables/matchResultGame.ts,
    spacetimedb/src/types/enums.ts,
    spacetimedb/src/schema.ts,
    .planning/phases/06-anonymous-play-and-player-stats/06-CONTEXT.md
  </read_first>
  <action>
Load the spacetimedb skill before writing any code.

**A. PK Expansions (per D-38 through D-46):**

1. **PlayerStat** (`playerStats.ts`): Expand PK from `[userId, gameMode, draftMode]` to `[userId, gameMode, draftMode, seasonId, matchType, teamSize]`. Add 3 new columns BEFORE the audit columns:
   - `seasonId: t.u32()` (default 0 for pre-season, per D-45)
   - `matchType: MatchType` (import from enums)
   - `teamSize: t.u8()`
   Update the primaryKey array. Update btree index `by_user_mode_draft` to include all 6 PK columns with accessor `by_user_mode_draft_season_type_size`. Keep the `by_user` single-column index.
   Change `public: true` to `public: false` (per D-33 — stats are private, served via per-user view).

2. **PlayerCharacterStat** (`characterStats.ts`): Expand PK from `[userId, characterName, gameMode, draftMode]` to `[userId, characterName, gameMode, draftMode, seasonId, matchType, teamSize]`. Add 7 new columns BEFORE the audit columns:
   - `seasonId: t.u32()`
   - `matchType: MatchType`
   - `teamSize: t.u8()`
   - `timesBannedInMatch: t.u32()` (per D-22)
   - `timesFaced: t.u32()` (per D-24)
   - `winsAgainst: t.u32()` (per D-24)
   - `lossesAgainst: t.u32()` (per D-24)
   Update primaryKey array. Update btree index to `by_user_char_mode_draft_season_type_size` with all 7 PK columns. Keep `by_user` index.
   Change `public: true` to `public: false` (per D-33).

3. **PlayerRelationship** (`playerRelationship.ts`): Expand PK from `[userId, otherUserId, gameMode, draftMode]` to `[userId, otherUserId, gameMode, draftMode, seasonId, matchType, teamSize]`. Add 3 new columns before audit:
   - `seasonId: t.u32()`
   - `matchType: MatchType`
   - `teamSize: t.u8()`
   Update primaryKey array. Update btree index to `by_user_other_mode_draft_season_type_size`. Keep `by_user` index.
   Change `public: true` to `public: false` (per D-33).

4. **MmrRating** (`mmrRating.ts`): Expand PK from `[userId, gameMode]` to `[userId, gameMode, seasonId]` (per D-41). Change `seasonId` from `t.u32().optional()` to `t.u32()` (no longer optional — part of PK). Update btree index `by_user_and_mode` to `by_user_mode_season` with columns `[userId, gameMode, seasonId]`.

5. **Leaderboard** (`leaderboard.ts`): Expand PK from `[category, rank]` to `[category, rank, seasonId]` (per D-42). Change `seasonId` from `t.u32().optional()` to `t.u32()`. Update btree index `by_category_and_rank` to `by_category_rank_season` with columns `[category, rank, seasonId]`.

**B. Column Modifications:**

6. **Lobby** (`lobby.ts`):
   - Replace `isOpenRoster: t.bool()` with `rosterVisibility: RosterVisibility` (import from enums, per D-10/D-59).
   - Add `requireOwnership: t.bool()` after rosterVisibility (per D-17/D-61).
   - Keep all other columns unchanged. Add `isTournamentControlled: t.bool()` after bracketMatchId (useful for inheritance chain, already on MatchResultRecord).

7. **LobbyCursorEvent** (`lobbyCursorEvent.ts`): Add `anonymousLabel: t.string().optional()` after `senderUserId` (per D-62). Keep event: true.

8. **MatchSessionStep** (`matchSessionStep.ts`): Add `anonymousLabel: t.string().optional()` after `actorUserId` (per D-62).

9. **MatchSessionHistory** (`matchSessionHistory.ts`): Remove `rosterBlue: t.string()` and `rosterRed: t.string()` columns (per D-52/D-57). These are now derived from MatchSessionStepHistory step rows with action=Pick.

10. **MatchSessionStepHistory** (`matchSessionStepHistory.ts`): Complete rework per D-50/D-58. Replace the single JSON `steps` column with individual step columns. New schema:
    ```
    matchHistoryId: t.u32(),     // FK to MatchSessionHistory.id
    sequence: t.u32(),           // Step number (1, 2, 3...)
    actorUserId: t.u32(),        // Who performed the action
    actorDisplayName: t.string(), // Denormalized for replay (per D-53/D-64)
    teamSide: TeamLabel,         // Blue/Red/Spectator
    action: ActionType,          // Pick, Ban, Bid, etc.
    characterName: t.string().optional(), // Character involved (null for Pause/Undo)
    payload: t.string().optional(),       // JSON for action-specific data (bid amount, etc.)
    audit columns
    ```
    PK: `[matchHistoryId, sequence]`. Btree index on `by_match_history` with columns `[matchHistoryId]`. Import TeamLabel, ActionType from enums.

11. **MatchParticipantHistory** (`matchParticipantHistory.ts`): Add `displayName: t.string()` after `teamSide` (per D-53/D-64).

**C. New Tables:**

12. **Season** (`spacetimedb/src/tables/season.ts`): Per D-47.
    ```
    id: t.u32().primaryKey().autoInc(),
    name: t.string(),              // e.g. "Patch 3.0"
    startDate: t.timestamp(),
    endDate: t.timestamp().optional(), // null = current season
    isActive: t.bool(),
    audit columns
    ```
    Public table. Index on `is_active` (btree, columns: ['isActive']).

13. **GlobalCharacterStat** (`spacetimedb/src/tables/globalCharacterStat.ts`): Per D-34.
    ```
    characterName: t.string(),
    gameMode: GameMode,
    draftMode: DraftMode,
    seasonId: t.u32(),
    matchType: MatchType,
    teamSize: t.u8(),
    timesPicked: t.u32(),
    timesBanned: t.u32(),        // Community meta (per D-29)
    wins: t.u32(),
    losses: t.u32(),
    matchesPlayed: t.u32(),
    audit columns
    ```
    PK: `[characterName, gameMode, draftMode, seasonId, matchType, teamSize]`. Public table. Btree index `by_char_mode` on `[characterName, gameMode]`. Btree index `by_season` on `[seasonId]`.

14. **TournamentPlayerAccount** (`spacetimedb/src/tables/tournamentPlayerAccount.ts`): Per D-21.
    ```
    tournamentId: t.u32(),
    userId: t.u32(),
    hsrAccountId: t.u32(),       // The locked-in HSR account for this tournament
    audit columns
    ```
    PK: `[tournamentId, userId, hsrAccountId]`. Public table. Btree index `by_tournament_and_user` on `[tournamentId, userId]`. Btree index `by_user` on `[userId]`.

15. **MatchResultGameHistory** (`spacetimedb/src/tables/matchResultGameHistory.ts`): Per D-51. Mirrors MatchResultGame columns but with matchHistoryId FK instead of matchResultId.
    ```
    matchHistoryId: t.u32(),     // FK to MatchSessionHistory.id
    gameNumber: t.u8(),
    gameMode: GameMode,
    teamBlueScreenshotUrl: t.string().optional(),
    teamRedScreenshotUrl: t.string().optional(),
    teamBlueCyclesUsed: t.u32().optional(),
    teamRedCyclesUsed: t.u32().optional(),
    teamBlueScore: t.u64().optional(),
    teamRedScore: t.u64().optional(),
    teamBlueBoss1Score: t.u64().optional(),
    teamBlueBoss2Score: t.u64().optional(),
    teamRedBoss1Score: t.u64().optional(),
    teamRedBoss2Score: t.u64().optional(),
    winnerTeamSide: TeamLabel,
    audit columns
    ```
    PK: `[matchHistoryId, gameNumber]`. Public table. Btree index `by_match_history` on `[matchHistoryId]`.

**D. Schema Registration:**

16. In `schema.ts`: Add imports and registrations for Season, GlobalCharacterStat, TournamentPlayerAccount, MatchResultGameHistory. Group under appropriate comment sections.

**Important:** All columns must use camelCase naming. All audit columns inline per table (4 columns at end). Import MatchType from enums.ts in all files that add matchType column.
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - playerStats.ts contains `primaryKey: ['userId', 'gameMode', 'draftMode', 'seasonId', 'matchType', 'teamSize']`
    - playerStats.ts contains `public: false`
    - playerStats.ts contains `seasonId: t.u32()`
    - playerStats.ts contains `matchType: MatchType`
    - playerStats.ts contains `teamSize: t.u8()`
    - characterStats.ts contains `primaryKey: ['userId', 'characterName', 'gameMode', 'draftMode', 'seasonId', 'matchType', 'teamSize']`
    - characterStats.ts contains `public: false`
    - characterStats.ts contains `timesBannedInMatch: t.u32()`
    - characterStats.ts contains `timesFaced: t.u32()`
    - characterStats.ts contains `winsAgainst: t.u32()`
    - characterStats.ts contains `lossesAgainst: t.u32()`
    - playerRelationship.ts contains `primaryKey: ['userId', 'otherUserId', 'gameMode', 'draftMode', 'seasonId', 'matchType', 'teamSize']`
    - playerRelationship.ts contains `public: false`
    - mmrRating.ts contains `primaryKey: ['userId', 'gameMode', 'seasonId']`
    - mmrRating.ts contains `seasonId: t.u32()` (NOT optional)
    - leaderboard.ts contains `primaryKey: ['category', 'rank', 'seasonId']`
    - leaderboard.ts contains `seasonId: t.u32()` (NOT optional)
    - lobby.ts contains `rosterVisibility: RosterVisibility` (NOT isOpenRoster)
    - lobby.ts contains `requireOwnership: t.bool()`
    - lobbyCursorEvent.ts contains `anonymousLabel: t.string().optional()`
    - matchSessionStep.ts contains `anonymousLabel: t.string().optional()`
    - matchSessionHistory.ts does NOT contain `rosterBlue`
    - matchSessionHistory.ts does NOT contain `rosterRed`
    - matchSessionStepHistory.ts contains `actorDisplayName: t.string()`
    - matchSessionStepHistory.ts contains `sequence: t.u32()`
    - matchSessionStepHistory.ts does NOT contain `steps: t.string()`
    - matchParticipantHistory.ts contains `displayName: t.string()`
    - season.ts file exists and contains `export const Season`
    - globalCharacterStat.ts file exists and contains `export const GlobalCharacterStat`
    - tournamentPlayerAccount.ts file exists and contains `export const TournamentPlayerAccount`
    - matchResultGameHistory.ts file exists and contains `export const MatchResultGameHistory`
    - schema.ts imports Season, GlobalCharacterStat, TournamentPlayerAccount, MatchResultGameHistory
    - TypeScript compiles without errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>All table schema changes are in place: 5 PK expansions, 5 column modifications, 4 new tables created and registered in schema.ts. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Season admin reducers, publish --clear-database, generate bindings, update tests</name>
  <files>
    spacetimedb/src/reducers/seasonAdmin.ts,
    spacetimedb/src/index.ts,
    test/backend/match-results/elo-calculation.unit.test.ts,
    test/backend/match-results/account-rating.unit.test.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/eloAdmin.ts,
    spacetimedb/src/index.ts,
    spacetimedb/src/helpers/ensurePermissions.ts,
    spacetimedb/src/helpers/auditColumns.ts,
    test/backend/match-results/elo-calculation.unit.test.ts,
    test/backend/match-results/account-rating.unit.test.ts,
    .planning/phases/06-anonymous-play-and-player-stats/06-CONTEXT.md
  </read_first>
  <action>
Load the spacetimedb skill before writing any code.

**A. Season Admin Reducers** (`spacetimedb/src/reducers/seasonAdmin.ts`):

Create file with 2 admin reducers following the EloAdmin pattern (per D-47/D-48). Import spacetimedb from '../schema', t and SenderError from 'spacetimedb/server', ensureAdmin from '../helpers/ensurePermissions', auditInsert and auditUpdate from '../helpers/auditColumns'.

1. `create_season`: Admin-only. Parameters: `{ name: t.string(), startDate: t.timestamp(), endDate: t.timestamp().optional() }`.
   - Call `ensureAdmin(ctx)` to verify admin role.
   - Insert into Season table: `{ id: 0, name, startDate, endDate, isActive: false, ...auditInsert(ctx, admin.id) }`.
   - Log: `[SEASON] Season "${name}" created by admin #${admin.id}`.

2. `set_active_season`: Admin-only. Parameters: `{ seasonId: t.u32() }`.
   - Call `ensureAdmin(ctx)`.
   - Find Season by PK: `ctx.db.Season.id.find(seasonId)`. Throw SenderError if not found.
   - Deactivate all currently active seasons: iterate `[...ctx.db.Season.is_active.filter(true)]`, for each set `isActive: false` via `id.update({ ...row, isActive: false, ...auditUpdate(ctx, row, admin.id) })`.
   - Activate the target season: `ctx.db.Season.id.update({ ...season, isActive: true, ...auditUpdate(ctx, season, admin.id) })`.
   - Log: `[SEASON] Season #${seasonId} set as active by admin #${admin.id}`.

**B. Export reducers** in `index.ts`:
Add line: `export { create_season, set_active_season } from './reducers/seasonAdmin';`

**C. Update existing tests** (per D-65):
The tests in `test/backend/match-results/` may reference PlayerStat or MmrRating PK shapes. Read each test file and update any mock data or assertions that reference the old PK shapes:

- For `elo-calculation.unit.test.ts`: This tests pure ELO math functions (getKFactor, calculateExpectedScore, calculateRatingChange, etc.) — likely NO changes needed as these are pure math, not table-shape dependent. Verify and leave unchanged if confirmed.

- For `account-rating.unit.test.ts`: This tests accountRating calculation — likely NO changes needed. Verify and leave unchanged if confirmed.

If either test file constructs mock PlayerStat/MmrRating/Leaderboard rows with old shapes, update the mock objects to include the new PK fields (seasonId: 0, matchType: { tag: 'Ranked', value: {} }, teamSize: 1).

**D. Publish and generate:**
Run `spacetime publish hsrpvp-spacetimedb-nextjs-test1 --clear-database -y --module-path spacetimedb` to deploy with schema reset.
Then run `spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb` to regenerate client bindings.

**E. Run tests:**
Run `npx vitest run` from the test/ directory. If tests fail due to PK shape changes in mock data, update the mocks to include the new columns. The target is all existing tests pass.
  </action>
  <verify>
    <automated>cd "D:/GitsWork/hsrpvp-spacetimedb-nextjs" && npx vitest run --config test/vitest.config.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - seasonAdmin.ts file exists and contains `export const create_season` and `export const set_active_season`
    - create_season calls `ensureAdmin(ctx)` and inserts into Season table
    - set_active_season deactivates all active seasons before activating the target
    - index.ts contains `export { create_season, set_active_season } from './reducers/seasonAdmin'`
    - Module publishes successfully to maincloud with --clear-database
    - `spacetime generate` completes without error
    - src/module_bindings/ contains updated bindings with new tables and reducers
    - All existing tests pass (vitest exits 0)
  </acceptance_criteria>
  <done>Season admin reducers created and exported, module published to maincloud with --clear-database, client bindings regenerated, all existing tests pass with updated PK shapes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in spacetimedb/ — zero errors
2. `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT * FROM season LIMIT 1"` — table exists (empty, 0 rows is fine)
3. `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT * FROM global_character_stat LIMIT 1"` — table exists
4. `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT * FROM tournament_player_account LIMIT 1"` — table exists
5. `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT * FROM match_result_game_history LIMIT 1"` — table exists
6. `npx vitest run --config test/vitest.config.ts` — all tests pass
</verification>

<success_criteria>
- All 5 PK expansions landed (PlayerStat, PlayerCharacterStat, PlayerRelationship, MmrRating, Leaderboard)
- All 5 column modifications landed (Lobby, LobbyCursorEvent, MatchSessionStep, MatchSessionHistory, MatchSessionStepHistory, MatchParticipantHistory)
- 4 new tables created and registered (Season, GlobalCharacterStat, TournamentPlayerAccount, MatchResultGameHistory)
- PlayerStat, PlayerCharacterStat, PlayerRelationship are `public: false` (per D-33)
- Season admin reducers work (create_season, set_active_season)
- Module published to maincloud with --clear-database
- Client bindings regenerated
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-anonymous-play-and-player-stats/06-01-SUMMARY.md`
</output>
