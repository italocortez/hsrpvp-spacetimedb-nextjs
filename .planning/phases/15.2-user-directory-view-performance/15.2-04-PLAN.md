---
phase: 15.2-user-directory-view-performance
plan: 04
type: execute
wave: 3
depends_on:
  - 15.2-01
  - 15.2-02
  - 15.2-03
files_modified:
  - spacetimedb/src/module_bindings/index.ts
  - spacetimedb/src/module_bindings/types.ts
  - spacetimedb/src/module_bindings/user_table.ts
  - spacetimedb/src/module_bindings/view_my_profile_table.ts
  - src/module_bindings/index.ts
  - src/module_bindings/types.ts
  - src/module_bindings/user_table.ts
  - src/module_bindings/view_my_profile_table.ts
autonomous: false
user_setup:
  - service: spacetime-cli
    why: "Publishing the updated module to maincloud requires the spacetime CLI (already in use; no new install)"
    env_vars: []
    dashboard_config:
      - task: "Verify default server is maincloud via `spacetime server list`"
        location: "Local shell — project uses the default server marked with ***"
requirements:
  - D-02
  - D-05
  - D-08
must_haves:
  truths:
    - "`spacetime publish` succeeds on the default (maincloud) server without data loss (non-destructive publish)"
    - "`deleted_user` table exists on the published module"
    - "`user` table on the published module no longer declares `isPrivate`"
    - "Regenerated bindings in both `spacetimedb/src/module_bindings/` and `src/module_bindings/` reflect the new shape"
  artifacts:
    - path: spacetimedb/src/module_bindings/index.ts
      provides: "Server-side bindings — includes DeletedUser table accessor"
    - path: src/module_bindings/index.ts
      provides: "Frontend bindings — includes DeletedUser, drops User.isPrivate"
  key_links:
    - from: spacetime publish
      to: maincloud
      via: "non-destructive publish (no --clear-database, no --delete-data)"
      pattern: "deleted_user.*created"
---

<objective>
Publish the Phase 15.2 schema changes to maincloud and regenerate bindings in both the server (`spacetimedb/src/module_bindings/`) and the frontend (`src/module_bindings/`). Adds the `deleted_user` table, drops `User.isPrivate` from generated types, lands the view flip change.

**Non-destructive by default.** This publish preserves existing user rows — no `--clear-database` and no `--delete-data=on-conflict` are used unless the human operator explicitly approves a re-seed afterwards. SpacetimeDB's schema-evolution handles the `isPrivate` column drop and the new `deleted_user` table addition in-place.

Purpose: Without this plan, Plan 05's integration tests and any runtime validation of the code changes from Plans 01-03 cannot observe the new table surface. Publishing is the gate that converts compiled TypeScript into running module state.

Output:
- Published module on maincloud with 15.2 changes live
- Regenerated bindings committed in both binding directories
- Human-verified `spacetime sql "SELECT * FROM deleted_user"` returns 0 rows (empty table)
- Human-verified `spacetime sql "SELECT * FROM user LIMIT 1"` no longer has `is_private` column
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.2-user-directory-view-performance/15.2-CONTEXT.md
@.planning/phases/15.2-user-directory-view-performance/15.2-RESEARCH.md
@scripts/post-publish.ts
@.claude/skills/spacetimedb/SKILL.md

<interfaces>
Verified against repo on 2026-04-15:

From `.claude/CLAUDE.md` §Deployment:
- Maincloud is the default publishing target
- Default server marked `***` in `spacetime server list`
- Publishing to maincloud is free
- Dashboard URL: `https://spacetimedb.com/@<username>/<database-name>`

From `memory/feedback_post_publish_only.md` (loaded via MEMORY.md index):
- After `--clear-database`, run ONLY `scripts/post-publish.ts`. Never call `register_server` manually.
- This plan does NOT use `--clear-database`, so post-publish.ts is NOT strictly required. Running it is still safe (it is idempotent on a populated database) but not mandatory — include it only when re-seeding is desired.

From `docs/auth/architecture.md` + existing project convention:
- Binding regeneration: `npx spacetime generate --project-path spacetimedb --lang typescript --out-dir spacetimedb/src/module_bindings`
- Frontend binding regen: `npx spacetime generate --project-path spacetimedb --lang typescript --out-dir src/module_bindings`
- Confirm `spacetime.json` points to the correct paths before running
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + publish to maincloud (non-destructive)</name>
  <files>(publishes module; no source files modified by this step)</files>
  <read_first>
    - .claude/CLAUDE.md §Deployment
    - .claude/skills/spacetimedb/SKILL.md §publishing section
    - spacetime.json (confirm module path)
    - scripts/post-publish.ts (understand what it does — only run after clear-database)
  </read_first>
  <action>
Execute the following commands in order. STOP and escalate to the human operator if any command fails with unexpected output.

1. Verify default server:
   ```bash
   spacetime server list
   ```
   Confirm the line with `***` marker is `maincloud`. If it is not, escalate — do not switch servers silently.

2. Typecheck before publish (safety — Plans 01-03 should have left this green already):
   ```bash
   cd spacetimedb && npx tsc --noEmit -p .
   ```
   Exit 0 required.

3. Publish the module NON-DESTRUCTIVELY (NO `--clear-database`, NO `--delete-data=on-conflict`):
   ```bash
   cd spacetimedb && spacetime publish
   ```
   Expected output indicates:
   - Module compiled
   - New `deleted_user` table created
   - Column `is_private` removed from `user` table
   - Existing data preserved

   If the publish output signals data loss required (e.g. "refusing to drop column is_private; existing rows present" or similar), STOP and escalate — do not add `--delete-data=on-conflict` unilaterally. The CONTEXT.md §domain does not authorize destructive migration; `isPrivate` is dead code and SpacetimeDB should accept its drop without data loss.

4. Do NOT run `scripts/post-publish.ts` in this task. That script is reserved for post-`--clear-database` re-seeding (memory/feedback_post_publish_only.md). The publish here is non-destructive; existing users stay.

**Do NOT:**
- Add `--clear-database` without explicit human approval
- Add `--delete-data=on-conflict` without explicit human approval
- Commit any `.env*` file changes (that would be a Gitignore Guardrail violation — the rule is absolute per `.claude/CLAUDE.md`)
- Run `scripts/register-server.ts` — only valid after a clear-database reset, not here
  </action>
  <verify>
    <automated>spacetime sql "SELECT COUNT(*) FROM deleted_user"</automated>
  </verify>
  <acceptance_criteria>
    - `spacetime server list` shows `***` on maincloud
    - `cd spacetimedb && npx tsc --noEmit -p .` exits 0
    - `spacetime publish` (no destructive flags) exits 0
    - `spacetime sql "SELECT COUNT(*) FROM deleted_user"` returns a single-row result with count 0 (table exists, empty)
    - `spacetime sql "SELECT * FROM user LIMIT 1"` output does NOT contain `is_private` column header
    - `spacetime sql "SELECT username FROM user WHERE username = 'SYSTEM'"` returns exactly 1 row (existing SYSTEM preserved — non-destructive publish confirmed)
  </acceptance_criteria>
  <done>
    Module published to maincloud non-destructively. `deleted_user` table exists and is empty. `user.is_private` no longer exists. SYSTEM user and any pre-existing accounts are intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate bindings (server + frontend)</name>
  <files>
    spacetimedb/src/module_bindings/index.ts,
    spacetimedb/src/module_bindings/types.ts,
    spacetimedb/src/module_bindings/user_table.ts,
    spacetimedb/src/module_bindings/view_my_profile_table.ts,
    src/module_bindings/index.ts,
    src/module_bindings/types.ts,
    src/module_bindings/user_table.ts,
    src/module_bindings/view_my_profile_table.ts
  </files>
  <read_first>
    - spacetime.json (binding output paths)
    - .claude/skills/spacetimedb/SKILL.md §"Module bindings" table
    - Current `src/module_bindings/user_table.ts` (confirm `isPrivate` will be removed after regen)
  </read_first>
  <action>
Regenerate both binding directories. The backend expects `spacetimedb/src/module_bindings/`; the Next.js frontend expects `src/module_bindings/`. Both MUST be regenerated — stale frontend bindings are a Phase 15.4 WR-class bug per its SUMMARYs.

1. Server bindings regen:
   ```bash
   npx spacetime generate --project-path spacetimedb --lang typescript --out-dir spacetimedb/src/module_bindings
   ```

2. Frontend bindings regen:
   ```bash
   npx spacetime generate --project-path spacetimedb --lang typescript --out-dir src/module_bindings
   ```

3. Verify the new table binding exists:
   ```bash
   ls src/module_bindings/deleted_user_table.ts
   ls spacetimedb/src/module_bindings/deleted_user_table.ts
   ```
   (The exact filename may be `deleted_user_table.ts` or similar — SpacetimeDB generates one file per table. Confirm by `ls src/module_bindings/ | grep deleted`.)

4. Confirm `isPrivate` is gone from both binding dirs:
   ```bash
   grep -r "isPrivate" src/module_bindings/ spacetimedb/src/module_bindings/
   ```
   Must return 0 matches.

5. Frontend typecheck — this is the gate that confirms nothing in the Next.js app was reading `User.isPrivate`:
   ```bash
   npm run build
   ```
   If the build fails on a missing `isPrivate` reference, that indicates a dead-code access path that Phase 15.2 missed. Fix the offending frontend file(s) by removing the access. The CONTEXT.md §domain explicitly states `isPrivate` is never read for gating — any frontend reference should be a no-op display or similar and can be deleted.

   If the build fails on any OTHER error, escalate — those are outside Phase 15.2 scope.

**Do NOT:**
- Hand-edit any file under `*/module_bindings/` — those are generated artifacts
- Skip the frontend regen. Phase 15.4 had a WR incident for exactly this — stale frontend bindings break the next phase silently.
- Add frontend fixes for compile errors that are unrelated to `isPrivate` removal
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls spacetimedb/src/module_bindings/ | grep -i deleted_user` matches at least one file
    - `ls src/module_bindings/ | grep -i deleted_user` matches at least one file
    - `grep -r "isPrivate" spacetimedb/src/module_bindings/` returns 0 matches
    - `grep -r "isPrivate" src/module_bindings/` returns 0 matches
    - `grep -rE "displayName.*string" spacetimedb/src/module_bindings/ | grep -i deleted` returns >= 1 (DeletedUser binding projects displayName)
    - `npm run build` exits 0
    - `git status` shows modifications under both `spacetimedb/src/module_bindings/` and `src/module_bindings/` (regen touched files)
  </acceptance_criteria>
  <done>
    Both binding directories regenerated. `deleted_user_table.ts` (or equivalent) exists in each. `isPrivate` eradicated from bindings. `npm run build` passes — frontend has no `User.isPrivate` references or they were patched out. Changes ready for commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies maincloud state before unlocking Plan 05</name>
  <files>(verification-only checkpoint; no source file modified)</files>
  <read_first>
    - .claude/CLAUDE.md §Autonomous Execution "DO pause for" list (this checkpoint is a cross-boundary verification gate, matches the list)
    - .claude/CLAUDE.md §Git Rules (`.env*` never committed)
  </read_first>
  <action>
STOP execution and request human verification before Plan 05 proceeds. Present the output of the commands below and wait for an explicit "approved" response. Do NOT auto-advance.

Commands to run and display output for:

1. `spacetime sql "SELECT COUNT(*) FROM deleted_user"` — expect `count = 0`
2. `spacetime sql "SELECT username FROM user WHERE username = 'SYSTEM'"` — expect exactly 1 row
3. `spacetime sql "SELECT COUNT(*) FROM user"` — compare to pre-publish user count (should be equal — no users lost)
4. `spacetime sql "SELECT COUNT(*) FROM user_deletion_job"` — note the value; it should be stable or only grow naturally
5. Open the maincloud dashboard at `https://spacetimedb.com/@<username>/<database-name>` and confirm the new `deleted_user` row appears in the tables list
6. Confirm `git status` shows only `module_bindings/` file modifications AND no accidental `.env*` modifications

Resume signal: human types "approved" once the dashboard shows `deleted_user` and SYSTEM user is intact.

If the human reports any anomaly (unexpected user row loss, missing `deleted_user` table, `.env` drift, etc.) STOP and escalate. Do NOT attempt remediation without explicit direction.
  </action>
  <verify>
    <automated>spacetime sql "SELECT COUNT(*) FROM deleted_user"</automated>
  </verify>
  <acceptance_criteria>
    - Operator has inspected the 6 verification outputs above
    - Operator has typed "approved"
    - `git status` shows no modifications under `.env*`
  </acceptance_criteria>
  <done>
    Human has approved after confirming: (a) deleted_user exists and is empty, (b) SYSTEM user is intact, (c) user count unchanged from pre-publish, (d) no `.env*` drift in `git status`. Plan 05 may now proceed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → maincloud | `spacetime publish` authenticated via CLI identity; writes module schema |
| module bindings regen → frontend build | Generated artifacts; mismatched bindings break compilation (safety gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-09 | Tampering | Unintended destructive migration | mitigate | Explicitly non-destructive publish; `--clear-database` and `--delete-data=on-conflict` prohibited without human approval; acceptance criteria verifies SYSTEM user preservation + user count parity |
| T-15.2-10 | Repudiation | Accidental `.env.local` overwrite | mitigate | CLAUDE.md Git Rules: NEVER delete or commit `.env.local`/`.env`; this plan does NOT run `post-publish.ts`; human verify step confirms `git status` cleanliness |
| T-15.2-11 | Denial of Service | Frontend build failure from stale bindings | mitigate | Both binding dirs regenerated in the same task; `npm run build` is the gate; Phase 15.4 WR incident informed this discipline |
</threat_model>

<verification>
- `spacetime sql "SELECT COUNT(*) FROM deleted_user"` returns `count = 0`
- `grep -r "isPrivate" spacetimedb/src/module_bindings/ src/module_bindings/` returns 0
- `npm run build` exits 0
- Human-verified via dashboard and SYSTEM-user preservation
</verification>

<success_criteria>
- Module reflects Plan 01+02+03 changes on maincloud
- Bindings match the new schema in both locations
- Frontend compiles
- No data lost, no `.env` drift, no SYSTEM user gone
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-user-directory-view-performance/15.2-04-SUMMARY.md` covering:
- The exact `spacetime publish` output (indicating which columns were dropped / which tables were added)
- Pre- and post-publish user row counts (from SQL)
- List of binding files regenerated (from `git status`)
- Confirmation that `post-publish.ts` was NOT run (non-destructive publish)
- Hand-off note: "Plan 05 runs integration tests and updates docs/auth/architecture.md + docs/views/architecture.md."
</output>
