---
phase: 15.2-user-directory-view-performance
plan: 04
subsystem: backend+build
tags: [spacetimedb, publish, bindings, maincloud, clear-database]

# Dependency graph
requires:
  - phase: 15.2-01
    provides: DeletedUser table + isPrivate drop
  - phase: 15.2-02
    provides: view flip + R1 fix
  - phase: 15.2-03
    provides: cascade refactor + schema.ts registration

provides:
  - Phase 15.2 schema live on maincloud (DeletedUser added, isPrivate dropped, view_user_directory auth-gated)
  - Regenerated TypeScript bindings in both spacetimedb/src/module_bindings/ and src/module_bindings/
  - Frontend compiles clean against new schema (npm run build exits 0)
  - post-publish fresh init: SYSTEM user registered, 6 game data tables seeded, token in .env.local (gitignored)

affects:
  - 15.2-05 (tests run against the live maincloud module)
  - Future phases that subscribe to User table see no ghosts (D-09 ensures eviction on delete)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "clear-database publish: acceptable for dev-phase work; follow with post-publish.ts to rebootstrap SYSTEM + seed data"
    - "dual bindings regen: spacetimedb/src/module_bindings/ for server-side helpers, src/module_bindings/ for frontend"
  pitfalls_avoided:
    - ".env.local NOT staged despite modification (CLAUDE.md Git Rule)"
    - "no git add -f, no --no-verify"
    - "no manual register_server call (memory rule — post-publish owns registration after --clear-database)"
---

# Plan 15.2-04 — Publish & Regenerate Bindings

**User-authorized `--clear-database` publish.** Plan was originally designed as non-destructive but the user confirmed data loss was acceptable for this phase.

## What shipped

**Commit:** `0b2a4f9 feat(15.2-04): regenerate bindings after --clear-database publish`

1. **Publish to maincloud (Task 1):**
   - `cd spacetimedb && spacetime publish --clear-database` succeeded
   - Post-publish script (`npm run post-publish`) ran cleanly:
     - SYSTEM user row created
     - All 6 game data tables reseeded (83 chars, 156 lightcones, 10 synergies × 3 modes)
     - Fresh auth token written to `.env.local` — gitignored, NOT committed

2. **Bindings regen (Task 2):**
   - `spacetimedb/src/module_bindings/types.ts` updated with new schema surface
   - `spacetimedb/src/module_bindings/hsr_account_lightcone_table.ts` deleted (stale — the table was renamed/restructured upstream and the old binding was orphaned)
   - `src/module_bindings/types.ts`, `user_table.ts`, `view_my_profile_table.ts` regenerated
   - `isPrivate` count across all generated bindings = 0 (grep-verified)
   - `deleted_user` is a private table (`public: false`), so no binding file generated for it — expected per SpacetimeDB convention

3. **Verification (Task 3 — checkpoint pre-authorized):**
   - `spacetime sql ... "SELECT COUNT(*) AS n FROM deleted_user"` → `n=0` (table exists, empty on fresh bootstrap)
   - `npm run build` → exit 0 (frontend TS compiles clean; no residual `isPrivate` references)
   - Human-verify checkpoint marked pre-authorized per user instruction ("data loss is fine this phase, just publish")

## Files changed (committed)

```
spacetimedb/src/module_bindings/hsr_account_lightcone_table.ts  | DELETED
spacetimedb/src/module_bindings/types.ts                        | M
src/module_bindings/types.ts                                    | M
src/module_bindings/user_table.ts                               | M
src/module_bindings/view_my_profile_table.ts                    | M
```

## Files modified but NOT committed (by design)

- `.env.local` — updated by `post-publish.ts` with fresh maincloud token. Gitignored, MUST NEVER be committed.
- `spacetimedb/dist/bundle.js` — pre-existing modification from before Phase 15.2; unchanged by this plan.

## Notes

- API 500 interrupted the executor mid-bookkeeping after the work commit landed. SUMMARY.md and STATE/ROADMAP updates were completed post-outage from orchestrator context. No work lost.
- Phase 15.2 is now ready for Plan 05 (tests + arch docs sync).

## Hand-off

- Plan 05 can now execute integration tests against the live module.
- `resolveUserLabel` and the R1 fix paths are deployed — tests will exercise end-to-end behavior.
