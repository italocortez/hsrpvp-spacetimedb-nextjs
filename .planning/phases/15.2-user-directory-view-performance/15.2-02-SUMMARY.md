---
phase: 15.2-user-directory-view-performance
plan: 02
subsystem: auth
tags: [spacetimedb, views, scheduled-reducers, soft-delete, security]

# Dependency graph
requires:
  - phase: 15.2-01
    provides: DeletedUser table, User.isPrivate removal, schema foundation for D-06/D-10

provides:
  - view_user_directory flipped to spacetimedb.view() — rejects anonymous subscribers at framework level
  - Dead .filter(u => !u.deletedAt) removed from view_user_directory body
  - admin_ban_user inserts UserDeletionJob after User.deletedAt update (D-10 site 1)
  - clientConnected ban-reconnect branch inserts UserDeletionJob after User.deletedAt update (D-10 site 2)
  - ScheduleAt imported from 'spacetimedb' at both R1 fix sites

affects:
  - 15.2-04 (publish + regen — validates the R1 fix against running database)
  - 15.2-05 (tests — D-16 anonymous rejection test, D-19 UserDeletionJob insert tests)
  - Phase 16 (frontend subscription lifecycle — view_user_directory now requires auth)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-phase soft-delete: all three writers (admin_delete_row, admin_ban_user, clientConnected) now uniformly set deletedAt + insert UserDeletionJob(scheduledAt: now+5s)"
    - "spacetimedb.view() anonymous rejection: framework rejects before body executes — no explicit getAuthenticatedUser() call needed in view body"

key-files:
  created: []
  modified:
    - spacetimedb/src/views/identityViews.ts
    - spacetimedb/src/reducers/banAdmin.ts
    - spacetimedb/src/index.ts

key-decisions:
  - "D-06: view_user_directory uses spacetimedb.view() — anonymous rejection is framework-level, no explicit auth check in body (matches view_my_identity pattern)"
  - "D-10: admin_ban_user actor is admin.id (human admin present); clientConnected actor is user.id (automated detection, no human admin)"
  - "D-09/filter removal: .filter(u => !u.deletedAt) removed from view body — ghost accumulation solved at write path by Plan 01 DeletedUser eviction, not at query time"

patterns-established:
  - "Pattern: ScheduleAt always imported from 'spacetimedb' not 'spacetimedb/server'"
  - "Pattern: UserDeletionJob.insert placed inside if(!user.deletedAt) guard to prevent double-scheduling during 5s pending window"

requirements-completed:
  - D-06
  - D-10

# Metrics
duration: 15min
completed: 2026-04-15
---

# Phase 15.2 Plan 02: View Auth Flip + R1 Bug Fix Summary

**`view_user_directory` flipped to authenticated-only and all three soft-delete writers now uniformly schedule the 5-second cascade via `UserDeletionJob`**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-15T14:00:00Z
- **Completed:** 2026-04-15T14:15:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `view_user_directory` now declared via `spacetimedb.view(...)` — anonymous subscribers receive a framework-level `SenderError` before the body executes. No explicit `getAuthenticatedUser()` call added; the framework handles rejection (confirmed by `view_my_identity` precedent at identityViews.ts:22).
- Dead `.filter(u => !u.deletedAt)` removed from view body. Ghost users are evicted to `DeletedUser` archive by Plan 01's D-09 implementation, making this filter a permanent no-op. Removal prevents future-reader confusion.
- Both R1 latent bug sites fixed: `admin_ban_user` (banAdmin.ts) and `clientConnected` ban-on-reconnect (index.ts) now insert `UserDeletionJob` immediately after setting `User.deletedAt`, scheduled 5 seconds out. All three soft-delete writers now use the identical two-phase pattern from `admin.ts:181-188`.

## Task Commits

1. **Task 1: Flip view_user_directory to authenticated-only + remove dead filter** - `99943ca` (feat)
2. **Task 2: Fix R1 bug — add UserDeletionJob.insert at both missing soft-delete writers** - `b5323ad` (fix)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified

- `spacetimedb/src/views/identityViews.ts` — `anonymousView` → `view` on `view_user_directory`; dead `.filter` removed; D-07 projection unchanged (9 fields)
- `spacetimedb/src/reducers/banAdmin.ts` — Added `import { ScheduleAt } from 'spacetimedb'`; added `UserDeletionJob.insert` inside `if (user && !user.deletedAt)` block with `auditInsert(ctx, admin.id)`
- `spacetimedb/src/index.ts` — Added `import { ScheduleAt } from 'spacetimedb'`; added `UserDeletionJob.insert` inside `if (isBanned && !user.deletedAt)` block with `auditInsert(ctx, user.id)`

## Decisions Made

- Actor convention confirmed: `admin_ban_user` uses `admin.id` (human admin initiated the ban — matches `BanRecord.insert` on banAdmin.ts:52); `clientConnected` uses `user.id` (automated detection, no human actor — matches existing `isOnline` audit on index.ts:122).
- `ScheduleAt` import from `'spacetimedb'` (not `'spacetimedb/server'`) — verified against `admin.ts` canonical pattern and RESEARCH.md Pitfall 2.
- Both `UserDeletionJob.insert` calls placed inside the existing `!user.deletedAt` guards — prevents double-scheduling if user reconnects during the 5-second pending window (Open Question 2 resolved: no issue, guard is sufficient).

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. TypeScript exited 0 after each task. All acceptance criteria passed on first run.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 03 (cascade refactor — `performUserDeletion` D-09 eviction to `DeletedUser`) is independent of this plan's files and can proceed immediately.
- **Plan 04 (publish + regen) will validate the R1 fix end-to-end against the running database.** The `UserDeletionJob` inserts require a live SpacetimeDB module to exercise the scheduled reducer path.
- **Plan 05 (tests) will add D-16 (anonymous rejection regression) and D-19 (admin_ban_user + clientConnected UserDeletionJob insert) test coverage.**
- Phase 16 frontend subscription lifecycle must subscribe to `view_user_directory` only after authentication — the server-side anchor (D-06) is now in place.

---
*Phase: 15.2-user-directory-view-performance*
*Completed: 2026-04-15*
