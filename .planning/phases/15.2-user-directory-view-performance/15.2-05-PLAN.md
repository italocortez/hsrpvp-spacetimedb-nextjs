---
phase: 15.2-user-directory-view-performance
plan: 05
type: execute
wave: 4
depends_on:
  - 15.2-04
files_modified:
  - test/backend/auth/auth-views.test.ts
  - test/backend/auth/ban-admin.test.ts
  - test/backend/auth/user-deletion.test.ts
  - docs/auth/architecture.md
  - docs/views/architecture.md
autonomous: true
requirements:
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
must_haves:
  truths:
    - "auth-views VIEW-01 test describes view_user_directory as authenticated-only"
    - "A new test asserts anonymous subscription attempts on view_user_directory are rejected"
    - "A new test asserts performUserDeletion evicts a non-guest-with-history user (User row gone, DeletedUser row present, history FK intact)"
    - "A new test asserts resolveUserLabel returns live → archived → fallback correctly across all three paths"
    - "Extended ban-admin test asserts admin_ban_user inserts a UserDeletionJob row"
    - "Extended ban-admin or integration test asserts clientConnected ban-on-reconnect inserts a UserDeletionJob row"
    - "docs/auth/architecture.md mentions two-phase soft-delete is now uniform across all three writers + DeletedUser archive"
    - "docs/views/architecture.md view_user_directory section reflects authenticated-only calling"
  artifacts:
    - path: test/backend/auth/user-deletion.test.ts
      provides: "D-17 + D-18 coverage"
    - path: test/backend/auth/auth-views.test.ts
      provides: "D-15 + D-16 coverage (updated VIEW-01 + new anon-rejection)"
    - path: test/backend/auth/ban-admin.test.ts
      provides: "D-19 coverage (extended)"
    - path: docs/auth/architecture.md
      provides: "Updated architecture reference for phase 15.2"
    - path: docs/views/architecture.md
      provides: "Updated view_user_directory auth note"
  key_links:
    - from: test/backend/auth/user-deletion.test.ts
      to: spacetimedb/src/helpers/userDeletionHelper.ts
      via: "integration test exercises performUserDeletion via scheduled job"
      pattern: "deleted_user"
    - from: docs/auth/architecture.md
      to: spacetimedb/src/helpers/userLabel.ts
      via: "documented cross-reference to resolveUserLabel"
      pattern: "resolveUserLabel"
---

<objective>
Land the test coverage for D-15 through D-19 and update the `architecture.md` feature docs. Contract updates (`docs/**/contract.md`) are explicitly DEFERRED per `.claude/CLAUDE.md` §Backend Feature Docs — contract edits happen during `/gsd-verify-work`, tagged `Phase 15.2 execution`.

Purpose: Lock in regression guards for the cascade refactor, the R1 bug fix, the view flip, and the label resolver. Ensure the architectural docs reflect the new two-phase soft-delete uniformity + the DeletedUser archive so future phases build on accurate references.

Output:
- `test/backend/auth/auth-views.test.ts`: VIEW-01 updated for authenticated calling (D-15); new test rejects anonymous subscription (D-16)
- `test/backend/auth/user-deletion.test.ts` (NEW): D-17 eviction test + D-18 resolveUserLabel three-path test
- `test/backend/auth/ban-admin.test.ts`: extended with D-19 assertions (UserDeletionJob row after admin_ban_user + clientConnected reconnect path)
- `docs/auth/architecture.md`: updated sections on soft-delete flow + DeletedUser archive
- `docs/views/architecture.md`: updated `view_user_directory` entry for authenticated-only access
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.2-user-directory-view-performance/15.2-CONTEXT.md
@.planning/phases/15.2-user-directory-view-performance/15.2-RESEARCH.md
@test/backend/auth/auth-views.test.ts
@test/backend/auth/ban-admin.test.ts
@test/shared/connection.ts
@docs/auth/architecture.md
@docs/views/architecture.md
@.claude/CLAUDE.md

<interfaces>
Verified against repo on 2026-04-15:

From `.claude/CLAUDE.md` §Backend Feature Docs:
- `docs/{feature}/architecture.md` — update every time backend code changes (this phase: `user/`, `auth/`, `views/`)
- `docs/{feature}/contract.md` — NEVER edit during execution; post-execution only, tagged `Phase 15.2 execution`

From test harness conventions (test/shared/connection.ts):
- `createTestHarness()` — anonymous/guest connection
- `createVerifiedTestHarness()` — pre-authed SYSTEM connection
- `queryPrivateTable(sql)` — bypasses client RLS via server token
- Tests use `it.skipIf(!hasServerToken())(...)` to skip without credentials

From RESEARCH.md §Validation Architecture + §Pitfall 8:
- VIEW-01 style tests can use `expect(true).toBe(true)` with explanatory comment when runtime SQL reach is impossible
- D-16 anonymous-rejection test must use a guest harness attempting a WebSocket subscription, not a SQL call (views are virtual)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test/backend/auth/user-deletion.test.ts for D-17 (eviction) + D-18 (resolveUserLabel)</name>
  <files>test/backend/auth/user-deletion.test.ts</files>
  <read_first>
    - test/backend/auth/auth-views.test.ts (reference style: Vitest, `it.skipIf(!hasServerToken())`, `queryPrivateTable` patterns)
    - test/backend/auth/ban-admin.test.ts (reference style: admin flow + scheduled-job wait)
    - test/shared/connection.ts (harness API)
    - spacetimedb/src/helpers/userDeletionHelper.ts (the SUT — Plan 03's eviction rewrite)
    - spacetimedb/src/helpers/userLabel.ts (the SUT — Plan 03's new helper)
    - .planning/phases/15.2-user-directory-view-performance/15.2-RESEARCH.md §Test pattern for D-17 (performUserDeletion eviction)
  </read_first>
  <behavior>
    - D-17 Test: `performUserDeletion` on a non-guest-with-history user → User row gone, DeletedUser row present with preserved displayName, history FK rows (mmr_history, match_result_participant, etc.) still present
    - D-17 Test: `performUserDeletion` on a guest-no-history user → User row gone, NO DeletedUser row created (guest fast path preserved)
    - D-18 Test: `resolveUserLabel` on a live user id → returns `{ displayName: <live.displayName>, isDeleted: false }`
    - D-18 Test: `resolveUserLabel` on an archived user id → returns `{ displayName: <archive.displayName>, isDeleted: true }`
    - D-18 Test: `resolveUserLabel` on an unknown user id → returns `{ displayName: 'User #<N>', isDeleted: true }`
  </behavior>
  <action>
Create a NEW test file `test/backend/auth/user-deletion.test.ts`. Structure:

1. Imports from `vitest` (`describe`, `it`, `expect`, `beforeAll`, `afterAll`) and `test/shared/connection.ts` (`hasServerToken`, `createVerifiedTestHarness`, `queryPrivateTable`, and any existing helpers for seeding MMR/history rows — find them by reading the reference test files first).

2. Two `describe` blocks:

   **describe('D-17: performUserDeletion eviction')**
   - `it.skipIf(!hasServerToken())('non-guest with history is evicted to deleted_user, User row removed, history FKs intact', async () => {...})`:
     1. Create verified user via harness; capture `userId` + `displayName`
     2. Insert a synthetic history row (mmr_history or match_result_participant — use whichever pattern the reference tests already establish; if none: insert directly via admin reducer or `queryPrivateTable` INSERT)
     3. Call `admin_delete_row('User', <userId-json>)` (the existing admin reducer that seeds a UserDeletionJob)
     4. Wait ~6 seconds for the scheduled cascade to fire (5s schedule + sync margin; use the same wait pattern other scheduled-job tests in the repo use — check `ban-admin.test.ts` or `identity-gc.test.ts` for the convention)
     5. Assert `queryPrivateTable('SELECT COUNT(*) FROM user WHERE id = <userId>')` returns count 0
     6. Assert `queryPrivateTable('SELECT display_name FROM deleted_user WHERE id = <userId>')` returns 1 row, `display_name` equal to the captured original
     7. Assert the synthetic history row is still present (FK reference to the now-gone User id is intact — SpacetimeDB does not enforce FK referential integrity)

   - `it.skipIf(!hasServerToken())('guest with no history is hard-deleted, no deleted_user row created', async () => {...})`:
     1. Create a guest user via `login_as_guest` harness (no history)
     2. Call the admin delete path
     3. Wait for cascade
     4. Assert `user` row absent AND `deleted_user` row ALSO absent for that id

   **describe('D-18: resolveUserLabel three-path resolution')**

   Since `resolveUserLabel` is a pure server-side TS helper with no reducer binding, it cannot be invoked directly from an integration test. Two options — pick ONE and document the choice in-test:

   OPTION A (preferred if it fits — matches the D-18 "integration" flavor in CONTEXT.md):
   - Add a lightweight debug reducer (NO — do NOT add reducers. Skip this option.)

   OPTION B (preferred, matches RESEARCH.md §Open Question 1 spirit):
   - Write a UNIT test using Vitest's normal describe/it WITHOUT `skipIf(!hasServerToken())`. Import `resolveUserLabel` directly from `spacetimedb/src/helpers/userLabel`. Mock `ctx` as `{ db: { User: { id: { find: (n) => <fixture> } }, DeletedUser: { id: { find: (n) => <fixture> } } } }`. Assert the three branches:
     - `find(1)` in User returns a fixture → `{ displayName: 'Alice', isDeleted: false }`
     - `find(2)` in User returns null, `find(2)` in DeletedUser returns a fixture → `{ displayName: 'Bob (archived)', isDeleted: true }`
     - `find(3)` in both returns null → `{ displayName: 'User #3', isDeleted: true }`

   Use OPTION B. Mark the describe block explicitly as "Unit test (no server connection needed) — verifies resolveUserLabel branches via mocked ctx".

3. Each test is ~15-40 lines. Full file target: ~150-200 lines.

**Do NOT:**
- Add any new reducer to spacetimedb/ for test convenience. This is a test-only plan; modifying production code from a test file is out of scope. Resolve any needed test accessibility through existing reducers (admin_delete_row, queryPrivateTable) or by unit-testing the pure helper directly.
- Use `spacetime sql` CLI — tests must run in-process via the harness (RESEARCH.md Pitfall 8).
- Introduce a 10+ second sleep without documenting why — use the existing wait-helper pattern from other scheduled-job tests.
  </action>
  <verify>
    <automated>npm run test:integration -- test/backend/auth/user-deletion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls test/backend/auth/user-deletion.test.ts` exists
    - `grep -cE "describe\\('D-17" test/backend/auth/user-deletion.test.ts` returns 1
    - `grep -cE "describe\\('D-18" test/backend/auth/user-deletion.test.ts` returns 1
    - `grep -c "deleted_user" test/backend/auth/user-deletion.test.ts` returns >= 2 (SQL queries targeting the new table)
    - `grep -c "resolveUserLabel" test/backend/auth/user-deletion.test.ts` returns >= 4 (import + 3 branch assertions)
    - `grep -cE "User #3|User #\\\$\\{" test/backend/auth/user-deletion.test.ts` returns >= 1 (fallback format assertion)
    - `grep -c "isDeleted: true" test/backend/auth/user-deletion.test.ts` returns >= 2
    - `grep -c "isDeleted: false" test/backend/auth/user-deletion.test.ts` returns >= 1
    - `npm run test:integration -- test/backend/auth/user-deletion.test.ts` exits 0 with all tests passing (or skipIf-skipped when credentials unavailable; unit D-18 tests run regardless)
  </acceptance_criteria>
  <done>
    New test file covers D-17 eviction + guest fast path + D-18 resolveUserLabel three branches. The D-18 unit test runs without credentials; the D-17 integration tests run when the server token is present. Full file passes vitest with zero failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update auth-views.test.ts (D-15 + D-16) and ban-admin.test.ts (D-19)</name>
  <files>
    test/backend/auth/auth-views.test.ts,
    test/backend/auth/ban-admin.test.ts
  </files>
  <read_first>
    - test/backend/auth/auth-views.test.ts (current VIEW-01 test — the anchor for D-15/D-16)
    - test/backend/auth/ban-admin.test.ts (current ban tests — the anchor for D-19)
    - test/shared/connection.ts (`createTestHarness` for guest/anonymous connections needed in D-16)
    - .planning/phases/15.2-user-directory-view-performance/15.2-CONTEXT.md §decisions D-15, D-16, D-19
    - .planning/phases/15.2-user-directory-view-performance/15.2-RESEARCH.md §Pitfall 8 (how view tests express auth rejection)
  </read_first>
  <action>
Two files, surgical updates only.

**Edit 2a — `test/backend/auth/auth-views.test.ts`:**

1. Locate the VIEW-01 test (grep for `VIEW-01` or `view_user_directory`). Update its description and doc-comment to reflect that `view_user_directory` is now authenticated-only. Concretely:
   - If the test previously asserted "anonymous can subscribe" — flip the assertion: anonymous subscription should fail.
   - If the test was the RESEARCH.md §Pitfall 8 pattern (`expect(true).toBe(true)` with explanatory comment), update the comment to say "view_user_directory is now authenticated-only per Phase 15.2 D-06".

2. Add a NEW test for D-16 in the same describe block or a new adjacent one:
   ```typescript
   it.skipIf(!hasServerToken())('D-16: anonymous subscription on view_user_directory is rejected', async () => {
       const anon = await createTestHarness();  // guest/anonymous
       // Attempt to subscribe to view_user_directory; expect the subscription to error out
       // because spacetimedb.view(...) rejects anonymous callers at framework level.
       let rejected = false;
       try {
           await anon.conn.subscribe('SELECT * FROM view_user_directory');  // adapt to the actual harness API
           // If subscribe resolves without error, the test fails.
       } catch (err) {
           rejected = true;
       }
       expect(rejected).toBe(true);
       await anon.close();
   });
   ```
   Adapt the `subscribe(...)` call to whatever method the existing harness uses for WebSocket subs — grep other tests for the pattern first. If the harness doesn't expose a subscribe-and-await-error API cleanly, use the Pitfall-8 documented `expect(true).toBe(true)` pattern with a comment referencing that the framework-level rejection is covered in TypeScript by `spacetimedb.view(...)` at compile time.

**Edit 2b — `test/backend/auth/ban-admin.test.ts`:**

Add (or extend) two test assertions for D-19. These MAY live in the existing describe block for `admin_ban_user` or a new one.

1. After calling `admin_ban_user`, assert that a `UserDeletionJob` row exists for the affected user:
   ```typescript
   it.skipIf(!hasServerToken())('D-19: admin_ban_user schedules UserDeletionJob', async () => {
       const admin = await createVerifiedTestHarness();
       // (assume a target user with a Discord ID already exists — reuse the existing test setup)
       const targetUserId = <from setup>;
       await admin.reducers.adminBanUser({ banTypeTag: 'DiscordId', providerId: 'TEST_DID', reason: 'test' });
       // Verify the job row
       const jobs = await queryPrivateTable(`SELECT * FROM user_deletion_job WHERE user_id = ${targetUserId}`);
       expect(jobs.length).toBe(1);
   });
   ```

2. For `clientConnected` ban-on-reconnect, the full path is hard to drive from an integration test (requires racing a fresh connect against a BanRecord insert). Accept the RESEARCH.md Pitfall 8 pattern: add an `expect(true).toBe(true)` test with a block comment explaining: "D-19 clientConnected branch: the insert pattern matches admin.ts:183 and banAdmin.ts; covered structurally by grep in the PLAN-02 acceptance criteria + the D-19 admin_ban_user integration test above. Full race-condition coverage requires a dedicated reconnect harness (deferred)."

**Do NOT:**
- Delete or rewrite any passing test that is not explicitly targeted by D-15/D-16/D-19
- Add a `describe.skip` block — use `it.skipIf(!hasServerToken())` consistently
- Fix any test failures unrelated to Phase 15.2 scope — escalate instead
  </action>
  <verify>
    <automated>npm run test:integration -- test/backend/auth/auth-views.test.ts test/backend/auth/ban-admin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "D-16.*(anonymous|rejected)" test/backend/auth/auth-views.test.ts` returns >= 1
    - `grep -c "authenticated-only" test/backend/auth/auth-views.test.ts` returns >= 1 (VIEW-01 comment updated for D-15)
    - `grep -cE "D-19.*UserDeletionJob|user_deletion_job" test/backend/auth/ban-admin.test.ts` returns >= 1
    - `grep -c "user_deletion_job" test/backend/auth/ban-admin.test.ts` returns >= 1 (SQL assertion exists)
    - `npm run test:integration -- test/backend/auth/auth-views.test.ts test/backend/auth/ban-admin.test.ts` exits 0 with all tests passing or skipped via skipIf
  </acceptance_criteria>
  <done>
    VIEW-01 comment reflects authenticated-only semantics. A new D-16 test runs (or uses Pitfall-8 with documented reason). `ban-admin.test.ts` has at least one active D-19 assertion. Tests pass or are skipIf-skipped cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update docs/auth/architecture.md + docs/views/architecture.md</name>
  <files>
    docs/auth/architecture.md,
    docs/views/architecture.md
  </files>
  <read_first>
    - docs/auth/architecture.md (current content — identify the soft-delete / user deletion section)
    - docs/views/architecture.md (current content — identify the `view_user_directory` entry)
    - .claude/CLAUDE.md §Backend Feature Docs (architecture.md update requirement; contract.md deferred)
    - .planning/phases/15.2-user-directory-view-performance/15.2-CONTEXT.md §canonical_refs "Feature docs to update during verify-work"
  </read_first>
  <action>
Architecture.md updates ONLY. Contract.md changes are deferred to `/gsd-verify-work` per `.claude/CLAUDE.md`.

**Edit 3a — `docs/auth/architecture.md`:**

Find the section describing user deletion / soft-delete flow. Update to reflect:

1. **Uniform two-phase pattern across ALL three soft-delete writers** (prior to Phase 15.2, only `admin_delete_row` did both steps; post-15.2, `admin_ban_user` and `clientConnected` ban-on-reconnect also schedule `UserDeletionJob`):
   - admin_delete_row (admin.ts) — phase 1: set deletedAt; phase 2: insert UserDeletionJob
   - admin_ban_user (banAdmin.ts) — same pattern (added Phase 15.2)
   - clientConnected ban-on-reconnect (index.ts) — same pattern (added Phase 15.2)

2. **DeletedUser archive** — new row in the table/reducer reference. Describe:
   - Purpose: preserves `displayName` for history-table rendering after non-guest-with-history users are evicted
   - Schema: `{ id, displayName, isGuest, deletedAt }`, `public: false`, no audit columns, no indexes
   - Writer: `performUserDeletion` non-guest-with-history branch only (never by admin/user-facing reducer)
   - Reader: `resolveUserLabel` helper (docs cross-ref)

3. **performUserDeletion flow** — update any diagram or bullet list:
   - Guest + no history → hard-delete User row (unchanged)
   - All other cases → evict to DeletedUser + hard-delete User row (prev: in-place soft-delete update leaving a ghost)

4. **resolveUserLabel** — add a short reference describing the three-path lookup and where it is called (lobbyViews, finalizationHelpers, lobbyLifecycle kick/ban label sites).

5. Add Phase 15.2 marker in the doc's phase-history table if one exists (do NOT touch `docs/auth/contract.md` — that is post-execution work).

**Edit 3b — `docs/views/architecture.md`:**

Find the `view_user_directory` entry in the view reference table (or section). Update:

1. Calling: `anonymousView` → `view` (authenticated only)
2. Projection: unchanged (still D-16 safe subset — id, username, displayName, role, avatarCharacterName, isOnline, isGuest, hasDiscordLinked, displayedAchievementId)
3. Add note: "Post-Phase 15.2, ghost users no longer accumulate in the backing `user` table — `performUserDeletion` evicts non-guest-with-history users to `deleted_user` archive. The iter cost is bounded by live users only."

Do NOT edit `docs/views/contract.md` or `docs/user/contract.md` — contract edits are `/gsd-verify-work` scope.

**Do NOT:**
- Modify any other architecture.md file (only `auth/` and `views/` are in scope)
- Touch contract.md files (explicit rule in CLAUDE.md)
- Rewrite entire docs — edit surgically, preserving existing structure and section ordering
- Invent phase history if the doc doesn't currently track that
  </action>
  <verify>
    <automated>test -f docs/auth/architecture.md && test -f docs/views/architecture.md && grep -q "DeletedUser\|deleted_user" docs/auth/architecture.md && grep -qE "authenticated|view_user_directory" docs/views/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DeletedUser\|deleted_user" docs/auth/architecture.md` returns >= 2 (archive introduced + cross-ref)
    - `grep -c "resolveUserLabel" docs/auth/architecture.md` returns >= 1
    - `grep -cE "admin_ban_user|clientConnected" docs/auth/architecture.md` returns >= 1 (uniform two-phase pattern documented)
    - `grep -cE "view_user_directory" docs/views/architecture.md` returns >= 1
    - `grep -cE "authenticated|spacetimedb\\.view" docs/views/architecture.md` returns >= 1 near the view_user_directory section
    - No edits to `docs/auth/contract.md` or `docs/views/contract.md` — verify with `git diff --name-only docs/` excluding architecture.md files
    - Doc-file existence: `ls docs/auth/architecture.md docs/views/architecture.md` both exist after edits
  </acceptance_criteria>
  <done>
    Architecture docs reflect Phase 15.2 changes: DeletedUser archive, uniform two-phase pattern, view_user_directory authenticated-only. Contract docs untouched. No structural rewrites.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → maincloud | Uses server token; tests already run against the published module |
| doc edits → repo | Plain-text markdown writes; no executable change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-12 | Repudiation | Unverified claim that D-10 / D-06 actually work at runtime | mitigate | D-17 + D-19 integration tests exercise the scheduler end-to-end; D-16 test + Pattern 2 framework-level rejection verifies view auth flip |
| T-15.2-13 | Tampering | Test file creation/deletion outside explicit scope | accept | `.claude/CLAUDE.md` forbids test edits without explicit task — this plan IS the explicit task for D-15/D-16/D-17/D-18/D-19 |
</threat_model>

<verification>
- `npm run test:integration -- test/backend/auth/` exits 0 across all three auth test files
- `grep -c "DeletedUser\|deleted_user" docs/auth/architecture.md` returns >= 2
- No file under `docs/**/contract.md` is modified by this plan
- Phase 15.2 test coverage is complete per the Phase Requirements → Test Map in RESEARCH.md §Validation Architecture
</verification>

<success_criteria>
- All five decisions D-15/D-16/D-17/D-18/D-19 have at least one automated assertion each
- `user-deletion.test.ts` is a new file committing the D-17 + D-18 regression guards
- Architecture docs are factual for Phase 15.2 state
- Contract docs are untouched (post-execution work for `/gsd-verify-work`)
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-user-directory-view-performance/15.2-05-SUMMARY.md` covering:
- Final count of tests added per decision ID (D-15/D-16/D-17/D-18/D-19)
- Confirmation that `docs/**/contract.md` were NOT edited
- List of architecture.md sections updated in each of the two docs
- Hand-off note: "Phase 15.2 ready for `/gsd-verify-work` — doc-update checkpoint will prompt for contract.md updates tagged `Phase 15.2 execution`."
</output>
