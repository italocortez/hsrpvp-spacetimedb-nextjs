---
phase: 09-mouse-tracking-chat-and-lobby-browser
plan: 09
type: execute
wave: 7
depends_on: [09-08]
files_modified:
  - docs/match-session/architecture.md
  - docs/chat/architecture.md
  - docs/lobby/architecture.md
  - docs/match-results/architecture.md
  - docs/views/architecture.md
  - test/backend/phase3/tournament-system.test.ts
  - test/backend/phase4/bracket-generation.test.ts
  - test/backend/phase5/match-results-mmr.test.ts
  - test/backend/phase6/anonymous-play-stats.test.ts
autonomous: true
requirements:
  - MOUS-01
  - MOUS-02
  - MOUS-03
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - LBBY-01
  - LBBY-02

must_haves:
  truths:
    - "match-session/architecture.md accurately describes the current schema including auction state, split budgets, pause tracking, and all stage transitions"
    - "chat/architecture.md documents rolling window, metadata schema, anonymous enforcement, and moderation"
    - "lobby/architecture.md documents all new columns, LobbyBan, TournamentStandIn, LobbyPreset, LobbyStage expansion, browser view"
    - "match-results/architecture.md documents budget-based handicap path for Auction mode"
    - "views/architecture.md documents all per-client views including anonymous enforcement and history visibility"
    - "Existing test files compile after field renames"
  artifacts:
    - path: "docs/match-session/architecture.md"
      provides: "Full rewrite with current schema"
      contains: "isAuctionPhase"
    - path: "docs/chat/architecture.md"
      provides: "Updated chat architecture"
      contains: "rolling window"
    - path: "docs/lobby/architecture.md"
      provides: "Updated lobby architecture"
      contains: "LobbyBan"
    - path: "docs/match-results/architecture.md"
      provides: "Budget handicap documentation"
      contains: "characterBudget"
    - path: "docs/views/architecture.md"
      provides: "View documentation"
      contains: "view_my_lobby_chat"
  key_links:
    - from: "docs/match-session/architecture.md"
      to: "spacetimedb/src/tables/matchSession.ts"
      via: "documentation of current schema"
      pattern: "isAuctionPhase|nextNominatorTeam|teamBlueCharBudget"
---

<objective>
Architecture doc rewrites and test file updates for field renames.

Purpose: Per CLAUDE.md, architecture docs must be updated on every backend change. Phase 9 changes the most tables and adds the most new reducers of any phase. The match-session doc needs a FULL REWRITE (D-94). Test files referencing renamed fields (characterName→targetName, auctionBudget→characterBudget/lightconeBudget) must compile.

Output: 5 updated/created architecture docs, updated test files.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-CONTEXT.md
@.planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-08-SUMMARY.md
@spacetimedb/src/tables/matchSession.ts
@spacetimedb/src/tables/lobby.ts
@spacetimedb/src/tables/lobbyMember.ts
@spacetimedb/src/tables/chatMessage.ts
@spacetimedb/src/tables/matchSessionHistory.ts
@spacetimedb/src/tables/matchSessionStepHistory.ts
@spacetimedb/src/tables/matchParticipantHistory.ts
@spacetimedb/src/tables/lobbyBan.ts
@spacetimedb/src/tables/lobbyPreset.ts
@spacetimedb/src/tables/tournamentStandIn.ts
@spacetimedb/src/views/anonymousViews.ts
@spacetimedb/src/views/securityViews.ts
@docs/match-session/architecture.md
@docs/chat/architecture.md
@docs/lobby/architecture.md
@docs/match-results/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite match-session, chat, and lobby architecture docs</name>
  <files>
    docs/match-session/architecture.md,
    docs/chat/architecture.md,
    docs/lobby/architecture.md
  </files>
  <read_first>
    docs/match-session/architecture.md,
    docs/chat/architecture.md,
    docs/lobby/architecture.md,
    spacetimedb/src/tables/matchSession.ts,
    spacetimedb/src/tables/lobby.ts,
    spacetimedb/src/tables/lobbyMember.ts,
    spacetimedb/src/tables/chatMessage.ts,
    spacetimedb/src/tables/lobbyBan.ts,
    spacetimedb/src/tables/lobbyPreset.ts,
    spacetimedb/src/tables/tournamentStandIn.ts,
    spacetimedb/src/tables/lobbyGcJob.ts,
    spacetimedb/src/reducers/lobbyLifecycle.ts,
    spacetimedb/src/reducers/lobbySettings.ts,
    spacetimedb/src/reducers/lobbyPresets.ts,
    spacetimedb/src/reducers/draftClassic.ts,
    spacetimedb/src/reducers/draftAuction.ts,
    spacetimedb/src/reducers/draftControl.ts,
    spacetimedb/src/reducers/postDraft.ts,
    spacetimedb/src/reducers/chat.ts,
    spacetimedb/src/reducers/tournamentLobby.ts,
    spacetimedb/src/reducers/lobbyGc.ts,
    .planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-CONTEXT.md
  </read_first>
  <action>
**Per D-94: FULL REWRITE of docs/match-session/architecture.md.**

The current doc is OUTDATED and describes columns that don't exist. Rewrite from scratch with:

1. **Table Relationships** section: Diagram showing MatchSession (PK: lobbyId) linked to Lobby, MatchSessionStep (FK: lobbyId), MatchResultRecord, MatchResultParticipant. Show new columns.

2. **MatchSession Table** section: Complete column listing including all auction state (isAuctionPhase, nextNominatorTeam, blueCharactersWon, redCharactersWon, currentNomination, currentBidAmount, currentBidTeam), split budgets (teamBlueCharBudget, teamRedCharBudget, teamBlueLcBudget, teamRedLcBudget), pause tracking (pausesUsedBlue, pausesUsedRed). PKs, indexes.

3. **MatchSessionStep Table** section: Column listing with all ActionType variants (Pick, Ban, Nominate, Bid, AuctionSold, Pause, Undo, EquipLightcone, ArrangeLineup, ConfirmLineup).

4. **Stage Transitions** section: Waiting → Drafting → Equipping → Scoring → Finished. What happens at each transition.

5. **Draft Modes** section: Classic (fixed sequence from BanMode) and Auction (ban-only sequence + dynamic auction). Describe steal-skip logic, budget enforcement, EMPTY CHARACTER.

6. **Reducer Reference Table**: All draft reducers (start_draft, pick_character, ban_character, nominate_character, place_bid, pass_bid, timer_expiry_classic, timer_expiry_auction, undo_last_step, pause_draft, resume_draft, equip_lightcone, arrange_lineup, confirm_lineup, advance_stage) with permissions, stage requirements, description.

7. **Timer and Pause Model**: Turn timer, reserve bank, pause limits (3/team + unlimited referee), auto-pause on disconnect.

8. **Data Patterns**: Composite PK update pattern, turn validation, captain-only actions.

**Per D-95: Update docs/chat/architecture.md.**

Add/update sections for:
- Rolling window (50 messages, oldest deleted on insert) per D-13
- Metadata JSON schema: `{ type: "text" | "reply" | "emoji_only", replyToMessageId?: number }` per D-14
- Unicode emoji storage (stored as-is) and custom shortcode convention (`:shortcode:` deferred to v1) per D-15
- Anonymous enforcement (real userId stored, views anonymize) per D-18
- Moderation: host/referee/admin can delete messages per D-26
- System messages for join/leave/stage changes per D-11
- Cleanup: cascade deleted with lobby (CHAT-03)

**Per D-96: Update docs/lobby/architecture.md.**

Add/update sections for:
- All new Lobby columns (matchType, currentPlayerCount, characterBudget, lightconeBudget, minimumBidRaise, allowMirrorPicks, autoRandomPick, referee power bools, allowPlayerPause)
- LobbyBan table (composite PK, hard-deleted with lobby)
- TournamentStandIn table (composite PK, per-bracket-match)
- LobbyPreset table (autoInc PK, permission hierarchy)
- LobbyStage expansion: Waiting, Drafting, Equipping, Scoring, Finished
- Browser view: projected columns, finished exclusion, tournament/cost set name lookup
- Lobby lifecycle: create, join, leave, close, kick, ban
- Team assignment: join as Spectator, free movement during Waiting, confirm/unconfirm ready
- One-lobby-per-user enforcement
- Guest restrictions (no Ranked, forced ClosedNoRating)
- Tournament lobby: settings inheritance, participant validation, stand-in system
- Scheduled GC: Waiting/Finished idle > 30 min cleanup
- Reducer reference table with all lobby reducers and permissions
  </action>
  <verify>
    <automated>ls docs/match-session/architecture.md docs/chat/architecture.md docs/lobby/architecture.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - docs/match-session/architecture.md is rewritten and contains `isAuctionPhase`, `nextNominatorTeam`, `teamBlueCharBudget`, `teamBlueLcBudget`, `pausesUsedBlue`, `EquipLightcone`, `ArrangeLineup`, `ConfirmLineup`, stage transitions (Waiting→Drafting→Equipping→Scoring→Finished)
    - docs/match-session/architecture.md does NOT contain old column names (`currentPhase`, `currentTeam`, `currentStep`, `isPaused`, `isCompleted`, `rosterBlue`, `rosterRed`)
    - docs/chat/architecture.md contains `rolling window`, `50 messages`, metadata schema `{ type: "text" | "reply" | "emoji_only"`, `anonymousLabel`
    - docs/lobby/architecture.md contains `LobbyBan`, `TournamentStandIn`, `LobbyPreset`, `characterBudget`, `lightconeBudget`, `refereeCanUndo`, `Equipping`, `Scoring`, `currentPlayerCount`
  </acceptance_criteria>
  <done>3 architecture docs rewritten/updated per D-94/D-95/D-96 with complete schema documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Update match-results/views docs and fix test file references</name>
  <files>
    docs/match-results/architecture.md,
    docs/views/architecture.md,
    test/backend/phase5/match-results-mmr.test.ts,
    test/backend/phase6/anonymous-play-stats.test.ts
  </files>
  <read_first>
    docs/match-results/architecture.md,
    docs/views/architecture.md,
    spacetimedb/src/views/anonymousViews.ts,
    spacetimedb/src/views/securityViews.ts,
    spacetimedb/src/helpers/finalizationHelpers.ts
  </read_first>
  <action>
**Per D-97: Update docs/match-results/architecture.md.**

Add a section documenting the budget-based handicap path for Auction mode:
- **Classic mode handicap**: Cost table sums (existing). Character costs looked up from HsrCharacterCost. Difference in total cost → handicap adjustment.
- **Auction mode handicap per D-51/D-52**: Budget spent difference between teams. Team with more unspent budget gets the handicap bonus. Formula unchanged: MoC/AA = -0.1875 cycles per cost point difference. Apocalyptic Shadow = +30 score per point difference. But the input source differs (Classic: cost table sums, Auction: budget spent tracked via teamBlueSpent/teamRedSpent).
- Document `teamBlueSpent`, `teamRedSpent`, `handicapApplied` columns on MatchSessionHistory.

**Per D-99: Create or update docs/views/architecture.md.**

Document all per-client views:
- `view_lobby_browser` (anonymousView): projected columns, finished exclusion, tournament/cost set name cross-lookup
- `view_my_lobbies`: lobbies where caller is a member
- `view_my_lobby_chat`: ChatMessage with anonymous enforcement per D-92
- `view_my_lobby_members`: LobbyMember with anonymous enforcement per D-92
- `view_my_match_steps`: MatchSessionStep with anonymous enforcement per D-92
- `view_my_match_participants`: MatchResultParticipant with anonymous enforcement per D-92
- `view_match_history`: MatchSessionHistory with isPubliclyVisible filter + participant check per D-93
- All existing security views (roster visibility, cost set drafts, etc.)

For each view document: name, type (view vs anonymousView), row type, filtering logic, anonymous behavior.

**Per D-98: Fix test file references.**

Search all test files in test/backend/ for references to:
1. `characterName` on MatchSessionStepHistory → rename to `targetName`
2. `auctionBudget` on LobbyConfigSnapshot or Lobby → replace with `characterBudget` / `lightconeBudget`
3. `teamBlueBudget` / `teamRedBudget` on MatchSession → replace with `teamBlueCharBudget` / `teamRedCharBudget`
4. `BanMode.Two` or `{ tag: 'Two' }` → remove or replace with `BanMode.Four`

For each test file, make the minimal rename to fix the reference. Do NOT change test logic or assertions — just update field names so the files compile. Any test failures that reveal broken logic should be reported, not auto-fixed.

**Publish module to verify everything compiles and run test count check.**
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit && spacetime publish hsrpvp-spacetimedb-nextjs-test1 2>&1 | tail -5 && spacetime generate --lang typescript --out-dir ../src/module_bindings --project-path . 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - docs/match-results/architecture.md contains `characterBudget`, `lightconeBudget`, `teamBlueSpent`, Auction handicap calculation description
    - docs/views/architecture.md exists and contains `view_my_lobby_chat`, `view_my_lobby_members`, `view_my_match_steps`, `view_my_match_participants`, `view_match_history`, `isPubliclyVisible`
    - No test file in test/backend/ references `characterName` on step history (should be `targetName`)
    - No test file references `auctionBudget` (should be `characterBudget`/`lightconeBudget`)
    - `npx tsc --noEmit` passes in spacetimedb/
    - Module publishes and bindings generate successfully
  </acceptance_criteria>
  <done>Match results doc updated with budget handicap path (D-97). Views doc created (D-99). Test file field references fixed (D-98). Final publish and binding generation confirms full Phase 9 completion.</done>
</task>

</tasks>

<verification>
- All 5 architecture docs updated/created per D-94 through D-99
- Module publishes without error
- Test files compile with updated field names
- Bindings reflect all Phase 9 changes
</verification>

<success_criteria>
- match-session/architecture.md is a complete, accurate rewrite (D-94)
- chat/architecture.md documents rolling window, metadata, anonymous enforcement (D-95)
- lobby/architecture.md documents all new tables, columns, reducers (D-96)
- match-results/architecture.md documents budget-based handicap for Auction (D-97)
- views/architecture.md documents all per-client views (D-99)
- Test files compile after field renames (D-98)
- Phase 9 complete: all 8 requirement IDs fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/09-mouse-tracking-chat-and-lobby-browser/09-09-SUMMARY.md`
</output>
