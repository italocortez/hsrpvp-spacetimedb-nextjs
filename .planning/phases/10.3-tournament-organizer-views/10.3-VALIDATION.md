---
phase: 10.3
slug: tournament-organizer-views
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-04
---

# Phase 10.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing) |
| **Config file** | `spacetimedb/vitest.config.ts` |
| **Quick run command** | `cd spacetimedb && npx vitest run --reporter=verbose` |
| **Full suite command** | `cd spacetimedb && npx vitest run --reporter=verbose` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd spacetimedb && npx vitest run --reporter=verbose`
- **After every plan wave:** Run `cd spacetimedb && npx vitest run --reporter=verbose`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.3-01-01 | 01 | 1 | TO-VIEW-01 | publish + sql | `spacetime publish` | N/A | ⬜ pending |
| 10.3-01-02 | 01 | 1 | TO-VIEW-02 | publish + sql | `spacetime publish` | N/A | ⬜ pending |
| 10.3-01-03 | 01 | 1 | TO-VIEW-03 | publish + sql | `spacetime publish` | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. Views are validated by successful module publish and UAT SQL queries.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Views return correct rows for TO identity | TO-VIEW-01/02/03 | Views require published module + authenticated identity | `spacetime sql` with identity context during UAT |
| Views use btree indexes (no iter()) | TO-VIEW-03 | Code review — no runtime assertion for index usage | Grep for `.iter()` in view code; verify all lookups use `.filter()` or `.find()` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
