---
phase: 15.4-cost-table-draftmode-restructure
fixed_at: 2026-04-14T00:00:00Z
review_path: .planning/phases/15.4-cost-table-draftmode-restructure/15.4-REVIEW.md
iteration: 1
findings_in_scope: 4
fixed: 3
skipped: 1
status: all_fixed
---

# Phase 15.4: Code Review Fix Report

**Fixed at:** 2026-04-14
**Source review:** `.planning/phases/15.4-cost-table-draftmode-restructure/15.4-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope (Warning severity): 4
- Fixed: 3 (WR-01, WR-02, WR-04)
- Skipped: 1 (WR-03 — explicitly DISMISSED upstream, not a fix target)

All three accepted warnings were applied per explicit routing. Status `all_fixed` because WR-03 was not a fixable finding (reviewer-dismissed, user-confirmed), so the fixable scope was 3/3.

## Fixed Issues

### WR-01: Deterministic gameMode collapse in getLightconeCost

**Files modified:** `components/features/game-data/components/DataHelpers.ts`, `components/features/costs/hooks/useLightconeCostTable.ts`
**Commit:** `0e3090f`
**Applied fix:** Option (b) from REVIEW.md — deterministic collapse. Both loops now skip rows whose `gameMode.tag !== 'MemoryOfChaos'` before dispatching on `draftMode.tag`. Replaces the previous nondeterministic "last-row-wins across gameMode" behavior with a stable canonical-mode pick. LightconeCost retains its 2-key `{Classic, Auction}` shape (Phase 17 will revisit the gameMode axis).

### WR-02: draftMode=Classic filter in timer_expiry autoRandomPick

**Files modified:** `spacetimedb/src/reducers/draftClassic.ts`
**Commit:** `b8f4156`
**Applied fix:** Both `timer_expiry_classic` autoRandomPick branches (pick-expiry L693-708 and ban-expiry L754-767) now filter `r.draftMode.tag === 'Classic'` in addition to `gameMode.tag`. Matches the already-correct filter in the normal `pick_character` path at L361-367. Prevents Auction-only character rows from leaking into a Classic-lobby auto-pool (which would have been auto-picked at `costPaid=0`).

### WR-04: Stamp gameNumber on postDraft step inserts

**Files modified:** `spacetimedb/src/reducers/postDraft.ts`
**Commit:** `31125e1`
**Applied fix:** Added `gameNumber: session.currentGameNumber` to all three `MatchSessionStep.insert` calls in `postDraft.ts`:
- `equip_lightcone` (L90-105) — session already loaded at L44-45.
- `arrange_lineup` (L180-196) — added `session` lookup + `SenderError('Match session not found.')` guard at L159-161.
- `confirm_lineup` (L244-260) — added `session` lookup + guard at L234-236.

Matches the pattern used by all draft-phase inserts in `draftClassic.ts` / `draftAuction.ts`. Fixes archival integrity for `MatchSessionStepHistory` (PK includes `gameNumber`) in bestOf>1 series. Not a runtime draft-logic bug — no in-game reducer filters postDraft steps by gameNumber.

## Skipped Issues

### WR-03: getAuctionWonCharacters and getBannedCharacters iterate all-game steps

**File:** `spacetimedb/src/reducers/draftAuction.ts:12-39`
**Reason:** Explicitly DISMISSED in REVIEW.md (annotated "Do not fix" by user, verified 2026-04-15). `seriesManagement.advance_to_next_game:79-82` deletes all `MatchSessionStep` rows for the lobby between games, and D-42 defines auction exclusivity as pool-wide. Adding a gameNumber filter would be a no-op at best, regression at worst.
**Original issue:** Reviewer flagged that auction-won and banned-character aggregations lack a `gameNumber` filter. User verified the filter is unnecessary given the between-game wipe + D-42 pool-wide exclusivity.

## Verification

**Frontend `npx tsc --noEmit`** (repo root):
- Zero errors reference any of the four modified files (`DataHelpers.ts`, `useLightconeCostTable.ts`, `draftClassic.ts`, `postDraft.ts`).
- Pre-existing errors in unrelated `test/backend/**/*.test.ts` files (missing `requireOwnership` property on Tournament fixtures, `lobby possibly undefined` in `draft-control.test.ts`) existed before these fixes — unchanged by this pass.

**Backend `npx tsc --noEmit --project spacetimedb/tsconfig.json`:**
- Clean. Zero diagnostics. Matches pre-fix state.

**Targeted tests (not run):**
The three target test files (`cost-set-pk.test.ts`, `cost-set-lifecycle.test.ts`, `post-draft.test.ts`) are integration tests under `test/vitest.integration.config.ts`, which triggers a full maincloud clear+reseed via `test/global-setup.ts`. An agent-initiated integration run would reset the live test database as a side effect. Recommend the user trigger `npm run test:integration -- test/backend/reducers/admin/cost-set-pk.test.ts test/backend/cost-sets/cost-set-lifecycle.test.ts test/backend/match-session/post-draft.test.ts` manually if end-to-end confirmation is desired. Unit-level type-check confirms the three fixes did not introduce new type errors.

## Commit History

```
31125e1 fix(15.4): stamp gameNumber on postDraft step inserts (WR-04)
b8f4156 fix(15.4): filter draftMode=Classic in timer_expiry autoRandomPick (WR-02)
0e3090f fix(15.4): deterministic gameMode collapse in getLightconeCost (WR-01)
```

All three commits are atomic (one finding per commit, all affected files in the same commit).

---

_Fixed: 2026-04-14_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
