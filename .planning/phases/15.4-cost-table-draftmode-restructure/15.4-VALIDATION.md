---
phase: 15.4
slug: cost-table-draftmode-restructure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-14
---

# Phase 15.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (integration tests against live SpacetimeDB) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npm run test:phase -- 15.4` |
| **Full suite command** | `npm run test:integration` |
| **Estimated runtime** | ~30 seconds (phase-scoped) / ~3 minutes (full suite) |

---

## Sampling Rate

- **After every task commit:** `npm run test:phase -- 15.4` (when phase tests exist)
- **After every plan wave:** `npm run build && npm run test:integration`
- **Before `/gsd-verify-work`:** Full suite must be green + module published + post-publish bootstrap clean
- **Max feedback latency:** ~30 seconds (phase) / 3 minutes (full)

---

## Per-Task Verification Map

*To be populated by planner. Derived from PLAN.md tasks once generated.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | — | — | — | — | — | — | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `spacetime publish --clear-database` then `npx tsx scripts/post-publish.ts` — migration requires clean state (new PK tuple; old rows cannot be mapped 1:1 to draft-mode-partitioned shape)
- [ ] Regenerate bindings: `spacetime generate --lang typescript --out-dir module_bindings`
- [ ] Existing `test/integration/` suite covers cross-feature surface; no new framework install needed

*Wave 0 is data-destructive — coordinates with project rule that `.env.local` is only touched by `post-publish.ts`.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Admin bulk upsert UX in `/admin` | — | UI flow requires browser; reducer call itself is covered by integration test | Load `/admin`, upsert a cost row with `draftMode: Classic`, verify row appears in SQL snapshot |
| Draft-mode column display in tableColumns | D-27 | Visual verification of column header rendering | Open admin table explorer for `HsrCharacterCost`, confirm `draftMode` column renders |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers bindings regen + clean database
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for phase-scoped runs
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
