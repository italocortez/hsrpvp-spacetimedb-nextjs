---
phase: 15.4
slug: cost-table-draftmode-restructure
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-14
---

# Phase 15.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (integration tests against live SpacetimeDB) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npm run test:phase -- 15.4` |
| **Full suite command** | `npm run test:integration` |
| **Estimated runtime** | ~30 seconds (phase-scoped) / ~3 minutes (full suite) |

---

## Sampling Rate

- **After every task commit:** `npm run test:phase -- 15.4` (when phase tests exist)
- **After every plan wave:** `npm run build && npm run test:integration`
- **Before `/gsd-verify-work`:** Full suite must be green + module published + post-publish bootstrap clean
- **Max feedback latency:** ~30 seconds (phase) / 3 minutes (full)

---

## Per-Task Verification Map

Each row is one task from the four plans. Automated commands come directly from each task's `<automated>` block in the PLAN.md files. Status is set during execution; planning only pre-populates the row. **Nyquist rule:** every row has an automated command — no gaps.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.4-01-01 | 01 | 1 | REQ-154-01, REQ-154-02, REQ-154-03 | — | Schema: six cost tables carry `draftMode` + `costs` (char/lc); HsrSynergyCost retains autoInc id + new `by_tuple` btree; pkTest sources removed | Static | `cd spacetimedb && npx tsc --noEmit 2>&1 \| grep -E "(hsrCharacterCost\|hsrLightconeCost\|hsrSynergyCost\|costSetDraftCharacter\|costSetDraftLightcone\|costSetDraftSynergy\|pkTest)\.ts" \| head -20` (expect zero lines for table files) | ✅ W0 (pre-existing) | ⬜ pending |
| 15.4-01-02 | 01 | 1 | REQ-154-05, REQ-154-06, REQ-154-07, REQ-154-08 | — | All cost reducers (admin_bulk_upsert, edit_draft_*, create_cost_set, publish_cost_set, draft readers, postDraft LC lookup) operate on `(name, gameMode, draftMode, costSetId)` tuple; A2 decision recorded in SUMMARY | Static | `cd spacetimedb && npx tsc --noEmit 2>&1; echo exit:$?` + `grep -cE "classicCosts\|auctionBaseBid" spacetimedb/src/reducers/*.ts spacetimedb/src/tables/*.ts` (expect exit 0 + zero count) | ✅ W0 (pre-existing) | ⬜ pending |
| 15.4-01-03a | 01 | 1 | REQ-154-04 | — | `spacetime publish --delete-data=on-conflict` lands on maincloud; `hsr_character_cost` exists and is empty post-wipe | Runtime | `spacetime server list \| grep -E '\*\*\*.*maincloud'` + `spacetime sql <db> "SELECT 1"` + `spacetime sql <db> "SELECT count(*) FROM hsr_character_cost"` (expect 0) | ✅ W0 (environment) | ⬜ pending |
| 15.4-01-03b | 01 | 1 | REQ-154-10 | — | Both `module_bindings/` dirs regenerated cleanly; `draftMode` + `costsJson` present; old `classicCosts` / `auctionBaseBid` absent; backend tsc exits 0 | Static | `grep -lE "classicCosts\|auctionBaseBid\|classicCostsJson\|auctionBaseBidJson" src/module_bindings/*.ts spacetimedb/src/module_bindings/*.ts` + `grep -l "draftMode" src/module_bindings/types.ts spacetimedb/src/module_bindings/types.ts` + `cd spacetimedb && npx tsc --noEmit` | ✅ W0 (bindings regen via spacetime generate) | ⬜ pending |
| 15.4-01-03c | 01 | 1 | REQ-154-04 | — | `docs/cost-sets/architecture.md` + `docs/cost-tables/architecture.md` updated with draftMode schema, reducer signatures, Phase 15.4 History entries | Static | `grep -qE "draftMode.*DraftMode\|draftMode: DraftMode" docs/cost-sets/architecture.md` + `grep -qE "Phase 15\.4 execution" docs/cost-sets/architecture.md` + `grep -qE "draftMode" docs/cost-tables/architecture.md` + `grep -qE "Phase 15\.4 execution" docs/cost-tables/architecture.md` | ✅ (pre-existing docs) | ⬜ pending |
| 15.4-02-01 | 02 | 2 | REQ-154-09 | — | Admin payload contract surface (`UPSERT_TABLE_COLUMNS` / `TABLE_ENUM_COLUMNS` / `BulkUpsert.tsx` templates / `TableExplorer.tsx`) updated to `costs` + `draftMode` per D-27 | Static | `grep -rnE "classicCosts\|auctionBaseBid" components/features/types/ components/features/admin-view/` (expect zero) + `grep -nE "draftMode" components/features/types/tableColumns.ts` + `npx tsc --noEmit 2>&1 \| grep -E "tableColumns\|BulkUpsert\|TableExplorer" \| head -20` (expect zero TS errors in these three files) | ✅ (pre-existing components) | ⬜ pending |
| 15.4-02-02 | 02 | 2 | REQ-154-09 | — | Fix-to-compile: DataHelpers, cost hooks, CharacterCostTable, SynergyDisplay (Classic-only per D-31), CostBreakdownChart, GameDataProvider all consume `costs` + `draftMode`; `npm run build` exits 0 | Build | `grep -rnE "classicCosts\|auctionBaseBid" components/` (expect zero) + `npm run build 2>&1 \| tail -50; echo exit:$?` (expect exit 0) | ✅ (pre-existing components) | ⬜ pending |
| 15.4-03-01 | 03 | 2 | REQ-154-11 | — | Production seed (`scripts/seed-data.ts`) fans out one row per present sub-block for chars / lcs / pairings; no zero-padding; `draftMode: 'Classic' \| 'Auction'` threaded | Static | `grep -nE "ZERO_EIDOLON\|ZERO_SUPERPOSITION" scripts/seed-data.ts` (expect absent) + `grep -nE "classicCosts\|auctionBaseBid" scripts/seed-data.ts` (expect absent) + `grep -cE "draftMode: 'Classic'\|draftMode: 'Auction'" scripts/seed-data.ts` (expect ≥6) | ✅ (pre-existing seed) | ⬜ pending |
| 15.4-03-02 | 03 | 2 | REQ-154-11 | — | `pairing_template.json` carries sibling-block `{ classic, auction }` shape; README transform tables updated; harness seed mirrors production | Static | `node -e "JSON.parse(require('fs').readFileSync('test/data-templates/pairing_template.json','utf8'))"` + `grep -cE "classic.*modifier" test/data-templates/pairing_template.json` (expect ≥3) + `grep -nE "ZERO_EIDOLON\|ZERO_SUPERPOSITION\|classicCosts\|auctionBaseBid" test/shared/seed-data.ts` (expect absent) + `grep -ciE "draftMode\|sub-block\|pairing" test/data-templates/README.md` (expect ≥3) | ✅ (pre-existing templates + README) | ⬜ pending |
| 15.4-03-03 | 03 | 2 | REQ-154-11 | — | User confirms local `test/data/*.json` re-migration + `post-publish.ts` bootstrap against empty+new-schema maincloud; no gitignored paths staged | Gate (human-action + automated gitignore check) | `git check-ignore test/data/*.json >/dev/null && echo OK \|\| exit 1` + `if git status --short \| grep -qE "^[ MA?]+ (test/data/\|tmp/)"; then exit 1; fi` (both BLOCKING on failure) | ✅ (gitignore entries pre-existing) | ⬜ pending |
| 15.4-04-01 | 04 | 3 | REQ-154-12 | — | Admin reducer regression trio (cost-set-pk, partial-update, seed-cost-extraction) green against new schema; draftMode-based filter + Pitfall 1 + D-18 scenarios covered | Integration | `npm run test -- --run test/backend/reducers/admin/cost-set-pk.test.ts test/backend/reducers/admin/partial-update.test.ts test/backend/reducers/admin/seed-cost-extraction.test.ts 2>&1 \| tail -30; echo exit:$?` | ⚠️ **MISSING — rewritten in-task** (files exist pre-phase but rewrite happens in this task per D-24) | ⬜ pending |
| 15.4-04-02 | 04 | 3 | REQ-154-12 | — | Lifecycle + round-trip + post-draft trio green; publish_cost_set produces 2 rows when draft has Classic+Auction (Pitfall 7 regression); LC cost lookup reads `costs` | Integration | `npm run test -- --run test/backend/cost-sets/cost-set-lifecycle.test.ts test/backend/seed/round-trip.test.ts test/backend/match-session/post-draft.test.ts 2>&1 \| tail -30; echo exit:$?` | ⚠️ **MISSING — rewritten in-task** | ⬜ pending |
| 15.4-04-03 | 04 | 3 | REQ-154-12 | — | NEW file `synergy-auction-round-trip.test.ts` covers first-ever D-25a synergy auction insertion/coexistence/update/publish round-trip; full `npm run test:integration` green; binding accessor casing verified + recorded in SUMMARY | Integration | `grep -E "(byTuple\|by_tuple\|adminBulkUpsert\|admin_bulk_upsert\|editDraftSynergyCost\|edit_draft_synergy_cost)" src/module_bindings/*.ts \| head -20` (expect ≥1 hit per accessor family) + `npm run test -- --run test/backend/cost-sets/synergy-auction-round-trip.test.ts 2>&1 \| tail -30` + `npm run test:integration 2>&1 \| tail -50; echo exit:$?` (expect 0) | ⚠️ **MISSING — new file created in task** | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Tasks 15.4-04-01/02/03 are flagged `⚠️ MISSING — rewritten in-task` because the test files either (a) exist today in old shape and are rewritten in-place per D-24 (the 6 existing files) or (b) are new (the synergy-auction-round-trip file). This is not a Wave-0-scaffold gap — the rewrites/creation ARE the task work per CONTEXT.md D-24. No separate Wave 0 test-scaffold task needed.*

---

## Wave 0 Requirements

- [x] `spacetime publish --clear-database` equivalent (`--delete-data=on-conflict`) then DEFERRED `post-publish.ts` (Plan 03 Task 3) — satisfied by Plan 01 Task 3a. Migration requires clean state (new PK tuple; old rows cannot be mapped 1:1 to draft-mode-partitioned shape).
- [x] Regenerate bindings: `spacetime generate --lang typescript --out-dir module_bindings` — satisfied by Plan 01 Task 3b (both `spacetimedb/src/module_bindings/` and `src/module_bindings/`).
- [x] Existing `test/integration/` suite covers cross-feature surface; no new framework install needed — pre-existing infrastructure.

*Wave 0 checklist items are TICKED to indicate the plan tasks that WILL satisfy each requirement during execution. `wave_0_complete` in frontmatter stays `false` until Wave 1 runtime actually executes — it flips to `true` when the maincloud publish + regen land during execution.*

*Wave 0 is data-destructive — coordinates with project rule that `.env.local` is only touched by `post-publish.ts`.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Admin bulk upsert UX in `/admin` | REQ-154-09 | UI flow requires browser; reducer call itself is covered by integration test | Load `/admin`, upsert a cost row with `draftMode: Classic`, verify row appears in SQL snapshot |
| Draft-mode column display in tableColumns | D-27 | Visual verification of column header rendering | Open admin table explorer for `HsrCharacterCost`, confirm `draftMode` column renders |
| Local `test/data/*.json` re-migration | REQ-154-11 | Files are gitignored per CLAUDE.md — Claude cannot edit them | See Plan 03 Task 3 `<how-to-verify>`: migrate `pairing_table.json` to sibling-block shape (chars/lcs unchanged), run `npx tsx scripts/post-publish.ts`, spot-check via `spacetime sql` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — every row in the Per-Task Verification Map has an automated command.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — every task verifies within its own commit.
- [x] Wave 0 covers bindings regen + clean database — Plan 01 Task 3a (publish) + Task 3b (regen).
- [x] No watch-mode flags — all commands single-shot.
- [x] Feedback latency < 30s for phase-scoped runs — `npm run test:phase -- 15.4` targets ~30s.
- [x] `nyquist_compliant: true` set in frontmatter — flipped from `false` in this revision after populating the map.

**Approval:** planner self-approval pending checker re-review (revision 1, 2026-04-14).
