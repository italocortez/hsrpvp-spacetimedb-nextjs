---
phase: 15.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/tables/hsrCharacterCost.ts
  - spacetimedb/src/tables/hsrLightconeCost.ts
  - spacetimedb/src/tables/hsrSynergyCost.ts
  - spacetimedb/src/tables/costSetDraftCharacter.ts
  - spacetimedb/src/tables/costSetDraftLightcone.ts
  - spacetimedb/src/tables/costSetDraftSynergy.ts
  - spacetimedb/src/tables/pkTest.ts
  - spacetimedb/src/reducers/pkTest.ts
  - spacetimedb/src/reducers/admin.ts
  - spacetimedb/src/reducers/costSetManagement.ts
  - spacetimedb/src/reducers/draftClassic.ts
  - spacetimedb/src/reducers/draftAuction.ts
  - spacetimedb/src/reducers/postDraft.ts
  - spacetimedb/src/module_bindings/index.ts
  - spacetimedb/src/module_bindings/types.ts
  - spacetimedb/src/module_bindings/hsr_character_cost_table.ts
  - spacetimedb/src/module_bindings/hsr_lightcone_cost_table.ts
  - spacetimedb/src/module_bindings/hsr_synergy_cost_table.ts
  - spacetimedb/src/module_bindings/cost_set_draft_character_table.ts
  - spacetimedb/src/module_bindings/cost_set_draft_lightcone_table.ts
  - spacetimedb/src/module_bindings/cost_set_draft_synergy_table.ts
  - spacetimedb/src/module_bindings/edit_draft_character_cost_reducer.ts
  - spacetimedb/src/module_bindings/edit_draft_lightcone_cost_reducer.ts
  - spacetimedb/src/module_bindings/edit_draft_synergy_cost_reducer.ts
  - src/module_bindings/index.ts
  - src/module_bindings/types.ts
  - src/module_bindings/hsr_character_cost_table.ts
  - src/module_bindings/hsr_lightcone_cost_table.ts
  - src/module_bindings/hsr_synergy_cost_table.ts
  - src/module_bindings/cost_set_draft_character_table.ts
  - src/module_bindings/cost_set_draft_lightcone_table.ts
  - src/module_bindings/cost_set_draft_synergy_table.ts
  - src/module_bindings/edit_draft_character_cost_reducer.ts
  - src/module_bindings/edit_draft_lightcone_cost_reducer.ts
  - src/module_bindings/edit_draft_synergy_cost_reducer.ts
  - docs/cost-sets/architecture.md
  - docs/cost-tables/architecture.md
autonomous: true
requirements:
  - REQ-154-01
  - REQ-154-02
  - REQ-154-03
  - REQ-154-04
  - REQ-154-05
  - REQ-154-06
  - REQ-154-07
  - REQ-154-08
  - REQ-154-10

must_haves:
  truths:
    - "Backend module compiles and publishes to maincloud after a single `spacetime publish --delete-data=on-conflict` (data-destructive cutover per D-22)."
    - "Every cost table (HsrCharacterCost / HsrLightconeCost / HsrSynergyCost / CostSetDraftCharacter / CostSetDraftLightcone / CostSetDraftSynergy) exposes a `draftMode: DraftMode` column and either a `costs` struct (char/lc) or unchanged `costModifier: f32` (synergy)."
    - "All cost-related reducers (admin_bulk_upsert / edit_draft_character_cost / edit_draft_lightcone_cost / edit_draft_synergy_cost / create_cost_set / publish_cost_set / draftClassic.getCharacterBaseCost / draftAuction.getCharacterBaseCost / postDraft.ts LC lookup) operate on the new shape with draftMode-included tuple match."
    - "Both module_bindings directories (server-side and client-side) are regenerated and committed; old `classicCosts` / `auctionBaseBid` accessors are absent."
    - "PkTest probe artifacts (table + reducer source files + maincloud table) are removed via the same publish (D-23)."
    - "Architecture docs (docs/cost-sets/architecture.md, docs/cost-tables/architecture.md) describe the new schema, indexes, and row-absence semantic before the plan completes."
  artifacts:
    - path: "spacetimedb/src/tables/hsrCharacterCost.ts"
      provides: "Character cost row schema with `costs` + `draftMode` + extended PK tuple + 4-col btree"
      contains: "draftMode: DraftMode"
    - path: "spacetimedb/src/tables/hsrLightconeCost.ts"
      provides: "Lightcone cost row schema mirroring char shape"
      contains: "draftMode: DraftMode"
    - path: "spacetimedb/src/tables/hsrSynergyCost.ts"
      provides: "Synergy row schema with autoInc id (kept) + draftMode + extended source_mode + new by_tuple btree"
      contains: "by_tuple"
    - path: "spacetimedb/src/tables/costSetDraftSynergy.ts"
      provides: "Draft synergy schema with composite-PK tuple extended by draftMode + by_tuple btree"
      contains: "draftMode: DraftMode"
    - path: "spacetimedb/src/reducers/admin.ts"
      provides: "admin_bulk_upsert HsrCharacterCost / HsrLightconeCost / HsrSynergyCost cases rewritten to new shape + btree filter (D-19)"
      contains: "by_character_mode_and_set.filter"
    - path: "spacetimedb/src/reducers/costSetManagement.ts"
      provides: "edit_draft_*_cost reducers with new signatures (D-16/D-17/D-18); create_cost_set + publish_cost_set carry draftMode (D-20/D-21)"
      contains: "draftModeTag"
    - path: "spacetimedb/src/reducers/draftAuction.ts"
      provides: "getCharacterBaseCost filters draftMode==='Auction' and reads costRow.costs (D-11)"
      contains: "draftMode.tag === 'Auction'"
    - path: "spacetimedb/src/module_bindings/types.ts"
      provides: "Regenerated TypeScript bindings reflecting new schema"
    - path: "src/module_bindings/types.ts"
      provides: "Client-side regenerated bindings mirror"
    - path: "docs/cost-sets/architecture.md"
      provides: "Updated schema + reducer reference + row-absence semantic per D-28"
    - path: "docs/cost-tables/architecture.md"
      provides: "Updated read-path documentation per D-28"
  key_links:
    - from: "spacetimedb/src/reducers/admin.ts"
      to: "ctx.db.HsrCharacterCost.by_character_mode_and_set.filter([name, gameMode, draftMode, costSetId])"
      via: "extended 4-col btree filter for tuple match"
      pattern: "by_character_mode_and_set\\.filter"
    - from: "spacetimedb/src/reducers/admin.ts"
      to: "ctx.db.HsrSynergyCost.by_tuple.filter([source, target, gameMode, draftMode, costSetId])"
      via: "new 5-col btree on synergy"
      pattern: "by_tuple\\.filter"
    - from: "spacetimedb/src/reducers/draftAuction.ts:getCharacterBaseCost"
      to: "costRow.costs[eidolonKey]"
      via: "single struct column read after draftMode filter"
      pattern: "costRow\\.costs"
    - from: "spacetimedb/src/reducers/postDraft.ts"
      to: "ctx.db.HsrLightconeCost.by_lightcone_mode_and_set.filter([name, gameMode, CLASSIC_DRAFT_MODE, costSetId])"
      via: "extended 4-positional btree filter (Pitfall 6 fix)"
      pattern: "CLASSIC_DRAFT_MODE"
    - from: "spacetimedb/src/reducers/costSetManagement.ts:publish_cost_set"
      to: "liveRows.find(r => ... && r.draftMode.tag === draft.draftMode.tag)"
      via: "extended existingLive predicate (Pitfall 7 fix)"
      pattern: "draftMode\\.tag === draft\\.draftMode\\.tag"
---

<objective>
Rewrite the SpacetimeDB backend module to replace the parallel `classicCosts` + `auctionBaseBid` column design with a single `costs` struct + `draftMode: DraftMode` discriminator on all six cost tables. Extend PK tuples + btree indexes to include `draftMode`. Rewrite every cost-touching reducer (bulk upsert / edit_draft_* / create_cost_set / publish_cost_set / draft readers / postDraft LC lookup). Remove PkTest probe artifacts. Publish with `--delete-data=on-conflict` and regenerate both `module_bindings/` directories. Update architecture docs in lock-step.

Purpose: Establish the clean schema foundation Phase 16/17 will write against. Single atomic publish — module won't compile/publish unless schema + reducers are consistent, so they ship together.

Output: Compiling, publishable backend module on maincloud with regenerated bindings committed in both directories, plus updated architecture docs. Frontend will be broken at this point — Plan 02 fixes that.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/skills/spacetimedb/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.4-cost-table-draftmode-restructure/15.4-CONTEXT.md
@.planning/phases/15.4-cost-table-draftmode-restructure/15.4-RESEARCH.md
@.planning/phases/15.4-cost-table-draftmode-restructure/15.4-PATTERNS.md
@.planning/phases/15.4-cost-table-draftmode-restructure/15.4-VALIDATION.md
@CLAUDE.md
@docs/cost-sets/architecture.md
@docs/cost-tables/architecture.md
@spacetimedb/src/types/enums.ts
@spacetimedb/src/types/structs.ts
@spacetimedb/src/helpers/auditColumns.ts

<interfaces>
<!-- Existing types this plan extends. Pulled from spacetimedb/src/types/enums.ts and structs.ts. -->

From spacetimedb/src/types/enums.ts (L44 — already declared, REUSE):
```typescript
export const DraftMode = t.enum('DraftMode', ['Classic', 'Auction']);
```

From spacetimedb/src/types/structs.ts (L48-64 — already declared, REUSE):
```typescript
export const EidolonCost = t.struct('EidolonCost', {
    e0: t.f32(), e1: t.f32(), e2: t.f32(),
    e3: t.f32(), e4: t.f32(), e5: t.f32(), e6: t.f32(),
});
export const SuperimpositionCost = t.struct('SuperimpositionCost', {
    s1: t.f32(), s2: t.f32(), s3: t.f32(), s4: t.f32(), s5: t.f32(),
});
```

Enum struct passing convention (established throughout admin.ts / costSetManagement.ts):
- Insert: `{ tag: 'Classic', value: {} } as any`
- Filter predicate: `row.draftMode.tag === 'Classic'`
- Multi-col btree filter: pass struct object positionally `.filter([name, { tag: 'Auction', value: {} }, csId])`

Existing `auditInsert` / `auditUpdate` helpers (used unchanged):
```typescript
export function auditInsert(ctx: any, userId: number): { createdById, createdDate, lastModifiedById, lastModifiedDate };
export function auditUpdate(ctx: any, existing: any, userId: number): { ...preserves createdBy, updates lastModified };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema rewrite — six cost tables + delete pkTest sources</name>
  <files>spacetimedb/src/tables/hsrCharacterCost.ts, spacetimedb/src/tables/hsrLightconeCost.ts, spacetimedb/src/tables/hsrSynergyCost.ts, spacetimedb/src/tables/costSetDraftCharacter.ts, spacetimedb/src/tables/costSetDraftLightcone.ts, spacetimedb/src/tables/costSetDraftSynergy.ts, spacetimedb/src/tables/pkTest.ts, spacetimedb/src/reducers/pkTest.ts</files>
  <read_first>
    - spacetimedb/src/tables/hsrCharacterCost.ts (current shape — model for char/lc transform)
    - spacetimedb/src/tables/hsrSynergyCost.ts (current shape — distinct autoInc-id pattern)
    - spacetimedb/src/tables/costSetDraftSynergy.ts (current shape — composite-PK NO autoInc; verified in PATTERNS.md analog block)
    - spacetimedb/src/types/enums.ts (confirm DraftMode at L44)
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-PATTERNS.md "Schema tables — char/lc live" + "Schema — HsrSynergyCost" + "Schema — draft tables" sections (lines 84-204) — exact before/after blocks
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-CONTEXT.md D-01..D-09, D-23
  </read_first>
  <action>
**HsrCharacterCost (D-01):**
Add `import { DraftMode } from '../types/enums';` (already imports `GameMode`).
Replace columns block with this exact ordering (mirrors PATTERNS.md target):
```typescript
export const hsrCharacterCostColumns = {
    characterName: t.string(),
    gameMode: GameMode,
    draftMode: DraftMode,           // ADD
    costs: EidolonCost,             // ADD (was: classicCosts + auctionBaseBid — DROP both)
    costSetId: t.u32(),
    createdById: t.u32(),
    createdDate: t.timestamp(),
    lastModifiedById: t.u32(),
    lastModifiedDate: t.timestamp(),
};
```
Update `primaryKey: ['characterName', 'gameMode', 'draftMode', 'costSetId']` (decorative no-op per D-06 finding, kept for self-documentation).
Extend the `by_character_mode_and_set` btree to `columns: ['characterName', 'gameMode', 'draftMode', 'costSetId']`. Keep `cost_set_id` btree unchanged.

**HsrLightconeCost (D-02):**
Mirror HsrCharacterCost transform with `lightconeName` instead of `characterName`, `SuperimpositionCost` instead of `EidolonCost`, and `by_lightcone_mode_and_set` instead of `by_character_mode_and_set`.

**HsrSynergyCost (D-03, D-06, D-07, D-08):**
KEEP `id: t.u32().primaryKey().autoInc()` as the first column — this is the only engine-enforced constraint per D-06 live probe. Add `draftMode: DraftMode` column between `gameMode` and `costModifier`.
Extend the `source_mode` btree: `columns: ['sourceName', 'gameMode', 'draftMode']` (D-08).
ADD a new btree:
```typescript
{ accessor: 'by_tuple', algorithm: 'btree', columns: ['sourceName', 'targetName', 'gameMode', 'draftMode', 'costSetId'] },
```
Keep `target_name` and `cost_set_id` btrees unchanged. The auto-derived `hsrSynergyCostUpsertKeys` will pick up `draftMode` automatically because it filters out `id` and audit columns; verify after edit by reading the line.

**CostSetDraftCharacter (D-04):**
Add `import { DraftMode } from '../types/enums';`. Add `draftMode: DraftMode` column. Replace `classicCosts` + `auctionBaseBid` with single `costs: EidolonCost`. Extend `primaryKey` to include `draftMode`. Extend the existing multi-col btree (e.g., `by_set_character_and_mode` or similar) to include `draftMode` as an additional column at the position consistent with other columns.

**CostSetDraftLightcone (D-04):**
Same transform as CostSetDraftCharacter, with lightcone field names.

**CostSetDraftSynergy (D-04, D-09):**
Add `draftMode: DraftMode` column between `gameMode` and `costModifier`. Extend `primaryKey: ['costSetId', 'sourceName', 'targetName', 'gameMode', 'draftMode']` (decorative).
Replace the existing `by_set_source_target_and_mode` btree (or rename to `by_tuple` for consistency with HsrSynergyCost) with:
```typescript
{ accessor: 'by_tuple', algorithm: 'btree', columns: ['costSetId', 'sourceName', 'targetName', 'gameMode', 'draftMode'] },
```
Keep `cost_set_id` btree unchanged.

**Probe artifact removal (D-23):**
Delete files entirely:
- `spacetimedb/src/tables/pkTest.ts`
- `spacetimedb/src/reducers/pkTest.ts`
Also remove their imports/exports from `spacetimedb/src/schema.ts` if present. Use Grep to find references first, then remove the import lines and any export aggregator lines. Do NOT delete `tmp/test-composite-pk.ts` — it stays as documented reference per memory/reference_composite_pk_decorative.md.

**Implements:** REQ-154-01, REQ-154-02, REQ-154-03 per D-01..D-09 and D-23.
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit 2>&1 | grep -E "(hsrCharacterCost|hsrLightconeCost|hsrSynergyCost|costSetDraftCharacter|costSetDraftLightcone|costSetDraftSynergy|pkTest)\.ts" | head -20</automated>
    Expected: ZERO output from the table files (TS compile clean for table layer); reducer files will still error at this stage because they reference dropped columns — that's fixed in Task 2. Also run `grep -r "pkTest" spacetimedb/src/` and confirm zero results except optional comment in tmp/.
  </verify>
  <done>
    - All 6 table files contain `draftMode: DraftMode` column.
    - HsrCharacterCost / HsrLightconeCost have `costs:` (no `classicCosts` / `auctionBaseBid`).
    - HsrSynergyCost retains `id: t.u32().primaryKey().autoInc()`.
    - HsrSynergyCost has new `by_tuple` index on 5 columns; `source_mode` extended to 3 columns.
    - CostSetDraftSynergy has `by_tuple` (or renamed equivalent) on 5 columns including `draftMode`.
    - `pkTest.ts` table + reducer source files deleted from filesystem.
    - `grep -rn "pkTest\\|PkTest" spacetimedb/src/` returns zero matches in remaining source files.
    - All primary-key tuples include `draftMode` (decorative declaration per D-06).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reducers rewrite — admin_bulk_upsert + costSetManagement + draft readers + postDraft</name>
  <files>spacetimedb/src/reducers/admin.ts, spacetimedb/src/reducers/costSetManagement.ts, spacetimedb/src/reducers/draftClassic.ts, spacetimedb/src/reducers/draftAuction.ts, spacetimedb/src/reducers/postDraft.ts</files>
  <read_first>
    - spacetimedb/src/reducers/admin.ts L487-631 (HsrCharacterCost / HsrLightconeCost / HsrSynergyCost bulk-upsert cases — current iter() + insert shapes)
    - spacetimedb/src/reducers/costSetManagement.ts L70-100 (create_cost_set clone), L108-200 (edit_draft_character_cost + edit_draft_lightcone_cost + edit_draft_synergy_cost current signatures and bodies), L346-416 (publish_cost_set 3 phases)
    - spacetimedb/src/reducers/draftClassic.ts (getCharacterBaseCost current shape — find the function; mirror of draftAuction)
    - spacetimedb/src/reducers/draftAuction.ts L45-62 (getCharacterBaseCost — current auctionBaseBid access)
    - spacetimedb/src/reducers/postDraft.ts L47-61 (LC cost lookup with 3-arg btree filter — Pitfall 6 site)
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-PATTERNS.md "Reducer — admin.ts" sections (lines 207-314), "Reducer — edit_draft_character_cost" (lines 318-357), "Reducer — create_cost_set clone" (lines 361-390), "Reducer — publish_cost_set" (lines 394-418), "Reducer-helper — draftAuction.ts" (lines 422-471), "Reducer-helper — postDraft.ts" (lines 475-501)
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-RESEARCH.md "Common Pitfalls" section (1-7), specifically Pitfall 1, 2, 5, 6, 7
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-CONTEXT.md D-10, D-11, D-16..D-21
  </read_first>
  <action>
**admin.ts HsrCharacterCost case (D-19, replaces L487-538):**
Replace the iter() existence check with btree filter:
```typescript
const draftModeStruct = { tag: r.draftMode, value: {} } as any;
const existing = [...ctx.db.HsrCharacterCost.by_character_mode_and_set.filter(
    [r.characterName, { tag: r.gameMode, value: {} }, draftModeStruct, csId]
)][0] ?? null;
```
If during initial test the enum struct in `.filter([])` does NOT work (Assumption A2 in RESEARCH.md falls), fall back to the iter() loop with extended predicate:
```typescript
let existing: any = null;
for (const e of ctx.db.HsrCharacterCost.iter()) {
    if (e.characterName === r.characterName &&
        e.gameMode.tag === r.gameMode &&
        e.draftMode.tag === r.draftMode &&
        e.costSetId === csId) { existing = e; break; }
}
```
Update `HSR_CHARACTER_COST_FIELDS` (or whatever the field-list constant is named at the top of the case): `['classicCosts', 'auctionBaseBid']` → `['costs']`.
Update the incoming partial-update assembly: `{ classicCosts, auctionBaseBid }` → `{ costs: r.costs }`.
Update the insert payload to:
```typescript
ctx.db.HsrCharacterCost.insert({
    characterName: r.characterName,
    gameMode: { tag: r.gameMode, value: {} },
    draftMode: { tag: r.draftMode, value: {} },
    costs: r.costs,
    costSetId: csId,
    ...auditInsert(ctx, admin.id),
} as any);
```
The delete branch (`ctx.db.HsrCharacterCost.delete(existing)`) stays as-is.

**admin.ts HsrLightconeCost case (D-19, mirror of char):**
Same transformation with `lightconeName` / `SuperimpositionCost` / `by_lightcone_mode_and_set`. Field-list: `['costs']`.

**admin.ts HsrSynergyCost case (D-19, distinct id.update() pattern):**
Replace iter() existence check with `by_tuple` btree filter:
```typescript
const draftModeStruct = { tag: r.draftMode, value: {} } as any;
const existing = [...ctx.db.HsrSynergyCost.by_tuple.filter(
    [r.sourceName, r.targetName, { tag: r.gameMode, value: {} }, draftModeStruct, csId]
)][0] ?? null;
```
Same fallback to iter() with `e.draftMode.tag === r.draftMode` if A2 fails.
Insert payload adds `draftMode: { tag: r.draftMode, value: {} },` between `gameMode` and `costModifier`. The `id.update()` branch keeps `...existing` spread (which now naturally preserves draftMode); only `costModifier` and audit fields mutate. No other change needed there.
Field-list constant for synergy stays `['costModifier']`.

**costSetManagement.ts edit_draft_character_cost (D-16):**
Replace the args block:
```typescript
{
    costSetId: t.u32(),
    characterName: t.string(),
    gameModeTag: t.string(),
    draftModeTag: t.string(),   // ADD
    costsJson: t.string(),      // RENAME (was: classicCostsJson + auctionBaseBidJson)
}
```
And the destructure: `(ctx, { costSetId, characterName, gameModeTag, draftModeTag, costsJson }) => { ... }`.
Body changes:
- Single `JSON.parse(costsJson)` instead of two parses; reuse the existing try/catch pattern with error message updated to mention `costsJson`.
- Single e0..e6 numeric validation loop (delete the duplicate that was for auctionBaseBidJson).
- Add `const draftMode = { tag: draftModeTag, value: {} } as any;` near the existing `const gameMode` line.
- Existence find:
```typescript
const drafts = [...ctx.db.CostSetDraftCharacter.cost_set_id.filter(costSetId)];
const existing = drafts.find(d =>
    d.characterName === characterName &&
    d.gameMode.tag === gameModeTag &&
    d.draftMode.tag === draftModeTag);
```
- Delete + insert pattern (composite-PK update — D-06 reason): `ctx.db.CostSetDraftCharacter.delete(existing)` then insert `{ costSetId, characterName, gameMode, draftMode, costs, ...auditInsert(...)|auditUpdate(ctx, existing, ...) }` — same audit-helper choice as current code (auditUpdate when existing was found, auditInsert when new).

**costSetManagement.ts edit_draft_lightcone_cost (D-17):**
Mirror of D-16 with `lightconeName` and `SuperimpositionCost` (s1..s5 validation loop).

**costSetManagement.ts edit_draft_synergy_cost (D-18):**
New args block (no JSON, scalar payload):
```typescript
{
    costSetId: t.u32(),
    sourceName: t.string(),
    targetName: t.string(),
    gameModeTag: t.string(),
    draftModeTag: t.string(),
    costModifier: t.f32(),
}
```
Body: existence find via `[...ctx.db.CostSetDraftSynergy.cost_set_id.filter(costSetId)].find(...)` matching all 5 tuple fields. Delete + insert with `{ costSetId, sourceName, targetName, gameMode, draftMode, costModifier, ...audit }`. No JSON parsing.

**costSetManagement.ts create_cost_set clone (D-20, ~L70-100):**
For each of the three clone branches (chars, lcs, synergies), add `draftMode: row.draftMode,` to the insert payload and replace `classicCosts: row.classicCosts, auctionBaseBid: row.auctionBaseBid` with `costs: row.costs` for char/lc. Synergy gains `draftMode: row.draftMode,` only. The source-table filter stays `gameMode.tag === gameModeTag` — both Classic and Auction rows match and both get cloned without code branching.

**costSetManagement.ts publish_cost_set (D-21, Pitfall 7 fix, L346-416):**
For ALL THREE phases (A=chars L350-365, B=lcs L367-385, C=synergies L387-416), extend the `existingLive.find` predicate to include `r.draftMode.tag === draft.draftMode.tag`:
```typescript
const existingLive = liveRows.find(r =>
    r.characterName === draft.characterName &&
    r.gameMode.tag === draft.gameMode.tag &&
    r.draftMode.tag === draft.draftMode.tag);   // ADD
```
Update the insert payload in phases A and B to swap `classicCosts: draft.classicCosts, auctionBaseBid: draft.auctionBaseBid` → `draftMode: draft.draftMode, costs: draft.costs`. Phase C (synergy) `id.update()` path keeps `...existingLive` spread which preserves draftMode; the insert branch adds `draftMode: draft.draftMode,`.

**draftClassic.ts getCharacterBaseCost (D-10, D-11):**
Locate the `find()` predicate. Add `&& r.draftMode.tag === 'Classic'`. Replace whatever the current eidolon column access is (likely `costRow.classicCosts[eidolonKey]`) with `costRow.costs[eidolonKey] ?? 0`. Keep the `if (!costRow) return 0` early return.

**draftAuction.ts getCharacterBaseCost (D-10, D-11, L45-62, Pitfall 2):**
Replace the entire function body — do NOT patch piecewise. Pattern (verbatim from PATTERNS.md):
```typescript
function getCharacterBaseCost(
    ctx: any, characterName: string, eidolon: number,
    costSetId: number, gameMode: any
): number {
    if (characterName === 'EMPTY') return 0;
    const costRows = [...ctx.db.HsrCharacterCost.cost_set_id.filter(costSetId)];
    const costRow = costRows.find((r: any) =>
        r.characterName === characterName &&
        r.gameMode.tag === gameMode.tag &&
        r.draftMode.tag === 'Auction'
    );
    if (!costRow) return 0;
    const eidolonKey = `e${eidolon}` as keyof typeof costRow.costs;
    return costRow.costs[eidolonKey] ?? 0;
}
```

**postDraft.ts LC lookup (D-10, D-11, L47-61, Pitfall 6):**
Add at the function scope (or imported constant):
```typescript
const CLASSIC_DRAFT_MODE = { tag: 'Classic', value: {} } as any;
```
Replace the existing 3-positional `.filter([lightconeName, lobby.gameMode, costSetIdToTry])` with 4-positional including `CLASSIC_DRAFT_MODE` in the third slot (matching the extended `by_lightcone_mode_and_set` index from D-02). Apply the same to the 0-fallback `.filter([lightconeName, lobby.gameMode, CLASSIC_DRAFT_MODE, 0])`.
Replace `costRow.classicCosts` → `costRow.costs` (both the truthy guard `if (costRow && costRow.costs)` and the indexed access `(costRow.costs as any)[sField]`).
If A2 fails, fall back to filtering by `cost_set_id` only and finding by predicate that includes the draftMode and lightconeName comparisons.

**Implements:** REQ-154-05, REQ-154-06, REQ-154-07, REQ-154-08 per D-10/D-11/D-16/D-17/D-18/D-19/D-20/D-21.
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit 2>&1 | tee /tmp/sdb-tsc.log; echo "---exit:$?---"; grep -cE "classicCosts|auctionBaseBid" spacetimedb/src/reducers/*.ts spacetimedb/src/tables/*.ts</automated>
    Expected: TS compile exits 0 (`---exit:0---`); the second grep returns 0 occurrences across all listed files. If the tsc log shows errors, they must be related to bindings (which haven't been regenerated yet at this point — Task 3 handles regen). All in-file references to `classicCosts` / `auctionBaseBid` are gone from source.
  </verify>
  <done>
    - admin.ts bulk-upsert: all three cases use 4-tuple (char/lc) or 5-tuple (synergy) tuple match with `draftMode`.
    - admin.ts char/lc insert payload uses `draftMode: { tag, value }` and `costs: r.costs` (no `classicCosts` / `auctionBaseBid`).
    - costSetManagement.ts: all three `edit_draft_*_cost` reducers have new signatures (D-16/D-17/D-18); body inserts include `draftMode`.
    - costSetManagement.ts: `create_cost_set` clone branches carry `draftMode` through; `publish_cost_set` all three phases extend `existingLive` predicate with `draftMode.tag` match.
    - draftClassic + draftAuction: `getCharacterBaseCost` filters by `draftMode.tag === 'Classic' | 'Auction'` respectively and reads `costRow.costs[eidolonKey]`.
    - postDraft.ts: 4-positional btree filter with `CLASSIC_DRAFT_MODE`; reads `costRow.costs`.
    - `grep -rn "classicCosts\\|auctionBaseBid" spacetimedb/src/reducers/ spacetimedb/src/tables/` returns zero matches.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Publish to maincloud + regenerate bindings + update architecture docs</name>
  <files>spacetimedb/src/module_bindings/index.ts, spacetimedb/src/module_bindings/types.ts, spacetimedb/src/module_bindings/hsr_character_cost_table.ts, spacetimedb/src/module_bindings/hsr_lightcone_cost_table.ts, spacetimedb/src/module_bindings/hsr_synergy_cost_table.ts, spacetimedb/src/module_bindings/cost_set_draft_character_table.ts, spacetimedb/src/module_bindings/cost_set_draft_lightcone_table.ts, spacetimedb/src/module_bindings/cost_set_draft_synergy_table.ts, spacetimedb/src/module_bindings/edit_draft_character_cost_reducer.ts, spacetimedb/src/module_bindings/edit_draft_lightcone_cost_reducer.ts, spacetimedb/src/module_bindings/edit_draft_synergy_cost_reducer.ts, src/module_bindings/index.ts, src/module_bindings/types.ts, src/module_bindings/hsr_character_cost_table.ts, src/module_bindings/hsr_lightcone_cost_table.ts, src/module_bindings/hsr_synergy_cost_table.ts, src/module_bindings/cost_set_draft_character_table.ts, src/module_bindings/cost_set_draft_lightcone_table.ts, src/module_bindings/cost_set_draft_synergy_table.ts, src/module_bindings/edit_draft_character_cost_reducer.ts, src/module_bindings/edit_draft_lightcone_cost_reducer.ts, src/module_bindings/edit_draft_synergy_cost_reducer.ts, docs/cost-sets/architecture.md, docs/cost-tables/architecture.md</files>
  <read_first>
    - docs/cost-sets/architecture.md (current schema diagrams, reducer reference, data patterns sections)
    - docs/cost-tables/architecture.md (current read-path documentation)
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-CONTEXT.md D-22, D-23, D-28
    - .planning/phases/15.4-cost-table-draftmode-restructure/15.4-RESEARCH.md "Runtime State Inventory" + "Environment Availability" sections
    - memory/feedback_post_publish_only.md (publish flow constraint — `--clear-database` followed by ONLY post-publish.ts)
    - spacetime.json (verify both bindings dirs are configured for `spacetime generate`)
  </read_first>
  <action>
**Step 3.1 — Verify pre-publish state:**
Run `cd spacetimedb && npx tsc --noEmit` — must exit 0. If errors remain, fix them in Task 1/2 before continuing. Run `spacetime server list` and confirm the default-marked (`***`) server is `maincloud`. If not, ABORT and pause for user — do not publish to a different target.

**Step 3.2 — Publish with data wipe (D-22, data-destructive cutover):**
This is the BLOCKING data-destructive step. Run from project root:
```bash
cd spacetimedb && spacetime publish --delete-data=on-conflict <db-name-from-spacetime.json>
```
Where `<db-name>` is read from `spacetime.json`. Capture stdout + stderr to a temp log. Expected outcomes: schema diff applied, all rows in HsrCharacterCost / HsrLightconeCost / HsrSynergyCost / CostSetDraft* / pk_test wiped. If publish fails with anything other than "schema migration applied," HALT and report.

**Step 3.3 — Regenerate bindings (both directories):**
```bash
spacetime generate --lang typescript --out-dir spacetimedb/src/module_bindings
spacetime generate --lang typescript --out-dir src/module_bindings
```
(Run from project root with appropriate db-spec args — match whatever pattern `spacetime.json` or prior phase scripts established. If a single `spacetime generate` command per `spacetime.json` regenerates both at once per Assumption A1, use that — verify by checking `spacetime.json` for `generate` entries.)

After regen, verify:
- `grep -E "classicCosts|auctionBaseBid" src/module_bindings/*.ts spacetimedb/src/module_bindings/*.ts` returns zero matches.
- `grep -E "draftMode" src/module_bindings/types.ts` returns matches in the HsrCharacterCost / HsrLightconeCost / HsrSynergyCost row types.
- `grep -E "draftModeTag" src/module_bindings/edit_draft_character_cost_reducer.ts` returns a match.
- `grep -E "costsJson" src/module_bindings/edit_draft_character_cost_reducer.ts` returns a match (and `classicCostsJson` / `auctionBaseBidJson` return zero).

**Step 3.4 — Run post-publish bootstrap:**
Per memory/feedback_post_publish_only.md, only `post-publish.ts` is run after `--clear-database`-equivalent flow. At this point the seed pipelines still emit OLD-shape rows (Plan 02 hasn't run yet), so seed will fail. That's expected — DO NOT run `npx tsx scripts/post-publish.ts` here. Note in commit message: "post-publish reseed deferred to after Plan 02 (seed pipeline rewrite)."
The maincloud DB is empty + new-schema. Plan 02 will rewrite seeds and reseed via post-publish.ts; Plan 04 will run integration tests.

**Step 3.5 — Update docs/cost-sets/architecture.md (D-28):**
Find the schema-diagram section. Update each cost-table block (HsrCharacterCost / HsrLightconeCost / HsrSynergyCost / CostSetDraftCharacter / CostSetDraftLightcone / CostSetDraftSynergy) to:
- Replace `classicCosts: EidolonCost\nauctionBaseBid: EidolonCost` with `costs: EidolonCost\ndraftMode: DraftMode` (char/lc).
- For HsrSynergyCost: keep `id: u32 PK autoInc` + `costModifier: f32`; add `draftMode: DraftMode`.
- Update PK tuple notation under each table to include `draftMode`.
- Update btree index list: `by_character_mode_and_set` (4 cols), `by_lightcone_mode_and_set` (4 cols), `source_mode` (3 cols), new `by_tuple` (5 cols on synergy).

Find the "Reducer reference" table. Update the row signatures for `edit_draft_character_cost`, `edit_draft_lightcone_cost`, `edit_draft_synergy_cost`, `admin_bulk_upsert` to reflect new arg names (`draftModeTag`, `costsJson`, single-row payload).

Find or add a "Data patterns" subsection. Add a "Row-absence semantic" note: "A missing row for `(name, gameMode, draftMode, costSetId)` means 'not configured for that draft mode.' Reader functions return 0; lobby-creation coverage validation is deferred to a separate todo (see CONTEXT.md Deferred Ideas)."

Add a "Phase History" entry: "Phase 15.4 execution — draftMode column added to all 6 cost tables; classicCosts/auctionBaseBid collapsed to single `costs`; first-ever synergy auction support."

**Step 3.6 — Update docs/cost-tables/architecture.md (D-28):**
Find the read-path section. Update the description of `getCharacterBaseCost` to note the draftMode filter ('Classic' for draftClassic.ts; 'Auction' for draftAuction.ts; postDraft.ts uses 'Classic' for LC equip lookup). Note that `costRow.costs` is the unified column; `auctionBaseBid` no longer exists. Add a Phase History entry tagged `Phase 15.4 execution`.

**Implements:** REQ-154-04, REQ-154-10 per D-22, D-28 (and side-effects of D-23 maincloud wipe).
  </action>
  <verify>
    <automated>echo "--- bindings shape check ---"; grep -lE "classicCosts|auctionBaseBid" src/module_bindings/*.ts spacetimedb/src/module_bindings/*.ts || echo "OK: no stale field refs"; echo "--- new shape check ---"; grep -l "draftMode" src/module_bindings/types.ts spacetimedb/src/module_bindings/types.ts; echo "--- backend tsc ---"; cd spacetimedb && npx tsc --noEmit; echo "exit:$?"</automated>
    Expected: First grep returns "OK: no stale field refs" (no files match). Second grep returns both `types.ts` paths. Backend tsc exits 0 (full module compiles cleanly with new bindings).
  </verify>
  <done>
    - `spacetime publish` succeeded against maincloud with `--delete-data=on-conflict`.
    - `spacetime generate` produced fresh bindings in BOTH `spacetimedb/src/module_bindings/` AND `src/module_bindings/`.
    - Regenerated bindings contain `draftMode`, `costs` (char/lc), and `draftModeTag` / `costsJson` (reducers); contain ZERO references to `classicCosts` / `auctionBaseBid` / `classicCostsJson` / `auctionBaseBidJson`.
    - `cd spacetimedb && npx tsc --noEmit` exits 0.
    - `docs/cost-sets/architecture.md` updated with new schema, reducer signatures, row-absence note, Phase 15.4 History entry.
    - `docs/cost-tables/architecture.md` updated with new read-path filter notes + Phase 15.4 History entry.
    - All bindings files staged for commit (per CLAUDE.md "Gitignore Guardrail" — module_bindings/ IS committed; test/data/ stays gitignored and is NOT touched here).
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `cd spacetimedb && npx tsc --noEmit` → exit 0
2. `grep -rn "classicCosts\\|auctionBaseBid" spacetimedb/src/ src/module_bindings/` → zero matches
3. `grep -rn "draftMode" spacetimedb/src/tables/hsrCharacterCost.ts spacetimedb/src/tables/hsrLightconeCost.ts spacetimedb/src/tables/hsrSynergyCost.ts` → matches in all three
4. `grep -rn "by_tuple" spacetimedb/src/tables/hsrSynergyCost.ts spacetimedb/src/tables/costSetDraftSynergy.ts` → matches in both
5. `ls spacetimedb/src/tables/pkTest.ts spacetimedb/src/reducers/pkTest.ts` → both files absent
6. `git ls-files spacetimedb/src/module_bindings src/module_bindings` → both directories tracked
7. Maincloud dashboard reflects new schema (manual sanity check via `spacetime sql <db> "SELECT * FROM hsr_character_cost LIMIT 1"` after seed in Plan 02 — not blocking for this plan)
8. `git diff --stat` shows changes in: 6 table files, 5 reducer files, 2 deleted pkTest files, 22 module_bindings files (11 per directory), 2 architecture docs

Frontend will fail to compile at this point — that's Plan 02's job.
</verification>

<success_criteria>
- Backend module compiles, publishes, regenerates clean.
- All three live cost tables and all three draft cost tables have the new shape per D-01..D-04.
- HsrSynergyCost retains autoInc id (D-06) + has new `by_tuple` btree (D-07) + extended `source_mode` (D-08).
- Probe artifacts removed from filesystem (D-23) and from maincloud via the same publish.
- All cost-touching reducers operate on new shape with draftMode-included tuple match (D-10, D-11, D-16..D-21).
- Both bindings directories regenerated and committed.
- Architecture docs reflect the new shape (D-28).
- `npm run build` (frontend) is EXPECTED to fail with TS errors referencing missing `classicCosts` / `auctionBaseBid` — that breakage is Plan 02's input contract.
</success_criteria>

<output>
After completion, create `.planning/phases/15.4-cost-table-draftmode-restructure/15.4-01-SUMMARY.md` documenting:
- Final shape of each table (column list, PK tuple, indexes)
- Whether the btree-enum-struct filter pattern (Assumption A2) worked or fell back to iter() — record outcome for Task 2 of subsequent plans
- Maincloud publish outcome + regen output paths
- Architecture doc additions (which sections grew, which Phase History entries landed)
- Confirmation that pkTest sources are absent
- Explicit note that frontend (Plan 02) and seed (Plan 03) and tests (Plan 04) are still broken — this is intentional handoff state
</output>
