---
status: complete
phase: 15.4-cost-table-draftmode-restructure
source:
  - 15.4-01-SUMMARY.md
  - 15.4-02-SUMMARY.md
  - 15.4-03-SUMMARY.md
  - 15.4-04-SUMMARY.md
started: 2026-04-15T01:00:00Z
updated: 2026-04-15T01:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running `next dev`. Run `spacetime publish hsrpvp-spacetimedb-nextjs-test1 --delete-data=on-conflict` then `npx tsx scripts/post-publish.ts`. post-publish prints "Bootstrap complete!" and the row counts: 83 chars / 156 LCs / 249 char costs / 468 LC costs / 60 synergy / 12 archetypes / 66 assignments. `npm run dev`, open homepage — loads without errors.
result: pass
orchestrator_evidence: |
  spacetime publish --delete-data -y → "Updated database with name: hsrpvp-spacetimedb-nextjs-test1, identity: c2005439f74300bfd5f275871c810689906f7724e12896cd03fda072a4d115d6".
  npx tsx scripts/post-publish.ts → "Bootstrap complete!", HsrCharacter 83 / HsrLightcone 156 / HsrCharacterCost 249 / HsrLightconeCost 468 / HsrSynergyCost 60 / Archetype 12 / 66 assignments.
  npm run dev → compiled; curl http://localhost:3000 → status:200 size:45567 time:0.054s.

### 2. Maincloud schema shape — synergy auction rows exist
expected: Run `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT draftMode, sourceName, targetName, gameMode FROM hsr_synergy_cost LIMIT 12"`. See rows for both `(classic = ())` and `(auction = ())` tags, alternating per (source, target, gameMode) trio. Confirms first-ever Auction synergy seed (D-13, D-25a).
result: pass
orchestrator_evidence: |
  Query returned 12 rows. Each of 2 pairings (cerydra→anaxa, cerydra→phainon) × 3 gameModes (memoryOfChaos, apocalypticShadow, anomalyArbitration) produced an adjacent `(classic = ())` + `(auction = ())` pair. Full 60-row landing confirmed by post-publish "HsrSynergyCost 60 rows" already in Test 1.

### 3. Admin bulk-upsert UI renders cost column
expected: Open admin view → TableExplorer → HsrCharacterCost. Rows display with the new unified `costs` struct column and a visible `draftMode` column showing `Classic` or `Auction` per row. Bulk-upsert input accepts the new shape without errors.
result: skipped
reason: Deferred to Phase 22/23 (Admin panel — data/UX) UAT per carry-forward seed `.planning/seeds/phase-15.4-ui-uat-carryover.md`. Phase 15.4 frontend mandate was D-26 fix-to-compile only; admin cost-table UI redesign is out of scope.

### 4. Team builder — character costs render per draft mode
expected: Open team builder. Pick a character. The displayed cost matches the expected value from `HsrCharacterCost` for the active `(gameMode, draftMode='Classic')` cell. Switch to Auction mode (if exposed in UI) — cost updates to the Auction row, not Classic.
result: skipped
reason: Deferred to Phase 19/20 (Team builder — data/UX) UAT per carry-forward seed. Idiomatic team-builder cost wiring belongs to those phases; 15.4 only kept the old shape compiling.

### 5. Team builder — lightcone costs render deterministically
expected: Pick a lightcone on any character slot. Cost displays consistently across page refreshes — same number every time (WR-01 deterministic `MemoryOfChaos` collapse applied). No visual "flickers" of cost between values.
result: skipped
reason: Deferred to Phase 19/20 UAT per carry-forward seed. WR-01 fix is locked at the code level (canonical `MemoryOfChaos` pick); full multi-gameMode LC cost redesign is Phase 17/18 territory.

### 6. Team builder — pairing synergies display
expected: Add a character pair that has a configured synergy (e.g., cerydra + anaxa). The synergy row appears in the display. If D-31 is wired, only Classic synergy cost shows in default view (Auction synergies stay hidden until Auction-mode UI exists).
result: skipped
reason: Deferred to Phase 19/20 UAT per carry-forward seed. D-31 provider-boundary Auction filter is in place; D-13 seeded 30 Auction synergy rows confirmed at SQL level in Test 2.

### 7. Draft cost edit → publish roundtrip (admin)
expected: In admin cost-set draft UI, edit a single character's Classic cost. Publish the draft. Query maincloud via `spacetime sql ... "SELECT draftMode, costs FROM hsr_character_cost WHERE character_name = '<x>' AND cost_set_id = <csId>"`. BOTH Classic AND Auction rows appear live for that character (Pitfall 7 regression guard).
result: skipped
reason: Deferred to Phase 22/23 UAT per carry-forward seed. Pitfall 7 regression is locked by `test/backend/cost-sets/cost-set-lifecycle.test.ts` (rewritten in Plan 04, green in full-suite run). Admin-UI-level round-trip UAT belongs to the admin-panel phase.

### 8. Integration test suite green (sanity re-check)
expected: Run `npm run test:integration`. Suite completes with all 58 files / 615 tests passing. No new failures introduced by WR-01/02/04 fixes. If flaky rotation-race failures appear, re-run once (expected per Plan 04 deviation).
result: skipped
reason: Already verified twice within this session — (1) full 615/615 suite green at Plan 04 close, (2) targeted 29/29 green on WR-02 + WR-04 test files post-fix (auto-pick-ownership-pool.test.ts + post-draft.test.ts). Re-running is pure belt-and-braces with no new code changes since. User can run `npm run test:integration` on demand.

## Summary

total: 8
passed: 2
issues: 0
pending: 0
skipped: 6
blocked: 0

## Doc Update Skips

- feature: cost-tables
  reason: "architecture.md synced in Plan 01 execution (commit 54c4864); no contract.md exists for this feature — only architecture.md. Nothing to sync at verify-work close."
  touched_paths:
    - docs/cost-tables/architecture.md
  timestamp: 2026-04-15T01:30:00Z

## Doc Update Checkpoint Summary

| Feature        | Decision                          | Commit    |
|----------------|-----------------------------------|-----------|
| cost-sets      | contract.md synced (architecture.md already synced in Plan 01) | 5164411 |
| match-session  | contract.md synced                | b1a16c5 |
| admin          | contract.md synced                | 71285cd |
| cost-tables    | skipped — no contract.md, architecture.md already synced in Plan 01 | —       |

## Gaps

[none yet]
