---
title: Phase 16.3 audit notes (changelog + delete-flag sweep)
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
date: 2026-05-03
references:
  - .planning/notes/spacetimedb-version-drift.md
  - .planning/ROADMAP.md (Phase 16.3, lines 289–308)
  - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md (D-11, D-12, D-13)
  - .planning/milestones/v0.5-phases/12.2-sdk-upgrade-audit/12.2-RESEARCH.md (prior 2.0.3 → 2.1.0 audit; not re-done here)
---

# Phase 16.3 audit notes

This file is the durable output of Plan 03's two read-mostly tasks. It rides into Commit B
of this phase (alongside the manifest bumps + bindings regen) so future SDK-bump readers
find both the lesson (`.planning/notes/spacetimedb-version-drift.md`) AND the v2.0.4 → v2.2.0
audit findings in one place. Cross-reference per D-15's spirit.

## Changelog audit (v2.0.4 → v2.2.0) — D-11

**Sources read** (GitHub Releases, official changelogs):
- v2.0.4: https://github.com/clockworklabs/SpacetimeDB/releases/tag/v2.0.4
- v2.0.5: https://github.com/clockworklabs/SpacetimeDB/releases/tag/v2.0.5
- v2.1.0: https://github.com/clockworklabs/SpacetimeDB/releases/tag/v2.1.0 (also previously audited in `.planning/milestones/v0.5-phases/12.2-sdk-upgrade-audit/12.2-RESEARCH.md` § "SDK Changelog Audit (v2.0.3 -> v2.1.0)")
- v2.2.0: https://github.com/clockworklabs/SpacetimeDB/releases/tag/v2.2.0

**Already enumerated in ROADMAP SC** (no action — covered by Plan 02's bindings regen + Plan 04's republish + SQL probes):
- v2.2.0 #4709 — `[TS] Fix access before initialization in codegen` — ROADMAP SC#3 (cosmetic codegen reorder; benign per typecheck + test pass)
- v2.2.0 #4902 — `fix: update seq table when migrating table` — ROADMAP SC#5 (autoinc next-value advance after migration; verified by Plan 04 SQL probe of `st_sequence`)
- v2.2.0 #4666 — `Fix primary key migration causing stale schema` — ROADMAP SC#5 (verified by Plan 04 SQL probe of `st_table.table_primary_key`)
- v2.2.0 #4770 — `Require confirmation for spacetime delete` — ROADMAP SC#4 (delete-flag sweep below)

### Unenumerated findings

The audit re-read the v2.0.4 / v2.0.5 / v2.1.0 / v2.2.0 release notes against project codebase patterns. Findings classified per Plan 03's `<action>` Step 2 categories.

#### v2.0.4

| Fix / change | Project pattern | Classification | Rationale |
|---|---|---|---|
| #4596 — Fix Bool deserialized as number in TS SDK fast path | All boolean column reads in client | **covered (auto)** | Bug fix in TS SDK fast path; benign on bump. No project-side action — module bindings already use the typed boolean accessors generated by `spacetime generate`. Net positive. |
| #4519 — feat(tanstack): add SSR prefetching for Tanstack Start | Project does not use Tanstack Start | **out-of-scope** | Not used. Frontend is Next.js + `spacetimedb/react`. |
| #4525 — Make `accessor` required for table-level index defs in typescript | All `t.btreeIndex(...)` table options | **covered** | Already audited during Phase 12.2 (post-2.0.3); current schema definitions already use the required form. No bump-time impact. |
| #4567 — Export Range/Bound from spacetimedb/server (TS SDK) | Project reducers do not import `Range` or `Bound` | **out-of-scope** | Verified by grep — no imports of `Range` or `Bound` from `spacetimedb` in `spacetimedb/src/`. New types available but not consumed. |
| #4540 — Expose `RawModuleDefV10` via HTTP schema route | Project does not consume the schema route | **out-of-scope** | Not used. |
| #4549 — Persist host type update and honor stored value after restart | Maincloud-managed runtime | **covered (auto)** | Server-side fix; transparent on republish. |
| #4547 — Add implicit query builder conversions from `bool` to `BoolExpr` | `subscriptionBuilder` / view query builders | **covered (auto)** | Ergonomic improvement; existing queries continue to work. |
| #4404 — Append commit instead of individual transactions to commitlog | Maincloud durability | **covered (auto)** | Server-side; benign on republish. |
| #4605 — Better error message for semijoin predicates | Subscription queries that use semijoins | **covered (auto)** | Diagnostic-only; no behavior change. |
| #4572 — [Rust] Primary keys for query builder views | TS module — no Rust | **out-of-scope** | Rust-side change. |

**v2.0.4 net:** No project-side action required. All bug fixes are auto-applied on the bump; no codebase patterns trigger behavior changes.

#### v2.0.5

| Fix / change | Project pattern | Classification | Rationale |
|---|---|---|---|
| #4607 — Fix disconnects breaking view updates for other connections | 2 anonymous views (`view_lobby_browser` in `lobbyViews.ts`, plus `identityViews.ts`) and many named views | **covered (auto)** — net positive | View update consumers (frontend `subscriptionBuilder` calls in `AuthProvider.tsx`, `app/(authed)/layout.tsx`) directly benefit. Bug-fix-only; no action. |
| #4630 — Bump HTTP procedure timeouts (500ms/10s → 30s/180s) | Project does not currently use HTTP-invoked procedures | **out-of-scope** | Reducers are called via WebSocket. (Procedures may be adopted in 16.4 via the new `useProcedure` hook — informational only.) |
| #4619 — Attempt to repair databases with wrong host type | Maincloud-managed | **covered (auto)** | Server-side recovery; benign. |
| #4567 — Export Range/Bound from spacetimedb/server | (Same as v2.0.4 row) | **out-of-scope** | No imports. |
| #4566 — Rust SDK additional logging | TS module — no Rust | **out-of-scope** | |
| #4598 — Upgrade prometheus to 0.14.0 | Server-side metrics | **covered (auto)** | Internal dependency bump. |

**v2.0.5 net:** No project-side action required. The #4607 disconnect/view fix is the highest-value automatic win for this project (we have 2 anonymous views + heavy `subscriptionBuilder` use in auth flow); it lands transparently on the v2.2.0 bump.

#### v2.1.0 — already audited in 12.2-RESEARCH.md, re-read for completeness

The Phase 12.2 audit (lines 264–292 of `12.2-RESEARCH.md`) already enumerated the v2.0.3 → v2.1.0 changes against project patterns. Re-reading against the current codebase confirms the prior conclusions hold:

| Fix / change (PR) | Phase 12.2 assessment | 16.3 re-confirmation |
|---|---|---|
| #4682 — Confirmed reads default ON for TypeScript | Action item — added `.withConfirmedReads(false)` in providers | Still in place at `app/providers.tsx:36`, all test harness files, all scripts. 14 active call sites. |
| #4580 — `useTable` isReady stale closure fix | Net positive | 9 `useTable(...)` call sites in `components/` benefit transparently. |
| #4648 — Client disconnects no longer drop subscriptions for other v2 clients | Net positive | Stability win for multi-client scenarios. |
| #4627 — Column names instead of accessors for TS queries | Already working under 2.1.0 install | No regression on the 2.2.0 bump. |
| #4646 — Anonymous view subscription cleanup | Net positive (we have 2 anonymous views) | Confirmed — anonymous views still cleanly wired post-bump. |
| #4638 — View table `count` method added | Available, not consumed | Still not consumed; nice-to-have. |
| #4639 — Refresh views during one-shot schedule cleanup | Relevant — `lobbyGc.ts`, `identityGc.ts`, `banAdmin.ts`, `admin.ts` use scheduled tables | Net positive auto-fix; scheduled GC reducers benefit on republish. No code change. |

**v2.1.0 net:** No new project-side action beyond what Phase 12.2 already addressed.

#### v2.2.0 — beyond the enumerated #4709/#4902/#4666/#4770

| Fix / change (PR) | Project pattern | Classification | Rationale |
|---|---|---|---|
| #4761 — v3 WebSocket transport (frame-batched) | Phase 16.4 success criterion #4 + #8 (bandwidth pre/post measurement) | **16.4** | Explicitly deferred to 16.4 per CONTEXT.md `<deferred>`. Auto-applied on bump (TS SDK uses v3 by default per #4784); 16.4 owns measurement + verification. Informational record only. |
| #4784 — Update typescript SDK to use v3 websocket api | (Same as #4761) | **16.4** | Same — auto-applied; verification deferred. |
| #4729 — Add module ABI & API for `clear`ing tables (Table.clear()) | Project does not call `Table.clear()` (grep: 0 matches in `spacetimedb/src/`) | **16.4** | Available to GC reducers (`lobbyGc.ts`, `identityGc.ts`, `banAdmin.ts`) — Phase 16.4 owns adoption per CONTEXT.md. Not consumed today, no action this phase. |
| #4733 — Bytes-key B-tree index support | Project has no bytes-key columns (grep: 0 `t.bytes()` columns; identity columns use the dedicated identity type) | **out-of-scope** (informational) | Phase 16.4 success criterion #9 frames this as opportunistic adoption for future schema changes. Currently no bytes-key indexes exist to migrate. |
| #4649 — Export AuthCtx and JwtClaims in stdb/server typescript sdk | Project reducers do not currently consume `AuthCtx` / `JwtClaims` (grep: 0 matches in `spacetimedb/src/`; only present in vendor `node_modules/`) | **16.4** | Phase 16.4 owns adoption. Typed surface becomes available on bump but is opt-in — no current code uses these symbols, no breakage. |
| #4752 — Add useProcedure React hook and export procedures from codegen | Project does not currently call `useProcedure` (grep: 0 matches in `app/`, `components/`, `hooks/`; only vendor) | **16.4** | Phase 16.4 owns adoption. Typed hook available; opt-in. |
| #4721 — Add `enabled` option to useTable hook | 9 active `useTable(...)` call sites — all use the simple `useTable(tables.X)` form, no `{enabled}` option | **16.4** | New option available. Existing call sites continue to work unchanged. Adoption is opt-in; 16.4 territory if/when needed. |
| #4593 — Allow dropping empty tables during auto-migration | Schema migrations on republish | **covered (auto)** | Net positive; reduces manual migration friction on future schema-change publishes. No action today. |
| #4502 — Add `spacetime lock/unlock` | ROADMAP SC#6 | **OBSOLETE per D-13** — see dedicated section below | User owns manually, fully outside GSD scope. |
| #4769 — Show database names in `spacetime list` | CLI ergonomics | **out-of-scope** (informational) | No project consumption; nicer CLI UX. |
| #4885 — `spacetime publish --yes` takes optional arguments (skip-only-named-prompts) | `npm run spacetime:publish` flow — currently invokes `spacetime publish --server maincloud` | **out-of-scope** | The project's publish script doesn't pass `--yes` today, so the new fine-grained option doesn't change anything. No action. |
| #4688 — Astro + TypeScript template | Project uses Next.js | **out-of-scope** | Not relevant. |
| #4777 — [TS] Implement near-heap-limit termination | Maincloud-managed JS module runtime | **covered (auto)** | Server-side crash resilience. Net positive. |
| #4684 — Rotate V8 isolate on heap growth or fragmentation | (Same as #4777) | **covered (auto)** | Server-side. |
| #4891 / #4892 / #4890 — Snapshot / `metadata.toml` / pid file fdatasync hardening | Maincloud durability | **covered (auto)** | Server-side. Net positive on power-loss / crash scenarios. |
| #4903 — Unreal SDK overlapping subscriptions duplicate `OnInsert` fix | Project uses TS SDK, not Unreal | **out-of-scope** | |
| #4838 — Update defaultValue check to allow falsy/undefined values | Reducer parameters with default values | **covered (auto)** — investigate | If any reducer parameter declares a falsy default (`0`, `""`, `false`), the previous behavior may have skipped applying it. Worth a 16.4 spot-check, but no production breakage observed (full test suite passes — Plan 02). Recorded as **follow-up** for 16.4. |
| #4693 — Make Timestamp a FilterableValue | Reducers using `Timestamp` columns | **covered (auto)** | Enables future `WHERE timestamp > ?` query patterns. Project uses `Timestamp` in many tables (audit columns, scheduled reducers); adoption is opt-in for new query code. No action today. |
| #4702 / #4815 — Add more error context | Diagnostic only | **covered (auto)** | |

**v2.2.0 net:** Beyond the 4 enumerated fixes, **0 NEW findings requiring Phase 16.3 action**. 6 items are 16.4 territory (auto-applied capability or deferred adoption). All others are auto-fixes (covered) or out-of-scope. One item (#4838 default-value check change) is a `follow-up` candidate for a 16.4 spot-check, but the full test suite (197/197 PASS in Plan 02) provides structural sensor coverage — no production breakage observed.

### Net result

**No additional Phase 16.3 action items beyond what Plan 02 already executed and what Plan 04 will verify.**

- 0 NEW findings (no v2.0.4–v2.2.0 fix touches an unenumerated codebase pattern in a way that would require 16.3-scope work)
- 1 follow-up tag: #4838 default-value check — recommend 16.4 spot-check of any reducer parameters with falsy defaults
- Multiple 16.4-territory items confirmed (Table.clear, AuthCtx/JwtClaims, useTable {enabled}, useProcedure, v3 transport bandwidth measurement) — already in CONTEXT.md `<deferred>`, no new deferral entries needed

The Plan 02 working tree (manifest+lockfile+bindings) plus Plan 04's republish+SQL-probe gate is sufficient to close the SDK bump. Refactor work that *uses* v2.2.0 features remains parked in Phase 16.4 per CONTEXT.md `<domain>`.

## Delete-flag sweep (`spacetime delete --yes`) — D-12

**Sweep scope (per CONTEXT.md D-12):** `docs/`, `.claude/skills/`, `README.md`, onboarding docs. ~5 min cap.

**Sweep results:**

| Path | Status | Action |
|------|--------|--------|
| `.claude/skills/spacetimedb/references/how-to-guides.md:180` | Stale (bare `spacetime delete`) | **Patched** — appended `--yes` (now reads `spacetime delete <db-name> --server maincloud --yes`) |
| `scripts/post-publish.ts:4` | NO-OP | Comment references `spacetime publish --clear-database`, NOT `spacetime delete`. Unaffected by #4770 (which only changed `spacetime delete` confirmation default). |
| `scripts/nuke-test-data.ts:10` | NO-OP | Same `spacetime publish --clear-database` pattern as `post-publish.ts`. Unaffected. |
| `docs/smoke/contract.md:13` | NO-OP | Same `spacetime publish --clear-database` pattern. Unaffected. |
| `README.md` | 0 hits | No `spacetime delete` references in onboarding doc. |
| `docs/` (recursive markdown) | 0 hits | No `spacetime delete` references in feature docs. |
| Codebase (`*.ts`/`*.tsx`/`*.js`/`*.json`/`*.yml`/`*.yaml`, excluding `node_modules` / `.next` / `spacetimedb/dist` / `test/data` / `tmp` / `.planning`) | 0 hits | No `spacetime delete` invocations exist in tracked code (re-verified 2026-05-03; consistent with CONTEXT.md `<canonical_refs>` line 88 grep verified 2026-05-02). |

**Net result:** ROADMAP SC#4 closes via 1 doc patch (`how-to-guides.md`); no script changes required. `spacetime delete` is **not in production use** — `scripts/post-publish.ts` and `scripts/nuke-test-data.ts` use `spacetime publish --clear-database` instead, which is unaffected by #4770. The single stale skill-doc reference has been patched in place; the patch rides into Plan 04's Commit B alongside the manifest/lockfile/bindings work.

**Unexpected findings:** None. Sweep results match the pre-grep prediction in 16.3-03-PLAN.md `<interfaces>` table exactly.

## ROADMAP SC#6 (`spacetime lock`) — OBSOLETE per D-13

ROADMAP success criterion #6 (`spacetime lock` applied to maincloud production database) is
**OBSOLETE in this phase and will not be planned for in any subsequent phase**. Per CONTEXT.md
D-13, the user owns this operationally outside the GSD workflow. Downstream-phase planners
MUST NOT add `spacetime lock` work to 16.4 or any future phase plan. Do not surface this as
a "deferred" item, a "future phase will pick this up" item, or any equivalent — it is fully
out of GSD scope.
