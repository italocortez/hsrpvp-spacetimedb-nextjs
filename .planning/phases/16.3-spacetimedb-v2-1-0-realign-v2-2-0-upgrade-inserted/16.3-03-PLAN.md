---
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
plan: 03
type: execute
wave: 2
depends_on:
  - 16.3-01
files_modified:
  - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md
  - .claude/skills/spacetimedb/references/how-to-guides.md
autonomous: true
requirements: []
success_criteria_addressed:
  - "#4"
  - "#7"
decisions_addressed:
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "v2.0.4, v2.0.5, v2.1.0, v2.2.0 release notes read; codebase greps run for any pattern affected by fixes not already enumerated in ROADMAP success criteria; findings recorded as a short audit-notes section (D-11)"
    - "Codebase + docs swept for stale `spacetime delete` references that should now use `--yes` (the v2.2.0 confirm-by-default change, #4770); each occurrence either patched with `--yes` or explicitly classified as 'no-op — not in production use' with rationale (D-12)"
    - "Audit notes record explicit confirmation that `spacetime delete` is not invoked in any tracked code (consistent with CONTEXT.md `<canonical_refs>` line 88 grep verification 2026-05-02); only doc/skill prose mentions the command, and those are covered by the doc sweep"
    - "ROADMAP success criterion #6 (`spacetime lock`) is flagged OBSOLETE in audit notes per D-13 — user owns it manually outside the GSD workflow; this phase will NOT mark #6 done and downstream phases will NOT plan for it"
    - "Audit notes file path is referenced in Plan 04's Commit B message body alongside the drift note, so the durable artifact (D-15 spirit + D-11 output) is discoverable from git history"
    - "Phase scope is bounded to the SDK bump only — the 5 matched todos (CostSetDraft consolidation, lobby cost-set coverage, synergy half-assignment, team-builder synergy display, placeholder test conversion) are NOT folded into 16.3; they share keywords with `spacetimedb/` but are unrelated to the SDK bump and remain parked for natural assignment to Phase 17+ per CONTEXT.md `<deferred>` (D-14)"
  protocol:
    - "Output is a SINGLE new file: `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md`. This file gets staged into Plan 04's Commit B (not committed by this plan)."
    - "Doc/skill patches (e.g. `.claude/skills/spacetimedb/references/how-to-guides.md`) are made in-place AND staged into Plan 04's Commit B — they ride with the v2.2.0 bump, not separately, because they describe v2.2.0 behavior."
    - "If the changelog audit surfaces a code-side issue (a fix in v2.0.4–v2.2.0 that affects an unenumerated codebase pattern), DO NOT fix it in this plan. Record the finding in audit notes with a 'follow-up' tag and surface to the user. Phase 16.3 is bounded to manifest/lockfile/bindings/republish; refactor work is Phase 16.4 territory."
    - "Time budget: D-11 ~30–60 min reading + grepping; D-12 ~5 min sweep. If either blows past 90 min, surface to the user and stop — the criterion is a sanity check, not an exhaustive review."
    - "Do NOT add any `spacetime lock` content to the audit notes other than the explicit OBSOLETE flag for SC#6 per D-13. No 'future phase will pick this up' language. The user owns it manually outside GSD."
  artifacts:
    - path: ".planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md"
      provides: "D-11 changelog audit findings + D-12 delete-flag sweep findings + D-13 SC#6 obsolete flag"
      contains: "Changelog Audit (v2.0.4 → v2.2.0)"
    - path: ".claude/skills/spacetimedb/references/how-to-guides.md"
      provides: "Updated `spacetime delete` example with --yes flag (D-12)"
      contains: "spacetime delete <db-name> --server maincloud --yes"
  key_links:
    - from: "16.3-AUDIT-NOTES.md"
      to: ".planning/notes/spacetimedb-version-drift.md"
      via: "Cross-reference in audit-notes header"
      pattern: "spacetimedb-version-drift"
    - from: "16.3-AUDIT-NOTES.md"
      to: "Plan 04 Commit B message body"
      via: "Plan 04 cites the audit-notes path alongside the drift note (D-15 spirit)"
      pattern: "16.3-AUDIT-NOTES"
---

<objective>
Two read-mostly tasks that produce a single audit-notes artifact + at most one doc patch:
1. **D-11 changelog audit** — Read v2.0.4 / v2.0.5 / v2.1.0 / v2.2.0 release notes; grep the codebase for any pattern affected by fixes not already enumerated in ROADMAP success criteria; record findings as a short "audit notes" section.
2. **D-12 delete-flag sweep** — Sweep `docs/`, `.claude/skills/`, `README.md`, and onboarding docs for stale `spacetime delete` references that should now have `--yes`. Patch each occurrence in place, OR explicitly classify as no-op with rationale.

Both feed into a single output file `.planning/phases/.../16.3-AUDIT-NOTES.md` that Plan 04 stages into Commit B alongside the manifest/bindings changes.

Purpose: D-11 catches anything the upfront roadmap audit missed (v-fixes #4709/#4902/#4666/#4770 are enumerated but the v2.0.4 and v2.0.5 patches were not deeply audited). D-12 closes ROADMAP SC#4 either by patching docs or by explicitly recording "no-op — not in production use" with rationale. D-13 flags SC#6 obsolete in the same notes file so it is permanently surfaced for downstream phases.

Output: 1 audit-notes file + 1 doc patch (the known stale ref at `.claude/skills/spacetimedb/references/how-to-guides.md:180`); both staged but NOT committed (Plan 04 finalizes Commit B).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md
@.planning/notes/spacetimedb-version-drift.md
@.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-01-SUMMARY.md
@.claude/CLAUDE.md
@.claude/skills/spacetimedb/SKILL.md

<interfaces>
<!-- Pre-edit state extracted via grep — executor should NOT re-explore the codebase from scratch. -->

D-12 sweep targets — known starting points (per CONTEXT.md `<canonical_refs>` lines 84–88 + this plan's pre-grep):

| Path | Line | Current text | Action |
|------|------|-------------|--------|
| `.claude/skills/spacetimedb/references/how-to-guides.md` | 180 | `spacetime delete <db-name> --server maincloud` | Patch: append ` --yes` (this is the only stale doc ref found by pre-grep across tracked code) |
| `scripts/post-publish.ts` | 4 | `spacetime publish --clear-database` | NO-OP — `publish --clear-database` is unaffected by #4770 (which only changed `spacetime delete` confirmation default). Record in audit notes. |
| `scripts/nuke-test-data.ts` | 10 | `spacetime publish --clear-database` (same pattern) | NO-OP — same rationale. Record. |
| `docs/smoke/contract.md` | 13 | `spacetime publish --clear-database` (same pattern) | NO-OP — same rationale. Record. |

No `spacetime delete` invocations exist in tracked code (CONTEXT.md `<canonical_refs>` line 88, grep verified 2026-05-02). Re-verification grep is a task step.

D-11 release-note targets:
- v2.0.4: https://github.com/clockworklabs/SpacetimeDB/releases/tag/v2.0.4 (or whatever the canonical changelog source is — `.claude/skills/spacetimedb/SKILL.md` may have a pointer)
- v2.0.5: https://github.com/clockworklabs/SpacetimeDB/releases/tag/v2.0.5
- v2.1.0: already partially audited in Phase 12.2-RESEARCH.md SDK Changelog Audit section — re-read for completeness against THIS phase's scope
- v2.2.0: ROADMAP enumerates #4709 / #4902 / #4666 / #4770. Look for any other landed PR/fix in the v2.2.0 release notes not on that list.

Patterns the changelog audit should grep for in the codebase (catch unenumerated impacts):
- `iterate` + `delete` adjacency in reducers (Table.clear() candidate — flag for 16.4, do NOT touch here)
- `useTable(` calls (useTable({enabled}) adoption — flag for 16.4)
- `subscriptionBuilder` / `subscribeToAll` (anonymous-view / disconnect-cleanup behavior changes)
- bare `connection.reducers.X(` in React components (useProcedure adoption candidate — 16.4 territory)
- `JwtClaims` / `AuthCtx` strings (already-typed surface? 16.4 will adopt; just record presence/absence)
- Bytes-key B-tree usage patterns (#4733 — 16.4 territory)
- Anything else specific to v2.0.4 / v2.0.5 fixes the executor surfaces

D-13 — `spacetime lock` is OUT OF SCOPE for 16.3 entirely. Audit notes flag SC#6 as OBSOLETE. Do NOT search for `spacetime lock` references in the codebase as a sweep target — the user owns it manually outside GSD per D-13.

ROADMAP SC#7 wording (line 304): "Note .planning/notes/spacetimedb-version-drift.md referenced in the phase plan and final commit message so future SDK bumps catch the same trap." — this audit-notes file's header should also reference the drift note (consistent with the spirit of D-15) so the audit findings are linked back to the lesson narrative.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-11 changelog audit + create 16.3-AUDIT-NOTES.md skeleton (D-11, D-13, D-15)</name>
  <files>.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 16.3" success criteria (lines 297–304 — enumerated v2.2.0 fixes #4709/#4902/#4666/#4770)
    - .planning/notes/spacetimedb-version-drift.md (header reference per D-15 spirit)
    - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md (D-11, D-13)
    - .claude/skills/spacetimedb/SKILL.md (any pointer to canonical changelog source)
    - Phase 12.2 RESEARCH.md SDK Changelog Audit section (if it exists — for v2.1.0 audit completeness reference)
  </read_first>
  <action>
**Step 1 — Read v2.0.4, v2.0.5, v2.1.0, v2.2.0 release notes (D-11):**
Use the WebFetch tool with the canonical changelog URL pattern (per `.claude/skills/spacetimedb/SKILL.md` if it has one; otherwise GitHub releases page for clockworklabs/SpacetimeDB). For each version, extract:
- Bug fixes (PR/issue numbers)
- Behavior changes (especially defaults, like #4770)
- New features (mostly informational; v2.2.0 features are 16.4 territory)
- Migration notes / breaking changes

Record the source URL(s) in the audit-notes header.

**Step 2 — Grep codebase for unenumerated impact patterns:**
Run targeted greps to catch fixes NOT already enumerated in ROADMAP SC. Examples (executor expands per release-note findings):

```bash
# v2.0.x fixes — anything affecting reducer error patterns, subscription cleanup, etc.
grep -rn "subscriptionBuilder" app/ components/ hooks/ src/ 2>/dev/null | head -20
grep -rn "useTable(" app/ components/ hooks/ 2>/dev/null | head -20
grep -rn "anonymousView" spacetimedb/src/ 2>/dev/null | head -20

# v2.2.0 unenumerated patterns (catch anything not on the #4709/#4902/#4666/#4770 list)
grep -rn "JwtClaims\|AuthCtx" spacetimedb/src/ 2>/dev/null | head -20
grep -rn "Table.clear" spacetimedb/src/ 2>/dev/null | head -20

# Bytes-key B-tree adoption candidates (#4733 — informational only, 16.4 territory)
grep -rn "btree.*Bytes\|Bytes.*btree" spacetimedb/src/ 2>/dev/null | head -20
```

For each grep result, classify:
- **Already covered** by ROADMAP SC#1–#5 + #7
- **16.4 territory** (refactor work — flag in audit notes, do NOT fix here)
- **Out of scope entirely** (no impact)
- **NEW finding** (something the v2.0.4–v2.2.0 fixes touch that wasn't on the radar — surface to user before continuing)

**Step 3 — Write `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` with this exact skeleton:**

```markdown
---
title: Phase 16.3 audit notes (changelog + delete-flag sweep)
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
date: <today's date>
references:
  - .planning/notes/spacetimedb-version-drift.md
  - .planning/ROADMAP.md (Phase 16.3, lines 289–308)
  - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md (D-11, D-12, D-13)
---

# Phase 16.3 audit notes

This file is the durable output of Plan 03's two read-mostly tasks. It rides into Commit B
of this phase (alongside the manifest bumps + bindings regen) so future SDK-bump readers
find both the lesson (`spacetimedb-version-drift.md`) AND the v2.0.4 → v2.2.0 audit findings
in one place.

## Changelog audit (v2.0.4 → v2.2.0) — D-11

**Sources read:**
- v2.0.4: <URL>
- v2.0.5: <URL>
- v2.1.0: <URL>
- v2.2.0: <URL>

**Already enumerated in ROADMAP SC:**
- #4709 (codegen reorder, cosmetic) — ROADMAP SC#3
- #4902 (autoinc next-value advance) — ROADMAP SC#5
- #4666 (PK migration / `st_table.table_primary_key`) — ROADMAP SC#5
- #4770 (`spacetime delete --yes` default) — ROADMAP SC#4 (see delete-flag sweep below)

**Unenumerated findings:**
<For each grep / changelog finding the executor surfaced:>
- **<Fix or behavior>**: <one-line description> — **classification**: [covered | 16.4 | out-of-scope | NEW finding] — **action**: <follow-up phase or no-op>

**Net result:** [no additional impact | N findings recorded for 16.4 follow-up | NEW finding requires user discussion before proceeding]

## Delete-flag sweep (`spacetime delete --yes`) — D-12

(Filled in by Task 2.)

## ROADMAP SC#6 (`spacetime lock`) — OBSOLETE per D-13

ROADMAP success criterion #6 (`spacetime lock` applied to maincloud production database) is
**OBSOLETE in this phase and will not be planned for in any subsequent phase**. Per CONTEXT.md
D-13, the user owns this operationally outside the GSD workflow. Downstream-phase planners
MUST NOT add `spacetime lock` work to 16.4 or any future phase plan. Do not surface this as
a "deferred" item, a "future phase will pick this up" item, or any equivalent — it is fully
out of GSD scope.
```

The skeleton's "Delete-flag sweep" section is intentionally empty — Task 2 fills it.
  </action>
  <verify>
    <automated>test -f .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md && grep -q "OBSOLETE per D-13" .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md && grep -q "spacetimedb-version-drift" .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` exists
    - File header references `.planning/notes/spacetimedb-version-drift.md` (D-15 spirit)
    - "Changelog audit (v2.0.4 → v2.2.0) — D-11" section present with at least the 4 enumerated v2.2.0 fixes (#4709, #4902, #4666, #4770) categorized as "covered"
    - "Unenumerated findings" subsection populated with the result of the codebase greps (either explicit "no additional impact" line, OR enumerated findings tagged 16.4 / out-of-scope / NEW)
    - "ROADMAP SC#6 (`spacetime lock`) — OBSOLETE per D-13" section present with the obsolete-flag prose verbatim from the skeleton
    - File does NOT contain any "future phase will pick up `spacetime lock`" / "deferred to 16.4" / "deferred to user" wording (D-13 absolute)
    - "Delete-flag sweep" section header exists but is empty placeholder for Task 2
    - No `git commit` invoked — file remains in working tree untracked
  </acceptance_criteria>
  <done>Audit-notes file created with changelog audit findings (D-11) + SC#6 obsolete flag (D-13) + drift-note cross-reference (D-15 spirit). Delete-flag sweep section is empty placeholder for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 delete-flag sweep + patch known stale ref + finalize audit notes (D-12)</name>
  <files>.claude/skills/spacetimedb/references/how-to-guides.md, .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md</files>
  <read_first>
    - .claude/skills/spacetimedb/references/how-to-guides.md (lines 170–185 — confirm the bare `spacetime delete` example at line 180)
    - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md `<canonical_refs>` (D-12 sweep scope: docs/, .claude/skills/, README.md, onboarding docs)
  </read_first>
  <action>
**Step 1 — Re-verify the codebase grep (5-min sweep, D-12):**
Run the canonical sweep grep to confirm no `spacetime delete` invocations exist outside docs/skills:

```bash
grep -rn "spacetime delete" --include="*.md" docs/ README.md .claude/skills/ 2>/dev/null
grep -rn "spacetime delete" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.yml" --include="*.yaml" scripts/ tools/ . --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=spacetimedb/dist --exclude-dir=test/data --exclude-dir=tmp --exclude-dir=.planning 2>/dev/null
```

Expected results:
- First grep (docs/skills): 1 hit at `.claude/skills/spacetimedb/references/how-to-guides.md:180` (the known stale ref)
- Second grep (code): 0 hits (CONTEXT.md `<canonical_refs>` line 88 grep was verified 2026-05-02; this re-verifies)

If anything else surfaces, classify it (NO-OP if it's a `spacetime publish --clear-database` false positive; PATCH if it's a real `spacetime delete` invocation; surface to user if it's something unexpected).

**Step 2 — Patch the known stale ref:**
Use the Edit tool on `.claude/skills/spacetimedb/references/how-to-guides.md` to replace exactly:
```
spacetime delete <db-name> --server maincloud
```
with:
```
spacetime delete <db-name> --server maincloud --yes
```
This adds the `--yes` flag per the v2.2.0 confirm-by-default change (#4770). The skill doc serves as a reference for future onboarding; updating it ensures future readers don't paste the bare form.

Optional: add a one-line note above or below explaining why `--yes` is required post-2.2.0. Use judgment — if the surrounding doc context already covers v2.2.0 nuances, no extra prose is needed. Time-box at 5 min total per D-12.

**Step 3 — Fill in the "Delete-flag sweep" section of `16.3-AUDIT-NOTES.md`:**
Append this section (replacing the empty placeholder Task 1 left):

```markdown
## Delete-flag sweep (`spacetime delete --yes`) — D-12

**Sweep scope (per CONTEXT.md D-12):** `docs/`, `.claude/skills/`, `README.md`, onboarding docs. ~5 min cap.

**Sweep results:**

| Path | Status | Action |
|------|--------|--------|
| `.claude/skills/spacetimedb/references/how-to-guides.md:180` | Stale (bare `spacetime delete`) | **Patched** — appended `--yes` |
| `scripts/post-publish.ts:4` | NO-OP | Comment references `spacetime publish --clear-database`, NOT `spacetime delete`. Unaffected by #4770. |
| `scripts/nuke-test-data.ts:10` | NO-OP | Same pattern as post-publish.ts. Unaffected. |
| `docs/smoke/contract.md:13` | NO-OP | Same pattern. Unaffected. |
| Codebase (grep `--include="*.ts" --include="*.tsx" --include="*.js"`) | 0 hits | No `spacetime delete` invocations exist in tracked code. |

**Net result:** ROADMAP SC#4 closes via 1 doc patch (`how-to-guides.md`); no script changes required. `spacetime delete` is **not in production use** — `scripts/post-publish.ts` and `scripts/nuke-test-data.ts` use `spacetime publish --clear-database` instead, which is unaffected by #4770.
```

If Step 1 surfaced unexpected results, expand the table to include them with appropriate classification.
  </action>
  <verify>
    <automated>grep -q "spacetime delete <db-name> --server maincloud --yes" .claude/skills/spacetimedb/references/how-to-guides.md && grep -q "Delete-flag sweep" .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md && grep -q "Patched.*--yes" .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spacetime delete <db-name> --server maincloud --yes" .claude/skills/spacetimedb/references/how-to-guides.md` returns 1
    - `grep -c "spacetime delete <db-name> --server maincloud$" .claude/skills/spacetimedb/references/how-to-guides.md` returns 0 (bare form gone)
    - `16.3-AUDIT-NOTES.md` "Delete-flag sweep" section is filled with the result table
    - "Delete-flag sweep" section explicitly classifies `scripts/post-publish.ts`, `scripts/nuke-test-data.ts`, and `docs/smoke/contract.md` as NO-OP with `--clear-database vs delete` rationale
    - "Net result" line records "ROADMAP SC#4 closes via 1 doc patch" or equivalent explicit closure
    - `git status` shows 2 modifications: `16.3-AUDIT-NOTES.md` (new file from Task 1, edited in this task) and `.claude/skills/spacetimedb/references/how-to-guides.md`
    - No `git commit` invoked — both files remain in working tree
  </acceptance_criteria>
  <rollback>
    To revert the doc patch: `git checkout -- .claude/skills/spacetimedb/references/how-to-guides.md`.
    To remove the audit-notes file entirely: `rm .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md`.
    Plan 01's drift-fix commit is unaffected by either rollback.
  </rollback>
  <done>D-12 sweep complete: 1 doc patch applied, 3 NO-OP classifications recorded with rationale, 0 unexpected findings (or surfaced to user if any). Audit notes finalized. ROADMAP SC#4 closed. No commit (Plan 04 finalizes Commit B).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external changelog (GitHub releases) → audit notes | WebFetch reads release-note prose; trust is provenance (GitHub URL recorded in audit notes) |
| codebase grep → classification | Grep results are the source of truth for "what patterns exist"; classification is the executor's judgment per D-11 categories |
| skill doc edit → future onboarding | The patched `how-to-guides.md` is consumed by future readers as authoritative; bad patches mislead onboarding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-03-01 | Information Disclosure | WebFetch on release-note URLs leaks user identity to GitHub via fetch headers | accept | Public release notes; no authenticated endpoint; same trust surface as any web research the user does manually. |
| T-16.3-03-02 | Tampering | A "NEW finding" from D-11 audit is silently classified as 16.4 territory when it actually requires Phase 16.3 action | mitigate | Plan 03 protocol explicitly says "if the changelog audit surfaces a code-side issue, DO NOT fix it in this plan. Record the finding and surface to the user." NEW findings are flagged in the audit notes table for explicit user review during /gsd-verify-work. |
| T-16.3-03-03 | Spoofing | A doc patch introduces an incorrect `--yes` flag placement that makes the example fail when copy-pasted | mitigate | The patch is a single trailing flag append in canonical CLI position. Acceptance grep verifies the exact resulting string. CLI accepts trailing flags. |
| T-16.3-03-04 | Repudiation | The audit-notes file is amended after Plan 04's commit, masking the at-commit-time findings | accept | Audit notes are committed in Commit B; future amendments would themselves be commits. Standard git history applies. |
| T-16.3-03-05 | Tampering | D-13 SC#6 obsolete flag is silently weakened ("deferred" instead of "obsolete") | mitigate | Acceptance grep checks for the literal "OBSOLETE per D-13" string. Plan 03 protocol explicitly forbids "deferred"/"future phase" wording for SC#6. |
</threat_model>

<verification>
1. `test -f .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` succeeds
2. `grep -c "OBSOLETE per D-13" .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` returns ≥1
3. `grep -c "spacetimedb-version-drift" .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` returns ≥1
4. `grep -c "spacetime delete <db-name> --server maincloud --yes" .claude/skills/spacetimedb/references/how-to-guides.md` returns 1
5. Audit-notes file contains a "Delete-flag sweep" section with at least 1 "NO-OP" classification row
6. Audit-notes file does NOT contain any "deferred" / "future phase" / "16.4 will pick up" prose for `spacetime lock` (D-13)
7. No `git commit` was invoked by this plan
</verification>

<success_criteria>
- ROADMAP success criterion #4 closed via either patch (the 1 stale doc ref) OR explicit "no-op — `spacetime delete` not in use" classification (per D-12 wording)
- ROADMAP success criterion #7 partially seeded: audit-notes file references the drift note, ensuring Plan 04's Commit B body cites BOTH artifacts (drift note + audit notes)
- D-11 satisfied: changelog audit findings recorded in audit notes
- D-12 satisfied: codebase + docs swept; 1 patch applied; classifications recorded
- D-13 satisfied: SC#6 flagged OBSOLETE in audit notes; no future-phase planning for `spacetime lock`
- D-15 spirit satisfied: audit-notes header cross-references the drift note
- No commit, no push, no scope creep into Plan 02's manifest/bindings territory
</success_criteria>

<output>
After completion, create `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-03-SUMMARY.md` recording: count of unenumerated findings (and their classification), list of doc files patched (and the exact replacement), explicit confirmation of 0 NEW findings (or list NEW findings if any were surfaced), and confirmation that no commit/push was made.
</output>
