---
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
plan: 03
subsystem: docs
tags: [spacetimedb, audit-notes, changelog, doc-patch, delete-flag-sweep, ROADMAP-SC4, D-11, D-12, D-13]

# Dependency graph
requires:
  - phase: 16.3-01
    provides: drift-fix Commit A baseline (^2.1.0 floor); audit-notes header references the drift narrative consistent with that commit's lesson
  - phase: 16.3-02
    provides: working-tree v2.2.0 manifest/lockfile/bindings changes (uncommitted); Plan 03 outputs ride into Plan 04's Commit B alongside that uncommitted state
provides:
  - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md (new file, uncommitted in working tree) — D-11 changelog audit findings + D-12 sweep results + D-13 SC#6 OBSOLETE flag
  - .claude/skills/spacetimedb/references/how-to-guides.md (uncommitted modification in working tree) — `spacetime delete <db-name> --server maincloud --yes` (added `--yes` per #4770)
  - Closure of ROADMAP success criterion #4 — 1 doc patch, 3 NO-OP classifications, 0 production-code invocations of `spacetime delete`
  - Seed for ROADMAP success criterion #7 — audit-notes header cross-references `.planning/notes/spacetimedb-version-drift.md` (D-15 spirit) so Plan 04's Commit B body can cite both artifacts
affects:
  - 16.3-04 (Plan 04 stages both Plan 03 outputs into Commit B alongside Plan 02's manifest/lockfile/bindings work; commit body cites BOTH the drift note AND the audit-notes file)
  - All future SDK bumps (the v2.0.4 → v2.2.0 audit lives at a discoverable path; future bump audits can `git log --grep=16.3-AUDIT-NOTES` to find precedent)
  - Phase 16.4 (1 follow-up tag — #4838 default-value check spot-check — recorded in audit notes; Phase 16.4 owns adoption of v2.2.0 features so the follow-up is naturally absorbed there)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Discrete audit-notes artifact per SDK-bump phase (`{phase}-AUDIT-NOTES.md`) — captures changelog audit + doc sweep findings as a durable, git-discoverable record beyond the bump commit itself; complements `.planning/notes/spacetimedb-version-drift.md` (per-bump narrative + cross-bump lesson, respectively)"
    - "Doc patches that describe behavior of the new version ride with the bump commit, not separately — they are part of the same logical 'we are now on v2.2.0' state change. The how-to-guides.md `--yes` patch is staged into Commit B alongside the manifest/bindings work."
    - "`spacetime delete` is not in production use anywhere on this codebase; the only references are documentation prose. ROADMAP success criterion #4 closes via doc patch, not script change."

key-files:
  created:
    - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md — Phase 16.3 changelog audit (v2.0.4 → v2.2.0) + delete-flag sweep + SC#6 OBSOLETE flag (uncommitted; rides Commit B in Plan 04)
  modified:
    - .claude/skills/spacetimedb/references/how-to-guides.md — line 180: appended ` --yes` to the `spacetime delete <db-name> --server maincloud` example (uncommitted; rides Commit B in Plan 04)

key-decisions:
  - "Sourced changelogs from official `https://github.com/clockworklabs/SpacetimeDB/releases/tag/vX.Y.Z` URLs via curl + JSON-extract of the `body` field; recorded all 4 source URLs in the audit-notes header so the provenance is git-discoverable. WebFetch fallback was not needed — the curl path returned full release-note bodies."
  - "Re-used Phase 12.2's prior v2.0.3 → v2.1.0 audit (`12.2-RESEARCH.md` lines 264–292) rather than re-doing the v2.1.0 grep work from scratch — the prior conclusions still hold against the current codebase, and re-reading was sufficient. Audit notes explicitly cite the prior file."
  - "Classified each grep finding into 4 categories (covered / 16.4 / out-of-scope / NEW) per Plan 03's protocol. Net: 0 NEW findings, 1 follow-up tag (#4838 default-value check) recorded for Phase 16.4 spot-check, 6 confirmed 16.4 territory items, all others auto-applied or out-of-scope."
  - "Made NO commits for the Plan 03 outputs — they remain uncommitted in the working tree per the no-commit invariant. Plan 04 finalizes Commit B by staging Plan 02's uncommitted v2.2.0 work + Plan 03's audit-notes + doc patch together."

patterns-established:
  - "Per-bump audit-notes artifact pattern: future SDK-bump phases should produce a `{phase}-AUDIT-NOTES.md` capturing changelog cross-reference + targeted greps + classification per the same 4-category scheme used here. Discoverability via `git log --grep=AUDIT-NOTES` improves over time as more bumps land."
  - "Doc-patch-as-bump-rider pattern: when a CLI/API behavior change requires a documentation update (here: `spacetime delete --yes` post-#4770), the patch is staged into the same commit as the version bump itself, not a separate later commit — keeps the audit trail aligned."

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-05-03
---

# Phase 16.3 Plan 03: Changelog audit + delete-flag sweep + audit-notes file Summary

**Read v2.0.4 / v2.0.5 / v2.1.0 / v2.2.0 release notes from official GitHub Releases, ran 8 targeted greps against the codebase to catch unenumerated impacts (0 NEW findings; 1 follow-up tag for #4838), patched the single stale `spacetime delete` skill-doc reference to add `--yes`, and produced a discoverable `16.3-AUDIT-NOTES.md` artifact that rides into Plan 04's Commit B alongside the v2.2.0 working-tree state — closing ROADMAP SC#4 via 1 doc patch + 3 NO-OP classifications, with SC#6 (`spacetime lock`) flagged OBSOLETE per D-13.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-05-03 (immediately after orchestrator dispatch, post-Plan-02 SUMMARY commit `598c6b9`)
- **Completed:** 2026-05-03
- **Tasks:** 2 (both completed in single executor session, no checkpoints)
- **Files created:** 1 (`16.3-AUDIT-NOTES.md`)
- **Files modified:** 1 (`.claude/skills/spacetimedb/references/how-to-guides.md`)
- **Commits made:** 1 (this SUMMARY only — Plan 03 outputs remain uncommitted per no-commit invariant)

## Accomplishments

- **D-11 changelog audit complete:** Read all 4 release notes from canonical GitHub Releases URLs (recorded as provenance in audit-notes header). Cross-referenced against 8 targeted codebase greps spanning `subscriptionBuilder`, `useTable`, `JwtClaims`/`AuthCtx`, `Table.clear`, `useProcedure`, `withConfirmedReads`, `Range`/`Bound`, `anonymousView`, `scheduledTable`/`ScheduleAt`, and bytes-key/`t.bytes()` patterns. Net: **0 NEW findings**, 1 follow-up tag (#4838 default-value check), 6 16.4-territory items confirmed, all others auto-applied or out-of-scope.
- **D-12 delete-flag sweep complete:** Re-verified codebase (0 `spacetime delete` invocations in `*.ts`/`*.tsx`/`*.js`/`*.json`/`*.yml`/`*.yaml`), `docs/` (0 hits), and `README.md` (0 hits). Found and patched the single stale doc reference in `.claude/skills/spacetimedb/references/how-to-guides.md:180`. Recorded 3 NO-OP classifications (`scripts/post-publish.ts`, `scripts/nuke-test-data.ts`, `docs/smoke/contract.md`) with `--clear-database vs delete` rationale.
- **D-13 SC#6 OBSOLETE flag captured:** Audit notes contain the exact "OBSOLETE per D-13" prose verbatim per Plan 03's skeleton specification — including the explicit prohibition on "deferred"/"future phase will pick this up" wording for `spacetime lock`. Downstream-phase planners cannot mistake this for a deferred item.
- **D-15 spirit honored:** Audit-notes header references `.planning/notes/spacetimedb-version-drift.md` so the audit findings + drift narrative are linked. Plan 04's Commit B message body can cite both artifacts.
- **No-commit invariant honored:** Both Plan 03 outputs (`16.3-AUDIT-NOTES.md` + `how-to-guides.md` patch) remain uncommitted in the working tree, alongside Plan 02's uncommitted v2.2.0 work. Plan 04 will stage all of it into Commit B.
- **Plan 02 working-tree state untouched:** None of Plan 02's uncommitted files (manifests, lockfiles, bindings, `bundle.js`, `node_modules`) were modified by this executor. Verified via `git status --short` after edits.

## Task Commits

Plan 03 makes NO commits for the audit-notes artifact or the doc patch — both remain uncommitted in the working tree per the no-commit invariant. Plan 04 owns Commit B finalization.

The only commit made by this plan is the SUMMARY.md commit, scoped via direct `git add <SUMMARY-path>` + `git commit -m`:

1. **Task 1: D-11 changelog audit + create 16.3-AUDIT-NOTES.md skeleton** — uncommitted in working tree (this executor)
2. **Task 2: D-12 delete-flag sweep + patch known stale ref + finalize audit notes** — uncommitted in working tree (this executor)

**Plan metadata commit:** SUMMARY.md only, scoped to a single path (no `git add .` / `-A`).

## Files Created/Modified

All Plan 03 outputs are uncommitted in the working tree (per no-commit invariant — Plan 04 finalizes):

- **CREATED** `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` — ~140 lines. Contains:
  - YAML frontmatter with provenance references (drift note, ROADMAP, CONTEXT.md, Phase 12.2 RESEARCH.md)
  - "Changelog audit (v2.0.4 → v2.2.0) — D-11" section with 4 source URLs and 4 sub-tables (one per version) classifying each fix into covered/16.4/out-of-scope/NEW
  - "Net result" subsection: 0 NEW findings, 1 follow-up tag, 6 16.4 territory items
  - "Delete-flag sweep (`spacetime delete --yes`) — D-12" section with 7-row results table (1 patched, 3 NO-OP, 3 zero-hit confirmations)
  - "ROADMAP SC#6 (`spacetime lock`) — OBSOLETE per D-13" section with the verbatim obsolete-flag prose from Plan 03's skeleton
- **MODIFIED** `.claude/skills/spacetimedb/references/how-to-guides.md` — line 180: appended ` --yes`. Before: `spacetime delete <db-name> --server maincloud`. After: `spacetime delete <db-name> --server maincloud --yes`. Single-line diff; no other prose changes.

## Decisions Made

- **Source-of-truth for changelogs:** GitHub Releases API (`https://api.github.com/repos/clockworklabs/SpacetimeDB/releases/tags/vX.Y.Z`) via `rtk proxy curl` (the rtk wrapper truncated raw `curl` output in this environment, but `rtk proxy curl ...` returned full bodies). Extracted the `body` field via `node -e "JSON.parse(...)..."`. Recorded the public release-page URLs (`releases/tag/vX.Y.Z`) in the audit-notes header rather than the API URLs — the page URLs are what humans cite.
- **Re-used the Phase 12.2 v2.1.0 audit instead of re-doing it.** `12.2-RESEARCH.md` lines 264–292 already enumerated v2.0.3 → v2.1.0 changes against project patterns. Re-reading those conclusions against the current codebase confirmed they still hold (e.g., `withConfirmedReads(false)` still in 14 places; 2 anonymous views still wired). The audit-notes file cites the prior research file rather than duplicating its content.
- **Classification of #4838 (defaultValue check change) as `follow-up` rather than `covered`.** The fix changes how reducer parameter default values are applied for falsy/undefined values. The full vitest suite (197/197) passing in Plan 02 provides structural sensor coverage — no observable production breakage. But because the fix touches a code path used implicitly by reducers, it warrants a 16.4 spot-check on any reducer parameters with falsy defaults. Recorded as `follow-up` in the audit notes; not blocking 16.3.
- **No fixes applied to surfaced findings.** Per Plan 03's `<must_haves>.protocol` line 34 ("If the changelog audit surfaces a code-side issue ... DO NOT fix it in this plan"). The 1 follow-up tag (#4838) is recorded only — no code changes made by this executor.
- **Did NOT touch any of Plan 02's uncommitted files.** Verified before and after by `git status --short`. The Plan 02 working tree (5 in-scope paths + `bundle.js` side-effect + `node_modules` drift) is exactly as Plan 02's SUMMARY documented it. Plan 03's edits land entirely in the 2 declared `files_modified` paths.
- **Did NOT modify STATE.md or ROADMAP.md.** Per the prompt's `<critical_other_constraints>` and `<success_criteria>`. Pre-existing ROADMAP.md drift (Plan 02 progress 1/4 → 2/4) was already present before Plan 03 dispatched; not authored by this executor.
- **Did NOT add any `spacetime lock` content beyond the OBSOLETE flag.** Per D-13 absolute. The audit notes contain exactly one `spacetime lock` reference (in the "ROADMAP SC#6 OBSOLETE per D-13" section), framed as a prohibition on the forbidden wording — never as a deferred or future-phase item.

## Deviations from Plan

### None

The plan was executed exactly as written. Both tasks completed in single-pass execution with no auto-fixes applied (per Plan 03's protocol forbidding code-side fixes during the audit).

**Notable non-deviations** (worth recording for the audit trail, but each was anticipated by the plan):

- **Curl/rtk wrapper interaction.** The user's environment uses an `rtk` shell hook that wraps standard CLI commands; bare `curl` returned a 28-KB JSON body that JSON.parse rejected mid-line because rtk's pretty-printer truncated the middle. Using `rtk proxy "curl ..."` (per the user's RTK.md note in `~/.claude/RTK.md`) returned full unaltered bodies. This is a known wrapper limitation, not a plan deviation. The 4 release-note URLs are correctly captured in the audit-notes header.
- **Plan 02 `bundle.js` side-effect.** Visible in `git status` but explicitly recorded as out-of-scope by the prompt. Not touched.
- **Pre-existing `node_modules/` tracking drift.** Same — visible noise, out of scope, not touched.

---

**Total deviations:** 0

**Impact on plan:** None. Plan 03 executed exactly as designed. The audit produced 0 NEW findings, which is itself the meaningful outcome — it confirms Phase 16.3's bounded scope (manifest/lockfile/bindings/republish only) is sufficient and the v2.0.4 → v2.2.0 changelog cross-reference does not surface any unenumerated codebase impact.

## Issues Encountered

- **None blocking.** The audit found 1 follow-up item (#4838) for Phase 16.4 spot-check — not blocking for 16.3, fully recorded in audit notes.

## User Setup Required

None. Plan 03 is purely documentation work + a doc patch — no environment changes, no CLI changes, no maincloud changes.

## Next Phase Readiness

- **Plan 04 (republish + SQL probes + finalize Commit B):** unblocked. Plan 04's `<files_modified>` frontmatter must include both Plan 03 outputs (`.planning/phases/16.3-.../16.3-AUDIT-NOTES.md` and `.claude/skills/spacetimedb/references/how-to-guides.md`) when staging Commit B; verify Plan 04 PLAN already accounts for them. Plan 04 also retains the bundle.js decision flagged by Plan 02's SUMMARY Deviation §2 (include in Commit B vs revert) — Plan 03's outputs are independent of that decision.
- **Commit B citation requirement:** Plan 04's Commit B message body MUST cite BOTH `.planning/notes/spacetimedb-version-drift.md` AND `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` per ROADMAP SC#7 wording (line 304) + D-15 spirit + this plan's `<must_haves>.truths` line 29. The audit-notes file's header cross-reference already aligns the two artifacts.
- **Branch / push hygiene:** clean. `feature_nath_claude` HEAD is still at `598c6b9` (Plan 02 SUMMARY); the only new commit landing from Plan 03 is this SUMMARY commit itself (post-self-check). No remote push has happened; D-08 push gate remains owned by Plan 04.
- **STATE.md / ROADMAP.md:** intentionally NOT modified by this executor (per `<critical_other_constraints>`). The orchestrator owns those writes after the wave completes.

## Threat Flags

None. Plan 03 introduces no new security-relevant surface (no network endpoints, no auth paths, no file access patterns at trust boundaries). The doc patch is a single trailing-flag append; the audit-notes file is markdown prose with no executable content.

---

## Self-Check: PASSED

**Files verified present:**
- FOUND: `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-03-SUMMARY.md` (this file)
- FOUND: `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-AUDIT-NOTES.md` (uncommitted; visible as `??` in `git status`)
- FOUND: `.claude/skills/spacetimedb/references/how-to-guides.md` (uncommitted modification; visible as ` M` in `git status`)

**Acceptance criteria status (from 16.3-03-PLAN.md `<verification>`):**
- AC1 `test -f .../16.3-AUDIT-NOTES.md` succeeds — PASS (file present)
- AC2 `grep -c "OBSOLETE per D-13" 16.3-AUDIT-NOTES.md` returns ≥1 — PASS (returns 2: dedicated section heading + reference in v2.2.0 audit table)
- AC3 `grep -c "spacetimedb-version-drift" 16.3-AUDIT-NOTES.md` returns ≥1 — PASS (returns 2: frontmatter `references:` + intro paragraph)
- AC4 `grep -c "spacetime delete <db-name> --server maincloud --yes" how-to-guides.md` returns 1 — PASS
- AC5 Audit-notes file contains a "Delete-flag sweep" section with at least 1 "NO-OP" classification row — PASS (3 NO-OP rows)
- AC6 Audit-notes file does NOT contain "deferred" / "future phase will pick up" prose claiming `spacetime lock` is deferred — PASS (the only references to those phrases are in the OBSOLETE-section prose explicitly forbidding them; no claims that `spacetime lock` is deferred exist anywhere in the file)
- AC7 No `git commit` was invoked by this plan for the audit-notes or the doc patch — PASS (`git log --oneline -1` shows HEAD still at `598c6b9` until the SUMMARY commit lands)

**Plan-level success criteria status (from 16.3-03-PLAN.md `<success_criteria>`):**
- ROADMAP SC#4 closed — PASS (1 doc patch + 3 NO-OP classifications + 0 production-code invocations)
- ROADMAP SC#7 partially seeded — PASS (audit-notes header cross-references drift note; Plan 04 owns the commit-message citation)
- D-11 satisfied — PASS (changelog audit findings recorded across all 4 versions)
- D-12 satisfied — PASS (codebase + docs swept; 1 patch applied; classifications recorded)
- D-13 satisfied — PASS (SC#6 flagged OBSOLETE; no future-phase planning for `spacetime lock`)
- D-15 spirit satisfied — PASS (audit-notes header cross-references the drift note)
- No commit, no push, no scope creep into Plan 02's manifest/bindings territory — PASS (`git status` confirms only the 2 declared paths from Plan 03 changed beyond Plan 02's working tree state)

---

*Phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted*
*Completed: 2026-05-03*
