---
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
plan: 02
type: execute
wave: 2
depends_on:
  - 16.3-01
files_modified:
  - spacetimedb/package.json
  - spacetimedb/package-lock.json
  - package.json
  - package-lock.json
  - src/module_bindings/
autonomous: true
requirements: []
success_criteria_addressed:
  - "#2"
  - "#3"
decisions_addressed:
  - D-01
  - D-02
  - D-03
  - D-09
  - D-10

must_haves:
  truths:
    - "Both `spacetimedb/package.json` and root `package.json` declare spacetimedb: ^2.2.0; both lockfiles re-resolved via plain `npm install` to 2.2.x — closes ROADMAP success criterion #2 (D-01 Commit B core)"
    - "src/module_bindings/ regenerated via `npm run generate` (cargo path per D-10, NOT spacetime:generate); index.ts header stamp reflects 2.2.x cli version (post-#4709 codegen)"
    - "`npm run test:typecheck` exits 0 against the regenerated bindings (D-09 — typecheck pass = sufficient evidence #4709 codegen reorder is cosmetic)"
    - "Full `npm test` exits 0 against the bumped manifests + regenerated bindings (D-09 — full suite is the structural sensor for any non-cosmetic codegen change)"
    - "Bindings regen lands STAGED in this plan but is NOT committed yet — Commit B is finalized in Plan 04 after maincloud republish + SQL probe verification (D-02 atomicity, D-04 republish-before-push, D-08 push gate)"
  protocol:
    - "Stage order is forced by D-02 atomicity: bindings regen MUST travel with the manifest+lockfile bumps in the same commit (no transient state where module is on 2.2.0 but bindings are on 2.1.0). All 4 manifest/lockfile edits + the `src/module_bindings/` regen happen in this plan; the actual `git commit` is deferred to Plan 04."
    - "Use `npm run generate` (root, cargo-based) NOT `spacetime:generate` (CLI-based) per D-10 — the cargo path is the project's canonical workflow and runs `npm --prefix spacetimedb install` first as a side effect, which also re-resolves the module lockfile against the new ^2.2.0."
    - "Use plain `npm install` (NOT `npm ci`) for the root lockfile refresh per the drift narrative §'Why this is weird' — `npm ci` is lockfile-strict and would not re-resolve."
    - "Do NOT run `git commit` in this plan. Leave the changes staged-or-unstaged but uncommitted; Plan 04 finalizes Commit B after the maincloud republish + SQL probe verification gate."
    - "Do NOT run `spacetime publish` in this plan. The publish step is D-04's republish, which lives in Plan 04 (republish-before-push gate). This plan stops at typecheck + test pass."
    - "Working tree state at end of plan: changes to all 4 manifest/lockfile files + `src/module_bindings/` regen visible in `git status` as either staged or unstaged. Plan 04's first task will inspect this working tree and stage/finalize the commit."
  artifacts:
    - path: "spacetimedb/package.json"
      provides: "Module manifest at v2.2.0 floor"
      contains: '"spacetimedb": "^2.2.0"'
    - path: "package.json"
      provides: "Root client manifest at v2.2.0 floor"
      contains: '"spacetimedb": "^2.2.0"'
    - path: "spacetimedb/package-lock.json"
      provides: "Re-resolved module lockfile at 2.2.x"
      contains: '"spacetimedb"'
    - path: "package-lock.json"
      provides: "Re-resolved root lockfile at 2.2.x"
      contains: '"spacetimedb"'
    - path: "src/module_bindings/index.ts"
      provides: "Regenerated client bindings with v2.2.x generator stamp"
      contains: "spacetimedb cli version 2.2"
  key_links:
    - from: "spacetimedb/package.json + package.json"
      to: "spacetimedb/package-lock.json + package-lock.json"
      via: "npm install re-resolution"
      pattern: '"version":\s*"2\.2\.'
    - from: "spacetimedb/package.json (^2.2.0)"
      to: "src/module_bindings/index.ts (header stamp)"
      via: "npm run generate (cargo + prettier pipeline)"
      pattern: "cli version 2\\.2"
    - from: "src/module_bindings/ (regenerated)"
      to: "frontend consumers (components/, hooks/, app/)"
      via: "TypeScript module imports"
      pattern: "from ['\"]@?/?module_bindings"
---

<objective>
Stage Commit B's core artifacts: bump both `spacetimedb/package.json` and root `package.json` from `^2.1.0` to `^2.2.0`, refresh both lockfiles, regenerate `src/module_bindings/` via `npm run generate` (cargo path), and prove the codegen change is benign by passing `npm run test:typecheck` + full `npm test`. Do NOT commit — Plan 04 finalizes Commit B after the maincloud republish + SQL probe verification gate.

Purpose: This is the heart of the v2.2.0 upgrade. Per D-02 atomicity, the manifest bumps + lockfile refreshes + bindings regen MUST land in a single commit (no transient mid-state where module is on 2.2.0 but bindings are still on 2.1.0). Per D-09, typecheck pass + full `npm test` pass is sufficient evidence that the v2.2.0 codegen reorder (#4709) is cosmetic-only — no separate diff scan, no manual artifact. Per D-10, bindings regen uses `npm run generate` (cargo) not `spacetime:generate` (CLI), because `generate` runs `npm --prefix spacetimedb install` first as a side effect.

Output: Working tree contains 4 modified manifest/lockfile files + a regenerated `src/module_bindings/` directory. Typecheck and full test suite pass. NO commit yet.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md
@.planning/notes/spacetimedb-version-drift.md
@.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-01-SUMMARY.md
@.claude/CLAUDE.md
@.claude/skills/spacetimedb/SKILL.md

<interfaces>
<!-- State at the start of this plan (after Plan 01's drift fix landed). -->

From `spacetimedb/package.json` (after Plan 01 — current target for THIS plan's bump):
```json
"dependencies": {
  "spacetimedb": "^2.1.0"
},
```
Target after this plan:
```json
"dependencies": {
  "spacetimedb": "^2.2.0"
},
```

From root `package.json` (line 46, current — UNCHANGED in Plan 01):
```json
"spacetimedb": "^2.1.0",
```
Target after this plan:
```json
"spacetimedb": "^2.2.0",
```

From root `package.json` (line 14, the canonical bindings-regen command per D-10):
```json
"generate": "npm --prefix spacetimedb install && cargo run -p gen-bindings -- --out-dir src/module_bindings --module-path spacetimedb && prettier --write src/module_bindings",
```
This pipeline does 3 things in order:
1. `npm --prefix spacetimedb install` — re-resolves `spacetimedb/package-lock.json` against whatever the module manifest now declares (so this is ALSO the lockfile-refresh step for the module side after the manifest bump)
2. `cargo run -p gen-bindings` — regenerates `src/module_bindings/` from the module source
3. `prettier --write src/module_bindings` — formats the regenerated output

NOT to be confused with `spacetime:generate` (line 15):
```json
"spacetime:generate": "spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb",
```
D-10 explicitly forbids this in favor of `generate`.

From `src/module_bindings/index.ts` (line 4, current generator stamp — drift target):
```
// This was generated using spacetimedb cli version 2.1.0 (commit 6981f48b4bc1a71c8dd9bdfe5a2c343f6370243d).
```
Post-regen against the v2.2.x SDK, expect the stamp to update to `2.2.x` with a different commit hash.

From `package.json` test scripts (lines 18, 24):
```json
"test": "vitest run --config test/vitest.config.ts",
"test:typecheck": "tsc --noEmit"
```
These are the two acceptance commands per D-09. `npm test` is the unit suite; `test:integration` is separate but per CONTEXT.md "Claude's discretion" the unit suite is enough for this plan (integration runs in standard `npm test:all` paths and would be redundant here — typecheck + unit catches every codegen-shape change).

From CONTEXT.md `<canonical_refs>` line 88:
> No `spacetime delete` invocations exist anywhere in tracked code (grep verified 2026-05-02)
This means D-12 has nothing actionable in `scripts/` and `Plan 03` covers the docs/skills sweep; this plan does NOT touch `--clear-database` flows or the audit at all.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump both manifests to ^2.2.0 and re-resolve both lockfiles (D-01 Commit B core)</name>
  <files>spacetimedb/package.json, package.json, spacetimedb/package-lock.json, package-lock.json</files>
  <read_first>
    - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-01-SUMMARY.md (confirm Plan 01 landed cleanly; HEAD is the drift-fix commit)
    - spacetimedb/package.json (verify current state is `"spacetimedb": "^2.1.0"` after Plan 01)
    - package.json (verify current state is `"spacetimedb": "^2.1.0"`)
    - .planning/notes/spacetimedb-version-drift.md §"Lesson for future SDK bumps" (audit BOTH manifests AND BOTH lockfiles)
  </read_first>
  <action>
**Step 1 — Confirm clean starting state:**
```bash
git rev-parse --abbrev-ref HEAD
git status
git log -1 --oneline
```
Expected: branch is `feature_nath_claude`, working tree is clean, HEAD is Plan 01's drift-fix commit. If working tree is dirty or HEAD is wrong, STOP — do not proceed until the executor and user agree on starting state.

**Step 2 — Edit `spacetimedb/package.json`:**
Use the Edit tool to replace exactly:
```
"spacetimedb": "^2.1.0"
```
with:
```
"spacetimedb": "^2.2.0"
```
in `spacetimedb/package.json` ONLY. No other field touched.

**Step 3 — Edit root `package.json`:**
Use the Edit tool to replace exactly:
```
"spacetimedb": "^2.1.0",
```
with:
```
"spacetimedb": "^2.2.0",
```
in `package.json` (root) ONLY. The trailing comma matters (it's inside `dependencies`); preserve it.

**Step 4 — Re-resolve root lockfile via plain `npm install` (NOT `npm ci`):**
```bash
npm install
```
This resolves the new `^2.2.0` constraint into `package-lock.json`. The drift narrative explicitly cautions against `npm ci` here — use plain `npm install`.

**Step 5 — Sanity-check the manifest + root-lockfile diff:**
```bash
git diff --stat package.json package-lock.json spacetimedb/package.json
git diff package.json spacetimedb/package.json
```
- `package.json` and `spacetimedb/package.json` diffs: each must show ONE line changed (`^2.1.0` → `^2.2.0`).
- `package-lock.json` diff: should ONLY touch `spacetimedb` and its transitive deps owned by spacetimedb. If unrelated entries change, STOP and surface to the user.

(Note: the module lockfile `spacetimedb/package-lock.json` is refreshed in Task 2 by `npm run generate`'s first sub-step. Don't manually `npm install` inside `spacetimedb/` here — let `generate` do it so the operations are sequenced correctly.)
  </action>
  <verify>
    <automated>grep -q '"spacetimedb": "\^2\.2\.0"' spacetimedb/package.json && grep -q '"spacetimedb": "\^2\.2\.0",' package.json && ! grep -q '"spacetimedb": "\^2\.1\.0"' spacetimedb/package.json && ! grep -q '"spacetimedb": "\^2\.1\.0",' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"spacetimedb": "\^2\.2\.0"' spacetimedb/package.json` returns 1
    - `grep -c '"spacetimedb": "\^2\.2\.0",' package.json` returns 1
    - `grep -c '"spacetimedb": "\^2\.1\.0"' spacetimedb/package.json` returns 0
    - `grep -c '"spacetimedb": "\^2\.1\.0",' package.json` returns 0
    - `package-lock.json` diff touches only `spacetimedb`-related entries (no scope creep)
    - `git status` shows the 3 files modified (`spacetimedb/package.json`, `package.json`, `package-lock.json`); `spacetimedb/package-lock.json` will change in Task 2
    - No `git commit` was invoked — changes remain in working tree
  </acceptance_criteria>
  <rollback>
    `git checkout -- spacetimedb/package.json package.json package-lock.json` reverts the working tree. Plan 01's drift-fix commit remains untouched.
  </rollback>
  <done>Both manifests declare `^2.2.0`; root lockfile re-resolved against ^2.2.0; module lockfile still on 2.1.x (will be refreshed by `npm run generate` in Task 2); no commit yet.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate bindings via `npm run generate` (cargo path per D-10) and prove benign via typecheck + full test (D-02, D-09, D-10)</name>
  <files>spacetimedb/package-lock.json, src/module_bindings/</files>
  <read_first>
    - package.json (line 14, the `generate` script — confirm cargo pipeline)
    - src/module_bindings/index.ts (current state — confirm 2.1.0 stamp, will become 2.2.x)
    - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
**Step 1 — Run `npm run generate` (D-10, NOT `spacetime:generate`):**
```bash
npm run generate
```
This pipeline (per the script in root `package.json`):
1. `npm --prefix spacetimedb install` — refreshes `spacetimedb/package-lock.json` against the new `^2.2.0` constraint
2. `cargo run -p gen-bindings -- --out-dir src/module_bindings --module-path spacetimedb` — regenerates the bindings tree
3. `prettier --write src/module_bindings` — formats output

If the cargo step fails, surface the error verbatim to the user — do NOT attempt to fix the module source code in this plan (out of scope; module source is unchanged from Plan 01's state).

**Step 2 — Confirm the bindings stamp moved to 2.2.x:**
```bash
head -5 src/module_bindings/index.ts
```
Expected: line 4 contains `cli version 2.2.` (some 2.2.x patch number with a new commit hash). If it still shows `2.1.0`, the cargo step didn't pick up the new module install — STOP and investigate.

**Step 3 — Confirm the module lockfile re-resolved:**
```bash
git diff --stat spacetimedb/package-lock.json
grep -A1 '"node_modules/spacetimedb"' spacetimedb/package-lock.json | head -5
```
Expected: lockfile shows a 2.2.x version line for the `spacetimedb` package.

**Step 4 — Run typecheck (D-09 evidence #1):**
```bash
npm run test:typecheck
```
MUST exit 0. Any TypeScript error indicates a non-cosmetic codegen change that would need a code-side fix — surface to the user with the exact error output. Do NOT attempt to fix codegen errors in this plan; per D-09 they would be a finding worth halting the phase to discuss.

**Step 5 — Run full test suite (D-09 evidence #2):**
```bash
npm test
```
MUST exit 0. Per CONTEXT.md "Claude's discretion" the unit `npm test` (vitest run --config test/vitest.config.ts) is sufficient; integration runs are not gated here. Surface any failure verbatim — do NOT attempt fixes in this plan.

**Step 6 — Inspect working tree shape:**
```bash
git status
git diff --stat
```
Expected modified files (working tree, no commit yet):
- `spacetimedb/package.json` (Task 1)
- `spacetimedb/package-lock.json` (Task 2 step 1)
- `package.json` (Task 1)
- `package-lock.json` (Task 1)
- `src/module_bindings/` — many files inside (Task 2 step 1)

NO files from `tmp/`, `test/data/`, or `.env*` (gitignore guardrail). NO files outside `files_modified` (Plan 03 owns docs sweep; this plan touches code-only paths).
  </action>
  <verify>
    <automated>npm run test:typecheck && npm test && head -5 src/module_bindings/index.ts | grep -q "cli version 2\.2"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:typecheck` exits 0 (D-09)
    - `npm test` exits 0 (D-09 — full unit suite is the structural sensor for non-cosmetic codegen changes per #4709)
    - `head -5 src/module_bindings/index.ts | grep -c "cli version 2\.2"` returns 1 (regenerator stamp moved to 2.2.x)
    - `head -5 src/module_bindings/index.ts | grep -c "cli version 2\.1"` returns 0 (old stamp gone)
    - `git status` shows working tree dirty across the expected 5 paths (`spacetimedb/package.json`, `spacetimedb/package-lock.json`, `package.json`, `package-lock.json`, `src/module_bindings/...`)
    - `git status` does NOT show any `tmp/`, `test/data/`, `.env*`, or out-of-scope files modified
    - NO `git commit` was invoked — Commit B finalization is Plan 04's job
    - NO `git push` was invoked
    - NO `spacetime publish` was invoked — republish is Plan 04's job (D-04 republish-before-push gate)
  </acceptance_criteria>
  <rollback>
    To discard the bindings regen + lockfile refresh: `git checkout -- src/module_bindings/ spacetimedb/package-lock.json package-lock.json package.json spacetimedb/package.json`. The drift-fix commit (Plan 01) remains untouched.
    If `npm run generate` partially completes and leaves the bindings tree in an inconsistent state: `git clean -fd src/module_bindings/` then `git checkout -- src/module_bindings/` restores the pre-regen tree.
  </rollback>
  <done>Bindings regenerated against v2.2.x (`cli version 2.2.x` stamp); module lockfile refreshed to 2.2.x; typecheck + full unit test suite both pass; working tree contains all 5 changes staged-or-unstaged; no commit yet (Plan 04 finalizes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local checkout → npm registry | `npm install` + `npm --prefix spacetimedb install` fetch packages; integrity hashes are the trust anchor |
| module source → generated bindings | `cargo run -p gen-bindings` reads `spacetimedb/src/**`, writes `src/module_bindings/`; output is committed and consumed across `app/`, `components/`, `hooks/` |
| working tree → typecheck/test sensors | `tsc --noEmit` and `vitest run` are the two acceptance gates per D-09; their pass/fail is the trust signal that the codegen reorder is cosmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-02-01 | Tampering | npm registry serves a tampered `spacetimedb@2.2.x` package during re-resolution | accept | npm default integrity-hash verification + lockfile pin captured in Plan 04's commit. Same risk surface as every prior `npm install` in this repo. |
| T-16.3-02-02 | Elevation of Privilege | `cargo run -p gen-bindings` executes arbitrary build code from the module's Rust dependencies | accept | Cargo lockfile + the gen-bindings crate are under repo ownership; no transitive Rust dep changes are part of an SDK floor-bump. |
| T-16.3-02-03 | Tampering | Bindings regen produces a non-cosmetic codegen change that breaks downstream consumers but typecheck happens to pass (false negative) | mitigate | D-09 explicitly requires BOTH `test:typecheck` AND full `npm test`. The full unit suite exercises reducer-call signatures, table-row shapes, and reducer-arg encoding — any non-cosmetic codegen change would surface as runtime test failure even if types happened to align. |
| T-16.3-02-04 | Repudiation | Working tree contains the staged Commit B but is amended/overwritten before Plan 04 finalizes, losing the regenerated bindings | mitigate | Plan 04's first task inspects the working tree state via `git diff` against Plan 01's commit and verifies the expected 5-path shape before staging Commit B. Mismatch surfaces immediately. |
| T-16.3-02-05 | Information Disclosure | Re-resolved `package-lock.json` exposes a private-registry token in a resolution URL | accept | Project uses public npm registry only; no private auth tokens have ever appeared in repo lockfiles (verified by Plan 01's identical risk evaluation). |
| T-16.3-02-06 | Denial of Service | `cargo run -p gen-bindings` consumes excessive build time / disk on first run after toolchain change | accept | One-time cost during a manual phase; not a production concern. If it hangs >10 min the executor surfaces to the user. |
</threat_model>

<verification>
1. `grep '"spacetimedb"' spacetimedb/package.json` shows `^2.2.0`
2. `grep '"spacetimedb"' package.json` shows `^2.2.0`
3. `head -5 src/module_bindings/index.ts` shows `cli version 2.2.x` stamp
4. `npm run test:typecheck` exits 0
5. `npm test` exits 0
6. `git status` shows 5 expected paths dirty; no out-of-scope or gitignored paths
7. `git log -1 --oneline` is still Plan 01's drift-fix commit (no commit was made by this plan)
</verification>

<success_criteria>
- ROADMAP success criterion #2 closed: both manifests declare ^2.2.0; both lockfiles refreshed
- ROADMAP success criterion #3 closed: bindings regenerated; typecheck + full `npm test` pass
- D-01 Commit B core artifacts staged in working tree (commit deferred to Plan 04 per D-04 republish-before-push gate)
- D-02 atomicity preserved: bindings regen is part of the same future commit as the manifest/lockfile bumps; no transient mid-state in git history
- D-09 evidence threshold met: typecheck PASS + `npm test` PASS = sufficient to declare #4709 codegen reorder cosmetic
- D-10 followed: `npm run generate` (cargo) used, NOT `spacetime:generate` (CLI)
- No `git commit`, no `git push`, no `spacetime publish` invoked — those gates live in Plan 04
</success_criteria>

<output>
After completion, create `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-02-SUMMARY.md` capturing: the resolved `spacetimedb` 2.2.x version on each side (module + root lockfile), the new bindings generator stamp from `src/module_bindings/index.ts`, typecheck + test exit codes, the file count under `git status` for `src/module_bindings/`, and a note that NO commit was made (intentional — finalized in Plan 04).
</output>
