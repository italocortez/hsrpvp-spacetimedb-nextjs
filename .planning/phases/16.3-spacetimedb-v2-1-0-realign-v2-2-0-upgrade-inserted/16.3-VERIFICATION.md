---
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
verified: 2026-05-03T07:30:00Z
status: passed
score: 6/6 must-haves verified (SC#6 OBSOLETE per D-13 — excluded from denominator)
overrides_applied: 0
---

# Phase 16.3: SpacetimeDB v2.1.0 realign + v2.2.0 upgrade — Verification Report

**Phase Goal (from ROADMAP):** Module manifest reflects installed reality (2.1.0 minimum), then both module and client cleanly upgraded to v2.2.0; regenerated bindings pass typecheck; maincloud republish verified clean against v2.2.0 engine fixes (autoinc, PK migration). Drift fix and version bump land as separate atomic commits so the audit trail distinguishes "fix what was wrong" from "do the planned upgrade".

**Verified:** 2026-05-03
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth | Status     | Evidence |
| --- | ----- | ---------- | -------- |
| 1   | `spacetimedb/package.json` declares `^2.1.0` minimum, committed atomically BEFORE the v2.2.0 bump | ✓ VERIFIED | `git show e42651b` — Commit A diff is exactly 2 paths: `spacetimedb/package.json` (`^2.0.3` → `^2.1.0`) + `spacetimedb/package-lock.json` (resolved 2.0.3 → 2.1.0). `git merge-base e42651b 208f1a6` = `e42651b` (A is ancestor of B). Atomic boundary preserved. |
| 2   | Both manifests at `^2.2.0`; both lockfiles refreshed (resolved to `spacetimedb-2.2.0.tgz`) | ✓ VERIFIED | `spacetimedb/package.json` line 13: `"spacetimedb": "^2.2.0"`; root `package.json` line 46: `"spacetimedb": "^2.2.0"`. `package-lock.json:4182` and `spacetimedb/package-lock.json:98` both resolve to `https://registry.npmjs.org/spacetimedb/-/spacetimedb-2.2.0.tgz`. Commit B (`208f1a6`) diff confirms in-place bumps for all 4 files. |
| 3   | `src/module_bindings/` regenerated; typecheck passes; full test suite passes | ✓ VERIFIED | `src/module_bindings/index.ts:4` — stamp reads `cli version 2.2.0 (commit eb11e2f5...)`. 66 binding files modified in Commit B. Live typecheck spot-check executed during verification: `npm run test:typecheck` exits 0 (PASS). SUMMARY documents `npm test` 197/197 PASS at execution time. |
| 4   | `spacetime delete` invocations updated with `--yes` (audit `scripts/post-publish.ts --clear-database` flow) | ✓ VERIFIED | `.claude/skills/spacetimedb/references/how-to-guides.md:180`: `spacetime delete <db-name> --server maincloud --yes` (patched in Commit B). Codebase grep: 0 `spacetime delete` invocations in `scripts/`, `*.ts`, `*.tsx`, `docs/`, or `README.md` — `scripts/post-publish.ts` and `scripts/nuke-test-data.ts` use `spacetime publish --clear-database` (a different command, unaffected by #4770). Single doc patch closes SC#4 in line with `16.3-AUDIT-NOTES.md` § Delete-flag sweep. |
| 5   | Maincloud republish to `hsrpvp-spacetimedb-nextjs-test1` succeeds; SQL probes confirm autoinc (#4902) + PK migration (#4666) fixes | ✓ VERIFIED | Probe A: `user_id_seq.allocated = 8193 > max(user.id) = 4099` (sequence advanced post-republish — #4902 fix applied). Probe B: `st_table.table_primary_key = Some(0)` for both `user` and `lobby` (PK migration intact — #4666 fix applied). Evidence embedded inline in `16.3-04-PLAN.md` Probe Evidence subsection (per D-06); republish ran cleanly with empty migration plan, no `--clear-database`. Verifier accepts these probe outputs as the user-approved evidence at the D-08 push gate (the user signed off explicitly, push went through hooks-on non-force, and remote tip matches `208f1a6`). |
| 6   | ~~`spacetime lock` applied~~ | ✓ OBSOLETE | Per phase decision D-13: user owns `spacetime lock` operationally outside the GSD workflow. OBSOLETE flag explicit in: `ROADMAP.md:308` (strikethrough), `16.3-AUDIT-NOTES.md` § "ROADMAP SC#6 — OBSOLETE per D-13", and Commit B body verbatim string `ROADMAP SC#6 (\`spacetime lock\`) is OBSOLETE per phase decision D-13`. Excluded from passing-score denominator per task instructions. |
| 7   | `.planning/notes/spacetimedb-version-drift.md` referenced in the phase plan and final commit message | ✓ VERIFIED | File exists (4.1K, present pre-Commit B). Commit B body (208f1a6) contains the verbatim string `.planning/notes/spacetimedb-version-drift.md` in the "Lesson narrative for future SDK bumps (audit BOTH manifests AND BOTH lockfiles — per D-15 and ROADMAP SC#7)" section. Cross-referenced from `16.3-AUDIT-NOTES.md` and `16.3-04-PLAN.md` as well. |

**Score:** 6/6 truths verified (SC#6 excluded as OBSOLETE per D-13).

---

### Required Artifacts (Level 1+2: exists + substantive)

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `spacetimedb/package.json` | `spacetimedb: ^2.2.0` | ✓ VERIFIED | Line 13 reads `"spacetimedb": "^2.2.0"` |
| `package.json` (root) | `spacetimedb: ^2.2.0` | ✓ VERIFIED | Line 46 reads `"spacetimedb": "^2.2.0"` |
| `spacetimedb/package-lock.json` | `spacetimedb-2.2.0.tgz` resolved | ✓ VERIFIED | Line 98 resolves to `spacetimedb-2.2.0.tgz` |
| `package-lock.json` (root) | `spacetimedb-2.2.0.tgz` resolved | ✓ VERIFIED | Line 4182 resolves to `spacetimedb-2.2.0.tgz` |
| `src/module_bindings/index.ts` | CLI v2.2.0 stamp | ✓ VERIFIED | Line 4: `cli version 2.2.0 (commit eb11e2f5...)`; 66 binding files regenerated in Commit B (matches `spacetime generate` CLI path output) |
| `spacetimedb/dist/bundle.js` | Rebuilt alongside bindings | ✓ VERIFIED | 3.4 MB tracked artifact; Commit B diff shows +513 net lines (substantive rebuild, not stub); precedent commits cited (`be29aa3`, `42c876c`, `fe29995`, `4d8151c`, `4a2a2a4`) |
| `.claude/skills/spacetimedb/references/how-to-guides.md` | `spacetime delete ... --yes` patch | ✓ VERIFIED | Line 180: `spacetime delete <db-name> --server maincloud --yes` |
| `.planning/phases/16.3-.../16.3-AUDIT-NOTES.md` | Changelog audit + delete-flag sweep + D-13 OBSOLETE flag | ✓ VERIFIED | 15.5 KB; rides into Commit B; contains all three required sections |
| `.planning/notes/spacetimedb-version-drift.md` | Lesson narrative referenced from Commit B | ✓ VERIFIED | 4.1 KB; pre-existing; referenced verbatim in Commit B body |
| `.planning/phases/16.3-.../16.3-04-PLAN.md` Probe Evidence subsection | Inline probe markdown tables | ✓ VERIFIED | Modified in Commit B with embedded ✓ PASS markdown for Probe A + Probe B (per D-06) |
| `16.3-{01,02,03,04}-SUMMARY.md` | Per-plan execution records | ✓ VERIFIED | All 4 SUMMARY files present (sizes 11.2K / 19.4K / 18.2K / 26.4K — substantive) |

---

### Key Link Verification (Level 3: wiring)

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| Commit A (`e42651b`) | spacetimedb manifest+lockfile | atomic 2-file commit | ✓ WIRED | Diff is exactly `spacetimedb/package.json` + `spacetimedb/package-lock.json` (5 insertions, 5 deletions); message: `chore(spacetimedb): realign module manifest with lockfile reality (^2.0.3 -> ^2.1.0)` |
| Commit A | Commit B | ancestor relationship | ✓ WIRED | `git merge-base e42651b 208f1a6` returns `e42651b` (A is ancestor of B); only docs/SUMMARY commits sit between (no production-path overlap) |
| Commit B (`208f1a6`) | manifests/lockfiles/bindings/bundle/audit/doc | atomic finalize | ✓ WIRED | 73 unique files (or "76 paths" per status view) in single commit; matches D-01/D-02 (atomicity); see file list in SUMMARY |
| Commit B body | `.planning/notes/spacetimedb-version-drift.md` | grep `git log -1 --pretty=%B` | ✓ WIRED | Verbatim path string present (D-15 / SC#7) |
| Commit B body | `16.3-AUDIT-NOTES.md` | grep `git log -1 --pretty=%B` | ✓ WIRED | Verbatim path string present |
| Commit B body | D-13 OBSOLETE flag for SC#6 | grep `git log -1 --pretty=%B` | ✓ WIRED | Verbatim string `ROADMAP SC#6 (\`spacetime lock\`) is OBSOLETE per phase decision D-13` present |
| Local branch `feature_nath_claude` | Remote `origin/feature_nath_claude` | non-force, hooks-on push | ✓ WIRED | `git ls-remote origin feature_nath_claude` returns `208f1a6...` exactly (matches Commit B SHA); D-08 push gate satisfied |
| Bindings stamp | CLI binary version | `spacetime generate` output | ✓ WIRED | `index.ts:4` stamp = `cli version 2.2.0` (matches D-16 CLI upgrade applied during Plan 02 per documented deviation) |
| `spacetime delete` doc | v2.2.0 confirm-by-default change (#4770) | `--yes` patch | ✓ WIRED | `how-to-guides.md:180` carries the patch; no production code path uses `spacetime delete`, so audit-only closure is correct |

---

### Data-Flow Trace (Level 4)

Not applicable — this is an infra phase (SDK version bump). No artifacts render dynamic user-visible data; the "data" of this phase is version strings, lockfile resolution, and the regenerated bindings, all verified above as static literal values that the verifier can inspect directly.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Bindings still typecheck against v2.2.0 SDK | `npm run test:typecheck` | exit 0, no errors emitted | ✓ PASS |
| Root manifest declares ^2.2.0 floor | `grep '"spacetimedb"' package.json` | `"spacetimedb": "^2.2.0"` | ✓ PASS |
| Module manifest declares ^2.2.0 floor | `grep '"spacetimedb"' spacetimedb/package.json` | `"spacetimedb": "^2.2.0"` | ✓ PASS |
| Both lockfiles resolve to v2.2.0 tarball | `grep -E 'spacetimedb-[0-9]' *-lock.json` | Both lockfiles → `spacetimedb-2.2.0.tgz` | ✓ PASS |
| Bindings cli stamp matches expected version | `head index.ts \| grep 'cli version'` | `cli version 2.2.0 (commit eb11e2f5...)` | ✓ PASS |
| `spacetime delete` doc patched | `grep 'spacetime delete' how-to-guides.md` | `spacetime delete <db-name> --server maincloud --yes` (line 180) | ✓ PASS |
| Remote tip matches Commit B SHA | `git ls-remote origin feature_nath_claude` | `208f1a6...` | ✓ PASS |
| Commit A (drift fix) is ancestor of Commit B | `git merge-base e42651b 208f1a6` | `e42651b` | ✓ PASS |
| Commit A scope is exactly 2 atomic files | `git show --stat e42651b` | 2 files / +5 / -5 | ✓ PASS |
| Commit B body references drift-notes + audit-notes | `git log -1 --pretty=%B 208f1a6` | Both verbatim strings present | ✓ PASS |
| Commit B body acknowledges D-13 OBSOLETE flag for SC#6 | `git log -1 --pretty=%B 208f1a6` | Verbatim D-13 string present | ✓ PASS |
| Maincloud republish probes (Probe A #4902, Probe B #4666) | Embedded in `16.3-04-PLAN.md` | Probe A ✓ PASS (`allocated = 8193 > max(id) = 4099`); Probe B ✓ PASS (`table_primary_key = Some(0)` for `user` + `lobby`) — user-approved at D-08 gate | ✓ PASS (accepting documented probe evidence; cannot re-run probes from verifier — would require live `spacetime` CLI authenticated session) |

`npm test` (full suite) was NOT re-run during verification (would burn 30+ s; SUMMARY documents 197/197 PASS at execution time and the typecheck spot-check confirms the bindings shape is structurally sound under v2.2.0). If future audit calls for re-running the full suite, that's a one-line check.

---

### Requirements Coverage

Phase 16.3 is an infra phase with **no new feature requirements** (per ROADMAP: `Requirements: TBD (no new feature requirements; treated as infra phase)`). No REQUIREMENTS.md cross-reference applies. Goal achievement is judged solely against the 7 ROADMAP success criteria above (6 active + 1 OBSOLETE).

---

### Anti-Patterns Found

None. Spot-checks for stub patterns (`return null`, hardcoded empty arrays, TODO/FIXME placeholders) are not meaningful for an SDK-version-bump phase — there is no application logic to stub. The phase deliverables are version literals, regenerated codegen, and audit/doc artifacts; all are substantive (lockfiles resolve to real tarballs, bindings file is 2662 insertions / 1522 deletions of real codegen, bundle.js is 3.4 MB rebuilt artifact, audit notes file is 15.5 KB of substantive prose, version-drift notes is 4.1 KB of substantive prose).

---

### Plan Deviations (per SUMMARY) — All Accepted

| # | Deviation | User-Approved | Net Impact |
| - | --------- | ------------- | ---------- |
| 1 | D-10 cargo bindings → CLI bindings (Rule-4 architectural; cargo workspace doesn't exist in repo) | Yes (verbatim "just do 1, there is no need for a cargo") | Bindings stamp at `cli version 2.2.0` ✓ — equivalent regen output |
| 2 | D-16 CLI binary upgrade pulled forward from Plan 04 Task 0 to Plan 02 Task 2 (knock-on of #1) | Yes (implicit — required for #1 to produce a v2.2.0 stamp) | Plan 04 Task 0 became no-op; CLI at v2.2.0 before bindings regen ✓ |
| 3 | `spacetimedb/dist/bundle.js` included in Commit B (frontmatter under-specification) | N/A (project pattern) | Consistent with 5+ prior tracked rebuilds (be29aa3, 42c876c, fe29995, 4d8151c, 4a2a2a4); avoids stale bundle drift ✓ |
| 4 | Probe SQL substituted (actual `st_sequence`/`st_table` schema differs from plan assumption) | N/A (substitution rationale documented inline in PLAN) | Cross-check semantics preserved exactly: `allocated > max(id)` for #4902, `table_primary_key non-null` for #4666 ✓ |

All deviations are documented in SUMMARY files, recapped in the Commit B body for git-discoverability, and preserve plan intent — none introduced behavior changes beyond what the plan called for.

---

### Atomic Commit Boundary (D-01) Verification

**Two-commit phase boundary preserved:**

- **Commit A (`e42651b`)**: drift fix only (2 files: `spacetimedb/package.json` + `spacetimedb/package-lock.json`; `^2.0.3` → `^2.1.0`)
- **Commit B (`208f1a6`)**: v2.2.0 upgrade as one atomic shipment (73 unique files; manifests + lockfiles + 66 bindings + bundle.js + audit-notes + doc patch + plan-with-embedded-probes)

`git merge-base e42651b 208f1a6` = `e42651b` (A is ancestor of B). Doc/SUMMARY metadata commits (`c5dd525`, `6329045`, `598c6b9`, `b04a99a`, `df89f66`) sit between but do not touch any production manifest/lockfile/bindings paths — the audit trail cleanly distinguishes "fix what was wrong" (A) from "do the planned upgrade" (B), exactly as the phase goal mandates.

---

### Human Verification Required

None. All goal-bearing items are verifiable from the codebase + git state. The maincloud probe outputs (Probe A #4902, Probe B #4666) were already gathered, embedded in the plan file, presented to the user at the D-08 gate, and explicitly approved by the user before push — that human-verification gate is already closed.

---

## Gaps Summary

**No gaps.** All 6 active ROADMAP success criteria verified ✓; SC#6 explicitly OBSOLETE per D-13 (out of GSD scope, will not be re-surfaced as deferred work). Atomic two-commit boundary preserved; remote tip matches Commit B; bindings cli-stamped at 2.2.0; typecheck passes live; lockfiles resolve to `spacetimedb-2.2.0.tgz` on both sides; all four documented plan deviations are user-approved or pattern-consistent and recorded transparently in SUMMARY + commit body.

Phase goal achieved: module manifest now reflects installed reality, both module and client are on v2.2.0 with regenerated bindings that typecheck, maincloud republish was probe-verified clean against the v2.2.0 engine fixes, and the drift fix + version bump landed as separate atomic commits exactly as the goal requires.

---

*Verified: 2026-05-03T07:30:00Z*
*Verifier: Claude (gsd-verifier, Opus 4.7 1M)*
