---
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
plan: 02
subsystem: infra
tags: [spacetimedb, npm, lockfile, manifest, bindings-regen, v2.2.0, cli-upgrade]

# Dependency graph
requires:
  - phase: 16.3-01
    provides: drift-fix Commit A (^2.1.0 floor on both manifests + module lockfile pinned to 2.1.0); the clean starting state this plan bumps from
provides:
  - Both `spacetimedb/package.json` and root `package.json` declare `"spacetimedb": "^2.2.0"` (uncommitted in working tree)
  - Both lockfiles re-resolved to `spacetimedb@2.2.0` (uncommitted in working tree)
  - `src/module_bindings/` regenerated with `cli version 2.2.0` stamp (uncommitted in working tree)
  - Working-tree state is the staged Commit B core; finalized in Plan 04 after maincloud republish + SQL probes
  - Local `spacetime` CLI binary upgraded from v2.1.0 → v2.2.0 (D-16 pulled forward from Plan 04 Task 0)
affects:
  - 16.3-03 (Docs/skills sweep — operates on this same uncommitted working tree)
  - 16.3-04 (Maincloud republish + SQL probe verification + Commit B finalization + push gate; Task 0 CLI upgrade now a no-op)
  - All future SpacetimeDB feature work (v2.2.0 floor enables AuthCtx/JwtClaims/useTable enabled/useProcedure adoption in Phase 16.4)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CLI bindings-regen path (`spacetime generate --lang typescript ...`) is the ONLY working bindings-regen route on this codebase — the cargo path documented in `npm run generate` references a `gen-bindings` cargo workspace that does not exist in tracked code"
    - "Bindings stamp tracks CLI binary version, not the npm package version: post-`spacetime generate` the stamp reads `cli version 2.2.0` (the upgraded local CLI), confirming the generator is what drives the stamp"
    - "The CLI's TypeScript generator also rebuilds `spacetimedb/dist/bundle.js` as a side effect (the bundled module schema source is part of how generate extracts the API surface) — established project pattern with prior tracked rebuilds at `be29aa3`, `42c876c`, `fe29995`"

key-files:
  created: []
  modified:
    - spacetimedb/package.json — `"spacetimedb": "^2.1.0"` → `"spacetimedb": "^2.2.0"` (Task 1, prior executor)
    - package.json — `"spacetimedb": "^2.1.0"` → `"spacetimedb": "^2.2.0"` (Task 1, prior executor)
    - spacetimedb/package-lock.json — re-resolved to `spacetimedb@2.2.0` (Task 1, prior executor)
    - package-lock.json — re-resolved to `spacetimedb@2.2.0` (Task 1, prior executor)
    - src/module_bindings/ — full directory regenerated, ~158 .ts files; stamp moved `2.1.0 (commit 6981f48...)` → `2.2.0 (commit eb11e2f5c41dce6979715ad407996270d61329f6)` (Task 2)
    - spacetimedb/dist/bundle.js — rebuilt as side effect of `spacetime generate` (Task 2; not in plan frontmatter — see Deviations)

key-decisions:
  - "User-approved Rule-4 deviation: substituted `spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb` (CLI path) for `npm run generate` (cargo path mandated by D-10). The cargo path is structurally unworkable on this codebase — no `Cargo.toml`, no `gen-bindings` package has ever been tracked. User decision verbatim: 'just do 1, there is no need for a cargo'."
  - "D-16 (CLI binary upgrade) pulled forward from Plan 04 Task 0 to Plan 02 Task 2 to enable the CLI-based bindings regen. Local CLI was at v2.1.0; ran `spacetime version upgrade` → v2.2.0. Result: bindings stamp at `cli version 2.2.0`, satisfying acceptance criterion #4. Plan 04 Task 0 becomes a no-op (CLI is already at 2.2.x)."
  - "NO commit was made for the manifest/lockfile/bindings/bundle.js changes — they remain uncommitted in the working tree per the plan's no-commit invariant. Plan 04 finalizes Commit B after maincloud republish + SQL probes."

patterns-established:
  - "CLI vs cargo bindings regen: the canonical D-10 'use `npm run generate`' assumption was based on the script existing in `package.json` (line 14), but the script's middle step (`cargo run -p gen-bindings`) targets a cargo workspace that doesn't exist in tracked code. Future SDK bumps should use `spacetime generate` (the CLI path the script's name was steering away from) and prettier-format the output separately if formatting matters."
  - "CLI upgrade as a side-step precondition: when bindings regen depends on a CLI binary, the binary version must be aligned with the target SDK version BEFORE regen. Idempotent (`spacetime version upgrade` is a no-op if already current) and reversible (`spacetime version use <prior>`)."

requirements-completed: []

# Metrics
duration: ~12min
completed: 2026-05-03
---

# Phase 16.3 Plan 02: SpacetimeDB v2.2.0 module + client bump with bindings regen Summary

**Bumped both `spacetimedb/package.json` and root `package.json` to `^2.2.0`, re-resolved both lockfiles to `spacetimedb@2.2.0`, regenerated `src/module_bindings/` (158 .ts files) via the user-approved CLI path (instead of the structurally-unworkable cargo path mandated by D-10), and proved benign via `tsc --noEmit` PASS + 197/197 vitest PASS — all uncommitted in working tree, awaiting Plan 04's finalization gate.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-03 (continuation executor; Task 1 was completed by a prior executor)
- **Completed:** 2026-05-03
- **Tasks:** 2 (Task 1 verified intact; Task 2 executed with user-approved Rule-4 deviation)
- **Files modified:** 5 in-scope (4 manifests/lockfiles + 1 bindings tree of 158 files) + 1 out-of-frontmatter (`spacetimedb/dist/bundle.js` rebuilt as a side effect of `spacetime generate`); all uncommitted

## Accomplishments

- ROADMAP success criterion #2 staged in working tree: both manifests at `^2.2.0`, both lockfiles refreshed to 2.2.0
- ROADMAP success criterion #3 staged in working tree: `src/module_bindings/` regenerated with `cli version 2.2.0` stamp; full unit test suite (197 tests) passes; typecheck passes
- D-09 evidence threshold met: both `npm run test:typecheck` (exit 0) and `npm test` (197/197 pass) — sufficient evidence that the v2.2.0 codegen reorder (#4709) is cosmetic-only
- D-02 atomicity preserved: bindings regen + manifest bumps + lockfile refreshes all sit in the working tree together; will land in a single Commit B in Plan 04
- D-16 pulled forward and executed: local CLI now at v2.2.0 (idempotent for any later re-runs)
- No-commit invariant honored: zero commits made for the v2.2.0 work; Plan 04 owns Commit B finalization

## Task Commits

This plan made NO commits for the v2.2.0 working-tree changes per the no-commit invariant. Only the SUMMARY.md commit lands at the end of this plan, scoped to a single file.

1. **Task 1: Bump both manifests to ^2.2.0 + re-resolve both lockfiles** — completed by prior executor; uncommitted in working tree (verified intact by this executor)
2. **Task 2: Regenerate bindings + prove benign via typecheck + full test** — executed by this executor with user-approved Rule-4 deviation (CLI path); uncommitted in working tree

**Plan metadata commit:** SUMMARY.md only, scoped via direct `git add <path>` + `git commit -m`

## Files Created/Modified

All changes are uncommitted in the working tree (per no-commit invariant — Plan 04 finalizes):

- `spacetimedb/package.json` — manifest at `^2.2.0`
- `package.json` — manifest at `^2.2.0`
- `spacetimedb/package-lock.json` — `spacetimedb` resolved to `2.2.0` (sha512-H/bCrIegtwrL5TYqRz0ljIiESWwhMRI8KShc8UQdmMXQGo/egfor8cUxK5yDf+wH2Aa4kNPr93v+wuZloqYP8Q==)
- `package-lock.json` — `spacetimedb` resolved to `2.2.0` (same integrity hash)
- `src/module_bindings/` — 158 modified .ts files; `index.ts` line 4 reads `// This was generated using spacetimedb cli version 2.2.0 (commit eb11e2f5c41dce6979715ad407996270d61329f6).`
- `spacetimedb/dist/bundle.js` — rebuilt as a side effect of `spacetime generate` (~415 insertions, ~98 deletions); not in plan frontmatter (see Deviations §2)

Out-of-scope working-tree noise also visible in `git status` (NOT touched by this executor, NOT to be staged in Commit B by Plan 04):
- `spacetimedb/node_modules/**` — 385+ tracked files showing as modified, plus 9 new untracked files (the v2.2.0 SDK adds `useProcedure`, `websocket_protocols`, `websocket_v3_frames`); pre-existing tracking drift; out of scope per Plan 02 prompt and per the orchestrator's separate Plan 01 drift-fix scope decisions

## Decisions Made

- **Used the CLI path for bindings regen instead of the cargo path D-10 mandates.** User-approved per the prior executor's Rule-4 escalation. See Deviations §1 for the structural reason.
- **Pulled D-16 (CLI upgrade) forward from Plan 04 Task 0 to Plan 02 Task 2.** Required because the CLI path produces a stamp tied to the local CLI binary version, and the acceptance criterion requires `cli version 2.2.x` in the bindings stamp. Idempotent — Plan 04 Task 0 will see the CLI already at 2.2.0 and become a no-op.
- **Ran prettier separately after `spacetime generate`** since the cargo `npm run generate` pipeline's third step (`prettier --write src/module_bindings`) was the formatting source the project relies on. Prettier reported "All files formatted correctly" — the CLI generator output is already formatted to project conventions, so the separate prettier run was a defensive no-op (no diffs).
- **Did NOT touch `spacetimedb/node_modules/**`** — pre-existing tracking drift is out of scope per the prompt; will surface as visible-but-unstaged noise in Plan 04's `git status`, and Plan 04 must continue to use explicit `git add <path>` (not `-A`) to avoid pulling the noise into Commit B.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4 - Architectural / User-Approved] Substituted CLI bindings-regen path for the cargo path D-10 mandates**

- **Found during:** Task 2 — pre-execution by the prior executor (escalated to user, who approved Option 1 verbatim: "just do 1, there is no need for a cargo")
- **Issue:** D-10 mandates `npm run generate` for bindings regeneration. The script's middle step is `cargo run -p gen-bindings -- --out-dir src/module_bindings --module-path spacetimedb`. The prior executor verified via Glob/git log that no `Cargo.toml`, no `gen-bindings` cargo workspace, and no related Rust source exists anywhere in tracked code — and never has. The CONTEXT.md `<code_context>` claim that the cargo bindings generator is "already wired" contradicts the tracked code reality. Running `npm run generate` would fail at the cargo step.
- **Fix:** Used `spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb` (the CLI path) for bindings regen. To make the bindings stamp meet acceptance criterion #4 (`cli version 2.2.x`), pulled D-16 (CLI binary upgrade from Plan 04 Task 0) forward into this plan: ran `spacetime version upgrade` to bump the local CLI from v2.1.0 → v2.2.0 BEFORE invoking `spacetime generate`. Then ran `npx prettier --write src/module_bindings` to match the formatting step the cargo pipeline would have applied (no-op — output already formatted).
- **Files modified:** `src/module_bindings/` (158 .ts files regenerated); local CLI install at `C:\Users\Admin\AppData\Local\SpacetimeDB\bin\current\spacetimedb-cli.exe` (per-machine; no tracked artifact)
- **Verification:** `head -5 src/module_bindings/index.ts` shows `// This was generated using spacetimedb cli version 2.2.0 (commit eb11e2f5c41dce6979715ad407996270d61329f6).` — stamp matches `cli version 2.2`. `npm run test:typecheck` exited 0. `npm test` printed `Test Files 11 passed (11)` / `Tests 197 passed (197)`. `spacetime --version` now reports `spacetimedb tool version 2.2.0` with no upgrade banner.
- **Committed in:** N/A — no-commit invariant. The deviation lives in this SUMMARY.md only; the orchestrator/user will decide later whether to formally amend D-10 or leave the deviation as-is. **Plan 04 Task 0 is now a no-op** (CLI is already at 2.2.0, `spacetime version upgrade` will be idempotent if re-run).

**2. [Rule 3 - Blocking / Side-effect drift] `spacetimedb/dist/bundle.js` rebuilt by `spacetime generate` but absent from plan frontmatter**

- **Found during:** Task 2 — post-regen `git status` review
- **Issue:** Plan 02's `files_modified` frontmatter declares `[spacetimedb/package.json, spacetimedb/package-lock.json, package.json, package-lock.json, src/module_bindings/]`. After running `spacetime generate`, `git status` also showed `spacetimedb/dist/bundle.js` modified (~415 insertions, ~98 deletions). The CLI's TypeScript generator bundles the module source to extract the API surface, and the bundle.js is a tracked artifact (not gitignored) with a long history of similar rebuilds (`be29aa3`, `42c876c`, `fe29995` all explicitly committed bundle.js rebuilds after publishes). This is a structural side-effect of bindings regen, not unrelated work — but it's not declared in the plan frontmatter.
- **Fix:** Left bundle.js modified in the working tree. The no-commit invariant means nothing was staged or committed by this plan. Plan 04 (which finalizes Commit B from the working tree) needs to make an explicit decision: either (a) include bundle.js in Commit B's explicit `git add` list — consistent with the project's prior pattern of bundling these rebuilds with publish/regen commits — or (b) `git checkout -- spacetimedb/dist/bundle.js` to revert it before Commit B if the orchestrator wants strict frontmatter adherence.
- **Files modified:** `spacetimedb/dist/bundle.js` (uncommitted; ~415/-98 lines)
- **Verification:** `git diff --stat spacetimedb/dist/bundle.js` shows the rebuild diff. `git log --oneline spacetimedb/dist/bundle.js` confirms 3 prior tracked rebuilds in this repo's history.
- **Committed in:** N/A — no-commit invariant; deferred to Plan 04's discretion.

---

**Total deviations:** 2 documented (1 user-approved Rule-4 architectural, 1 Rule-3 frontmatter-coverage drift)

**Impact on plan:** The Rule-4 deviation is the more significant of the two — it preserves the plan's intent (regenerate bindings stamped at 2.2.x and prove benign via typecheck + full test) while substituting a working tool path for a non-working one. Acceptance criterion #4 is satisfied by the new stamp (`cli version 2.2.0`). The Rule-3 deviation is a frontmatter omission, not a behavior change — `spacetimedb/dist/bundle.js` always rebuilds during bindings regen on this codebase; the plan should have declared it. No scope creep beyond what the bindings-generation pipeline structurally produces.

## Issues Encountered

- **Pre-existing tracking drift in `spacetimedb/node_modules/**`** — the prior Plan 01 SUMMARY documented this same situation. After `npm install` (and after `spacetime generate`'s implicit npm operations), `git status` shows ~385+ files modified under `spacetimedb/node_modules/` plus 9 new untracked files (the v2.2.0 SDK adds new `dist/react/useProcedure.*`, `dist/sdk/websocket_protocols.*`, `dist/sdk/websocket_v3_frames.*`, and matching `src/` originals). These are out of scope per the prompt's `<critical_other_constraints>` section. The orchestrator will handle this drift separately (or accept it as out-of-scope working-tree noise). **NO files under `spacetimedb/node_modules/**` were staged or committed by this plan.**
- **No `npm install` step needed before `spacetime generate`** — Plan 02's `<interfaces>` block describes `npm run generate`'s first sub-step as `npm --prefix spacetimedb install`, which would have refreshed `spacetimedb/package-lock.json` against the new `^2.2.0` constraint. Since the prior executor's Task 1 already re-resolved `spacetimedb/package-lock.json` to 2.2.0 (via the prior executor's chosen install path), and the lockfile was already correct before regen, no additional `npm --prefix spacetimedb install` was needed. The CLI generator does its own internal schema bundling and does not touch the module's package-lock.

## User Setup Required

None for this plan. The CLI binary upgrade is per-machine (already done by this executor on the current machine; future executors on different machines should re-run `spacetime version upgrade` if their CLI is still at 2.1.0 — Plan 04 Task 0's checking step will prompt that automatically).

## Next Phase Readiness

- **Plan 03 (docs/skills sweep + audit notes):** can proceed immediately. Plan 03 operates on documentation paths (`.claude/skills/spacetimedb/references/how-to-guides.md`, `.planning/phases/16.3-.../16.3-AUDIT-NOTES.md`) — disjoint from Plan 02's working-tree changes. The shared working tree state Plan 03 inherits is exactly the 5 in-scope paths described above + the bundle.js side-effect.
- **Plan 04 (republish + SQL probes + finalize Commit B):** can proceed once Plan 03 completes. Plan 04 Task 0 (CLI upgrade) is now a no-op — `spacetime --version` already reports `2.2.0` and `spacetime version upgrade` is idempotent. Plan 04 Task 1 should explicitly stage exactly the paths it intends in Commit B (using direct `git add <path>`, NOT `-A`); the prompt's `files_modified` frontmatter for Plan 04 lists 5 paths from this plan + 2 from Plan 03 + this plan's SUMMARY-related-but-separate concerns. **Plan 04 must decide what to do about `spacetimedb/dist/bundle.js`** (see Deviations §2): include it in Commit B (consistent with prior project pattern) or revert it (strict frontmatter adherence).
- **Branch / push hygiene:** clean. `feature_nath_claude` has Plan 01's drift-fix commit as its tip locally; no remote push has happened; no force operations have been used; the v2.2.0 working-tree changes are uncommitted and reversible via `git checkout --` per the rollback procedures the plan documents.
- **STATE.md / ROADMAP.md:** intentionally NOT modified by this executor per the prompt's explicit instructions ("the orchestrator owns those writes after the wave completes").

---

## Self-Check: PASSED

**Files verified present:**
- FOUND: `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-02-SUMMARY.md` (this file)
- FOUND: `spacetimedb/package.json` (line 13: `"spacetimedb": "^2.2.0"`)
- FOUND: `package.json` (line 46: `"spacetimedb": "^2.2.0",`)
- FOUND: `spacetimedb/package-lock.json` (line 96-98: `"node_modules/spacetimedb"` resolves to `2.2.0`)
- FOUND: `package-lock.json` (line 4180-4182: `"node_modules/spacetimedb"` resolves to `2.2.0`)
- FOUND: `src/module_bindings/index.ts` (line 4: `// This was generated using spacetimedb cli version 2.2.0 (commit eb11e2f5c41dce6979715ad407996270d61329f6).`)

**Acceptance criteria status:**
- AC `npm run test:typecheck` exits 0 — PASS (Task 2 Step 4)
- AC `npm test` exits 0 with full unit suite — PASS (Task 2 Step 5; 11 test files, 197 tests, all passed)
- AC `head -5 src/module_bindings/index.ts | grep -c "cli version 2\.2"` returns 1 — PASS
- AC `head -5 src/module_bindings/index.ts | grep -c "cli version 2\.1"` returns 0 — PASS
- AC working tree dirty across the expected paths — PASS (4 manifests/lockfiles + bindings tree of 158 files, plus the documented Deviation §2 bundle.js)
- AC `git status` does NOT show any `tmp/`, `test/data/`, `.env*` files — PASS (none present)
- AC NO `git commit` was invoked for the v2.2.0 working-tree changes — PASS (the only commit this plan makes is for SUMMARY.md, scoped to that single path)
- AC NO `git push` was invoked — PASS
- AC NO `spacetime publish` was invoked — PASS (D-04 republish is Plan 04's job)
- AC (deviation-amended) bindings stamp moved to 2.2.0 via CLI path — PASS

**Commits verified present in `git log`:**
- The only commit produced by this plan is the SUMMARY.md commit (made immediately after this self-check). No other commits.

---

*Phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted*
*Completed: 2026-05-03*
