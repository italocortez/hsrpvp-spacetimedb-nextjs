---
phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/package.json
  - spacetimedb/package-lock.json
autonomous: true
requirements: []
success_criteria_addressed:
  - "#1"
decisions_addressed:
  - D-01
  - D-03
  - D-15

must_haves:
  truths:
    - "spacetimedb/package.json declares spacetimedb: ^2.1.0 (matches installed lockfile reality and root client manifest) — closes ROADMAP success criterion #1 (D-01 Commit A)"
    - "spacetimedb/package-lock.json resolves spacetimedb to 2.1.x (no longer pinned at 2.0.3) after `npm install` re-resolution"
    - "Drift-fix change lands as a single atomic commit on feature_nath_claude (D-01, D-03) before any v2.2.0 work begins, so the audit trail cleanly separates 'fix what was wrong' from 'do the planned upgrade'"
    - "Commit message references .planning/notes/spacetimedb-version-drift.md — the durable lesson artifact this phase preserves (D-15)"
    - "No remote push from this plan — all commits stay local on feature_nath_claude until Plan 04's verification gate (D-03)"
  protocol:
    - "Edit `spacetimedb/package.json` ONLY — root `package.json` stays at ^2.1.0 in this plan (root bump rides in Commit B / Plan 02)"
    - "Use `npm install` (not `npm ci`) inside `spacetimedb/` so the lockfile re-resolves; the drift narrative §'Why this is weird' identifies lockfile-strict installs as the suspected cause of the drift persisting"
    - "Bindings are NOT regenerated in this commit — `src/module_bindings/` stays stamped 2.1.0 (matches the new manifest); regen rides Commit B per D-02"
    - "No tests are run gating this commit — the manifest moves from a value (^2.0.3) that didn't reflect reality to one (^2.1.0) that does. Existing test suite already runs against installed 2.1.0 and stays green"
  artifacts:
    - path: "spacetimedb/package.json"
      provides: "Module manifest aligned with lockfile + root client (^2.1.0)"
      contains: '"spacetimedb": "^2.1.0"'
    - path: "spacetimedb/package-lock.json"
      provides: "Re-resolved module lockfile (2.1.x)"
      contains: '"spacetimedb"'
  key_links:
    - from: "spacetimedb/package.json"
      to: "spacetimedb/package-lock.json"
      via: "npm install re-resolution"
      pattern: '"version":\s*"2\.1\.'
    - from: "Commit A message"
      to: ".planning/notes/spacetimedb-version-drift.md"
      via: "explicit body reference per D-15"
      pattern: "spacetimedb-version-drift"
---

<objective>
Realign the module manifest with the installed lockfile (and the root client manifest) by bumping `spacetimedb/package.json` from `^2.0.3` to `^2.1.0` and re-resolving `spacetimedb/package-lock.json`. Land as a single atomic commit on `feature_nath_claude` — the drift-fix half of the 2-commit phase boundary (D-01 Commit A).

Purpose: The drift narrative (`.planning/notes/spacetimedb-version-drift.md`) documents that `spacetimedb/package.json` claims `^2.0.3` while the lockfile pins 2.0.3 yet the codebase uses 2.1.0-only features. This commit fixes the manifest to reflect reality BEFORE the v2.2.0 bump, so the audit trail cleanly separates "fix what was wrong" from "do the planned upgrade" (D-01 rationale, drift note §"What was done"). No behavior change; no bindings regen; no test pass required (existing test suite already runs against installed 2.1.0).

Output: 1 atomic commit on `feature_nath_claude` containing only the manifest bump + lockfile refresh, with a commit message that cites the drift note (D-15).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md
@.planning/notes/spacetimedb-version-drift.md
@.claude/CLAUDE.md
@.claude/skills/spacetimedb/SKILL.md

<interfaces>
<!-- Pre-edit state extracted from the codebase. Executor should NOT re-explore. -->

From `spacetimedb/package.json` (lines 12–14, current):
```json
"dependencies": {
  "spacetimedb": "^2.0.3"
},
```
Target after edit:
```json
"dependencies": {
  "spacetimedb": "^2.1.0"
},
```

From `package.json` (root, line 46, reference only — NOT edited in this plan):
```json
"spacetimedb": "^2.1.0",
```
Root manifest is already at ^2.1.0. The drift exists ONLY on the module side.

From `spacetimedb/package-lock.json` — resolved version is currently `2.0.3` (drift target). After `npm install` re-resolution against `^2.1.0`, expect `2.1.x` (likely `2.1.0` to match what the root lockfile already pins).

From the drift note `.planning/notes/spacetimedb-version-drift.md` §"Lesson for future SDK bumps":
> When upgrading SpacetimeDB (or any package present in both `spacetimedb/package.json` and root `package.json`), audit BOTH manifests AND BOTH lockfiles before proposing the new version.

This is the durable artifact this phase preserves — Commit A's message MUST cite the note path so future SDK-bump readers find it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drift-fix edit + lockfile re-resolution + atomic Commit A (D-01, D-03, D-15)</name>
  <files>spacetimedb/package.json, spacetimedb/package-lock.json</files>
  <read_first>
    - .planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-CONTEXT.md (D-01, D-03, D-15)
    - .planning/notes/spacetimedb-version-drift.md (drift narrative — must be cited verbatim path in commit message)
    - spacetimedb/package.json (verify current state is `"spacetimedb": "^2.0.3"`)
    - .claude/CLAUDE.md "Autonomous Execution" + "Gitignore Guardrail" sections
  </read_first>
  <action>
**Step 1 — Confirm current branch is `feature_nath_claude` (D-03):**
```bash
git rev-parse --abbrev-ref HEAD
```
Expected output: `feature_nath_claude`. If anything else, STOP and surface to the user — do NOT branch-switch silently. Per D-03 both commits land on this branch only.

**Step 2 — Edit `spacetimedb/package.json`:**
Use the Edit tool to replace exactly this string in `spacetimedb/package.json`:
```
"spacetimedb": "^2.0.3"
```
with:
```
"spacetimedb": "^2.1.0"
```
No other changes to that file. Do NOT touch `typescript`, scripts, name, version, or any other field.

**Step 3 — Re-resolve module lockfile via plain `npm install` (NOT `npm ci`):**
Per the drift narrative §"Why this is weird", lockfile-strict `npm ci` is the suspected reason 2.0.3 persisted in the lockfile. Use plain `npm install` to force re-resolution against the new `^2.1.0` constraint:
```bash
cd spacetimedb && npm install
```
This will rewrite `spacetimedb/package-lock.json` so the `spacetimedb` package resolves to `2.1.x` (expected `2.1.0` to match root lockfile reality).

**Step 4 — Sanity-check the lockfile change is exactly what was expected:**
```bash
git diff --stat spacetimedb/package.json spacetimedb/package-lock.json
git diff spacetimedb/package.json
```
The `package.json` diff MUST show only one line changed (`^2.0.3` → `^2.1.0`). The `package-lock.json` diff is allowed to be larger but should ONLY touch `spacetimedb`-related entries (version field, integrity hash, possibly transitive deps owned by spacetimedb itself). If the diff touches unrelated dependencies, STOP and surface to the user — something else got dragged in.

**Step 5 — Stage + commit atomically:**
```bash
git add spacetimedb/package.json spacetimedb/package-lock.json
git commit -m "$(cat <<'EOF'
chore(spacetimedb): realign module manifest with lockfile reality (^2.0.3 → ^2.1.0)

Drift fix — Commit A of Phase 16.3's 2-commit boundary (D-01).

The module manifest declared `spacetimedb: ^2.0.3` while the lockfile
pinned 2.0.3 even though the codebase uses 2.1.0-only features. The
runtime worked because maincloud is the actual execution environment,
but the manifest was misleading future readers and a fresh clone could
re-resolve to 2.0.3.

This commit aligns the module manifest with the root client manifest
(already ^2.1.0) and re-resolves the lockfile via plain `npm install`
(not `npm ci`) so future readers see one consistent floor across both
sides of the package.

See .planning/notes/spacetimedb-version-drift.md for the full narrative
and the lesson for future SDK bumps (audit BOTH manifests AND BOTH
lockfiles before proposing a new version).

The v2.2.0 bump itself rides in Commit B of this phase (D-01).
EOF
)"
```

**Step 6 — Confirm exactly one commit was added on `feature_nath_claude` and nothing was pushed:**
```bash
git log --oneline -1
git status
```
Expected: HEAD is the new commit, working tree clean, no `origin/...` ahead-marker (Commit A stays local until Plan 04 D-08 push gate).
  </action>
  <verify>
    <automated>git log -1 --pretty=%B | grep -q "spacetimedb-version-drift" && grep -q '"spacetimedb": "\^2\.1\.0"' spacetimedb/package.json && ! grep -q '"spacetimedb": "\^2\.0\.3"' spacetimedb/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"spacetimedb": "\^2\.1\.0"' spacetimedb/package.json` returns 1
    - `grep -c '"spacetimedb": "\^2\.0\.3"' spacetimedb/package.json` returns 0
    - `git log -1 --pretty=%B` contains the literal string `.planning/notes/spacetimedb-version-drift.md` (D-15)
    - `git log -1 --pretty=%B` contains the phrase "Drift fix" or "drift fix" (audit-trail clarity per D-01 rationale)
    - `git log -1 --name-only --pretty=` lists exactly 2 files: `spacetimedb/package.json` and `spacetimedb/package-lock.json`. Nothing else.
    - `git rev-parse --abbrev-ref HEAD` returns `feature_nath_claude` (D-03)
    - `git status` shows working tree clean
    - No `git push` was invoked — branch has not been pushed to origin (D-03, D-08)
  </acceptance_criteria>
  <rollback>
    If something went wrong (lockfile diff dragged in unrelated deps, wrong branch, etc.) BEFORE the commit was made: `git checkout -- spacetimedb/package.json spacetimedb/package-lock.json` reverts the working tree.
    If the commit was already made and is wrong: `git reset --soft HEAD~1` (keep the diff staged for inspection) or `git reset --hard HEAD~1` (discard entirely — only if the user approves the destructive form per CLAUDE.md autonomous-execution rules).
  </rollback>
  <done>One atomic commit on `feature_nath_claude` bumps `spacetimedb/package.json` from `^2.0.3` to `^2.1.0` with a refreshed `spacetimedb/package-lock.json`. Commit message cites `.planning/notes/spacetimedb-version-drift.md` (D-15). No remote push (D-03). No other files touched. No bindings regen (rides Commit B per D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local checkout → npm registry | `npm install` fetches packages from registry; integrity hashes in lockfile are the trust anchor |
| working tree → git history | `git commit` finalizes the change; pre-push (Plan 04) the change is reversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-01-01 | Tampering | npm registry serves a tampered `spacetimedb@2.1.x` package during re-resolution | accept | npm default integrity-hash verification + lockfile pin captured in commit. Same risk surface as every prior `npm install` in this repo. No PII or secrets touched. |
| T-16.3-01-02 | Elevation of Privilege | `npm install` postinstall script in a transitive dep executes arbitrary code on the dev machine | accept | This is the standard `npm install` risk surface; project has used `npm install` continuously without incident. Outside the scope of an SDK floor-bump. |
| T-16.3-01-03 | Repudiation | Commit A is force-pushed or amended later, masking the drift-fix audit trail | mitigate | Plan structure forbids force-push to `feature_nath_claude` per CLAUDE.md autonomous-execution rules. Plan 04's push gate is the only push event in the phase, and it pushes Commit A + Commit B atomically. |
| T-16.3-01-04 | Information Disclosure | `git diff` on `package-lock.json` accidentally exposes a private-registry token in resolution URLs | accept | Project uses public npm registry only; no private auth tokens are present in any lockfile in this repo (verified by prior commits — no `_authToken`, no scoped registry config). |
</threat_model>

<verification>
1. `grep '"spacetimedb"' spacetimedb/package.json` shows `^2.1.0`
2. `grep -A1 '"spacetimedb"' spacetimedb/package-lock.json | head -20` shows resolved version 2.1.x (not 2.0.3)
3. `git log -1 --pretty=%B` references `.planning/notes/spacetimedb-version-drift.md` literally
4. `git log -1 --name-only` lists exactly the 2 manifest/lockfile files
5. `git rev-parse --abbrev-ref HEAD` is `feature_nath_claude`
6. No `git push` was invoked (no `origin/feature_nath_claude` advancement)
</verification>

<success_criteria>
- ROADMAP success criterion #1 closed: `spacetimedb/package.json` declares `^2.1.0` minimum, committed atomically before the v2.2.0 bump
- D-01 Commit A satisfied: drift fix lands as a separate atomic commit, distinct from the v2.2.0 bump
- D-03 satisfied: commit lives on `feature_nath_claude`, not pushed
- D-15 satisfied: commit message references the drift note path verbatim
- Audit trail integrity: the commit touches ONLY the 2 declared files; no scope creep
</success_criteria>

<output>
After completion, create `.planning/phases/16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted/16.3-01-SUMMARY.md` capturing: the commit SHA of Commit A, the resolved `spacetimedb` version in the new lockfile, and any unexpected diff content (should be none).
</output>
