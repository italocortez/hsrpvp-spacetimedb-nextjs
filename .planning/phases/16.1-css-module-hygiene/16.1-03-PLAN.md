---
phase: 16.1-css-module-hygiene
plan: 03
type: execute
wave: 3
depends_on: [16.1-02]
files_modified:
  - components/features/team-builder/SynergyDisplay.module.css
  - components/features/team-builder/SynergyDisplay.tsx
  - app/(public)/teambuilder/page.module.css
autonomous: false
requirements: [R8-Rule-6, D-01, D-05]
must_haves:
  truths:
    - "SynergyDisplay.tsx imports styles from './SynergyDisplay.module.css'"
    - "Synergy pairs render with their tag backgrounds; empty-message shows when no synergies present"
    - ".synergies > h2 child-selector continues to style the header"
  artifacts:
    - path: components/features/team-builder/SynergyDisplay.module.css
      provides: "Sibling CSS module for SynergyDisplay"
      contains: ".synergies, .synergies > h2, .pair, .emptyMessage"
    - path: components/features/team-builder/SynergyDisplay.tsx
      provides: "SynergyDisplay with co-located import"
      contains: "import styles from \"./SynergyDisplay.module.css\";"
  key_links:
    - from: components/features/team-builder/SynergyDisplay.tsx
      to: components/features/team-builder/SynergyDisplay.module.css
      via: relative sibling import
      pattern: "from [\"']\\./SynergyDisplay\\.module\\.css[\"']"
---

<objective>
Co-locate SynergyDisplay styles into a new sibling module. Three classes + one child selector, all self-contained per D-07. Simplest extraction in the phase — no hover-state wiring, no @media splits, no keyframe co-location.

Purpose: Continue Phase 16.1 per-component atomic commits (commit 3 of 7 per D-10).

Output: New `SynergyDisplay.module.css` + updated `SynergyDisplay.tsx` import + thinned `page.module.css`.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md
@.planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md
@.planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md
@.planning/phases/16.1-css-module-hygiene/16.1-02-SUMMARY.md

<interfaces>
<!-- SynergyDisplay.tsx current state (from source read): -->
L5: import styles from "@/app/(public)/teambuilder/page.module.css";
<!-- Uses: styles.synergies, styles.pair, styles.emptyMessage — verified by rg -->
<!-- The .synergies > h2 rule is self-contained: h2 is a direct child rendered inside the <SynergyDisplay> return. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SynergyDisplay.module.css with .synergies (+ > h2 child selector), .pair, .emptyMessage</name>
  <files>components/features/team-builder/SynergyDisplay.module.css</files>
  <read_first>
    - app/(public)/teambuilder/page.module.css (lines 182-205 — the four rules)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-01, D-05, D-07)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (Pattern 1 code example lines 182-215 — exact CSS)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (SynergyDisplay class list lines 107-122)
    - components/features/team-builder/SynergyDisplay.tsx (verify `styles.synergies`, `styles.pair`, `styles.emptyMessage` are the only style references)
  </read_first>
  <action>
  Create `components/features/team-builder/SynergyDisplay.module.css` with file header + exact rules from `app/(public)/teambuilder/page.module.css` lines 182-205:

  ```css
  /* ═══════════════════════════════════════════════════════════════════
     SynergyDisplay — CSS Module
     ═══════════════════════════════════════════════════════════════════ */

  .synergies {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.5rem;
    padding-top: 0.375rem;
    border-top: 1px solid rgb(55, 65, 81);
  }
  .synergies > h2 {
    font-weight: bold;
    font-size: 1.25rem;
    line-height: 2.25rem;
  }
  .pair {
    padding: 0.125rem 0.375rem;
    font-weight: bold;
    border-radius: 0.5rem;
    background-color: rgb(31, 164, 169);
  }
  .emptyMessage {
    color: rgb(159, 176, 195);
    font-style: italic;
  }
  ```

  The `.synergies > h2` child selector is self-contained: the `<h2>` is rendered inside `<SynergyDisplay>`'s returned JSX alongside the `.synergies` container. Per D-07, no cross-component cascade concern.

  DO NOT use `:global()`. DO NOT copy any other class.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/features/team-builder/SynergyDisplay.module.css','utf8');const need=['.synergies','.synergies > h2','.pair','.emptyMessage'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1);}const banned=['.slot','.roster','.charactersContainer','.controls','.rosters',':global','@media','@keyframes'];const bad=banned.filter(b=>s.includes(b));if(bad.length){console.error('BANNED PRESENT:',bad);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/features/team-builder/SynergyDisplay.module.css` exists.
    - `grep -c '^\.synergies {' components/features/team-builder/SynergyDisplay.module.css` returns 1.
    - `grep -c '\.synergies > h2' components/features/team-builder/SynergyDisplay.module.css` returns 1.
    - `grep -c '^\.pair {' components/features/team-builder/SynergyDisplay.module.css` returns 1.
    - `grep -c '^\.emptyMessage {' components/features/team-builder/SynergyDisplay.module.css` returns 1.
    - No `:global()`, no `@media`, no `@keyframes`: `grep -cE ":global|@media|@keyframes" components/features/team-builder/SynergyDisplay.module.css` returns 0.
  </acceptance_criteria>
  <done>SynergyDisplay.module.css contains exactly `.synergies`, `.synergies > h2`, `.pair`, `.emptyMessage` (nothing else).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch SynergyDisplay.tsx import to sibling module; remove migrated rules from page.module.css</name>
  <files>components/features/team-builder/SynergyDisplay.tsx, app/(public)/teambuilder/page.module.css</files>
  <read_first>
    - components/features/team-builder/SynergyDisplay.tsx (L5 import line)
    - components/features/team-builder/SynergyDisplay.module.css (just created — class names confirmed)
    - app/(public)/teambuilder/page.module.css (rules being removed at lines 182-205, plus the `/* ── Synergies section ──── */` section comment at line 182)
  </read_first>
  <action>
  **Sub-step 2a — `SynergyDisplay.tsx`:**

  Replace import at L5 EXACTLY:
  - FROM: `import styles from "@/app/(public)/teambuilder/page.module.css";`
  - TO:   `import styles from "./SynergyDisplay.module.css";`
  (Double quotes.)

  No other edits to this file. All `styles.X` references remain unchanged (names are preserved).

  **Sub-step 2b — `app/(public)/teambuilder/page.module.css`:**

  Delete:
  - Section comment `/* ── Synergies section ──── */` (line 182)
  - `.synergies` block (lines 183-190)
  - `.synergies > h2` block (lines 191-195)
  - `.pair` block (lines 196-201)
  - `.emptyMessage` block (lines 202-205)

  KEEP in place (Plans 04, 05, and 06 own these):
  - File header, `.teamBuilder`, `.main`
  - `.controls*`, `.loadoutName`, `.helperText`, `.nameEditor`, `.button`, `.clearBtn`, `.menuBtn`
  - `.rosters*`, `.selectButton`, `.dropdownHeader`, `.dropdownTitle`, `.teamOption*`, `.mini*`
  - `.confirmation*`, `.dangerous`, `@keyframes grow-fade-in`, `@keyframes spin`
  </action>
  <verify>
    <automated>npm run test:typecheck 2>&1 | tail -5 && echo "---" && grep -c "from [\"']\\./SynergyDisplay\\.module\\.css[\"']" components/features/team-builder/SynergyDisplay.tsx && grep -c "@/app/.*page\\.module\\.css" components/features/team-builder/SynergyDisplay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from [\"']\\./SynergyDisplay\\.module\\.css[\"']" components/features/team-builder/SynergyDisplay.tsx` returns 1.
    - `grep -c "from [\"']@/app/.*page\\.module\\.css[\"']" components/features/team-builder/SynergyDisplay.tsx` returns 0.
    - page.module.css no longer contains SynergyDisplay-owned classes:
      - `grep -c "^\.synergies " app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.pair " app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.emptyMessage " app/\(public\)/teambuilder/page.module.css` returns 0
    - page.module.css still contains Plan 04/05/06 classes:
      - `grep -c "^\.controls " app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.rostersList " app/\(public\)/teambuilder/page.module.css` returns 1
    - `npm run test:typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>SynergyDisplay.tsx imports sibling module. page.module.css has no synergy-related rules. Build + typecheck pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-11 visual verification of SynergyDisplay migration on /teambuilder</name>
  <what-built>SynergyDisplay co-located CSS module (commit 3 of 7 per D-10).</what-built>
  <how-to-verify>
    1. `git status` confirms only 3 files touched: `SynergyDisplay.module.css` (new), `SynergyDisplay.tsx` (import swap), `page.module.css` (4 rules removed).
    2. `npm run dev`.
    3. Navigate to `http://localhost:3000/teambuilder`.
    4. Add at least 2 characters that have a known synergy pair (refer to your data — e.g., two characters of the same element/path that generate a synergy).
    5. The "Synergies" section below the roster should render:
       - `<h2>Synergies</h2>` header with bold, large font (`.synergies > h2` child selector).
       - Each synergy pair rendered as a pill (teal-cyan background `rgb(31, 164, 169)`).
       - Flex-wrap layout with 0.5rem gaps.
    6. Remove all characters. The "No synergies" (or equivalent empty state) text should render in italic muted color (`.emptyMessage` — `rgb(159, 176, 195)` italic).
    7. DevTools console: no new error.
  </how-to-verify>
  <resume-signal>Type "approved" if the synergy pills render correctly + empty message shows italic + no errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Pure CSS co-location + one import string change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-03-01 | N/A | N/A | accept | No new attack surface — CSS-only refactor, single TSX edit limited to import-path string. |
</threat_model>

<verification>
- `grep -rn "from [\"']@/app/.*\\.module\\.css[\"']" components/features/team-builder/SynergyDisplay.tsx` returns 0.
- `npm run test:typecheck` exits 0.
- `npm run build` exits 0.
- D-11 visual check confirms synergy pills and empty message render correctly.
</verification>

<success_criteria>
1. SynergyDisplay.module.css has `.synergies`, `.synergies > h2`, `.pair`, `.emptyMessage` only.
2. SynergyDisplay.tsx imports from `./SynergyDisplay.module.css`.
3. page.module.css no longer contains those 4 rules.
4. Visual D-11 passes (synergy pills + empty message render correctly).
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-css-module-hygiene/16.1-03-SUMMARY.md`
</output>
