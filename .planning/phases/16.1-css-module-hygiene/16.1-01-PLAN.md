---
phase: 16.1-css-module-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/features/team-builder/Teamslot.module.css
  - components/features/team-builder/Teamslot.tsx
  - app/(public)/teambuilder/page.module.css
autonomous: false
requirements: [R8-Rule-6, D-01, D-02, D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "Teamslot.tsx imports styles from './Teamslot.module.css' (sibling, relative)"
    - "@keyframes fade-in lives in Teamslot.module.css alongside .imposition"
    - "The imposition dropdown fades in smoothly when a lightcone is added"
    - "Responsive @media overrides for .path/.verticals/.clearButton/.portrait apply at <=800px and <=1070px"
    - "The clearButton hover-reveal works end-to-end once Plan 02 wires TeamRoster's hover state"
  artifacts:
    - path: components/features/team-builder/Teamslot.module.css
      provides: "Sibling CSS module for Teamslot component"
      contains: ".slot, .empty, .dragging, .dropTarget, .path, .portrait, .clearButton, .clearButtonVisible, .character, .icons, .element, .verticals, .eidolon, .imposition, @keyframes fade-in, @media (max-width: 800px), @media (max-width: 1070px)"
    - path: components/features/team-builder/Teamslot.tsx
      provides: "Teamslot component with co-located import and isRosterHovered prop"
      contains: "import styles from \"./Teamslot.module.css\";"
  key_links:
    - from: components/features/team-builder/Teamslot.tsx
      to: components/features/team-builder/Teamslot.module.css
      via: relative sibling import
      pattern: "from [\"']\\./Teamslot\\.module\\.css[\"']"
    - from: components/features/team-builder/Teamslot.module.css
      to: ".imposition { animation: fade-in 300ms; }"
      via: "local @keyframes fade-in in same file (CSS Modules scope)"
      pattern: "@keyframes fade-in"
---

<objective>
Co-locate Teamslot styles into a new sibling `components/features/team-builder/Teamslot.module.css`, move `@keyframes fade-in` with its consumer, split the responsive `@media` blocks so Teamslot-owned overrides travel here, add the `isRosterHovered` prop stub + `.clearButtonVisible` hover class, and switch the TSX import to the sibling module. Remove the migrated rules from `app/(public)/teambuilder/page.module.css`.

Purpose: Fix leaf→trunk CSS import inversion (R8 retroactive enforcement). Teamslot is the first of five team-builder components to de-couple from page.module.css.

Output: New `Teamslot.module.css` + updated `Teamslot.tsx` import + thinned `page.module.css`.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md
@.planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md
@.planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md

<interfaces>
<!-- Canonical import pattern for new sibling modules (from existing compliant code). -->
<!-- From components/features/costs/components/CharacterCostTable.tsx L9: -->
import styles from './CharacterCostTable.module.css';
<!-- From components/features/team-builder/cost-breakdown-chart/CostBreakdownChart.tsx L7: -->
import styles from "./CostBreakdownChart.module.css";

<!-- Teamslot.tsx current state (from source read): -->
L4: import styles from "@/app/(public)/teambuilder/page.module.css";
L12: interface TeamSlotProps { ... }
L97-98: onMouseEnter={() => setIsHovered(true)}    // per-slot hover, data-slot-hover — UNCHANGED
        onMouseLeave={() => setIsHovered(false)}

<!-- File header convention (from LightconeSelector.module.css L1-10): -->
/* ═══════════════════════════════════════════════════════════════════
   Teamslot — CSS Module

   isRosterHovered prop from TeamRoster toggles .clearButtonVisible to
   replace the cross-file cascade `.roster:hover .clearButton { opacity: 1 }`.
   ═══════════════════════════════════════════════════════════════════ */
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Teamslot.module.css with full Teamslot class set, @keyframes fade-in, and responsive overrides</name>
  <files>components/features/team-builder/Teamslot.module.css</files>
  <read_first>
    - app/(public)/teambuilder/page.module.css (lines 1-570 — source of truth; all rules to migrate come from here)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-01 through D-06 — partition rule, dead rules list, keyframe co-location rule, @media split rule, PascalCase filename, hover cascade resolution)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (Pitfall 1 keyframe co-location, Pitfall 2 @media split spans two components, Pitfall 3 clearButton opacity during inter-commit window; class-ownership map lines 473-486)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (File header comment convention lines 32-41, hover-reveal pattern lines 60-71, classes assigned to this file lines 74-85)
    - components/features/drafting/components/LightconeSelector.module.css (header comment + section-comment convention)
    - components/features/team-builder/Teamslot.tsx (verify every `styles.X` call site; existing Teamslot.tsx has `isHovered` state for `data-slot-hover` — do NOT confuse with new `isRosterHovered` prop)
  </read_first>
  <action>
  Create `components/features/team-builder/Teamslot.module.css` as a new file.

  File header (follow PATTERNS.md triple-bar convention):
  ```css
  /* ═══════════════════════════════════════════════════════════════════
     Teamslot — CSS Module

     isRosterHovered prop from TeamRoster toggles .clearButtonVisible
     to replace the cross-file cascade `.roster:hover .clearButton { opacity: 1 }`
     that was removed with the page.module.css partition (Phase 16.1 D-06).
     ═══════════════════════════════════════════════════════════════════ */
  ```

  Copy EXACTLY these rules from `app/(public)/teambuilder/page.module.css` into the new file, preserving order and section comments:

  1. `.slot` (lines 39-46)
  2. `.empty` (lines 49-57)
  3. `.dragging` (lines 60-63)
  4. `.dropTarget` (lines 64-66)
  5. `.path` (lines 69-77)
  6. `.portrait` (lines 80-89)
  7. `.clearButton` (lines 92-105) — keep `opacity: 0` and the `transition` line
  8. `.clearButton:hover` (lines 109-111)
  9. **NEW** `.clearButtonVisible { opacity: 1; }` — add this class immediately after `.clearButton:hover`. This is the D-06 JS-driven replacement for `.roster:hover .clearButton { opacity: 1 }` (the old page.module.css line 106-108 rule is NOT migrated; it is replaced).
  10. `.character`, `.character:hover`, `.character:active` (lines 114-120)
  11. `.icons`, `.element` (lines 123-136). **DO NOT** copy the commented-out `:global(.synergy-icon)` block at lines 137-141 — skip the comment entirely.
  12. `.verticals`, `.verticals option` (lines 144-157)
  13. `.eidolon, .imposition` (lines 159-171)
  14. `.imposition` override (lines 172-176) — includes `animation: fade-in 300ms;`
  15. `.eidolon:hover, .imposition:hover` (lines 177-180)
  16. `@keyframes fade-in` (lines 521-524) — MUST live in this file per D-03 / RESEARCH.md Pitfall 1 (CSS Modules scope keyframe names per file; cross-file animation reference silently fails).
  17. `@media screen and (max-width: 800px)` block — split per D-04 / RESEARCH.md Pitfall 2. Copy ONLY the Teamslot-owned overrides:
      ```css
      @media screen and (max-width: 800px) {
        .path {
          top: 0;
          height: 100%;
          object-fit: cover;
        }
        .verticals {
          left: 0.25rem;
          bottom: 0.25rem;
        }
        .clearButton {
          opacity: 1;
        }
      }
      ```
      (The `.charactersContainer` override stays in page.module.css for Plan 02 to migrate to TeamRoster.module.css.)
  18. `@media screen and (max-width: 1070px)` block (lines 563-570 from page.module.css) — copy the whole block: `.portrait { object-position: 50% 25%; }` and `.clearButton { opacity: 1; }`.

  DO NOT copy: `.teamBuilder`, `.main`, `.roster`, `.charactersContainer`, `.synergies`, `.pair`, `.emptyMessage`, `.controls*`, `.loadoutName`, `.helperText`, `.nameEditor`, `.button`, `.clearBtn`, `.menuBtn`, `.rosters*`, `.selectButton`, `.dropdownHeader`, `.dropdownTitle`, `.teamOption*`, `.mini*`, `.confirmation*`, `.dangerous`, `@keyframes grow-fade-in`, `@keyframes spin`.

  DO NOT use `:global()` anywhere in this file (PATTERNS.md Critical Gotcha 6).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/features/team-builder/Teamslot.module.css','utf8');const need=['.slot','.empty','.dragging','.dropTarget','.path','.portrait','.clearButton','.clearButtonVisible','.character','.icons','.element','.verticals','.eidolon','.imposition','@keyframes fade-in','max-width: 800px','max-width: 1070px'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1);}const banned=['.teamBuilder','.roster','.charactersContainer','.synergies','.controls','.confirmation','grow-fade-in','@keyframes spin',':global'];const bad=banned.filter(b=>s.includes(b));if(bad.length){console.error('BANNED PRESENT:',bad);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/features/team-builder/Teamslot.module.css` exists.
    - Grep each class is present in the new file:
      - `grep -c '^\.slot {' components/features/team-builder/Teamslot.module.css` returns 1
      - `grep -c '^\.clearButton {' components/features/team-builder/Teamslot.module.css` returns 1
      - `grep -c '^\.clearButtonVisible {' components/features/team-builder/Teamslot.module.css` returns 1 (NEW class)
      - `grep -c '^\.imposition {' components/features/team-builder/Teamslot.module.css` returns 1
      - `grep -c '@keyframes fade-in' components/features/team-builder/Teamslot.module.css` returns 1
      - `grep -c 'max-width: 800px' components/features/team-builder/Teamslot.module.css` returns 1
      - `grep -c 'max-width: 1070px' components/features/team-builder/Teamslot.module.css` returns 1
    - Grep none of the banned classes are present:
      - `grep -c '\.teamBuilder\|\.roster\b\|\.charactersContainer\|\.synergies\|\.controls\b\|\.confirmation' components/features/team-builder/Teamslot.module.css` returns 0
      - `grep -c 'grow-fade-in\|@keyframes spin\|:global' components/features/team-builder/Teamslot.module.css` returns 0
  </acceptance_criteria>
  <done>Teamslot.module.css file exists with all Teamslot-owned classes + `.clearButtonVisible` + `@keyframes fade-in` + both `@media` blocks (split per D-04); no banned/dead classes present; no `:global()`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch Teamslot.tsx import to sibling module + add isRosterHovered prop + conditional clearButtonVisible class; remove migrated rules from page.module.css</name>
  <files>components/features/team-builder/Teamslot.tsx, app/(public)/teambuilder/page.module.css</files>
  <read_first>
    - components/features/team-builder/Teamslot.tsx (current import line at L4; existing `isHovered` state for `data-slot-hover` on L97-98 must NOT be renamed; interface at L12; export signature at L36; destructured props at L54)
    - components/features/team-builder/TeamRoster.tsx (confirms how TeamSlot is called — L80 onwards; Plan 02 will add `isRosterHovered={isRosterHovered}` so the prop type in Plan 01 must be compatible)
    - components/features/team-builder/Teamslot.module.css (just created in Task 1 — confirms class names available)
    - app/(public)/teambuilder/page.module.css (source of rules being REMOVED by this commit)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (Pitfall 3 — reasoning for wiring `isRosterHovered` prop stub in commit 1 before TeamRoster wires the real value in commit 2)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (Teamslot interface + className pattern lines 278-287; import line replacement table lines 246-253)
  </read_first>
  <action>
  **Sub-step 2a — `Teamslot.tsx` edits:**

  1. Replace the import line at L4 EXACTLY:
     - FROM: `import styles from "@/app/(public)/teambuilder/page.module.css";`
     - TO:   `import styles from "./Teamslot.module.css";`
     (Double quotes — match the existing file's quote style.)

  2. Add `isRosterHovered` to the `TeamSlotProps` interface (around L12-onwards). Place as the LAST prop. Type `boolean`, NOT optional — D-06 requires TeamRoster to always pass it in Plan 02; defaulting here would hide the contract:
     ```typescript
     isRosterHovered: boolean;
     ```
     Update the destructured param list in the `export function TeamSlot({ ... }: TeamSlotProps)` signature (around L36-54) to include `isRosterHovered`.

  3. At the clearButton `<button>` element (find with `grep -n clearButton components/features/team-builder/Teamslot.tsx`), change the `className` prop from `styles.clearButton` to the conditional:
     ```tsx
     className={`${styles.clearButton}${isRosterHovered ? ` ${styles.clearButtonVisible}` : ""}`}
     ```
     Do NOT modify the button's other attributes (`onClick`, `title`, `aria-label`, icon children).

  4. Because Plan 01 is committed before Plan 02 wires TeamRoster's `isRosterHovered`, the existing `TeamSlot` call sites in `TeamRoster.tsx` (and anywhere else `<TeamSlot .../>` is rendered) will fail TypeScript compilation without the prop. Add the prop to every `<TeamSlot>` call site you find:
     - Run `grep -rn "<TeamSlot" components/ app/ lib/` to enumerate call sites.
     - For each call site, add `isRosterHovered={false}` as a temporary stub. Plan 02 will replace TeamRoster's `false` with the real hover state. Do NOT edit any other prop on these call sites.

  5. DO NOT rename or touch the existing per-slot `isHovered` state (around L97-98) — that is for `data-slot-hover` attribute and is unrelated to the roster-level hover signal. The two states coexist.

  **Sub-step 2b — `app/(public)/teambuilder/page.module.css` edits (REMOVE migrated rules):**

  Delete ONLY the rules this commit migrated to Teamslot.module.css (per D-10 commit 1 "atomic" convention). Keep everything else intact — Plans 02-05 and 06 will handle their own migrations and the dead-rule sweep.

  Delete these rule blocks from page.module.css:
  - `.slot` block (lines 39-46)
  - `.empty` block (lines 49-57)
  - `.dragging` and `.dropTarget` (lines 60-66)
  - `.path` block (lines 69-77)
  - `.portrait` block (lines 80-89)
  - `.clearButton` block (lines 92-105)
  - `.roster:hover .clearButton { opacity: 1; }` (lines 106-108) — this is the cross-file cascade replaced by `.clearButtonVisible` + JS (D-06). DELETE, do not relocate.
  - `.clearButton:hover` (lines 109-111)
  - `.character`, `.character:hover`, `.character:active` (lines 114-120)
  - `.icons` (lines 123-131)
  - `.element` (lines 132-136)
  - the commented `:global(.synergy-icon)` block (lines 137-141) — delete along with its section
  - `.verticals` (lines 144-153)
  - `.verticals option` (lines 154-157)
  - `.eidolon, .imposition` base (lines 159-171)
  - `.imposition` override (lines 172-176)
  - `.eidolon:hover, .imposition:hover` (lines 177-180)
  - `@keyframes fade-in` (lines 521-524)
  - Inside `@media screen and (max-width: 800px)` block (lines 545-561): delete `.path`, `.verticals`, `.clearButton` overrides. Keep `.charactersContainer` override — it goes to TeamRoster.module.css in Plan 02.
  - Entire `@media screen and (max-width: 1070px)` block (lines 563-570) — delete (both `.portrait` and `.clearButton` are Teamslot-owned per D-04).

  KEEP in place (Plans 02-05 and 06 own these):
  - File header comment (lines 1-7)
  - `.teamBuilder` (lines 9-13)
  - `.main`, `.roster`, `.charactersContainer`, `.synergies`, `.synergies > h2`, `.pair`, `.emptyMessage`, all `.controls*`, `.loadoutName`, `.helperText`, `.nameEditor`, `.button`, `.clearBtn`, `.menuBtn`, all `.rosters*`/`.selectButton`/`.dropdownHeader`/`.dropdownTitle`/`.teamOption*`/`.mini*`, all `.confirmation*`, `.dangerous`, `@keyframes grow-fade-in`, `@keyframes spin`
  - Remaining part of `@media (max-width: 800px)` with only `.charactersContainer` override
  </action>
  <verify>
    <automated>npm run test:typecheck 2>&1 | tail -20 && echo "---" && grep -c "from [\"']\\./Teamslot\\.module\\.css[\"']" components/features/team-builder/Teamslot.tsx && echo "---" && grep -c "isRosterHovered" components/features/team-builder/Teamslot.tsx && echo "---" && grep -c "@/app/.*page\\.module\\.css" components/features/team-builder/Teamslot.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from [\"']\\./Teamslot\\.module\\.css[\"']" components/features/team-builder/Teamslot.tsx` returns exactly 1 match.
    - `grep -n "from [\"']@/app/.*page\\.module\\.css[\"']" components/features/team-builder/Teamslot.tsx` returns zero matches.
    - `grep -c "isRosterHovered" components/features/team-builder/Teamslot.tsx` returns at least 3 (interface declaration + destructure + className conditional).
    - `grep -c "styles\\.clearButtonVisible" components/features/team-builder/Teamslot.tsx` returns at least 1.
    - Existing `isHovered` state still present: `grep -c "setIsHovered" components/features/team-builder/Teamslot.tsx` returns at least 2 (unchanged from before this plan).
    - Every `<TeamSlot` call site has `isRosterHovered=` prop: `grep -rn "<TeamSlot" components/ app/ lib/ | wc -l` equals `grep -rn "<TeamSlot[^>]*isRosterHovered" components/ app/ lib/ | wc -l`.
    - page.module.css no longer contains Teamslot-owned classes:
      - `grep -cE '^\.(slot|empty|dragging|dropTarget|path|portrait|clearButton|character|icons|element|verticals|eidolon|imposition) ' app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "@keyframes fade-in" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "roster:hover .clearButton" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "max-width: 1070px" app/\(public\)/teambuilder/page.module.css` returns 0
    - page.module.css still contains Plan 02-06 classes:
      - `grep -c "^\.roster " app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.charactersContainer " app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.synergies " app/\(public\)/teambuilder/page.module.css` returns 1
    - `npm run test:typecheck` exits 0 (stub `isRosterHovered={false}` on call sites satisfies TS until Plan 02 wires the real value).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Teamslot.tsx imports its sibling module, exposes `isRosterHovered` prop, applies `.clearButtonVisible` conditionally. page.module.css is thinned of Teamslot-owned rules. TypeScript + build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-11 visual verification of Teamslot migration on /teambuilder</name>
  <what-built>Teamslot co-located CSS module + import swap + isRosterHovered prop stub (commit 1 of 7 per D-10).</what-built>
  <how-to-verify>
    1. `git status` — confirm exactly these files changed: `components/features/team-builder/Teamslot.module.css` (new), `components/features/team-builder/Teamslot.tsx` (modified), `components/features/team-builder/TeamRoster.tsx` (stub added), `app/(public)/teambuilder/page.module.css` (thinned). No other files.
    2. Run `npm run dev`.
    3. Navigate to `http://localhost:3000/teambuilder`.
    4. Add a character to a slot (drag/drop or click). The slot portrait should render with the path background icon visible behind it.
    5. Hover the character slot — the `data-slot-hover` behavior (per-slot visuals like the LC search bar reveal if that's wired) should work as before.
    6. Hover over a slot — the red clearButton (remove/X button) in the top-left of the slot should remain invisible (opacity 0). THIS IS EXPECTED at this commit — the roster-level hover handler is not wired until Plan 02. DO NOT panic.
    7. Confirm the clearButton DOES show at the `<=800px` viewport width (resize browser) — the `@media (max-width: 800px)` override forces `opacity: 1`. Also at `<=1070px`.
    8. Add a lightcone to a slot — the imposition selector should FADE IN smoothly (the `@keyframes fade-in` animation). If it pops in instantly, the keyframe is mislocated. Regression.
    9. Resize browser below 800px — the characters grid should still be 4 columns at this commit (the `.charactersContainer` `@media` override still lives in page.module.css; Plan 02 moves it). This is EXPECTED.
    10. DevTools console: no "preload not used" warning is NOT yet expected (commit 7 sweep is where we verify). No TypeError / no runtime error is REQUIRED.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass or describe the issue (e.g., "imposition doesn't animate", "portrait missing").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan is a pure CSS + component-prop refactor with no new data flows, no network calls, no auth, no user input, no data persistence changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-01-01 | N/A | N/A | accept | No new attack surface — CSS module file + TSX import-path change + one new React prop (`isRosterHovered: boolean`) that flows strictly parent→child within an already-rendered subtree. No TSX logic changes beyond the import string and the className conditional. |
</threat_model>

<verification>
- `grep -rn "from [\"']@/app/.*\\.module\\.css[\"']" components/features/team-builder/Teamslot.tsx` returns zero matches.
- `grep -rn "from [\"']\\./Teamslot\\.module\\.css[\"']" components/features/team-builder/Teamslot.tsx` returns exactly 1 match.
- `npm run test:typecheck` exits 0.
- `npm run build` exits 0.
- D-11 visual verification passes (checkpoint above).
</verification>

<success_criteria>
1. Teamslot.module.css exists with all 16 migrated class blocks + `.clearButtonVisible` + `@keyframes fade-in` + both `@media` blocks (Teamslot-owned subsets per D-04).
2. Teamslot.tsx imports from `./Teamslot.module.css` and accepts `isRosterHovered: boolean` prop.
3. Every `<TeamSlot>` call site passes `isRosterHovered` (stubbed to `false` where TeamRoster hasn't wired it yet — Plan 02 completes the wiring).
4. page.module.css no longer contains Teamslot-owned class rules, but retains TeamRoster / SynergyDisplay / LoadoutDropdown / LoadoutControls / .teamBuilder / confirmation / dead-keyframe rules for subsequent plans.
5. TypeScript + build pass; the `/teambuilder` route visual verification (D-11) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-css-module-hygiene/16.1-01-SUMMARY.md`
</output>
