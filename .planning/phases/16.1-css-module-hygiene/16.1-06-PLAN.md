---
phase: 16.1-css-module-hygiene
plan: 06
type: execute
wave: 6
depends_on: [16.1-05]
files_modified:
  - components/features/costs/components/LightconeCostTable.module.css
  - components/features/costs/components/LightconeCostTable.tsx
autonomous: false
requirements: [R8-Rule-6, D-08, D-09]
must_haves:
  truths:
    - "LightconeCostTable.tsx no longer imports CharacterPool.module.css from the drafting feature"
    - "The 4 filter classes (filters, paths, searchBar, clearButton) live in a local LightconeCostTable.module.css"
    - "Filters row renders at the top of /costs with identical layout to before"
    - "Clear button has disabled + hover states matching the original"
    - "CharacterPool.module.css is untouched (D-09)"
  artifacts:
    - path: components/features/costs/components/LightconeCostTable.module.css
      provides: "Local filter-row CSS (duplicated from drafting per D-08, below shared-extract threshold)"
      contains: ".filters, .paths, .searchBar, .clearButton"
    - path: components/features/costs/components/LightconeCostTable.tsx
      provides: "LightconeCostTable consuming local filterStyles + existing styles (CharacterCostTable)"
      contains: "import filterStyles from './LightconeCostTable.module.css';"
  key_links:
    - from: components/features/costs/components/LightconeCostTable.tsx
      to: components/features/costs/components/LightconeCostTable.module.css
      via: relative sibling import as filterStyles
      pattern: "from [\"']\\./LightconeCostTable\\.module\\.css[\"']"
    - from: components/features/costs/components/LightconeCostTable.tsx
      to: components/features/costs/components/CharacterCostTable.module.css
      via: relative same-feature import as styles (for rarityBadge + tableWrapper — PATTERNS.md dual-import scenario)
      pattern: "from [\"']\\./CharacterCostTable\\.module\\.css[\"']"
---

<objective>
Eliminate the cross-feature CSS import `LightconeCostTable.tsx → @/components/features/drafting/components/CharacterPool.module.css`. Duplicate the 4 used filter classes (`filters`, `paths`, `searchBar`, `clearButton`) into a new local `components/features/costs/components/LightconeCostTable.module.css`. Per PATTERNS.md dual-import scenario: keep the existing same-feature `import styles from './CharacterCostTable.module.css'` for `rarityBadge` / `tableWrapper`. Introduce `filterStyles` alias for the new module.

Purpose: Commit 6 of 7 per D-10. Resolves ROADMAP success criterion #2 (no cross-feature CSS imports).

Output: New `LightconeCostTable.module.css` (4 filter classes) + dual-import in `LightconeCostTable.tsx` + 4 `poolStyles.X` → `filterStyles.X` rename. `CharacterPool.module.css` is NOT edited (D-09).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md
@.planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md
@.planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md
@.planning/phases/16.1-css-module-hygiene/16.1-05-SUMMARY.md
@components/features/drafting/components/CharacterPool.module.css
@components/features/costs/components/CharacterCostTable.module.css

<interfaces>
<!-- LightconeCostTable.tsx current state (from source read): -->
L10: import poolStyles from '@/components/features/drafting/components/CharacterPool.module.css';  // CROSS-FEATURE — REMOVE
L11: import styles from './CharacterCostTable.module.css';                                           // SAME-FEATURE — KEEP

<!-- Call sites (from grep): -->
L67:  <span className={styles.rarityBadge} ... />                    // styles (CharacterCostTable) — keep
L76:  <div className={poolStyles.filters}>                            // REPLACE with filterStyles
L78:  className={poolStyles.paths}                                     // REPLACE with filterStyles
L92:  className={poolStyles.searchBar}                                 // REPLACE with filterStyles
L97:  className={poolStyles.clearButton}                               // REPLACE with filterStyles
L117: base: `... ${styles.tableWrapper}`                              // styles (CharacterCostTable) — keep

<!-- Exact CSS content to duplicate (pre-validated in RESEARCH.md Pattern 4 lines 308-376 — copy verbatim except drop the .paths child-button selectors per Pitfall 6). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LightconeCostTable.module.css with 4 filter classes duplicated from CharacterPool.module.css</name>
  <files>components/features/costs/components/LightconeCostTable.module.css</files>
  <read_first>
    - components/features/drafting/components/CharacterPool.module.css (source — read `.filters`, `.paths` (container rule only), `.searchBar`, `.clearButton` rules including `:hover`, `:focus`, `:disabled` pseudo-classes)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-08, D-09 — duplicate-not-extract, do not edit CharacterPool)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (Pattern 4 lines 308-376 — the exact CSS content to copy; Pitfall 6 — do NOT copy `.paths > button` child-selector rules because LightconeCostTable uses `<FilterButtonGroup>` which owns its own button styles)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (LightconeCostTable.module.css pattern lines 164-176 — duplication rule + Pitfall 6 reminder)
    - components/features/costs/components/LightconeCostTable.tsx (confirm the 4 poolStyles call sites at L76, L78, L92, L97)
  </read_first>
  <action>
  Create `components/features/costs/components/LightconeCostTable.module.css`:

  File header:
  ```css
  /* ═══════════════════════════════════════════════════════════════════
     LightconeCostTable — CSS Module

     Filters section duplicated from CharacterPool.module.css
     (Phase 16.1 D-08 — 1 consumer × 4 classes is well below
     the ≥20 lines × ≥3 sites shared-extract threshold).
     ═══════════════════════════════════════════════════════════════════ */
  ```

  Copy EXACTLY these 4 class rules from `components/features/drafting/components/CharacterPool.module.css`. Use class names `.filters`, `.paths`, `.searchBar`, `.clearButton` (keep the same names; the TSX rename is from `poolStyles.X` → `filterStyles.X`, so the class names themselves do not change — Claude's Discretion note in D-08 allows renaming, but keeping the same names is simpler and reads clearly at the call site).

  Rules to copy (read exact values from CharacterPool.module.css):

  1. `.filters` — the container flex rule (width, display, flex-wrap, gap).
  2. `.paths` — CONTAINER RULE ONLY per RESEARCH.md Pitfall 6. Include: `flex-grow`, `height`, `overflow-x`, `scrollbar-width`, `-ms-overflow-style`, `display`, `justify-content`, `align-items`, `border`, `background-color`, `border-radius`, `box-shadow`, `transition`. DO NOT copy `.paths > button`, `.paths > button:not(.selected):hover`, `.paths > button:last-child`, or any `.roles > button` / `.elements > button` child selectors — LightconeCostTable renders `<FilterButtonGroup>` which provides its own internal button markup + styling.
  3. `.searchBar` base rule + `.searchBar:hover, .searchBar:focus` (both pseudo-classes share the same declaration block).
  4. `.clearButton` base rule + `.clearButton:hover:not(:disabled)` + `.clearButton:disabled`.

  Canonical reference: RESEARCH.md Pattern 4 code block at lines 308-376 provides the exact pre-validated CSS. Copy that block into the new file, ensuring:
  - No child-button selectors (Pitfall 6).
  - No `@keyframes`, no `@media`, no `:global()`.
  - No other classes from CharacterPool (e.g., `.container`, `.roles`, `.elements`, `.pool*`, `.character*` — all excluded).

  **Important:** `CharacterPool.module.css` is NOT edited (D-09). Only read from.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/features/costs/components/LightconeCostTable.module.css','utf8');const need=['.filters','.paths','.searchBar','.clearButton',':hover','flex-grow'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1);}const banned=['.paths > button','.roles','.elements','.pool',':global','@keyframes','@media','.rarityBadge','.tableWrapper'];const bad=banned.filter(b=>s.includes(b));if(bad.length){console.error('BANNED PRESENT:',bad);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/features/costs/components/LightconeCostTable.module.css` exists.
    - All 4 required classes + pseudo-classes present:
      - `grep -c '^\.filters {' components/features/costs/components/LightconeCostTable.module.css` returns 1
      - `grep -c '^\.paths {' components/features/costs/components/LightconeCostTable.module.css` returns 1
      - `grep -c '^\.searchBar {' components/features/costs/components/LightconeCostTable.module.css` returns 1
      - `grep -c '^\.clearButton {' components/features/costs/components/LightconeCostTable.module.css` returns 1
      - `grep -c '\.clearButton:hover:not(:disabled)' components/features/costs/components/LightconeCostTable.module.css` returns 1
      - `grep -c '\.clearButton:disabled' components/features/costs/components/LightconeCostTable.module.css` returns 1
    - NO child-button selectors copied (Pitfall 6):
      - `grep -c '\.paths > button' components/features/costs/components/LightconeCostTable.module.css` returns 0
      - `grep -cE '\\.(roles|elements)' components/features/costs/components/LightconeCostTable.module.css` returns 0
    - No other non-filter classes:
      - `grep -cE "\\.(pool|character|rarityBadge|tableWrapper)" components/features/costs/components/LightconeCostTable.module.css` returns 0
    - No `:global()`, no `@keyframes`, no `@media`:
      - `grep -cE ":global|@keyframes|@media" components/features/costs/components/LightconeCostTable.module.css` returns 0
    - CharacterPool.module.css is UNTOUCHED (D-09):
      - `git diff components/features/drafting/components/CharacterPool.module.css` returns empty
  </acceptance_criteria>
  <done>LightconeCostTable.module.css exists with 4 filter classes + their pseudo-classes, no child-button selectors, no stray content. CharacterPool.module.css unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite LightconeCostTable.tsx — drop poolStyles cross-feature import, add filterStyles local import, replace 4 call sites</name>
  <files>components/features/costs/components/LightconeCostTable.tsx</files>
  <read_first>
    - components/features/costs/components/LightconeCostTable.tsx (full file — confirm L10 `poolStyles` import, L11 `styles` import from CharacterCostTable.module.css; L67 `styles.rarityBadge`, L76 `poolStyles.filters`, L78 `poolStyles.paths`, L92 `poolStyles.searchBar`, L97 `poolStyles.clearButton`, L117 `styles.tableWrapper`)
    - components/features/costs/components/LightconeCostTable.module.css (just created)
    - components/features/costs/components/CharacterCostTable.module.css (verify `rarityBadge` and `tableWrapper` classes still exist there — they are the reason `styles` import stays)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-08, D-09 — exact change scope)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (Pitfall 5 — rename must be atomic at all 4 call sites)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (dual-import pattern lines 193-198; Critical Gotcha 4)
  </read_first>
  <action>
  Edit `components/features/costs/components/LightconeCostTable.tsx`:

  **Edit 1 — imports (L10-11):**
  - REMOVE line 10: `import poolStyles from '@/components/features/drafting/components/CharacterPool.module.css';`
  - ADD replacement on the same line: `import filterStyles from './LightconeCostTable.module.css';`
  - KEEP line 11: `import styles from './CharacterCostTable.module.css';` (unchanged — this is the dual-import per PATTERNS.md; `styles` owns `rarityBadge` and `tableWrapper`).

  Final imports block around L10-11:
  ```typescript
  import filterStyles from './LightconeCostTable.module.css';
  import styles from './CharacterCostTable.module.css';
  ```
  (Match the existing quote style — single quotes throughout this file.)

  **Edit 2 — 4 call-site renames (atomic per Pitfall 5):**
  - L76: `<div className={poolStyles.filters}>` → `<div className={filterStyles.filters}>`
  - L78: `className={poolStyles.paths}` → `className={filterStyles.paths}`
  - L92: `className={poolStyles.searchBar}` → `className={filterStyles.searchBar}`
  - L97: `className={poolStyles.clearButton}` → `className={filterStyles.clearButton}`

  **DO NOT touch** the `styles.X` call sites (L67 `styles.rarityBadge`, L117 `styles.tableWrapper` inside the template literal) — they continue to reference CharacterCostTable.module.css.

  **DO NOT touch** any other prop, import, component structure, or logic.
  </action>
  <verify>
    <automated>npm run test:typecheck 2>&1 | tail -10 && echo "---IMPORT CHECK---" && grep -n "from.*CharacterPool\\|filterStyles\\|poolStyles" components/features/costs/components/LightconeCostTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ZERO references to `poolStyles` in the file: `grep -c "poolStyles" components/features/costs/components/LightconeCostTable.tsx` returns 0.
    - ZERO cross-feature import: `grep -c "@/components/features/drafting/components/CharacterPool" components/features/costs/components/LightconeCostTable.tsx` returns 0.
    - `filterStyles` import present: `grep -c "from [\"']\\./LightconeCostTable\\.module\\.css[\"']" components/features/costs/components/LightconeCostTable.tsx` returns 1.
    - `styles` import preserved (same-feature dual-import): `grep -c "from [\"']\\./CharacterCostTable\\.module\\.css[\"']" components/features/costs/components/LightconeCostTable.tsx` returns 1.
    - All 4 filter call sites renamed: `grep -c "filterStyles\\." components/features/costs/components/LightconeCostTable.tsx` returns exactly 4.
    - `styles.rarityBadge` and `styles.tableWrapper` still referenced (rarityBadge + tableWrapper in template):
      - `grep -c "styles\\.rarityBadge" components/features/costs/components/LightconeCostTable.tsx` returns at least 1
      - `grep -c "styles\\.tableWrapper" components/features/costs/components/LightconeCostTable.tsx` returns at least 1
    - `npm run test:typecheck` exits 0 (no "Cannot find name 'poolStyles'" error).
    - `npm run build` exits 0.
    - GLOBAL success criterion #1: no component imports from `@/app/*.module.css`:
      - `grep -rn "from ['\\\"]@/app/.*\\.module\\.css['\\\"]" components/ 2>/dev/null` returns 0 matches (all 5 team-builder + costs migrations done).
    - GLOBAL success criterion #2: no cross-feature CSS imports:
      - Run `grep -rn "from ['\\\"]@/components/features/[^/]\\+/.\\+\\.module\\.css['\\\"]" components/features/` — any lines reported must have the importer's feature name match the imported path's feature name. For LightconeCostTable specifically, `grep -rn "CharacterPool\\.module\\.css" components/features/costs/` returns 0.
  </acceptance_criteria>
  <done>LightconeCostTable.tsx uses dual same-feature imports (filterStyles + styles). poolStyles and the cross-feature import are gone. Build + typecheck pass. All 6 violation points in the phase are now closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-11 visual verification of LightconeCostTable filter row on /costs</name>
  <what-built>LightconeCostTable local CSS module eliminates the last cross-feature import (commit 6 of 7 per D-10).</what-built>
  <how-to-verify>
    1. `git status` — 2 files touched: `LightconeCostTable.module.css` (new), `LightconeCostTable.tsx` (import + 4 rename). `CharacterPool.module.css` must NOT appear in the diff (D-09).
    2. `npm run dev`.
    3. Navigate to `http://localhost:3000/costs`.
    4. Switch to the Lightcone tab (or whichever sub-route surfaces `<LightconeCostTable>`).
    5. The filter row at the top should render:
       - Outer flex container with wrap + 0.5rem gaps (`.filters`).
       - Path filter buttons inside a bordered, teal-bordered scrollable strip (`.paths`). Buttons inside are rendered by `<FilterButtonGroup>` (its own styling — should look identical to before).
       - Search input box (`.searchBar`) with teal border on focus and hover.
       - Red "Clear filters" button (`.clearButton`) with dark red background, pink text, and subtle shadow. On hover (when enabled), background lightens slightly. When no filters are active, the button is disabled (opacity 0.33, no shadow, default cursor).
    6. Type something in the search box — the border should shift to teal on focus.
    7. Click a path filter — some rows should filter. Click Clear — all filters reset; the Clear button transitions to disabled state.
    8. Switch to the Character tab (if CharacterCostTable also renders on this page) — confirm no styling regression there (`styles.rarityBadge` + `styles.tableWrapper` still work because CharacterCostTable.module.css is unchanged).
    9. Navigate to `/draft` or wherever `<CharacterPool>` renders — its filter row must still look identical (D-09 confirms we didn't touch CharacterPool.module.css).
    10. DevTools console: no new error.
  </how-to-verify>
  <resume-signal>Type "approved" if filter row renders identically on /costs + drafting pool unaffected + no errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Pure CSS-content duplication + import-path swap + 4 call-site identifier renames.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-06-01 | N/A | N/A | accept | No new attack surface — CSS-only duplication, TSX edits limited to import strings + className identifier renames. No new data flow, no new dependency. |
</threat_model>

<verification>
- `grep -rn "from ['\"]@/app/.*\\.module\\.css['\"]" components/` returns 0 matches.
- `grep -rn "CharacterPool\\.module\\.css" components/features/costs/` returns 0 matches.
- `grep -c "poolStyles" components/features/costs/components/LightconeCostTable.tsx` returns 0.
- `git diff components/features/drafting/components/CharacterPool.module.css` empty (D-09 compliance).
- `npm run test:typecheck` exits 0.
- `npm run build` exits 0.
- D-11 visual check: /costs filter row unchanged; /draft CharacterPool unaffected.
</verification>

<success_criteria>
1. LightconeCostTable.module.css has 4 filter classes + their pseudo-classes (no child-button selectors per Pitfall 6).
2. LightconeCostTable.tsx uses dual same-feature imports (`filterStyles` + `styles`); 4 call sites renamed atomically.
3. CharacterPool.module.css is untouched (D-09).
4. Both ROADMAP success criteria are now met: no `@/app/*.module.css` imports from `components/`, no cross-feature CSS imports anywhere.
5. Visual D-11 passes on /costs filter row + /draft CharacterPool unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-css-module-hygiene/16.1-06-SUMMARY.md`
</output>
