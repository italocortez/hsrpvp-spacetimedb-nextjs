---
phase: 16.1-css-module-hygiene
plan: 05
type: execute
wave: 5
depends_on: [16.1-04]
files_modified:
  - components/features/team-builder/LoadoutControls.module.css
  - components/features/team-builder/LoadoutControls.tsx
  - app/(public)/teambuilder/page.module.css
autonomous: false
requirements: [R8-Rule-6, D-01, D-05]
must_haves:
  truths:
    - "LoadoutControls.tsx imports styles from './LoadoutControls.module.css'"
    - "Controls panel renders with loadout name header, action buttons, and clear/menu button variants"
    - "Name-edit inline textfield (nameEditor) appears when user clicks loadoutName"
  artifacts:
    - path: components/features/team-builder/LoadoutControls.module.css
      provides: "Sibling CSS module for LoadoutControls"
      contains: ".controls, .controlsHeader, .loadoutName, .helperText, .nameEditor, .controlsContent, .button, .clearBtn, .menuBtn"
    - path: components/features/team-builder/LoadoutControls.tsx
      provides: "LoadoutControls with co-located import"
      contains: "import styles from \"./LoadoutControls.module.css\";"
  key_links:
    - from: components/features/team-builder/LoadoutControls.tsx
      to: components/features/team-builder/LoadoutControls.module.css
      via: relative sibling import
      pattern: "from [\"']\\./LoadoutControls\\.module\\.css[\"']"
---

<objective>
Co-locate LoadoutControls styles (9 classes + nested hover variants) into a new sibling module. All selectors self-contained per D-07. Commit 5 of 7 per D-10. Last team-builder extraction before the costs cross-feature fix (Plan 06) and the sweep (Plan 07).

Output: New `LoadoutControls.module.css` + updated `LoadoutControls.tsx` import + thinned `page.module.css` (after this plan, page.module.css should contain only `.teamBuilder`, `.main`, `.confirmation*`, `.dangerous`, `@keyframes grow-fade-in`, `@keyframes spin` — all dead — ready for sweep in Plan 07).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md
@.planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md
@.planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md
@.planning/phases/16.1-css-module-hygiene/16.1-04-SUMMARY.md

<interfaces>
<!-- LoadoutControls.tsx current state: -->
L4: import styles from "@/app/(public)/teambuilder/page.module.css";
<!-- Uses: styles.controls, .controlsHeader, .loadoutName, .helperText, .nameEditor, .controlsContent, .button, .clearBtn, .menuBtn -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LoadoutControls.module.css with controls, header, name-edit, and button variant classes</name>
  <files>components/features/team-builder/LoadoutControls.module.css</files>
  <read_first>
    - app/(public)/teambuilder/page.module.css (lines 211-298 — controls section)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-01, D-05, D-07)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (LoadoutControls class list lines 144-158)
    - components/features/team-builder/LoadoutControls.tsx (verify every `styles.X` call site)
  </read_first>
  <action>
  Create `components/features/team-builder/LoadoutControls.module.css`:

  File header:
  ```css
  /* ═══════════════════════════════════════════════════════════════════
     LoadoutControls — CSS Module
     ═══════════════════════════════════════════════════════════════════ */
  ```

  Copy EXACTLY these blocks from `app/(public)/teambuilder/page.module.css` (lines 211-298), preserving order and section comments:

  1. `.controls` (lines 211-221)
  2. Section comment `/* ── Header (name + edit) ─── */` (line 223)
  3. `.controlsHeader` (lines 224-229)
  4. `.loadoutName` (lines 231-242)
  5. `.helperText` (lines 243-247)
  6. `.nameEditor` (lines 249-258)
  7. Section comment `/* ── Controls content (action buttons) ─── */` (line 260)
  8. `.controlsContent` (lines 261-266) + `.controlsContent > *` (lines 267-269)
  9. `.button` (lines 271-286)
  10. `.clearBtn` (lines 288-292) + `.clearBtn:hover` (line 293)
  11. `.menuBtn` (lines 295-297) + `.menuBtn:hover` (line 298)

  DO NOT use `:global()`. DO NOT copy any other class.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/features/team-builder/LoadoutControls.module.css','utf8');const need=['.controls','.controlsHeader','.loadoutName','.helperText','.nameEditor','.controlsContent','.button','.clearBtn','.menuBtn'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1);}const banned=['.slot ','.roster ','.charactersContainer','.synergies ','.rosters','.selectButton','.rostersList','.dropdownHeader','.dropdownTitle','.teamOption','.mini','.confirmation',':global','@keyframes'];const bad=banned.filter(b=>s.includes(b));if(bad.length){console.error('BANNED PRESENT:',bad);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/features/team-builder/LoadoutControls.module.css` exists.
    - All 9 classes present:
      - `grep -c '^\.controls ' components/features/team-builder/LoadoutControls.module.css` returns 1
      - `grep -c '^\.controlsHeader' components/features/team-builder/LoadoutControls.module.css` returns 1
      - `grep -c '^\.loadoutName' components/features/team-builder/LoadoutControls.module.css` returns 1
      - `grep -c '^\.helperText' components/features/team-builder/LoadoutControls.module.css` returns 1
      - `grep -c '^\.nameEditor' components/features/team-builder/LoadoutControls.module.css` returns 1
      - `grep -c '^\.controlsContent' components/features/team-builder/LoadoutControls.module.css` returns at least 1
      - `grep -c '^\.button' components/features/team-builder/LoadoutControls.module.css` returns 1
      - `grep -c '^\.clearBtn' components/features/team-builder/LoadoutControls.module.css` returns at least 1
      - `grep -c '^\.menuBtn' components/features/team-builder/LoadoutControls.module.css` returns at least 1
    - No banned classes: `grep -cE "\\.(slot |roster |charactersContainer|synergies |rostersList|teamOption|mini|confirmation)" components/features/team-builder/LoadoutControls.module.css` returns 0.
    - No `:global()`, no `@keyframes`: `grep -cE ":global|@keyframes" components/features/team-builder/LoadoutControls.module.css` returns 0.
  </acceptance_criteria>
  <done>LoadoutControls.module.css exists with exactly the 9 controls classes + hover variants. No stray rules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch LoadoutControls.tsx import to sibling module; remove migrated rules from page.module.css</name>
  <files>components/features/team-builder/LoadoutControls.tsx, app/(public)/teambuilder/page.module.css</files>
  <read_first>
    - components/features/team-builder/LoadoutControls.tsx (L4 import line)
    - components/features/team-builder/LoadoutControls.module.css (just created)
    - app/(public)/teambuilder/page.module.css (rules being removed at lines 207-298)
  </read_first>
  <action>
  **Sub-step 2a — `LoadoutControls.tsx`:**

  Replace import at L4:
  - FROM: `import styles from "@/app/(public)/teambuilder/page.module.css";`
  - TO:   `import styles from "./LoadoutControls.module.css";`

  No other edits.

  **Sub-step 2b — `app/(public)/teambuilder/page.module.css`:**

  Delete:
  - Section header `/* ═══ Controls ═══ */` (lines 207-209)
  - `.controls` (lines 211-221)
  - Section comment `/* ── Header (name + edit) ─── */` (line 223)
  - `.controlsHeader` (lines 224-229)
  - `.loadoutName` (lines 231-242)
  - `.helperText` (lines 243-247)
  - `.nameEditor` (lines 249-258)
  - Section comment `/* ── Controls content (action buttons) ─── */` (line 260)
  - `.controlsContent` + `.controlsContent > *` (lines 261-269)
  - `.button` (lines 271-286)
  - `.clearBtn` + `:hover` (lines 288-293)
  - `.menuBtn` + `:hover` (lines 295-298)

  KEEP in place (Plan 07 sweep will clean these):
  - File header (lines 1-7)
  - `.teamBuilder` (lines 9-13)
  - `.main` (lines 14-18) — dead, sweep will delete in Plan 07
  - `/* ═══ Confirmation Modal ═══ */` section + `.confirmationOverlay`, `.confirmationModal`, `.confirmationTitle`, `.confirmationMessage`, `.confirmationActions`, `.confirmationCancel`, `.confirmationConfirm`, `.dangerous` — ALL dead, sweep deletes in Plan 07
  - `/* ═══ Keyframes ═══ */` + `@keyframes grow-fade-in` + `@keyframes spin` — both dead (the only live keyframe `fade-in` moved to Teamslot.module.css in Plan 01). Sweep deletes both in Plan 07.

  After this plan's edits, `app/(public)/teambuilder/page.module.css` should contain ONLY: file header, `.teamBuilder`, `.main`, `.confirmation*` family, `.dangerous`, `@keyframes grow-fade-in`, `@keyframes spin`. Everything except `.teamBuilder` is dead — Plan 07 will delete them.
  </action>
  <verify>
    <automated>npm run test:typecheck 2>&1 | tail -5 && grep -c "from [\"']\\./LoadoutControls\\.module\\.css[\"']" components/features/team-builder/LoadoutControls.tsx && grep -c "@/app/.*page\\.module\\.css" components/features/team-builder/LoadoutControls.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from [\"']\\./LoadoutControls\\.module\\.css[\"']" components/features/team-builder/LoadoutControls.tsx` returns 1.
    - `grep -c "from [\"']@/app/.*page\\.module\\.css[\"']" components/features/team-builder/LoadoutControls.tsx` returns 0.
    - page.module.css no longer contains controls classes:
      - `grep -c "^\.controls " app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.loadoutName" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.button {" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.clearBtn" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.menuBtn" app/\(public\)/teambuilder/page.module.css` returns 0
    - page.module.css still contains `.teamBuilder` and the dead rules that Plan 07 will sweep:
      - `grep -c "^\.teamBuilder" app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.main" app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.confirmationOverlay" app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "@keyframes grow-fade-in" app/\(public\)/teambuilder/page.module.css` returns 1
    - NO component in `components/` imports `@/app/.../page.module.css` anymore (all 5 team-builder migrations complete):
      - `grep -rn "from ['\\\"]@/app/.*page\\.module\\.css['\\\"]" components/features/team-builder/` returns 0 matches
    - `npm run test:typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>LoadoutControls.tsx imports sibling module. page.module.css has no controls rules. All 5 team-builder components are now R8-compliant. Build + typecheck pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-11 visual verification of LoadoutControls + full team-builder integration on /teambuilder</name>
  <what-built>LoadoutControls co-located CSS module (commit 5 of 7 per D-10). All 5 team-builder components are now R8-compliant.</what-built>
  <how-to-verify>
    1. `git status` — 3 files touched: `LoadoutControls.module.css` (new), `LoadoutControls.tsx` (import swap), `page.module.css` (controls section removed).
    2. `npm run dev`.
    3. Navigate to `http://localhost:3000/teambuilder`.
    4. Controls panel (right side of the page) renders:
       - Padded container with vertical layout and ~1rem gaps (`.controls`).
       - Header row at top with loadout name styled in amber/gold bold (`.loadoutName`) + small helper text underneath (`.helperText`).
       - Action buttons at the bottom with flex-wrap layout (`.controlsContent`).
    5. Click on the loadout name — the inline text editor should appear (`.nameEditor` — amber border, transparent background, cursor active). Pressing Escape or clicking away returns to static display.
    6. Hover a "Clear" button — background should shift from darker red to slightly lighter (`.clearBtn:hover`).
    7. Hover a "Menu" button — background shifts from dark gray to medium gray (`.menuBtn:hover`).
    8. FULL REGRESSION SWEEP (all 5 components integrated):
       - Add/remove characters — Teamslot + TeamRoster hover-reveal works (Plans 01+02 intact).
       - Synergies section renders correctly (Plan 03 intact).
       - Click loadouts dropdown — triangle + mini-grid render (Plan 04 intact).
       - This plan's buttons + name-edit work (Plan 05).
    9. DevTools console: no new error. No "preload not used" warning yet expected (that appears as a side-effect of Plan 07 sweep + prod build).
  </how-to-verify>
  <resume-signal>Type "approved" if controls render correctly + name editor works + full team-builder integration regression passes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Pure CSS co-location + one import string change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-05-01 | N/A | N/A | accept | No new attack surface — CSS-only refactor, single TSX import-path change. |
</threat_model>

<verification>
- `grep -rn "from [\"']@/app/.*\\.module\\.css[\"']" components/features/team-builder/` returns 0 matches across all 5 components.
- `npm run test:typecheck` exits 0.
- `npm run build` exits 0.
- D-11 visual check: controls panel + name editor + full team-builder integration regression passes.
</verification>

<success_criteria>
1. LoadoutControls.module.css has the 9 controls classes + hover variants.
2. LoadoutControls.tsx imports from sibling module.
3. page.module.css contains ONLY `.teamBuilder`, `.main` (dead), `.confirmation*` (dead), `.dangerous` (dead), `@keyframes grow-fade-in` (dead), `@keyframes spin` (dead). All 5 team-builder components are now R8-compliant.
4. Visual D-11 passes across the full team-builder route.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-css-module-hygiene/16.1-05-SUMMARY.md`
</output>
