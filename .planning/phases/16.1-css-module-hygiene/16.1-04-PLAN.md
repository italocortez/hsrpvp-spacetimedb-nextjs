---
phase: 16.1-css-module-hygiene
plan: 04
type: execute
wave: 4
depends_on: [16.1-03]
files_modified:
  - components/features/team-builder/LoadoutDropdown.module.css
  - components/features/team-builder/LoadoutDropdown.tsx
  - app/(public)/teambuilder/page.module.css
autonomous: false
requirements: [R8-Rule-6, D-01, D-05]
must_haves:
  truths:
    - "LoadoutDropdown.tsx imports styles from './LoadoutDropdown.module.css'"
    - "Dropdown opens with correct layout, header dividers, and mini-grid previews"
    - ".rostersList::before pseudo triangle renders above the dropdown"
    - "Mini-slot portraits render with the correct aspect-ratio and cropping"
  artifacts:
    - path: components/features/team-builder/LoadoutDropdown.module.css
      provides: "Sibling CSS module for LoadoutDropdown"
      contains: ".rosters, .selectButton, .rostersList, .dropdownHeader, .dropdownTitle, .teamOption, .teamOptionTitle, .miniGrid, .miniSlot, .miniEmpty, .miniPortrait, .miniElement"
    - path: components/features/team-builder/LoadoutDropdown.tsx
      provides: "LoadoutDropdown with co-located import"
      contains: "import styles from \"./LoadoutDropdown.module.css\";"
  key_links:
    - from: components/features/team-builder/LoadoutDropdown.tsx
      to: components/features/team-builder/LoadoutDropdown.module.css
      via: relative sibling import
      pattern: "from [\"']\\./LoadoutDropdown\\.module\\.css[\"']"
---

<objective>
Co-locate LoadoutDropdown styles (13 classes + pseudo-elements) into a new sibling module. All selectors self-contained per D-07. Commit 4 of 7 per D-10.

Output: New `LoadoutDropdown.module.css` + updated `LoadoutDropdown.tsx` import + thinned `page.module.css`.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md
@.planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md
@.planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md
@.planning/phases/16.1-css-module-hygiene/16.1-03-SUMMARY.md

<interfaces>
<!-- LoadoutDropdown.tsx current state: -->
L4: import styles from "@/app/(public)/teambuilder/page.module.css";
<!-- Uses: styles.rosters, .selectButton, .rostersList, .dropdownHeader, .dropdownTitle, .teamOption, .teamOptionTitle, .miniGrid, .miniSlot, .miniEmpty, .miniPortrait, .miniElement -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LoadoutDropdown.module.css with all 13 classes + ::before/::after pseudo-elements</name>
  <files>components/features/team-builder/LoadoutDropdown.module.css</files>
  <read_first>
    - app/(public)/teambuilder/page.module.css (lines 300-448 — the dropdown block)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-01, D-05)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (LoadoutDropdown class list lines 125-140)
    - components/features/team-builder/LoadoutDropdown.tsx (verify every `styles.X` call site)
  </read_first>
  <action>
  Create `components/features/team-builder/LoadoutDropdown.module.css`:

  File header:
  ```css
  /* ═══════════════════════════════════════════════════════════════════
     LoadoutDropdown — CSS Module
     ═══════════════════════════════════════════════════════════════════ */
  ```

  Copy EXACTLY these blocks from `app/(public)/teambuilder/page.module.css` (lines 300-448), preserving order and section comments:

  1. `.rosters` block (lines 304-306 — empty rule with comment; keep it to preserve the `styles.rosters` reference at the call site)
  2. `.selectButton` (lines 308-325) + `.selectButton:hover` (line 326)
  3. `.rostersList` (lines 328-343)
  4. `.rostersList::before` (lines 344-355) — triangle pointer pseudo-element
  5. `.dropdownHeader` (lines 357-361)
  6. `.dropdownHeader::before, .dropdownHeader::after` (lines 362-368) — divider bars
  7. `.dropdownTitle` (lines 369-373)
  8. `.teamOption` (lines 375-387)
  9. `.teamOption:not(:disabled):hover` (lines 388-392)
  10. `.teamOption:disabled` (lines 393-396)
  11. `.teamOptionTitle` (lines 397-405)
  12. Section comment `/* ── Mini-grid inside loadout dropdown ─── */` (line 407)
  13. `.miniGrid` (lines 408-412)
  14. `.miniSlot` (lines 413-418)
  15. `.miniEmpty` (lines 419-428)
  16. `.miniPortrait` (lines 429-439)
  17. `.miniElement` (lines 440-448)

  DO NOT use `:global()`. DO NOT copy any confirmation-modal, controls, Teamslot, TeamRoster, or synergy classes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/features/team-builder/LoadoutDropdown.module.css','utf8');const need=['.rosters','.selectButton','.rostersList','.rostersList::before','.dropdownHeader','.dropdownTitle','.teamOption','.teamOptionTitle','.miniGrid','.miniSlot','.miniEmpty','.miniPortrait','.miniElement'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1);}const banned=['.slot ','.roster ','.charactersContainer','.synergies ','.controls ','.confirmation','@keyframes',':global'];const bad=banned.filter(b=>s.includes(b));if(bad.length){console.error('BANNED PRESENT:',bad);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/features/team-builder/LoadoutDropdown.module.css` exists.
    - Every dropdown class present via grep (13 classes + 3 pseudo-element refs):
      - `grep -c '\.rostersList::before' components/features/team-builder/LoadoutDropdown.module.css` returns 1
      - `grep -c '\.dropdownHeader::before' components/features/team-builder/LoadoutDropdown.module.css` returns 1
      - `grep -c '\.miniPortrait' components/features/team-builder/LoadoutDropdown.module.css` returns 1
    - No banned classes: `grep -cE "\\.(charactersContainer|synergies |confirmation)" components/features/team-builder/LoadoutDropdown.module.css` returns 0.
    - No `:global()`, no `@keyframes`: `grep -cE ":global|@keyframes" components/features/team-builder/LoadoutDropdown.module.css` returns 0.
  </acceptance_criteria>
  <done>LoadoutDropdown.module.css exists with all dropdown class rules + pseudo-elements. No stray rules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch LoadoutDropdown.tsx import to sibling module; remove migrated rules from page.module.css</name>
  <files>components/features/team-builder/LoadoutDropdown.tsx, app/(public)/teambuilder/page.module.css</files>
  <read_first>
    - components/features/team-builder/LoadoutDropdown.tsx (L4 import line)
    - components/features/team-builder/LoadoutDropdown.module.css (just created)
    - app/(public)/teambuilder/page.module.css (rules being removed at lines 300-448)
  </read_first>
  <action>
  **Sub-step 2a — `LoadoutDropdown.tsx`:**

  Replace import at L4:
  - FROM: `import styles from "@/app/(public)/teambuilder/page.module.css";`
  - TO:   `import styles from "./LoadoutDropdown.module.css";`

  No other edits.

  **Sub-step 2b — `app/(public)/teambuilder/page.module.css`:**

  Delete:
  - Section header comment `/* ═══ Loadout Dropdown ═══ */` (lines 300-302)
  - `.rosters` block (lines 304-306)
  - `.selectButton` + `:hover` (lines 308-326)
  - `.rostersList` + `::before` (lines 328-355)
  - `.dropdownHeader` + `::before, ::after` (lines 357-368)
  - `.dropdownTitle` (lines 369-373)
  - `.teamOption` + `:not(:disabled):hover` + `:disabled` (lines 375-396)
  - `.teamOptionTitle` (lines 397-405)
  - Section comment `/* ── Mini-grid inside loadout dropdown ─── */` (line 407)
  - `.miniGrid`, `.miniSlot`, `.miniEmpty`, `.miniPortrait`, `.miniElement` (lines 408-448)

  KEEP in place (Plan 05 and 06 own these):
  - File header, `.teamBuilder`, `.main`
  - `/* ═══ Controls ═══ */` section and all `.controls*` / `.button` / `.clearBtn` / `.menuBtn` rules
  - `.loadoutName`, `.helperText`, `.nameEditor`
  - `/* ═══ Confirmation Modal ═══ */` section + `.confirmation*` + `.dangerous`
  - `/* ═══ Keyframes ═══ */` + `@keyframes grow-fade-in` + `@keyframes spin`
  </action>
  <verify>
    <automated>npm run test:typecheck 2>&1 | tail -5 && grep -c "from [\"']\\./LoadoutDropdown\\.module\\.css[\"']" components/features/team-builder/LoadoutDropdown.tsx && grep -c "@/app/.*page\\.module\\.css" components/features/team-builder/LoadoutDropdown.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from [\"']\\./LoadoutDropdown\\.module\\.css[\"']" components/features/team-builder/LoadoutDropdown.tsx` returns 1.
    - `grep -c "from [\"']@/app/.*page\\.module\\.css[\"']" components/features/team-builder/LoadoutDropdown.tsx` returns 0.
    - page.module.css no longer contains dropdown classes:
      - `grep -c "^\.rosters" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.selectButton" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.rostersList" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.dropdownHeader" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.teamOption" app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.mini" app/\(public\)/teambuilder/page.module.css` returns 0
    - page.module.css still contains Plan 05/06 classes:
      - `grep -c "^\.controls " app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.loadoutName" app/\(public\)/teambuilder/page.module.css` returns 1
    - `npm run test:typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>LoadoutDropdown.tsx imports sibling module. page.module.css has no dropdown-related rules. Build + typecheck pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-11 visual verification of LoadoutDropdown migration on /teambuilder</name>
  <what-built>LoadoutDropdown co-located CSS module (commit 4 of 7 per D-10).</what-built>
  <how-to-verify>
    1. `git status` — 3 files touched: `LoadoutDropdown.module.css` (new), `LoadoutDropdown.tsx` (import swap), `page.module.css` (~17 rules removed).
    2. `npm run dev`.
    3. Navigate to `http://localhost:3000/teambuilder`.
    4. Click the loadouts dropdown button (the `.selectButton` — should trigger the dropdown to open).
    5. The dropdown panel should render:
       - Translucent dark background with blur (`.rostersList` backdrop-filter).
       - A small triangle ABOVE the panel pointing up at the selectButton (`.rostersList::before`) — rendered via ::before pseudo-element.
       - Header row with "Saved Teams" (or equivalent) centered between two divider bars (`.dropdownHeader::before/::after`).
       - Each saved team option renders as a bordered card (`.teamOption`) with a title row and a 4-column mini-grid of character portraits (`.miniGrid` + `.miniSlot` + `.miniPortrait`).
       - Empty slots render as dashed-border placeholders (`.miniEmpty`).
       - Element icons overlay the top-left of each mini-portrait (`.miniElement`).
    6. Hover a teamOption — border should brighten and subtle glow appears (`.teamOption:not(:disabled):hover`).
    7. A disabled teamOption (if any) renders dimmed with default cursor (`.teamOption:disabled`).
    8. Close and reopen the dropdown — behavior remains consistent.
    9. DevTools console: no new error.
  </how-to-verify>
  <resume-signal>Type "approved" if dropdown opens with triangle pointer + mini-grid previews + hover glow + no errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Pure CSS co-location + one import string change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-04-01 | N/A | N/A | accept | No new attack surface — CSS-only refactor, single TSX import-path change. |
</threat_model>

<verification>
- `grep -rn "from [\"']@/app/.*\\.module\\.css[\"']" components/features/team-builder/LoadoutDropdown.tsx` returns 0.
- `npm run test:typecheck` exits 0.
- `npm run build` exits 0.
- D-11 visual check confirms dropdown renders with triangle + mini-grid.
</verification>

<success_criteria>
1. LoadoutDropdown.module.css has all 13 classes + 3 pseudo-element rules from the original dropdown block.
2. LoadoutDropdown.tsx imports from sibling module.
3. page.module.css no longer contains those rules; retains Plans 05 + 06 classes.
4. Visual D-11 passes (dropdown renders correctly, triangle pointer visible, mini-grid + hover state intact).
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-css-module-hygiene/16.1-04-SUMMARY.md`
</output>
