---
phase: 16.1-css-module-hygiene
plan: 02
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - components/features/team-builder/TeamRoster.module.css
  - components/features/team-builder/TeamRoster.tsx
  - app/(public)/teambuilder/page.module.css
autonomous: false
requirements: [R8-Rule-6, D-01, D-04, D-05, D-06]
must_haves:
  truths:
    - "TeamRoster.tsx imports styles from './TeamRoster.module.css' (sibling, relative)"
    - "TeamRoster tracks roster-level hover state via useState + onMouseEnter/onMouseLeave"
    - "TeamRoster passes isRosterHovered={isRosterHovered} to every <TeamSlot> child"
    - "Hovering the roster reveals every slot's clearButton (opacity 1)"
    - "Mouse-leaving the roster hides clearButtons (opacity 0) unless viewport <=800px or <=1070px"
  artifacts:
    - path: components/features/team-builder/TeamRoster.module.css
      provides: "Sibling CSS module for TeamRoster (.roster, .charactersContainer + @media override)"
      contains: ".roster, .charactersContainer, @media (max-width: 800px) .charactersContainer"
    - path: components/features/team-builder/TeamRoster.tsx
      provides: "TeamRoster with co-located import + hover state wiring"
      contains: "import styles from \"./TeamRoster.module.css\"; const [isRosterHovered, setIsRosterHovered]"
  key_links:
    - from: components/features/team-builder/TeamRoster.tsx
      to: components/features/team-builder/Teamslot.tsx
      via: "isRosterHovered boolean prop propagation"
      pattern: "isRosterHovered=\\{isRosterHovered\\}"
    - from: components/features/team-builder/TeamRoster.tsx
      to: onMouseEnter/onMouseLeave on root div
      via: "useState hook sets/clears isRosterHovered"
      pattern: "setIsRosterHovered"
---

<objective>
Co-locate TeamRoster styles (`.roster`, `.charactersContainer`, responsive override) into a sibling module. Complete the D-06 JS-driven hover replacement started in Plan 01: wire `useState` + `onMouseEnter` / `onMouseLeave` on the roster root, propagate `isRosterHovered` to every `<TeamSlot>` child. Remove the migrated rules from `page.module.css`.

Purpose: Restore the "hover the roster → all clearButtons reveal" UX behavior that the Plan 01 cross-file cascade removal left dormant. Finish the hover replacement as the atomic second commit per D-10.

Output: New `TeamRoster.module.css` + wired TeamRoster.tsx + thinned page.module.css.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md
@.planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md
@.planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md
@.planning/phases/16.1-css-module-hygiene/16.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Teamslot.tsx now accepts -->
interface TeamSlotProps {
  /* ... existing props ... */
  isRosterHovered: boolean;  // not optional — must be passed
}

<!-- TeamRoster.tsx current state (from source read): -->
L2: import { useState, useCallback } from "react";
L4: import styles from "@/app/(public)/teambuilder/page.module.css";
L7: import { TeamSlot } from "./Teamslot";
L80: <TeamSlot ... isRosterHovered={false} ... />   // stub from Plan 01 Task 2 sub-step 4 — REPLACE with real value

<!-- RESEARCH.md Pattern 2 (lines 227-275) is the canonical hover-state wiring pattern. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create TeamRoster.module.css with .roster, .charactersContainer, and the <=800px .charactersContainer media override</name>
  <files>components/features/team-builder/TeamRoster.module.css</files>
  <read_first>
    - app/(public)/teambuilder/page.module.css (lines 21-36 for .roster + .charactersContainer; line 545-548 for the remaining @media block — note the Teamslot-owned overrides inside this block were already migrated by Plan 01)
    - .planning/phases/16.1-css-module-hygiene/16.1-CONTEXT.md (D-01 partition rule, D-04 @media split rule, D-05 PascalCase filename, D-06 JS hover replacement)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (class-ownership map lines 487-491)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (TeamRoster analog + classes assigned lines 89-103)
    - components/features/team-builder/TeamRoster.tsx (confirm every `styles.X` call site — only `styles.roster` and `styles.charactersContainer` are referenced)
  </read_first>
  <action>
  Create `components/features/team-builder/TeamRoster.module.css` as a new file.

  File header (PATTERNS.md triple-bar convention):
  ```css
  /* ═══════════════════════════════════════════════════════════════════
     TeamRoster — CSS Module

     The `.roster:hover .clearButton { opacity: 1 }` cross-file cascade
     was replaced by JS hover state (Phase 16.1 D-06). Hover tracking
     lives in TeamRoster.tsx; the visual reveal lives in Teamslot.module.css
     via the .clearButtonVisible class.
     ═══════════════════════════════════════════════════════════════════ */
  ```

  Copy EXACTLY these rules from `app/(public)/teambuilder/page.module.css`:

  1. Section comment `/* ── Roster container ──── */` and `.roster` block (page.module.css lines 20-28). Keep the class name as `.roster` (Claude's Discretion allows rename, but no rename is needed here; `TeamRoster.tsx` currently uses `styles.roster` — keeping the name avoids a rename at the TSX call site).

  2. Section comment `/* ── Characters grid ──── */` and `.charactersContainer` block (page.module.css lines 30-36).

  3. Responsive override block — ONLY the `.charactersContainer` rule from the `@media (max-width: 800px)` block (page.module.css lines 546-548):
     ```css
     @media screen and (max-width: 800px) {
       .charactersContainer {
         grid-template-columns: repeat(2, minmax(0, 1fr));
       }
     }
     ```
     Do NOT copy the `.path`, `.verticals`, `.clearButton` overrides inside the original @media block — Plan 01 already migrated those to Teamslot.module.css per D-04.

  DO NOT use `:global()`. DO NOT copy any other class.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/features/team-builder/TeamRoster.module.css','utf8');const need=['.roster','.charactersContainer','max-width: 800px','grid-template-columns: repeat(2'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1);}const banned=['.slot','.empty','.clearButton','.synergies','.controls','.path ','.portrait','.verticals',':global','1070px'];const bad=banned.filter(b=>s.includes(b));if(bad.length){console.error('BANNED PRESENT:',bad);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/features/team-builder/TeamRoster.module.css` exists.
    - `grep -c '^\.roster {' components/features/team-builder/TeamRoster.module.css` returns 1.
    - `grep -c '^\.charactersContainer {' components/features/team-builder/TeamRoster.module.css` returns 1.
    - `grep -c "max-width: 800px" components/features/team-builder/TeamRoster.module.css` returns 1.
    - `grep -c "grid-template-columns: repeat(2" components/features/team-builder/TeamRoster.module.css` returns 1.
    - None of the Teamslot-owned overrides are present: `grep -cE "\\.(path|verticals|clearButton|slot|portrait) " components/features/team-builder/TeamRoster.module.css` returns 0.
    - No `:global()`: `grep -c ":global" components/features/team-builder/TeamRoster.module.css` returns 0.
    - No `@media` for 1070px: `grep -c "1070px" components/features/team-builder/TeamRoster.module.css` returns 0.
  </acceptance_criteria>
  <done>TeamRoster.module.css exists with only `.roster`, `.charactersContainer`, and the 800px `.charactersContainer` media override. No stray Teamslot-owned classes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire TeamRoster.tsx — swap import, add useState hover tracker, onMouseEnter/onMouseLeave, pass isRosterHovered to every TeamSlot; remove migrated rules from page.module.css</name>
  <files>components/features/team-builder/TeamRoster.tsx, app/(public)/teambuilder/page.module.css</files>
  <read_first>
    - components/features/team-builder/TeamRoster.tsx (full file — identify the root `.roster` div, the existing `<TeamSlot>` call site at L80 with the `isRosterHovered={false}` stub from Plan 01, and confirm `useState` is already imported at L2)
    - components/features/team-builder/Teamslot.tsx (confirm the `isRosterHovered: boolean` prop from Plan 01 is in place)
    - components/features/team-builder/TeamRoster.module.css (just created — confirms class names available)
    - app/(public)/teambuilder/page.module.css (source of rules being REMOVED)
    - .planning/phases/16.1-css-module-hygiene/16.1-RESEARCH.md (Pattern 2 lines 227-275 — canonical hover-state wiring)
    - .planning/phases/16.1-css-module-hygiene/16.1-PATTERNS.md (TeamRoster wiring lines 257-289)
  </read_first>
  <action>
  **Sub-step 2a — `TeamRoster.tsx` edits:**

  1. Replace import at L4 EXACTLY:
     - FROM: `import styles from "@/app/(public)/teambuilder/page.module.css";`
     - TO:   `import styles from "./TeamRoster.module.css";`
     (Double quotes.)

  2. Add roster-level hover state inside the `TeamRoster` function component, co-located with existing `useState` calls:
     ```typescript
     const [isRosterHovered, setIsRosterHovered] = useState(false);
     ```
     `useState` is already imported from "react" at L2 — no new import needed.

  3. Attach `onMouseEnter` / `onMouseLeave` to the root `.roster` div. Find it with `grep -n "styles.roster\|className={.*roster" components/features/team-builder/TeamRoster.tsx` — it should be the outermost returned element wrapping the characters container + synergies. Add these handlers ONLY to that root div:
     ```tsx
     <div
       className={/* existing className value unchanged */}
       onMouseEnter={() => setIsRosterHovered(true)}
       onMouseLeave={() => setIsRosterHovered(false)}
     >
     ```
     Preserve every existing attribute (className expression, ref, data-* attrs if present). Do NOT wrap in another div.

  4. Find the `<TeamSlot>` call site (L80-ish) and replace the `isRosterHovered={false}` stub from Plan 01 with `isRosterHovered={isRosterHovered}`. Do NOT touch any other prop on that call site.

  **Sub-step 2b — `app/(public)/teambuilder/page.module.css` edits (REMOVE migrated rules):**

  Delete from page.module.css:
  - Section comment `/* ── Roster container ──── */` (line 20) + `.roster` block (lines 21-28)
  - Section comment `/* ── Characters grid ──── */` (line 30) + `.charactersContainer` block (lines 31-36)
  - Inside the remaining `@media screen and (max-width: 800px)` block (currently reduced by Plan 01 to only `.charactersContainer`): delete the `.charactersContainer` override — and because the `@media` block is now empty, DELETE the entire empty `@media screen and (max-width: 800px) { }` block too. The responsive section header comment `/* ═══ Responsive ═══ */` (lines 541-543) becomes pointless; delete it as well (no surviving `@media` blocks after Plan 01 + 02 since Plan 01 took the 1070px block and all Teamslot overrides).

  KEEP in place (Plans 03-05 and 06 own these):
  - File header (lines 1-7)
  - `.teamBuilder` block
  - `.main`, `.synergies`, `.synergies > h2`, `.pair`, `.emptyMessage`, all `.controls*`, `.loadoutName`, `.helperText`, `.nameEditor`, `.button`, `.clearBtn`, `.menuBtn`, `.rosters*`, `.selectButton`, `.dropdownHeader`, `.dropdownTitle`, `.teamOption*`, `.mini*`, `.confirmation*`, `.dangerous`, `@keyframes grow-fade-in`, `@keyframes spin`.
  </action>
  <verify>
    <automated>npm run test:typecheck 2>&1 | tail -10 && echo "---" && grep -c "from [\"']\\./TeamRoster\\.module\\.css[\"']" components/features/team-builder/TeamRoster.tsx && grep -c "isRosterHovered" components/features/team-builder/TeamRoster.tsx && grep -c "onMouseEnter.*setIsRosterHovered\\|setIsRosterHovered(true)" components/features/team-builder/TeamRoster.tsx && grep -c "@/app/.*page\\.module\\.css" components/features/team-builder/TeamRoster.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from [\"']\\./TeamRoster\\.module\\.css[\"']" components/features/team-builder/TeamRoster.tsx` returns 1.
    - `grep -c "from [\"']@/app/.*page\\.module\\.css[\"']" components/features/team-builder/TeamRoster.tsx` returns 0.
    - `grep -c "useState.*isRosterHovered\\|isRosterHovered.*useState" components/features/team-builder/TeamRoster.tsx` returns at least 1 (the useState declaration).
    - `grep -c "setIsRosterHovered(true)" components/features/team-builder/TeamRoster.tsx` returns at least 1.
    - `grep -c "setIsRosterHovered(false)" components/features/team-builder/TeamRoster.tsx` returns at least 1.
    - `grep -c "isRosterHovered={isRosterHovered}" components/features/team-builder/TeamRoster.tsx` returns at least 1 (the <TeamSlot> prop pass-through).
    - `grep -c "isRosterHovered={false}" components/features/team-builder/TeamRoster.tsx` returns 0 (Plan 01's stub replaced).
    - page.module.css no longer contains `.roster` / `.charactersContainer`:
      - `grep -c "^\.roster " app/\(public\)/teambuilder/page.module.css` returns 0
      - `grep -c "^\.charactersContainer " app/\(public\)/teambuilder/page.module.css` returns 0
    - page.module.css still contains Plan 03/04/05 classes:
      - `grep -c "^\.synergies " app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.controls " app/\(public\)/teambuilder/page.module.css` returns 1
      - `grep -c "^\.rostersList " app/\(public\)/teambuilder/page.module.css` returns 1
    - `npm run test:typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>TeamRoster.tsx imports sibling module, tracks roster hover via useState, propagates to every TeamSlot child. page.module.css is thinned of `.roster` + `.charactersContainer` + their media override. TypeScript + build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-11 visual verification of TeamRoster hover-reveal + import swap on /teambuilder</name>
  <what-built>TeamRoster co-located CSS module + hover-state wiring completes the D-06 JS replacement for `.roster:hover .clearButton` (commit 2 of 7 per D-10).</what-built>
  <how-to-verify>
    1. `git status` — confirm only these files changed: `components/features/team-builder/TeamRoster.module.css` (new), `components/features/team-builder/TeamRoster.tsx` (modified), `app/(public)/teambuilder/page.module.css` (thinned).
    2. `npm run dev`.
    3. Navigate to `http://localhost:3000/teambuilder`.
    4. THE CRITICAL CHECK: Hover the ENTIRE roster area (any character card or the padding around them). Every slot's red clearButton (top-left X icon) should now APPEAR (opacity 1) simultaneously. This is the D-06 behavior restored from Plan 01.
    5. Move the mouse AWAY from the roster (e.g., hover the synergies section or controls panel). All clearButtons should FADE OUT (opacity 0).
    6. Move back into the roster — clearButtons reappear for all slots together (not per-slot).
    7. Resize the browser below 800px width — the characters grid should switch to 2 columns (the `.charactersContainer` @media override is now in TeamRoster.module.css). Verify it worked.
    8. At <=800px, clearButtons should be permanently visible regardless of hover (the Teamslot.module.css @media forces opacity 1).
    9. Click-to-add and drag-and-drop still function — hover state must not interfere with drag.
    10. DevTools console: no new error, no runtime TypeError.
  </how-to-verify>
  <resume-signal>Type "approved" if hover-reveal works on the whole roster + <=800px grid switches to 2 columns + no errors. Otherwise describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. This plan is CSS co-location + a single React hover state with strict parent→child prop flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-02-01 | N/A | N/A | accept | No new attack surface — hover boolean is client-only ephemeral state, never persisted, never sent to server, never derived from user input beyond mouse position (which is trusted by the browser). |
</threat_model>

<verification>
- `grep -rn "from [\"']@/app/.*\\.module\\.css[\"']" components/features/team-builder/TeamRoster.tsx` returns 0.
- `grep -c "setIsRosterHovered" components/features/team-builder/TeamRoster.tsx` returns at least 3 (one declaration, onMouseEnter, onMouseLeave).
- `grep -c "isRosterHovered={isRosterHovered}" components/features/team-builder/TeamRoster.tsx` returns at least 1.
- `npm run test:typecheck` exits 0.
- `npm run build` exits 0.
- D-11 visual check confirms full-roster hover reveals all clearButtons; <=800px switches grid to 2 columns.
</verification>

<success_criteria>
1. TeamRoster.module.css has only `.roster`, `.charactersContainer`, and the 800px `.charactersContainer` override.
2. TeamRoster.tsx imports sibling module, tracks `isRosterHovered` via `useState`, attaches `onMouseEnter` / `onMouseLeave` to the root `.roster` div, passes `isRosterHovered={isRosterHovered}` to every `<TeamSlot>` child.
3. page.module.css no longer contains `.roster` / `.charactersContainer` / their @media override; retains all other non-Teamslot / non-TeamRoster classes.
4. Visual D-11 check: full-roster hover reveals all clearButtons; <=800px switches grid to 2 columns; no regression in drag/drop or click-to-add.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-css-module-hygiene/16.1-02-SUMMARY.md`
</output>
