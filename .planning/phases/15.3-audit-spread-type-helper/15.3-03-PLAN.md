---
phase: 15.3-audit-spread-type-helper
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/helpers/accountRating.ts
  - spacetimedb/src/helpers/achievementChecker.ts
  - spacetimedb/src/helpers/characterStatsIncrement.ts
  - spacetimedb/src/helpers/flagTransferHelpers.ts
  - spacetimedb/src/helpers/globalCharacterStatsIncrement.ts
  - spacetimedb/src/helpers/leaderboardRebuild.ts
  - spacetimedb/src/helpers/rosterHelpers.ts
  - spacetimedb/src/helpers/statsIncrement.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-15]
tags: [refactor, typecheck, audit, backend, helpers, delete-insert-carry]

must_haves:
  truths:
    - "Plain insert/update sites in 8 helper files migrate to new helpers"
    - "Delete+insert-carry sites KEEP the auditUpdate primitive (D-06, P4 pattern)"
    - "characterStatsIncrement (6 carry sites), statsIncrement (6 carry sites), globalCharacterStatsIncrement (2 carry sites) retain auditUpdate imports"
    - "npm run test:typecheck passes"
    - "test/backend/match-results integration tests pass (stats helpers exercised via finalization flow)"
  artifacts:
    - path: "spacetimedb/src/helpers/characterStatsIncrement.ts"
      provides: "Stats increment on two-tier API (new helper for non-carry; primitive for carry)"
      contains: "auditUpdate"
    - path: "spacetimedb/src/helpers/statsIncrement.ts"
      provides: "Stats increment on two-tier API"
      contains: "auditUpdate"
    - path: "spacetimedb/src/helpers/globalCharacterStatsIncrement.ts"
      provides: "Global stats increment on two-tier API"
      contains: "auditUpdate"
  key_links:
    - from: "these 8 helper files"
      to: "spacetimedb/src/helpers/auditHelpers.ts"
      via: "import { insertWithAudit, updateWithAudit }"
      pattern: "from './auditHelpers'"
---

<objective>
Migrate 26 audit call sites across 8 non-audit helper files. Wave 1 Cluster C.

This cluster contains the heaviest concentration of **delete+insert-carry sites** — 3 of the 8 files (characterStatsIncrement, statsIncrement, globalCharacterStatsIncrement) have composite-PK tables where the pattern is `.delete(existing)` then `.insert({...explicit fields, ...auditUpdate(ctx, existing, userId)})`. These sites KEEP the `auditUpdate` primitive per D-06 and P4 — do NOT attempt to force them through the new helpers.

Purpose: Exercise the two-tier API at scale. The stats helpers carry most of the delete+insert-carry canonical references cited in CONTEXT.md §code_context.

Output: 8 files on the two-tier API (new helpers for plain sites + `auditUpdate` primitive for carry sites). Typecheck + match-results integration tests green.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-audit-spread-type-helper/15.3-CONTEXT.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
@spacetimedb/src/helpers/auditHelpers.ts
@spacetimedb/src/helpers/auditColumns.ts
@spacetimedb/src/reducers/admin.ts

<skill-notice>
**MANDATORY:** Load `/spacetimedb` skill before executing.
</skill-notice>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer → stats helper (indirect) | Helpers called from finalization + achievement flows; no new direct client boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.3-audit-trail-preservation | Tampering / Repudiation | **Delete+insert-carry sites in statsIncrement, characterStatsIncrement, globalCharacterStatsIncrement** | mitigate | **KEEP the `auditUpdate` primitive at every `.delete(existing)` + `.insert({...row, ...auditUpdate(...)})` site** per D-06 and Pitfall 1. The primitive carries `createdById/createdDate` from `existing` — migrating to `insertWithAudit` would overwrite with the current actor (data corruption). |
| T-15.3-user-id-drift | Tampering | Non-carry sites on new helpers | mitigate | Copy the userId argument verbatim from original call site. Stats helpers use `actingUserId` (passed in) or `SYSTEM_USER_ID` (background updates). |

Severity MEDIUM — composite-PK audit trail corruption would break compliance invariant.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate stats helpers WITH carry-preservation (3 files, 15 sites)</name>
  <files>
    spacetimedb/src/helpers/characterStatsIncrement.ts,
    spacetimedb/src/helpers/statsIncrement.ts,
    spacetimedb/src/helpers/globalCharacterStatsIncrement.ts
  </files>
  <read_first>
    spacetimedb/src/helpers/characterStatsIncrement.ts,
    spacetimedb/src/helpers/statsIncrement.ts,
    spacetimedb/src/helpers/globalCharacterStatsIncrement.ts,
    spacetimedb/src/helpers/auditHelpers.ts,
    spacetimedb/src/reducers/admin.ts,
    .planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md,
    .planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
  </read_first>
  <action>
These 3 files are delete+insert-carry heavy (per CONTEXT.md §code_context). RESEARCH.md §Domain Clustering reports per-file audit counts: characterStatsIncrement (7), statsIncrement (5), globalCharacterStatsIncrement (3). Total 15.

**Classification first, migration second:**

For each audit call site in each file, determine pattern variant:

- **P4 Delete+insert-carry (KEEP PRIMITIVE):** look for `ctx.db.X.delete(existing)` immediately preceding `ctx.db.X.insert({...explicit fields..., ...auditUpdate(ctx, existing, userId)})`. Site is composite-PK re-insert. **DO NOT TOUCH.** Leave `auditUpdate` primitive in place.
- **P1 Plain insert:** standalone `.insert({..., ...auditInsert(ctx, userId)})` — migrate to `insertWithAudit`.
- **P2 Direct update:** `.update({...row, ...changes, ...auditUpdate(ctx, row, userId)})` with no preceding `.delete` — migrate to `updateWithAudit`.

**Canonical PATTERNS.md reference (Analog admin.ts L514-522):**
```ts
// THIS IS P4 — KEEP AS-IS (do not migrate)
ctx.db.HsrCharacterCost.delete(existing);
ctx.db.HsrCharacterCost.insert({
    characterName: existing.characterName,
    gameMode: existing.gameMode,
    /* ... full explicit row ... */
    ...auditUpdate(ctx, existing, admin.id),  // <-- primitive stays
} as any);
```

**Import management:**
- ADD: `import { insertWithAudit, updateWithAudit } from './auditHelpers';` (only include helpers actually used in the file after migration)
- KEEP: `import { auditUpdate } from './auditColumns';` — required for P4 carry sites (all 3 files have them per CONTEXT.md canonical refs)
- REMOVE from auditColumns import: `auditInsert` if every P1 site migrates (likely in all 3 files — none of these files are listed as conditional-site hosts)

**Warning signs (Pitfall 1):** if a git-diff shows a `...auditUpdate` call being replaced by `insertWithAudit`, that is wrong — revert that change.

After classifying and migrating each file, spot-check: `grep -c "auditUpdate" {file}` should remain >= the number of carry sites pre-migration. `characterStatsIncrement.ts` expected >= 6 after, `statsIncrement.ts` expected >= 6 after, `globalCharacterStatsIncrement.ts` expected >= 2 after (these numbers are from CONTEXT.md canonical refs).
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auditInsert" spacetimedb/src/helpers/characterStatsIncrement.ts` returns 0
    - `grep -c "auditInsert" spacetimedb/src/helpers/statsIncrement.ts` returns 0
    - `grep -c "auditInsert" spacetimedb/src/helpers/globalCharacterStatsIncrement.ts` returns 0
    - `grep -c "auditUpdate" spacetimedb/src/helpers/characterStatsIncrement.ts` returns >= 6 (carry sites retained)
    - `grep -c "auditUpdate" spacetimedb/src/helpers/statsIncrement.ts` returns >= 6 (carry sites retained)
    - `grep -c "auditUpdate" spacetimedb/src/helpers/globalCharacterStatsIncrement.ts` returns >= 2 (carry sites retained)
    - `grep -c "insertWithAudit\|updateWithAudit" spacetimedb/src/helpers/characterStatsIncrement.ts` returns >= 1
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>Stats helpers on two-tier API, carry sites preserved, typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate non-stats helpers (5 files, 10 sites)</name>
  <files>
    spacetimedb/src/helpers/accountRating.ts,
    spacetimedb/src/helpers/achievementChecker.ts,
    spacetimedb/src/helpers/flagTransferHelpers.ts,
    spacetimedb/src/helpers/leaderboardRebuild.ts,
    spacetimedb/src/helpers/rosterHelpers.ts
  </files>
  <read_first>
    spacetimedb/src/helpers/accountRating.ts,
    spacetimedb/src/helpers/achievementChecker.ts,
    spacetimedb/src/helpers/flagTransferHelpers.ts,
    spacetimedb/src/helpers/leaderboardRebuild.ts,
    spacetimedb/src/helpers/rosterHelpers.ts,
    spacetimedb/src/helpers/auditHelpers.ts,
    spacetimedb/src/reducers/admin.ts,
    .planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md
  </read_first>
  <action>
Task 2 migrates 10 call sites across 5 non-stats helper files (grep-verified 2026-04-17, excludes import lines):

| File | Call sites | Lines | Pattern |
|------|-----------|-------|---------|
| accountRating.ts | 1 | L156 (auditUpdate) | P2 direct update |
| achievementChecker.ts | 1 | L149 (auditInsert) | P1 plain insert |
| flagTransferHelpers.ts | 5 | L32, L41, L79, L86, L121 (all auditUpdate) | P2 direct updates |
| leaderboardRebuild.ts | 2 | L74, L119 (both auditInsert) | P1 plain inserts |
| rosterHelpers.ts | 1 | L51 (auditUpdate) | P2 direct update |
| **Total** | **10** | — | — |

All 10 sites are mainstream P1/P2 (no delete+insert-carry appears in these 5 files — grep confirmed no `.delete(` immediately preceding any `.insert({...auditInsert...})` pattern). Apply standard P1/P2 transforms. If file inspection reveals any carry pattern missed by the grep (e.g., leaderboardRebuild MmrRating snapshot), keep `auditUpdate` primitive there.

**Steps per file:**
1. Swap imports — use `auditHelpers` for new helpers; keep `auditColumns` import if carry sites exist.
2. P1 plain-insert → `insertWithAudit`.
3. P2 direct-update → `updateWithAudit(ctx, existing, {changes}, userId)`.
4. P4 carry sites → leave alone.

**First 2 excerpts** (achievementChecker.ts Achievement insert — apply identical pattern to all plain inserts):
```ts
// BEFORE
ctx.db.Achievement.insert({
    userId: target.id,
    achievementKey: key,
    earnedAt: ctx.timestamp,
    ...auditInsert(ctx, target.id),
} as any);

// AFTER
ctx.db.Achievement.insert(insertWithAudit(ctx, {
    userId: target.id,
    achievementKey: key,
    earnedAt: ctx.timestamp,
}, target.id));
```

```ts
// BEFORE (accountRating.ts — HsrAccountRating insert)
ctx.db.HsrAccountRating.insert({
    hsrAccountId: accountId,
    gameMode,
    rating: newRating,
    ...auditInsert(ctx, user.id),
} as any);

// AFTER
ctx.db.HsrAccountRating.insert(insertWithAudit(ctx, {
    hsrAccountId: accountId,
    gameMode,
    rating: newRating,
}, user.id));
```

If any file has zero remaining `auditUpdate` callers after migration, remove the `auditUpdate` import. If any file has zero remaining `auditInsert` callers, remove the `auditInsert` import.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auditInsert" spacetimedb/src/helpers/accountRating.ts spacetimedb/src/helpers/achievementChecker.ts spacetimedb/src/helpers/flagTransferHelpers.ts spacetimedb/src/helpers/leaderboardRebuild.ts spacetimedb/src/helpers/rosterHelpers.ts` returns 0 for each
    - `grep -c "insertWithAudit\|updateWithAudit" spacetimedb/src/helpers/flagTransferHelpers.ts` returns >= 6
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>5 non-stats helpers migrated, typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cluster C verification — typecheck + match-results integration tests</name>
  <files></files>
  <read_first>
    spacetimedb/src/helpers/characterStatsIncrement.ts,
    spacetimedb/src/helpers/statsIncrement.ts,
    spacetimedb/src/helpers/globalCharacterStatsIncrement.ts
  </read_first>
  <action>
Run Cluster C verification per RESEARCH.md §Sampling Rate: Cluster C → `test/backend/match-results` (stats helpers are exercised via finalization flow).

1. `npm run test:typecheck` — must exit 0
2. `npx vitest run --config test/vitest.integration.config.ts --dir test/backend/match-results` — must exit 0 (CRITICAL — stats helper behavior flows through match finalization)
3. Regression grep: total `as any` across the 8 files should have DECREASED by ~11 (plain inserts only — carry sites keep their trailing `} as any)` per Pitfall 3 / P4 excerpt).
4. Carry-preservation sanity check: `grep -c "auditUpdate" spacetimedb/src/helpers/characterStatsIncrement.ts` — count should NOT be zero; expected >= 6 per canonical refs.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:typecheck` exits 0
    - `test/backend/match-results` vitest run exits 0
    - Stats helper carry-site counts preserved (auditUpdate still imported + called in all 3 stats files)
    - `as any` count decreased across the 8 files
  </acceptance_criteria>
  <done>Cluster C green, two-tier API validated at scale.</done>
</task>

</tasks>

<verification>
- `npm run test:typecheck` exits 0
- Stats helper files preserve `auditUpdate` imports (carry sites untouched)
- Non-stats helper files have zero `auditInsert` / `auditUpdate` calls (if no carry sites)
- `test/backend/match-results` suite green
</verification>

<success_criteria>
- 26 audit call sites migrated across 8 helper files
- Delete+insert-carry sites preserved per D-06 and Pitfall 1
- Match-results integration tests confirm finalization flow unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15.3-audit-spread-type-helper/15.3-03-SUMMARY.md`. **Explicitly list the carry sites preserved** (file + line number + table name) so Wave 3 planner can confirm them when deciding Option α vs β for `auditInsert` deletion.
</output>
