---
phase: 15.3-audit-spread-type-helper
plan: 15
type: execute
wave: 5
depends_on: ["15.3-12", "15.3-13", "15.3-14"]
files_modified:
  - spacetimedb/dist/bundle.js
  - .planning/STATE.md
autonomous: false
requirements: [D-01, D-02, D-03, D-07, D-08, D-09]
tags: [publish, integration, phase-close, backend, maincloud]

user_setup:
  - service: spacetimedb-maincloud
    why: "Single big-bang publish of the refactored bundle to maincloud"
    env_vars: []
    dashboard_config:
      - task: "Confirm default server is maincloud (user has existing access)"
        location: "`spacetime server list` — *** marks default"

must_haves:
  truths:
    - "npm run test:typecheck passes clean pre-publish"
    - "npm run test:integration passes clean pre-publish (full 71-file suite)"
    - "Baseline `as any` grep across spacetimedb/src decreased by ~284 vs pre-phase (from 417 to ~133)"
    - "spacetime publish succeeds non-destructively against maincloud (no --clear-database)"
    - "Post-publish integration test re-run passes"
    - "STATE.md updated to reflect phase close + plan counts"
  artifacts:
    - path: "spacetimedb/dist/bundle.js"
      provides: "Rebuilt wasm bundle with migrated audit + typed helpers"
      contains: ""  # binary — commit presence only
    - path: ".planning/STATE.md"
      provides: "Phase 15.3 close-out state"
      contains: "Phase 15.3.*complete"
  key_links:
    - from: "local typecheck + integration green"
      to: "maincloud publish"
      via: "`spacetime publish <db>` (non-destructive)"
      pattern: ""
---

<objective>
Wave 5. Final plan — phase gate + deploy. Three gates in order:

1. **Pre-publish local gate:** typecheck + full integration test suite + regression grep verifying ~284 `as any` eliminated.
2. **Publish gate (cross-boundary, human-verify):** single `spacetime publish` to maincloud. **NO `--clear-database`** — zero schema change, this is a non-destructive bundle replacement.
3. **Post-publish gate:** re-run integration tests to confirm maincloud build matches local behavior.

Purpose: Ship the ~284 `as any` elimination + improved audit-call readability to maincloud in a single atomic deploy. Since the phase introduces zero schema change, this is the lowest-risk publish in v0.9 so far.

**STATE.md update:** after Wave 5 completes, append a Phase 15.3 close entry summarizing: total migration count, `as any` delta, two-tier API validation, Wave 2b type-hygiene outcomes, Wave 3 option chosen.

Output: Maincloud running the refactored bundle; phase marked complete in STATE.md; all regression signals green.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-audit-spread-type-helper/15.3-CONTEXT.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
@.planning/STATE.md

<skill-notice>
**MANDATORY:** Load `/spacetimedb` skill. Wave 5 involves maincloud publish per CLAUDE.md deployment rules.
</skill-notice>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → maincloud | Cross-boundary deploy. Irreversible short-term (bundle replacement), recoverable via `git revert` + re-publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.3-audit-trail-preservation | Tampering / Repudiation | Maincloud deploy of refactored code | mitigate | Pre-publish integration tests exercise every reducer subsystem. Post-publish re-run catches any environment-specific regression. Schema unchanged → zero risk of data migration drift. |
| T-15.3-user-id-drift | Tampering | Refactored code | mitigate | Full test suite green pre-publish = no userId drift escaped review. |
| T-15.3-maincloud-deploy-integrity | Tampering | spacetime publish command | mitigate | Use default server (maincloud per `spacetime server list ***`). No `--clear-database`. Verify `spacetime sql` post-publish returns expected rows on SYSTEM user. |

Severity MEDIUM — deploy is the irreversible step; testing rigor upfront is the mitigation.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-publish local gate — typecheck + full integration + as-any baseline</name>
  <files></files>
  <read_first>
    .planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
  </read_first>
  <action>
Run the pre-publish gate per RESEARCH.md §Wave 5:

1. **Typecheck:**
   ```bash
   npm run test:typecheck
   ```
   Must exit 0.

2. **Full integration test suite:**
   ```bash
   npm run test:integration
   ```
   Must exit 0. All 71 test files across every subsystem. Expected runtime ~5-10 min.

3. **Regression grep — `as any` delta:**
   ```bash
   grep -rn "as any" spacetimedb/src --include="*.ts" | wc -l
   ```
   Expected: ~133 (417 baseline - ~284 eliminated). If count is much higher (>200), the phase under-delivered; investigate which clusters didn't eliminate sites.

4. **Audit-call inventory — zero non-conditional `auditInsert` callers:**
   ```bash
   grep -rn "auditInsert" spacetimedb/src --include="*.ts" | grep -v "auditColumns.ts" | grep -v "auditHelpers.ts"
   ```
   Expected: only conditional-site matches (~7 sites per RESEARCH). No standalone callers.

5. **Audit-call inventory — `insertWithAudit` + `updateWithAudit` usage:**
   ```bash
   grep -rln "insertWithAudit\|updateWithAudit" spacetimedb/src --include="*.ts" | grep -v "auditHelpers.ts" | wc -l
   ```
   Expected: ~54 files (every non-auditColumns file touched).

**If any gate fails, STOP — do not proceed to Task 2.** Report failure and the investigation path.
  </action>
  <verify>
    <automated>npm run test:typecheck && npm run test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:typecheck` exits 0
    - `npm run test:integration` exits 0
    - `as any` count across spacetimedb/src is approximately 133 (≤ 160 acceptable; > 200 indicates under-delivery)
    - Non-conditional `auditInsert` callers: 0
    - 50+ files using new helpers
  </acceptance_criteria>
  <done>Pre-publish gate passed. Ready to publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Publish to maincloud (non-destructive) + post-publish verify</name>
  <files>spacetimedb/dist/bundle.js (rebuilt on publish)</files>
  <read_first>
    .claude/CLAUDE.md,
    .planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
  </read_first>
  <what-built>
    Phase 15.3 refactor complete locally. ~284 `as any` eliminated, 385 audit call sites migrated to new typed helpers, full integration suite green. Ready for single big-bang maincloud publish.
  </what-built>
  <action>
STOP and request human approval before the publish. Present the publish plan + pre-publish grep output. Wait for explicit "approved" reply. Do NOT auto-publish.
  </action>
  <how-to-verify>
**Step 1 — Confirm default server is maincloud:**
```bash
spacetime server list
```
Expected output: one entry marked `***` pointing to maincloud. If the default is NOT maincloud, switch first (per CLAUDE.md deployment rules).

**Step 2 — Publish non-destructively:**
```bash
spacetime publish <db-name>
```
(No `--clear-database` flag. Zero schema change, so no data migration needed.)

Expected: build succeeds, deploy finishes in ~2-3 minutes.

**Step 3 — Post-publish sanity check:**
```bash
spacetime sql <db-name> "SELECT count(*) FROM user"    # should return expected count (non-zero)
spacetime sql <db-name> "SELECT id, username, created_by_id FROM user WHERE username = 'SYSTEM'"    # should return SYSTEM row
```

**Step 4 — Post-publish integration re-run:**
```bash
npm run test:integration
```
Full 71-file suite re-run (against local sandbox OR maincloud if harness permits). Must exit 0.

**Step 5 — Dashboard spot-check:**
Visit `https://spacetimedb.com/@<username>/<db-name>` and confirm the module is healthy (no errored reducer calls in the log).

**If Step 4 (post-publish integration) fails:**
```bash
git revert HEAD    # revert the publish-time commit
spacetime publish <db-name>    # re-publish previous state
```
Report the failure for investigation.

**If all steps green, reply "approved" with the `spacetime sql` output.**
  </how-to-verify>
  <resume-signal>Reply "approved" with SQL output, or report failure + rollback result.</resume-signal>
  <verify>
    <automated>spacetime sql $MAINCLOUD_DB "SELECT count(*) FROM user" returns expected count; npm run test:integration exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `spacetime server list` shows maincloud as default (*** marker)
    - `spacetime publish <db>` succeeds without --clear-database
    - Post-publish `spacetime sql` confirms SYSTEM user row still present with intact audit fields
    - `npm run test:integration` re-run exits 0 against sandbox (or maincloud if harness permits)
    - Dashboard at https://spacetimedb.com/@<user>/<db> shows module healthy
    - Human reviewer types "approved"
  </acceptance_criteria>
  <done>Maincloud deploy complete, post-publish verification human-approved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md with phase-close entry</name>
  <files>.planning/STATE.md</files>
  <read_first>
    .planning/STATE.md
  </read_first>
  <action>
Append a Phase 15.3 close entry to STATE.md `## Decisions` section following the existing "[Phase 15.X]:" pattern. Example shape:

```markdown
- [Phase 15.3]: D-01..D-18 honored: 385 audit call sites migrated across 57 files, ~284 `as any` eliminated (68% of backend baseline, target hit). Two-tier API validated: new helpers + auditUpdate primitive for carry/conditional sites. Wave 2b D-17/D-18 eliminated 17 additional `as any` via mergeForUpdate signature tightening + build-then-insert typing from module_bindings. Wave 3 Option α selected (auditInsert kept exported as restricted-use primitive for ~7 conditional sites — grep-verified). Wave 4 converted 8 placeholder tests to it.todo. Single maincloud publish non-destructive; post-publish integration suite green. Zero behavior change.
```

Also update the top-of-file status if needed (e.g., `current_phase: 16` if 15.3 was the last v0.9 prep phase — it is per ROADMAP).

Additionally check `.planning/ROADMAP.md` §Phase 15.3 Plans list and mark plans `[x]` that are now shipped:
- [x] Wave 0 pilot (already checked)
- [x] Wave 1 — batch-mechanical migration
- [x] Wave 2 — individual-audit migration
- [x] Wave 2b — adjacent type-hygiene
- [x] Wave 3 — Option α (or β) executed
- [x] Wave 4 — placeholder-test cleanup
- [x] Wave 5 — publish + full integration pass
  </action>
  <verify>
    <automated>grep -c "Phase 15.3.*honored" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 15.3.*honored" .planning/STATE.md` returns >= 1 (close entry added)
    - ROADMAP.md §Phase 15.3 Plans list has all waves marked `[x]`
  </acceptance_criteria>
  <done>STATE.md + ROADMAP.md updated. Phase 15.3 is closed in the record.</done>
</task>

</tasks>

<verification>
- Pre-publish: typecheck + integration suite green + as-any count ≈ 133
- Publish succeeded non-destructively
- Post-publish: integration suite re-run green
- STATE.md + ROADMAP.md reflect phase close
</verification>

<success_criteria>
- Maincloud running refactored bundle
- Phase 15.3 marked complete in STATE.md and ROADMAP.md
- Full `/gsd-verify-work` can run next on Phase 15.3 without gaps
- ~284 `as any` eliminated in production (68% of baseline)
</success_criteria>

<output>
After completion, create `.planning/phases/15.3-audit-spread-type-helper/15.3-15-SUMMARY.md` PLUS a phase-close summary at `.planning/phases/15.3-audit-spread-type-helper/PHASE-SUMMARY.md` covering the full 7-wave migration. Include:
- Total call-site count migrated
- `as any` elimination numbers (baseline → final)
- Files touched (57 total)
- Option chosen for Wave 3 (α/β)
- Any anomalies uncovered during execution
- Link back to Wave 0 pilot commit `4f1dad6`
</output>
