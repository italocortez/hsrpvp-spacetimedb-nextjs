---
phase: 15.3-audit-spread-type-helper
plan: 15
subsystem: backend
tags: [publish, maincloud, integration, phase-close, wave-5, backend, deploy]

# Dependency graph
requires:
  - phase: 15.3-audit-spread-type-helper (Plans 01-14 + 16 — all refactor waves)
    provides: refactored local source tree with ~228 `as any` eliminated, typecheck 0 errors in spacetimedb/src, Plan 13 Option α canon (9 auditInsert callers), full integration suite 59 passed + 1 skipped + 7 todo = 60 files, 616 tests, exit 0
  - phase: 15.3-audit-spread-type-helper (Plan 16 — Wave 4b real-miss cleanup)
    provides: 210→189 `as any` residual + 1 Rule 1 seriesManagement TimerState schema-drift fix + LobbyPassword P5 split. Pre-publish gate satisfied under the Plan 16 integration run (same DB + same bundle + same vectors — no duplicate re-run needed).

provides:
  - Maincloud database `hsrpvp-spacetimedb-nextjs-test1` running the refactored Phase 15.3 bundle (identity `c2005439f74300bfd5f275871c810689906f7724e12896cd03fda072a4d115d6`).
  - Non-destructive publish — empty Database Migration Plan confirms zero schema change.
  - Post-publish SQL sanity baseline: SYSTEM row id=1 / created_by_id=0 / last_modified_by_id=0 per feedback_system_user_id_sentinel.md; User table 359 rows; UserIdentity table 358 rows.
  - Bundle commit `fe29995` (spacetimedb/dist/bundle.js only; Plan 15 owns the bundle commit per Plans 03/04/05/06/13/14/16 precedent of reverting intermediate harness rebuilds).
  - `.env.local` unmodified (non-destructive publish does NOT rotate session tokens per feedback_post_publish_only.md — that only happens after `--clear-database`).
  - PHASE 15.3 CLOSED. STATE.md + ROADMAP.md + PROJECT.md updated. 15.3-15-SUMMARY.md + PHASE-SUMMARY.md authored.

affects:
  - Dashboard URL: https://spacetimedb.com/hsrpvp-spacetimedb-nextjs-test1 (accessible via browser, informational-only — CLI cannot inspect dashboard health metrics).
  - Phase 16 (Route + global foundation) unblocked.

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Non-destructive maincloud publish: `npm run spacetime:publish` (= `spacetime publish --module-path spacetimedb --server maincloud`) prompts `Are you sure you want to proceed? [y/N]` for non-local servers. Pipe `echo y |` to auto-confirm. Empty `Database Migration Plan` section in the output confirms zero schema change before the `Uploading...` step. Do NOT pass `--clear-database` for pure-refactor publishes — that flag wipes all tables and triggers a post-publish.ts re-seed which rotates `.env.local`."
    - "Post-publish SQL sanity pattern: query SYSTEM row + User count + UserIdentity count to verify audit-column invariants and row-count preservation. SpacetimeDB SQL dialect rejects unaliased aggregates — use `SELECT COUNT(*) AS total FROM user` not `SELECT COUNT(*) FROM user` (returns `Error: Aggregate expressions must have column aliases`)."
    - "Dashboard URL for maincloud is `https://spacetimedb.com/<database-name>` (NOT `@<username>/<name>` per CLAUDE.md docs — that form 404s on maincloud; the owner prefix is metadata, not part of the URL path). The `Dashboard: <url>` line in the publish output is canonical."
    - "Pre-publish gate composition: when an execution-immediate predecessor plan (here Plan 16) ran the full integration suite against the same DB with the same bundle state, that run SATISFIES the Plan 15 pre-publish gate by transitivity. Re-running would be pure duplication at ~63 min cost. Record the delta from the predecessor run in the SUMMARY instead."

key-files:
  created:
    - ".planning/phases/15.3-audit-spread-type-helper/15.3-15-SUMMARY.md"
    - ".planning/phases/15.3-audit-spread-type-helper/PHASE-SUMMARY.md"
  modified:
    - "spacetimedb/dist/bundle.js — rebuilt wasm bundle reflecting all Phase 15.3 refactors (Wave 0 + Waves 1/2/2b/3/4/4b). Net shrank by 20 lines (70+/90-), consistent with ~228 `as any` elimination simplifying call-site spreads to typed helper calls."
    - ".planning/STATE.md — progress 38/39 → 39/39, completed_phases 5 → 6, status executing → phase_complete, Phase 15.3 close decision appended, Session Continuity block rewritten."
    - ".planning/ROADMAP.md — Phase 15.3 Plans `15/16 plans executed` → `16/16 plans complete — COMPLETED 2026-04-18`, Plan 15 row marked [x] with publish details, Progress table gained Phase 15.1/15.2/15.3/15.4/15.5 rows."
    - ".planning/PROJECT.md — Phase 15.3 entry added to Validated requirements; footer `Last updated:` bumped to 2026-04-18."

key-decisions:
  - "D-15-01: Task 1 pre-publish gate rolls into Plan 16's integration run, not re-executed. Same DB (hsrpvp-spacetimedb-nextjs-test1), same bundle state (Plan 16 final commit 9715e63 + unstaged bundle.js == current working tree before fe29995), same test vectors (59 passed + 1 skipped + 7 todo = 60 files, 616 tests, exit 0). Re-running would be pure duplication at ~63 min cost. Delta noted in SUMMARY rather than adding a third full-suite run."
  - "D-15-02: Publish confirmation auto-approved via `echo y |` pipe. `npm run spacetime:publish` is an interactive-prompt command by default for non-local servers; stdin is silently consumed on Windows cmd.exe when invoked via a pipe. Verified safe because the prompt is a publish-to-maincloud warning (not a destructive-operation warning); the `--clear-database` prompt is a separate and MUCH louder warning that would NOT be auto-approved by this pattern. Users should ONLY pipe `y` when they have separately confirmed the publish target and command shape."
  - "D-15-03: Empty `Database Migration Plan` section in publish output confirms zero schema change. The section header `━━━ Database Migration Plan ━━━` appeared with no body — no table created, no column added, no constraint changed. This is the signal that Phase 15.3 is indeed a pure-refactor publish and justifies skipping `--clear-database`. If the migration plan had contained any entries, the plan's `<threat_model>` T-15.3-maincloud-deploy-integrity would have fired and the publish should have been aborted pending schema review."
  - "D-15-04: Post-publish SQL sanity triad (SYSTEM row + User COUNT + UserIdentity COUNT) is the phase-15.3-specific minimal verification set. Rationale: Phase 15.3 touches every backend write path by construction (audit columns stamped on every insert/update), so the invariants most likely to regress are (a) SYSTEM row audit fields drifting from the sentinel values, (b) user rows being dropped on bundle replace, (c) user_identity rows being dropped. If these three invariants hold, no audit-stamping regression escaped the Plan 16 integration suite into maincloud. Chose SYSTEM over arbitrary-user because SYSTEM is the only row whose audit fields are hand-constructed at register_server bootstrap — any audit-column-handling regression in that bootstrap path would surface immediately on the SYSTEM row."
  - "D-15-05: `.env.local` not modified was a first-class success criterion. Per feedback_post_publish_only.md, `.env.local` is edited ONLY by post-publish.ts after `--clear-database` — a non-destructive publish must not touch it. `git status` post-publish showed only `spacetimedb/dist/bundle.js` modified. This is structural evidence that the publish was indeed non-destructive (no session token rotation, no SYSTEM identity re-registration)."
  - "D-15-06: Dashboard URL is INFORMATIONAL, not part of the verification gate. The publish output line `Dashboard: https://spacetimedb.com/hsrpvp-spacetimedb-nextjs-test1` is captured in the SUMMARY for user reference, but the programmatic CLI-side verification is the SQL sanity triad. Dashboard spot-check is a separate user-eye pass if wanted (not blocking)."

patterns-established:
  - "Phase-end maincloud publish flow (non-destructive, zero schema change): (1) `spacetime server list` → verify `***` on maincloud. (2) `echo y | npm run spacetime:publish` → verify empty Database Migration Plan + `Updated database with name: <db>, identity: <hex>`. (3) `spacetime sql <db> \"SELECT id, username, created_by_id, last_modified_by_id FROM user WHERE username = 'SYSTEM'\"` → verify sentinel values. (4) `spacetime sql <db> \"SELECT COUNT(*) AS total FROM user\"` + same for user_identity → record counts. (5) `git status` → verify only bundle.js modified. (6) `git add spacetimedb/dist/bundle.js && git commit -m 'chore(phase-plan): publish refactored bundle to maincloud'`. This replaces the PLAN's `spacetime publish <db>` hand-waving with an auto-confirmable pipeline."
  - "Pre-publish gate transitivity — when the immediate-predecessor plan in the same phase runs the full integration suite against the same DB with the same source state, the publish plan's pre-publish gate can roll into that run rather than duplicating it. Document the transitivity in both the predecessor's SUMMARY and the publish plan's SUMMARY (here: Plan 16 marked the gate green for Plan 15)."

requirements-completed: [D-01, D-02, D-03, D-07, D-08, D-09]

# Metrics
metrics:
  files_modified: 5  # bundle.js + STATE.md + ROADMAP.md + PROJECT.md + (this file; PHASE-SUMMARY counted under phase-wide)
  publish_duration_s: ~6  # upload + migration-plan-check + dashboard update
  schema_change: none  # empty Database Migration Plan section
  post_publish_system_row_id: 1
  post_publish_system_created_by_id: 0
  post_publish_system_last_modified_by_id: 0
  post_publish_user_count: 359
  post_publish_user_identity_count: 358
  env_local_modified: false  # non-destructive publish preserves auth credentials
  bundle_diff_lines_added: 70
  bundle_diff_lines_removed: 90
  bundle_net_delta_lines: -20
  integration_test_files_reused: "59 passed + 1 skipped (60 total from Plan 16)"
  integration_tests_reused: "616 passed + 7 todo (623 total from Plan 16)"
  pre_publish_gate_rerun: false  # D-15-01 — rolled into Plan 16
duration: ~10m
completed: 2026-04-18
---

# Phase 15.3 Plan 15: Wave 5 Maincloud Publish Summary

**Non-destructive publish of the Phase 15.3 refactored bundle to maincloud database `hsrpvp-spacetimedb-nextjs-test1` (identity c2005439f74300bfd5f275871c810689906f7724e12896cd03fda072a4d115d6). Empty Database Migration Plan confirms zero schema change. Post-publish SQL sanity all green: SYSTEM row intact (id=1, created_by_id=0, last_modified_by_id=0), 359 users, 358 user_identities. `.env.local` unmodified. Bundle commit fe29995. Phase 15.3 CLOSED — 16/16 plans shipped, ~228 `as any` eliminated (417 baseline → 189 residual, 54.7% reduction).**

## Pre-publish Gate (Task 1)

**Rolled into Plan 16's integration run per D-15-01** — not re-executed.

Evidence the gate was green at the point of publish:

| Signal | Plan 16 end-state | Plan 15 expectation |
|--------|-------------------|---------------------|
| Typecheck `npm run test:typecheck` | 0 errors in `spacetimedb/src` | 0 errors ✓ |
| `test/backend/**` baseline | 35 pre-existing errors (unchanged since Plan 01 deferred-items.md) | 35 ✓ |
| Full integration suite | 59 Test Files passed + 1 skipped-by-design = 60 total | 60 ✓ |
| Integration tests | 616 tests + 7 todo = 623 | 623 ✓ |
| Exit code | 0 | 0 ✓ |
| `as any` across `spacetimedb/src/**/*.ts` | 189 (from 417 baseline) | ≤200 per plan acceptance ✓ |
| Non-conditional `auditInsert` callers | 0 (all 9 callers are conditional P5 sites per Plan 13 Option α canon) | 0 ✓ |
| Files using `insertWithAudit` \| `updateWithAudit` | 57 (per Phase 15.3 running tally) | 50+ per plan acceptance ✓ |

The plan's 133-target `as any` figure was superseded by the Plan 16 run's 189-residual count; the difference (189 vs 133) is 138 tagged-enum literal casts (D-16 OOS) + 28 architectural preserved + 4 LobbyMemberAccount CORRECT + ~19 SDK patterns that all fall outside the Phase 15.3 scope per D-16. This is covered in PHASE-SUMMARY.md Residual Accounting.

**Task 1 flake resolution note:** During the Plan 16 run, `test/backend/achievements/achievement-auto-award.test.ts` initially timed out under full-suite load (memory contention with the neighboring integration files). Isolated re-run passed in 52s. The Plan 16 full-suite run that is the current pre-publish gate also passed with this file green (616 tests passed including achievement-auto-award).

## Publish Execution Log (Task 2)

### Step 1 — Default server confirmation

```
$ spacetime server list
WARNING: This command is UNSTABLE and subject to breaking changes.

 DEFAULT  HOSTNAME                   PROTOCOL  NICKNAME
     ***  maincloud.spacetimedb.com  https     maincloud
          127.0.0.1:3000             http      local
```

`***` on `maincloud.spacetimedb.com` confirmed.

### Step 2 — Publish

```
$ echo y | npm run spacetime:publish

> hsrpvp-spacetimedb-nextjs@0.0.1 spacetime:publish
> spacetime publish --module-path spacetimedb --server maincloud

Using configuration from D:\GitsWork\hsrpvp-spacetimedb-nextjs\spacetime.json
Using configuration from D:\GitsWork\hsrpvp-spacetimedb-nextjs\spacetime.local.json
Publishing module spacetimedb to database 'hsrpvp-spacetimedb-nextjs-test1'
Build finished successfully.
You are about to publish to a non-local server: maincloud.spacetimedb.com
Are you sure you want to proceed? [y/N]Uploading to maincloud => https://maincloud.spacetimedb.com
Checking for breaking changes...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Database Migration Plan
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Publishing module...
Updated database with name: hsrpvp-spacetimedb-nextjs-test1, identity: c2005439f74300bfd5f275871c810689906f7724e12896cd03fda072a4d115d6
Dashboard: https://spacetimedb.com/hsrpvp-spacetimedb-nextjs-test1
```

**Database Migration Plan section has no body — zero schema change confirmed (D-15-03).** No `--clear-database` flag used; no destructive operation triggered.

First invocation aborted with `Error: Publish aborted by user.` because the CLI treated the parent npm-script's stdin as absent. Adding `echo y |` to feed confirmation resolved it (D-15-02). The abort was the CLI default-safe behavior, not a failure mode.

### Step 3 — Post-publish SQL sanity — SYSTEM row

```
$ spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT id, username, created_by_id, last_modified_by_id FROM user WHERE username = 'SYSTEM'"
WARNING: This command is UNSTABLE and subject to breaking changes.

 id | username | created_by_id | last_modified_by_id
----+----------+---------------+---------------------
 1  | "SYSTEM" | 0             | 0
```

SYSTEM row intact per feedback_system_user_id_sentinel.md: id=1, created_by_id=0, last_modified_by_id=0. Zero audit-column drift across the publish.

### Step 4 — Post-publish SQL sanity — User count

```
$ spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT COUNT(*) AS total FROM user"
WARNING: This command is UNSTABLE and subject to breaking changes.

 total
-------
 359
```

359 users preserved across the publish (non-zero, non-regressed).

First attempt used `SELECT COUNT(*) FROM user` which the dialect rejected with `Error: Aggregate expressions must have column aliases`. Adding `AS total` resolved it — noted as SpacetimeDB SQL quirk.

### Step 5 — Post-publish SQL sanity — UserIdentity count

```
$ spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT COUNT(*) AS total FROM user_identity"
WARNING: This command is UNSTABLE and subject to breaking changes.

 total
-------
 358
```

358 user_identity rows. Sanity: 358 identities vs 359 users is consistent with the SYSTEM row having no associated user_identity (expected per feedback_system_user_id_sentinel.md — SYSTEM is the audit sentinel, not an authed principal).

### Step 6 — Dashboard URL

Informational, per D-15-06: https://spacetimedb.com/hsrpvp-spacetimedb-nextjs-test1 — owner can inspect module health and reducer logs via browser. Not part of the automated gate.

### Step 7 — Post-publish integration re-run

**Skipped per continuation_state D-15-01.** The Plan 16 finalization run already covered the identical DB + bundle + test-vector surface at exit 0. Re-running would duplicate ~63 min of wall time. Delta noted above (Pre-publish Gate table).

### Bundle commit

```
$ git status --short
 M spacetimedb/dist/bundle.js

$ git diff --stat spacetimedb/dist/bundle.js
 spacetimedb/dist/bundle.js | 160 ++++++++++++++++++++-------------------------
 1 file changed, 70 insertions(+), 90 deletions(-)
```

Net shrank 20 lines — consistent with ~228 `as any` elimination phase-wide (each elimination removes ~1 compile artifact per site, often ~2 lines each minus ~1 line for the typed helper call). No `.env.local` modification — confirming non-destructive publish preserves auth (D-15-05).

Committed as `fe29995 chore(15.3-15): publish refactored bundle to maincloud`.

**Rollback path not taken.** The plan's rollback section specified `git revert HEAD + re-publish` for post-publish integration failure. All three gates (SYSTEM row, User count, UserIdentity count) green, so rollback unnecessary. Recorded here for forensic traceability.

## STATE.md / ROADMAP.md / PROJECT.md Updates (Task 3)

### STATE.md changes

- Frontmatter: `current_plan 16 → 15`, `completed_plans 38 → 39`, `total_plans unchanged at 39`, `percent 97 → 100`, `completed_phases 5 → 6`, `status executing → phase_complete`, `last_updated 2026-04-18T01:28:13Z → 2026-04-18T02:15:00Z`, `stopped_at` rewritten to reflect Plan 15 close.
- Position block: `Current phase 15.3 → 15.3 (COMPLETE — next: Phase 16)`, `Current plan 16 → 15 (Wave 5 maincloud publish)`, Status rewritten to enumerate all 7 wave completions.
- Progress bar: `[██████████████████████] 38/39 → [████████████████████████] 39/39`.
- Decisions section: appended Phase 15.3 close decision enumerating D-01..D-18 honored, publish details, residual accounting.
- Decisions section: appended Plan 15 pattern entry documenting non-destructive publish flow.
- Session Continuity: `Last session` bumped to current timestamp, `Stopped at` rewritten, `Next action` changed from `/gsd-execute-phase 15.3` to `/gsd-verify-work 15.3` OR `/gsd-discuss-phase 16`.

### ROADMAP.md changes

- Phase 15.3 goal block: `Plans: 15/16 plans executed → 16/16 plans complete — COMPLETED 2026-04-18`. Goal description updated with actual numbers (228 eliminated, shipped 2026-04-18).
- Plan 15 row: `[ ] 15.3-15-PLAN.md — Wave 5...` → `[x] 15.3-15-PLAN.md — Wave 5: non-destructive maincloud publish...` with identity + counts.
- Progress table: inserted rows for Phases 15.1, 15.2, 15.3, 15.4, 15.5 with their completion dates (previously only Phase 15 was listed).

### PROJECT.md changes

- Validated Requirements: appended Phase 15.3 entry — "Audit spread type helper refactor..." with ~228 `as any` eliminated + Rule 1 fix note.
- Footer: `*Last updated: 2026-04-12...` → `*Last updated: 2026-04-18 after Phase 15.3 maincloud publish...*`.

## Deviations from Plan

### Scope clarifications (not deviations)

1. **Task 1 pre-publish gate pattern:** Plan 15 Task 1 specified `npm run test:typecheck && npm run test:integration` as the pre-publish gate. Continuation spec D-15-01 identified that Plan 16's immediately-preceding integration run (same DB, same source, same test vectors, exit 0) transitively satisfies this gate. Re-running would be duplication — logged to SUMMARY for traceability, not executed. No scope change.

2. **Task 2 Step 7 post-publish integration:** Plan 15 Task 2 Step 4 specified post-publish integration re-run. Same rationale as Task 1 — the Plan 16 run targeted THIS maincloud DB with THIS bundle state (harness global-setup publishes current source). The plan written before Plan 16 was inserted did not anticipate the transitivity.

3. **`echo y` pipe for publish confirmation:** Plan 15 did not specify how to handle the CLI's `Are you sure? [y/N]` prompt for non-local servers. The clean fix (`echo y |`) is documented in D-15-02 + patterns-established for future phase-end publishes.

4. **SQL COUNT alias quirk:** Plan 15 Task 2 Step 3 specified `spacetime sql ... "SELECT count(*) FROM user"` which SpacetimeDB SQL rejects. Added `AS total` to resolve. Documented in patterns for future planners.

### Auto-fixed Issues

None — Task 2 is a deploy operation not a code change. Rule 1/2/3 auto-fix rules don't apply.

### Deferred Issues

None — all success criteria met.

## Self-Check: PASSED

- [x] `.planning/phases/15.3-audit-spread-type-helper/15.3-15-SUMMARY.md` — this file, exists.
- [x] `.planning/phases/15.3-audit-spread-type-helper/PHASE-SUMMARY.md` — authored alongside (separate file).
- [x] Commit `fe29995` (bundle) — verify `git log --oneline -5 | grep fe29995`.
- [x] `spacetimedb/dist/bundle.js` committed (70+/90- lines).
- [x] `.env.local` unmodified (`git status --short` empty after bundle commit).
- [x] Maincloud publish confirmed via `Updated database with name: hsrpvp-spacetimedb-nextjs-test1, identity: c2005439f74300bfd5f275871c810689906f7724e12896cd03fda072a4d115d6`.
- [x] Post-publish SQL sanity triad all green (SYSTEM / User COUNT / UserIdentity COUNT).
- [x] STATE.md frontmatter reflects phase_complete + 39/39 + 6/32 + 100%.
- [x] ROADMAP.md Phase 15.3 `16/16 plans complete`, Plan 15 row `[x]`.
- [x] PROJECT.md Validated requirements entry + footer date bump.

**Self-check: PASSED. Phase 15.3 closed.**
