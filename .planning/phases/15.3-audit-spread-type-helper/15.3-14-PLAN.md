---
phase: 15.3-audit-spread-type-helper
plan: 14
type: execute
wave: 4
depends_on: []
files_modified:
  - test/backend/auth/auth-security.test.ts
  - test/backend/auth/auth-views.test.ts
  - test/backend/auth/ban-admin.test.ts
  - test/backend/auth/server-link-provider.test.ts
  - test/backend/brackets/bracket-advancement.test.ts
  - test/backend/match-results/rating-admin.test.ts
autonomous: true
requirements: [D-01, D-11, D-12]
tags: [refactor, tests, placeholder-cleanup, wave-4, wave-b]

must_haves:
  truths:
    - "All 8 expect(true).toBe(true) placeholder assertions replaced with it.todo(...)"
    - "Test names preserved as it.todo descriptions"
    - "No production code modified by this plan"
    - "Vitest reporter shows 8 new 'pending' tests"
    - "Existing tests in the same files continue to pass"
  artifacts:
    - path: "test/backend/auth/auth-security.test.ts"
      provides: "SEC-04 placeholder converted to it.todo"
      contains: "it.todo"
    - path: "test/backend/auth/auth-views.test.ts"
      provides: "3 placeholder tests converted to it.todo"
      contains: "it.todo"
    - path: "test/backend/brackets/bracket-advancement.test.ts"
      provides: "elimination bracket draw rejection converted"
      contains: "it.todo"
  key_links:
    - from: "these 6 test files"
      to: "vitest runner"
      via: "it.todo pending test indicator"
      pattern: "it\\.todo\\("
---

<objective>
Wave 4 (Wave B folded in per D-11). Convert 8 mechanical `expect(true).toBe(true)` placeholder assertions across 6 test files to `it.todo(...)`. Pure test-file hygiene. Independent of Wave 1-3 — can execute in parallel with Wave 1 clusters. Frontmatter labels `wave: 4` for consistency with CONTEXT.md D-08 (which labels placeholder-tests as Wave 4), but `depends_on: []` means the executor can wave-schedule it alongside Wave 1 (disjoint file sets: test/backend/** vs spacetimedb/src/**).

**8 sites verified by grep 2026-04-17 (per CONTEXT.md D-12 + RESEARCH.md §Wave 4 table):**

| File | Line | Test name |
|------|------|-----------|
| test/backend/auth/auth-security.test.ts | 208 | `SEC-04 is verified manually via API route test in Task 4 checkpoint` |
| test/backend/auth/auth-views.test.ts | 30 | `merges User + UserPrivate for caller identity (verified in UAT test 10)` |
| test/backend/auth/auth-views.test.ts | 39 | `returns only caller own identity mapping (verified in SEC-02)` |
| test/backend/auth/auth-views.test.ts | 49 | `gated by Moderator+ role (not testable via SQL — no sender context)` |
| test/backend/auth/ban-admin.test.ts | 244 | `D-19 clientConnected ban-on-reconnect: insert pattern matches admin_ban_user (structural coverage)` |
| test/backend/auth/server-link-provider.test.ts | 127 | Case 1b cross-browser merge (verified via UAT Paths 3+4) |
| test/backend/brackets/bracket-advancement.test.ts | 857 | `elimination bracket rejects draw (no winner) → error` |
| test/backend/match-results/rating-admin.test.ts | 337 | else-branch "account already has characters — skip gracefully" |

Purpose: Eliminate the false-green signal pattern. `expect(true).toBe(true)` passes on every run, providing no diagnostic value. `it.todo(name)` shows in Vitest reporter as a pending test that reminds maintainers the behavior needs coverage.

**Non-obvious cases (RESEARCH.md flagged):**

- `server-link-provider.test.ts` L127 — the placeholder is inside a describe block ("Case 1b: Cross-browser merge"), not a top-level `it(...)`. Read the file to confirm the exact `it(...)` wrapping line before transforming.
- `rating-admin.test.ts` L337 — inside an `else` branch of `if/else` inside a test body. Not a clean `it.todo` target. Two options: (a) remove the else branch entirely if it's dead code, (b) leave `expect(true).toBe(true)` with a clarifying comment ("intentional no-op for this branch — the outer test proves behavior on the if-branch"). Choose based on file inspection.

Output: 6 files with `it.todo` replacing placeholder assertions (or justified comment at L337). Vitest reporter shows 7-8 new pending tests.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-audit-spread-type-helper/15.3-CONTEXT.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
@test/backend/auth/auth-security.test.ts
@test/backend/auth/auth-views.test.ts
@test/backend/auth/ban-admin.test.ts
@test/backend/auth/server-link-provider.test.ts
@test/backend/brackets/bracket-advancement.test.ts
@test/backend/match-results/rating-admin.test.ts
</context>

<threat_model>
N/A — Wave 4 touches test files only. No production code affected. No new attack surface. Security impact: **ZERO**.

Per quality_gate: "N/A — Wave 4 touches test files only" is the correct threat_model content for this plan.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert auth test files (4 files, 6 sites)</name>
  <files>
    test/backend/auth/auth-security.test.ts,
    test/backend/auth/auth-views.test.ts,
    test/backend/auth/ban-admin.test.ts,
    test/backend/auth/server-link-provider.test.ts
  </files>
  <read_first>
    test/backend/auth/auth-security.test.ts,
    test/backend/auth/auth-views.test.ts,
    test/backend/auth/ban-admin.test.ts,
    test/backend/auth/server-link-provider.test.ts
  </read_first>
  <action>
For each placeholder site:

1. Read the surrounding `it(name, () => { ... })` block.
2. Extract the test name (the string argument to `it`).
3. Replace the entire `it(name, () => { ... })` block with `it.todo(name);`.

**Canonical excerpt** (auth-security.test.ts L208):
```ts
// BEFORE
it('SEC-04 is verified manually via API route test in Task 4 checkpoint', () => {
    expect(true).toBe(true);
});

// AFTER
it.todo('SEC-04 is verified manually via API route test in Task 4 checkpoint');
```

**Canonical excerpt** (auth-views.test.ts L30, L39, L49):
```ts
// BEFORE
it('merges User + UserPrivate for caller identity (verified in UAT test 10)', () => {
    expect(true).toBe(true);
});

// AFTER
it.todo('merges User + UserPrivate for caller identity (verified in UAT test 10)');
```

Apply the same transform to:
- auth-views.test.ts L39 (`returns only caller own identity mapping...`)
- auth-views.test.ts L49 (`gated by Moderator+ role...`)
- ban-admin.test.ts L244 (`D-19 clientConnected ban-on-reconnect...`)

**Non-obvious site — server-link-provider.test.ts L127:**

Read the file. The test at L127 is likely nested inside a `describe('Case 1b: Cross-browser merge', ...)` block. Find the actual `it(...)` that wraps the placeholder and replace it with `it.todo('<same name>')`. If the `it(...)` wrapper is missing (rare), wrap the description with `it.todo('<describe block name + context>')`.

Preserve the describe-block structure — do not remove describe wrappers.
  </action>
  <verify>
    <automated>npx vitest run --config test/vitest.integration.config.ts --dir test/backend/auth</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expect(true).toBe(true)" test/backend/auth/auth-security.test.ts` returns 0
    - `grep -c "expect(true).toBe(true)" test/backend/auth/auth-views.test.ts` returns 0
    - `grep -c "expect(true).toBe(true)" test/backend/auth/ban-admin.test.ts` returns 0
    - `grep -c "expect(true).toBe(true)" test/backend/auth/server-link-provider.test.ts` returns 0
    - `grep -c "it\\.todo" test/backend/auth/auth-security.test.ts` returns >= 1
    - `grep -c "it\\.todo" test/backend/auth/auth-views.test.ts` returns >= 3
    - `grep -c "it\\.todo" test/backend/auth/ban-admin.test.ts` returns >= 1
    - `grep -c "it\\.todo" test/backend/auth/server-link-provider.test.ts` returns >= 1
    - `npx vitest run --config test/vitest.integration.config.ts --dir test/backend/auth` exits 0 with existing tests still passing + pending (todo) count increased
  </acceptance_criteria>
  <done>6 auth-file placeholder sites converted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert brackets + rating-admin test files (2 files, 2 sites)</name>
  <files>
    test/backend/brackets/bracket-advancement.test.ts,
    test/backend/match-results/rating-admin.test.ts
  </files>
  <read_first>
    test/backend/brackets/bracket-advancement.test.ts,
    test/backend/match-results/rating-admin.test.ts
  </read_first>
  <action>
**Site 1 — bracket-advancement.test.ts L857:**

Clean `it.todo` candidate. Apply standard transform:
```ts
// BEFORE
it('elimination bracket rejects draw (no winner) → error', () => {
    expect(true).toBe(true);
});
// AFTER
it.todo('elimination bracket rejects draw (no winner) → error');
```

**Site 2 — rating-admin.test.ts L337 (non-obvious):**

This one is inside an `else` branch of a test body, not a standalone `it(...)`. Example shape:
```ts
it('recalculate-all seeds ratings for all HsrAccount rows', () => {
    // ... setup ...
    for (const account of accounts) {
        if (await hasCharacters(account.id)) {
            // ... assertions ...
        } else {
            expect(true).toBe(true);   // <-- L337, the placeholder
        }
    }
});
```

Two resolution strategies (pick one based on the actual code):

- **Strategy A — Remove the else branch entirely:** If the else branch represents "skip gracefully — no assertion needed", simply delete the else block (or convert it to a `continue`/comment). No `it.todo` involved.
- **Strategy B — Replace with comment:** If removing the branch changes loop logic:
  ```ts
  } else {
      // Intentional no-op: account already has characters — recalculate-all skips gracefully. Covered by the if-branch assertions above.
  }
  ```

Read the actual shape, pick the cleaner strategy. Do NOT leave `expect(true).toBe(true)` with no rationale — that's exactly what this wave eliminates.
  </action>
  <verify>
    <automated>npx vitest run --config test/vitest.integration.config.ts --dir test/backend/brackets --dir test/backend/match-results</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expect(true).toBe(true)" test/backend/brackets/bracket-advancement.test.ts` returns 0
    - `grep -c "expect(true).toBe(true)" test/backend/match-results/rating-admin.test.ts` returns 0 (placeholder removed or replaced with comment/no-op)
    - `grep -c "it\\.todo" test/backend/brackets/bracket-advancement.test.ts` returns >= 1
    - `npx vitest run --config test/vitest.integration.config.ts --dir test/backend/brackets` exits 0
    - `npx vitest run --config test/vitest.integration.config.ts --dir test/backend/match-results` exits 0
  </acceptance_criteria>
  <done>2 remaining sites converted; all 8 placeholders eliminated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full test/backend directory regression grep + vitest pass</name>
  <files></files>
  <read_first></read_first>
  <action>
Verify zero remaining placeholder sites across test/backend:

1. `grep -rn "expect(true).toBe(true)" test/backend --include="*.test.ts"` — must return 0 matches.
2. `grep -rn "it\\.todo" test/backend --include="*.test.ts" | wc -l` — should return 7+ (8 sites; one may have been strategy-B comment replacement).
3. `npx vitest run --config test/vitest.integration.config.ts --dir test/backend` — full backend test suite exits 0 with pending-tests count increased vs pre-Wave-4 baseline.
  </action>
  <verify>
    <automated>npx vitest run --config test/vitest.integration.config.ts --dir test/backend</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "expect(true).toBe(true)" test/backend --include="*.test.ts" | wc -l` returns 0
    - `grep -rn "it\\.todo" test/backend --include="*.test.ts" | wc -l` returns 7+
    - `npx vitest run --config test/vitest.integration.config.ts --dir test/backend` exits 0 (pending count up)
  </acceptance_criteria>
  <done>Wave 4 complete — 8 placeholder assertions eliminated; test suite healthy.</done>
</task>

</tasks>

<verification>
- Zero `expect(true).toBe(true)` assertions remain in test/backend
- 7+ new `it.todo(...)` pending tests appear in vitest reporter
- All pre-existing backend tests still pass
</verification>

<success_criteria>
- 8 placeholder sites eliminated across 6 files
- Zero production code touched by this plan
- Vitest reporter's pending-test count accurately reflects deferred coverage
</success_criteria>

<output>
After completion, create `.planning/phases/15.3-audit-spread-type-helper/15.3-14-SUMMARY.md`. Note: (a) exact mapping of 8 sites → resolution (it.todo vs comment replacement), (b) any non-obvious file-structure decisions.
</output>
