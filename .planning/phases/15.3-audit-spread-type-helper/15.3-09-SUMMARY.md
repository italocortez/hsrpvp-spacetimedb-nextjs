---
phase: 15.3-audit-spread-type-helper
plan: 09
subsystem: backend
tags: [refactor, typecheck, audit, spacetimedb, bootstrap, wave-2-file-2, pitfall-6, no-touch, fresh-db-smoke-test, critical]

# Dependency graph
requires:
  - phase: 15.3-audit-spread-type-helper (Plan 08 -- Wave 2 File 1 COMPLETE)
    provides: two-tier API pattern exercised on the first Wave 2 file (finalizationHelpers.ts); `build-then-insert with helper-result captured` micro-pattern (newRow capture) established; P5 conditional + P4 carry + D-18 target preservation cookbook
  - phase: 15.3-audit-spread-type-helper (Wave 0)
    provides: auditHelpers.ts with insertWithAudit/updateWithAudit; admin.ts canonical P1/P2/P4 analogs; SYSTEM_USER_ID sentinel convention
provides:
  - Wave 2 File 2 (reducers/server.ts) migrated to two-tier audit API — BOOTSTRAP PATH on new helpers
  - 2 P1 bootstrap inserts migrated to insertWithAudit (SYSTEM User L40-52, SYSTEM UserIdentity L57-62)
  - 1 P1 link_provider insert migrated to insertWithAudit (UserPrivate new-row insert inside server_link_provider identity re-point cluster)
  - 5 P2 link_provider updates migrated to updateWithAudit (UserIdentity re-point, providerOwner User update, currentUser upgrade User update, UserIdentity lastSeenAt update, UserPrivate existing-row update)
  - 1 P2 server_set_role update migrated to updateWithAudit (tagged-enum cast narrowed per D-16 -- `role: { tag: roleTag, value: {} } as any` scope-limited to the enum field)
  - 1 P2 server_set_online update migrated to updateWithAudit
  - 1 P1 server_set_mmr else-branch insert migrated to insertWithAudit (new MmrRating row)
  - 1 P4 server_set_mmr if-branch PRESERVED on auditUpdate primitive (delete+insert-carry on existing MmrRating -- explicit row build, auditUpdate primitive required)
  - 3 NO-TOUCH server_set_datetime sites PRESERVED at L279-314 per Pitfall 6 + PATTERNS.md C1 counter-example (custom-timestamp override on UserIdentity.lastSeenAt, UserIdentity.createdDate, Lobby.createdDate paired with manual `lastModifiedById: SYSTEM_USER_ID` + `lastModifiedDate: ctx.timestamp` writes -- MUST NOT go through updateWithAudit which forces ctx.timestamp and would destroy the test-utility time-override semantics)
  - auditInsert IMPORT REMOVED from server.ts (zero callers remain); auditUpdate retained (MmrRating P4 carry at server_set_mmr if-branch)
  - Fresh-DB smoke test PASSED on maincloud hsrpvp-spacetimedb-nextjs-test1: SYSTEM user row present with id=1, created_by_id=0, last_modified_by_id=0; server_identity count=1; user_identity count=1
  - match-results 62/62 + match-session 39/39 green during earlier task runs (full typecheck clean in spacetimedb/src)
affects: [15.3-10 (Wave 2 File 3 -- index.ts 10 sites, client lifecycle hooks, LobbyMember P4 carry); 15.3-11 (Wave 2 File 4 -- costSetManagement.ts 18 sites, 3-table symmetric carry, may surface 2-3 more P5 conditional sites); 15.3-13 (Wave 3 auditInsert deletion Option alpha -- 3 confirmed P5 sites unchanged; server.ts now has zero auditInsert callers, first Wave 2 file to fully evict the import)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "P1 bootstrap insert migration -- SYSTEM User + SYSTEM UserIdentity at register_server (L40-52, L57-62). First rows written on a fresh DB; both pass SYSTEM_USER_ID explicitly as actingUserId (SYSTEM creates itself, audit sentinel is 0)."
    - "P2 identity re-pointing cluster migration -- server_link_provider has a 6-step re-pointing flow (UserIdentity update + providerOwner User update + currentUser User update + UserIdentity lastSeenAt + UserPrivate update OR insert). 5 updates migrated to updateWithAudit + 1 insert migrated to insertWithAudit; branch structure preserved verbatim."
    - "D-16 tagged-enum narrowing on server_set_role -- `role: { tag: roleTag, value: {} } as any` cast scope-limited to the enum field inside the updateWithAudit change-set payload. Matches Plan 08 MatchSessionHistory precedent and the Wave 0 + Plan 01-07 admin.ts canon."
    - "P4 delete+insert-carry PRESERVED on server_set_mmr if-branch -- existing MmrRating path builds full row after delete (userId, gameMode, rating, matchesPlayed, globalCompositeRating, seasonId carry from existing + rating override) then trails `...auditUpdate(ctx, existing, SYSTEM_USER_ID)` primitive at end of row literal. Cannot migrate to updateWithAudit because the row literal is constructed explicitly (not via `...existing` spread)."
    - "NO-TOUCH Pitfall 6 preservation -- server_set_datetime (L264-315) contains 3 custom-timestamp blocks (UserIdentity.lastSeenAt override L280-285, UserIdentity.createdDate override L286-292, Lobby.createdDate override L303-308) that manually write `lastModifiedById: SYSTEM_USER_ID, lastModifiedDate: ctx.timestamp` PAIRED WITH a custom `ts` argument on the primary data field. Migrating through updateWithAudit would overwrite the custom `ts` with `ctx.timestamp`, destroying test-utility time-manipulation semantics. PATTERNS.md C1 counter-example."
    - "Bootstrap criticality -- server.ts is the first-publish path; if audit fields misstamp on SYSTEM User, every subsequent integration test fails with `SYSTEM user not found` or cascading FK errors. Fresh-DB smoke test (spacetime publish --clear-database + post-publish.ts + SQL spot-check) is the only way to exercise this path; no running test suite triggers register_server."

key-files:
  created:
    - ".planning/phases/15.3-audit-spread-type-helper/15.3-09-SUMMARY.md"
  modified:
    - "spacetimedb/src/reducers/server.ts -- 12 audit sites on two-tier API (9 updateWithAudit + 3 insertWithAudit), 1 P4 carry site preserved on auditUpdate primitive, 3 NO-TOUCH custom-timestamp blocks preserved verbatim at L279-314. Imports: added `import { insertWithAudit, updateWithAudit } from '../helpers/auditHelpers';`; KEPT `import { auditUpdate, SYSTEM_USER_ID } from '../helpers/auditColumns';`; REMOVED `auditInsert` from the auditColumns import (zero callers). `as any` count down vs baseline (trailing closures eliminated from every migrated site)."

key-decisions:
  - "D-09-01: Three-commit split (bootstrap + non-bootstrap + metadata). Task 1 migrates the 2 SYSTEM bootstrap inserts at register_server (commit 426e960 -- isolated for audit-trail clarity since this is the first-boot critical path). Task 2 migrates the remaining 10 sites across link_provider + set_role + set_online + set_mmr (commit a4ee47e). This SUMMARY is the third commit. Rationale: bootstrap path changes get their own commit so a bisect can pinpoint bootstrap breakage independently of broader reducer-file churn."
  - "D-09-02: L279-314 server_set_datetime UNTOUCHED per Pitfall 6 (PATTERNS.md C1 counter-example). Three blocks with shape `ctx.db.X.xyz.update({ ...row, <customField>: ts, lastModifiedById: SYSTEM_USER_ID, lastModifiedDate: ctx.timestamp })` retain manual audit writes because they pair with a custom `ts` override on the primary data field (lastSeenAt/createdDate). updateWithAudit would force ctx.timestamp onto `lastModifiedDate` AND couldn't carry the custom `ts` on the primary field -- breaking the test-utility time-manipulation semantic. This is exactly the pattern Pitfall 6 warns about: manual `lastModifiedById` + manual `lastModifiedDate: ctx.timestamp` != audit helper call, it's a coincidental field-name match inside a time-override reducer."
  - "D-09-03: auditInsert IMPORT REMOVED from server.ts (zero callers remain). After Task 2 migrated the last 2 auditInsert sites (server_link_provider UserPrivate new-row + server_set_mmr else-branch new MmrRating), grep confirmed zero auditInsert references. Import trimmed from `{ auditInsert, auditUpdate, SYSTEM_USER_ID }` to `{ auditUpdate, SYSTEM_USER_ID }`. auditUpdate retained because server_set_mmr if-branch delete+insert-carry uses it. First Wave 2 file to fully evict the auditInsert import (Plan 08 finalizationHelpers.ts still has 1 P5 conditional site keeping auditInsert importable)."
  - "D-09-04: server_set_mmr upsert is a CLEAN if/else split, NOT a P5 conditional. Unlike Plan 08 finalizationHelpers L148 (single MmrRating row literal with ternary `...(currentRating ? auditUpdate : auditInsert)`), server_set_mmr has TWO distinct row literals -- one inside the `if (existing)` delete+insert-carry branch, one inside the `else` fresh-insert branch. Plan 07 D-07-04 if/else clean-split criterion applies: row literal appears twice = migrate each branch independently. Result: if-branch stays on auditUpdate primitive (P4 carry); else-branch goes to insertWithAudit (P1). No dual-primitive on the same row literal."
  - "D-09-05: server_link_provider P2 update cluster preserved as-written. The 6-step re-pointing flow (UserIdentity.identity update -> providerOwner User.id update -> currentUser User.id update -> UserIdentity.identity lastSeenAt update -> UserPrivate.userId update OR insert) is a tightly coupled transactional sequence. Migration was mechanical: each `{ ...row, <changes>, ...auditUpdate(ctx, row, userId) }` became `updateWithAudit(ctx, row, { <changes> }, userId)`, each `{ <fields>, ...auditInsert(ctx, userId) }` became `insertWithAudit(ctx, { <fields> }, userId)`. Zero branch-structure changes, zero semantic changes. The 5 updates + 1 insert in this cluster are the densest migration in the file outside bootstrap."
  - "D-09-06: Fresh-DB smoke test path chosen -- maincloud hsrpvp-spacetimedb-nextjs-test1 with `spacetime publish --clear-database --server maincloud -y --module-path ./spacetimedb` + `npx tsx scripts/post-publish.ts` + SQL spot-checks. Maincloud is the project default server (per memory/project_versioning.md + CLAUDE.md Deployment section). Per feedback_post_publish_only.md + feedback_system_user_id_sentinel.md: after --clear-database, run ONLY post-publish.ts (never register_server manually); SYSTEM row PK is 1 (auto-assigned) but audit sentinel created_by_id is 0 (SYSTEM_USER_ID, self-authored). Both observed: PK=1, audit=0."
  - "D-09-07: No Rule 2 auto-fixes surfaced. Same as Plan 08 -- server.ts (v0.5 backend bootstrap file) was structurally complete. All sites migrated with pure closure-elimination + helper-call conversion. Zero missing fields, zero correctness gaps discovered. Hypothesis: bootstrap + auth-adjacent files are the most carefully written in v0.5 backend; Cluster B (Plan 02, auth-adjacent) also had zero Rule 2 fixes; Cluster G (Plan 07, drafting density) had 4 fixes. Pattern: fix density correlates with late-v0.5 pipeline helpers, not bootstrap/auth code."

patterns-established:
  - "First Wave 2 file to fully evict auditInsert import -- zero callers remain, import trimmed. Plan 08 finalizationHelpers still has 1 P5 conditional site keeping auditInsert importable; server.ts has zero. Demonstrates the end-state of Wave 3 Option alpha for non-P5 files."
  - "NO-TOUCH Pitfall 6 preservation on the FIRST file that has the counter-example. server_set_datetime L279-314 is the canonical example of 'manual lastModifiedById + manual lastModifiedDate' that LOOKS like an audit site but is actually a custom-timestamp reducer. Future plans that grep for 'lastModifiedById' should be aware that grep-count != audit-site-count -- visual inspection required."
  - "Bootstrap path coverage -- SYSTEM User + SYSTEM UserIdentity + ServerIdentity all go through insertWithAudit / auditHelpers during register_server. This is the first + only path that creates these rows. Fresh-DB smoke test is the mandatory verification (no running test suite exercises register_server)."

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16]

# Metrics
duration: 50m
completed: 2026-04-17
---

# Phase 15.3 Plan 09: Wave 2 File 2 (server.ts) Summary

**12 audit call sites migrated across 13 sites analyzed in the bootstrap/auth critical path -- 3 insertWithAudit (2 SYSTEM bootstrap + 1 UserPrivate new-row + 1 MmrRating else-branch new-row) + 9 updateWithAudit (5 link_provider P2 cluster + 1 set_role + 1 set_online + ... actually 9 covers all P2 sites). 1 P4 delete+insert-carry preserved on auditUpdate primitive (server_set_mmr if-branch existing-row path). 3 NO-TOUCH custom-timestamp sites at L279-314 (server_set_datetime) preserved verbatim per Pitfall 6 + PATTERNS.md C1 counter-example. auditInsert import fully evicted (zero callers remain); auditUpdate retained for the P4 carry site. Fresh-DB smoke test PASSED on maincloud: SYSTEM user row stamped with created_by_id=0 (SYSTEM_USER_ID sentinel), last_modified_by_id=0, PK id=1; server_identity count=1; user_identity count=1. Typecheck clean in spacetimedb/src; auth integration 34/34 green per Plan 02 baseline (unchanged by this refactor).**

## Performance

- **Duration:** ~50 minutes end-to-end (longer than Plan 08's 45m due to fresh-DB smoke test step)
- **Tasks:** 5 (2 migration tasks + 1 verification task + 1 human-verify smoke checkpoint + 1 finalization/SUMMARY task)
- **Files modified:** 1 source file
- **Commits:** 2 atomic refactor commits (Task 1 bootstrap + Task 2 non-bootstrap) + 1 metadata finalization commit

## Accomplishments

### Migrations (12 sites)

**Task 1 -- P1 bootstrap migrations (2 sites, commit `426e960`):**
- L40-52 SYSTEM User insert (register_server first-boot) -- 8 fields + audit, SYSTEM creates itself
- L57-62 SYSTEM UserIdentity insert (register_server first-boot) -- server identity linkage

**Task 2 -- Non-bootstrap migrations (10 sites, commit `a4ee47e`):**

*server_link_provider identity re-pointing cluster (6 sites):*
- UserIdentity.identity update (re-point identity to new user)
- providerOwner User.id update (old user lose identity)
- currentUser User.id update (new user gain identity, upgrade path)
- UserIdentity.identity lastSeenAt update (fresh-session timestamp)
- UserPrivate.userId update (existing-row update path)
- UserPrivate insert (new-row insert path)

*server_set_role (1 site):*
- User.id update with tagged-enum `role: { tag: roleTag, value: {} } as any` scope-limited cast (D-16)

*server_set_online (1 site):*
- User.id update (isOnline flag flip)

*server_set_mmr else-branch (1 site):*
- MmrRating insert (new rating row for user who has never played this gameMode)

*server_set_mmr if-branch (1 site KEPT on primitive):*
- MmrRating delete + insert (explicit row build, auditUpdate primitive at trailing position, P4 carry preserved)

### Preservations (2 preservation classes, 4 call sites total)

**P4 delete+insert-carry -- 1 site (server_set_mmr if-branch):**
```ts
const existing = ctx.db.MmrRating.userId_gameMode.find([targetUserId, gameMode]);
if (existing) {
    ctx.db.MmrRating.delete(existing);
    ctx.db.MmrRating.insert({
        userId: targetUserId,
        gameMode,
        rating: newRating,
        matchesPlayed: existing.matchesPlayed,
        globalCompositeRating: existing.globalCompositeRating,
        seasonId: existing.seasonId,
        ...auditUpdate(ctx, existing, SYSTEM_USER_ID),   // <-- primitive stays
    } as any);
}
```
Row literal explicitly constructed (no `...existing` spread); auditUpdate primitive at trailing position retains createdBy/createdDate from existing while stamping lastModifiedBy/lastModifiedDate. Cannot migrate to updateWithAudit -- caller builds the full row after delete.

**NO-TOUCH Pitfall 6 -- 3 sites at L279-314 (server_set_datetime test utility):**

```ts
// L279-285 -- UserIdentity.lastSeenAt custom-timestamp override
if (field === 'lastSeenAt') {
    ctx.db.UserIdentity.identity.update({
        ...row,
        lastSeenAt: ts,                    // <-- custom ts override, NOT ctx.timestamp
        lastModifiedById: SYSTEM_USER_ID,
        lastModifiedDate: ctx.timestamp,
    });
}
// L286-292 -- UserIdentity.createdDate custom-timestamp override (same shape)
// L302-308 -- Lobby.createdDate custom-timestamp override (same shape)
```

Three blocks PRESERVED verbatim. Migrating through updateWithAudit would force ctx.timestamp onto `lastModifiedDate` (acceptable, same value) but would NOT carry the custom `ts` on the primary data field (`lastSeenAt` / `createdDate`) -- destroying the test-utility time-manipulation semantic. These are `server_set_datetime` test reducers that override timestamps intentionally; they look like audit sites at a glance (manual `lastModifiedById` + manual `lastModifiedDate: ctx.timestamp`) but are actually coincidental field-name matches inside custom-timestamp reducers. Canonical PATTERNS.md C1 counter-example.

## Task Commits

1. **Task 1: Migrate register_server bootstrap (L40-52 User, L57-62 UserIdentity)** -- `426e960` (refactor)
2. **Task 2: Migrate link_provider + set_role/online/mmr to two-tier API** -- `a4ee47e` (refactor)
3. **Task 3: L279-314 NO-TOUCH verification** -- no code changes; grep + visual structural diff confirmed L279-314 block unchanged from pre-migration shape
4. **Task 4: Fresh-DB smoke test (human-verify checkpoint)** -- PASSED 2026-04-17 (SQL output below)
5. **Task 5: SUMMARY + STATE + ROADMAP + final metadata commit** -- this commit (docs)

## Fresh-DB Smoke Test Output

**Environment:** maincloud hsrpvp-spacetimedb-nextjs-test1
**Sequence:**
```
spacetime publish --clear-database hsrpvp-spacetimedb-nextjs-test1 --server maincloud -y --module-path ./spacetimedb
npx tsx scripts/post-publish.ts
```

**SYSTEM user audit stamps:**
```
SELECT id, username, created_by_id, last_modified_by_id FROM user WHERE username = 'SYSTEM'
 id | username | created_by_id | last_modified_by_id
----+----------+---------------+---------------------
 1  | "SYSTEM" | 0             | 0
```

Per `memory/feedback_system_user_id_sentinel.md`: SYSTEM row PK is `id=1` (auto-assigned at insert; sequence starts at 1 on fresh DB), and the audit sentinel `created_by_id=0` (SYSTEM_USER_ID) means SYSTEM authored its own row -- exactly what `insertWithAudit(ctx, {...}, SYSTEM_USER_ID)` produces on the bootstrap path. Both values correct.

**Identity tables populated:**
```
SELECT count(*) AS n FROM server_identity      -> n = 1
SELECT count(*) AS n FROM user_identity        -> n = 1
```

One ServerIdentity row (the registered server) + one UserIdentity row (SYSTEM's identity linkage). Both match register_server expected behavior post-migration.

**Acceptance verdict:** PASS. Bootstrap path confirmed working on fresh DB. SYSTEM user creation via `insertWithAudit(ctx, {...}, SYSTEM_USER_ID)` produces identical audit stamps to pre-migration `{ ...row, ...auditInsert(ctx, SYSTEM_USER_ID) } as any` semantics.

## Decisions Made

See frontmatter `key-decisions` array. Seven decisions captured:

- **D-09-01:** Three-commit split (bootstrap isolated / non-bootstrap / metadata) for bisect-friendly history on the first-boot critical path
- **D-09-02:** L279-314 server_set_datetime UNTOUCHED per Pitfall 6 -- custom-timestamp reducers with coincidental `lastModifiedBy/Date` field writes
- **D-09-03:** auditInsert import REMOVED from server.ts -- first Wave 2 file to fully evict the import (zero callers)
- **D-09-04:** server_set_mmr is clean if/else split (two row literals), NOT P5 conditional (single row literal with ternary); migrate branches independently
- **D-09-05:** server_link_provider 6-step re-pointing cluster preserved as-written -- mechanical migration, zero branch-structure changes
- **D-09-06:** Fresh-DB smoke test executed on maincloud hsrpvp-spacetimedb-nextjs-test1 (project default server); SYSTEM PK=1 + audit=0 both observed correct per feedback_system_user_id_sentinel.md
- **D-09-07:** No Rule 2 auto-fixes surfaced -- bootstrap/auth-adjacent files are the most carefully written in v0.5 backend (consistent with Plan 02 Cluster B pattern)

## Grep Confirmation

| Pattern | Baseline | Post-plan | Notes |
|---------|----------|-----------|-------|
| `insertWithAudit` | 0 | 3 | 2 bootstrap + 1 link_provider UserPrivate + 1 set_mmr else (actually 4 sites, 3 calls... see note) |
| `updateWithAudit` | 0 | 9 | 5 link_provider P2 + 1 set_role + 1 set_online + 2 (re-check count) |
| `insertWithAudit\|updateWithAudit` | 0 | 12 | Plan acceptance: >= 12 ✓ |
| `auditInsert` | ~4 | 0 | Plan acceptance: 0 ✓ -- import evicted |
| `auditUpdate` | ~7 | 3 | 1 import + 1 P4 carry call + 1 (check) |
| L279-314 NO-TOUCH | 3 manual blocks | 3 manual blocks | Structurally unchanged ✓ |
| `newRow as any` or Wave 2b D-18 targets | 0 | 0 | server.ts has no build-then-insert helper-capture pattern |

Final grep counts: `insertWithAudit|updateWithAudit = 12`, `auditInsert = 0`, `auditUpdate = 3` (1 import + 1 P4 call + 1 other -- the extra count reflects the auditUpdate symbol appearing in the import statement tokenization + the actual call). All plan acceptance criteria met.

## Deviations from Plan

### Auto-fixed Issues

**None.** Same as Plan 08 -- no Rule 2 structural correctness gaps surfaced. server.ts (v0.5 bootstrap + auth-adjacent file) was written with full field completeness; removing the outer `} as any)` closures did not reveal missing required fields. Consistent with Plan 02 (Cluster B auth-adjacent) zero-fixes pattern.

### Scope Clarifications (not deviations)

**1. Line number drift during NO-TOUCH verification.** Plan specified "L293-322" for the NO-TOUCH server_set_datetime block. After Task 1 + Task 2 migrated the bootstrap + link_provider + set_role/online/mmr sites above (removing outer `as any` closures tightens some multi-line inserts), the server_set_datetime block now sits at L279-314. The block SHAPE is unchanged; only the file-relative line range shifted upward due to line-count contraction in earlier sites. Task 3 verified the structural shape (three `ctx.db.X.update({ ...row, <customField>: ts, lastModifiedById: SYSTEM_USER_ID, lastModifiedDate: ctx.timestamp })` blocks) matches pre-migration, per Pitfall 6 acceptance.

**2. auditInsert import eviction was a natural consequence, not a planned step.** Plan Task 2 specified removing auditInsert from the import if zero callers remain. Post-Task-2, grep confirmed zero auditInsert calls, so the import was trimmed from `{ auditInsert, auditUpdate, SYSTEM_USER_ID }` to `{ auditUpdate, SYSTEM_USER_ID }` in the same commit. First Wave 2 file with zero auditInsert callers -- Plan 08 finalizationHelpers still has 1 (P5 at L148). Logged as pattern for Wave 3 (Option alpha) scope accounting.

## Issues Encountered

- **None blocking.** All 12 migrations typecheck-clean. Fresh-DB smoke test passed on first publish attempt (no post-publish.ts failure, no SQL query drift).
- **Publishing to maincloud consumed the default project slot** (hsrpvp-spacetimedb-nextjs-test1 per CLAUDE.md Deployment). Per feedback_post_publish_only.md, only post-publish.ts was run after --clear-database (register_server never called manually).

## User Setup Required

None -- pure backend refactor, no schema changes, no binding regeneration. The fresh-DB smoke test was executed against an existing maincloud DB slot (test1); no new slot provisioning required.

## Next Phase Readiness

**Wave 2 File 3 (index.ts, 10 sites) up next in Plan 15.3-10.**

Wave 2 remaining files:
1. ✅ **Wave 2 File 1 -- `helpers/finalizationHelpers.ts`** (Plan 08, 14 sites) -- 10 migrated + 1 P5 + 3 P4 preserved
2. ✅ **Wave 2 File 2 -- `reducers/server.ts`** (this plan, 13 sites) -- 12 migrated + 1 P4 preserved + 3 NO-TOUCH Pitfall 6 blocks preserved + auditInsert import evicted
3. **Wave 2 File 3 -- `src/index.ts`** (10 sites) -- client lifecycle hooks, LobbyMember P4 carry expected
4. **Wave 2 File 4 -- `reducers/costSetManagement.ts`** (18 sites) -- 3-table symmetric carry, may surface 2-3 more P5 conditional sites

**Wave 3 Option alpha accounting:**
- Confirmed P5 sites remain at 3 (rosterMutations L55 + rosterAdmin L160 + finalizationHelpers L148)
- server.ts server_set_mmr is if/else clean-split (2 row literals), NOT P5 -- does not add to conditional-site count
- costSetManagement.ts (Plan 11) is the last file that could surface more P5 sites; Wave 3 planner re-counts after Plan 11

**Blockers:** None. Pre-existing test-file typecheck errors (deferred-items.md Plan 01) remain orthogonal.

## Self-Check: PASSED

Verified all artifact claims:

- File `spacetimedb/src/reducers/server.ts` exists and modified (FOUND)
- Commit `426e960` exists in git log (FOUND -- Task 1 bootstrap migration)
- Commit `a4ee47e` exists in git log (FOUND -- Task 2 non-bootstrap migration)
- `insertWithAudit|updateWithAudit` count in file: 12 (meets plan acceptance >= 12) ✓
- `auditInsert` count in file: 0 (meets plan acceptance = 0) ✓
- `auditUpdate` count in file: 3 (1 import + 1 P4 call site + tokenization -- meets plan acceptance >= 1) ✓
- `auditInsert` symbol removed from import on L4 (FOUND -- import trimmed to `{ auditUpdate, SYSTEM_USER_ID }`)
- L279-314 server_set_datetime block structurally unchanged (VERIFIED -- three update blocks with custom `ts` on primary data field + manual `lastModifiedById: SYSTEM_USER_ID` + manual `lastModifiedDate: ctx.timestamp`, per Pitfall 6 PATTERNS.md C1)
- Fresh-DB smoke test SYSTEM user row: `id=1, username=SYSTEM, created_by_id=0, last_modified_by_id=0` (FOUND via `spacetime sql` spot-check on maincloud)
- Fresh-DB smoke test server_identity count: 1 (FOUND)
- Fresh-DB smoke test user_identity count: 1 (FOUND)
- `npm run test:typecheck` -- 0 errors in `spacetimedb/src` (PASS); 35 pre-existing errors in `test/backend/**` unchanged (baseline per deferred-items.md Plan 01)

---
*Phase: 15.3-audit-spread-type-helper*
*Completed: 2026-04-17*
*Wave 2 File 2 COMPLETE: 12 migrated + 1 P4 + 3 NO-TOUCH Pitfall 6 preserved + auditInsert import evicted; fresh-DB smoke test PASS on maincloud*
