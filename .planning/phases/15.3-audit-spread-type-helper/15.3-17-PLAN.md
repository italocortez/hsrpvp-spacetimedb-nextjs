---
phase: 15.3-audit-spread-type-helper
plan: 17
type: execute
wave: 4c
depends_on: ["15.3-15", "15.3-16"]
files_modified:
  - spacetimedb/src/reducers/lobbyLifecycle.ts
autonomous: true
requirements: [D-01, D-02]
tags: [refactor, typecheck, audit, backend, wave-4c, post-publish, trivial]

must_haves:
  truths:
    - "4 redundant ` as any` suffixes removed from lobbyLifecycle.ts L321, L324, L329, L354"
    - "Zero bundle change — pure TypeScript annotation removal, identical JavaScript output"
    - "npm run test:typecheck passes with 0 errors in spacetimedb/src"
    - "No re-publish needed — bundle already shipped in Plan 15 is semantically identical"
  artifacts:
    - path: "spacetimedb/src/reducers/lobbyLifecycle.ts"
      provides: "4 trailing `as any` eliminated from LobbyMemberAccount inserts; consistent with accountSelection.ts L81/L87 precedent (same table, same shape, no cast)"
      contains: "ctx.db.LobbyMemberAccount.insert\\(\\{ lobbyId, userId"
---

<objective>
Wave 4c post-phase-close trailing cleanup. 4 redundant `as any` casts on `LobbyMemberAccount.insert` calls in lobbyLifecycle.ts — erroneously classified CORRECT during Plan 16 triage ("LobbyMemberAccount has no audit columns → cast required by design"). That rationale was wrong. `accountSelection.ts:81,87` writes the identical 3-field shape on the same table WITHOUT `as any` and typechecks clean. The module_bindings entry exists at `types.ts:917`. The cast is pure legacy noise.

**Trivial scope:** Remove ` as any` from 4 lines. Zero other changes. Post-publish cleanup (Plan 15 already shipped); no re-publish needed because TypeScript annotations don't affect the emitted JavaScript bundle.

**Residual update:** 189 → 185 `as any` in spacetimedb/src (232 total eliminated from 417 baseline, up from 228).
</objective>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Remove 4 redundant as-any casts + verify + commit</name>
  <files>spacetimedb/src/reducers/lobbyLifecycle.ts</files>
  <action>
Edit 4 lines, replacing `} as any);` with `});` at exactly:
- L321: `ctx.db.LobbyMemberAccount.insert({ lobbyId, userId: user.id, hsrAccountId: activeAcct.id });`
- L324: `ctx.db.LobbyMemberAccount.insert({ lobbyId, userId: user.id, hsrAccountId: tpaEntries[0].hsrAccountId });`
- L329: `ctx.db.LobbyMemberAccount.insert({ lobbyId, userId: user.id, hsrAccountId: activeAcct.id });`
- L354: `ctx.db.LobbyMemberAccount.insert({ lobbyId, userId: user.id, hsrAccountId: activeAcct.id });`

Verify: `npm run test:typecheck` passes. Commit atomically.
  </action>
  <verify><automated>npm run test:typecheck</automated></verify>
  <acceptance_criteria>
    - `grep -c " as any" spacetimedb/src/reducers/lobbyLifecycle.ts` decreases by 4
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>4 redundant casts eliminated.</done>
</task>
</tasks>

<output>
Append a brief closing entry to PHASE-SUMMARY.md §Residual Accounting: "Plan 17 (2026-04-18): 4 more `as any` eliminated in lobbyLifecycle.ts LobbyMemberAccount inserts — misclassified CORRECT during Plan 16 triage, actually redundant (accountSelection.ts precedent). Post-publish trailing fix; no bundle change. Residual: 185."

Update STATE.md completed_plans and ROADMAP.md Phase 15.3 plan count 16/16 → 17/17.
</output>
