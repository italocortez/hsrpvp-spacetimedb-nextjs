---
phase: 15.3-audit-spread-type-helper
plan: 16
type: execute
wave: 4b
depends_on: ["15.3-07", "15.3-11", "15.3-12"]
files_modified:
  - spacetimedb/src/reducers/admin.ts
  - spacetimedb/src/reducers/bracketGeneration.ts
  - spacetimedb/src/reducers/draftAuction.ts
  - spacetimedb/src/reducers/draftClassic.ts
  - spacetimedb/src/reducers/draftControl.ts
  - spacetimedb/src/reducers/identityGc.ts
  - spacetimedb/src/reducers/lobbyGc.ts
  - spacetimedb/src/reducers/lobbySettings.ts
  - spacetimedb/src/reducers/matchResultSubmission.ts
  - spacetimedb/src/reducers/postDraft.ts
  - spacetimedb/src/reducers/profile.ts
  - spacetimedb/src/reducers/seriesManagement.ts
  - spacetimedb/src/reducers/tournamentLobby.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-17]
tags: [refactor, typecheck, audit, backend, wave-4b, real-miss-cleanup, pre-publish]

must_haves:
  truths:
    - "38 fixable `as any` sites across 13 files eliminated"
    - "30 P1 plain-insert sites migrated to `ctx.db.X.insert(insertWithAudit(ctx, {...}, userId))` — no outer closure"
    - "2 P2 direct-update sites in bracketGeneration migrated to `ctx.db.X.id.update(updateWithAudit(ctx, existing, {...}, userId))`"
    - "1 composite-PK delete in bracketGeneration.ts L152 migrated to row-object delete `ctx.db.GroupPhaseRecord.delete(gpr)` per bracketHelpers.ts L87/L90 precedent"
    - "1 mergeForUpdate internal self-cast in admin.ts L107 eliminated via K generic tightening already applied in Plan 12"
    - "4 narrow-cast or type-guard fixes on non-tagged-enum field accesses (draftAuction value accessor ×2, postDraft dict access, matchResultSubmission enum, tournamentLobby enum)"
    - "1 variable-type annotation on seriesManagement L104 initialTimerState"
    - "Tagged-enum `{tag: X, value: Y} as any` literals NOT modified this plan — out of scope per user direction"
    - "Zero P4/P5 preserved sites touched — all 28 preserved sites in costSetManagement / index / finalizationHelpers / characterStatsIncrement / server / statsIncrement / scoreEntry / rosterAdmin / rosterMutations / globalCharacterStatsIncrement / admin unchanged"
    - "Zero LobbyMemberAccount CORRECT sites touched — lobbyLifecycle L321/L324/L329/L354 ephemeral-join casts unchanged (table has no audit columns by design)"
    - "npm run test:typecheck passes — 0 errors in spacetimedb/src"
    - "npm run test:integration full suite passes (71 files)"
    - "Post-plan `as any` count in spacetimedb/src drops from 210 → ~172 (delta -38)"
  artifacts:
    - path: "spacetimedb/src/reducers/bracketGeneration.ts"
      provides: "7 sites fixed: 4 P1 inserts + 2 P2 updates + 1 composite-PK row-object delete"
      contains: "insertWithAudit|updateWithAudit|GroupPhaseRecord.delete\\(gpr\\)"
    - path: "spacetimedb/src/reducers/draftAuction.ts"
      provides: "7 sites fixed: 5 P1 inserts + 2 value-accessor narrow casts"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/draftClassic.ts"
      provides: "4 P1 inserts migrated"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/draftControl.ts"
      provides: "2 P1 inserts migrated"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/postDraft.ts"
      provides: "4 sites fixed: 3 P1 inserts + 1 dict-access narrow cast"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/seriesManagement.ts"
      provides: "4 sites fixed: 3 P1 inserts + 1 initialTimerState type annotation"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/identityGc.ts"
      provides: "2 P1 inserts migrated"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/lobbyGc.ts"
      provides: "2 P1 inserts migrated"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/lobbySettings.ts"
      provides: "1 P1 insert migrated"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/profile.ts"
      provides: "1 P1 insert migrated"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/matchResultSubmission.ts"
      provides: "2 sites fixed: P1 wrap on L50 + enum narrow cast on L173"
      contains: "insertWithAudit"
    - path: "spacetimedb/src/reducers/tournamentLobby.ts"
      provides: "1 enum narrow cast on matchType L124"
      contains: "matchType"
    - path: "spacetimedb/src/reducers/admin.ts"
      provides: "1 mergeForUpdate internal self-cast on L107 eliminated"
      contains: "mergeForUpdate"
  key_links:
    - from: "13 fixable files"
      to: "auditHelpers.ts"
      via: "import { insertWithAudit, updateWithAudit } from '../helpers/auditHelpers'"
      pattern: "insertWithAudit|updateWithAudit"
    - from: "bracketGeneration.ts L152"
      to: "GroupPhaseRecord row-object delete canon"
      via: "ctx.db.GroupPhaseRecord.delete(gpr) per bracketHelpers.ts L87/L90"
      pattern: "GroupPhaseRecord\\.delete\\("
---

<objective>
Wave 4b pre-publish cleanup. 38 genuinely fixable `as any` sites across 13 files — all audit-adjacent leftovers that Waves 1-2 cluster plans missed. Zero tagged-enum literals touched (user-directed OOS). Zero P4/P5/CORRECT preserved sites touched. Pure Wave 1 pattern extension to files Wave 1 didn't cover comprehensively.

**Background:** Plan 15 Task 1 (pre-publish gate) surfaced a 210-site `as any` residual vs the plan's ~133 target. Investigation (classification agent, 72 non-enum sites) produced a per-site table: 38 FIXABLE + 28 PRESERVED (P4/P5/Pitfall-6, architectural) + 4 CORRECT (LobbyMemberAccount no-audit ephemeral join by design). This plan closes the 38 FIXABLE bucket before Plan 15's publish, bringing the phase's genuine elimination count from ~207 to ~245 (pre-tagged-enum ceiling).

**Why these weren't done in Wave 1/2:** Mostly clustering accidents — these files sat in cluster boundaries (e.g., bracketGeneration.ts was scoped-out of Plan 06 Cluster F because only bracket-advancement audit-stamping was in scope; draft reducers had their tagged-enum density hide the P1 sites behind the enum-cast noise; GC reducers sat between clusters). No blocker surfaced during the clusters — the sites just weren't touched.

**Pattern reuse:** Every P1 insert follows the canonical form established in Wave 1 Cluster A (commits 5b09...c6):

```ts
// Before
ctx.db.X.insert({
    ...rowFields,
    ...auditInsert(ctx, user.id),
} as any);

// After
ctx.db.X.insert(insertWithAudit(ctx, {
    ...rowFields,
}, user.id));
```

The 2 P2 updates in bracketGeneration follow Wave 1 Cluster A P2 form:

```ts
// Before
ctx.db.X.id.update({
    ...existing,
    changedField: newValue,
    ...auditUpdate(ctx, existing, user.id),
} as any);

// After
ctx.db.X.id.update(updateWithAudit(ctx, existing, {
    changedField: newValue,
}, user.id));
```

The bracketGeneration L152 composite-PK delete follows the bracketHelpers.ts L87/L90 precedent (SAME TABLE, 50 lines away):

```ts
// Before
for (const gpr of [...ctx.db.GroupPhaseRecord.tournament_id.filter(tournamentId)]) {
    (ctx.db.GroupPhaseRecord as any).primaryKey.delete({
        tournamentId: gpr.tournamentId,
        groupId: gpr.groupId,
        teamId: gpr.teamId,
    });
}

// After
for (const gpr of [...ctx.db.GroupPhaseRecord.tournament_id.filter(tournamentId)]) {
    ctx.db.GroupPhaseRecord.delete(gpr);
}
```

The 4 narrow-cast / type-guard fixes replace `as any` with narrow typed casts or discriminated access — see task details.

Output: 13 files touched, 38 `as any` eliminated, typecheck + integration green, pre-publish gate reaches ~172 residual (target ~133 + ~39 held for tagged-enum phase coupling + architectural preserves).
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-audit-spread-type-helper/15.3-CONTEXT.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-01-SUMMARY.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-02-SUMMARY.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-04-SUMMARY.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-06-SUMMARY.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-07-SUMMARY.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-12-SUMMARY.md
@spacetimedb/src/helpers/auditHelpers.ts
@spacetimedb/src/helpers/bracketHelpers.ts

<skill-notice>
**MANDATORY:** Load `/spacetimedb` skill before modifying any backend file. CLAUDE.md rule.
</skill-notice>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure refactor. No new attack surface. No new exports. No reducer signatures changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.3-16-audit-drift | Tampering | 30 P1 inserts migrated to insertWithAudit | mitigate | insertWithAudit is the same canonical helper used by 50+ files already shipped in prior plans. Runtime semantics identical to `{...row, ...auditInsert(ctx, uid)}`. Cluster integration tests in Plans 05-07 already exercise this helper against every reducer subsystem. |
| T-15.3-16-composite-delete-correctness | Tampering | bracketGeneration.ts L152 composite-PK delete migration | mitigate | Switching from `(ctx.db.X as any).primaryKey.delete({...})` to `ctx.db.X.delete(row)` uses the SDK's typed row-object delete, identical semantics. Same pattern already in use in bracketHelpers.ts L87/L90 on same table. Generate_bracket integration test (test/backend/brackets/bracket-generation.test.ts) exercises the regeneration path end-to-end. |
| T-15.3-16-narrow-cast-correctness | Tampering | 4 narrow-cast fixes (payload accessors, enum casts) | mitigate | Each narrow cast is strictly tighter than `as any` — TypeScript verifies the target type matches. Per-task `npm run test:typecheck` gate catches any tag/value incompatibility. |

Severity LOW — same runtime behavior, tighter types, established-helper reuse.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bracket family (bracketGeneration.ts — 7 sites)</name>
  <files>spacetimedb/src/reducers/bracketGeneration.ts</files>
  <read_first>
    spacetimedb/src/reducers/bracketGeneration.ts,
    spacetimedb/src/helpers/bracketHelpers.ts,
    spacetimedb/src/helpers/auditHelpers.ts
  </read_first>
  <action>
**Step 1 — Add imports** (if not already present):
```ts
import { insertWithAudit, updateWithAudit } from '../helpers/auditHelpers';
```

**Step 2 — Migrate the 4 P1 plain inserts at L73, L96, L103, L317:**

For each: replace `ctx.db.X.insert({...row, ...auditInsert(ctx, userId)} as any);` with `ctx.db.X.insert(insertWithAudit(ctx, {...row}, userId));`. Do NOT rewrite the row literal — preserve field order and shape.

**Step 3 — Migrate the 2 P2 direct updates at L358, L365:**

For each: replace `ctx.db.X.id.update({...existing, field: val, ...auditUpdate(ctx, existing, userId)} as any);` with `ctx.db.X.id.update(updateWithAudit(ctx, existing, {field: val}, userId));`. Only the CHANGED fields go into the partial — drop the `...existing` spread.

**Step 4 — Migrate composite-PK delete at L152:**

```ts
// BEFORE (lines ~151-157)
for (const gpr of [...ctx.db.GroupPhaseRecord.tournament_id.filter(tournamentId)]) {
    (ctx.db.GroupPhaseRecord as any).primaryKey.delete({
        tournamentId: gpr.tournamentId,
        groupId: gpr.groupId,
        teamId: gpr.teamId,
    });
}

// AFTER
for (const gpr of [...ctx.db.GroupPhaseRecord.tournament_id.filter(tournamentId)]) {
    ctx.db.GroupPhaseRecord.delete(gpr);
}
```

**Step 5 — Drop `auditInsert` / `auditUpdate` imports from this file IF no other callers remain** (grep `auditInsert\|auditUpdate` inside this file — if zero, remove from import list).
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c " as any" spacetimedb/src/reducers/bracketGeneration.ts` decreases by 7
    - `grep -c "insertWithAudit\|updateWithAudit" spacetimedb/src/reducers/bracketGeneration.ts` >= 6
    - `grep -c "GroupPhaseRecord.delete(gpr)" spacetimedb/src/reducers/bracketGeneration.ts` >= 1
    - `npm run test:typecheck` exits 0 (src has 0 errors)
  </acceptance_criteria>
  <done>bracketGeneration.ts 7 sites migrated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Draft family (draftAuction.ts 7 + draftClassic.ts 4 + draftControl.ts 2 = 13 sites)</name>
  <files>
    spacetimedb/src/reducers/draftAuction.ts,
    spacetimedb/src/reducers/draftClassic.ts,
    spacetimedb/src/reducers/draftControl.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/draftAuction.ts,
    spacetimedb/src/reducers/draftClassic.ts,
    spacetimedb/src/reducers/draftControl.ts,
    spacetimedb/src/module_bindings/types.ts
  </read_first>
  <action>
**For each of the 11 P1 plain inserts** (draftAuction L192, L315, L444, L615, L678; draftClassic L375, L532, L699, L754; draftControl L72, L188):

Apply the Task 1 Step 2 transform. Keep all tagged-enum `{tag: X, value: Y} as any` INNER field casts INSIDE the row literal UNCHANGED — only eliminate the OUTER `} as any)` by using `insertWithAudit`. The inner tagged-enum casts are explicitly out-of-scope.

**For the 2 value-accessor narrow casts at draftAuction L419 and L657:**

```ts
// BEFORE
const eidolon: number = nominateStep ? (nominateStep.payload.value as any).eidolon : 0;

// AFTER — discriminated tag check
const eidolon: number = (nominateStep?.payload.tag === 'Nominate')
    ? nominateStep.payload.value.eidolon
    : 0;
```

TypeScript narrows `payload.value` to the `Nominate` branch automatically once the `tag` equality check is proven. If the narrowing fails (e.g., the union branch names differ), fall back to importing the payload type and using a narrow cast: `(nominateStep.payload.value as NominatePayload).eidolon`.

**Add `insertWithAudit` imports** to all 3 files if not already present.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - draftAuction.ts `as any` count drops by 7
    - draftClassic.ts `as any` count drops by 4
    - draftControl.ts `as any` count drops by 2
    - No tagged-enum `{tag, value} as any` literals modified (they stay)
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>Draft family 13 sites migrated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: GC + lifecycle family (identityGc.ts 2 + lobbyGc.ts 2 + lobbySettings.ts 1 = 5 sites)</name>
  <files>
    spacetimedb/src/reducers/identityGc.ts,
    spacetimedb/src/reducers/lobbyGc.ts,
    spacetimedb/src/reducers/lobbySettings.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/identityGc.ts,
    spacetimedb/src/reducers/lobbyGc.ts,
    spacetimedb/src/reducers/lobbySettings.ts
  </read_first>
  <action>
Apply Task 1 Step 2 P1 transform to:
- identityGc.ts L114, L170
- lobbyGc.ts L182, L230
- lobbySettings.ts L151

Add `insertWithAudit` imports if missing.

These are scheduled-reducer paths (GC) — actor is typically SYSTEM_USER_ID or the user being evicted. Preserve the existing actor argument verbatim. Do NOT substitute SYSTEM_USER_ID if the caller passed a specific actor.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 5 sites migrated across 3 files
    - `npm run test:typecheck` exits 0
    - Existing GC integration tests (test/backend/auth/identity-gc-job.test.ts, test/backend/lobby/lobby-gc.test.ts) still pass
  </acceptance_criteria>
  <done>GC + lifecycle 5 sites migrated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Series / profile / post-draft family (seriesManagement.ts 4 + profile.ts 1 + postDraft.ts 4 = 9 sites)</name>
  <files>
    spacetimedb/src/reducers/seriesManagement.ts,
    spacetimedb/src/reducers/profile.ts,
    spacetimedb/src/reducers/postDraft.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/seriesManagement.ts,
    spacetimedb/src/reducers/profile.ts,
    spacetimedb/src/reducers/postDraft.ts,
    spacetimedb/src/module_bindings/types.ts
  </read_first>
  <action>
**P1 inserts (7 sites)** — apply Task 1 Step 2:
- seriesManagement L134, L183, L252
- profile L81
- postDraft L102, L186, L247

**seriesManagement L104 variable-type annotation:**

Read the surrounding context (likely an object literal assigned to a `let initialTimerState` declaration). The right-hand side is a TimerState-shaped literal. Change `as any` to a narrow annotation on the LHS or a narrow cast on the RHS using the TimerState type from module_bindings:

```ts
// BEFORE
const timerState = { /* ... */ } as any;

// AFTER
import type { TimerState } from '../module_bindings/types';  // adjust path if needed
const timerState: TimerState = { /* ... */ };
```

If TimerState is a tagged union or contains tagged-enum sub-fields, use narrow cast `as TimerState` instead of direct annotation.

**postDraft L70 dict-access narrow cast:**

```ts
// BEFORE
const rawCost = (costRow.costs as any)[sField];

// AFTER
const rawCost = (costRow.costs as Record<string, number>)[sField];
```

Or if `costs` has a concrete struct type from module_bindings, import it and narrow accordingly.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 9 sites migrated across 3 files
    - `initialTimerState` no longer uses `as any`
    - `costRow.costs` access no longer uses `as any`
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>Series / profile / post-draft 9 sites migrated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Narrow-cast trifecta (admin.ts L107 + matchResultSubmission.ts 2 + tournamentLobby.ts L124 = 4 sites)</name>
  <files>
    spacetimedb/src/reducers/admin.ts,
    spacetimedb/src/reducers/matchResultSubmission.ts,
    spacetimedb/src/reducers/tournamentLobby.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/admin.ts,
    spacetimedb/src/reducers/matchResultSubmission.ts,
    spacetimedb/src/reducers/tournamentLobby.ts,
    spacetimedb/src/module_bindings/types.ts
  </read_first>
  <action>
**admin.ts L107 — mergeForUpdate internal self-cast:**

Plan 12 tightened the generic signature to `<T, K extends keyof T & string>(existing: T, incoming: Partial<Record<K, unknown>>, fields: readonly K[]): T`. Inside the function body (around L107), an internal cast `(merged as any)[f] = incoming[f];` may still exist. With the tightened K generic, this should become `(merged as Record<string, unknown>)[f] = incoming[f];` or simply `merged[f] = incoming[f] as T[K];` — whichever preserves the assignment without `as any`.

Read L105-110 context to pick the narrow form. Priority: eliminate `as any`.

**matchResultSubmission.ts L50 — P1 insert-with-audit partial wrap:**

```ts
// BEFORE (around L48-50)
ctx.db.MatchResultRecord.id.update(updateWithAudit(ctx, matchResult, {
    [sideFlag]: true,
} as any, user.id));
```

The `} as any` is on the partial-update object. TypeScript's strict index signature check rejects `[sideFlag]: true` against the typed partial. Fix options:
- (a) Cast the computed-key object: `{ [sideFlag]: true } as Partial<MatchResultRecord>` — narrow, safe.
- (b) Use a typed intermediate: `const patch: Partial<MatchResultRecord> = { [sideFlag]: true };` then pass `patch`.

Pick option (a) for minimal diff.

**matchResultSubmission.ts L173 — enum narrow cast:**

```ts
// BEFORE
status: newStatus as any,
```

`newStatus` is computed from control flow earlier (likely `'Confirmed'` / `'Disputed'` / etc). Replace `as any` with `as MatchResultRecord['status']` (narrow cast to the field's declared type). If `newStatus` is already typed correctly, drop the cast entirely.

**tournamentLobby.ts L124 — matchType enum narrow cast:**

```ts
// BEFORE
matchType: matchType as any,
```

`matchType` is a value derived from tournament (likely tagged-enum or string literal union). Cast narrowly to the lobby row's `matchType` column type: `matchType as Lobby['matchType']` or the specific tagged-union type imported from module_bindings.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 4 sites fixed with narrow casts or type annotations
    - Zero new `as any` introduced
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>Narrow-cast trifecta closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Final verification gate</name>
  <files></files>
  <read_first>
    spacetimedb/src/helpers/auditColumns.ts
  </read_first>
  <action>
**Step 1 — Grep delta confirmation:**
```bash
grep -rn "as any" spacetimedb/src --include="*.ts" | wc -l
```
Expected: 172 ± 3 (from 210 baseline, -38 eliminations). If significantly higher, audit which sites the per-task diffs failed to cover.

**Step 2 — auditInsert caller inventory unchanged:**
```bash
grep -rn "auditInsert(" spacetimedb/src --include="*.ts" | grep -v "auditColumns.ts" | grep -v "auditHelpers.ts" | wc -l
```
Expected: exactly 9 (per Plan 13 Option α canon). If the count changed, Plan 16 touched a preserved site by accident — STOP and revert.

**Step 3 — Typecheck:**
```bash
npm run test:typecheck
```
Expected: 0 errors in spacetimedb/src. 35 pre-existing baseline errors in test/backend/** unchanged.

**Step 4 — Full integration suite:**
```bash
npm run test:integration
```
Expected: all 71 files green. Plan 15's Task 1 surfaced 1 flake on `achievement-auto-award.test.ts` under full-suite load (isolated re-run passed in 52s). If that specific test times out again, re-run it in isolation to confirm flake vs regression.

If full suite passes cleanly, this plan + pre-publish gate are both green and Plan 15 can proceed to publish.
  </action>
  <verify>
    <automated>npm run test:typecheck &amp;&amp; npm run test:integration</automated>
  </verify>
  <acceptance_criteria>
    - as-any count: 172 ± 3
    - auditInsert non-helper callers: exactly 9 (Plan 13 Option α unchanged)
    - Typecheck: 0 src errors + 35 test/backend baseline
    - Integration: 71/71 files pass (or 70/71 if achievement-auto-award times out — acceptable if isolated re-run passes)
  </acceptance_criteria>
  <done>Plan 16 gate confirmed. Pre-publish gate now clean. Plan 15 publish unblocked.</done>
</task>

</tasks>

<verification>
- 38 `as any` eliminated, no preserved sites touched
- Full test/backend integration suite green (or achievement-auto-award isolated-re-run workaround)
- `as any` baseline in spacetimedb/src drops 210 → ~172
</verification>

<success_criteria>
- Pre-publish gate now clean pending Plan 15 Task 1 re-run
- Wave 4b closes the last fixable-without-SDK-work residual
- Residual 172 sites = 138 tagged-enum literals (OOS) + 28 PRESERVED P4/P5/Pitfall-6 (architectural) + 4 CORRECT LobbyMemberAccount (by-design) + ~2 SDK-type-handle gaps (unrelated to audit spread)
</success_criteria>

<output>
After completion, create `.planning/phases/15.3-audit-spread-type-helper/15.3-16-SUMMARY.md`. Document: (a) the 38-site grep delta, (b) per-file site counts confirmed, (c) any Rule 2 auto-fixes surfaced during the narrow-cast tasks (structural bugs hidden by prior `as any`), (d) integration suite result (or isolated re-run evidence if achievement-auto-award flaked again).
</output>
