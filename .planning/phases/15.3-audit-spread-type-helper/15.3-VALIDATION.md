---
phase: 15.3
slug: audit-spread-type-helper
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-04-17
---

# Phase 15.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Phase 15.3 is a pure-refactor zero-behavior-change phase; primary validation signal is `tsc --noEmit` passing after every wave, with integration tests as the secondary signal.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.x (integration config at `test/vitest.integration.config.ts`) |
| **Config file** | `test/vitest.integration.config.ts` |
| **Quick run command** | `npm run test:typecheck` (zero-runtime; ~10-20s) |
| **Full suite command** | `npm run test:integration` (71 files across all backend subsystems) |
| **Estimated runtime** | ~10-20s typecheck; ~5-10 min full integration suite |

---

## Sampling Rate

- **After every task commit:** `npm run test:typecheck` MUST pass (primary refactor signal)
- **After every plan wave:** `npm run test:typecheck` + per-subsystem vitest run (see mapping in RESEARCH.md §Sampling Rate)
- **Before `/gsd-verify-work`:** Full `npm run test:integration` green + zero regression on `grep -rn "as any" spacetimedb/src --include="*.ts" | wc -l` baseline
- **Max feedback latency:** 20s (typecheck) for per-task; 10min (full integration) for phase gate

---

## Per-Task Verification Map

*Populated by `/gsd-plan-phase 15.3` during planning. Template row shown for planner reference; planner fills actual task IDs per cluster.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.3-W1-A-01 | W1-cluster-A | 1 | D-04, D-07 | — | Audit stamping semantics preserved across calendar/chat subsystem | typecheck + unit | `npm run test:typecheck && npx vitest run --config test/vitest.integration.config.ts --dir test/backend/calendar` | ✅ | ⬜ pending |
| 15.3-W2-F1-01 | W2-finalizationHelpers | 2 | D-06, D-14 | T-15.3-audit-trail-corruption | `createdBy/Date` carries via primitive in MmrRating delete+insert; conditional retains both primitives | typecheck + integration | `npm run test:typecheck && npx vitest run --config test/vitest.integration.config.ts --dir test/backend/match-results --dir test/backend/match-session` | ✅ | ⬜ pending |
| 15.3-W2-F2-smoke | W2-server | 2 | Wave 2 File 2 bootstrap | — | SYSTEM user inserted with `created_by_id=0` on fresh DB | smoke + SQL | `tmp/smoke-server-refactor.ts` then `spacetime sql "SELECT id, username, created_by_id FROM user WHERE username='SYSTEM'"` → 1 row | Manual (recipe in RESEARCH §Wave 2 File 2) | ⬜ pending |
| 15.3-W2B-D17 | W2B-typecheck-hygiene | 2b | D-17 | — | `mergeForUpdate` typed signature + `as const` FIELDS; 10 `as any` eliminated from admin.ts | typecheck + grep | `npm run test:typecheck && [ $(grep -c " as any" spacetimedb/src/reducers/admin.ts) -le 1 ]` | ✅ | ⬜ pending |
| 15.3-W2B-D18 | W2B-typecheck-hygiene | 2b | D-18 | — | Build-then-insert variable types replace `let x: any`; 7 `as any` eliminated | typecheck + grep | Per-file grep decrement matches RESEARCH §D-18 target counts | ✅ | ⬜ pending |
| 15.3-W3-deletion | W3-auditInsert-removal | 3 | D-06 (Option α/β resolution) | — | If Option β: zero remaining `auditInsert` imports across repo; if Option α: no-op (primitive kept internal) | grep | `! grep -rn "auditInsert" spacetimedb/src --include="*.ts" \| grep -v auditColumns.ts \| grep -v auditHelpers.ts` | ✅ | ⬜ pending |
| 15.3-W4-placeholder | W4-placeholder-tests | 4 | D-11, D-12 | — | All 8 `expect(true).toBe(true)` sites converted to `it.todo(...)` with meaningful descriptions | grep | `[ $(grep -rn "expect(true).toBe(true)" test/backend --include="*.test.ts" \| wc -l) -eq 0 ]` | ✅ | ⬜ pending |
| 15.3-W5-publish | W5-publish-verify | 5 | Phase gate | — | Maincloud publish succeeds; full integration suite green against deployed DB | integration | `npm run test:integration` (post-publish) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Phase 15.3 Wave 0 is **already complete** (committed `4f1dad6`). No additional Wave 0 test-infrastructure work needed:

- [x] `spacetimedb/src/helpers/auditHelpers.ts` — the two helpers, validated across all 4 pattern variants
- [x] Type check green after Wave 0 migration
- [x] Pattern inventory captured in CONTEXT.md D-13/D-14/D-15/D-16

*Existing infrastructure covers all phase requirements — 71 integration test files under `test/backend/` exercise every reducer subsystem. No new test framework, config, or fixtures required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Fresh-DB bootstrap with SYSTEM user insert | Wave 2 File 2 (server.ts) post-migration | Bootstrap runs ONCE per DB reset; automated test would have to wipe + re-publish to exercise | 1. `spacetime publish --clear-database <db-name>` (local dev) 2. `tsx spacetimedb/post-publish.ts` 3. `spacetime sql "<db>" "SELECT id, username, created_by_id, created_date FROM user WHERE username='SYSTEM'"` 4. Assert: 1 row, `created_by_id=0`, `created_date` recent |
| Maincloud publish succeeds | Wave 5 | External deploy action — cross-boundary, not reversible | 1. `spacetime publish <maincloud-db>` 2. Wait for build+deploy to finish 3. `spacetime sql <maincloud-db> "SELECT count(*) FROM user"` — confirms reducers callable post-deploy |
| Audit-field invariant spot-check | D-06 (composite-PK carry) | Automated integration tests exercise happy paths; manual spot-check on one migrated carry site catches semantic drift | After any Wave 1/2 plan touching a delete+insert-carry site: `spacetime sql` before/after an edit reducer, confirm `created_by_id` + `created_date` unchanged, `last_modified_by_id` + `last_modified_date` updated |

*For all other Phase 15.3 behaviors, `npm run test:integration` provides automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify (typecheck + per-subsystem vitest) or Wave 0 dependencies met
- [ ] Sampling continuity: no 3 consecutive tasks without typecheck run (per-commit typecheck enforced by Autonomous Execution policy)
- [ ] Wave 0 covers all MISSING references (N/A — Wave 0 already shipped, zero gaps)
- [ ] No watch-mode flags in any `<automated>` command (`vitest run` not `vitest`)
- [ ] Feedback latency < 20s for per-task typecheck; < 10min for full-suite phase gate
- [ ] `nyquist_compliant: true` set in frontmatter when planner has populated the per-task rows

**Approval:** pending — populated during `/gsd-plan-phase 15.3` then signed off at phase close.
