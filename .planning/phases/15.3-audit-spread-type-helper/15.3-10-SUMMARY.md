---
phase: 15.3-audit-spread-type-helper
plan: 10
subsystem: backend
tags: [refactor, typecheck, audit, spacetimedb, client-lifecycle, auth-adjacent, wave-2-file-3, p4-carry, rule-2-auto-fix, latent-bug-surfacing]

# Dependency graph
requires:
  - phase: 15.3-audit-spread-type-helper (Plan 09 -- Wave 2 File 2 server.ts COMPLETE)
    provides: auditInsert-import-eviction precedent; bootstrap/auth-adjacent file migration template; three-commit-split pattern for critical-path files
  - phase: 15.3-audit-spread-type-helper (Wave 0)
    provides: auditHelpers.ts with insertWithAudit/updateWithAudit; admin.ts canonical P1/P2/P4 analogs; SYSTEM_USER_ID sentinel convention
provides:
  - Wave 2 File 3 (src/index.ts) migrated to two-tier audit API -- CLIENT LIFECYCLE HOOKS on new helpers
  - clientConnected cluster (4 sites): User isOnline=true update (P2), UserIdentity lastSeenAt hygiene update (hygiene migration, NOT Pitfall 6), ban-on-reconnect User soft-delete update (P2), UserDeletionJob schedule insert (P1)
  - clientDisconnected cluster (3 P1/P2 sites migrated + 3 P4 carry preserved): User isOnline=false update (P2), MatchSessionStep auto-pause insert (P1), MatchSession timerState.isPaused update (P2)
  - 3 LobbyMember composite-PK delete+insert-carry sites PRESERVED verbatim on auditUpdate primitive at L181 (Waiting/AwaitingResult/Finished), L192 (NoAction policy), L203 (Standard/Deferred)
  - 2 Rule 2 auto-fixes: MatchSessionStep auto-pause insert was missing `gameNumber` field AND had wrong enum type on `actorSlot` (LobbySlot vs TeamSide); both hidden by outer `as any)` closure
  - auditInsert IMPORT REMOVED from index.ts (zero callers remain); auditUpdate retained for 3 LobbyMember P4 carry sites -- second Wave 2 file to fully evict auditInsert import after server.ts Plan 09
  - auth 34/34 + garbage-collector 8/8 + lobby 103/103 integration tests green (145 total tests across 16 test files)
affects: [15.3-11 (Wave 2 File 4 -- costSetManagement.ts 18 sites, last Wave 2 file, may surface 2-3 more P5 conditional sites); 15.3-13 (Wave 3 auditInsert deletion Option alpha -- index.ts now has zero auditInsert callers like server.ts; only Plan 08 finalizationHelpers + Plan 11 costSetManagement still need to be checked before deletion)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "P1 scheduled-time insert migration -- UserDeletionJob (ScheduleAt.time payload). Same pattern as admin.ts L182 analog; BigInt micros addition works cleanly inside insertWithAudit row literal."
    - "P2 client-lifecycle update migration -- User.isOnline flag flip (true on connect, false on disconnect), User.deletedAt soft-delete on ban-reconnect, MatchSession.timerState auto-pause. All three migrated mechanically via updateWithAudit(ctx, existing, { <change-set> }, userId)."
    - "Hygiene migration -- UserIdentity lastSeenAt manual field-write pattern (line-matches the 'manual audit spread' pattern but uses ctx.timestamp, NOT custom `ts` variable). Distinct from Pitfall 6: updateWithAudit preserves semantics because lastModifiedDate was already ctx.timestamp. Verified post-migration: createdBy/Date carries from existing, lastModifiedById=user.id, lastModifiedDate=ctx.timestamp. Plan-level hygiene discovery (NOT one of the 385 grep-verified audit-spread sites) surfaced during Wave 2 File 3 analysis."
    - "P4 delete+insert-carry PRESERVED on auditUpdate primitive for 3 LobbyMember sites (L181 + L192 + L203). All three are `LobbyMember.by_lobby_and_user.delete([lobbyId, userId])` followed by explicit `.insert({...member, isOnline: false, ...auditUpdate(ctx, member, SYSTEM_USER_ID)} as any)` with optional `disconnectedAt: ctx.timestamp` on L203. Composite-PK (lobbyId, userId) forces delete+insert instead of update; auditUpdate trailing position carries createdBy/createdDate from the deleted row. Cannot migrate to insertWithAudit (would reset createdBy/Date) or updateWithAudit (no merge-into-existing -- caller builds explicit row)."
    - "Rule 2 auto-fix #1 -- MatchSessionStep missing `gameNumber` field. Outer `} as any)` closure was hiding TS2345 'Property gameNumber is missing' error. Fixed by adding `gameNumber: session.currentGameNumber` matching draftControl.ts L63/L179 canonical pattern. Plan 07 Cluster G caught the identical gap 2x in draftControl.ts -- third confirmed instance of the same latent bug pattern."
    - "Rule 2 auto-fix #2 -- MatchSessionStep actorSlot enum-type mismatch. Code passed `member.lobbySlot` (LobbySlot enum: BluePlayer/BlueCoach/RedPlayer/RedCoach/Spectator) but schema requires actorSlot: TeamSide (Blue/Red/Spectator only). Outer `as any)` suppressed the TS2345 incompatibility. Fixed with `slotToTeamSide(member.lobbySlot)` -- canonical conversion used by every other MatchSessionStep.insert caller in the codebase (draftControl, draftAuction, draftClassic, postDraft). Root cause: auto-pause path was the only MatchSessionStep writer not already using slotToTeamSide."
    - "Client-lifecycle criticality preservation -- hooks run on every socket connection. All 7 non-P4 sites migrated mechanically with no branch-structure changes. clientConnected re-point flow (User.isOnline + UserIdentity.lastSeenAt + ban-check + UserDeletionJob) and clientDisconnected flow (User.isOnline + LobbyMember cleanup + auto-pause) preserved verbatim. Auth-adjacent behavior unchanged."

key-files:
  created:
    - ".planning/phases/15.3-audit-spread-type-helper/15.3-10-SUMMARY.md"
  modified:
    - "spacetimedb/src/index.ts -- 7 audit sites migrated to two-tier API (4 updateWithAudit + 2 insertWithAudit in clientConnected; 2 updateWithAudit + 1 insertWithAudit in clientDisconnected), 3 LobbyMember P4 carry sites preserved on auditUpdate primitive (L181/L192/L203), 2 Rule 2 auto-fixes (gameNumber field + actorSlot enum conversion). Imports: added `insertWithAudit, updateWithAudit` from auditHelpers; added `slotToTeamSide` from lobbyHelpers (Rule 2 actorSlot fix); REMOVED auditInsert from auditColumns import (zero callers). auditUpdate retained (3 LobbyMember carry sites)."

key-decisions:
  - "D-10-01: Two-commit split (clientConnected + clientDisconnected-with-Rule-2-fixes). Task 1 migrates 4 clientConnected sites to two-tier API (commit e0b9f77). Task 2 migrates 3 clientDisconnected P1/P2 sites to two-tier API, preserves 3 LobbyMember P4 carry sites, AND applies 2 Rule 2 auto-fixes on the MatchSessionStep auto-pause insert (commit 4bc39ec). This SUMMARY is the third commit. Rationale: clientConnected and clientDisconnected are the two logical hooks; Rule 2 fixes belong with the Task 2 migration because they were surfaced BY removing the `as any` closure during Task 2."
  - "D-10-02: UserIdentity.lastSeenAt migration NOT Pitfall 6. Original code paired `lastSeenAt: ctx.timestamp, lastModifiedById: user.id, lastModifiedDate: ctx.timestamp` as manual field-writes (looks like audit spread). Distinguished from server.ts L279-314 (Pitfall 6 / PATTERNS.md C1) because there is NO custom `ts` variable -- both lastSeenAt and lastModifiedDate already use `ctx.timestamp`. Migration via `updateWithAudit(ctx, mapping, { lastSeenAt: ctx.timestamp }, user.id)` preserves semantics exactly: createdBy/Date carry from existing, lastSeenAt=ctx.timestamp (passed as a change), lastModifiedById=user.id (passed as actingUserId), lastModifiedDate=ctx.timestamp (helper default). Plan explicitly flagged this as a hygiene-migration site not appearing in the 385 grep count -- plan-authored discovery during Wave 2 File 3 analysis."
  - "D-10-03: Rule 2 auto-fix #1 -- MatchSessionStep.gameNumber missing field. TS2345 error surfaced after removing outer `} as any)` closure during Task 2 migration. Schema requires gameNumber: t.u8() (per matchSessionStep.ts L9 -- 1-indexed game number within the series per D-13 to prevent PK conflicts across games). Fixed by adding `gameNumber: session.currentGameNumber` -- canonical pattern from draftControl.ts L63 (undo_last_step pause step) and L179 (pause_draft step). Third confirmed occurrence of the same latent bug pattern across the codebase (Plan 07 Cluster G caught it 2x in draftControl.ts). Validates D-03 thesis that Phase 15.3 surfaces real latent correctness gaps, not just cosmetic as-any removal. Behavioral impact: auto-pause steps before this fix would have inserted with gameNumber=0 (or whatever TypeScript inferred -- likely undefined -> runtime error on PK conflict check); fix routes correctly to session.currentGameNumber."
  - "D-10-04: Rule 2 auto-fix #2 -- MatchSessionStep.actorSlot enum-type mismatch. TS2345 error surfaced after gameNumber fix: `actorSlot: member.lobbySlot` assigned a `LobbySlot` enum (BluePlayer/BlueCoach/RedPlayer/RedCoach/Spectator) to a field typed as `TeamSide` (Blue/Red/Spectator). Fixed with `actorSlot: slotToTeamSide(member.lobbySlot)` -- the canonical helper used by EVERY OTHER MatchSessionStep.insert caller in the codebase (draftControl L67/L183, draftAuction L187/L310/L434, draftClassic L370/L527, postDraft L97/L181/L242). Root cause: auto-pause writer in index.ts was the only MatchSessionStep inserter that hadn't adopted slotToTeamSide. Pre-migration behavior: outer `as any)` closure suppressed the type error, allowing incorrect LobbySlot value to serialize into the MatchSessionStep row -- downstream readers expecting Blue/Red/Spectator would have seen BluePlayer/BlueCoach/etc. tag values, potentially breaking team-attribution logic. Fix aligns auto-pause path with the rest of the codebase."
  - "D-10-05: 3 LobbyMember carry sites preserved on auditUpdate primitive per Pitfall 1 (PATTERNS.md P4). All three sites follow the same shape: `ctx.db.LobbyMember.by_lobby_and_user.delete([member.lobbyId, userId])` then `ctx.db.LobbyMember.insert({ ...member, <changes>, ...auditUpdate(ctx, member, SYSTEM_USER_ID) } as any)`. The trailing `as any` is kept because `LobbyMember.insert` has tagged-enum fields on other properties (not migrated since out-of-scope per Phase 15.3 decisions). Migration to insertWithAudit would reset createdBy/createdDate (cannot use); migration to updateWithAudit doesn't apply (no merge-into-existing pattern -- caller builds explicit row after delete). Primitive stays. Running total P4 carry sites preserved across Phase 15.3: 12 (2 Wave 0 admin.ts + 6 Plan 03 stats helpers + 1 Plan 06 scoreEntry MatchResultGame + 3 Plan 08 finalizationHelpers MmrRating + 1 Plan 09 server.ts MmrRating + 3 Plan 10 index.ts LobbyMember = 16 total wait let me recount -- actually 16 per running tally). Note: recount in Wave 3 plan."
  - "D-10-06: auditInsert import REMOVED from index.ts -- second Wave 2 file to fully evict after server.ts Plan 09. Pre-migration had 2 auditInsert callers (UserDeletionJob L151 + MatchSessionStep L228); post-migration has zero. Import trimmed from `{ auditInsert, auditUpdate, SYSTEM_USER_ID }` to `{ auditUpdate, SYSTEM_USER_ID }`. Wave 3 Option alpha accounting: 3 confirmed P5 sites remain unchanged (rosterMutations L55 + rosterAdmin L160 + finalizationHelpers L148). Plan 11 costSetManagement.ts is the last file that could surface more P5 sites."
  - "D-10-07: Integration tests green -- auth 34/34 (138s) + garbage-collector 8/8 (73s) + lobby 103/103 (622s) = 145 tests across 16 test files. Lobby suite specifically exercises LobbyMember disconnect paths (P4 carry sites) via disconnect-gc, disconnect-leave-lobby, disconnect-admin-tools, disconnect-concede; all green confirms the 3 preserved sites behave identically to pre-migration. Auth suite exercises clientConnected (login, ban-on-reconnect via ban-admin.test.ts, user-deletion scheduling); all green confirms clientConnected cluster behavior unchanged. GC suite exercises UserDeletionJob scheduling via identity-gc path. Zero regression across 145 integration tests."

patterns-established:
  - "Client-lifecycle-hook migration template -- index.ts is the only file in the codebase with clientConnected/clientDisconnected handlers. Migration pattern: (1) mechanical P1/P2 substitution inside connected/disconnected hooks, (2) preserve P4 LobbyMember composite-PK carry sites verbatim, (3) apply Rule 2 fixes that surface when the outer `as any)` closure is removed. Full typecheck + auth + GC + lobby integration tests form the verification triad."
  - "Hygiene-migration vs audit-spread-migration distinction -- UserIdentity.lastSeenAt in clientConnected was manual field-writes, not an auditInsert/auditUpdate spread. Plan surfaced it for migration ANYWAY because updateWithAudit preserves semantics exactly (no custom ts override). Distinguishable from Pitfall 6 by: (1) no `ts` variable in scope, (2) both the primary data field and lastModifiedDate use ctx.timestamp already. Future plans: audit spread migration != every manual lastModifiedBy/Date write; distinguishing criterion is whether a custom timestamp argument is paired with the manual audit write."
  - "Rule 2 auto-fix surfacing during auth-adjacent migration -- contradicts Plan 02 (Cluster B auth-adjacent) and Plan 09 (server.ts bootstrap) 'zero Rule 2 fixes' hypothesis. Plan 10 discovered 2 latent bugs (gameNumber + actorSlot) on the clientDisconnected auto-pause path. Refined hypothesis: auth-adjacent code is carefully written AROUND the authentication state machine (user lookups, mapping validation, ban checks) but UNrelated-to-auth side effects (MatchSessionStep insert, LobbyMember cleanup) can harbor latent gaps. Lesson: `as any` closures on tables with many required fields (MatchSessionStep has 10 fields) are statistically likely to hide schema-drift gaps regardless of file criticality."

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16]

# Metrics
duration: 35m
completed: 2026-04-17
---

# Phase 15.3 Plan 10: Wave 2 File 3 (src/index.ts) Summary

**7 audit call sites migrated + 3 LobbyMember P4 composite-PK delete+insert-carry sites preserved verbatim in the client-lifecycle hook file. clientConnected cluster (4 sites): User isOnline=true, UserIdentity lastSeenAt hygiene migration (NOT Pitfall 6), ban-on-reconnect User soft-delete, UserDeletionJob schedule. clientDisconnected cluster (3 sites migrated): User isOnline=false, MatchSessionStep auto-pause insert, MatchSession timerState auto-pause update. 2 Rule 2 auto-fixes surfaced on the MatchSessionStep auto-pause path -- missing `gameNumber` field (third confirmed instance of the same latent gap after Plan 07 caught it 2x in draftControl.ts) + wrong enum type on `actorSlot` (LobbySlot vs TeamSide -- only MatchSessionStep caller in the codebase that wasn't using slotToTeamSide). auditInsert import fully evicted (zero callers remain, second Wave 2 file after server.ts Plan 09 to do so); auditUpdate retained for 3 LobbyMember P4 carry sites. Integration tests green: auth 34/34 + garbage-collector 8/8 + lobby 103/103 (145 tests across 16 files). Typecheck clean in spacetimedb/src.**

## Performance

- **Duration:** ~35 minutes end-to-end
- **Tasks:** 3 (Task 1 clientConnected migration + Task 2 clientDisconnected migration with Rule 2 fixes + Task 3 integration-test verification)
- **Files modified:** 1 source file
- **Commits:** 2 atomic refactor commits (Task 1 clientConnected + Task 2 clientDisconnected+Rule2) + 1 metadata finalization commit

## Accomplishments

### clientConnected Migration (4 sites, commit `e0b9f77`)

**Site 1 -- User.isOnline=true (P2):**
```ts
// BEFORE
ctx.db.User.id.update({
    ...user,
    isOnline: true,
    ...auditUpdate(ctx, user, user.id),
});
// AFTER
ctx.db.User.id.update(updateWithAudit(ctx, user, { isOnline: true }, user.id));
```

**Site 2 -- UserIdentity.lastSeenAt hygiene migration (NOT Pitfall 6):**
```ts
// BEFORE
ctx.db.UserIdentity.identity.update({
    ...mapping,
    lastSeenAt: ctx.timestamp,
    lastModifiedById: user.id,
    lastModifiedDate: ctx.timestamp,
});
// AFTER
ctx.db.UserIdentity.identity.update(updateWithAudit(ctx, mapping, {
    lastSeenAt: ctx.timestamp,
}, user.id));
```
**Why safe:** no `ts` variable in scope; lastModifiedDate was already ctx.timestamp. updateWithAudit preserves createdBy/Date from existing, forces lastModifiedBy=user.id, forces lastModifiedDate=ctx.timestamp. Net behavioral delta: 0.

**Site 3 -- Ban-on-reconnect User soft-delete (P2):**
```ts
// BEFORE
ctx.db.User.id.update({
    ...user,
    isOnline: false,
    deletedAt: ctx.timestamp,
    ...auditUpdate(ctx, user, user.id),
});
// AFTER
ctx.db.User.id.update(updateWithAudit(ctx, user, {
    isOnline: false,
    deletedAt: ctx.timestamp,
}, user.id));
```

**Site 4 -- UserDeletionJob scheduled insert (P1):**
```ts
// BEFORE
ctx.db.UserDeletionJob.insert({
    scheduledId: 0n,
    scheduledAt: ScheduleAt.time(deleteAt),
    userId: user.id,
    ...auditInsert(ctx, user.id),
});
// AFTER
ctx.db.UserDeletionJob.insert(insertWithAudit(ctx, {
    scheduledId: 0n,
    scheduledAt: ScheduleAt.time(deleteAt),
    userId: user.id,
}, user.id));
```

### clientDisconnected Migration (3 P1/P2 sites + Rule 2 fixes, commit `4bc39ec`)

**Site 1 -- User.isOnline=false (P2):** updateWithAudit transform.

**Site 5 -- MatchSessionStep auto-pause insert (P1) + 2 Rule 2 auto-fixes:**
```ts
// BEFORE
ctx.db.MatchSessionStep.insert({
    id: 0,
    lobbyId: member.lobbyId,
    sequence: session.turnIndex,
    actorUserId: SYSTEM_USER_ID,
    anonymousLabel: undefined,
    actorSlot: member.lobbySlot,                                    // <-- LobbySlot enum
    action: { tag: 'Pause', value: {} },
    payload: { tag: 'Pause', value: { ... } },
    timestamp: ctx.timestamp,
    ...auditInsert(ctx, SYSTEM_USER_ID),
} as any);                                                          // <-- hiding TS errors
// AFTER (2 Rule 2 fixes applied)
ctx.db.MatchSessionStep.insert(insertWithAudit(ctx, {
    id: 0,
    lobbyId: member.lobbyId,
    gameNumber: session.currentGameNumber,                          // <-- Rule 2 FIX #1: required schema field
    sequence: session.turnIndex,
    actorUserId: SYSTEM_USER_ID,
    anonymousLabel: undefined,
    actorSlot: slotToTeamSide(member.lobbySlot),                    // <-- Rule 2 FIX #2: canonical conversion
    action: { tag: 'Pause', value: {} } as any,
    payload: { tag: 'Pause', value: { ... } } as any,
    timestamp: ctx.timestamp,
}, SYSTEM_USER_ID));
```

**Site 6 -- MatchSession.timerState auto-pause (P2):** updateWithAudit transform.

### Preservations (P4 delete+insert-carry -- 3 LobbyMember sites)

All three preserved verbatim at L181, L192, L203:
```ts
// L181 -- Waiting/AwaitingResult/Finished stages
ctx.db.LobbyMember.by_lobby_and_user.delete([member.lobbyId, userId]);
ctx.db.LobbyMember.insert({
    ...member,
    isOnline: false,
    ...auditUpdate(ctx, member, SYSTEM_USER_ID),  // <-- primitive stays
} as any);

// L192 -- NoAction disconnectPolicy (same shape)

// L203 -- Standard/Deferred policy (adds disconnectedAt: ctx.timestamp)
ctx.db.LobbyMember.insert({
    ...member,
    isOnline: false,
    disconnectedAt: ctx.timestamp,
    ...auditUpdate(ctx, member, SYSTEM_USER_ID),  // <-- primitive stays
} as any);
```

Cannot migrate to insertWithAudit (would reset createdBy/Date); cannot migrate to updateWithAudit (no merge-into-existing -- caller builds full explicit row after `.delete()`). Primitive retained per D-06 Wave 0 canon.

## Task Commits

1. **Task 1: Migrate clientConnected cluster (4 sites)** -- `e0b9f77` (refactor)
2. **Task 2: Migrate clientDisconnected cluster (3 P1/P2 sites) + 2 Rule 2 auto-fixes (gameNumber + actorSlot) + preserve 3 LobbyMember P4 carry sites + evict auditInsert import** -- `4bc39ec` (refactor)
3. **Task 3: Verification** -- auth 34/34 + garbage-collector 8/8 + lobby 103/103 integration tests green; no code changes
4. **Task 4: SUMMARY + STATE + ROADMAP + final metadata commit** -- this commit (docs)

## Decisions Made

See frontmatter `key-decisions` array. Seven decisions captured:

- **D-10-01:** Two-commit refactor split (clientConnected + clientDisconnected-with-Rule-2-fixes) for bisect-friendly history on the auth-adjacent critical path
- **D-10-02:** UserIdentity.lastSeenAt migration is NOT Pitfall 6 -- no custom `ts` variable, both fields already use ctx.timestamp; updateWithAudit preserves semantics exactly
- **D-10-03:** Rule 2 auto-fix #1 -- MatchSessionStep missing `gameNumber` field (third confirmed instance after Plan 07 caught it 2x in draftControl.ts)
- **D-10-04:** Rule 2 auto-fix #2 -- MatchSessionStep.actorSlot LobbySlot->TeamSide conversion (index.ts auto-pause was the only MatchSessionStep caller missing slotToTeamSide)
- **D-10-05:** 3 LobbyMember P4 carry sites preserved on auditUpdate primitive (L181/L192/L203) per Pitfall 1 canon
- **D-10-06:** auditInsert import evicted (zero callers remain); second Wave 2 file after server.ts Plan 09
- **D-10-07:** Integration tests green -- auth 34/34 + GC 8/8 + lobby 103/103 confirm zero regression

## Grep Confirmation

| Pattern | Baseline | Post-plan | Notes |
|---------|----------|-----------|-------|
| `insertWithAudit` calls | 0 | 3 | L142 UserDeletionJob + L215 MatchSessionStep + 1 more |
| `updateWithAudit` calls | 0 | 6 | L121 User.isOnline=true + L126 UserIdentity.lastSeenAt + L135 User soft-delete + L164 User.isOnline=false + L227 MatchSession + 1 symbol-match |
| `insertWithAudit\|updateWithAudit` total | 0 | 9 | Plan acceptance: >= 7 ✓ (2 from import, 7 call sites) |
| `auditInsert` refs | 2 | 0 | Plan acceptance: 0 ✓ -- import evicted |
| `auditUpdate` call sites | 5 | 3 | Plan acceptance: >= 3 ✓ -- L181/L192/L203 LobbyMember carry |
| `auditUpdate` (incl. import) | 6 | 4 | 1 import token + 3 calls |
| LobbyMember P4 preservation | 3 blocks | 3 blocks | Structurally unchanged ✓ |

All plan acceptance criteria met:
- `grep -c "insertWithAudit\|updateWithAudit" index.ts` = 9 (plan: >= 7) ✓
- `grep -c "auditUpdate" index.ts` = 4 (plan: >= 3 carry calls + import) ✓
- `grep -c "auditInsert" index.ts` = 0 (plan: 0) ✓
- Typecheck clean in spacetimedb/src ✓

## Deviations from Plan

### Auto-fixed Issues (Rule 2)

**1. [Rule 2 - Missing Critical Field] MatchSessionStep.gameNumber added**
- **Found during:** Task 2 (clientDisconnected migration)
- **Issue:** Outer `} as any)` closure was hiding TS2345 'Property gameNumber is missing' error. MatchSessionStep schema (matchSessionStep.ts L9) requires `gameNumber: t.u8()` for composite PK uniqueness across games in a series.
- **Fix:** Added `gameNumber: session.currentGameNumber` to the auto-pause insert payload. Matches canonical pattern from draftControl.ts L63 (undo_last_step pause step) and L179 (pause_draft step).
- **Files modified:** spacetimedb/src/index.ts (L213)
- **Commit:** `4bc39ec`
- **Pattern:** Third confirmed occurrence of the same latent bug (Plan 07 Cluster G caught it 2x in draftControl.ts). Validates D-03 thesis that Phase 15.3 surfaces real latent correctness gaps.

**2. [Rule 2 - Type Mismatch] MatchSessionStep.actorSlot enum conversion**
- **Found during:** Task 2 (after gameNumber fix)
- **Issue:** Code passed `actorSlot: member.lobbySlot` assigning a `LobbySlot` enum value (BluePlayer/BlueCoach/RedPlayer/RedCoach/Spectator) to a field typed as `TeamSide` (Blue/Red/Spectator only). Outer `as any)` closure suppressed the TS2345 incompatibility. Pre-migration behavior: incorrect LobbySlot tag values would serialize into MatchSessionStep rows; downstream readers expecting TeamSide tags could have mis-attributed auto-pause actions to teams.
- **Fix:** Added `slotToTeamSide(member.lobbySlot)` -- the canonical helper used by EVERY OTHER MatchSessionStep.insert caller in the codebase (draftControl, draftAuction, draftClassic, postDraft -- 10+ sites).
- **Files modified:** spacetimedb/src/index.ts (L7 import + L216 call)
- **Commit:** `4bc39ec`
- **Pattern:** Auto-pause path in clientDisconnected was the ONLY MatchSessionStep writer not already routed through slotToTeamSide. Likely an oversight when the disconnect-auto-pause feature landed in a later phase than the draft reducers.

### Scope Clarifications (not deviations)

**1. auditInsert import eviction was a natural consequence of Task 2 completion.** After Task 2 migrated the last 2 auditInsert call sites (L151 UserDeletionJob + L228 MatchSessionStep), grep confirmed zero auditInsert references; import trimmed from `{ auditInsert, auditUpdate, SYSTEM_USER_ID }` to `{ auditUpdate, SYSTEM_USER_ID }` in the same commit. Second Wave 2 file after server.ts Plan 09 to fully evict the import. Plan 08 finalizationHelpers still has 1 P5 conditional site keeping it callable; Plan 11 costSetManagement TBD.

**2. Rule 2 fixes committed with Task 2 migration, not a separate commit.** The plan specified a single Task 2 commit for clientDisconnected migration. Rule 2 fixes were SURFACED BY the migration (removing `as any)` closure exposed the errors), so they belong with the migration commit. Commit message calls out both fixes explicitly for audit trail.

## Issues Encountered

- **None blocking.** All 7 migrations typecheck-clean after Rule 2 fixes. Integration tests (auth 34/34 + GC 8/8 + lobby 103/103) green on first run post-migration.
- **Pre-existing typecheck errors in test/backend/** unchanged (35 errors per deferred-items.md Plan 01 -- SCOPE BOUNDARY: out of scope for this plan).

## User Setup Required

None -- pure backend refactor, no schema changes, no binding regeneration required. Client-lifecycle hooks are compiled as part of the backend module; changes take effect on next `spacetime publish`.

## Next Phase Readiness

**Wave 2 File 4 (costSetManagement.ts, 18 sites) up next in Plan 15.3-11.**

Wave 2 progress:
1. COMPLETE **Wave 2 File 1 -- `helpers/finalizationHelpers.ts`** (Plan 08, 14 sites) -- 10 migrated + 1 P5 + 3 P4 preserved
2. COMPLETE **Wave 2 File 2 -- `reducers/server.ts`** (Plan 09, 13 sites) -- 12 migrated + 1 P4 + 3 NO-TOUCH Pitfall 6 + auditInsert import evicted
3. COMPLETE **Wave 2 File 3 -- `src/index.ts`** (this plan, 10 sites) -- 7 migrated + 3 P4 + 2 Rule 2 auto-fixes + auditInsert import evicted
4. PENDING **Wave 2 File 4 -- `reducers/costSetManagement.ts`** (18 sites) -- 3-table symmetric carry, may surface 2-3 more P5 conditional sites per Plan 11 planner notes

**Wave 3 Option alpha accounting:**
- Confirmed P5 sites remain at 3 (rosterMutations L55 + rosterAdmin L160 + finalizationHelpers L148)
- index.ts post-migration: zero P5 sites, zero auditInsert callers
- Two Wave 2 files (server.ts + index.ts) now have zero auditInsert callers
- costSetManagement.ts (Plan 11) is the final count-check before Wave 3 (Plan 13) auditInsert-deletion planner

**Blockers:** None. Pre-existing test-file typecheck errors (deferred-items.md Plan 01) remain orthogonal.

## Self-Check: PASSED

Verified all artifact claims:

- File `spacetimedb/src/index.ts` exists and modified (FOUND)
- Commit `e0b9f77` exists in git log (FOUND -- Task 1 clientConnected migration)
- Commit `4bc39ec` exists in git log (FOUND -- Task 2 clientDisconnected migration + Rule 2 fixes)
- `insertWithAudit|updateWithAudit` count in file: 9 (meets plan acceptance >= 7) ✓
- `auditInsert` count in file: 0 (meets plan acceptance = 0) ✓
- `auditUpdate` count in file: 4 (1 import + 3 P4 carry calls; meets plan acceptance >= 3) ✓
- `auditInsert` symbol removed from import on L3 (FOUND -- import trimmed to `{ auditUpdate, SYSTEM_USER_ID }`)
- 3 LobbyMember P4 carry sites preserved at L181, L192, L203 (FOUND -- structurally unchanged from pre-migration)
- slotToTeamSide import added on L7 (FOUND)
- gameNumber field present at L213 MatchSessionStep insert (FOUND)
- `npm run test:typecheck` -- 0 errors in `spacetimedb/src` (PASS); 35 pre-existing errors in `test/backend/**` unchanged (baseline per deferred-items.md Plan 01)
- auth 34/34 integration tests green (VERIFIED)
- garbage-collector 8/8 integration tests green (VERIFIED)
- lobby 103/103 integration tests green (VERIFIED)

---
*Phase: 15.3-audit-spread-type-helper*
*Completed: 2026-04-17*
*Wave 2 File 3 COMPLETE: 7 migrated + 3 P4 LobbyMember carry preserved + 2 Rule 2 auto-fixes (gameNumber + actorSlot) + auditInsert import evicted (2nd Wave 2 file); auth+GC+lobby 145/145 green*
