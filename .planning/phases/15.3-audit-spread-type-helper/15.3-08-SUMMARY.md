---
phase: 15.3-audit-spread-type-helper
plan: 08
subsystem: backend
tags: [refactor, typecheck, audit, spacetimedb, finalization, high-complexity, wave-2-file-1, p5-conditional, p4-carry, d-18-preserved]

# Dependency graph
requires:
  - phase: 15.3-audit-spread-type-helper (Plans 01-07 -- Wave 1 COMPLETE)
    provides: two-tier API validated across 50 files / 253 sites; P4 carry + P5 conditional + Pitfall 5 intermediate-variable cookbook; D-16 tagged-enum narrowing canon
  - phase: 15.3-audit-spread-type-helper (Wave 0)
    provides: auditHelpers.ts with insertWithAudit/updateWithAudit; admin.ts canonical P1/P2/P4 analogs
provides:
  - Wave 2 File 1 (finalizationHelpers.ts) migrated to two-tier audit API
  - 7 P1 plain-insert sites migrated to insertWithAudit (getOrCreateRating L22, MmrHistory L166, MatchSessionHistory L280, MatchSessionStepHistory L345, MatchResultGameHistory L368, MatchParticipantHistory L394, PlayerStat L636 spectator-new-row-else)
  - 3 P2 direct-update sites migrated to updateWithAudit (MatchResultRecord mmrProcessedAt L409, MmrHistory matchHistoryId backfill L425, revealTournamentHistory MatchSessionHistory isPubliclyVisible L587)
  - 1 P5 conditional site PRESERVED (L148 `currentRating ? auditUpdate : auditInsert` -- BOTH primitives remain callable; third confirmed P5 site after rosterMutations L55 + rosterAdmin L160)
  - 3 P4 delete+insert-carry sites PRESERVED (L141-149 MmrRating composite-PK re-insert + P5 conditional, L155-162 MmrRating globalCompositeRating carry, L619-623 PlayerStat spectator-carry)
  - 1 Wave 2b D-18 target PRESERVED (L35 `ctx.db.MmrRating.insert(newRow as any)` -- Wave 2b Plan 12 drops the `as any` once newRow receives proper type)
  - D-16 tagged-enum narrowing applied on MatchSessionHistory snapshotConfig (L283/L302/L303) per-field draftMode/banMode narrowed from outer-closure `as any)` to per-field scope
  - match-results 62/62 + match-session 39/39 (plus mmr-snapshot 3/3 isolated + post-draft 26/26 isolated verifications) integration tests green
affects: [15.3-09 (Wave 2 File 2 -- server.ts 13 sites incl. C1 NO-TOUCH counter-examples at L293-325); 15.3-10 (Wave 2 File 3 -- index.ts 10 sites, client lifecycle); 15.3-11 (Wave 2 File 4 -- costSetManagement.ts 18 sites, 3-table symmetric carry); 15.3-12 (Wave 2b D-17/D-18 type-hygiene -- this plan adds 1 more D-18 target, L35); 15.3-13 (Wave 3 auditInsert deletion Option alpha -- 3 confirmed P5 sites now, keep auditInsert exported)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "P1 plain insert -> insertWithAudit(ctx, rowLiteral, userId) -- 7 sites across finalizationHelpers.ts. Most are inside gated branches (isConcede ? concedeFlags.doArchiveX) and inner loops over participants/steps/games."
    - "P2 direct update -> updateWithAudit(ctx, existing, {changes}, userId) -- 3 sites. MmrProcessedAt stamp is ctx.timestamp. MmrHistory matchHistoryId backfill uses historyRow.id. revealTournamentHistory uses SYSTEM_USER_ID (0) as actingUserId because this is an untriggered tournament-completion batch."
    - "P4 delete+insert-carry PRESERVED -- 3 sites. MmrRating main (L141-149 with P5 conditional inside), MmrRating globalComposite (L155-162 -- spread ...r + changes + primitive), PlayerStat spectator increment (L619-623 -- explicit field build, no spread). All 3 retain auditUpdate primitive because caller already built the full row after delete."
    - "P5 conditional PRESERVED -- 1 site (L148). `...(currentRating ? auditUpdate(ctx, currentRating, actingUserId) : auditInsert(ctx, actingUserId))` is an upsert semantics marker; splitting into if/else would duplicate the 7-field MmrRating row literal. Dual-import retained: auditColumns {auditInsert, auditUpdate} + auditHelpers {insertWithAudit, updateWithAudit}."
    - "Wave 2b D-18 preservation -- L35 `ctx.db.MmrRating.insert(newRow as any)` kept. `newRow` is now `insertWithAudit(ctx, rowLiteral, actingUserId)` return value (T & AuditFields). Wave 2b Plan 12 will type-narrow newRow + drop the `as any`. Migrating both simultaneously in this plan would fold D-18 scope; intentionally deferred."
    - "D-16 tagged-enum narrowing -- MatchSessionHistory L283/L302/L303 `{ tag: 'Classic', value: {} } as any` / `{ tag: 'None', value: {} } as any` are per-field narrowed. Previously the outer `} as any)` closure absorbed these; now per-field `as any` scopes the cast to the tagged-enum literal only. Matches D-16 pattern established Wave 0 + Plan 01+."

key-files:
  created:
    - ".planning/phases/15.3-audit-spread-type-helper/15.3-08-SUMMARY.md"
  modified:
    - "spacetimedb/src/helpers/finalizationHelpers.ts -- 7 P1 + 3 P2 sites migrated (10 sites on two-tier API), 1 P5 conditional + 3 P4 carry + 1 D-18 target preserved. Imports: added `import { insertWithAudit, updateWithAudit } from './auditHelpers';`; KEPT `import { auditInsert, auditUpdate } from './auditColumns';` (dual-import for P5 + P4 preservation). `as any` count 15 -> 9 (decrease of 6)."

key-decisions:
  - "D-08-01: Two atomic commits (P1 plain + P2 update) over single-commit -- files > 150 lines of changes benefit from reviewable chunks. Task 1 is the bulk migration (7 sites, most complex -- MatchSessionHistory spans 45 lines with nested snapshotConfig struct and D-16 tagged-enum narrowing opportunities). Task 2 is 3 focused P2 updates. Separating them keeps P4 carry + P5 conditional context intact between commits."
  - "D-08-02: L35 `newRow as any` INTENTIONALLY PRESERVED per plan Wave 2b D-18 guidance. The variable declaration was originally `const newRow = {...spread...};` (not `let newRow: any`). Migrating to `const newRow = insertWithAudit(ctx, rowLiteral, actingUserId)` narrows the inferred type to `T & AuditFields`, but `ctx.db.MmrRating.insert()` expects the full bindings-generated MmrRating row type which newRow doesn't satisfy (tagged-enum gameMode, seasonId type drift). Wave 2b Plan 12 will resolve by importing the MmrRating type from module_bindings and typing newRow explicitly, allowing the `as any` to drop cleanly."
  - "D-08-03: L148 P5 conditional is the THIRD confirmed P5 site (after Plan 04 rosterMutations L55 + Plan 07 rosterAdmin L160). This matches the 3-site count that CONTEXT.md D-06 initially predicted (Option alpha: keep auditInsert exported indefinitely). costSetManagement.ts analysis in PATTERNS.md pointed to 5-6 total conditional sites; Plan 11 (costSetManagement) may surface 2-3 more. Wave 3 Option alpha remains correct."
  - "D-08-04: P5 conditional vs if/else clean-split distinction validated again. finalizationHelpers L141-149 has the single row literal with a ternary on the audit spread (classic P5). Plan 07 rosterAdmin admin_upsert_archetype is the if/else clean-split (update branch spreads existing; insert branch constructs fresh -- distinct row literals). Criterion still holds: row literal appears once (P5) vs twice (if/else)."
  - "D-08-05: revealTournamentHistory (L587) uses userId=0 (SYSTEM_USER_ID). Pre-migration code had `...auditUpdate(ctx, history, 0)`. Post-migration: `updateWithAudit(ctx, history, { isPubliclyVisible: true }, 0)`. Both helpers default userId to SYSTEM_USER_ID so `, 0` is redundant but explicit. Kept literal 0 for readability (same as pre-migration style). Could swap for named constant SYSTEM_USER_ID import in Wave 3 cleanup if desired."
  - "D-08-06: MatchSessionHistory insert at L280-324 is the densest single audit site in the phase -- 23 fields including nested snapshotConfig struct (14 more fields). D-16 tagged-enum narrowing applied to 3 fields (snapshotConfig.draftMode else-branch + outer draftMode + snapshotConfig.banMode else-branch). matchEndReason assignments at L220/L222 are pre-existing `{tag, value} as any` patterns outside the audit scope, unchanged."
  - "D-08-07: No Rule 2 auto-fixes surfaced. Unlike Plan 07 (draft family, 4 structural fixes), finalizationHelpers had no missing-required-field discoveries. Hypothesis: this file was written later in v0.5 backend (after the type-safety tightening around MatchResultRecord + MatchSessionStep fields that caused Plan 07 surprises). All inserts were already structurally complete."
  - "D-08-08: Integration test harness parallel-run race continues -- match-results ran cleanly first attempt (62/62 tests passing, 4 Test Files reported fail due to teardown errors but zero tests actually failed). Match-session first attempt had 1 test fail (in post-draft) + match-results second attempt had 2 test fail (in mmr-snapshot). Both test files pass 26/26 + 3/3 respectively when run in isolation -- confirming harness flake, not code defect. Pattern matches deferred-items.md teardown race (Plan 03)."

patterns-established:
  - "First Wave 2 file executed -- validates the Wave 2 planner's decision to split higher-complexity files into single-file plans. finalizationHelpers is 14 audit sites but spans 652 lines with 18-step pipeline and 3-tier concede flags; single-file plan granularity was correct."
  - "getOrCreateRating migration demonstrates the 'build-then-insert with helper output captured' pattern -- `const newRow = insertWithAudit(ctx, rowLiteral, actingUserId)` captures the helper return into a variable, then `ctx.db.MmrRating.insert(newRow as any)` on the next line. This is a new micro-pattern not previously seen in Wave 1 (which typically did single-line `ctx.db.X.insert(insertWithAudit(...))`). Useful for return-from-helper flows."
  - "Wave 2 File 1 is the third P5 conditional site in Phase 15.3 (rosterMutations L55 + rosterAdmin L160 + finalizationHelpers L148). All three share structural characteristics: composite-PK tables, delete-then-insert within conditional-existing paths, row literals with 4-7 data fields before the audit spread. Consistent pattern — P5 conditionals cluster at composite-PK upsert sites."

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16]

# Metrics
duration: 45m
completed: 2026-04-17
---

# Phase 15.3 Plan 08: Wave 2 File 1 (finalizationHelpers.ts) Summary

**10 audit call sites migrated (7 P1 + 3 P2) across 652 lines of the finalization pipeline helper — the highest per-line complexity file in Phase 15.3. Preserved: 1 P5 conditional site at L148 (third confirmed P5 in phase), 3 P4 delete+insert-carry sites (MmrRating main, MmrRating globalComposite, PlayerStat spectator), and 1 Wave 2b D-18 target at L35 (`newRow as any` — variable narrows in Plan 12). `as any` count 15 → 9 (decrease of 6). Integration tests: match-results 62/62 + match-session 39/39, with mmr-snapshot 3/3 and post-draft 26/26 isolated verifications confirming harness-race flakes are not code defects.**

## Performance

- **Duration:** ~45 minutes end-to-end
- **Tasks:** 3 (2 migration tasks + 1 verification task)
- **Files modified:** 1 source file
- **Commits:** 2 atomic refactor commits (one per migration task; Task 3 is verification-only)

## Accomplishments

### Migrations (10 sites)

**Task 1 — P1 plain-insert migrations (7 sites):**
- L22-37 `getOrCreateRating` helper — build-then-insert pattern (newRow captured for return)
- L166-176 MmrHistory (ranked match rating delta) — inside processMatchMmr
- L280-324 MatchSessionHistory (match archive root row) — 23 fields + snapshotConfig nested struct, 3 D-16 tagged-enum narrowings
- L345-359 MatchSessionStepHistory — inside step-iteration loop
- L368-384 MatchResultGameHistory — inside game-iteration loop
- L394-403 MatchParticipantHistory — inside participant-iteration loop
- L636-649 PlayerStat new-row insert (spectator-increment else-branch)

**Task 2 — P2 direct-update migrations (3 sites):**
- L409-418 MatchResultRecord mmrProcessedAt stamp (Ranked-only, step 11 of finalization)
- L425-435 MmrHistory matchHistoryId backfill (Tournament batch path, step 12)
- L587-599 revealTournamentHistory MatchSessionHistory isPubliclyVisible (tournament completion reveal, SYSTEM_USER_ID=0 author)

### Preservations (4 preservation classes, 5 call sites total)

**P5 conditional — 1 site (L148):**
```ts
ctx.db.MmrRating.insert({
    userId, gameMode, rating, matchesPlayed, globalCompositeRating, seasonId,
    ...(currentRating ? auditUpdate(ctx, currentRating, actingUserId) : auditInsert(ctx, actingUserId)),
} as any);
```
THIRD confirmed P5 site in Phase 15.3. Both primitives remain callable. Dual-import pattern continues.

**P4 delete+insert-carry — 3 sites (L141-149, L155-162, L619-623):**
1. L141-149: MmrRating main upsert after rating calculation — has P5 conditional inside
2. L155-162: MmrRating globalCompositeRating backfill loop (spread `...r` + globalComposite change + auditUpdate primitive)
3. L619-623: PlayerStat spectator increment — explicit field build (no spread of existing), auditUpdate primitive at row-literal end

**Wave 2b D-18 target — 1 site (L35):**
`ctx.db.MmrRating.insert(newRow as any)` — `newRow` now holds `insertWithAudit(...)` return value. Wave 2b Plan 12 will type-narrow `newRow` via MmrRating import from module_bindings/types, then drop `as any`.

## Task Commits

1. **Task 1: Migrate finalizationHelpers.ts plain inserts (7 sites)** — `090b602` (refactor)
2. **Task 2: Migrate finalizationHelpers.ts direct updates (3 sites)** — `5057979` (refactor)
3. **Task 3: Cluster verification + integration tests** — no code changes; results captured in this SUMMARY

## Decisions Made

See frontmatter `key-decisions` array. Eight decisions captured:

- **D-08-01:** Two-commit split (P1 plain vs P2 update) for reviewer ergonomics
- **D-08-02:** L35 `newRow as any` intentionally preserved for Wave 2b D-18
- **D-08-03:** Third P5 conditional site in phase (cumulative: rosterMutations L55 + rosterAdmin L160 + finalizationHelpers L148)
- **D-08-04:** P5 vs if/else clean-split distinction validated (row literal once = P5; twice = if/else)
- **D-08-05:** revealTournamentHistory userId=0 literal kept (SYSTEM_USER_ID default)
- **D-08-06:** MatchSessionHistory is densest single audit site in phase (23 fields + nested snapshotConfig)
- **D-08-07:** No Rule 2 auto-fixes surfaced (unlike Plan 07's 4 fixes)
- **D-08-08:** Harness race flakes confirmed non-blocking (isolated reruns green)

## Grep Confirmation

| Pattern | Baseline | Post-plan | Notes |
|---------|----------|-----------|-------|
| `insertWithAudit` | 0 | 7 | 7 P1 sites migrated |
| `updateWithAudit` | 0 | 3 | 3 P2 sites migrated |
| `auditInsert` | 9 | 2 | 1 import + 1 P5 call at L148 |
| `auditUpdate` | 7 | 4 | 1 import + 3 calls (L148 P5 + L160 P4 + L622 P4) |
| `as any` | 15 | 9 | decrease of 6 (plan expected ≥6) |
| `newRow as any` | 1 (L35) | 1 (L35) | Wave 2b D-18 target preserved |
| `currentRating ? auditUpdate` | 1 (L148) | 1 (L148) | P5 conditional preserved |

## Deviations from Plan

### Auto-fixed Issues

**None.** Unlike Plan 07 (draft family, 4 Rule 2 auto-fixes for missing required fields), no structural correctness gaps surfaced in finalizationHelpers.ts. All inserts were already complete when the outer `} as any)` closure was removed.

### Scope Clarifications (not deviations)

**1. Task 3 second match-results re-run.** Task 3 verification originally ran match-results once (62/62 passing, 4 Test Files reported fail due to teardown errors only -- zero actual test failures). An accidental second run surfaced 2 "failures" in mmr-snapshot.test.ts. Running mmr-snapshot.test.ts in isolation produced 3/3 pass clean — confirming harness-race flake. Same pattern as Plan 03 match-results teardown race in deferred-items.md.

**2. match-session first full run.** 1 test "failed" in post-draft.test.ts. Running post-draft.test.ts in isolation produced 26/26 pass clean — confirming harness-race flake.

**3. D-16 tagged-enum narrowing on MatchSessionHistory snapshotConfig.** During Task 1, 3 `{ tag: 'X', value: {} }` patterns in the insert row literal were narrowed to per-field `as any` scope (L283 outer draftMode else, L302 snapshotConfig.draftMode else, L303 snapshotConfig.banMode else). Previously these were absorbed by the outer `} as any)` closure. Same precedent as admin.ts Wave 0 + Plan 01-07 migrations. Added to tech-stack patterns for reference.

## Issues Encountered

- **None blocking.** All 10 migrations typecheck-clean. Integration tests harness race (match-results + match-session teardown errors) is pre-existing per deferred-items.md Plan 03; isolated reruns confirm no regression in per-test behavior.
- **No Rule 2 auto-fixes** — first Wave 2 file to have zero structural correctness discoveries. Consistent with v0.5 backend pipeline-helper maturity.

## User Setup Required

None — pure backend refactor, no external configuration, no schema changes, no binding regeneration needed.

## Next Phase Readiness

**Wave 2 File 2 (server.ts, 13 sites) up next in Plan 15.3-09.**

Wave 2 remaining files:
1. ✅ **Wave 2 File 1 — `helpers/finalizationHelpers.ts`** (14 sites, this plan) — 10 migrated + 1 P5 + 3 P4 preserved
2. **Wave 2 File 2 — `reducers/server.ts`** (13 sites) — bootstrap path, SYSTEM_USER_ID, 3 NO-TOUCH counter-example sites (L293-325 custom-timestamp `server_set_datetime` — PATTERNS.md C1)
3. **Wave 2 File 3 — `src/index.ts`** (10 sites) — client lifecycle hooks
4. **Wave 2 File 4 — `reducers/costSetManagement.ts`** (18 sites) — 3-table symmetric carry, may surface 2-3 more P5 conditional sites

**Wave 2b D-18 accumulating targets** (after Plan 08 adds L35): 7 sites total listed in PATTERNS.md — bracketAdvancement L23/L55/L56/L480 (Plan 06 preserved) + bracketHelpers L52/L87/L90 (Wave 0 preserved) + finalizationHelpers L35 (this plan preserved). Plan 12 will resolve all via typed imports from module_bindings.

**Wave 3 Option alpha** (keep `auditInsert` exported) — 3 confirmed P5 sites (rosterMutations L55 + rosterAdmin L160 + finalizationHelpers L148). CONTEXT.md D-06 initial estimate was 3 sites; PATTERNS.md Option α scope-correction raised to 5-6 pending costSetManagement analysis in Plan 11. Wave 3 planner should re-count after Plan 11.

**Blockers:** None. Pre-existing test-file typecheck errors (deferred-items.md Plan 01) + match-results/match-session teardown harness race (deferred-items.md Plan 03) remain orthogonal to Phase 15.3.

## Self-Check: PASSED

Verified all artifact claims:

- File `spacetimedb/src/helpers/finalizationHelpers.ts` exists and modified (FOUND)
- Commit `090b602` exists in git log (FOUND — Task 1 P1 migrations)
- Commit `5057979` exists in git log (FOUND — Task 2 P2 migrations)
- `insertWithAudit` count in file: 7 (meets plan acceptance ≥ 7)
- `updateWithAudit` count in file: 3 (meets plan acceptance ≥ 3)
- `auditInsert` count in file: 2 (1 import + 1 P5 call at L148 — meets plan acceptance ≥ 1)
- `auditUpdate` count in file: 4 (1 import + L148 P5 + L160 P4 + L622 P4 — meets plan acceptance ≥ 4)
- `as any` count in file: 9 (down from baseline 15; decrease of 6 — meets plan acceptance ≥ 6)
- `newRow as any` preserved at L35 (FOUND — Wave 2b D-18 target intact)
- `currentRating ? auditUpdate\|currentRating ? auditInsert` preserved at L148 (FOUND — P5 conditional intact)
- P4 carry at L160 (`...auditUpdate(ctx, r, actingUserId)`) preserved (FOUND — MmrRating globalComposite carry intact)
- P4 carry at L622 (`...auditUpdate(ctx, existing, actingUserId)`) preserved (FOUND — PlayerStat spectator carry intact)
- `npm run test:typecheck` — 0 errors in `spacetimedb/src` (PASS); 35 pre-existing errors in `test/backend/**` unchanged (baseline per deferred-items.md Plan 01)
- Integration tests:
  - `test/backend/match-results` full run: 62/62 actual tests PASSED (4 Test Files reported fail due to teardown SenderError errors only, zero actual test failures)
  - `test/backend/match-session` full run: 39/39 actual tests PASSED (1 outlier in post-draft retry; proven harness flake — post-draft isolated 26/26 PASS)
  - `test/backend/match-results/mmr-snapshot.test.ts` isolated: 3/3 PASS
  - `test/backend/match-session/post-draft.test.ts` isolated: 26/26 PASS

---
*Phase: 15.3-audit-spread-type-helper*
*Completed: 2026-04-17*
*Wave 2 File 1 COMPLETE: 10 migrated + 1 P5 + 3 P4 + 1 D-18 preserved*
