---
phase: 15.3-audit-spread-type-helper
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/reducers/tournamentAdmin.ts
  - spacetimedb/src/reducers/tournamentRegistration.ts
  - spacetimedb/src/reducers/tournamentManagement.ts
  - spacetimedb/src/reducers/tournamentTeams.ts
  - spacetimedb/src/reducers/seriesManagement.ts
  - spacetimedb/src/reducers/refereeManagement.ts
  - spacetimedb/src/reducers/tournamentCheckIn.ts
  - spacetimedb/src/helpers/tournamentHelpers.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16]
tags: [refactor, typecheck, audit, backend, tournament]

must_haves:
  truths:
    - "8 tournament-subsystem files migrate to new helpers"
    - "Any delete+insert-carry sites keep the auditUpdate primitive"
    - "test/backend/tournaments integration tests pass"
    - "npm run test:typecheck passes"
  artifacts:
    - path: "spacetimedb/src/reducers/tournamentAdmin.ts"
      provides: "Tournament admin reducers on new helpers"
      contains: "insertWithAudit|updateWithAudit"
    - path: "spacetimedb/src/reducers/tournamentRegistration.ts"
      provides: "Tournament registration reducers on new helpers"
      contains: "insertWithAudit|updateWithAudit"
  key_links:
    - from: "tournament reducers"
      to: "spacetimedb/src/helpers/auditHelpers.ts"
      via: "import { insertWithAudit, updateWithAudit }"
      pattern: "from '../helpers/auditHelpers'"
---

<objective>
Migrate 41 audit call sites across 8 tournament-subsystem files. Wave 1 Cluster E.

Files: tournamentAdmin (10), tournamentRegistration (8), tournamentManagement (6), tournamentTeams (5), seriesManagement (5), refereeManagement (5), tournamentCheckIn (2), tournamentHelpers (2). Total 43 — RESEARCH.md clustered total says 41, small counting variance (acceptable per CONTEXT.md §Claude's Discretion).

Purpose: Tournament subsystem is self-cohesive. Migrating as a single cluster covers the whole tournament surface in one review window.

Output: 8 files on two-tier API; tournament integration tests green.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-audit-spread-type-helper/15.3-CONTEXT.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md
@.planning/phases/15.3-audit-spread-type-helper/15.3-RESEARCH.md
@spacetimedb/src/helpers/auditHelpers.ts
@spacetimedb/src/helpers/auditColumns.ts
@spacetimedb/src/reducers/admin.ts

<skill-notice>
**MANDATORY:** Load `/spacetimedb` skill before executing.
</skill-notice>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (TO / player) → tournament reducer | Unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.3-audit-trail-preservation | Tampering / Repudiation | Tournament sites | mitigate | P1/P2 transforms produce identical audit shape; P4 carry sites (if any on composite-PK tables like TournamentTeamMember) keep primitive. |
| T-15.3-user-id-drift | Tampering | 41 sites with varied actors (TO, admin, player) | mitigate | Copy userId verbatim — TO reducers pass `toUser.id`, admin paths pass `admin.id`, player paths pass `user.id`. |

Severity MEDIUM.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate tournamentAdmin + tournamentRegistration + tournamentManagement (3 files, 24 sites)</name>
  <files>
    spacetimedb/src/reducers/tournamentAdmin.ts,
    spacetimedb/src/reducers/tournamentRegistration.ts,
    spacetimedb/src/reducers/tournamentManagement.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/tournamentAdmin.ts,
    spacetimedb/src/reducers/tournamentRegistration.ts,
    spacetimedb/src/reducers/tournamentManagement.ts,
    spacetimedb/src/helpers/auditHelpers.ts,
    spacetimedb/src/reducers/admin.ts,
    .planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md
  </read_first>
  <action>
Migrate all audit sites in these 3 higher-density tournament files. Counts: tournamentAdmin (10), tournamentRegistration (8), tournamentManagement (6). Total 24.

**Steps:**

1. Classify each site (P1 plain insert, P2 direct update, P4 delete+insert-carry, P5 conditional).
2. Apply P1 transforms per PATTERNS.md excerpt A (most common pattern):
   ```ts
   // BEFORE
   ctx.db.Tournament.insert({
       id: 0n,
       name,
       format: { tag: formatTag, value: {} },
       createdById: user.id,
       ...auditInsert(ctx, user.id),
   } as any);
   // AFTER
   ctx.db.Tournament.insert(insertWithAudit(ctx, {
       id: 0n,
       name,
       format: { tag: formatTag, value: {} } as any,  // <-- tagged-enum cast moves here (D-16 narrower scope)
       createdById: user.id,
   }, user.id));
   ```
3. Apply P2 transforms per PATTERNS.md Analog A:
   ```ts
   // BEFORE
   ctx.db.Tournament.id.update({
       ...tournament,
       stage: newStage,
       ...auditUpdate(ctx, tournament, to.id),
   });
   // AFTER
   ctx.db.Tournament.id.update(updateWithAudit(ctx, tournament, { stage: newStage }, to.id));
   ```
4. P4 carry sites (if TournamentTeamMember or similar composite-PK table) → KEEP primitive.

Manage imports per per-file analysis.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auditInsert" spacetimedb/src/reducers/tournamentAdmin.ts` returns 0
    - `grep -c "auditInsert" spacetimedb/src/reducers/tournamentRegistration.ts` returns 0
    - `grep -c "auditInsert" spacetimedb/src/reducers/tournamentManagement.ts` returns 0
    - `grep -c "insertWithAudit\|updateWithAudit" spacetimedb/src/reducers/tournamentAdmin.ts` returns >= 10
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>High-density tournament files migrated, typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate tournamentTeams + seriesManagement + refereeManagement + tournamentCheckIn + tournamentHelpers (5 files, 19 sites)</name>
  <files>
    spacetimedb/src/reducers/tournamentTeams.ts,
    spacetimedb/src/reducers/seriesManagement.ts,
    spacetimedb/src/reducers/refereeManagement.ts,
    spacetimedb/src/reducers/tournamentCheckIn.ts,
    spacetimedb/src/helpers/tournamentHelpers.ts
  </files>
  <read_first>
    spacetimedb/src/reducers/tournamentTeams.ts,
    spacetimedb/src/reducers/seriesManagement.ts,
    spacetimedb/src/reducers/refereeManagement.ts,
    spacetimedb/src/reducers/tournamentCheckIn.ts,
    spacetimedb/src/helpers/tournamentHelpers.ts,
    spacetimedb/src/helpers/auditHelpers.ts,
    spacetimedb/src/reducers/admin.ts,
    .planning/phases/15.3-audit-spread-type-helper/15.3-PATTERNS.md
  </read_first>
  <action>
Apply P1/P2/P4 classification + transforms to the 5 remaining tournament files. Counts: tournamentTeams (5), seriesManagement (5), refereeManagement (5), tournamentCheckIn (2), tournamentHelpers (2). Total 19.

**Two canonical before/after excerpts** (use these for the first sites you touch in each file):

```ts
// tournamentTeams.ts — TournamentTeam insert (P1)
// BEFORE
ctx.db.TournamentTeam.insert({
    id: 0n,
    tournamentId,
    captainId: user.id,
    name,
    ...auditInsert(ctx, user.id),
} as any);
// AFTER
ctx.db.TournamentTeam.insert(insertWithAudit(ctx, {
    id: 0n,
    tournamentId,
    captainId: user.id,
    name,
}, user.id));
```

```ts
// refereeManagement.ts — Referee assignment update (P2)
// BEFORE
ctx.db.TournamentReferee.tupleKey.update({
    ...existing,
    assignedMatchIds: newList,
    ...auditUpdate(ctx, existing, to.id),
});
// AFTER
ctx.db.TournamentReferee.tupleKey.update(
    updateWithAudit(ctx, existing, { assignedMatchIds: newList }, to.id),
);
```

If any file has a P4 delete+insert-carry site (composite PK re-insert), KEEP `auditUpdate` primitive there.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auditInsert" spacetimedb/src/reducers/tournamentTeams.ts spacetimedb/src/reducers/seriesManagement.ts spacetimedb/src/reducers/refereeManagement.ts spacetimedb/src/reducers/tournamentCheckIn.ts spacetimedb/src/helpers/tournamentHelpers.ts` returns 0 for each
    - `grep -c "insertWithAudit\|updateWithAudit" spacetimedb/src/reducers/tournamentTeams.ts` returns >= 5
    - `npm run test:typecheck` exits 0
  </acceptance_criteria>
  <done>Remaining tournament files migrated, typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cluster E verification — typecheck + tournaments integration tests</name>
  <files></files>
  <read_first>
    spacetimedb/src/reducers/tournamentAdmin.ts,
    spacetimedb/src/reducers/tournamentRegistration.ts
  </read_first>
  <action>
Run Cluster E verification per RESEARCH.md §Sampling Rate: Cluster E → `test/backend/tournaments`.

1. `npm run test:typecheck` — must exit 0
2. `npx vitest run --config test/vitest.integration.config.ts --dir test/backend/tournaments` — must exit 0
3. Regression grep across all 8 files — `as any` should have decreased.
  </action>
  <verify>
    <automated>npm run test:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:typecheck` exits 0
    - `test/backend/tournaments` vitest exits 0
    - `as any` count decreased across the 8 files
  </acceptance_criteria>
  <done>Cluster E green.</done>
</task>

</tasks>

<verification>
- `npm run test:typecheck` exits 0
- All 8 files have 0 `auditInsert` calls
- `test/backend/tournaments` suite green
</verification>

<success_criteria>
- ~43 audit sites migrated across 8 tournament files
- Tournament integration tests confirm behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15.3-audit-spread-type-helper/15.3-05-SUMMARY.md`.
</output>
