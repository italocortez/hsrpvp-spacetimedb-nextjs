---
phase: 15.3-audit-spread-type-helper
plan: 16
subsystem: backend
tags: [refactor, typecheck, audit, backend, wave-4b, real-miss-cleanup, pre-publish]

# Dependency graph
requires:
  - phase: 15.3-audit-spread-type-helper (Plans 01-13 — Wave 1 + Wave 2 + Wave 2b + Wave 3)
    provides: insertWithAudit/updateWithAudit canonical pattern across 50 files; auditColumns.ts Option-α JSDoc; auditHelpers.ts typed helpers
  - phase: 15.3-audit-spread-type-helper (Plan 12 Wave 2b D-17)
    provides: tightened mergeForUpdate generic `<T, K extends keyof T & string>` that makes the internal narrow cast at admin.ts L107 expressible as `merged[f] = incoming[f] as T[K]` without fallback casts
provides:
  - 7 sites fixed in bracketGeneration.ts (6 P2 inline-manual-audit updates + 1 composite-PK row-object delete)
  - 2 narrow-cast fixes in draftAuction.ts (L419/L657 `nominateStep.payload.value as NominatePayload`)
  - 4 as-any dropped across 2 GC scheduled-job files (identityGc + lobbyGc) — unnecessary casts removed without introducing insertWithAudit (scheduled-job tables have no audit columns)
  - 1 lobbySettings.ts LobbyPassword P5 upsert split into clean if/else using insertWithAudit/updateWithAudit helpers (no 10th auditInsert primitive caller — Plan 13 Option-α canon holds)
  - 4 P1-shape migrations in seriesManagement.ts (1 initialTimerState TimerState annotation + 3 ChatMessage inserts converted from inline manual audit to insertWithAudit)
  - 1 Rule 1 latent-bug fix in seriesManagement.ts — initialTimerState was writing nonexistent `pausedAt`/`pauseRemainingMs` fields; corrected to real schema fields (`teamBlueReserveMs`, `teamRedReserveMs`, `accumulatedPauseMs`) per draftClassic.ts L124-130 canonical shape
  - 1 profile.ts TournamentTeam lazy-sync update migrated to updateWithAudit (Plan 01 D-02-ADJUNCT retired)
  - 1 postDraft.ts dict-access narrow cast (`(costs as any)[s]` → `(costs as Record<string, number>)[s]`)
  - 1 admin.ts mergeForUpdate internal self-cast narrowed (`(merged as any)[f] = incoming[f]` → `merged[f] = incoming[f] as T[K]`)
  - 2 matchResultSubmission.ts narrow casts (L50 partial-patch → `Partial<MatchResultRecord>`; L173 status → `MatchResultRecord['status']`)
  - 1 tournamentLobby.ts matchType cast narrowed to `Lobby['matchType']`
  - 21 as any eliminated total (210 → 189)
  - Plan 13 auditInsert canon preserved: exactly 9 primitive callers across 4 files (Wave 3 Option-α unchanged)
  - npm run test:typecheck — 0 errors in spacetimedb/src; 35 test/backend baseline errors unchanged
  - Full integration suite: 59 Test Files passed + 1 skipped = 60 total; 616 Tests passed + 7 todo = 623 total; 63 min runtime; exit code 0
affects: [15.3-15 (Wave 5 maincloud publish — unblocked, pre-publish typecheck + integration gates both green)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Scheduled-job table inserts (no audit columns) do NOT migrate to insertWithAudit. The trailing `as any` on `{scheduledId: 0n, scheduledAt: ScheduleAt.time(...)}` was unnecessary and drops cleanly against the current SDK types. Helpers.auditHelpers is intentionally type-constrained to `T extends object` with expected audit fields — forcing scheduled job rows through it would add createdBy*/lastModified* fields the schema doesn't declare."
    - "P5 upsert avoidance via helper-pair if/else — when an inline ternary-P5 (`existing ? auditUpdate(ctx, ex, uid) : auditInsert(ctx, uid)` mixed with manual field writes) surfaces LATE in the phase, a clean split into `if (existing) { .delete(); .insert(updateWithAudit(ctx, existing, {changes}, uid)); } else { .insert(insertWithAudit(ctx, row, uid)); }` preserves the two-branch semantics without adding to the Plan 13 Option-α canon of exactly 9 permanent auditInsert primitive callers. Used at lobbySettings.ts L144-152."
    - "admin.ts mergeForUpdate internal cast narrow — Plan 12 D-03b flagged `(merged as any)[f] = incoming[f]` as internal-helper-exclusion. Plan 16 retires the exclusion by narrowing to `merged[f] = incoming[f] as T[K]`; valid because Plan 12 D-17 had already tightened K to `keyof T & string`. The cast is now at a field-level T[K] rather than suppressing the entire write."
    - "Narrow-cast to module_bindings field types — when a pre-migration site had `as any` on an enum-assigned value (status/matchType/sideFlag), replace with `as TableName['fieldName']` index-access type. TypeScript's type index access resolves to the tagged-enum declaration automatically, keeping the one-line cast narrow without manually naming the enum type."
    - "Rule 1 latent-bug surfacing via TimerState typing — annotating `initialTimerState: TimerState` in seriesManagement.ts advance_to_next_game exposed a 2-field schema drift (code wrote `pausedAt`/`pauseRemainingMs` which do not exist on the current TimerState schema; real fields are `teamBlueReserveMs`/`teamRedReserveMs`/`accumulatedPauseMs`). The outer `as any` had suppressed this since whenever TimerState was last refactored. Fix copies the canonical shape from draftClassic.ts L124-130 start_draft initial state."

key-files:
  created:
    - ".planning/phases/15.3-audit-spread-type-helper/15.3-16-SUMMARY.md"
  modified:
    - "spacetimedb/src/reducers/bracketGeneration.ts — 7 sites migrated (6 P2 + 1 composite-PK delete to row-object form). updateWithAudit added to imports (insertWithAudit already present)."
    - "spacetimedb/src/reducers/draftAuction.ts — 2 narrow casts (L419/L657 nominateStep payload value access). NominatePayload type-only import added from module_bindings/types."
    - "spacetimedb/src/reducers/identityGc.ts — 2 as-any dropped on scheduled-job inserts L114/L170."
    - "spacetimedb/src/reducers/lobbyGc.ts — 2 as-any dropped on scheduled-job inserts L182/L230."
    - "spacetimedb/src/reducers/lobbySettings.ts — 1 site: LobbyPassword P5 upsert at L144-152 split into clean if/else using updateWithAudit + insertWithAudit helpers. insertWithAudit added to imports."
    - "spacetimedb/src/reducers/seriesManagement.ts — 4 sites + 1 Rule 1 fix: initialTimerState TimerState annotation with canonical-shape fix at L90; 3 ChatMessage inserts migrated to insertWithAudit at L134/L183/L252. TimerState type-only import + insertWithAudit added to imports."
    - "spacetimedb/src/reducers/profile.ts — 1 site: TournamentTeam lazy-sync update L81 migrated to updateWithAudit."
    - "spacetimedb/src/reducers/postDraft.ts — 1 narrow cast at L70 (costRow.costs dict access)."
    - "spacetimedb/src/reducers/admin.ts — 1 narrow cast at L107 (mergeForUpdate internal write-through, narrowed to `as T[K]`)."
    - "spacetimedb/src/reducers/matchResultSubmission.ts — 2 sites: L50 partial-patch narrowed to `as Partial<MatchResultRecord>`; L173 status cast narrowed to `as MatchResultRecord['status']`. MatchResultRecord type-only import added."
    - "spacetimedb/src/reducers/tournamentLobby.ts — 1 site: L124 matchType cast narrowed to `as Lobby['matchType']`. Lobby type-only import added."

key-decisions:
  - "D-16-01: Plan's per-file site counts were stale — many sites the plan labeled as 'P1 inserts needing migration' were already migrated in Wave 1 Plan 07 / Cluster G (draft family density). Actual residual `as any` shape in those files is tagged-enum inner casts INSIDE already-migrated `insertWithAudit`/`updateWithAudit` payloads, which are preserved per D-16. True Plan-16-fixable count was 24 (not 38), and actual migration yielded -21 as any (210→189) because some of the 'fixable' sites were secondary to already-migrated wrappers. No scope creep — stayed strictly inside the 13 files listed in files_modified."
  - "D-16-02: draftClassic.ts (15 as-any) + draftControl.ts (4 as-any) touched zero times this plan. All residual `as any` in both files are tagged-enum literals inside already-Wave-1-migrated helper payloads. Evidence: grep output before plan showed both files at their Wave 1 end-state with no P1 or P2 closures remaining."
  - "D-16-03: Scheduled-job `as any` removal is a distinct transform from P1 insertWithAudit migration. identity_gc_job + lobby_gc_job schemas have NO audit columns (they're scheduled-reducer-trigger tables). Wrapping their inserts in insertWithAudit would ADD createdBy*/lastModified* fields the schema does not declare, producing runtime shape mismatch. Instead, the fix is to simply drop the unnecessary outer `as any` — the current SDK's row-typing accepts the naked `{scheduledId: 0n, scheduledAt: ScheduleAt.time(...)}` shape without cast. Verified via typecheck after each of the 4 edits."
  - "D-16-04: lobbySettings.ts L151 LobbyPassword upsert was a latent P5 — inline ternary `createdById: existing ? existing.createdById : user.id` + twin for createdDate. Plan 13 Option-α canon is exactly 9 permanent auditInsert primitive callers. Rather than growing the canon to 10 by introducing a tenth ternary, split into clean if/else where the existing branch delete+reinserts using updateWithAudit (preserves original createdBy/Date semantically) and the fresh branch uses insertWithAudit (fresh audit stamps). Plan 13 canon verified unchanged post-migration (grep still returns 9)."
  - "D-16-05: Rule 1 fix in seriesManagement.ts advance_to_next_game initialTimerState — after adding the TimerState type annotation, the typechecker surfaced 'pausedAt does not exist in type' because the TimerState schema has fields {turnStartAt, teamBlueReserveMs, teamRedReserveMs, isPaused, accumulatedPauseMs} but the code wrote {isPaused, pausedAt: undefined, pauseRemainingMs: 0, turnStartAt}. Fix applied per draftClassic.ts L124-130 canonical start_draft shape. No persisted-data change at steady state (advance_to_next_game is called BetweenGames→Drafting; the new-game timer reserve budgets at lobby.reserveBankSeconds * 1000 matches start_draft's fresh-game behavior). Plan 01 D-03-ADJUNCT precedent: helper typing surfaces latent bugs hidden by the outer `as any` closure."
  - "D-16-06: admin.ts L107 narrow cast was the ONLY Plan 12-deferred internal cast with a post-D-17 narrow form. After Plan 12 tightened mergeForUpdate's K generic to `keyof T & string`, `merged[f] = incoming[f] as T[K]` type-checks cleanly — verified 0 errors in spacetimedb/src post-edit. This retires Plan 12's D-03b exclusion for this specific site."
  - "D-16-07: The 3-field-access narrow casts (matchResultSubmission status + sideFlag + tournamentLobby matchType) all used module_bindings index-access types (`TableName['fieldName']`) rather than importing dedicated enum type names. This keeps the cast one token wider than necessary (e.g., `Lobby['matchType']` instead of `MatchType`), but avoids bespoke enum-type imports that would clutter the import list across 3 files. Consistent with Plan 12's writable-mapped-type pattern: narrower than `as any`, no architectural effect."
  - "D-16-08: Zero new tagged-enum `{tag: X, value: Y} as any` literals added or modified this plan. Where a downstream cast near a migrated site was necessary (e.g., seriesManagement.ts ChatMessage senderType tagged-enum, moved INTO the insertWithAudit payload), the inner tagged-enum cast stays narrow per D-16 canon. No expansion of phase scope into the SDK-tagged-union cleanup that remains out-of-scope per D-03b."
  - "D-16-09: bundle.js regenerated during the integration test run (vitest harness bootstrap). Reverted via `git checkout -- spacetimedb/dist/bundle.js` per Phase 15.3 Plans 03/04/05/06/13/14 precedent — Plan 15 explicitly owns the pre-publish bundle commit."

patterns-established:
  - "Plan's stale counts are fine in principle if all fixable sites get touched — but staying inside files_modified is non-negotiable. Actual per-file audits at execution time trump the plan's line-number tables when prior plans have already migrated most of the sites."
  - "Scheduled-job-table insert as-any is a distinct bucket from audit-closure as-any. Don't confuse them — the former drops because the SDK accepts the naked row shape; the latter needs insertWithAudit because the inline audit-spread creates the type hole."
  - "Late-arriving P5 inline ternaries should be split into helper-pair if/else rather than growing the Plan 13 auditInsert canon. The split preserves dual-branch audit semantics without adding new primitive callers."

requirements-completed: [D-01, D-02, D-03, D-17]

# Metrics
metrics:
  files_modified: 11  # 13 in plan frontmatter, but draftClassic.ts + draftControl.ts untouched per D-16-02
  as_any_delta: -21
  as_any_before: 210
  as_any_after: 189
  audit_insert_callers_before: 9
  audit_insert_callers_after: 9  # Plan 13 Option-α preserved
  typecheck_errors_spacetimedb_src: 0
  typecheck_errors_test_backend_baseline: 35  # pre-existing per Plan 01 deferred-items.md, unchanged
  rule_1_auto_fixes: 1  # seriesManagement TimerState schema-drift correction
  rule_2_auto_fixes: 0
  integration_test_files: "59 passed + 1 skipped (60 total)"
  integration_tests: "616 passed + 7 todo (623 total)"
  integration_duration_s: 3799.83
duration: 73m
completed: 2026-04-18
---

# Phase 15.3 Plan 16: Wave 4b Real-Miss Cleanup Summary

**21 `as any` eliminated across 11 files in Wave 4b pre-publish cleanup: 6 P2 inline-manual-audit updates + 1 composite-PK row-object delete in bracketGeneration, 4 P1 migrations + 1 Rule 1 TimerState schema-drift fix in seriesManagement, 1 LobbyPassword P5 split into helper-pair if/else (Plan 13 Option-α canon preserved), 4 scheduled-job unnecessary as-any dropped in identityGc+lobbyGc, and 7 narrow-cast fixes across admin/matchResultSubmission/tournamentLobby/postDraft/draftAuction/profile. Zero tagged-enum literals modified. Typecheck clean (0 errors in spacetimedb/src); full integration suite 59/60 files green + 1 skipped (616 tests + 7 todo). Pre-publish gate unblocked.**

## Performance

- **Duration:** ~73 minutes
- **Started:** 2026-04-18T00:15:09Z
- **Completed:** 2026-04-18T01:28:13Z
- **Tasks:** 6 (5 migration tasks + 1 verification task)
- **Files modified:** 11 (draftClassic.ts + draftControl.ts listed in files_modified were left untouched — see D-16-02)
- **Commits:** 5 atomic refactor commits + 1 upcoming docs commit

## Accomplishments

### Per-file breakdown (delta as any)

| File | Before | After | Delta | What |
|------|--------|-------|-------|------|
| `spacetimedb/src/reducers/bracketGeneration.ts` | 10 | 3 | **-7** | 6 P2 updates + 1 composite-PK row-object delete |
| `spacetimedb/src/reducers/draftAuction.ts` | 19 | 17 | **-2** | 2 `payload.value as NominatePayload` narrow casts |
| `spacetimedb/src/reducers/identityGc.ts` | 2 | 0 | **-2** | 2 scheduled-job insert unnecessary as-any dropped |
| `spacetimedb/src/reducers/lobbyGc.ts` | 2 | 0 | **-2** | 2 scheduled-job insert unnecessary as-any dropped |
| `spacetimedb/src/reducers/lobbySettings.ts` | 3 | 2 | **-1** | P5 upsert split into helper if/else |
| `spacetimedb/src/reducers/seriesManagement.ts` | 7 | 6 | **-1*** | *4 fixable sites migrated + 1 Rule 1 Fix. Delta -1 because `senderType: { tag, value } as any` moves INTO the insertWithAudit payload per D-16 (inner tagged-enum cast stays). |
| `spacetimedb/src/reducers/profile.ts` | 1 | 0 | **-1** | TournamentTeam inline manual audit → updateWithAudit |
| `spacetimedb/src/reducers/postDraft.ts` | 18 | 17 | **-1** | `(costs as any)[s]` → `(costs as Record<string, number>)[s]` |
| `spacetimedb/src/reducers/admin.ts` | 8 | 7 | **-1** | mergeForUpdate internal narrow cast `as T[K]` |
| `spacetimedb/src/reducers/matchResultSubmission.ts` | 5 | 3 | **-2** | Partial + status narrow casts |
| `spacetimedb/src/reducers/tournamentLobby.ts` | 5 | 4 | **-1** | matchType `as Lobby['matchType']` |
| **Total** | | | **-21** | |

Actual delta -21, not the plan-stated -38. Cause: plan's per-file counts included ~14 sites already migrated in Wave 1 Plan 07 (Cluster G draft family) that only had tagged-enum inner casts left (D-16 preserved). See D-16-01.

### auditInsert canon (Plan 13 Option α)

Before Plan 16: 9 permanent auditInsert primitive callers across 4 files.
After Plan 16: **9** — unchanged. Plan 16 introduced zero new auditInsert callers (the P5 split at lobbySettings used helper-pair if/else instead of growing the canon — see D-16-04).

### Integration verification

- **Typecheck:** 0 errors in `spacetimedb/src`. 35 `test/backend/**` baseline errors unchanged per Plan 01 deferred-items.md (requireOwnership missing from fixtures + draft-control `lobby` possibly undefined narrowings).
- **Full integration suite (Task 6 gate):**
  - Test Files: 59 passed + 1 skipped = 60 total
  - Tests: 616 passed + 7 todo = 623 total
  - Duration: 3799.83s (~63 min)
  - Exit code 0
- **Suite file count (60) vs plan's "71":** The plan's 71 figure referenced a pre-Plan-14 baseline. Wave 4 Plan 14's todo conversions + the 1 file consistently skipped produce the current 60. All 59 non-skipped test files pass cleanly; the 7 `it.todo` entries are the Plan 14 Wave 4 conversions (auth 6 + brackets 1).

## Task Commits

1. **Task 1: Migrate bracketGeneration P2 + composite-PK delete (7 sites)** — `709413a` (refactor)
2. **Task 2: Narrow-cast draftAuction value accessors (2 sites)** — `be4bd99` (refactor)
3. **Task 3: Drop unnecessary as-any on scheduled + LobbyPassword upsert (5 sites)** — `316160c` (refactor)
4. **Task 4: Migrate series+profile+post-draft family (6 sites + 1 Rule 1 fix)** — `9d322f4` (refactor)
5. **Task 5: Narrow-cast trifecta on mergeForUpdate+enum fields (4 sites)** — `9715e63` (refactor)
6. **Task 6: Final verification gate (typecheck + full integration)** — no code changes; results in this SUMMARY

## Decisions Made

See frontmatter `key-decisions` array (D-16-01 through D-16-09). Summary:

- **D-16-01:** Plan's per-file site counts were stale after Wave 1 Plan 07; actual Plan-16-fixable count = 24, not 38.
- **D-16-02:** draftClassic.ts + draftControl.ts left untouched — all residual `as any` are tagged-enum literals inside already-migrated payloads (D-16 canon).
- **D-16-03:** Scheduled-job inserts don't migrate to insertWithAudit (no audit columns on their tables); the outer `as any` simply drops.
- **D-16-04:** lobbySettings LobbyPassword P5 split into helper-pair if/else — Plan 13 Option-α canon of 9 preserved.
- **D-16-05:** Rule 1 fix: seriesManagement initialTimerState had 2 nonexistent fields (`pausedAt`, `pauseRemainingMs`) — corrected to real schema per draftClassic canonical shape.
- **D-16-06:** admin.ts L107 mergeForUpdate internal cast retires Plan 12 D-03b exclusion — now narrow `as T[K]`.
- **D-16-07:** 3-field-access narrow casts use module_bindings index-access types (e.g., `Lobby['matchType']`) rather than imported enum types to keep import lists lean.
- **D-16-08:** Zero tagged-enum `{tag, value} as any` literals added or modified. D-16 canon preserved.
- **D-16-09:** Stray bundle.js rebuild from vitest harness reverted per Plans 03/04/05/06/13/14 precedent — Plan 15 owns the bundle commit.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] seriesManagement.ts advance_to_next_game initialTimerState schema drift**
- **Found during:** Task 4 typecheck after adding `TimerState` annotation
- **Issue:** Code wrote `{ isPaused, pausedAt: undefined, pauseRemainingMs: 0, turnStartAt }` — but TimerState schema has `{ turnStartAt, teamBlueReserveMs, teamRedReserveMs, isPaused, accumulatedPauseMs }`. Two fields (`pausedAt`, `pauseRemainingMs`) did not exist on the type; three required fields were missing. The outer `as any` had suppressed this schema mismatch since the last TimerState refactor.
- **Fix:** Replaced with the canonical shape from draftClassic.ts L124-130 start_draft initial timer state: `{ turnStartAt: ctx.timestamp, teamBlueReserveMs: lobby.reserveBankSeconds * 1000, teamRedReserveMs: lobby.reserveBankSeconds * 1000, isPaused: false, accumulatedPauseMs: 0 }`.
- **Files modified:** spacetimedb/src/reducers/seriesManagement.ts L90-96
- **Verification:** typecheck 0 errors in spacetimedb/src after fix; full integration suite pass includes match-session + draft-control + finalization flows exercising the BetweenGames→Drafting transition.
- **Committed in:** 9d322f4 (folded into Task 4 commit)

### Scope clarifications (not deviations)

**1. Plan's per-file site counts were stale after Wave 1.** Files like draftClassic.ts (plan: "4 P1 inserts to migrate") were 100% already migrated — remaining `as any` were all D-16 tagged-enum literals inside helper payloads. Similarly draftControl.ts ("2 P1 inserts"). No migration was needed on those 2 files. Stayed inside files_modified frontmatter; committed only touched files. Documented in D-16-01 + D-16-02.

**2. Plan's "38 `as any` target" was inferred from stale counts.** Real-world Plan-16-fixable count = 24 (per file-by-file grep at execution start). Actual delta -21 because some fixable-labeled sites were secondary (e.g., inner tagged-enum casts that moved into a helper payload and stay). Plan's spirit — close the Wave 4b real-miss gap, preserve D-16/P4/P5 canon — fully achieved.

**3. scheduled-job insert transform is distinct from P1 insertWithAudit.** Plan's "Task 3 — 5 P1 inserts" lumped 4 scheduled-job inserts with 1 true P1 (lobbySettings LobbyPassword). Discovered during Task 3 execution: scheduled-job tables (identity_gc_job + lobby_gc_job) have no audit columns, so `insertWithAudit` would inject 4 extraneous fields. Fix: simply drop the unnecessary `as any` on scheduled-job inserts; use helpers only for LobbyPassword. Result: same 5-site count, different transforms per site.

**4. Plan-integration-suite count "71 files".** Actual current baseline is 60. The 71 figure pre-dated Plan 14's todo conversions. All 59 non-skipped files pass cleanly — integration gate intent met.

## Issues Encountered

- **TimerState schema drift** (Rule 1, documented above). Fix window: ~2 minutes (direct diagnostic from TS error + grep for canonical analog).
- **Plan's stale per-file counts** forced per-file grep verification at Task start; no fix needed — just careful tracking of what was actually fixable vs. what Plan 16 over-counted. Zero scope creep outside files_modified.
- **Windows bash background-command quirks** during integration test poll — `until` loops in `bash -c` context on this shell were backgrounded rather than blocking. Switched to foreground `for`-loop polling with periodic size checks. Tests completed cleanly in 63 min (exit code 0).

## User Setup Required

None — pure backend type-hygiene refactor, no external configuration, no schema changes, no binding regeneration needed, no publish step.

## Next Phase Readiness

**Plan 15 (Wave 5 maincloud publish) unblocked.** All Wave 1-4 + Wave 2b + Wave 3 + Wave 4b refactor changes are now:
- Typecheck clean (0 errors in spacetimedb/src)
- Integration-suite green (59/60 files, 1 skipped, 616/616 runnable tests pass)
- auditInsert canon preserved (exactly 9 Option-α permanent retention sites)
- Zero new tagged-enum literals added to the SDK-tagged-union out-of-scope bucket
- bundle.js left unstaged for Plan 15's exclusive ownership

Plan 15's pre-publish gate will re-run typecheck + integration as its first step (same gates Plan 16 just cleared); publish can proceed if maincloud-auth is still valid.

**Blockers:** None. Pre-existing `test/backend/**` typecheck errors (35, unchanged) remain orthogonal to any Phase 15.3 plan and tracked in deferred-items.md (Plan 01).

**Residual Phase 15.3 `as any` count in spacetimedb/src: 189**
- ~138 tagged-enum `{tag, value} as any` literals (SDK friction — explicitly OOS per D-03b)
- ~28 P4/P5/Pitfall-6 preserved sites (costSetManagement + finalizationHelpers conditional ternaries + server.ts Pitfall-6 + scoreEntry P4 + admin L522/L570 P4 + characterStatsIncrement/statsIncrement/globalCharacterStatsIncrement P4 + rosterAdmin/rosterMutations P5 — all architectural, out-of-scope per plan-level canon)
- ~4 CORRECT LobbyMemberAccount ephemeral-join casts in lobbyLifecycle (table has no audit columns by design)
- ~19 other narrow-as-any sites (tagged-enum inner casts inside already-migrated helper payloads, SDK-type-handle gaps, etc.) — these will persist into future phases and require separate SDK-coupled work

## Self-Check: PASSED

Verified all artifact claims below:

- ✅ File `.planning/phases/15.3-audit-spread-type-helper/15.3-16-SUMMARY.md` exists (this file — FOUND after write)
- ✅ Commit `709413a` exists in git log — Task 1 bracketGeneration
- ✅ Commit `be4bd99` exists in git log — Task 2 draftAuction narrow casts
- ✅ Commit `316160c` exists in git log — Task 3 scheduled + LobbyPassword
- ✅ Commit `9d322f4` exists in git log — Task 4 series/profile/post-draft + Rule 1 fix
- ✅ Commit `9715e63` exists in git log — Task 5 narrow-cast trifecta
- ✅ File `spacetimedb/src/reducers/bracketGeneration.ts` modified — 7 sites migrated, as-any 10→3
- ✅ File `spacetimedb/src/reducers/draftAuction.ts` modified — 2 narrow casts, NominatePayload import added
- ✅ File `spacetimedb/src/reducers/identityGc.ts` modified — 2 as-any dropped
- ✅ File `spacetimedb/src/reducers/lobbyGc.ts` modified — 2 as-any dropped
- ✅ File `spacetimedb/src/reducers/lobbySettings.ts` modified — P5 split into helper if/else
- ✅ File `spacetimedb/src/reducers/seriesManagement.ts` modified — 4 sites + Rule 1 fix, TimerState import added
- ✅ File `spacetimedb/src/reducers/profile.ts` modified — 1 site migrated
- ✅ File `spacetimedb/src/reducers/postDraft.ts` modified — 1 narrow cast
- ✅ File `spacetimedb/src/reducers/admin.ts` modified — 1 mergeForUpdate internal cast narrowed
- ✅ File `spacetimedb/src/reducers/matchResultSubmission.ts` modified — 2 narrow casts, MatchResultRecord import added
- ✅ File `spacetimedb/src/reducers/tournamentLobby.ts` modified — 1 narrow cast, Lobby import added
- ✅ `grep -rn "as any" spacetimedb/src --include="*.ts" | wc -l` returns 189 (target ~172 per plan; actual 189 due to stale counts in plan — see D-16-01)
- ✅ `grep -rn "auditInsert(" spacetimedb/src --include="*.ts" | grep -v "auditColumns.ts" | grep -v "auditHelpers.ts" | wc -l` returns 9 (Plan 13 Option-α canon preserved)
- ✅ `npm run test:typecheck` — 0 errors in spacetimedb/src; 35 baseline errors in test/backend (unchanged)
- ✅ `npm run test:integration` — 59 Test Files passed + 1 skipped; 616 Tests passed + 7 todo; exit code 0
- ✅ Stray dist/bundle.js rebuild reverted (Plan 15 owns the bundle commit per Plans 03/04/05/06/13/14 precedent)

---
*Phase: 15.3-audit-spread-type-helper*
*Completed: 2026-04-18*
*Wave 4b COMPLETE: 21 `as any` eliminated (210→189) across 11 files. Plan 13 Option-α canon preserved (9 auditInsert callers). Pre-publish gate (typecheck + integration) clean. Plan 15 Wave 5 maincloud publish unblocked.*
