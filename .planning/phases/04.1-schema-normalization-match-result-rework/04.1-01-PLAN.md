---
phase: 04.1-schema-normalization-match-result-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/types/enums.ts
  - spacetimedb/src/tables/matchResult.ts
  - spacetimedb/src/tables/matchResultParticipant.ts
  - spacetimedb/src/tables/matchResultGame.ts
  - spacetimedb/src/tables/bracketMatch.ts
  - spacetimedb/src/tables/groupStanding.ts
  - spacetimedb/src/tables/matchSessionHistory.ts
  - spacetimedb/src/tables/matchSessionStepHistory.ts
  - spacetimedb/src/tables/mmrHistory.ts
  - spacetimedb/src/tables/playerStats.ts
  - spacetimedb/src/tables/characterStats.ts
  - spacetimedb/src/tables/playerRelationship.ts
  - spacetimedb/src/tables/matchParticipantHistory.ts
  - spacetimedb/src/schema.ts
  - spacetimedb/src/helpers/bracketGeneration.ts
autonomous: true
requirements: [NORM-01, NORM-02, NORM-03]

must_haves:
  truths:
    - "MatchOutcome enum exists with variants BlueWins, RedWins, Draw, Aborted; MatchResult enum no longer exists"
    - "MatchType enum exists with variants Casual, Ranked, Tournament"
    - "PlayerSnapshot struct is KEPT in structs.ts (user decision -- will be used when history archival is implemented)"
    - "All 10 table definitions compile with new column names, PKs, and types"
    - "Two new tables (PlayerRelationship, MatchParticipantHistory) are defined and registered in schema.ts"
    - "BracketMatchDescriptor interface uses team1Id/team2Id/winnerTeamId field names"
  artifacts:
    - path: "spacetimedb/src/types/enums.ts"
      provides: "MatchOutcome and MatchType enums"
      contains: "MatchOutcome|MatchType"
    - path: "spacetimedb/src/tables/playerRelationship.ts"
      provides: "PlayerRelationship table definition"
      exports: ["PlayerRelationship", "playerRelationshipColumns"]
    - path: "spacetimedb/src/tables/matchParticipantHistory.ts"
      provides: "MatchParticipantHistory table definition"
      exports: ["MatchParticipantHistory", "matchParticipantHistoryColumns"]
    - path: "spacetimedb/src/schema.ts"
      provides: "Updated schema with PlayerStat, PlayerCharacterStat, PlayerRelationship, MatchParticipantHistory"
  key_links:
    - from: "spacetimedb/src/tables/matchSessionHistory.ts"
      to: "spacetimedb/src/types/enums.ts"
      via: "import MatchOutcome"
      pattern: "import.*MatchOutcome.*from.*enums"
    - from: "spacetimedb/src/tables/matchResult.ts"
      to: "spacetimedb/src/types/enums.ts"
      via: "import MatchType"
      pattern: "import.*MatchType.*from.*enums"
    - from: "spacetimedb/src/schema.ts"
      to: "spacetimedb/src/tables/playerRelationship.ts"
      via: "import and register"
      pattern: "PlayerRelationship"
---

<objective>
Define all type-level and table-level schema changes for Phase 04.1. This is the foundation layer: enums, all 10 modified table definitions, 2 new tables, schema registration, and the BracketMatchDescriptor helper interface rename.

Purpose: Establish the new schema contracts that Plan 02 (reducers) and Plan 03 (docs) will build against. TypeScript will have errors in reducer files after this plan (expected -- fixed in Plan 02).
Output: All table/type/interface definitions updated. New tables created and registered. Helper interface renamed.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-schema-normalization-match-result-rework/04.1-CONTEXT.md
@.planning/phases/04.1-schema-normalization-match-result-rework/04.1-RESEARCH.md

Load the spacetimedb skill before executing any code changes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enum changes and new table definitions</name>
  <files>
    spacetimedb/src/types/enums.ts
    spacetimedb/src/tables/playerRelationship.ts
    spacetimedb/src/tables/matchParticipantHistory.ts
  </files>
  <read_first>
    spacetimedb/src/types/enums.ts
    .planning/phases/04.1-schema-normalization-match-result-rework/04.1-RESEARCH.md (Change 9, 10, 13, 14 sections)
  </read_first>
  <action>
**enums.ts changes:**
1. Rename `MatchResult` enum to `MatchOutcome` (line 73-78). Change:
   - `export const MatchResult = t.enum('MatchResult', {` to `export const MatchOutcome = t.enum('MatchOutcome', {`
   - Keep all 4 variants unchanged: BlueWins, RedWins, Draw, Aborted

2. Add new `MatchType` enum AFTER the MatchOutcome enum:
   ```typescript
   export const MatchType = t.enum('MatchType', {
       Casual: t.unit(),
       Ranked: t.unit(),
       Tournament: t.unit(),
   });
   ```

**DO NOT modify structs.ts.** PlayerSnapshot struct is KEPT per user decision -- it will be used when history archival is implemented.

**NEW FILE: spacetimedb/src/tables/playerRelationship.ts:**
3. Create with this exact content:
   ```typescript
   import { table, t } from 'spacetimedb/server';
   import { GameMode, DraftMode } from '../types/enums';

   export const playerRelationshipColumns = {
       userId: t.u32(),
       otherUserId: t.u32(),
       gameMode: GameMode,
       draftMode: DraftMode,
       matchesAsAlly: t.u32(),
       winsAsAlly: t.u32(),
       matchesAsOpponent: t.u32(),
       winsAsOpponent: t.u32(),
       createdById: t.u32(),
       createdDate: t.timestamp(),
       lastModifiedById: t.u32(),
       lastModifiedDate: t.timestamp(),
   };

   export const PlayerRelationship = table({
       name: 'player_relationship',
       public: true,
       primaryKey: ['userId', 'otherUserId', 'gameMode', 'draftMode'],
       indexes: [
           { accessor: 'by_user', algorithm: 'btree', columns: ['userId'] },
           { accessor: 'by_user_and_other_mode_draft', algorithm: 'btree', columns: ['userId', 'otherUserId', 'gameMode', 'draftMode'] },
       ],
   }, playerRelationshipColumns);
   ```

**NEW FILE: spacetimedb/src/tables/matchParticipantHistory.ts:**
4. Create with this exact content:
   ```typescript
   import { table, t } from 'spacetimedb/server';
   import { TeamLabel } from '../types/enums';

   export const matchParticipantHistoryColumns = {
       userId: t.u32(),
       matchHistoryId: t.u32(),
       teamSide: TeamLabel,
       createdById: t.u32(),
       createdDate: t.timestamp(),
       lastModifiedById: t.u32(),
       lastModifiedDate: t.timestamp(),
   };

   export const MatchParticipantHistory = table({
       name: 'match_participant_history',
       public: true,
       primaryKey: ['userId', 'matchHistoryId'],
       indexes: [
           { accessor: 'by_user', algorithm: 'btree', columns: ['userId'] },
           { accessor: 'by_match_history', algorithm: 'btree', columns: ['matchHistoryId'] },
           { accessor: 'by_user_and_match', algorithm: 'btree', columns: ['userId', 'matchHistoryId'] },
       ],
   }, matchParticipantHistoryColumns);
   ```
  </action>
  <verify>
    <automated>grep -c "MatchOutcome" spacetimedb/src/types/enums.ts && grep -c "MatchType" spacetimedb/src/types/enums.ts && grep -c "MatchResult" spacetimedb/src/types/enums.ts && grep -c "PlayerSnapshot" spacetimedb/src/types/structs.ts && test -f spacetimedb/src/tables/playerRelationship.ts && test -f spacetimedb/src/tables/matchParticipantHistory.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "MatchOutcome" enums.ts returns 1+ matches (enum exists)
    - grep "MatchType" enums.ts returns 1+ matches (enum exists)
    - grep "MatchResult" enums.ts returns 0 matches (old enum removed)
    - grep "PlayerSnapshot" structs.ts returns 1+ matches (struct KEPT per user decision)
    - playerRelationship.ts exists with "player_relationship" table name and composite PK ['userId', 'otherUserId', 'gameMode', 'draftMode']
    - matchParticipantHistory.ts exists with "match_participant_history" table name and composite PK ['userId', 'matchHistoryId']
  </acceptance_criteria>
  <done>MatchResult renamed to MatchOutcome, MatchType enum added, PlayerSnapshot KEPT in structs.ts, two new table files created</done>
</task>

<task type="auto">
  <name>Task 2: All table definition updates and schema registration</name>
  <files>
    spacetimedb/src/tables/matchResult.ts
    spacetimedb/src/tables/matchResultParticipant.ts
    spacetimedb/src/tables/matchResultGame.ts
    spacetimedb/src/tables/bracketMatch.ts
    spacetimedb/src/tables/groupStanding.ts
    spacetimedb/src/tables/matchSessionHistory.ts
    spacetimedb/src/tables/matchSessionStepHistory.ts
    spacetimedb/src/tables/mmrHistory.ts
    spacetimedb/src/tables/playerStats.ts
    spacetimedb/src/tables/characterStats.ts
    spacetimedb/src/schema.ts
    spacetimedb/src/helpers/bracketGeneration.ts
  </files>
  <read_first>
    spacetimedb/src/tables/matchResult.ts
    spacetimedb/src/tables/matchResultParticipant.ts
    spacetimedb/src/tables/matchResultGame.ts
    spacetimedb/src/tables/bracketMatch.ts
    spacetimedb/src/tables/groupStanding.ts
    spacetimedb/src/tables/matchSessionHistory.ts
    spacetimedb/src/tables/matchSessionStepHistory.ts
    spacetimedb/src/tables/mmrHistory.ts
    spacetimedb/src/tables/playerStats.ts
    spacetimedb/src/tables/characterStats.ts
    spacetimedb/src/schema.ts
    spacetimedb/src/helpers/bracketGeneration.ts
    .planning/phases/04.1-schema-normalization-match-result-rework/04.1-RESEARCH.md (Changes 1-8, 11-12 sections)
  </read_first>
  <action>
**matchResult.ts (Change 1):**
- Import `MatchType` from enums: add `MatchType` to the import line alongside `MatchResultStatus`
- REMOVE columns: `player1Id`, `player2Id`, `team1Confirmed`, `team2Confirmed`
- RENAME `isTournamentMatch: t.bool()` to `isTournamentControlled: t.bool()`
- RENAME `winnerId: t.u32().optional()` to `winnerUserId: t.u32().optional()`
- CHANGE `matchType: t.u8()` to `matchType: MatchType`
- REMOVE indexes: `player_1_id` and `player_2_id` (those columns no longer exist)
- Keep remaining indexes: `lobby_id`, `tournament_id`

Final matchResultColumns should have these columns (in order): id, bracketMatchId, lobbyId, isTournamentControlled, status, winnerUserId, mmrProcessedAt, refereeUserId, disputedByUserId, disputeReason, tournamentId, matchType, createdById, createdDate, lastModifiedById, lastModifiedDate

**matchResultParticipant.ts (Change 2):**
- ADD `isCaptain: t.bool()` after teamSide
- ADD `isConfirmed: t.bool()` after isCaptain
- No other changes (indexes stay the same)

**matchResultGame.ts (Change 3):**
- ADD import: `import { GameMode, ValidationStatus, TeamLabel } from '../types/enums';` (add TeamLabel)
- Rename all 12 player* columns to teamBlue*/teamRed*:
  - `player1ScreenshotUrl` -> `teamBlueScreenshotUrl`
  - `player2ScreenshotUrl` -> `teamRedScreenshotUrl`
  - `player1CyclesUsed` -> `teamBlueCyclesUsed`
  - `player2CyclesUsed` -> `teamRedCyclesUsed`
  - `player1Score` -> `teamBlueScore`
  - `player2Score` -> `teamRedScore`
  - `player1Boss1Score` -> `teamBlueBoss1Score`
  - `player1Boss2Score` -> `teamBlueBoss2Score`
  - `player2Boss1Score` -> `teamRedBoss1Score`
  - `player2Boss2Score` -> `teamRedBoss2Score`
- CHANGE `winnerId: t.u32().optional()` to `winnerTeamSide: TeamLabel` (NOT optional -- TeamLabel enum)
  Wait, check CONTEXT.md: "winnerId (u32 optional) -> winnerTeamSide (TeamLabel enum)". The TeamLabel is an enum with Spectator/Blue/Red. Since the game may not have a winner yet, it should remain optional. Use `winnerTeamSide: t.option(TeamLabel)` pattern. Actually, looking at SpacetimeDB patterns in this codebase, optional enums are NOT used with t.option(). They use the enum directly and rely on the enum having a "none" variant or leaving it undefined. Since TeamLabel has Spectator/Blue/Red but no "None" variant, and the column was previously optional, make it: `winnerTeamSide: TeamLabel` (required -- the game must have a result by the time this row is written). Actually, re-reading the CONTEXT: the original was `winnerId: t.u32().optional()`. The RESEARCH says "Change winnerId (u32 optional) to winnerTeamSide (TeamLabel)". The column type changes from optional u32 to TeamLabel enum. Since per-game winner is determined at game recording time, making it required is correct. Use just `winnerTeamSide: TeamLabel`.
- RENAME `validatedById` -> `validatedByUserId` (same type: t.u32().optional())

**bracketMatch.ts (Change 4):**
- RENAME `participant1Id: t.u32().optional()` -> `team1Id: t.u32().optional()`
- RENAME `participant2Id: t.u32().optional()` -> `team2Id: t.u32().optional()`
- RENAME `winnerId: t.u32().optional()` -> `winnerTeamId: t.u32().optional()`
- No index changes needed (indexes reference column names by string, and the existing indexes use 'tournamentId' and 'lobbyId' which are unchanged)

**groupStanding.ts (Change 5):**
- RENAME `participantTeamId: t.u32()` -> `teamId: t.u32()`
- UPDATE primaryKey array: `['tournamentId', 'groupId', 'participantTeamId']` -> `['tournamentId', 'groupId', 'teamId']`
- UPDATE index `by_tournament_group_and_team` columns: `['tournamentId', 'groupId', 'participantTeamId']` -> `['tournamentId', 'groupId', 'teamId']`

**matchSessionHistory.ts (Change 6):**
- CHANGE `id: t.string().primaryKey()` to `id: t.u32().primaryKey().autoInc()`
- REMOVE `blueTeamMembers: t.array(PlayerSnapshot)` column
- REMOVE `redTeamMembers: t.array(PlayerSnapshot)` column
- REMOVE import of `PlayerSnapshot` from `../types/structs` (line 3). Update import to only import `LobbyConfigSnapshot`: `import { LobbyConfigSnapshot } from '../types/structs';`
- RENAME `result: MatchResult` to `outcome: MatchOutcome`
- UPDATE import: change `MatchResult` to `MatchOutcome` in the enums import line: `import { DraftMode, GameMode, MatchOutcome } from '../types/enums';`
- NOTE: PlayerSnapshot stays in structs.ts (user decision). Only its IMPORT is removed from matchSessionHistory.ts because blueTeamMembers/redTeamMembers columns are removed.

**matchSessionStepHistory.ts (Change 7):**
- RENAME `matchId: t.string().primaryKey()` to `matchHistoryId: t.u32().primaryKey()`
- Note: NOT autoInc -- this is an FK to MatchSessionHistory.id, so the value is set explicitly

**mmrHistory.ts (Change 8):**
- RENAME column `matchResultId: t.u32()` to `matchHistoryId: t.u32()`
- UPDATE index: rename accessor from `match_result_id` to `match_history_id`, columns from `['matchResultId']` to `['matchHistoryId']`

**playerStats.ts (Change 11):**
- RENAME export: `playerStatsColumns` to `playerStatColumns`, `PlayerStats` to `PlayerStat`
- CHANGE table name: `'player_stats'` to `'player_stat'`
- CHANGE PK from `userId: t.u32().primaryKey()` to `userId: t.u32()` (no longer single-column PK)
- ADD composite primaryKey: `primaryKey: ['userId', 'gameMode', 'draftMode']`
- ADD columns after userId: `gameMode: GameMode` and `draftMode: DraftMode`
- ADD imports: `import { GameMode, DraftMode } from '../types/enums';`
- REMOVE columns: `bestAllyUserId: t.u32().optional()` and `nemesisUserId: t.u32().optional()`
- REPLACE indexes: remove `wins` index, add:
  ```
  { accessor: 'by_user', algorithm: 'btree', columns: ['userId'] },
  { accessor: 'by_user_mode_draft', algorithm: 'btree', columns: ['userId', 'gameMode', 'draftMode'] },
  ```

**characterStats.ts (Change 12):**
- RENAME export: `characterStatsColumns` to `playerCharacterStatColumns`, `CharacterStats` to `PlayerCharacterStat`
- CHANGE table name: `'character_stats'` to `'player_character_stat'`
- EXPAND PK: `['userId', 'characterName']` to `['userId', 'characterName', 'gameMode', 'draftMode']`
- ADD columns after characterName: `gameMode: GameMode` and `draftMode: DraftMode`
- ADD imports: `import { GameMode, DraftMode } from '../types/enums';`
- UPDATE indexes:
  - Keep `by_user` as-is: `{ accessor: 'by_user', algorithm: 'btree', columns: ['userId'] }`
  - Rename `by_user_and_character` to `by_user_and_character_mode_draft`: `{ accessor: 'by_user_and_character_mode_draft', algorithm: 'btree', columns: ['userId', 'characterName', 'gameMode', 'draftMode'] }`

**schema.ts (Change registrations):**
- UPDATE import on line 54: `import { PlayerStats } from './tables/playerStats';` to `import { PlayerStat } from './tables/playerStats';`
- UPDATE import on line 55: `import { CharacterStats } from './tables/characterStats';` to `import { PlayerCharacterStat } from './tables/characterStats';`
- ADD two new imports after line 47 (MatchResultParticipant import):
  ```typescript
  import { PlayerRelationship } from './tables/playerRelationship';
  import { MatchParticipantHistory } from './tables/matchParticipantHistory';
  ```
- UPDATE schema registration (line 145): `PlayerStats,` to `PlayerStat,`
- UPDATE schema registration (line 146): `CharacterStats,` to `PlayerCharacterStat,`
- ADD to schema registration in appropriate sections:
  - After MatchResultParticipant: `PlayerRelationship,`
  - In the History section after MatchSessionStepHistory: `MatchParticipantHistory,`

**helpers/bracketGeneration.ts (BracketMatchDescriptor interface):**
- RENAME interface fields (lines 14-18):
  - `participant1Id?: number;` to `team1Id?: number;`
  - `participant2Id?: number;` to `team2Id?: number;`
  - `winnerId?: number;` to `winnerTeamId?: number;`
- Update comments: `// TournamentTeam.id` stays, `// Pre-set for BYE matches` stays
- RENAME all usages of these field names within the SAME FILE (the helper functions that build descriptors):
  - Every `participant1Id:` assignment in descriptor objects -> `team1Id:`
  - Every `participant2Id:` assignment in descriptor objects -> `team2Id:`
  - Every `winnerId` reference in descriptor objects -> `winnerTeamId`
  - This affects generateSingleElimBracket, generateDoubleElimBracket, generateGroupPhaseBracket, generateHybridBracket
  - Search for ALL instances with word boundaries. Expected count: ~25-30 instances across the file.
  </action>
  <verify>
    <automated>grep -c "player1Id\|player2Id\|team1Confirmed\|team2Confirmed" spacetimedb/src/tables/matchResult.ts && grep -c "isTournamentControlled" spacetimedb/src/tables/matchResult.ts && grep -c "winnerUserId" spacetimedb/src/tables/matchResult.ts && grep -c "team1Id" spacetimedb/src/tables/bracketMatch.ts && grep -c "participant1Id" spacetimedb/src/tables/bracketMatch.ts && grep -c "teamId" spacetimedb/src/tables/groupStanding.ts && grep -c "participantTeamId" spacetimedb/src/tables/groupStanding.ts && grep -c "MatchOutcome" spacetimedb/src/tables/matchSessionHistory.ts && grep -c "PlayerSnapshot" spacetimedb/src/tables/matchSessionHistory.ts && grep -c "PlayerStat" spacetimedb/src/schema.ts && grep -c "PlayerCharacterStat" spacetimedb/src/schema.ts && grep -c "PlayerRelationship" spacetimedb/src/schema.ts && grep -c "MatchParticipantHistory" spacetimedb/src/schema.ts && grep -c "participant1Id\|participant2Id" spacetimedb/src/helpers/bracketGeneration.ts && echo "VERIFY_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - matchResult.ts: 0 matches for "player1Id|player2Id|team1Confirmed|team2Confirmed", 1+ for "isTournamentControlled", 1+ for "winnerUserId", 1+ for "MatchType"
    - bracketMatch.ts: 1+ matches for "team1Id", 0 for "participant1Id", 1+ for "winnerTeamId", 0 for bare "winnerId"
    - groupStanding.ts: 1+ matches for "teamId" (as column), 0 for "participantTeamId"
    - matchSessionHistory.ts: 1+ for "MatchOutcome", 0 for "MatchResult" (as import), 0 for "PlayerSnapshot", 0 for "blueTeamMembers", 0 for "redTeamMembers", id type is u32 autoInc
    - matchSessionStepHistory.ts: "matchHistoryId" present, "matchId" absent, type is u32 not string
    - mmrHistory.ts: "matchHistoryId" present, "matchResultId" absent, index accessor is "match_history_id"
    - playerStats.ts: export name is "PlayerStat" (not PlayerStats), table name is 'player_stat', PK is composite, no bestAllyUserId/nemesisUserId
    - characterStats.ts: export name is "PlayerCharacterStat", table name is 'player_character_stat', PK includes gameMode and draftMode
    - schema.ts: imports PlayerStat (not PlayerStats), imports PlayerCharacterStat (not CharacterStats), imports+registers PlayerRelationship and MatchParticipantHistory
    - matchResultParticipant.ts: has isCaptain and isConfirmed columns
    - matchResultGame.ts: all player* columns renamed to teamBlue*/teamRed*, winnerId replaced with winnerTeamSide, validatedById replaced with validatedByUserId
    - helpers/bracketGeneration.ts: BracketMatchDescriptor has team1Id/team2Id/winnerTeamId, 0 occurrences of participant1Id/participant2Id as field assignments
  </acceptance_criteria>
  <done>All 10 modified table definitions, 2 new tables, schema registration, and BracketMatchDescriptor interface updated with new column names and types</done>
</task>

</tasks>

<verification>
After both tasks:
- All table definitions compile with new column names
- schema.ts registers all tables including 2 new ones
- BracketMatchDescriptor interface uses team1Id/team2Id/winnerTeamId
- TypeScript will show errors in reducer files (expected -- Plan 02 fixes those)
- No remaining references to old names in table definitions or helper interface
- PlayerSnapshot struct remains in structs.ts (user decision)
</verification>

<success_criteria>
- Zero occurrences of participant1Id/participant2Id in any table definition file
- Zero occurrences of player1Id/player2Id/team1Confirmed/team2Confirmed in matchResult.ts
- Zero occurrences of PlayerSnapshot import in matchSessionHistory.ts (import removed, columns removed)
- PlayerSnapshot struct EXISTS in structs.ts (KEPT per user decision)
- Zero occurrences of MatchResult enum name in enums.ts (replaced by MatchOutcome)
- PlayerRelationship.ts and MatchParticipantHistory.ts exist with correct exports
- schema.ts imports and registers all renamed/new tables
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-schema-normalization-match-result-rework/04.1-01-SUMMARY.md`
</output>
