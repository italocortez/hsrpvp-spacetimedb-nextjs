---
phase: 15
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - spacetimedb/src/reducers/admin.ts
autonomous: true
requirements:
  - FOUND-01
tags: [backend, spacetimedb, reducers, admin, partial-update]

must_haves:
  truths:
    - "`admin_bulk_upsert` preserves every unsent-value field on an existing row for all 5 table cases (HsrCharacter, HsrLightcone, HsrCharacterCost, HsrSynergyCost, HsrLightconeCost)"
    - "`HsrCharacterCost` existing-row match uses the full `(characterName, gameMode, costSetId)` tuple — distinct `costSetId`s never collide"
    - "`HsrLightconeCost` existing-row match uses the full `(lightconeName, gameMode, costSetId)` tuple"
    - "`HsrSynergyCost` existing-row match uses `(sourceName, targetName, gameMode, costSetId)`"
    - "Spine + positioning columns on `HsrCharacter` are editable through the existing router — no new reducer"
    - "Insert branch still applies schema defaults for required columns; partial-update semantic kicks in ONLY on existing rows"
    - "JSON wire convention: every key required by `EXPECTED_KEYS` must be present; value `null` means 'preserve existing on update' and 'apply default on insert'"
  artifacts:
    - path: "spacetimedb/src/reducers/admin.ts"
      provides: "Reworked admin_bulk_upsert with partial-update semantics across 5 table cases + costSetId tuple fixes"
      contains: "EXPECTED_KEYS"
  key_links:
    - from: "spacetimedb/src/reducers/admin.ts#HsrCharacter case"
      to: "spacetimedb/src/tables/hsrCharacter.ts (Plan 02 columns)"
      via: "merged update path preserving existing values"
      pattern: "if \\(r\\.skelUrl !== null\\) merged\\.skelUrl = r\\.skelUrl"
    - from: "spacetimedb/src/reducers/admin.ts#HsrCharacterCost case"
      to: "spacetimedb/src/tables/hsrCharacterCost.ts (by_character_mode_and_set index)"
      via: "composite PK tuple existence check"
      pattern: "r\\.costSetId"
---

<objective>
Rework `admin_bulk_upsert` in `spacetimedb/src/reducers/admin.ts` (L251 onward) across all 5 table cases so that (a) partial updates preserve every unsent-value field on existing rows instead of zeroing them, (b) `HsrCharacterCost` / `HsrLightconeCost` / `HsrSynergyCost` existence checks include `costSetId` in the match tuple, and (c) the Spine and positioning columns added in Plan 02 are editable through the existing router (no dedicated setter per D-11).

Purpose: Fix two latent bugs the user surfaced in discussion: (1) unsent fields get zeroed on update because the current reducer rebuilds the row with aggressive defaults then spreads over `existing`; (2) `HsrCharacterCost` with non-default `costSetId` is silently overwritten by seed runs. Close FOUND-01 editing half by making Spine + positioning ride the existing router.

Output: Single reworked `admin.ts` file; build + publish clean; `EXPECTED_KEYS` updated to include the 6 new HsrCharacter columns.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/skills/spacetimedb/SKILL.md
</execution_context>

<context>
@.planning/phases/15-backend-pre-work/15-CONTEXT.md
@.planning/phases/15-backend-pre-work/15-RESEARCH.md
@spacetimedb/src/reducers/admin.ts
@spacetimedb/src/tables/hsrCharacter.ts
@spacetimedb/src/tables/hsrLightcone.ts
@spacetimedb/src/tables/hsrCharacterCost.ts
@spacetimedb/src/tables/hsrLightconeCost.ts
@spacetimedb/src/tables/hsrSynergyCost.ts

**Load the `/spacetimedb` skill before writing code.**

<interfaces>
Partial-update wire convention (D-09/D-10 + RESEARCH.md §Admin router partial-update mechanics):

- `EXPECTED_KEYS['HsrCharacter']` et al. still demand every non-audit column be present in the incoming JSON (`validateKeys` contract, admin.ts:51-71 — do NOT relax this, per RESEARCH.md §Pitfall 3 and D-27).
- Partial-update is expressed in VALUES, not key presence:
  - `null` value → "preserve existing on update; apply schema default on insert".
  - Non-null value → "set this field to the given value".
- This convention MUST be documented in a block comment above `admin_bulk_upsert` and mirrored in the updated architecture doc (Plan 06).

Composite PK existence patterns:

- `HsrCharacterCost`: PK `[characterName, gameMode, costSetId]`; index `by_character_mode_and_set` exists (verified `hsrCharacterCost.ts:20`).
- `HsrLightconeCost`: PK `[lightconeName, gameMode, costSetId]`; same composite index shape.
- `HsrSynergyCost`: PK is auto-inc `id`; the existing-row match uses `(sourceName, targetName, gameMode)` and MUST add `costSetId` to the match tuple per D-09 generalisation.

Tuple-filter API fallback (RESEARCH.md A2): if `by_character_mode_and_set.filter([name, { tag, value: {} }, csId])` doesn't accept enum structs in composite filter tuples, fall back to `iter()` + full-triple equality check. At 82 chars × 3 modes × few cost sets, O(n) iter is acceptable.
</interfaces>

<convention>
- Insert branch keeps the current default-injection logic; partial-update applies ONLY to the update branch (D-12).
- All schema-required fields must be present with a non-null value on INSERT; for REQUIRED columns, `null` on insert gets a schema default (`''`, `0`, `[]`, PascalCase enum default).
- For OPTIONAL columns (`skelUrl`, `atlasUrl`): `null` on insert stays `null`; on update, `null` preserves existing.
- Audit helpers stay the same: `auditInsert(ctx, admin.id)` / `auditUpdate(ctx, existing, admin.id)`. Do not rewrite these.
- `validateEnum` continues to run for every field whose key is present AND non-null. Skip enum validation on `null` values.
- `costSetId` on cost tables: `r.costSetId ?? 0` — honor the sentinel so omitting it or sending `null` still means "default cost set".
</convention>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EXPECTED_KEYS + add partial-update merge helper</name>
  <files>spacetimedb/src/reducers/admin.ts</files>
  <read_first>
    spacetimedb/src/reducers/admin.ts (full file — full router structure),
    spacetimedb/src/reducers/admin.ts:42-49 (EXPECTED_KEYS map),
    spacetimedb/src/reducers/admin.ts:51-71 (validateKeys implementation),
    spacetimedb/src/reducers/admin.ts:251-454 (admin_bulk_upsert body),
    .planning/phases/15-backend-pre-work/15-RESEARCH.md (§Admin router partial-update mechanics)
  </read_first>
  <action>
    In `spacetimedb/src/reducers/admin.ts`:

    1. **Update `EXPECTED_KEYS['HsrCharacter']`** to include the 6 new keys added in Plan 02:
       - Add `'skelUrl'`, `'atlasUrl'`, `'atlasImgUrls'`, `'posX'`, `'posY'`, `'width'`.
       - Every other key stays. The set should match the non-audit columns of `HsrCharacter` exactly.

    2. **Leave `EXPECTED_KEYS['HsrLightcone']` / `EXPECTED_KEYS['HsrLightconeCost']` / `EXPECTED_KEYS['HsrCharacterCost']` / `EXPECTED_KEYS['HsrSynergyCost']` structurally as they are** — only add the `costSetId` key to `HsrSynergyCost` if it is not already listed there (check the current file; the synergy-cost existing check in the reducer currently ignores costSetId per D-09).

    3. **Add a partial-update merge helper function** near the top of the file (after `validateEnum`, before `admin_bulk_upsert`):

       ```typescript
       /**
        * Phase 15 D-08/D-10: Partial-update merge for the admin_bulk_upsert router.
        *
        * Wire convention:
        *   - Every EXPECTED_KEYS entry must appear in the incoming JSON row (validateKeys rule).
        *   - Value `null` on an EXISTING row = "preserve this field".
        *   - Value `null` on an INSERT row = "apply schema default" (required columns) OR "stay null" (optional columns).
        *   - Non-null value = "set to this value".
        *
        * This helper returns the merged row for UPDATE only. The insert branch stays on its current
        * default-injection path per D-12.
        */
       function mergeForUpdate<T extends Record<string, any>>(existing: T, incoming: Record<string, any>, fields: (keyof T)[]): T {
           const merged: T = { ...existing };
           for (const f of fields) {
               const key = f as string;
               if (incoming[key] !== null && incoming[key] !== undefined) {
                   (merged as any)[key] = incoming[key];
               }
               // null | undefined → preserve existing[f] (no-op on the spread).
           }
           return merged;
       }
       ```

    4. **Add a top-of-file block comment** above `admin_bulk_upsert` documenting the wire convention for downstream callers (seed scripts + future admin UI).

    Do NOT change `validateKeys` itself — per D-27 and RESEARCH.md Pitfall 3 we keep the strict key-set contract and express partial-update in values.
  </action>
  <verify>
    <automated>cd spacetimedb &amp;&amp; npm run build &amp;&amp; grep -c "'skelUrl'\|'atlasUrl'\|'atlasImgUrls'\|'posX'\|'posY'\|'width'" src/reducers/admin.ts</automated>
  </verify>
  <acceptance_criteria>
    - `EXPECTED_KEYS['HsrCharacter']` (or equivalent) in `admin.ts` contains all 6 new keys (`skelUrl`, `atlasUrl`, `atlasImgUrls`, `posX`, `posY`, `width`). Grep count ≥ 6.
    - `admin.ts` contains a function named `mergeForUpdate` (or equivalent single-purpose helper with the wire convention described).
    - `admin.ts` contains a block comment on the wire convention with the words "preserve" and "null" in the comment body.
    - `cd spacetimedb && npm run build` exits 0.
    - `validateKeys` function body is unchanged (no subset-allowed relaxation).
  </acceptance_criteria>
  <done>EXPECTED_KEYS extended for the 6 new HsrCharacter columns; `mergeForUpdate` helper + wire-convention comment added; build clean; no changes to `validateKeys` contract.</done>
</task>

<task type="auto">
  <name>Task 2: Rework 5 table cases in admin_bulk_upsert (HsrCharacter, HsrLightcone, HsrCharacterCost, HsrSynergyCost, HsrLightconeCost)</name>
  <files>spacetimedb/src/reducers/admin.ts</files>
  <read_first>
    spacetimedb/src/reducers/admin.ts:251-454 (admin_bulk_upsert full body),
    spacetimedb/src/reducers/admin.ts:267-285 (HsrCharacter case — target of D-08 fix),
    spacetimedb/src/reducers/admin.ts:349-370 (HsrCharacterCost case — target of D-09 fix),
    spacetimedb/src/reducers/admin.ts:380-387 (HsrLightconeCost case — D-09 generalisation),
    spacetimedb/src/reducers/admin.ts:416-423 (HsrSynergyCost case — D-09 generalisation),
    spacetimedb/src/tables/hsrCharacterCost.ts (PK + indexes — by_character_mode_and_set verified),
    spacetimedb/src/tables/hsrLightconeCost.ts (PK shape),
    spacetimedb/src/tables/hsrSynergyCost.ts (PK = auto-inc id; existence check uses sourceName/targetName/gameMode/costSetId per D-09),
    .planning/phases/15-backend-pre-work/15-RESEARCH.md (§HsrCharacterCost PK fix — concrete fix snippets)
  </read_first>
  <action>
    Rework each of the 5 `case` branches in `admin_bulk_upsert` to apply the partial-update convention on UPDATE and the costSetId-tuple fix on the three cost tables. Work one case at a time; keep the diff tight.

    **Case `HsrCharacter`:**

    - Fields to preserve on partial update (full list — use this verbatim in the `mergeForUpdate` call):
      `displayName`, `aliases`, `rarity`, `path`, `element`, `role`, `imageUrl`, `versionReleased`, `treatAsVersion`, `skelUrl`, `atlasUrl`, `atlasImgUrls`, `posX`, `posY`, `width`.
    - Existence check: `ctx.db.HsrCharacter.name.find(r.name)` (unchanged).
    - UPDATE branch: call `mergeForUpdate(existing, r, [<fields above>])`; then `ctx.db.HsrCharacter.name.update({ ...merged, ...auditUpdate(ctx, existing, admin.id) })`.
    - INSERT branch (new row): keep the existing default-injection logic but ADD the 6 new columns with defaults:
      - `skelUrl: r.skelUrl ?? null`, `atlasUrl: r.atlasUrl ?? null`, `atlasImgUrls: r.atlasImgUrls ?? []`, `posX: r.posX ?? 0`, `posY: r.posY ?? 0`, `width: r.width ?? 0`.
    - `validateEnum` calls: wrap each `validateEnum(r.path, ...)` / `validateEnum(r.element, ...)` / `validateEnum(r.role, ...)` so they run only when the value is non-null (null on update = preserve → skip validation).

    **Case `HsrLightcone`:**

    - Fields to preserve on partial update: `displayName`, `path`, `rarity`, `imageUrl`, `versionReleased`, `treatAsVersion`, `posX`, `posY`, `width` (exact list — check existing EXPECTED_KEYS and `hsrLightcone.ts` columns; include every non-audit column).
    - Existence check: `ctx.db.HsrLightcone.name.find(r.name)` (unchanged).
    - UPDATE branch: same `mergeForUpdate` pattern as HsrCharacter.
    - INSERT branch: keep current logic.
    - `validateEnum` on path only when non-null.

    **Case `HsrCharacterCost` (D-09 fix):**

    - Replace the existing-row lookup at `admin.ts:360-366` which does:
      ```typescript
      for (const e of ctx.db.HsrCharacterCost.iter()) {
          if (e.characterName === r.characterName && e.gameMode.tag === r.gameMode) { existing = e; break; }
      }
      ```
    - With a composite-tuple match that includes `costSetId`:
      ```typescript
      const csId = r.costSetId ?? 0;
      let existing: any = null;
      for (const e of ctx.db.HsrCharacterCost.iter()) {
          if (e.characterName === r.characterName &&
              e.gameMode.tag === r.gameMode &&
              e.costSetId === csId) {
              existing = e;
              break;
          }
      }
      ```
      (RESEARCH.md §HsrCharacterCost PK fix notes that the tuple-filter API with enum structs may not work — use the iter + triple-check fallback as the safe path. If the composite-index filter does work during execution, feel free to substitute `by_character_mode_and_set.filter(...)`; not required.)
    - Fields to preserve on partial update: `cost` (and any other non-PK cost columns — verify against `hsrCharacterCost.ts`).
    - UPDATE branch: `mergeForUpdate` pattern.
    - INSERT branch: use `csId` as the `costSetId` value for the new row.

    **Case `HsrLightconeCost`:**

    - Same shape as `HsrCharacterCost`. Existing-row match uses `(lightconeName, gameMode, costSetId)`.
    - ```typescript
      const csId = r.costSetId ?? 0;
      let existing: any = null;
      for (const e of ctx.db.HsrLightconeCost.iter()) {
          if (e.lightconeName === r.lightconeName &&
              e.gameMode.tag === r.gameMode &&
              e.costSetId === csId) {
              existing = e;
              break;
          }
      }
      ```
    - Fields to preserve on partial update: `cost`.

    **Case `HsrSynergyCost`:**

    - PK is auto-inc `id`; existence lookup is by `(sourceName, targetName, gameMode, costSetId)`.
    - ```typescript
      const csId = r.costSetId ?? 0;
      let existing: any = null;
      for (const e of ctx.db.HsrSynergyCost.iter()) {
          if (e.sourceName === r.sourceName &&
              e.targetName === r.targetName &&
              e.gameMode.tag === r.gameMode &&
              e.costSetId === csId) {
              existing = e;
              break;
          }
      }
      ```
    - Fields to preserve on partial update: `costModifier` (verify against `hsrSynergyCost.ts` columns).

    After all 5 cases are reworked:

    - `cd spacetimedb && npm run build` passes.
    - `cd spacetimedb && spacetime publish` succeeds.

    Do NOT touch `admin_update_user`, `admin_delete_row`, or any other admin reducer (per D-Deferred — scope excludes the broader admin audit).
  </action>
  <verify>
    <automated>cd spacetimedb &amp;&amp; npm run build &amp;&amp; grep -c "costSetId === csId\|e\.costSetId === " src/reducers/admin.ts</automated>
  </verify>
  <acceptance_criteria>
    - `admin.ts` HsrCharacter UPDATE branch calls `mergeForUpdate(existing, r, [...])` with a field list containing all 15 fields enumerated above (`displayName`, `aliases`, `rarity`, `path`, `element`, `role`, `imageUrl`, `versionReleased`, `treatAsVersion`, `skelUrl`, `atlasUrl`, `atlasImgUrls`, `posX`, `posY`, `width`).
    - `admin.ts` HsrCharacter INSERT branch initializes the 6 new columns with the defaults listed (`skelUrl: r.skelUrl ?? null`, etc.).
    - `admin.ts` HsrLightcone UPDATE branch calls `mergeForUpdate`.
    - `admin.ts` HsrCharacterCost existence loop has ALL THREE equality checks: `characterName`, `gameMode.tag`, `costSetId`.
    - `admin.ts` HsrLightconeCost existence loop has ALL THREE equality checks: `lightconeName`, `gameMode.tag`, `costSetId`.
    - `admin.ts` HsrSynergyCost existence loop has ALL FOUR equality checks: `sourceName`, `targetName`, `gameMode.tag`, `costSetId`.
    - Grep `e\.costSetId === ` in `admin.ts` returns ≥ 3 matches (one per cost table).
    - Grep `mergeForUpdate` in `admin.ts` returns ≥ 5 call sites (one per case).
    - `cd spacetimedb && npm run build` exits 0.
    - `spacetime publish` succeeds with no registration/compilation errors.
    - NO changes to `admin_update_user` or `admin_delete_row` in the git diff for this task.
  </acceptance_criteria>
  <done>All 5 cases in `admin_bulk_upsert` apply partial-update on existing rows and composite-tuple existence checks on the 3 cost tables; build + publish clean; no new reducers added (Spine rides the existing router per D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → reducer | Admin client submits JSON rows to `admin_bulk_upsert`; `ensureAdmin(ctx)` at admin.ts:254 is the only auth gate. |
| reducer → table | `ctx.db.X.update(...)` / `.insert(...)` — validated JSON becomes persistent state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `admin_bulk_upsert` HsrCharacter update branch | mitigate | **THIS IS THE FIX.** Current default-injection (`imageUrl: r.imageUrl \|\| ''`) silently zeros unsent fields. `mergeForUpdate` restricts overwrites to explicitly non-null values. Regression guard: partial-update tests in Plan 06. |
| T-15-03 | Tampering (PK collision) | HsrCharacterCost / HsrLightconeCost / HsrSynergyCost existence check | mitigate | Existence loop now includes `costSetId` in equality — distinct cost sets never collide. Regression guard: cost-set-isolation tests in Plan 06. |
| T-15-04 | Tampering (round-trip corruption) | Seed consuming reworked router | mitigate | Seed rework in Plan 05 aligns with the new wire convention; round-trip test in Plan 06 asserts clean DB survives a reseed. |
| T-15-05 | Spoofing | `ctx.sender` / admin gate | accept | `ensureAdmin(ctx)` unchanged; no new auth surface. |
| T-15-router-A | Input injection | `validateKeys` / `validateEnum` | mitigate | Strict key-set contract preserved (per D-27). New keys (`skelUrl` etc.) added to EXPECTED_KEYS in Task 1; enum validation wrapped in non-null guard so `null`-preserve values skip validation safely. |
</threat_model>

<verification>
- `cd spacetimedb && npm run build` succeeds.
- `spacetime publish` succeeds.
- Grep counts for `mergeForUpdate` call sites (≥ 5) and `costSetId === ` equality checks (≥ 3) meet the acceptance criteria.
- No untouched-scope regressions (diff limited to the 5 cases + EXPECTED_KEYS + merge helper + wire-convention comment).
</verification>

<success_criteria>
Editing half of FOUND-01 complete: Spine + positioning editable via existing router. Cost-set PK collision bug fixed for all 3 cost tables. Wire convention documented and consistent across 5 cases.
</success_criteria>

<output>
After completion, create `.planning/phases/15-backend-pre-work/15-03-SUMMARY.md`.
</output>
