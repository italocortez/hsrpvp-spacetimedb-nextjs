---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/views/lobbyViews.ts
  - spacetimedb/src/views/identityViews.ts
  - spacetimedb/src/views/costSetViews.ts
  - spacetimedb/src/views/statsViews.ts
  - spacetimedb/src/views/socialViews.ts
  - spacetimedb/src/views/matchViews.ts
  - spacetimedb/src/views/matchHistoryViews.ts
  - spacetimedb/src/views/tournamentViews.ts
  - spacetimedb/src/views/anonymousViews.ts
  - spacetimedb/src/views/securityViews.ts
  - spacetimedb/src/index.ts
autonomous: true
requirements:
  - FOUND-02
tags: [backend, spacetimedb, refactor, views]

must_haves:
  truths:
    - "Every existing view continues to register and publish identically after the split"
    - "Each of 8 new domain view files holds exactly the views assigned by D-02"
    - "spacetimedb/src/index.ts re-exports every view from its new file path"
    - "`spacetime generate` produces the same set of `view_*_table.ts` bindings as before (29 files, unchanged surface)"
  artifacts:
    - path: "spacetimedb/src/views/lobbyViews.ts"
      provides: "view_lobby_browser, view_my_lobbies, view_my_lobby_chat, view_my_lobby_members (4)"
    - path: "spacetimedb/src/views/identityViews.ts"
      provides: "view_my_identity, view_my_profile, view_user_directory, view_public_accounts, view_admin_user_private (5)"
    - path: "spacetimedb/src/views/costSetViews.ts"
      provides: "view_my_cost_sets, view_my_draft_character_costs, view_my_draft_lightcone_costs, view_my_draft_synergy_costs (4)"
    - path: "spacetimedb/src/views/statsViews.ts"
      provides: "view_my_player_stats, view_my_character_stats (2)"
    - path: "spacetimedb/src/views/socialViews.ts"
      provides: "view_my_relationships, view_my_roster_visibility, view_my_roster (3)"
    - path: "spacetimedb/src/views/matchViews.ts"
      provides: "view_my_match_steps, view_my_match_participants (2)"
    - path: "spacetimedb/src/views/matchHistoryViews.ts"
      provides: "view_match_history, view_match_participant_history, view_match_step_history (3 existing; 5 new views land here in Plan 04)"
    - path: "spacetimedb/src/views/tournamentViews.ts"
      provides: "7 tournament views"
  key_links:
    - from: "spacetimedb/src/index.ts"
      to: "spacetimedb/src/views/{lobby,identity,costSet,stats,social,match,matchHistory,tournament}Views.ts"
      via: "named re-exports (Phase 12.2 barrel)"
      pattern: "export \\{[^}]+\\} from './views/(lobby|identity|costSet|stats|social|match|matchHistory|tournament)Views'"
---

<objective>
Split `spacetimedb/src/views/securityViews.ts` (915 lines) and `spacetimedb/src/views/anonymousViews.ts` (453 lines) into 8 domain files whose names mirror the `tables/` directory layout. This refactor lands BEFORE any new views (D-04) so the 5 new history views (Plan 04) land in their permanent home (`matchHistoryViews.ts`) from day one.

Purpose: End the god-file pattern so future-you can find views by domain. `anonymousViews.ts` currently mis-labels several authed views (e.g., `view_my_lobby_chat`); renaming by domain fixes that.

Output: 8 domain view files + unchanged `spacetimedb/src/index.ts` re-export surface; old god files deleted; `spacetime publish` + `spacetime generate` produce an identical set of 29 `view_*_table.ts` bindings.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/skills/spacetimedb/SKILL.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-backend-pre-work/15-CONTEXT.md
@.planning/phases/15-backend-pre-work/15-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@spacetimedb/src/views/securityViews.ts
@spacetimedb/src/views/anonymousViews.ts
@spacetimedb/src/index.ts

**Load the `/spacetimedb` skill before writing code.** This refactor depends on the Phase 12.2 view-registration rule (named re-export from the barrel at `spacetimedb/src/index.ts`).

<convention>
- Views register via `export const view_xxx = spacetimedb.view(...)` AND a named re-export in `spacetimedb/src/index.ts` (lines 10-46).
- Side-effect imports no longer register views (Phase 12.2 change — see CONVENTIONS.md:233-252).
- Renaming the view-name string (first arg to `spacetimedb.view`) breaks `src/module_bindings/view_xxx_table.ts`. D-03 forbids renames.
</convention>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 domain view files with moved contents</name>
  <files>
    spacetimedb/src/views/lobbyViews.ts,
    spacetimedb/src/views/identityViews.ts,
    spacetimedb/src/views/costSetViews.ts,
    spacetimedb/src/views/statsViews.ts,
    spacetimedb/src/views/socialViews.ts,
    spacetimedb/src/views/matchViews.ts,
    spacetimedb/src/views/matchHistoryViews.ts,
    spacetimedb/src/views/tournamentViews.ts
  </files>
  <read_first>
    spacetimedb/src/views/securityViews.ts (915L, source for most views),
    spacetimedb/src/views/anonymousViews.ts (453L, also contains authed views despite the name),
    spacetimedb/src/index.ts (lines 1-46, current barrel),
    spacetimedb/src/views/securityViews.ts:378 (view_my_character_stats — reference for stats-views file),
    spacetimedb/src/views/securityViews.ts:710 (view_my_tournament_matches — reference for tournament-views file)
  </read_first>
  <action>
    Create 8 new domain files in `spacetimedb/src/views/`. For each file, MOVE the `export const view_xxx = spacetimedb.view(...)` blocks verbatim from the source god file, preserving:

    - The exact first-argument object to `spacetimedb.view(...)` (`{ name: 'view_xxx', public: true }`) — NO renames per D-03.
    - The exact view body function.
    - All helper functions and imports needed by those views (e.g., `getMyTournamentIds` moves with the tournament file; `view_my_character_stats`'s local helpers move with the stats file).

    Domain-to-view mapping (per D-02):

    | File | Views |
    |------|-------|
    | `lobbyViews.ts` | `view_lobby_browser`, `view_my_lobbies`, `view_my_lobby_chat`, `view_my_lobby_members` (4) |
    | `identityViews.ts` | `view_my_identity`, `view_my_profile`, `view_user_directory`, `view_public_accounts`, `view_admin_user_private` (5) |
    | `costSetViews.ts` | `view_my_cost_sets`, `view_my_draft_character_costs`, `view_my_draft_lightcone_costs`, `view_my_draft_synergy_costs` (4) |
    | `statsViews.ts` | `view_my_player_stats`, `view_my_character_stats` (2) |
    | `socialViews.ts` | `view_my_relationships`, `view_my_roster_visibility`, `view_my_roster` (3) |
    | `matchViews.ts` | `view_my_match_steps`, `view_my_match_participants` (2) |
    | `matchHistoryViews.ts` | `view_match_history`, `view_match_participant_history`, `view_match_step_history` (3 — the 5 new views land here in Plan 04) |
    | `tournamentViews.ts` | 7 existing tournament views (enumerate from source god file) |

    For each file add a top-of-file JSDoc section divider (match existing `securityViews.ts` style) plus `// Phase 15 D-01/D-02: moved from securityViews.ts` comment on each view export.

    Do NOT delete the old files in this task — the next task updates the barrel and only then removes the empty god files. Work in move-then-switch order to keep `spacetimedb.view(...)` registration live throughout.
  </action>
  <verify>
    <automated>ls spacetimedb/src/views/{lobby,identity,costSet,stats,social,match,matchHistory,tournament}Views.ts &amp;&amp; grep -c "spacetimedb.view(" spacetimedb/src/views/lobbyViews.ts spacetimedb/src/views/identityViews.ts spacetimedb/src/views/costSetViews.ts spacetimedb/src/views/statsViews.ts spacetimedb/src/views/socialViews.ts spacetimedb/src/views/matchViews.ts spacetimedb/src/views/matchHistoryViews.ts spacetimedb/src/views/tournamentViews.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist at the paths listed above.
    - Each file's `spacetimedb.view(` count matches the D-02 view count for that domain (4/5/4/2/3/2/3/7).
    - `grep "name: 'view_" spacetimedb/src/views/*.ts` produces exactly the SAME unique view-name strings as before the refactor (zero renames, zero additions in this task).
    - No new `spacetimedb.view({ name: 'view_my_match_session_history'...` or other Plan 04 views yet (those arrive in Plan 04).
  </acceptance_criteria>
  <done>8 domain view files exist with every `spacetimedb.view(...)` block moved verbatim; no view renamed; no new views yet.</done>
</task>

<task type="auto">
  <name>Task 2: Update barrel re-exports, delete old god files, and republish</name>
  <files>
    spacetimedb/src/index.ts,
    spacetimedb/src/views/securityViews.ts (delete),
    spacetimedb/src/views/anonymousViews.ts (delete)
  </files>
  <read_first>
    spacetimedb/src/index.ts (lines 1-46 — current barrel),
    .planning/codebase/CONVENTIONS.md (lines 233-252 — Phase 12.2 view registration rule),
    .planning/phases/15-backend-pre-work/15-RESEARCH.md (§View File Reorganization Mechanics)
  </read_first>
  <action>
    Rewrite the view re-export block in `spacetimedb/src/index.ts` (lines ~10-46) to point at the 8 new domain files. Replace the `export { ... } from './views/securityViews'` and `export { ... } from './views/anonymousViews'` blocks with 8 grouped exports, one per domain file. Example layout:

    ```typescript
    export {
        view_lobby_browser,
        view_my_lobbies,
        view_my_lobby_chat,
        view_my_lobby_members,
    } from './views/lobbyViews';

    export {
        view_my_identity,
        view_my_profile,
        view_user_directory,
        view_public_accounts,
        view_admin_user_private,
    } from './views/identityViews';

    // ... 6 more blocks following D-02 mapping ...
    ```

    After the barrel is updated: DELETE `spacetimedb/src/views/securityViews.ts` AND `spacetimedb/src/views/anonymousViews.ts` (they are now empty of exports used by the barrel — verify no other file imports from them before deleting; grep `from '.*securityViews'` and `from '.*anonymousViews'` across `spacetimedb/src/` and update any stragglers to the new paths).

    Then run:
    - `cd spacetimedb && npm run build` — must succeed.
    - `cd spacetimedb && spacetime publish` (to local or maincloud, whichever is the default `*` in `spacetime server list`) — must succeed without `[registerExport]` warnings.
    - `cd spacetimedb && spacetime generate` — must produce the same 29 `src/module_bindings/view_*_table.ts` files as before (no new files, no deletions — D-03 invariant).

    If `spacetime publish` emits any `[registerExport]` warnings, stop and audit the barrel against the view files (Pitfall 2 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd spacetimedb &amp;&amp; npm run build &amp;&amp; ls src/views/securityViews.ts 2&gt;/dev/null &amp;&amp; echo "FAIL: securityViews.ts still exists" || ls src/views/anonymousViews.ts 2&gt;/dev/null &amp;&amp; echo "FAIL: anonymousViews.ts still exists" || echo "OK: god files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `spacetimedb/src/views/securityViews.ts` does NOT exist.
    - `spacetimedb/src/views/anonymousViews.ts` does NOT exist.
    - `spacetimedb/src/index.ts` contains `from './views/lobbyViews'`, `from './views/identityViews'`, `from './views/costSetViews'`, `from './views/statsViews'`, `from './views/socialViews'`, `from './views/matchViews'`, `from './views/matchHistoryViews'`, `from './views/tournamentViews'` (8 distinct export-from statements).
    - `spacetimedb/src/index.ts` contains ZERO references to `./views/securityViews` or `./views/anonymousViews`.
    - `cd spacetimedb && npm run build` exits 0.
    - `grep -r "from '.*securityViews'" spacetimedb/src/` and `grep -r "from '.*anonymousViews'" spacetimedb/src/` return no matches.
    - Post-`spacetime generate`: `ls spacetimedb/src/module_bindings/view_*_table.ts | wc -l` equals 29 (pre-Phase-15 count; no new views in this plan).
  </acceptance_criteria>
  <done>Barrel re-exports from 8 new domain files; god files deleted; `spacetime publish` succeeds with no registration warnings; binding surface unchanged (29 `view_*_table.ts` files).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module publish | SpacetimeDB module code → runtime view registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information disclosure | All moved `view_my_*` | accept | No semantic change — view bodies moved verbatim; every view keeps its existing `ctx.sender` filter. Reorg is move-only per D-03. |
| T-15-reorg-A | Tampering | `spacetimedb/src/index.ts` barrel | mitigate | Phase 12.2 rule: every view must remain named-re-exported from the barrel. Task 2 acceptance criteria verifies 8 `from './views/...Views'` statements. |
| T-15-reorg-B | Spoofing (binding drift) | `src/module_bindings/view_*_table.ts` | mitigate | Count invariant — 29 bindings pre, 29 post. Any new or deleted binding indicates a silent rename; Task 2 acceptance criteria enforces the count. |
</threat_model>

<verification>
- `cd spacetimedb && npm run build` succeeds.
- `spacetime publish` succeeds with no `[registerExport]` warnings.
- `ls spacetimedb/src/module_bindings/view_*_table.ts | wc -l` returns 29.
- Old god files (`securityViews.ts`, `anonymousViews.ts`) do not exist.
- No file in `spacetimedb/src/` imports from `./views/securityViews` or `./views/anonymousViews`.
</verification>

<success_criteria>
All 8 domain view files exist; every pre-existing view lives in its domain home; binding surface invariant (29 files) holds; build + publish + generate are idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/15-backend-pre-work/15-01-SUMMARY.md`.
</output>
