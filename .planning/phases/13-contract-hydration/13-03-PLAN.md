---
phase: 13-contract-hydration
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - docs/achievements/contract.md
  - docs/admin/contract.md
  - docs/anonymous-play/contract.md
  - docs/archetypes/contract.md
  - docs/auth/contract.md
  - docs/brackets/contract.md
  - docs/calendar/contract.md
  - docs/chat/contract.md
  - docs/cost-sets/contract.md
  - docs/lobby/contract.md
  - docs/match-results/contract.md
  - docs/match-session/contract.md
  - docs/mmr/contract.md
  - docs/player-stats/contract.md
  - docs/roster/contract.md
  - docs/smoke/contract.md
  - docs/tournament/contract.md
  - docs/views/contract.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "All 18 contract.md files follow the D-06 through D-11 template standard"
    - "Every contract has Architecture link, Feature Overview, Reducers, Acceptance Scenarios, Edge Cases, Integration Points, Phase History sections"
    - "Every reducer is fully documented with Purpose/Permission/Parameters/Flow/State Changes/Error Cases format (per D-08)"
    - "Reducer documentation is sourced from actual codebase (per D-09), not copied from stale docs"
    - "No cost-tables contract created (per D-10 -- explicitly skipped)"
  artifacts:
    - path: "docs/achievements/contract.md"
      provides: "Fully hydrated achievements contract"
      contains: "**Architecture:** [architecture.md](architecture.md)"
    - path: "docs/admin/contract.md"
      provides: "Fully hydrated admin contract"
      contains: "**Architecture:** [architecture.md](architecture.md)"
    - path: "docs/brackets/contract.md"
      provides: "Fully hydrated brackets contract"
      contains: "## Reducers"
    - path: "docs/lobby/contract.md"
      provides: "Fully hydrated lobby contract"
      contains: "## Reducers"
    - path: "docs/match-results/contract.md"
      provides: "Fully hydrated match-results contract"
      contains: "## Reducers"
    - path: "docs/match-session/contract.md"
      provides: "Fully hydrated match-session contract"
      contains: "## Reducers"
    - path: "docs/tournament/contract.md"
      provides: "Fully hydrated tournament contract"
      contains: "## Reducers"
    - path: "docs/views/contract.md"
      provides: "Fully hydrated views contract"
      contains: "## Acceptance Scenarios"
  key_links:
    - from: "docs/*/contract.md"
      to: "docs/*/architecture.md"
      via: "Architecture link at top of every contract"
      pattern: "\\*\\*Architecture:\\*\\*.*architecture\\.md"
    - from: "docs/*/contract.md"
      to: "spacetimedb/src/reducers/"
      via: "Reducer documentation sourced from actual source files"
      pattern: "\\*\\*Purpose:\\*\\*"
---

<objective>
Fully hydrate all 18 contract.md files to the D-06 through D-11 template standard by reading every reducer from the actual SpacetimeDB codebase.

Purpose: Complete, accurate behavioral documentation for every feature. This is the highest-effort plan in Phase 13 because it requires reading 155 reducer exports from 44 source files.
Output: 18 fully hydrated contract.md files across docs/{feature}/ directories
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-contract-hydration/13-CONTEXT.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md
@.planning/phases/13-contract-hydration/13-02-SUMMARY.md

<interfaces>
<!-- Template to follow for all 18 files (created in Plan 01) -->
@docs/_templates/contract-template.md

<!-- Reference example (D-11) -->
@docs/roster/contract.md

<!-- Architecture files (created/normalized in Plan 02) -- contracts link to these -->
@docs/*/architecture.md

<!-- Reducer-to-feature mapping from RESEARCH.md -->
<!--
HIGHEST EFFORT (missing Reducers section, need full hydration from scratch):
- anonymous-play: views only (anonymousViews.ts)
- brackets: 7 reducers (bracketGeneration.ts, bracketAdvancement.ts)
- cost-sets: 8 reducers (costSetManagement.ts)
- match-results: 8+ reducers (matchResultSubmission.ts, matchFinalization.ts, scoreEntry.ts)
- player-stats: views/helpers only
- views: 32+ views (anonymousViews.ts, securityViews.ts)

MEDIUM EFFORT (have Reducers section, need verification and expansion):
- achievements: 7 reducers (achievementManagement.ts)
- admin: 8 reducers (admin.ts, adminMatchTools.ts, banAdmin.ts)
- auth: 1 + server reducers (auth.ts)
- calendar: 12+ reducers (4 files)
- chat: 2 reducers (chat.ts)
- lobby: 15+ reducers (4 files)
- match-session: 15+ reducers (5 files)
- mmr: 6+ reducers (eloAdmin.ts, seasonAdmin.ts, ratingAdmin.ts)
- roster: 12+ reducers (4 files) -- reference, minimal changes
- smoke: 5+ reducers (server.ts, identityGc.ts, userDeletion.ts)
- tournament: 25+ reducers (8 files)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hydrate contracts -- high-effort features (9 files: achievements through match-results)</name>
  <files>docs/achievements/contract.md, docs/admin/contract.md, docs/anonymous-play/contract.md, docs/archetypes/contract.md, docs/auth/contract.md, docs/brackets/contract.md, docs/calendar/contract.md, docs/chat/contract.md, docs/cost-sets/contract.md</files>
  <read_first>
    - docs/_templates/contract-template.md (template standard from Plan 01)
    - docs/roster/contract.md (reference example per D-11, full file)
    - docs/achievements/contract.md (current state)
    - docs/admin/contract.md (current state)
    - docs/anonymous-play/contract.md (current state)
    - docs/archetypes/contract.md (current state)
    - docs/auth/contract.md (current state)
    - docs/brackets/contract.md (current state -- missing Reducers section)
    - docs/calendar/contract.md (current state)
    - docs/chat/contract.md (current state)
    - docs/cost-sets/contract.md (current state -- missing Reducers section)
  </read_first>
  <action>
For EACH of the 9 contract files, apply this process:

1. **Read current file** to preserve existing Acceptance Scenarios, Edge Cases, Integration Points, and Phase History entries
2. **Read ALL corresponding reducer source files** in `spacetimedb/src/reducers/` for that feature to get exact parameter names/types, validation logic, error messages, and state changes
3. **Cross-reference the Plan 02 architecture.md** for that feature to avoid duplication (architecture = "how it works", contract = "what it should do")
4. **Rewrite the file** following the template standard:

Structure for every file:
```markdown
# {Feature Name}

**Architecture:** [architecture.md](architecture.md)

## Feature Overview
{1 paragraph -- what the feature does from a user perspective}

## Reducers

### reducer_name

**Purpose:** {one line}
**Permission:** {who can call this -- specific role/ownership}
**Parameters:**
| Param | Type | Required | Description |
|-------|------|----------|-------------|
| paramName | paramType | Yes/No | description |

**Flow:**
1. {validation step}
2. {business logic step}
...

**Expected State Changes:**
- {table}.{column} {action}

**Error Cases:**
| Condition | Error Message |
|-----------|--------------|
| {when} | "{exact message from source code}" |

{Repeat for every reducer in this feature}

## Acceptance Scenarios
{Preserve existing scenarios, add any missing ones revealed by reducer reading}

### Scenario Name
**Given:** ...
**When:** ...
**Then:** ...

## Edge Cases
| Case | Expected Behavior | Notes |
|------|-------------------|-------|

## Integration Points
| This Feature | Connects To | How | Direction |
|-------------|------------|-----|-----------|

## Phase History
{Preserve existing entries with original provenance}
{Add: "Full hydration from codebase | Phase 13 normalization | 2026-04-09"}
```

**Feature-specific notes:**
- **achievements**: 7 reducers from achievementManagement.ts: create_achievement, update_achievement, delete_achievement, manually_award_achievement, revoke_achievement, set_displayed_achievement, check_and_award_achievements (internal). Auto-award fires in finalization pipeline at step 16.5.
- **admin**: 8 reducers from admin.ts (admin_update_user, admin_delete_user) + adminMatchTools.ts (admin_force_finalize, admin_void_match, admin_set_bracket_winner) + banAdmin.ts (admin_ban_discord, admin_unban, admin_list_bans)
- **anonymous-play**: No reducers. Use "## Anonymous Label Computation" instead of "## Reducers". Document the anonymous view functions from anonymousViews.ts and how labels are generated (teamLabel + memberIndex).
- **archetypes**: 4 reducers from rosterAdmin.ts: admin_upsert_archetype, admin_delete_archetype, admin_assign_character_archetypes, admin_remove_character_archetypes
- **auth**: login_as_guest from auth.ts + server lifecycle: clientConnected, clientDisconnected, register_server, server_link_provider from server.ts. Document ban-on-connect check (Phase 12).
- **brackets**: 7 reducers from bracketGeneration.ts + bracketAdvancement.ts: generate_bracket, seed_bracket, swap_seeds, advance_bracket_match, submit_and_advance_bracket, rollback_bracket_match, advance_group_to_elimination. Currently MISSING Reducers section -- full hydration from scratch.
- **calendar**: 12+ reducers from 4 files. Group by sub-feature: availability CRUD, saved calendar CRUD, event CRUD + invites, invite response.
- **chat**: 2 reducers: send_chat_message, delete_chat_message. Simple but document rolling window and metadata validation.
- **cost-sets**: 8 reducers from costSetManagement.ts: create_cost_set, clone_cost_set, edit_cost_set_draft_*, publish_cost_set, unpublish_cost_set, delete_cost_set. Currently MISSING Reducers section -- full hydration from scratch.

**CRITICAL per D-09:** Read EVERY reducer from the actual codebase. Error messages must be exact strings from source code. Parameter types must match actual TypeScript types. Flow steps must reflect actual validation order.
  </action>
  <verify>
    <automated>for f in achievements admin anonymous-play archetypes auth brackets calendar chat cost-sets; do grep -c "## Reducers\|## Anonymous Label" "docs/$f/contract.md"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files have `**Architecture:** [architecture.md](architecture.md)` within first 3 lines
    - All 9 files contain `## Feature Overview`
    - 8 of 9 files (all except anonymous-play) contain `## Reducers` with at least one reducer documented in full D-08 format
    - anonymous-play/contract.md contains `## Anonymous Label Computation` instead of `## Reducers`
    - brackets/contract.md has 7 reducers documented (was missing Reducers section entirely)
    - cost-sets/contract.md has 8 reducers documented (was missing Reducers section entirely)
    - All 9 files contain `## Acceptance Scenarios`, `## Edge Cases`, `## Integration Points`, `## Phase History`
    - Phase History tables contain entry with `Phase 13 normalization`
    - Reducer error messages match actual source code strings (spot-check at least 3)
  </acceptance_criteria>
  <done>9 contract files (achievements through cost-sets) fully hydrated to template standard with reducer documentation sourced from current codebase</done>
</task>

<task type="auto">
  <name>Task 2: Hydrate contracts -- remaining features (9 files: lobby through views)</name>
  <files>docs/lobby/contract.md, docs/match-results/contract.md, docs/match-session/contract.md, docs/mmr/contract.md, docs/player-stats/contract.md, docs/roster/contract.md, docs/smoke/contract.md, docs/tournament/contract.md, docs/views/contract.md</files>
  <read_first>
    - docs/_templates/contract-template.md (template standard from Plan 01)
    - docs/roster/contract.md (reference example per D-11 -- this file gets minor updates only)
    - docs/lobby/contract.md (current state)
    - docs/match-results/contract.md (current state -- missing Reducers section)
    - docs/match-session/contract.md (current state)
    - docs/mmr/contract.md (current state)
    - docs/player-stats/contract.md (current state -- missing Reducers section)
    - docs/smoke/contract.md (current state)
    - docs/tournament/contract.md (current state -- secondary reference, 321 lines)
    - docs/views/contract.md (current state -- missing Reducers section)
  </read_first>
  <action>
Same process as Task 1 for the remaining 9 contract files.

**Feature-specific notes:**
- **lobby**: 15+ reducers across 4 files. Group by: lifecycle (create/join/leave/close/kick/ban), settings (update_lobby_settings, set_team_slot, ready up), presets (CRUD), tournament lobby (create_tournament_lobby, approve_stand_in), GC (scheduled). This is a high-reducer-count feature -- ensure all are documented.
- **match-results**: 8+ reducers across 3 files (matchResultSubmission, matchFinalization, scoreEntry). Currently MISSING Reducers section. Key reducers: submit_match_result, validate_match_result, finalize_match_result, auto_finalize_casual, record_game_scores, override_match_result, process_tournament_mmr. Document the full finalization pipeline (18 steps).
- **match-session**: 15+ reducers across 5 files. Group by: classic draft (pick/ban/timer), auction draft (nominate/bid/pass/timer), draft control (undo/pause/resume/start_draft), post-draft (equip/arrange/confirm/advance_stage), series (advance_to_next_game/shelve_series/resume_series).
- **mmr**: 6+ reducers from eloAdmin.ts, seasonAdmin.ts, ratingAdmin.ts. Admin-oriented: seed_elo_config, update_elo_config, admin_recalculate_elo, set_active_season, create_season, admin_recalculate_all_ratings.
- **player-stats**: No direct reducers. Use "## Stat Computation" instead of "## Reducers". Document how PlayerStat, PlayerCharacterStat, GlobalCharacterStat, PlayerRelationship are updated during finalization. Currently MISSING Reducers section.
- **roster**: Reference example (D-11). Already well-structured at 416 lines. Apply minor updates only: verify Phase History has `Phase 13 normalization` entry, verify all reducer docs match current source (especially after Phase 10.4 account selection and Phase 11 accountRating changes). Do not restructure.
- **smoke**: Server lifecycle and infrastructure. Use "## Bootstrap Procedures" instead of "## Reducers" where appropriate. Document register_server, scheduled identity GC, admin_delete_user cascade.
- **tournament**: 25+ reducers across 8 files -- largest feature. Group by: management (CRUD, lifecycle, stage transitions), registration (register/withdraw, check-in), teams (create/join/accept/reject/transfer/DQ), admin (override, DQ, assistants), lobby (create_tournament_lobby, approve_stand_in), referee (transfer/reclaim/set_coach/remove_coach), concede (concede_match, claim_forfeit, defer_match).
- **views**: 32+ views. Use "## View Definitions" instead of "## Reducers". Document each view with: name, purpose, tables read, output shape, permission model. Group by: anonymous views, security views, TO views, roster views, match history views.

**For roster and tournament specifically:** Since roster IS the reference (D-11) and tournament is the secondary reference mentioned in CLAUDE.md, apply minimal changes -- verify current content against source, add Phase History entry, ensure template sections present.

**CRITICAL per D-09:** Same rule as Task 1 -- full hydration from actual source code.
  </action>
  <verify>
    <automated>for f in lobby match-results match-session mmr player-stats roster smoke tournament views; do grep -c "## Reducers\|## Stat Computation\|## Bootstrap Procedures\|## View Definitions" "docs/$f/contract.md"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files have `**Architecture:** [architecture.md](architecture.md)` within first 3 lines
    - All 9 files contain `## Feature Overview`
    - lobby, match-results, match-session, mmr, roster, tournament contain `## Reducers`
    - player-stats/contract.md contains `## Stat Computation` instead of `## Reducers`
    - smoke/contract.md contains `## Bootstrap Procedures` or `## Reducers`
    - views/contract.md contains `## View Definitions` instead of `## Reducers`
    - match-results/contract.md has 8+ reducers documented (was missing Reducers section)
    - All 9 files contain `## Acceptance Scenarios`, `## Edge Cases`, `## Integration Points`, `## Phase History`
    - Phase History tables contain entry with `Phase 13 normalization`
    - roster/contract.md retains existing well-structured content, only gains missing sections and Phase History entry
  </acceptance_criteria>
  <done>9 contract files (lobby through views) fully hydrated to template standard. All 18 contracts complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- docs-only, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | I (Information Disclosure) | contract.md files | accept | Contracts describe public API behavior specs; no secrets disclosed. Error messages are already visible to clients. |
</threat_model>

<verification>
- `find docs/ -name "contract.md" | wc -l` returns 18 (no cost-tables contract per D-10)
- `find docs/ -name "contract.md" -exec grep -l "Architecture.*architecture.md" {} \; | wc -l` returns 18
- `find docs/ -name "contract.md" -exec grep -l "## Acceptance Scenarios" {} \; | wc -l` returns 18
- `find docs/ -name "contract.md" -exec grep -l "## Phase History" {} \; | wc -l` returns 18
- Spot-check: `grep "**Purpose:**" docs/brackets/contract.md | wc -l` returns 7 (7 reducers)
</verification>

<success_criteria>
All 18 contract.md files follow the template standard with every reducer fully documented from current source code. The 6 contracts that were missing Reducers sections now have complete reducer documentation. No stale references.
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-03-SUMMARY.md`
</output>
