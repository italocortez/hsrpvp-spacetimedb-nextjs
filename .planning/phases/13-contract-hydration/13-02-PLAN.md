---
phase: 13-contract-hydration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - docs/achievements/architecture.md
  - docs/admin/architecture.md
  - docs/anonymous-play/architecture.md
  - docs/archetypes/architecture.md
  - docs/auth/architecture.md
  - docs/brackets/architecture.md
  - docs/calendar/architecture.md
  - docs/chat/architecture.md
  - docs/cost-sets/architecture.md
  - docs/cost-tables/architecture.md
  - docs/lobby/architecture.md
  - docs/match-results/architecture.md
  - docs/match-session/architecture.md
  - docs/mmr/architecture.md
  - docs/player-stats/architecture.md
  - docs/roster/architecture.md
  - docs/smoke/architecture.md
  - docs/tournament/architecture.md
  - docs/views/architecture.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "All 19 architecture.md files follow the D-01 through D-05 template standard"
    - "Every architecture file has Overview, Table Relationships, Reducer Flows, and Phase History sections"
    - "Table diagrams use tree notation exclusively with connectors"
    - "All files have Last updated timestamp"
    - "Content is sourced from actual codebase, not copied from stale docs"
  artifacts:
    - path: "docs/achievements/architecture.md"
      provides: "Normalized achievements architecture"
      contains: "-- Architecture"
    - path: "docs/admin/architecture.md"
      provides: "Normalized admin architecture"
      contains: "-- Architecture"
    - path: "docs/anonymous-play/architecture.md"
      provides: "Normalized anonymous-play architecture"
      contains: "-- Architecture"
    - path: "docs/archetypes/architecture.md"
      provides: "Normalized archetypes architecture"
      contains: "-- Architecture"
    - path: "docs/auth/architecture.md"
      provides: "Normalized auth architecture"
      contains: "-- Architecture"
    - path: "docs/brackets/architecture.md"
      provides: "Normalized brackets architecture"
      contains: "-- Architecture"
    - path: "docs/calendar/architecture.md"
      provides: "Normalized calendar architecture"
      contains: "-- Architecture"
    - path: "docs/chat/architecture.md"
      provides: "Normalized chat architecture"
      contains: "-- Architecture"
    - path: "docs/cost-sets/architecture.md"
      provides: "Normalized cost-sets architecture"
      contains: "-- Architecture"
    - path: "docs/cost-tables/architecture.md"
      provides: "Normalized cost-tables architecture"
      contains: "-- Architecture"
    - path: "docs/lobby/architecture.md"
      provides: "Normalized lobby architecture"
      contains: "-- Architecture"
    - path: "docs/match-results/architecture.md"
      provides: "Normalized match-results architecture"
      contains: "-- Architecture"
    - path: "docs/match-session/architecture.md"
      provides: "Normalized match-session architecture"
      contains: "-- Architecture"
    - path: "docs/mmr/architecture.md"
      provides: "Normalized mmr architecture"
      contains: "-- Architecture"
    - path: "docs/player-stats/architecture.md"
      provides: "Normalized player-stats architecture"
      contains: "-- Architecture"
    - path: "docs/roster/architecture.md"
      provides: "Normalized roster architecture (reference)"
      contains: "-- Architecture"
    - path: "docs/smoke/architecture.md"
      provides: "Normalized smoke architecture"
      contains: "-- Architecture"
    - path: "docs/tournament/architecture.md"
      provides: "Normalized tournament architecture"
      contains: "-- Architecture"
    - path: "docs/views/architecture.md"
      provides: "Normalized views architecture"
      contains: "-- Architecture"
  key_links:
    - from: "docs/*/architecture.md"
      to: "spacetimedb/src/tables/"
      via: "Table definitions sourced from actual table files"
      pattern: "## Table Relationships"
    - from: "docs/*/architecture.md"
      to: "spacetimedb/src/reducers/"
      via: "Reducer flows sourced from actual reducer files"
      pattern: "## Reducer Flows"
---

<objective>
Normalize all 19 architecture.md files to the D-01 through D-05 template standard by reading actual source code from the SpacetimeDB module.

Purpose: Every architecture file follows the same structure, sourced from current codebase truth. This enables consistent contract hydration in Plan 03.
Output: 19 normalized architecture.md files across docs/{feature}/ directories
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-contract-hydration/13-CONTEXT.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md
@.planning/phases/13-contract-hydration/13-01-SUMMARY.md

<interfaces>
<!-- Template to follow for all 19 files (created in Plan 01) -->
@docs/_templates/architecture-template.md

<!-- Reference example (D-05) -->
@docs/roster/architecture.md

<!-- Reducer-to-feature mapping from RESEARCH.md -->
<!--
achievements: achievementManagement.ts (7 reducers)
admin: admin.ts, adminMatchTools.ts, banAdmin.ts (8 reducers)
anonymous-play: views only (anonymousViews.ts)
archetypes: rosterAdmin.ts archetype reducers (4 reducers)
auth: auth.ts (1 reducer + server reducers)
brackets: bracketGeneration.ts, bracketAdvancement.ts (7 reducers)
calendar: calendarAvailability.ts, calendarEvents.ts, calendarInviteResponse.ts, calendarSaved.ts (12+ reducers)
chat: chat.ts (2 reducers)
cost-sets: costSetManagement.ts (8 reducers)
cost-tables: (admin data, no feature-specific reducers)
lobby: lobbyLifecycle.ts, lobbySettings.ts, lobbyPresets.ts, lobbyGc.ts (15+ reducers)
match-results: matchResultSubmission.ts, matchFinalization.ts, scoreEntry.ts (8+ reducers)
match-session: draftClassic.ts, draftAuction.ts, draftControl.ts, postDraft.ts, seriesManagement.ts (15+ reducers)
mmr: eloAdmin.ts, seasonAdmin.ts, ratingAdmin.ts (6+ reducers)
player-stats: views/helpers only
roster: roster.ts, rosterAdmin.ts, profile.ts, accountSelection.ts (12+ reducers)
smoke: server.ts, identityGc.ts, userDeletion.ts (5+ reducers)
tournament: tournamentManagement.ts, tournamentRegistration.ts, tournamentTeams.ts, tournamentAdmin.ts, tournamentCheckIn.ts, tournamentLobby.ts, refereeManagement.ts, concede.ts (25+ reducers)
views: views/anonymousViews.ts, views/securityViews.ts (32+ views)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize architecture files -- achievements through match-session (10 files)</name>
  <files>docs/achievements/architecture.md, docs/admin/architecture.md, docs/anonymous-play/architecture.md, docs/archetypes/architecture.md, docs/auth/architecture.md, docs/brackets/architecture.md, docs/calendar/architecture.md, docs/chat/architecture.md, docs/cost-sets/architecture.md, docs/cost-tables/architecture.md</files>
  <read_first>
    - docs/_templates/architecture-template.md (template standard from Plan 01)
    - docs/roster/architecture.md (reference example per D-05, full file)
    - docs/achievements/architecture.md (current state)
    - docs/admin/architecture.md (current state)
    - docs/anonymous-play/architecture.md (current state)
    - docs/archetypes/architecture.md (current state)
    - docs/auth/architecture.md (current state)
    - docs/brackets/architecture.md (current state)
    - docs/calendar/architecture.md (current state)
    - docs/chat/architecture.md (current state)
    - docs/cost-sets/architecture.md (current state)
    - docs/cost-tables/architecture.md (current state)
  </read_first>
  <action>
For EACH of the 10 architecture files, apply this process:

1. **Read current file** to preserve any existing content worth keeping (especially Phase History entries and decision references)
2. **Read the corresponding table definition files** in `spacetimedb/src/tables/` for that feature's tables to get current column names, types, PKs, FKs, and privacy markers
3. **Read the corresponding reducer files** in `spacetimedb/src/reducers/` for that feature to get current reducer signatures, flows, and error messages
4. **Rewrite the file** following the template standard:

Structure for every file:
```
# {Feature Name} -- Architecture

Last updated: 2026-04-09

## Overview
{1-2 paragraphs from existing content + source code context}

## Table Relationships
{Tree notation per D-04, sourced from actual table files}
{Annotate [PRIVATE] for tables with public:false}
{Annotate decision refs like [D-XX, Phase Y] where known}

## Reducer Flows
{For each reducer in this feature: ### reducer_name(params) with numbered steps}
{Source from actual reducer code, not existing doc content}

## Phase History
{Preserve existing Phase History entries with original provenance}
{Add entry: "Normalized to standard template | Phase 13 normalization | 2026-04-09"}
```

**Feature-specific notes:**
- **achievements**: 7 reducers from achievementManagement.ts; Achievement, AchievementCriteria, UserAchievement tables
- **admin**: 8 reducers from admin.ts + adminMatchTools.ts + banAdmin.ts; admin-only operations, BanRecord table
- **anonymous-play**: No direct reducers -- document anonymous views from anonymousViews.ts; describe how anonymous labels are computed
- **archetypes**: 4 reducers from rosterAdmin.ts (admin_upsert_archetype, admin_delete_archetype, admin_assign_character_archetypes, admin_remove_character_archetypes); Archetype, HsrCharacterArchetype tables
- **auth**: login_as_guest + server lifecycle reducers (clientConnected, clientDisconnected, register_server, server_link_provider); User, UserIdentity, UserPrivate tables
- **brackets**: 7 reducers from bracketGeneration.ts + bracketAdvancement.ts; BracketMatch, GroupPhaseRecord tables
- **calendar**: 12+ reducers from 4 files; CalendarAvailability, CalendarEvent, CalendarEventInvite, SavedCalendar tables
- **chat**: 2 reducers from chat.ts; ChatEvent event table
- **cost-sets**: 8 reducers from costSetManagement.ts; CostSet + 3 draft tables + 3 live cost tables
- **cost-tables**: Admin seed data tables (HsrCharacter, HsrCharacterCost, HsrLightcone, HsrLightconeCost, HsrSynergyCost); document admin_bulk_upsert reducers

**CRITICAL:** Read actual source files for every reducer flow. Do NOT copy from existing docs -- existing docs have accumulated drift over 12+ phases. The `spacetimedb/src/` directory is the single source of truth.
  </action>
  <verify>
    <automated>for f in achievements admin anonymous-play archetypes auth brackets calendar chat cost-sets cost-tables; do head -1 "docs/$f/architecture.md"; done | grep -c "Architecture"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files have first line matching `# {Feature Name} -- Architecture` (em dash)
    - All 10 files have `Last updated: 2026-04-09` within first 3 lines
    - All 10 files contain sections: `## Overview`, `## Table Relationships`, `## Reducer Flows`, `## Phase History`
    - Table Relationships sections use tree notation with `+--` or `|--` or indented connectors
    - Phase History tables contain entry with `Phase 13 normalization`
    - No references to removed/renamed entities (TournamentParticipant, GroupStanding, player1Id/player2Id, winnerUserId)
  </acceptance_criteria>
  <done>10 architecture files (achievements through cost-tables) normalized to template standard with content sourced from current codebase</done>
</task>

<task type="auto">
  <name>Task 2: Normalize architecture files -- lobby through views (9 files)</name>
  <files>docs/lobby/architecture.md, docs/match-results/architecture.md, docs/match-session/architecture.md, docs/mmr/architecture.md, docs/player-stats/architecture.md, docs/roster/architecture.md, docs/smoke/architecture.md, docs/tournament/architecture.md, docs/views/architecture.md</files>
  <read_first>
    - docs/_templates/architecture-template.md (template standard from Plan 01)
    - docs/roster/architecture.md (reference example per D-05 -- this file gets minor updates only)
    - docs/lobby/architecture.md (current state -- 436 lines, needs structural reorganization)
    - docs/match-results/architecture.md (current state -- 524 lines, largest file)
    - docs/match-session/architecture.md (current state)
    - docs/mmr/architecture.md (current state)
    - docs/player-stats/architecture.md (current state)
    - docs/smoke/architecture.md (current state)
    - docs/tournament/architecture.md (current state)
    - docs/views/architecture.md (current state -- 476 lines)
  </read_first>
  <action>
Same process as Task 1, for the remaining 9 architecture files.

**Feature-specific notes:**
- **lobby**: 15+ reducers across 4 files (lobbyLifecycle, lobbySettings, lobbyPresets, lobbyGc); Lobby, LobbyMember, LobbyMemberAccount, LobbyBan, LobbyPreset, LobbyGcJob tables. Large file -- reorganize structure, don't just add missing sections
- **match-results**: 8+ reducers across 3 files (matchResultSubmission, matchFinalization, scoreEntry); MatchResultRecord, MatchResultParticipant, MatchResultGame tables. Largest existing file -- ensure reducer flows match current source
- **match-session**: 15+ reducers across 5 files (draftClassic, draftAuction, draftControl, postDraft, seriesManagement); MatchSession, MatchSessionStep tables
- **mmr**: 6+ reducers from eloAdmin.ts, seasonAdmin.ts, ratingAdmin.ts; EloConfig, MmrRating, MmrHistory, Leaderboard, Season tables
- **player-stats**: No direct reducers -- stats are computed via helpers during finalization. Document PlayerStat, PlayerCharacterStat, GlobalCharacterStat, PlayerRelationship tables and the stat increment helpers
- **roster**: Reference example (D-05). Already well-structured. Apply minor updates only: verify `Last updated` timestamp, verify `## Phase History` section exists, verify content reflects Phase 10.4 (PRIVATE markers) and Phase 11 (accountRating matrix) changes
- **smoke**: Server lifecycle and test infrastructure; server.ts (register_server), identityGc.ts (scheduled GC), userDeletion.ts (admin_delete_user cascade). IdentityGcJob, GcResult tables (Phase 12.1)
- **tournament**: 25+ reducers across 8 files -- the largest feature. Tournament, TournamentEnrolled, TournamentTeamMember, TournamentTeam, TournamentTeamRequest, TournamentPlayerAccount, TournamentStandIn tables
- **views**: 32+ server-side views from anonymousViews.ts and securityViews.ts. Document each view with its purpose and which tables it reads. No reducers -- use "## View Definitions" as the section instead of "## Reducer Flows"

**For roster specifically:** Since it IS the reference (D-05), only add missing sections (Phase History if absent) and verify `Last updated` date. Do not restructure what already works.

**CRITICAL:** Same rule as Task 1 -- read actual source files for every piece of content. Do NOT trust existing doc content as baseline.
  </action>
  <verify>
    <automated>for f in lobby match-results match-session mmr player-stats roster smoke tournament views; do head -1 "docs/$f/architecture.md"; done | grep -c "Architecture"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files have first line matching `# {Feature Name} -- Architecture` (em dash)
    - All 9 files have `Last updated: 2026-04-09` within first 3 lines
    - All 9 files contain sections: `## Overview`, `## Table Relationships`, `## Phase History`
    - 8 of 9 files (all except views) contain `## Reducer Flows`
    - views/architecture.md contains `## View Definitions` instead of `## Reducer Flows`
    - Phase History tables contain entry with `Phase 13 normalization`
    - roster/architecture.md retains existing well-structured content, only gains missing sections
    - No references to removed/renamed entities
  </acceptance_criteria>
  <done>9 architecture files (lobby through views) normalized to template standard with content sourced from current codebase. All 19 architecture files complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- docs-only, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | I (Information Disclosure) | architecture.md files | accept | Architecture docs describe public API structure; no secrets disclosed |
</threat_model>

<verification>
- `find docs/ -name "architecture.md" -exec head -1 {} \; | grep -c "Architecture"` returns 19
- `find docs/ -name "architecture.md" -exec grep -l "## Overview" {} \; | wc -l` returns 19
- `find docs/ -name "architecture.md" -exec grep -l "## Table Relationships" {} \; | wc -l` returns 19
- `find docs/ -name "architecture.md" -exec grep -l "## Phase History" {} \; | wc -l` returns 19
- `find docs/ -name "architecture.md" -exec grep -l "Last updated:" {} \; | wc -l` returns 19
</verification>

<success_criteria>
All 19 architecture.md files follow the template standard with content sourced from current SpacetimeDB source code. No stale references. Plan 03 (contract hydration) can proceed using these as the architecture cross-references.
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-02-SUMMARY.md`
</output>
