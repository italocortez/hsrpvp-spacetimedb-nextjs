---
phase: 13-contract-hydration
plan: 05
type: execute
wave: 4
depends_on: ["13-02", "13-03", "13-04"]
files_modified:
  - docs/FRONTEND-HANDOFF.md
  - notes/erd-mermaid.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "FRONTEND-HANDOFF.md is rewritten from scratch referencing finalized normalized docs (per D-14)"
    - "FRONTEND-HANDOFF.md accurately reflects current project state (155 reducers, 67 tables, all phases complete)"
    - "ERD mermaid includes all production tables (~67) with PKs, FKs, and cardinality annotations (per D-15)"
    - "Cardinality uses explicit text notation like 'User (one) to UserPrivate (one-optional)' (per D-16)"
  artifacts:
    - path: "docs/FRONTEND-HANDOFF.md"
      provides: "Complete frontend handoff documentation"
      contains: "v1 frontend"
    - path: "notes/erd-mermaid.md"
      provides: "Complete ERD with cardinality annotations"
      contains: "erDiagram"
  key_links:
    - from: "docs/FRONTEND-HANDOFF.md"
      to: "docs/*/architecture.md"
      via: "References normalized feature docs as entry points"
      pattern: "docs/"
    - from: "notes/erd-mermaid.md"
      to: "spacetimedb/src/tables/"
      via: "Entity definitions sourced from actual table files"
      pattern: "erDiagram"
---

<objective>
Rewrite FRONTEND-HANDOFF.md from scratch and update the ERD mermaid diagram with all production tables and cardinality annotations.

Purpose: FRONTEND-HANDOFF.md is the primary entry point for the v1 frontend milestone. It must accurately reference all finalized feature docs. The ERD serves as a quick-reference schema map. Both must reflect current state after all 20 backend phases.
Output: Rewritten docs/FRONTEND-HANDOFF.md, updated notes/erd-mermaid.md
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-contract-hydration/13-CONTEXT.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md
@.planning/phases/13-contract-hydration/13-02-SUMMARY.md
@.planning/phases/13-contract-hydration/13-03-SUMMARY.md
@.planning/phases/13-contract-hydration/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FRONTEND-HANDOFF.md from scratch</name>
  <files>docs/FRONTEND-HANDOFF.md</files>
  <read_first>
    - docs/FRONTEND-HANDOFF.md (current state -- stale references to verify what NOT to carry forward)
    - .planning/ROADMAP.md (phase completion status, requirements coverage)
    - .planning/REQUIREMENTS.md (full v0.5 requirement list, traceability table)
    - .planning/codebase/ARCHITECTURE.md (regenerated in Plan 04 -- current architecture overview)
    - .planning/codebase/STACK.md (regenerated in Plan 04 -- current technology stack)
  </read_first>
  <action>
Rewrite `docs/FRONTEND-HANDOFF.md` completely from scratch (per D-14). Do NOT incrementally update the existing file.

The current file has these stale references (from RESEARCH.md):
- References `docs/teams/` directory (doesn't exist)
- Claims Phases 7-11 are incomplete (all are complete)
- Claims "80+ reducers" (actual: 155 reducer exports)
- Claims "55 tables" (actual: 67 table definition files)

**Structure for the rewrite:**

1. **Header and Purpose**
   - Title: `# HSRPVP Backend -- Frontend Handoff`
   - Last updated date: 2026-04-09
   - Purpose: Document for the v1 frontend milestone. The backend is complete (all v0.5 requirements implemented). This document maps what exists and how to build the frontend on top of it.

2. **Backend Summary**
   - SpacetimeDB SDK version (2.1.0)
   - Module stats: ~67 tables, ~155 reducer exports, ~32 server-side views, 44 reducer source files
   - All 83 mapped v0.5 requirements complete (per REQUIREMENTS.md traceability)
   - Test suite: 673+ tests across 41 integration files (Phase 10.5 baseline)

3. **Feature Map**
   For each of the 19 feature areas, provide a one-line summary and link to both architecture.md and contract.md:
   ```
   | Feature | Tables | Reducers | Docs |
   |---------|--------|----------|------|
   | Roster Management | 3 | 12+ | [arch](roster/architecture.md) / [contract](roster/contract.md) |
   ```
   List all 19 features with accurate counts from Plan 02/03 output.

4. **Authentication Flow**
   - Guest login -> Discord link -> verified user
   - Session cookie pattern (stdb_session for SSR-safe auth)
   - view_my_profile for authenticated user data
   - Ban checking on connect

5. **Data Access Patterns**
   - Subscriptions: Which tables are public vs private
   - Views: 32 server-side views replace raw table subscriptions for bandwidth/privacy
   - Reducer calls: How to call reducers from Next.js client
   - Client bindings: Generated via `spacetime generate`, located in `src/module_bindings/`

6. **Key Conventions for Frontend**
   - Anonymous play: Labels computed server-side, frontend just displays them
   - Roster visibility: Server-enforced, views handle privacy
   - Tournament lifecycle: Stage machine (Draft -> Registration -> CheckIn -> Seeding -> InProgress -> Completed/Cancelled)
   - Match lifecycle: Lobby stages (Waiting -> Drafting -> Equipping -> Scoring -> AwaitingResult -> Completed)
   - Best-of-N: Series management via advance_to_next_game / shelve_series / resume_series

7. **Missing from Backend (v1 Frontend Tasks)**
   - Seasons implementation (schema exists, logic deferred)
   - HoYoverse API import (deferred)
   - Computer vision import (deferred)
   - Swiss-system brackets (deferred)
   - Persistent teams TEAM-01/02/03 (deferred)

8. **Entry Points for Development**
   - Start here: `docs/roster/` (simplest feature, gold standard docs)
   - High priority: lobby, match-session, tournament (core gameplay loop)
   - Read the ERD: `notes/erd-mermaid.md` for schema overview
  </action>
  <verify>
    <automated>head -5 docs/FRONTEND-HANDOFF.md && echo "---" && grep -c "architecture.md" docs/FRONTEND-HANDOFF.md && grep -c "contract.md" docs/FRONTEND-HANDOFF.md && grep "155\|67\|32" docs/FRONTEND-HANDOFF.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - docs/FRONTEND-HANDOFF.md exists and first line contains `Frontend Handoff` or `HSRPVP Backend`
    - File contains `Last updated: 2026-04-09`
    - File references all 19 feature areas with links to architecture.md and contract.md
    - File does NOT reference `docs/teams/` (non-existent directory)
    - File does NOT claim phases 7-11 are incomplete
    - File mentions approximately 155 reducer exports (not "80+")
    - File mentions approximately 67 tables (not "55")
    - File mentions SpacetimeDB SDK 2.1.0
    - File mentions 32 server-side views
    - File contains sections for authentication, data access patterns, and entry points
  </acceptance_criteria>
  <done>FRONTEND-HANDOFF.md completely rewritten from scratch with accurate references to finalized normalized docs</done>
</task>

<task type="auto">
  <name>Task 2: Update ERD mermaid with all tables and cardinality annotations</name>
  <files>notes/erd-mermaid.md</files>
  <read_first>
    - notes/erd-mermaid.md (current ERD -- 492 lines, 65 entities, existing cardinality)
    - spacetimedb/src/schema.ts (table registration -- authoritative list of all tables)
  </read_first>
  <action>
Update `notes/erd-mermaid.md` to include all production tables with full cardinality annotations (per D-15, D-16).

**Process:**

1. **Audit current ERD against actual tables:**
   - Read `spacetimedb/src/schema.ts` to get the authoritative list of all registered tables
   - List all files in `spacetimedb/src/tables/` to get table definition count
   - Compare against ERD entities to find missing tables
   - Per RESEARCH.md: likely missing IdentityGcJob, GcResult (Phase 12.1), AccountRatingConfig (Phase 11), and possibly LobbyMemberAccount (Phase 10.4)

2. **Add missing table entities:**
   - For each missing table, read its definition file to get columns, PK, and FKs
   - Add entity definition in mermaid ERD format
   - Add relationship lines to connected tables

3. **Verify cardinality annotations on ALL relationships:**
   - Per D-16, every relationship must have explicit cardinality text: `"userId (many-to-one)"`, `"User (one) to UserPrivate (one-optional)"` style
   - Review existing relationship lines and add missing cardinality text
   - Cardinality types to use: one-to-one, one-to-many, many-to-one, one-to-one-optional, many-to-many
   - Determine cardinality from FK constraints and index types (unique btree = one-to-one, non-unique = one-to-many)

4. **Update header comment:**
   - Update entity count to match actual total
   - Update date to 2026-04-09
   - Note: "All v0.5 tables included"

5. **Verify mermaid syntax:**
   - Ensure the diagram renders (no syntax errors)
   - Use standard mermaid ERD notation: `||--o{`, `||--||`, `}o--||`, etc.

**CRITICAL:** Do NOT remove existing entities or relationships that are still valid. Only add missing ones and update cardinality annotations. Verify each change against the actual table definition files.
  </action>
  <verify>
    <automated>grep -c "erDiagram\|entity\|{" notes/erd-mermaid.md | head -1 && echo "---" && grep -c "one-to-one\|one-to-many\|many-to-one\|one-to-one-optional\|many-to-many" notes/erd-mermaid.md</automated>
  </verify>
  <acceptance_criteria>
    - notes/erd-mermaid.md exists and contains `erDiagram`
    - Entity count in the file matches actual table count (~67)
    - ERD contains entities for IdentityGcJob, GcResult (if these tables exist)
    - ERD contains entity for AccountRatingConfig (Phase 11 addition)
    - ERD contains entity for LobbyMemberAccount (Phase 10.4 addition)
    - All relationship lines have cardinality annotation text
    - No references to removed tables (TournamentParticipant, GroupStanding)
    - File header date updated to 2026-04-09
  </acceptance_criteria>
  <done>ERD mermaid updated with all production tables, complete cardinality annotations, and no stale references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- docs-only, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | I (Information Disclosure) | FRONTEND-HANDOFF.md | accept | Handoff doc is internal project reference; no secrets disclosed |
| T-13-06 | I (Information Disclosure) | erd-mermaid.md | accept | ERD shows schema structure only; no data content |
</threat_model>

<verification>
- `wc -l docs/FRONTEND-HANDOFF.md` shows substantial content (>100 lines)
- FRONTEND-HANDOFF.md references all 19 feature areas
- `wc -l notes/erd-mermaid.md` shows entity count matching ~67 tables
- ERD contains cardinality annotations throughout
</verification>

<success_criteria>
FRONTEND-HANDOFF.md is a complete, accurate entry point for the v1 frontend milestone. ERD mermaid is the definitive schema reference with all tables and relationships annotated. Phase 13 documentation normalization is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-05-SUMMARY.md`
</output>
