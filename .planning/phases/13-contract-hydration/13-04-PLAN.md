---
phase: 13-contract-hydration
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements:
  - "All backend phases complete"
must_haves:
  truths:
    - "All 7 codebase docs are regenerated from scratch (per D-12)"
    - "ARCHITECTURE.md reflects current module structure including Phase 12.2 SDK upgrade"
    - "STRUCTURE.md reflects current project file tree including all Phase 10-12 additions"
    - "TESTING.md reflects current test infrastructure including Phase 10.5 stabilization"
    - "CONVENTIONS.md reflects current coding patterns including Phase 12.2 view exports"
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Current architecture overview"
      contains: "SpacetimeDB"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Known issues and tech debt"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Coding conventions and patterns"
      contains: "reducer"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "External integrations documentation"
    - path: ".planning/codebase/STACK.md"
      provides: "Technology stack inventory"
      contains: "SpacetimeDB"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Project file tree and layout"
    - path: ".planning/codebase/TESTING.md"
      provides: "Test infrastructure and patterns"
      contains: "vitest"
  key_links:
    - from: ".planning/codebase/ARCHITECTURE.md"
      to: "spacetimedb/src/"
      via: "Module structure sourced from actual file tree"
      pattern: "reducers"
    - from: ".planning/codebase/STRUCTURE.md"
      to: "project root"
      via: "File tree generated from actual filesystem"
      pattern: "spacetimedb"
---

<objective>
Regenerate all 7 .planning/codebase/ documentation files from scratch by reading the actual codebase.

Purpose: Current codebase docs are dated 2026-04-06 (pre-Phase 12.2 SDK upgrade). Full regeneration ensures they reflect the complete state of the project after all 20 completed phases. These docs are used by planning agents for codebase context.
Output: 7 regenerated .planning/codebase/*.md files
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-contract-hydration/13-CONTEXT.md
@.planning/phases/13-contract-hydration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate STACK.md, STRUCTURE.md, and ARCHITECTURE.md</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - .planning/codebase/STACK.md (current state -- to understand expected format, then regenerate)
    - .planning/codebase/STRUCTURE.md (current state -- to understand expected format, then regenerate)
    - .planning/codebase/ARCHITECTURE.md (current state -- to understand expected format, then regenerate)
    - package.json (dependency inventory for STACK.md)
    - spacetimedb/package.json (backend dependency inventory)
    - tsconfig.json (TypeScript configuration)
    - next.config.ts (Next.js configuration)
    - vitest.config.ts (test configuration)
  </read_first>
  <action>
Regenerate 3 files from scratch (per D-12). Do NOT incrementally update -- rewrite completely.

**STACK.md:**
Read `package.json` and `spacetimedb/package.json` to build the technology inventory:
- Runtime: Node.js version, Next.js version, React version
- Backend: SpacetimeDB SDK version (2.1.0 after Phase 12.2), module structure
- Frontend: Tailwind CSS, next/font, any UI libraries
- Testing: Vitest version, test runner configuration
- Build: TypeScript version, bundler, deployment target
- List every dependency with its purpose (not just name/version)

**STRUCTURE.md:**
Generate the project file tree using actual filesystem. Include:
- Top-level directories with purpose annotations
- `spacetimedb/src/` subtree: tables/, reducers/, helpers/, views/, enums.ts, structs.ts, schema.ts
- `app/` subtree: route structure, layouts, pages, API routes
- `components/` subtree: component organization
- `test/` subtree: shared helpers, integration test files, configuration
- `docs/` subtree: feature documentation layout (19 feature dirs + _templates)
- `.planning/` subtree: high-level overview only
- `.claude/` subtree: skills listing
- Notable files: CLAUDE.md, package.json, next.config.ts, etc.

**ARCHITECTURE.md:**
Read the actual module structure to document:
- SpacetimeDB module architecture (tables -> reducers -> views -> helpers pattern)
- Client-server communication model (subscriptions, reducer calls, views)
- Authentication flow (guest -> Discord link -> verified user)
- Key architectural patterns: transactional tables, event tables, composite PKs, btree indexes
- Feature domain map: which features own which tables/reducers
- Recent architectural additions: identity GC (Phase 12.1), UserPrivate isolation (Phase 12), view exports (Phase 12.2)

Include file counts for each category: `ls spacetimedb/src/tables/*.ts | wc -l` for tables, etc.
  </action>
  <verify>
    <automated>wc -l .planning/codebase/STACK.md .planning/codebase/STRUCTURE.md .planning/codebase/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/codebase/STACK.md exists and contains `SpacetimeDB` and version `2.1.0`
    - .planning/codebase/STACK.md lists Vitest as test framework
    - .planning/codebase/STRUCTURE.md exists and contains `spacetimedb/src/tables`
    - .planning/codebase/STRUCTURE.md contains `docs/_templates`
    - .planning/codebase/ARCHITECTURE.md exists and references UserPrivate (Phase 12)
    - .planning/codebase/ARCHITECTURE.md references identity GC (Phase 12.1)
    - All 3 files are non-trivial (at least 50 lines each)
  </acceptance_criteria>
  <done>STACK.md, STRUCTURE.md, and ARCHITECTURE.md regenerated from scratch reflecting full project state through Phase 12.2</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate CONVENTIONS.md, CONCERNS.md, INTEGRATIONS.md, and TESTING.md</name>
  <files>.planning/codebase/CONVENTIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (current state -- understand format, then regenerate)
    - .planning/codebase/CONCERNS.md (current state -- understand format, then regenerate)
    - .planning/codebase/INTEGRATIONS.md (current state -- understand format, then regenerate)
    - .planning/codebase/TESTING.md (current state -- understand format, then regenerate)
    - .claude/CLAUDE.md (project conventions and rules)
    - spacetimedb/src/reducers/roster.ts (example of reducer patterns)
    - spacetimedb/src/helpers/ensurePermissions.ts (permission pattern example)
    - spacetimedb/src/views/anonymousViews.ts (view pattern example)
    - test/shared/helpers/index.ts (test helper patterns if exists)
  </read_first>
  <action>
Regenerate 4 files from scratch (per D-12). Do NOT incrementally update -- rewrite completely.

**CONVENTIONS.md:**
Read representative source files to document coding patterns:
- Reducer naming: snake_case for public reducers, camelCase for helpers
- Permission pattern: ensureVerifiedUser / ensureAdmin / ensureModerator / ensureTournamentAccess hierarchy
- Table access patterns: PK.find() vs btree filter vs iter() and when each is appropriate
- Error handling: throw new Error("message") pattern, exact error message conventions
- Audit columns: createdAt/createdBy/updatedAt/updatedBy pattern with ctx.sender
- View patterns: exported view functions, procedural views with btree filters (not iter() on large tables)
- Composite PK patterns: multi-column btree indexes, filter([val1, val2])[0] accessor
- Privacy markers: `public: false` on tables with [PRIVATE] annotation convention in docs
- TypeScript patterns: type-only imports for SDK types, BigInt handling, enum variant access
- SDK 2.1.0 patterns: view exports (must be in module exports), .catch() for reducer error handling

**CONCERNS.md:**
Document known technical debt and issues:
- iter() usage on tables that should have indexes (identified in codebase audit)
- Performance considerations at scale (currently designed for 100-user baseline)
- Any TODO/FIXME comments in source code (grep for them)
- Schema migration constraints (SpacetimeDB requires --clear-database for some changes)
- Known SDK limitations or workarounds

**INTEGRATIONS.md:**
Document external integration points:
- SpacetimeDB maincloud (publishing, dashboard, energy budget)
- Discord OAuth via Next.js API route (/api/auth/link-discord)
- Imgur (screenshot URLs in match results -- stored as strings, no API integration)
- Vercel deployment (Next.js hosting)
- No other external APIs (HoYoverse import deferred)

**TESTING.md:**
Read test infrastructure to document:
- Vitest configuration (vitest.config.ts)
- Integration test architecture: SpacetimeDB module connection, test harness
- Test helper organization: test/shared/helpers/ structure (Phase 10.5)
- Test cleanup patterns: afterAll with cleanupLobby/cleanupTournament (Phase 10.5)
- Test data patterns: bootstrap data, seed scripts
- Runtime baseline (documented in Phase 10.5)
- Test commands: npm test, npm run test:all, test isolation vs suite mode
  </action>
  <verify>
    <automated>wc -l .planning/codebase/CONVENTIONS.md .planning/codebase/CONCERNS.md .planning/codebase/INTEGRATIONS.md .planning/codebase/TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/codebase/CONVENTIONS.md exists and contains `ensureVerifiedUser` or `ensureAdmin` (permission pattern)
    - .planning/codebase/CONVENTIONS.md mentions view export pattern (Phase 12.2)
    - .planning/codebase/CONCERNS.md exists and mentions `iter()` performance consideration
    - .planning/codebase/INTEGRATIONS.md exists and mentions Discord OAuth
    - .planning/codebase/INTEGRATIONS.md mentions SpacetimeDB maincloud
    - .planning/codebase/TESTING.md exists and mentions Vitest
    - .planning/codebase/TESTING.md mentions afterAll cleanup pattern (Phase 10.5)
    - All 4 files are non-trivial (at least 30 lines each)
  </acceptance_criteria>
  <done>CONVENTIONS.md, CONCERNS.md, INTEGRATIONS.md, and TESTING.md regenerated from scratch. All 7 codebase docs complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- docs-only, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | I (Information Disclosure) | .planning/codebase/ | accept | Codebase docs are internal planning context; repo is private. No secrets (env vars, API keys) documented. |
</threat_model>

<verification>
- `ls .planning/codebase/*.md | wc -l` returns 7
- Each file is non-trivial (>30 lines)
- STACK.md references SpacetimeDB SDK 2.1.0
- TESTING.md references Vitest and afterAll cleanup patterns
- CONVENTIONS.md references view export patterns
</verification>

<success_criteria>
All 7 .planning/codebase/ files regenerated from scratch, reflecting the complete state of the project after all 20 completed backend phases. Planning agents loading these files will have accurate context for Phase 14+.
</success_criteria>

<output>
After completion, create `.planning/phases/13-contract-hydration/13-04-SUMMARY.md`
</output>
