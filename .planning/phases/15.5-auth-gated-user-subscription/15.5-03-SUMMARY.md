---
phase: 15.5
plan: 03
type: execute
status: complete
wave: 2
summary_version: 1
requirements:
  - S-05
  - D-01
  - D-02
key_files:
  created:
    - test/backend/auth/auth-subscriptions.test.ts
  modified:
    - test/backend/auth/auth-views.test.ts
  deleted: []
decisions:
  - id: S-05
    summary: "Tests our FRONTEND CHOICE not to subscribe, NOT server rejection. Phase 15.2 UAT Test 4 proved spacetimedb.view() does NOT reject anon callers; Test 1 uses raw DbConnection.builder() + Stage-1-only subscribe to assert conn.db.User.iter().length === 0 for the anon path."
  - id: D-02
    summary: "VIEW-01 placeholder describe block removed from auth-views.test.ts — no test still documents the dead view_user_directory enforcement model. Net deletion: ~43 lines (97 → 52). Imports cleaned (hasServerToken no longer needed)."
metrics:
  tasks_total: 2
  tasks_completed: 2
  files_before_lines: 97
  files_after_lines: 52
  test_suite: "vitest (test/vitest.integration.config.ts)"
  test_command: "npx vitest run --config test/vitest.integration.config.ts test/backend/auth"
  test_result: "6 test files passed, 34 tests passed, 0 failures, 0 skips"
  test_duration_s: 138
  commits: 2
---

# Plan 15.5-03 Execution Summary

## What Shipped

1. **VIEW-01 placeholder block** removed from `test/backend/auth/auth-views.test.ts` — the dead-code tests documented an enforcement model that never actually existed (Phase 15.2 UAT Test 4 proved `spacetimedb.view()` does NOT reject anonymous callers at the framework level).
2. **New `test/backend/auth/auth-subscriptions.test.ts`** — integration regression guard for Plan 02's Stage 1 / Stage 2 subscription split. Two tests:
   - **Anonymous path** (always runs): raw `DbConnection.builder()` + Stage-1-only `subscribe('SELECT * FROM view_my_profile')`. Asserts `conn.db.User.iter().length === 0` — the frontend's choice not to subscribe to `user` is what keeps the cache empty.
   - **Authenticated path** (gated on `SPACETIMEDB_SERVER_TOKEN`): uses `createTestHarness()` which calls `loginAsGuest` + `subscribeToAllTables`. Asserts `conn.db.User.iter().length > 0` AND the harness's own guest user row is present.

## Task Outcomes

### Task 3.1: Delete VIEW-01 placeholder block
**Status:** complete (commit `aea2e32`)

- Removed the entire `describe('VIEW-01: view_user_directory', ...)` block (lines 23-65 in pre-edit file).
- Removed `hasServerToken` import (now unused — VIEW-02/03/04 are unconditional `expect(true).toBe(true)` placeholders).
- Retained outer `describe('Auth Views (Security Views)', …)` wrapper, file-level JSDoc, and VIEW-02 / VIEW-03 / VIEW-04 blocks.
- File shrank from 97 lines → 52 lines.
- Grep verification (all passed): VIEW-01=0, view_user_directory=0, UserDirectoryRow=0, D-15=0, D-16=0, VIEW-02/03/04≥1, hasServerToken=0.

### Task 3.2: Create auth-subscriptions.test.ts
**Status:** complete (commit `6190123`)

- File created at `test/backend/auth/auth-subscriptions.test.ts` exactly matching the plan's provided source.
- Top-level `describe('Auth Subscription Gate (Phase 15.5 S-05)', ...)` with two `it(...)` tests.
- `afterAll` teardown iterates `harnesses[]` and disconnects each.
- Test 1 uses raw `DbConnection.builder()` chain — no `loginAsGuest`, no `subscribeToAllTables` — subscribes only to `view_my_profile` and asserts empty `User` cache in `onApplied`.
- Test 2 uses `it.skipIf(!hasServerToken())` + `createTestHarness()`; asserts `User` cache is populated and contains the harness's own `userId`.
- Grep verification (all passed): `Auth Subscription Gate`=1, `anonymous visitor`=1, `authenticated visitor`=1, `SELECT \* FROM view_my_profile`=1, `loginAsGuest|subscribeToAllTables`=0 (neither in the test file's own code), `conn.db.User.iter`=3, `hasServerToken`=2.

## Test Run

```bash
npx vitest run --config test/vitest.integration.config.ts test/backend/auth
```

Result (via Monitor in-session):

```
Test Files  6 passed (6)
     Tests  34 passed (34)
  Duration  138.41s
```

All 6 files in `test/backend/auth/`:
- auth-security.test.ts
- auth-subscriptions.test.ts ← **new**
- auth-views.test.ts ← **trimmed**
- ban-admin.test.ts
- server-link-provider.test.ts
- user-deletion.test.ts

**Test 2 ran rather than skipped** — `SPACETIMEDB_SERVER_TOKEN` is present in this workstation's `.env.local`, so `hasServerToken()` returned true and the authenticated-path test exercised the full `createTestHarness` flow against maincloud. No flake observed in the single test run.

## Deviations from Plan

None. Both tasks executed exactly as specified.

## Script Reminder

The package.json `test:phase` script is defined as `vitest run --config test/vitest.integration.config.ts --dir` — the `--dir` flag expects a trailing directory arg but vitest reported "No test files found" when invoked as `npm run test:phase test/backend/auth`. Working invocation used for this run: `npx vitest run --config test/vitest.integration.config.ts test/backend/auth` (path as positional filter, not via `--dir`). Not in scope to fix the package.json script; noted for the executor's SUMMARY and potentially for a future tooling-cleanup todo.

## Self-Check: PASSED

All 6 Plan success criteria satisfied:
1. ✓ VIEW-01 block fully removed; VIEW-02/03/04 intact; file parses and runs.
2. ✓ `auth-subscriptions.test.ts` created with two tests (anon always runs; authed `skipIf(!hasServerToken())`).
3. ✓ Test 1 uses raw `DbConnection.builder()` — NO `loginAsGuest`, NO `subscribeToAllTables()`, NO sub to `user` — only `view_my_profile`.
4. ✓ Test 2 uses `createTestHarness()` (which calls loginAsGuest + subscribeToAllTables) and asserts `conn.db.User.iter().length > 0` + `myUser` defined.
5. ✓ `npx tsc --noEmit` clean on both files; full auth suite passes (34 tests, 0 failures).
6. ✓ `hasServerToken` import removed from `auth-views.test.ts` after VIEW-01 deletion.
