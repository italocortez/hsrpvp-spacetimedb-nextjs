---
phase: 15.5
slug: auth-gated-user-subscription
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-16
---

# Phase 15.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (integration config) |
| **Config file** | `test/vitest.integration.config.ts` |
| **Quick run command** | `npm run test:phase test/backend/auth` |
| **Full suite command** | `npm run test:integration` |
| **Estimated runtime** | ~30 seconds (auth dir); ~2-3 min (full suite against maincloud) |

---

## Sampling Rate

- **After every task commit:** Run `npm run test:phase test/backend/auth`
- **After every plan wave:** Run `npm run test:integration`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds per-task

---

## Per-Task Verification Map

> Task IDs finalized during planning. This row structure seeds gsd-planner.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.5-01-* | Backend cleanup | 1 | D-02, D-04 | — | Bindings regen removes `view_user_directory`, produces `view_public_hsr_accounts` | Build gate | `npm run build` (tsc over both module_bindings/) | ✅ existing | ⬜ pending |
| 15.5-02-* | Frontend Stage 1/Stage 2 split | 2 | S-03, D-01 | T-15.5-01 | Anonymous visitor never triggers `SELECT * FROM user` subscription | Integration (harness) | `npm run test:phase test/backend/auth` | ❌ W0: `auth-subscriptions.test.ts` | ⬜ pending |
| 15.5-02-* | `guestLoginPending` isolation | 2 | D-03 | — | Spinner state driven by `guestLoginPending && !currentUser` only | Manual (UI) | `/gsd-verify-work` observation | N/A | ⬜ pending |
| 15.5-03-* | VIEW-01 placeholder deletion | 2 | D-02 | — | `auth-views.test.ts` VIEW-01 block removed | Grep | `grep -c "VIEW-01" test/backend/auth/auth-views.test.ts` = 0 | ✅ existing | ⬜ pending |
| 15.5-04-* | Doc updates (views/auth/roster/FRONTEND-HANDOFF) | 3 | D-04, D-05 | — | No stale `view_public_accounts` or `view_user_directory` references | Grep | `git grep -n "view_user_directory\|view_public_accounts" docs/` returns 0 lines | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/backend/auth/auth-subscriptions.test.ts` — NEW file covering S-05 / D-01 (anonymous visitor path + authenticated visitor path via `createTestHarness`)

*No framework install needed — Vitest integration config already exists at `test/vitest.integration.config.ts` (verified).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Login button spinner shows only during `loginAsGuest` pending window | D-03 | UX concern; isolation from `isLoadingData`/`isConnecting`/`isWaitingForData`/`isLinkingDiscord` must be visually confirmed | 1. Clear localStorage + cookies. 2. Open app anon. 3. Click "Login as Guest" — spinner appears inline on button. 4. After currentUser resolves, spinner disappears. 5. Trigger Discord link flow — confirm spinner does NOT appear on Login button during that flow. 6. Reconnect WS mid-session — confirm spinner does NOT reappear. |
| `spacetime generate` produces expected final bindings shape | D-02, D-04 | Requires observation of CLI output and filesystem state | After Wave 1 publish: `ls spacetimedb/src/module_bindings/ | grep -E "view_user_directory|view_public_accounts"` returns nothing; `ls spacetimedb/src/module_bindings/ | grep view_public_hsr_accounts` returns the new file. Repeat for `src/module_bindings/`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (new `auth-subscriptions.test.ts`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s per-task
- [ ] `nyquist_compliant: true` set in frontmatter after plan-checker verifies coverage

**Approval:** pending
