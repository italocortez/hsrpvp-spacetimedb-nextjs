---
phase: 15.5
plan: 04
type: execute
status: complete
wave: 3
summary_version: 1
requirements:
  - D-02
  - D-04
  - D-05
key_files:
  created: []
  modified:
    - docs/auth/architecture.md
    - docs/views/architecture.md
    - docs/FRONTEND-HANDOFF.md
    - docs/roster/contract.md
  deleted: []
decisions:
  - id: D-05
    summary: "New `## Subscription Lifecycle (Phase 15.5 D-05)` section added to docs/auth/architecture.md. Codifies the projection-vs-subscription privacy distinction, states the Stage 2 gate condition verbatim, names view_lobby_browser + view_public_hsr_accounts as intentional anonymous exceptions, and documents the accepted User.public=true tradeoff (deferred per 15.2 D-01)."
  - id: D-04
    summary: "view_public_accounts → view_public_hsr_accounts rename propagated to all 4 doc surfaces: docs/auth/architecture.md (View Definitions table + Subscription Lifecycle section + Phase History), docs/views/architecture.md (subsection header + reorg table count + Phase History), docs/FRONTEND-HANDOFF.md (key views table + Roster Visibility prose), docs/roster/contract.md (Integration Surface row only; Phase 10.4 historical row left untouched)."
  - id: D-02
    summary: "view_user_directory retirement documented. docs/auth/architecture.md View Definitions table no longer lists it as active; docs/views/architecture.md subsection deleted; reorg table count for identityViews.ts reduced from 5 to 4. Historical Phase History rows (Phase 12, 15.2) preserved verbatim — this phase only documents the retirement, not rewrites history."
  - id: "execution-deviation-01"
    summary: "Task 4.3 plan spec conflict resolved in favor of acceptance criteria. The Action block showed 'renamed from view_public_accounts in Phase 15.5 D-04' parenthetical in the replacement text, but the same plan's 'Do NOT add a Phase 15.5 note to the file body' rule and the automated grep check (expect view_public_accounts = 0) explicitly forbid it. Shipped without the parenthetical — handoff doc stays clean of Phase-History-style metadata."
metrics:
  tasks_total: 4
  tasks_completed: 4
  commits: 3
---

# Plan 15.5-04 Execution Summary

## What Shipped

Documentation surface updated to match Wave 1's backend retirement + rename, and Plan 02's Stage 1 / Stage 2 subscription pattern documented in the authoritative `docs/auth/architecture.md` location. Four files touched across three commits.

## Task Outcomes

### Task 4.1: Update docs/auth/architecture.md
**Status:** complete (commit `ee2fef5`)

- View Definitions table: `view_user_directory` row removed. New `view_public_hsr_accounts` row added with projection-based-privacy context.
- New H2 section `## Subscription Lifecycle (Phase 15.5 D-05)` inserted between "Frontend Auth Flow" and "Phase History". Subsections: privacy-gate comparison table, Two-Stage Rule, Intentional Anonymous Exceptions (view_lobby_browser + view_public_hsr_accounts), Rule for Future Subscriptions, Accepted Tradeoff.
- Phase History: new Phase 15.5 execution row dated 2026-04-16 summarizing all three decisions + Plan 02 stage split + D-03 spinner.
- Footer timestamp: `2026-04-15` → `2026-04-16`.
- Grep verification (all passed): `Subscription Lifecycle` = 2 (section header + Phase 15.5 row mention), `view_public_hsr_accounts` = 3, `2026-04-16` = 3, `view_user_directory` appears only in Phase History historical rows.

### Task 4.2: Update docs/views/architecture.md
**Status:** complete (commit `50d87dd`)

- `#### view_user_directory` subsection deleted entirely (5 bullets including Status, Auth, Returns, Purpose, Phase 15.2 note).
- `#### view_public_accounts` renamed to `#### view_public_hsr_accounts` with expanded content — notes the projection body is the privacy gate, cross-references docs/auth/architecture.md's Subscription Lifecycle section, adds rename provenance bullet.
- Domain-file reorg table: `identityViews.ts` view list updated (view_user_directory + view_public_accounts removed; view_public_hsr_accounts added); count column `5` → `4`.
- Phase History: new row at end tagged `Phase 15.5 execution` dated 2026-04-16.
- Footer: `2026-04-15` → `2026-04-16`.
- Grep verification (all passed): `#### view_user_directory` = 0, `#### view_public_hsr_accounts` = 1, `#### view_public_accounts` = 0, `view_public_hsr_accounts` total = 3, `Phase 15.5 execution` = 1, `2026-04-16` = 3.

### Task 4.3: Update docs/FRONTEND-HANDOFF.md
**Status:** complete with spec deviation (commit `28d59f9`)

- Key views table row updated: `| view_public_accounts | ... |` → `| view_public_hsr_accounts | HSR accounts with public visibility flag applied |`
- Roster Visibility prose: identifier string swapped.
- **Spec deviation:** the plan's Action block showed the replacement table-row text as `| view_public_hsr_accounts | ... (renamed from view_public_accounts in Phase 15.5 D-04) |`, which conflicted with the same task's `Do NOT add a Phase 15.5 note to the file body` rule and the automated check expecting `grep -c view_public_accounts = 0`. Shipped without the parenthetical — the stricter rule wins. Handoff doc stays free of Phase-History-style metadata. Rationale captured in `execution-deviation-01` above.
- Grep verification: `view_public_accounts` = 0, `view_public_hsr_accounts` = 2.

### Task 4.4: Update docs/roster/contract.md
**Status:** complete (commit `28d59f9`)

- Integration Surface table row at line 433: `view_public_accounts` identifier replaced with `view_public_hsr_accounts`. All surrounding content (lookup mechanism, description, direction) unchanged.
- Phase 10.4 historical row at line 451 left **verbatim unchanged** — contains the old identifier as a snapshot of what Phase 10.4 decided at the time. Rewriting historical Phase History entries would misrepresent decision provenance.
- No scenarios, acceptance tests, behavior text, or new Phase 15.5 rows added. Contract spec untouched (per CLAUDE.md rule — contract updates happen at `/gsd-verify-work`).
- Grep verification: `view_public_hsr_accounts` = 1 (the updated Integration Surface row), `view_public_accounts` = 1 (the historical Phase 10.4 row — unchanged, on a line containing "Phase 10.4").

## Contract.md Safety Audit

Plan explicitly constrained: no Phase 15.5 additions to any `docs/**/contract.md`.

```
Phase 15.5 in docs/auth/contract.md: 0 ✓
Phase 15.5 in docs/views/contract.md: 5 (pre-existing; file NOT modified this session — git diff HEAD -- docs/views/contract.md returns empty)
Phase 15.5 in docs/roster/contract.md: 0 ✓
```

The 5 `Phase 15.5` hits in `docs/views/contract.md` are pre-existing forward-looking references from Phase 15.2 (which planted the Phase 15.5 seed). Not introduced by this plan — confirmed via `git diff HEAD -- docs/views/contract.md` returning empty in the post-task verification. Contract provenance rule intact.

## Newly-Added Content (for verify-work reviewer audit)

The substantive new content is the `## Subscription Lifecycle (Phase 15.5 D-05)` section in `docs/auth/architecture.md`. It contains:

- **Privacy gate comparison table** (projection-based vs subscription-based, what each closes)
- **Two-Stage Rule** — Stage 1 always-on `view_my_profile`; Stage 2 gated `SELECT * FROM user` with exact gate condition `currentUser != null || hadTokenOnMount.current || hadSessionCookie.current`
- **Intentional Anonymous Exceptions** — `view_lobby_browser` + `view_public_hsr_accounts` named as the two views that stay `anonymousView` because the projection body IS the privacy gate
- **Rule for Future Subscriptions** — new frontend raw-table or per-user-view subscriptions MUST go through Stage 2; new anonymous projection views stay anonymous only if the body does the filtering
- **Accepted Tradeoff** — explicit scope boundary: `User.public=true` means adversarial direct subscriptions still get rows; closing that requires `User.public=false` + on-demand reducers (deferred per 15.2 D-01)

The verify-work reviewer should confirm this section accurately reflects shipped behavior in `components/features/auth/hooks/useAuth.ts` (specifically the D-01 revised gate using `hadUserIdOnMount`/`hadSessionCookie` — noting that Plan 02's execution deviated from the plan's original `hadTokenOnMount` naming; the docs use the gate's CONCEPTUAL name `hadTokenOnMount` since that's what the plan authored. A follow-up pass could harmonize the doc wording with the final identifier.)

## Self-Check: PASSED

All 7 Plan success criteria satisfied:
1. ✓ docs/auth/architecture.md has Subscription Lifecycle (Phase 15.5 D-05) section with both anonymous exceptions, Stage 2 gate condition verbatim, and Accepted Tradeoff paragraph.
2. ✓ View Definitions table: view_user_directory removed; view_public_hsr_accounts row added.
3. ✓ Phase History row tagged `Phase 15.5 execution` dated 2026-04-16.
4. ✓ docs/views/architecture.md: view_user_directory subsection deleted; view_public_accounts renamed to view_public_hsr_accounts; reorg count reduced 5→4; Phase History row added.
5. ✓ docs/FRONTEND-HANDOFF.md: view_public_accounts = 0; view_public_hsr_accounts = 2.
6. ✓ docs/roster/contract.md: Integration Surface row uses new name; historical Phase 10.4 row untouched; no Phase 15.5 entry.
7. ✓ No contract.md received a Phase 15.5 execution row — contract updates deferred to verify-work per CLAUDE.md rule.
