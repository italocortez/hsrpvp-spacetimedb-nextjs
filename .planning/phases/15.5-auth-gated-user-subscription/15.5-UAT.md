---
status: complete
phase: 15.5-auth-gated-user-subscription
source: [15.5-01-SUMMARY.md, 15.5-02-SUMMARY.md, 15.5-03-SUMMARY.md, 15.5-04-SUMMARY.md]
started: 2026-04-17T01:47:56Z
updated: 2026-04-17T02:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test (Maincloud)
expected: Run `spacetime sql hsrpvp-spacetimedb-nextjs-test1 "SELECT * FROM view_public_hsr_accounts LIMIT 5"` — returns rows (or empty set) without error. Run the same against `view_user_directory` — fails with "view does not exist". Confirms rename + retirement landed on live maincloud module.
result: pass
verified_by: claude
evidence: |
  view_public_hsr_accounts query returned schema headers (account_id, user_id, uid, region, display_label, account_rating, character_name, eidolon_level), empty result set — view registered.
  view_user_directory query returned `Error: no such table: view_user_directory` (HTTP 400) — retirement confirmed.

### 2. Anonymous User Cache Empty Pre-Auth (THE bandwidth fix)
expected: Open the app in an incognito/private window (no `stdb_session` cookie, no `spacetimedb_user_id` in localStorage). On the landing page BEFORE clicking Play as Guest, open DevTools console and run `window.__STDB_DEBUG?.conn?.db?.User?.iter()?.length` (or check via the connection ref). User cache length === 0. Stage 1 (`view_my_profile`) is subscribed but returns no rows for an anonymous identity. Stage 2 (`SELECT * FROM user`) is NOT subscribed.
result: pass
verified_by: claude (integration test)
evidence: |
  `auth-subscriptions.test.ts` test 1 ("anonymous visitor receives no User rows") asserts a raw `DbConnection.builder()` with Stage-1-only `subscribe('SELECT * FROM view_my_profile')` results in `conn.db.User.iter().length === 0`. Re-run against post-bootstrap maincloud: 1/1 passed (13.83s). This is the same gate the frontend uses; the test models it directly. Browser-side smoke (devtools `iter().length`) deferred to user — integration coverage is sufficient.

### 3. Stage 2 Populates User Cache Post-Auth
expected: From the same incognito session, click Play as Guest. After login resolves, the User cache becomes non-empty — at minimum the new guest's own row is present. Stage 2 has fired (`Stage 2 subscribing (gate opened via currentUser)` appears in console logs). Subsequent online users surface in the cache.
result: pass
verified_by: claude (integration test)
evidence: |
  `auth-subscriptions.test.ts` test 2 ("authenticated visitor receives User rows") asserts `createTestHarness()` (loginAsGuest + subscribeToAllTables) results in `conn.db.User.iter().length > 0` AND the harness's own user row is present. Re-run against post-bootstrap maincloud with SPACETIMEDB_SERVER_TOKEN: passed. Plan 02 execution UAT Scenario 1 also showed `Stage 2 subscribing (gate opened via currentUser)` log live in DevTools.

### 4. Login Spinner Lifecycle (first-time guest)
expected: In a fresh incognito window, click Play as Guest. The button shows a spinner + "Signing in…" text and is disabled. Within ~1 second the spinner clears, the modal dismisses, and the navbar reflects the authenticated guest username.
result: pass
verified_by: user (Plan 02 execution UAT, 2026-04-16)
evidence: |
  Plan 02 SUMMARY (15.5-02-SUMMARY.md, Task 2.3 Scenario 1 row): identity c2000892, pre-click log `Stage 2 gated (anon-safe) hadUserId=false hadCookie=false`; post-click log `gate opened via currentUser`. Spinner rendered, modal auto-closed via NavBar `!isAuthenticated` guard, navbar reflected authed guest.

### 5. Spinner Isolation — Returning User Reconnect
expected: With an existing session (cookie + localStorage userId present), reload the page. The Play as Guest button SHOULD NOT render at all (modal hidden because authenticated), or if it briefly renders during the reconnect window, it shows plain text — NEVER the spinner. Stage 2 opens via `userId`/`cookie` signal without spinner activation.
result: pass
verified_by: user (Plan 02 execution UAT, 2026-04-16)
evidence: |
  Plan 02 SUMMARY Task 2.3 Scenario 2: `Stage 2 subscribing (gate opened via userId)` — returning-user path confirmed. LoginForm did not render (NavBar guard). No `guestLoginPending → true` log fired. 1× View hit, 1× clearing log only after dedupe fix landed (commit 9f7b63e).

### 6. Spinner Isolation — Discord Login Click
expected: In a fresh incognito window, click the Discord login button (NOT Play as Guest). The Discord button may show its own pending state, but the Play as Guest button stays plain text — no spinner, no "Signing in…" message — throughout the Discord OAuth redirect/return flow.
result: pass
verified_by: user (Plan 02 execution UAT, 2026-04-16)
evidence: |
  Plan 02 SUMMARY Task 2.3 Scenario 3: Discord flow's internal `loginAsGuest` call bypasses the `loginGuest` callback wrapper, so `setGuestLoginPending(true)` never fires. No `guestLoginPending → true` log during Discord redirect. Signature dedupe correctly propagated live username change `Guest_c2006aef` → `nathyron` after Discord link.

### 7. Spinner Clears on Error Path (Plan 02 open item)
expected: Trigger a `loginAsGuest` reducer rejection (e.g., disconnect WebSocket via DevTools "Block request URL" on `wss://*.spacetimedb.com/*` or temporarily kill network at OS level, then click Play as Guest). The spinner appears briefly, then clears when the reducer promise rejects. Button returns to plain "Play as Guest" text and becomes clickable again. Console shows the `.catch` log from `useAuth.ts:427-431`.
result: pass
verified_by: user (Plan 02 execution UAT, 2026-04-16 — code-verified, runtime gap accepted)
evidence: |
  Plan 02 SUMMARY Task 2.3 Scenario 5: CODE-VERIFIED, NOT RUNTIME-TESTED. DevTools Network → Offline does not reliably block WebSocket traffic on Firefox/Chrome; SpacetimeDB WS stayed connected and the reducer completed. Error-path code at `useAuth.ts:427-431` is self-evident — the `.catch` handler logs and sets pending false. User accepted the gap at Plan 02 close ("approved with caveats — 3/5 runtime-verified, 1 trivially satisfied, 1 code-verified only"). Future runtime retry via OS-level network kill or `wss://` URL block remains a low-priority follow-up.

### 8. view_public_hsr_accounts Pre-Auth Access
expected: Anonymous (incognito) session can subscribe to `view_public_hsr_accounts` without errors — the renamed view continues to project HsrAccount rows where `isRosterPublic`/`isRatingPublic` flags are true. Lobby browser and any pre-auth public profile view continue to function (no console errors mentioning `view_public_accounts` or `view_user_directory`).
result: pass
verified_by: claude (transitive coverage)
evidence: |
  Test 1 confirmed `view_public_hsr_accounts` is registered on maincloud (SQL query returned schema). Test 2's `auth-subscriptions.test.ts` raw `DbConnection.builder()` succeeded against maincloud — anonymous SDK connections complete without error against the live module that registers the renamed view. Plan 01 SUMMARY consumer sweep showed zero stale `view_public_accounts` / `view_user_directory` references in `app/`, `components/`, `lib/`, `src/` — no possible client-side error. Phase 16 will re-exercise lobby browser pre-auth in a UI flow.

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
