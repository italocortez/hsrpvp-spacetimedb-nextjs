---
phase: 15.5
plan: 04
type: execute
wave: 3
depends_on: [15.5-01]
files_modified:
  - docs/auth/architecture.md
  - docs/views/architecture.md
  - docs/FRONTEND-HANDOFF.md
  - docs/roster/contract.md
autonomous: true
requirements:
  - D-02
  - D-04
  - D-05
user_setup: []

must_haves:
  truths:
    - "docs/auth/architecture.md has a new Subscription Lifecycle section documenting Stage 1 / Stage 2 rule and naming view_lobby_browser + view_public_hsr_accounts as anonymous exceptions"
    - "docs/auth/architecture.md no longer describes view_user_directory as an active view (retirement documented)"
    - "docs/views/architecture.md no longer has a view_user_directory subsection; view_public_accounts subsection renamed to view_public_hsr_accounts"
    - "docs/FRONTEND-HANDOFF.md has view_public_accounts references updated to view_public_hsr_accounts"
    - "docs/roster/contract.md has view_public_accounts references updated to view_public_hsr_accounts"
    - "No contract.md edits performed during execution (per CLAUDE.md rule — contract updates happen at /gsd-verify-work tagged 'Phase 15.5 execution')"
  artifacts:
    - path: "docs/auth/architecture.md"
      provides: "Subscription Lifecycle section + view_user_directory retirement entry in Phase History"
      contains: "Subscription Lifecycle"
      contains: "view_public_hsr_accounts"
    - path: "docs/views/architecture.md"
      provides: "view_user_directory retired; view_public_accounts renamed"
      contains: "view_public_hsr_accounts"
      contains_not_in_view_list: "view_user_directory (must only appear in Phase History rows, not as an active view)"
  key_links:
    - from: "docs/auth/architecture.md Subscription Lifecycle section"
      to: "useAuth.ts Stage 1 / Stage 2 split (Plan 02)"
      via: "named rule + two named anonymous exceptions"
      pattern: "Stage 2"
---

<objective>
Update the documentation surface to match the Wave 1 backend retirement + rename and document the Plan 02 Stage 1 / Stage 2 subscription pattern. Scoped to `docs/auth/architecture.md` (new Subscription Lifecycle section + view retirement), `docs/views/architecture.md` (view list update), and two additional doc sites with stale `view_public_accounts` strings (`docs/FRONTEND-HANDOFF.md`, `docs/roster/contract.md`).

Purpose: D-05 requires the projection-vs-subscription privacy distinction to be documented in `docs/auth/architecture.md` so future phases don't recreate the leak. D-04 requires rename consistency across doc sites so future developers reading the docs don't write code against a non-existent view name (Pitfall 5).

**CLAUDE.md CONSTRAINT:** Contract.md files are NOT edited during execution. `docs/auth/contract.md`, `docs/views/contract.md`, `docs/roster/contract.md` Phase History entries get added at `/gsd-verify-work` tagged `Phase 15.5 execution`. This plan makes ONE exception: `docs/roster/contract.md` has `view_public_accounts` strings in its Integration Surface table (lines 433, 451) that are TEXTUAL REFERENCES to an external identifier — not contract spec content — and must be updated in sync with the rename to avoid dangling references. No new Phase History entry or contract spec change is added; only the identifier string is replaced.

Output: Four doc files updated. Contract.md Phase History entries deferred to verify-work.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.5-auth-gated-user-subscription/15.5-CONTEXT.md
@.planning/phases/15.5-auth-gated-user-subscription/15.5-RESEARCH.md
@.planning/phases/15.5-auth-gated-user-subscription/15.5-PATTERNS.md
@.planning/phases/15.5-auth-gated-user-subscription/15.5-01-SUMMARY.md

@docs/auth/architecture.md
@docs/views/architecture.md
@docs/FRONTEND-HANDOFF.md
@docs/roster/contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Update docs/auth/architecture.md — add Subscription Lifecycle section + retire view_user_directory entry</name>
  <files>docs/auth/architecture.md</files>
  <read_first>
    - `docs/auth/architecture.md` (full — skim ~220 lines; pay attention to lines 101-117 View Definitions table, lines 182-194 Frontend Auth Flow section, lines 196-212 Phase History table)
    - `.planning/phases/15.5-auth-gated-user-subscription/15.5-CONTEXT.md` D-05 (content requirements for the new section)
    - `.planning/phases/15.5-auth-gated-user-subscription/15.5-RESEARCH.md` §Security Domain + Architectural Responsibility Map
    - `components/features/auth/hooks/useAuth.ts` (final post-Plan-02 state — for accurate section wording)
  </read_first>
  <action>
    Three distinct edits in `docs/auth/architecture.md`:

    **Edit A — View Definitions table (currently lines 101-108).**
    Remove the `view_user_directory` row. Before (line 108):
    ```
    | `view_user_directory` | Authenticated clients only | Projected UserDirectoryRow -- safe D-16 subset (excludes audit cols, deletedAt, lastLoginAt, auth IDs); Phase 15.2 D-06: authenticated-only via `spacetimedb.view()` |
    ```
    After: delete the entire row. The table should now have exactly 3 rows: `view_my_profile`, `view_my_identity`, `view_admin_user_private`.

    Add one new row ABOVE the existing rows (after the header separator, before `view_my_profile`) — or alphabetize if that matches the file's convention; current file is not alphabetized so append at the end of the 3-row table:
    ```
    | `view_public_hsr_accounts` | Anonymous (projection-based privacy) | HsrAccount rows where `isRosterPublic=true`; rating included when `isRatingPublic=true`. Flat rows (one per character). Renamed from `view_public_accounts` in Phase 15.5 (D-04) — `hsr_` names the source table explicitly. Stays `anonymousView` per Phase 15.5 D-05 (projection body is the privacy gate, not subscription timing). |
    ```

    **Edit B — Add new "Subscription Lifecycle" section.**
    Insert a new H2 section immediately AFTER the existing "Frontend Auth Flow (useAuth.ts)" section (currently ends at line 194, right before "## Phase History" at line 196).

    Insert this block verbatim:

    ```markdown
    ## Subscription Lifecycle (Phase 15.5 D-05)

    SpacetimeDB offers two complementary privacy tools for client-facing data:

    | Privacy Gate | Where It Runs | What It Closes |
    |--------------|---------------|----------------|
    | **Projection-based** (anonymous views) | Server — inside the view body | What a caller *sees* (field masking, row filtering by opt-in flags) |
    | **Subscription-based** (client gating) | Client — inside `useAuth.ts` | When a subscription *fires* (anonymous visitors cause zero pre-auth egress on sensitive tables) |

    These are not substitutes. Projection-based privacy is authoritative for any data the user has opted to share anonymously; subscription-based gating is how the client decides when to incur the bandwidth cost of a raw table subscription.

    ### The Two-Stage Rule (`useAuth.ts`)

    - **Stage 1 — always on:** `SELECT * FROM view_my_profile` subscribes as soon as the SpacetimeDB connection is active. The server applies the `ctx.sender` filter — anonymous callers receive 0 rows, authenticated callers receive their single merged User + UserPrivate row.
    - **Stage 2 — gated:** `SELECT * FROM user` subscribes ONLY when `currentUser != null || hadTokenOnMount.current || hadSessionCookie.current`. This gate composition matches `isWaitingForData`'s signal set (reuses existing refs; zero new infrastructure). Token / cookie presence is a UX hint — if either is stale/revoked, the WS handshake itself fails (`isActive` stays false) and Stage 2 never fires regardless.

    A truly anonymous visitor (no cached token, no session cookie, no resolved User) causes zero egress on the User table. The bandwidth leak that Phase 15.2 UAT Test 4 exposed — unconditional `SELECT * FROM user` pre-auth — is closed by this split.

    ### Intentional Anonymous Exceptions

    Two views stay `anonymousView` by design. Their privacy gate is the projection body, not subscription timing:

    - **`view_lobby_browser`** — filters out config details (timers, budgets, penalties, audit columns, hostId) and filters to active stages only. Anonymous BY DESIGN so visitors can browse lobbies pre-auth.
    - **`view_public_hsr_accounts`** — filters to `isRosterPublic=true` rows; rating field only emitted when `isRatingPublic=true`. Anonymous BY DESIGN so pre-auth profile browsing works.

    These do NOT need Stage 2 gating — applying it would regress intentional pre-auth UX.

    ### Rule for Future Subscriptions

    Any new frontend subscription to a raw table (no projection view body) OR a per-user view MUST go through the Stage 2 gate. If a new anonymous projection view is added whose body performs the privacy filtering, it may remain `anonymousView` — document that decision in this section alongside the two existing exceptions.

    ### Accepted Tradeoff (from 15.2 D-01 / 15.5 CONTEXT)

    `User.public = true` means a malicious actor subscribing directly to `SELECT * FROM user` (not via our frontend) still receives rows. Closing that requires `User.public = false` + on-demand read reducers — explicitly deferred in Phase 15.5 CONTEXT deferred ideas. This phase's scope is "our frontend does not auto-subscribe anonymous," not "adversarial clients cannot query User."
    ```

    **Edit C — Phase History table (currently ends at line 212).**
    Append one new row at the bottom of the table (just above the `---` separator at line 214). Use this exact content:
    ```
    | Phase 15.5: `view_user_directory` deleted (dead code — zero client subscribers, cosmetic 15.2 D-06 flip retired); `view_public_accounts` renamed to `view_public_hsr_accounts` (D-04) — underlying table is HsrAccount, name now explicit; `useAuth.ts` subscription split into Stage 1 (`view_my_profile`, always) and Stage 2 (`SELECT * FROM user`, gated on `currentUser != null \|\| hadTokenOnMount.current \|\| hadSessionCookie.current`) — closes the UAT Test 4 bandwidth leak; new `guestLoginPending` state drives a narrow Login-button spinner scoped strictly to the `loginAsGuest` pending window (D-03); `docs/auth/architecture.md` Subscription Lifecycle section codifies the projection-vs-subscription privacy distinction (D-05) with `view_lobby_browser` and `view_public_hsr_accounts` named as intentional anonymous exceptions | Phase 15.5 execution | 2026-04-16 |
    ```

    **Update the file-footer timestamp** at line 216 from `*Last updated: 2026-04-15*` to `*Last updated: 2026-04-16*`.

    Do NOT touch any other section (Overview, Table Relationships, Reducer Flows, Ban Enforcement, API Route, performUserDeletion, resolveUserLabel, Frontend Auth Flow).

    Do NOT touch `docs/auth/contract.md` — contract updates happen at verify-work.
  </action>
  <verify>
    Grep assertions on `docs/auth/architecture.md`:
    - `grep -c "Subscription Lifecycle" docs/auth/architecture.md` ≥ 1 (section header landed)
    - `grep -c "view_user_directory" docs/auth/architecture.md` — should appear ONLY in the Phase History table rows (existing rows at old lines 207, 211, 212 + the new Phase 15.5 row). Count expected: 4 (3 pre-existing historical mentions + 1 new retirement entry). **Must NOT appear in the current View Definitions table.**
    - `grep -c "view_public_hsr_accounts" docs/auth/architecture.md` ≥ 3 (View Definitions table row + Subscription Lifecycle section naming it as exception + Phase History entry)
    - `grep -c "view_public_accounts\b" docs/auth/architecture.md | ` — should appear only in the Phase History row referencing the rename source name. Count expected: 1 (the new Phase 15.5 entry says "renamed from `view_public_accounts`"). All OTHER occurrences must use the new name.
    - Grep for the word-boundary `\bview_lobby_browser\b` and `\bview_public_hsr_accounts\b` both appear in the new section naming them as anonymous exceptions.
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && grep -c "Subscription Lifecycle" docs/auth/architecture.md && test $(grep -c "view_public_hsr_accounts" docs/auth/architecture.md) -ge 3 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/auth/architecture.md` View Definitions table no longer has a `view_user_directory` row
    - `docs/auth/architecture.md` View Definitions table has a new `view_public_hsr_accounts` row (anonymous, projection-based privacy)
    - `docs/auth/architecture.md` has an H2 section titled exactly `## Subscription Lifecycle (Phase 15.5 D-05)`
    - That section names `view_lobby_browser` AND `view_public_hsr_accounts` as the two anonymous exceptions
    - That section states the Stage 2 gate condition verbatim (`currentUser != null || hadTokenOnMount.current || hadSessionCookie.current`)
    - That section includes the "Accepted Tradeoff" paragraph referencing 15.2 D-01
    - Phase History table has a new row tagged `Phase 15.5 execution` with date `2026-04-16`
    - Last-updated footer shows `2026-04-16`
    - No edits to `docs/auth/contract.md`
  </acceptance_criteria>
  <done>
    D-05's Subscription Lifecycle documentation exists. The file no longer lists `view_user_directory` as an active view.
  </done>
</task>

<task type="auto">
  <name>Task 4.2: Update docs/views/architecture.md — retire view_user_directory, rename view_public_accounts</name>
  <files>docs/views/architecture.md</files>
  <read_first>
    - `docs/views/architecture.md` (full — skim ~200 lines; pay attention to lines 28-33 `view_user_directory` subsection, lines 40-44 `view_public_accounts` subsection, line 150 reorg table, lines 186-189 Phase History rows)
    - Task 4.1 acceptance criteria — docs/auth/architecture.md must land first for coherent cross-ref in views/architecture
  </read_first>
  <action>
    Five distinct edits in `docs/views/architecture.md`:

    **Edit A — Delete the `view_user_directory` subsection (lines 28-33).**
    Remove lines 28-33 in their entirety:
    ```
    #### view_user_directory
    - **Status:** Dead code — scheduled for removal in Phase 15.5 ...
    - **Auth (as declared):** `spacetimedb.view()` ...
    - **Returns:** `UserDirectoryRow[]` ...
    - **Purpose (historical):** User search for invites, team requests, etc. Never wired on the frontend.
    - **Phase 15.2 note:** Ghost users no longer accumulate ...
    ```
    After deletion, `#### view_my_roster` immediately follows `#### view_admin_user_private` (line 27).

    **Edit B — Rename the `view_public_accounts` subsection (currently lines 40-43).**
    Change the header from `#### view_public_accounts` to `#### view_public_hsr_accounts`.
    Expand the purpose line to note the rename:
    Before:
    ```
    #### view_public_accounts
    - **Auth:** Anonymous (`anonymousView`)
    - **Returns:** `HsrAccount[]` where `isRosterPublic=true` for a target userId
    - **Purpose:** Public profile browsing
    ```
    After:
    ```
    #### view_public_hsr_accounts
    - **Auth:** Anonymous (`anonymousView`) — projection body is the privacy gate (isRosterPublic / isRatingPublic opt-ins); intentional anonymous exception per `docs/auth/architecture.md` Subscription Lifecycle section
    - **Returns:** Flat `PublicHsrAccountRow[]` — one row per account-character pair where `isRosterPublic=true`; rating field emitted only when `isRatingPublic=true`
    - **Purpose:** Pre-auth public profile browsing
    - **Renamed:** Phase 15.5 (D-04) from `view_public_accounts` — `hsr_` in the name identifies the underlying HsrAccount table
    ```

    **Edit C — Update the domain-file reorg table (line 150).**
    Before:
    ```
    | `identityViews.ts` | view_my_identity, view_my_profile, view_user_directory, view_public_accounts, view_admin_user_private | 5 |
    ```
    After:
    ```
    | `identityViews.ts` | view_my_identity, view_my_profile, view_public_hsr_accounts, view_admin_user_private | 4 |
    ```
    Note: count changes from `5` to `4`.

    **Edit D — Phase History table, add new row.**
    The current last row is the Phase 15.2 UAT entry at line 189. Append one new row just above the `---` separator at line 191:
    ```
    | Phase 15.5: `view_user_directory` retired (dead code, zero client subscribers); `view_public_accounts` renamed to `view_public_hsr_accounts` (D-04) — `hsr_` names the source table. Both bindings dirs regenerated via single-shot `spacetime generate` after publish. Frontend subscription lifecycle codified in `docs/auth/architecture.md` Subscription Lifecycle section (D-05): Stage 1 `view_my_profile` always; Stage 2 raw User subscription gated on `currentUser != null \|\| hadTokenOnMount.current \|\| hadSessionCookie.current`. `view_lobby_browser` + `view_public_hsr_accounts` named as intentional anonymous exceptions (projection-based privacy) | Phase 15.5 execution | 2026-04-16 |
    ```

    **Edit E — Last-updated footer (line 193).**
    Change `*Last updated: 2026-04-15*` to `*Last updated: 2026-04-16*`. Leave the "Feature owner" line untouched.

    Do NOT touch any other view subsection. Do NOT touch `docs/views/contract.md` — contract updates happen at verify-work.
  </action>
  <verify>
    Grep assertions on `docs/views/architecture.md`:
    - `grep -c "#### view_user_directory" docs/views/architecture.md` = 0 (subsection header gone)
    - `grep -c "#### view_public_hsr_accounts" docs/views/architecture.md` = 1 (new subsection header)
    - `grep -c "#### view_public_accounts" docs/views/architecture.md` = 0 (old subsection header gone)
    - `grep -c "view_public_accounts" docs/views/architecture.md` — should only appear in the Phase 15.5 history row and one prior Phase 10.4 history row. Count expected: ≤ 3 (prior 10.4 rows + new 15.5 rename mention).
    - `grep -c "view_public_hsr_accounts" docs/views/architecture.md` ≥ 3 (subsection + reorg table + Phase 15.5 row)
    - `grep -c "Phase 15.5 execution" docs/views/architecture.md` = 1 (new row)
    - `grep -c "2026-04-16" docs/views/architecture.md` ≥ 2 (Phase 15.5 row date + footer)
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && test $(grep -c "#### view_user_directory" docs/views/architecture.md) -eq 0 && test $(grep -c "#### view_public_hsr_accounts" docs/views/architecture.md) -eq 1 && test $(grep -c "#### view_public_accounts" docs/views/architecture.md) -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - No `#### view_user_directory` subsection header in the file
    - Exactly one `#### view_public_hsr_accounts` subsection header; no `#### view_public_accounts` subsection header
    - Reorg table at line ~150 shows `identityViews.ts` with 4 views (no `view_user_directory`; `view_public_hsr_accounts` instead of `view_public_accounts`); count column reads `4`
    - Phase History table has a new row tagged `Phase 15.5 execution` with date `2026-04-16` naming both the retirement and the rename
    - Last-updated footer is `2026-04-16`
    - No edits to `docs/views/contract.md`
  </acceptance_criteria>
  <done>
    Views architecture doc reflects the retirement + rename. Phase History records the phase execution.
  </done>
</task>

<task type="auto">
  <name>Task 4.3: Update docs/FRONTEND-HANDOFF.md — rename view_public_accounts references</name>
  <files>docs/FRONTEND-HANDOFF.md</files>
  <read_first>
    - `docs/FRONTEND-HANDOFF.md:97-115` (key views table around line 109)
    - `docs/FRONTEND-HANDOFF.md:180-195` (Roster Visibility section around line 187)
    - `.planning/phases/15.5-auth-gated-user-subscription/15.5-RESEARCH.md` §Pitfall 5 (enumerates the exact two line numbers)
  </read_first>
  <action>
    Two string replacements in `docs/FRONTEND-HANDOFF.md`:

    **Edit A — Key views table row, line ~109.**
    Before:
    ```
    | `view_public_accounts` | HSR accounts with public visibility flag applied |
    ```
    After:
    ```
    | `view_public_hsr_accounts` | HSR accounts with public visibility flag applied (renamed from `view_public_accounts` in Phase 15.5 D-04) |
    ```

    **Edit B — Roster Visibility prose, line ~187.**
    Before:
    ```
    Roster visibility (public/private) is server-enforced at the view layer. `view_public_accounts` and `view_my_roster` handle the filtering. The frontend never needs to manually check `isRosterPublic`.
    ```
    After:
    ```
    Roster visibility (public/private) is server-enforced at the view layer. `view_public_hsr_accounts` and `view_my_roster` handle the filtering. The frontend never needs to manually check `isRosterPublic`.
    ```

    Do NOT touch any other line. Do NOT add a Phase 15.5 note to the file body — this is a handoff doc, not a Phase History ledger.

    Do a final grep to confirm no remaining `view_public_accounts` strings exist outside of deliberate historical mentions:
    ```bash
    grep -n "view_public_accounts" docs/FRONTEND-HANDOFF.md
    ```
    Must return zero hits after the edits.
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && test $(grep -c "view_public_accounts" docs/FRONTEND-HANDOFF.md) -eq 0 && test $(grep -c "view_public_hsr_accounts" docs/FRONTEND-HANDOFF.md) -ge 2 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "view_public_accounts" docs/FRONTEND-HANDOFF.md` = 0
    - `grep -c "view_public_hsr_accounts" docs/FRONTEND-HANDOFF.md` ≥ 2 (table row + Roster Visibility prose)
    - No other lines in the file modified
  </acceptance_criteria>
  <done>
    Handoff doc rename complete.
  </done>
</task>

<task type="auto">
  <name>Task 4.4: Update docs/roster/contract.md — rename view_public_accounts references (identifier-only, no contract spec change)</name>
  <files>docs/roster/contract.md</files>
  <read_first>
    - `docs/roster/contract.md:425-460` — the Integration Surface table containing lines 433 and 451
    - `.planning/CLAUDE.md` "Backend Feature Docs" rule — contract.md is off-limits for contract spec changes during execution; this task updates IDENTIFIER STRINGS ONLY (references to an external symbol that was renamed), not contract specification content or Phase History
    - `.planning/phases/15.5-auth-gated-user-subscription/15.5-RESEARCH.md` §Pitfall 5 confirms these are the only two roster/contract.md occurrences
  </read_first>
  <action>
    Two identifier string replacements. DO NOT add a Phase 15.5 history row. DO NOT touch contract scenarios, behaviors, or the Phase History table. The ONLY edit is the two references to the renamed view identifier.

    **Edit A — Line 433:**
    Before:
    ```
    | view_public_accounts | HsrAccount.iter() + HsrAccountCharacter.hsr_account_id | Public roster view | Reads |
    ```
    After:
    ```
    | view_public_hsr_accounts | HsrAccount.iter() + HsrAccountCharacter.hsr_account_id | Public roster view | Reads |
    ```

    **Edit B — Line 451:**
    Before:
    ```
    | HsrAccount and HsrAccountCharacter made private — view_my_roster and view_public_accounts replace raw subscriptions (D-20) | Phase 10.4 execution | 2026-04-04 |
    ```
    After: **LEAVE THIS LINE UNCHANGED.** This is a Phase History entry documenting what Phase 10.4 did at the time. It names the view by the name it had at the time. Re-writing historical Phase History entries is wrong — they record what was decided and shipped. If a future reader wonders why the identifier differs from current source, they'll find the Phase 15.5 rename row in `docs/views/architecture.md` and `docs/auth/architecture.md`.

    So only Edit A applies. Verify with a pre/post grep:
    - Before: `grep -n "view_public_accounts" docs/roster/contract.md` returns 2 lines (433, 451)
    - After: `grep -n "view_public_accounts" docs/roster/contract.md` returns 1 line (451 — historical Phase 10.4 row)
    - After: `grep -n "view_public_hsr_accounts" docs/roster/contract.md` returns 1 line (433 — renamed Integration Surface row)

    **Rationale for the historical-row policy:** CLAUDE.md's `feedback_docs_maintenance.md` and the contract.md rule treat Phase History rows as immutable provenance — they document what a past phase did. The Integration Surface table at line 433 is current-state metadata (what the code references RIGHT NOW) and must track the rename. The Phase History row at line 451 is past-state metadata (what Phase 10.4 decided) and stays as-is.
  </action>
  <verify>
    <automated>cd D:/GitsWork/hsrpvp-spacetimedb-nextjs && test $(grep -c "view_public_hsr_accounts" docs/roster/contract.md) -eq 1 && test $(grep -c "view_public_accounts" docs/roster/contract.md) -eq 1 && grep -n "view_public_accounts" docs/roster/contract.md | grep -q "Phase 10.4" && echo PASS</automated>
    The remaining `view_public_accounts` occurrence MUST be on a line containing the substring "Phase 10.4" (confirms it's the historical row, not the live integration reference).
  </verify>
  <acceptance_criteria>
    - `grep -c "view_public_hsr_accounts" docs/roster/contract.md` = 1 (the renamed Integration Surface row)
    - `grep -c "view_public_accounts\b" docs/roster/contract.md` = 1 (the historical Phase 10.4 row — unchanged)
    - The remaining `view_public_accounts` occurrence is on the Phase 10.4 historical row (`grep -n` confirms line contains "Phase 10.4")
    - No Phase 15.5 Phase History entry added to `docs/roster/contract.md`
    - No scenario, acceptance test, or behavior text modified
  </acceptance_criteria>
  <done>
    Roster contract Integration Surface reference updated to the new identifier. Historical Phase 10.4 row preserved verbatim. No spec change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer reading docs → future code | Stale doc identifiers cause devs to write code against non-existent view names (Pitfall 5 from RESEARCH.md) |
| docs → reviewers on subsequent phases | Subscription Lifecycle section is load-bearing for Phase 16+ subscription placement decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-01 | Information Disclosure | Documented accepted tradeoff (User.public=true adversarial query) | accept | Subscription Lifecycle section's "Accepted Tradeoff" paragraph explicitly calls out the 15.2 D-01 scope boundary — future devs see the limitation documented, preventing a false sense of closure. |
| T-15.5-02 | Spoofing (cosmetic) | N/A | accept | N/A — doc-only plan. |
| T-15.5-03 | Elevation of Privilege | Stale token + cookie opens Stage 2 | mitigate (defense-in-depth) | Subscription Lifecycle section documents the WS-handshake defense-in-depth layer: even if stale signals flip the client gate, the handshake fails and `isActive` stays false. Future devs adding new subscription work see the layered defense and don't weaken it. |
</threat_model>

<verification>
```bash
# docs/auth/architecture.md:
grep -c "Subscription Lifecycle" docs/auth/architecture.md           # ≥ 1
grep -c "view_public_hsr_accounts" docs/auth/architecture.md         # ≥ 3
grep -c "2026-04-16" docs/auth/architecture.md                       # ≥ 2

# docs/views/architecture.md:
grep -c "#### view_user_directory" docs/views/architecture.md        # 0
grep -c "#### view_public_hsr_accounts" docs/views/architecture.md   # 1
grep -c "#### view_public_accounts" docs/views/architecture.md       # 0
grep -c "Phase 15.5 execution" docs/views/architecture.md            # 1

# docs/FRONTEND-HANDOFF.md:
grep -c "view_public_accounts" docs/FRONTEND-HANDOFF.md              # 0
grep -c "view_public_hsr_accounts" docs/FRONTEND-HANDOFF.md          # ≥ 2

# docs/roster/contract.md (identifier-only change):
grep -c "view_public_hsr_accounts" docs/roster/contract.md           # 1
grep -c "view_public_accounts\b" docs/roster/contract.md             # 1 (historical Phase 10.4 row)
grep -n "view_public_accounts" docs/roster/contract.md | grep "Phase 10.4"   # 1 match

# Contract.md safety — NO phase 15.5 edits in contract.md files:
grep -c "Phase 15.5" docs/auth/contract.md                           # 0
grep -c "Phase 15.5" docs/views/contract.md                          # 0
# docs/roster/contract.md allowed to contain "view_public_hsr_accounts" but NO "Phase 15.5" string
grep -c "Phase 15.5" docs/roster/contract.md                         # 0
```
</verification>

<success_criteria>
Plan 04 ships when ALL of:
1. `docs/auth/architecture.md` has a new Subscription Lifecycle (Phase 15.5 D-05) H2 section naming both `view_lobby_browser` and `view_public_hsr_accounts` as anonymous exceptions, stating the Stage 2 gate condition verbatim, and including the Accepted Tradeoff paragraph.
2. `docs/auth/architecture.md` View Definitions table no longer lists `view_user_directory` as an active view; has a new `view_public_hsr_accounts` row.
3. `docs/auth/architecture.md` Phase History has a new Phase 15.5 execution row dated 2026-04-16.
4. `docs/views/architecture.md` no longer has a `#### view_user_directory` subsection; the `#### view_public_accounts` subsection is renamed to `#### view_public_hsr_accounts` with expanded purpose; domain-file reorg table count reads 4 (was 5); Phase History row added.
5. `docs/FRONTEND-HANDOFF.md` no longer contains the string `view_public_accounts`; contains `view_public_hsr_accounts` at both affected sites.
6. `docs/roster/contract.md` Integration Surface row uses the new name; historical Phase 10.4 row left untouched; no new Phase 15.5 entry added.
7. No edits to any `docs/**/contract.md` that add contract spec content or Phase History entries — those are deferred to `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/15.5-auth-gated-user-subscription/15.5-04-SUMMARY.md` capturing:
- Final grep-check outputs from the verification block (showing all expected counts met)
- Confirmation that no contract.md file received a Phase 15.5 execution row (that happens at verify-work)
- The relocated / newly-added content verbatim if it helps a reviewer audit at `/gsd-verify-work` time
</output>
