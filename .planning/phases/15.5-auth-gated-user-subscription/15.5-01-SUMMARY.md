---
phase: 15.5
plan: 01
subsystem: backend
tags: [spacetimedb, views, bindings, regen, cleanup]
depends_on: []
dependency_graph:
  requires: []
  provides:
    - view_public_hsr_accounts registered on maincloud
    - view_user_directory deregistered from maincloud
    - both module_bindings/ dirs regenerated without stale files
  affects:
    - spacetimedb/src/views/identityViews.ts
    - spacetimedb/src/index.ts
    - spacetimedb/src/module_bindings/
    - src/module_bindings/
tech_stack:
  added: []
  patterns:
    - single-shot publish + two-dir regen (Phase 15.2 established pattern, --module-path flag variant)
key_files:
  created:
    - spacetimedb/src/module_bindings/view_public_hsr_accounts_table.ts
    - src/module_bindings/view_public_hsr_accounts_table.ts
  modified:
    - spacetimedb/src/views/identityViews.ts
    - spacetimedb/src/index.ts
    - spacetimedb/src/module_bindings/index.ts
    - src/module_bindings/index.ts
    - spacetimedb/dist/bundle.js
  deleted:
    - spacetimedb/src/module_bindings/view_user_directory_table.ts
    - spacetimedb/src/module_bindings/view_public_accounts_table.ts
    - src/module_bindings/view_user_directory_table.ts
    - src/module_bindings/view_public_accounts_table.ts
decisions:
  - id: D-04
    summary: view_public_accounts renamed to view_public_hsr_accounts; underlying table name now explicit in view name
  - id: D-02
    summary: view_user_directory deleted cleanly — type, export, bindings, and file-header comment entry all removed
metrics:
  duration: ~8 minutes
  completed: "2026-04-16T22:01:17Z"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 5
  files_created: 2
  files_deleted: 4
requirements:
  - S-01
  - S-02
  - D-02
  - D-04
---

# Phase 15.5 Plan 01: Backend Cleanup — SUMMARY

**One-liner:** Retired `view_user_directory` and renamed `view_public_accounts` → `view_public_hsr_accounts` in a single-shot publish + two-dir regen pass; build green.

---

## Tasks Completed

| Task | Name | Commit | Key Changes |
|------|------|--------|-------------|
| 1.1 | Delete view_user_directory + rename view_public_accounts | 9e94a6a | identityViews.ts lines 30-70 deleted, view_public_hsr_accounts export created, index.ts re-export block updated |
| 1.2 | Publish to maincloud + regen both module_bindings | 41761b2 | spacetime publish succeeded; both bindings dirs regenerated; stale files deleted |
| 1.3 | Build gate — server tsc + Next build green | (no-op) | npx tsc --noEmit exits 0; npm run build exits 0; consumer sweep empty |

---

## Source File Changes

### `spacetimedb/src/views/identityViews.ts`

**Lines deleted (formerly lines 30-70):**
- Section header comment: `// 2. Public User Directory (anonymous view) — safe subset for all clients.`
- `const UserDirectoryRow = t.object('UserDirectoryRow', { ... })` (type definition)
- `export const view_user_directory = spacetimedb.view(...)` (entire view export + body + Phase 15.2 D-06/D-09 comments)

**Lines updated:**
- File-header comment block renumbered from 5 entries to 4; line 6 now `// 3. view_public_hsr_accounts  — anonymous, HsrAccount rows where isRosterPublic=true (D-22; renamed Phase 15.5 D-04)`
- Section header `// 4. view_public_accounts —` → `// 3. view_public_hsr_accounts —`
- `const PublicAccountRow` → `const PublicHsrAccountRow`; `t.object('PublicAccountRow', ...)` → `t.object('PublicHsrAccountRow', ...)`; `t.array(PublicAccountRow)` → `t.array(PublicHsrAccountRow)`
- `anonymousView({ name: 'view_public_accounts', ... })` → `anonymousView({ name: 'view_public_hsr_accounts', ... })`
- `export const view_public_accounts = spacetimedb.anonymousView(` → `export const view_public_hsr_accounts = spacetimedb.anonymousView(`
- Added provenance comment: `// Phase 15.5 D-04: renamed from view_public_accounts — underlying table is HsrAccount; name now names the source.`

### `spacetimedb/src/index.ts`

Re-export block at lines 20-26 changed from 5 names to 4:
- Removed: `view_user_directory,`
- Renamed: `view_public_accounts,` → `view_public_hsr_accounts,`

---

## Publish Output

```
Uploading to maincloud => https://maincloud.spacetimedb.com
Database Migration Plan:
  Removed anonymous view: view_public_accounts
  Removed view: view_user_directory
  Created anonymous view: view_public_hsr_accounts
Updated database with name: hsrpvp-spacetimedb-nextjs-test1
Dashboard: https://spacetimedb.com/hsrpvp-spacetimedb-nextjs-test1
```

---

## Regen Verification

### Server bindings (`spacetimedb/src/module_bindings/`)

```
ls ... | grep view_public_hsr_accounts_table  → view_public_hsr_accounts_table.ts  (PRESENT)
ls ... | grep view_user_directory              → (empty)                             (ABSENT)
ls ... | grep view_public_accounts_table       → (empty)                             (ABSENT)
grep -c "view_public_hsr_accounts" index.ts   → 3                                  (REGISTERED)
grep -c "view_user_directory" index.ts        → 0                                  (ABSENT)
```

### Client bindings (`src/module_bindings/`)

```
ls ... | grep view_public_hsr_accounts_table  → view_public_hsr_accounts_table.ts  (PRESENT)
ls ... | grep view_user_directory              → (empty)                             (ABSENT)
ls ... | grep view_public_accounts_table       → (empty)                             (ABSENT)
grep -c "view_public_hsr_accounts" index.ts   → 3                                  (REGISTERED)
grep -c "view_user_directory" index.ts        → 0                                  (ABSENT)
```

---

## Consumer Sweep

`grep -rn "view_public_accounts\|view_user_directory" app components lib src --include='*.ts' --include='*.tsx' --exclude-dir=module_bindings`

Result: **no matches** — zero stale references in app code (confirms CONTEXT.md's claim of zero frontend subscribers).

---

## Build Gate

- `npx tsc --noEmit -p spacetimedb/tsconfig.json` → exit 0 (zero `error TS` lines)
- `npm run build` → exit 0, "Compiled successfully in 9.0s"

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `spacetime generate --project-path` flag not recognized**
- **Found during:** Task 1.2
- **Issue:** The `--project-path` flag documented in Phase 15.2 PLAN precedent is no longer accepted by the installed spacetime CLI version. Error: "unexpected argument '--project-path' found; similar argument: --uproject-dir"
- **Fix:** Used `--module-path` instead (the equivalent flag per `spacetime generate --help`)
- **Files modified:** None (CLI flag change only)
- **Commit:** 41761b2

**2. [Rule 3 - Blocking] `spacetime generate` prompts for deletion confirmation interactively**
- **Found during:** Task 1.2
- **Issue:** First `spacetime generate` run without `-y` prompted "Are you sure you want to delete these files?" for the stale `view_public_accounts_table.ts` and `view_user_directory_table.ts` files. Without confirmation, files were not deleted.
- **Fix:** Re-ran with `-y` flag to auto-confirm deletion.
- **Files modified:** Stale files deleted from both bindings dirs
- **Commit:** 41761b2

---

## Known Stubs

None — this plan is pure backend cleanup (view retirement + rename + regen). No UI components, no data stubs.

---

## Threat Flags

None introduced. This plan reduces egress surface (one fewer view registered: `view_user_directory` retired). The rename `view_public_accounts` → `view_public_hsr_accounts` is a semantic no-op on the access-control layer (same `anonymousView` body, same projection, same `public: true` flag).

---

## Self-Check: PASSED

| Item | Status |
|------|--------|
| `spacetimedb/src/views/identityViews.ts` exists | FOUND |
| `spacetimedb/src/index.ts` exists | FOUND |
| `spacetimedb/src/module_bindings/view_public_hsr_accounts_table.ts` exists | FOUND |
| `src/module_bindings/view_public_hsr_accounts_table.ts` exists | FOUND |
| `spacetimedb/src/module_bindings/view_user_directory_table.ts` absent | CONFIRMED |
| `src/module_bindings/view_user_directory_table.ts` absent | CONFIRMED |
| Commit 9e94a6a exists | FOUND |
| Commit 41761b2 exists | FOUND |
