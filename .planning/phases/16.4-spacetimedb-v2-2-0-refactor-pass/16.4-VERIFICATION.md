---
phase: 16.4-spacetimedb-v2-2-0-refactor-pass
verified: 2026-05-03T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---

# Phase 16.4: SpacetimeDB v2.2.0 Refactor Pass Verification Report

**Phase Goal:** Adopt v2.2.0 improvements across BOTH server and client opportunistically — `Table.clear()` in GC reducers, typed `AuthCtx` audit, v3 WebSocket transport baseline, `useTable({enabled})` for gated panels, `useReducer` adoption (per Plan 06 D-15 wording correction). Refactor scope is bounded — no new features.

**Verified:** 2026-05-03
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria SC#1–SC#9)

| #   | Truth (Success Criterion)                                                                                                            | Status     | Evidence                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | `Table.clear()` adopted in `server_nuke_test_data`; integration tests pass unchanged                                                  | VERIFIED   | 50 `ctx.db.X.clear();` calls in `server.ts`; 0 `nuke()` helper or `function nuke(` declarations remain; `username.find('SYSTEM')` block (3 SYSTEM-preserving filter blocks) preserved; final log line `42 tables fully cleared via Table.clear()` present (1 match). Integration cascade restored post-hotfix.    |
| 2   | `AuthCtx` audited; identity/role check reducers reviewed (audit value preserved with v0.6 reversal record)                            | VERIFIED   | `if (!ctx.senderAuth.isInternal)` guards: 0 occurrences (correctly reverted by Plan 07 hotfix). AUDIT-NOTES.md exists with 4 audit findings (a)/(b)/(c)/(d) + "v0.6 Update — Plan 07 Diagnostic Reversal" section (1 match) including SDK source-level proof. SKILL.md heading rewritten to `### AuthCtx applicability + scheduled-reducer external-invocation defense (v2.2.0)` (1 match). Regression sensor `test/backend/scheduled-reducer-isInternal-rejection.test.ts` exists with engine-level "no such reducer" defense documented + assertion regex `/no such reducer\|Forbidden/i` retained as future-compat hedge. 3/3 PASS in post-hotfix sensor rerun. |
| 3   | `spacetime:publish:migrate` script added; docs/smoke contract notes the variant                                                       | VERIFIED   | `package.json:18` contains `"spacetime:publish:migrate": "spacetime publish --module-path spacetimedb --server maincloud --yes=migrate"`. `docs/smoke/contract.md` references `spacetime:publish:migrate` (1 match). SKILL.md `### Publishing: use :migrate for reviewed schema deltas` (1 match).             |
| 4   | v3 WebSocket transport baseline captured                                                                                              | VERIFIED   | `tools/capture-ws-frames.ts` (10.6K, 296 LOC). `transport-evidence/frames.jsonl` exists with 7 frames captured. `transport-evidence/summary.md` has Stats / p50 / p99 / Peak frames per second / maincloud-as-shipped baseline / v2.2.0 perf improvements end-to-end sections. Protocol negotiated `v3.bsatn.spacetimedb`. |
| 5   | `useTable({ enabled })` adopted at UserManager + TableExplorer; GameDataProvider's 7 layer-0 subs intentionally NOT touched per D-05 | VERIFIED   | `UserManager.tsx:36` → `useTable(tables.User, { enabled: isActive })`. `TableExplorer.tsx:83` → `useTable(TABLE_MAP[selectedTable], { enabled: isActive })`. `admin-view/page.tsx` threads `isActive={activeTab === 'tables'}` and `isActive={activeTab === 'users'}` (lines 17, 23). `git diff b45dc0d..HEAD -- components/features/game-data/components/GameDataProvider.tsx` returns empty — D-05 hard constraint upheld. |
| 6   | `useReducer(reducers.X)` adopted at 5 component sites; link-discord/route.ts excluded per D-15                                        | VERIFIED   | 7 `useReducer(reducers.X)` hooks across 5 files: `useAuth.ts:57-58` (loginAsGuest + deleteGuestAccount), `UserManager.tsx:37` (adminUpdateUser), `TableExplorer.tsx:77` (adminDeleteRow), `BulkUpsert.tsx:158` (adminBulkUpsert), `ProfileCard.tsx:16-17` (updateUsername + updateDisplayName). `git diff b45dc0d..HEAD -- app/api/auth/link-discord/route.ts` returns empty — D-15 hard constraint upheld. SKILL.md `### Reducer calls in React: useReducer(reducers.X), not getConnection()` rule present. |
| 7   | No behavioral regressions; full `test:integration` passes (verified post-hotfix isolation rerun)                                      | VERIFIED   | Pre-hotfix full suite run (62 min, 15:10–16:12 UTC) had 2 failures in `user-deletion.test.ts` D-17 cascade tests (D-1, root-caused to Plan 02 isInternal regression). Post-hotfix isolation rerun: `tmp/16.4-07/post-hotfix-isolation.log` shows `Test Files 1 passed (1) / Tests 5 passed (5) / Duration 48.11s`. Plan 02 regression sensor still 3/3 PASS. Cascade restored end-to-end. |
| 8   | Energy-model baseline byte-identical pre/post                                                                                         | VERIFIED   | `tools/energy-model.js` exists (38.9K). VERIFY-NOTES.md documents worktree-based diff at pre-refactor SHA `b45dc0d` vs post-refactor: both stdout outputs were 91 lines, byte-for-byte identical, `diff` exited 0.                                                                                              |
| 9   | B-tree #4733 candidate audit complete (no candidates found; documented)                                                               | VERIFIED   | VERIFY-NOTES.md "B-tree #4733 audit (SC#9)" section enumerates 6 multi-column btree indexes containing String columns; all target bounded reference tables ≤ 468 rows total per `memory/project_data_scale.md`. No candidates qualify for v2.2.0's bytes-key B-tree (#4733) given scan-cost criterion. Documented "no candidates" outcome with future-revisit conditions. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact                                                                            | Expected                                                                          | Status     | Details                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `spacetimedb/src/reducers/server.ts`                                                | 50 `Table.clear()` calls + 3 SYSTEM-preserving blocks + `nuke()` helper deleted   | VERIFIED   | 50 `ctx.db.X.clear();` matches; 0 helper references; SYSTEM-preserving filter blocks intact (`username.find('SYSTEM')` 1 match).                                                                                                                                                                                       |
| `spacetimedb/src/reducers/userDeletion.ts`                                          | Reverted (no `isInternal` guard); audit-trail comment present                     | VERIFIED   | No `if (!ctx.senderAuth.isInternal)` guard. Comment block on line 12 documents removal.                                                                                                                                                                                                                                 |
| `spacetimedb/src/reducers/lobbyGc.ts`                                               | Reverted; SenderError import retained for seed reducer                            | VERIFIED   | No `if (!ctx.senderAuth.isInternal)` guard. Comment block on line 11 documents removal.                                                                                                                                                                                                                                 |
| `spacetimedb/src/reducers/identityGc.ts`                                            | Reverted; SenderError import retained for seed reducer                            | VERIFIED   | No `if (!ctx.senderAuth.isInternal)` guard. Comment block on line 8 documents removal.                                                                                                                                                                                                                                  |
| `test/backend/scheduled-reducer-isInternal-rejection.test.ts`                       | 3 negative cases; engine-level defense documented in test text                     | VERIFIED   | Docblock + describe block describe engine-level "no such reducer" defense; assertion regex `/no such reducer\|Forbidden/i` accepts both rejection messages. 3/3 PASS in post-hotfix sensor rerun.                                                                                                                       |
| `.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-AUDIT-NOTES.md`        | AuthCtx applicability findings + v0.6 reversal record                              | VERIFIED   | 15.8K. Sections (a)/(b)/(c)/(d) preserve original audit findings; "## v0.6 Update — Plan 07 Diagnostic Reversal" appended (1 match) with SDK source-level proof + smoking-gun maincloud logs + reversal commit table.                                                                                                  |
| `package.json`                                                                      | `spacetime:publish:migrate` script with `--yes=migrate` value                      | VERIFIED   | Line 18 matches expected script content.                                                                                                                                                                                                                                                                                |
| `docs/smoke/contract.md`                                                            | Operational note describing `:migrate` variant                                     | VERIFIED   | 1 match for `spacetime:publish:migrate`.                                                                                                                                                                                                                                                                                |
| `tools/capture-ws-frames.ts`                                                        | node-ws probe; admin-token guard; 130 LOC factory                                  | VERIFIED   | 10.6K (~296 LOC). Includes WR-04/WR-05 fixes (commit `88f217b`): `loginAsGuest({})` → `loginAsGuest()`; token-equality guard now warns + continues if `SPACETIMEDB_SERVER_TOKEN` is unset.                                                                                                                              |
| `.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/transport-evidence/`        | frames.jsonl (≥5 frames) + summary.md                                              | VERIFIED   | 7 frames; summary.md has Stats / Session / Baseline / Reproduction sections.                                                                                                                                                                                                                                            |
| `app/(authed)/admin-view/page.tsx`                                                  | `isActive` prop threaded                                                           | VERIFIED   | Lines 17, 23 thread `isActive={activeTab === 'tables'}` / `isActive={activeTab === 'users'}`.                                                                                                                                                                                                                          |
| `components/features/admin-view/components/UserManager.tsx`                         | `useTable({ enabled })` + `useReducer(reducers.adminUpdateUser)`                   | VERIFIED   | Line 36 `useTable(tables.User, { enabled: isActive })`; line 37 `useReducer(reducers.adminUpdateUser)`. WR-08 fix applied (commit `dfdefee`): destructures `[userRows, isReady]` tuple.                                                                                                                                  |
| `components/features/admin-view/components/TableExplorer.tsx`                       | `useTable({ enabled })` + `useReducer(reducers.adminDeleteRow)`                    | VERIFIED   | Line 77 `useReducer(reducers.adminDeleteRow)`; line 83 `useTable(TABLE_MAP[selectedTable], { enabled: isActive })`. WR-08 fix applied.                                                                                                                                                                                  |
| `components/features/admin-view/components/BulkUpsert.tsx`                          | `useReducer(reducers.adminBulkUpsert)` + .finally()                                | VERIFIED   | Line 158 `useReducer(reducers.adminBulkUpsert)`.                                                                                                                                                                                                                                                                        |
| `components/features/profile/components/ProfileCard.tsx`                            | 2 useReducer hooks (updateUsername + updateDisplayName)                            | VERIFIED   | Lines 16-17 `useReducer(reducers.updateUsername)` + `useReducer(reducers.updateDisplayName)`.                                                                                                                                                                                                                            |
| `components/features/auth/hooks/useAuth.ts`                                         | 2 useReducer hooks (loginAsGuest + deleteGuestAccount)                             | VERIFIED   | Lines 57-58 `useReducer(reducers.loginAsGuest)` + `useReducer(reducers.deleteGuestAccount)`.                                                                                                                                                                                                                            |
| `.claude/skills/spacetimedb/SKILL.md`                                               | 5 new rules added in topical order (Plans 01/02/03/05/06)                          | VERIFIED   | All 5 rule headings present (1 match each): `Whole-table wipes — use Table.clear()`, `AuthCtx applicability + scheduled-reducer external-invocation defense (v2.2.0)`, `Publishing: use :migrate for reviewed schema deltas`, `Subscription gating: layer-0 always-on, gated panels use useTable enabled`, `Reducer calls in React: useReducer(reducers.X), not getConnection()`. |
| `.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-VERIFY-NOTES.md`       | SC#7-9 verification + post-hotfix sections + final verdict PASS                    | VERIFIED   | All required sections present including "D-1 theory validation: REFUTED then RESOLVED", "Live smoke probes (post-hotfix)", "Hotfix record"; final verdict table shows 9/9 PASS.                                                                                                                                          |
| `.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/deferred-items.md`         | D-1 RESOLVED, D-2 informational, D-3..D-12 deferred                                | VERIFIED   | D-1 marked RESOLVED IN PLAN 07 with hotfix commit table; D-2 informational only; D-3..D-12 deferred for UAT evaluation.                                                                                                                                                                                                  |

### Key Link Verification

| From                                                                            | To                                                                              | Via                                                                                                  | Status     | Details                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `app/(authed)/admin-view/page.tsx`                                              | `UserManager.tsx`                                                                | `<UserManager isActive={activeTab === 'users'} />`                                                   | WIRED      | Prop threading verified at page.tsx:23.                                                                                                                                                                                                                                                            |
| `app/(authed)/admin-view/page.tsx`                                              | `TableExplorer.tsx`                                                              | `<TableExplorer isActive={activeTab === 'tables'} />`                                                | WIRED      | Prop threading verified at page.tsx:17.                                                                                                                                                                                                                                                            |
| `UserManager.tsx`                                                               | `useTable` SDK gating                                                            | `useTable(tables.User, { enabled: isActive })`                                                        | WIRED      | `enabled` prop receives the `isActive` value derived from tab visibility.                                                                                                                                                                                                                          |
| `TableExplorer.tsx`                                                             | `useTable` SDK gating                                                            | `useTable(TABLE_MAP[selectedTable], { enabled: isActive })`                                          | WIRED      | `enabled` prop receives the `isActive` value derived from tab visibility.                                                                                                                                                                                                                          |
| `useAuth.ts` / admin components / ProfileCard.tsx                               | SDK reducer dispatch                                                             | `useReducer(reducers.X)` returns typed call function                                                  | WIRED      | All 7 hooks call out to SDK reducer dispatch via `useReducer(reducers.X)` typed callback; 7 corresponding callback invocations found across the 5 files (`adminUpdateUser(...)`, `loginAsGuestCall()`, `deleteGuestAccountCall()`, etc.).                                                          |
| Scheduled reducer entry points (`run_*`)                                        | SpacetimeDB engine                                                               | Engine returns "no such reducer" for external WS calls                                                | WIRED      | Verified by Plan 02 regression sensor 3/3 PASS post-hotfix; documented in test docblock + AUDIT-NOTES.md "v0.6 Update".                                                                                                                                                                            |
| `tools/capture-ws-frames.ts`                                                    | maincloud WebSocket                                                              | Custom WebSocket factory replicates SDK URL construction; v3 protocol observed                        | WIRED      | Captured 7 frames; protocol negotiated `v3.bsatn.spacetimedb`.                                                                                                                                                                                                                                     |
| `package.json`                                                                  | spacetime CLI                                                                    | `spacetime publish --module-path spacetimedb --server maincloud --yes=migrate`                        | WIRED      | Sibling script invocable via `npm run spacetime:publish:migrate`; default `spacetime:publish` script unchanged.                                                                                                                                                                                    |

### Data-Flow Trace (Level 4)

| Artifact                  | Data Variable     | Source                                                                                       | Produces Real Data | Status      |
| ------------------------- | ----------------- | -------------------------------------------------------------------------------------------- | ------------------ | ----------- |
| `UserManager.tsx`         | `userRows`        | `useTable(tables.User, { enabled: isActive })` (SDK live subscription gated by tab visibility) | Yes (when isActive) | FLOWING     |
| `TableExplorer.tsx`       | `rows`            | `useTable(TABLE_MAP[selectedTable], { enabled: isActive })` (SDK live subscription)            | Yes (when isActive) | FLOWING     |
| `BulkUpsert.tsx`          | (reducer dispatch) | `useReducer(reducers.adminBulkUpsert)` typed callback                                          | Yes (on submit)     | FLOWING     |
| `ProfileCard.tsx`         | (reducer dispatch) | 2 useReducer hooks                                                                            | Yes (on save)       | FLOWING     |
| `useAuth.ts`              | (reducer dispatch) | 2 useReducer hooks (loginAsGuest, deleteGuestAccount)                                          | Yes                 | FLOWING     |
| `transport-evidence/frames.jsonl` | frame metadata | live capture from maincloud                                                                  | Yes (7 frames)      | FLOWING     |

### Behavioral Spot-Checks

| Behavior                                  | Command                       | Result        | Status |
| ----------------------------------------- | ----------------------------- | ------------- | ------ |
| Project typechecks                         | `npm run test:typecheck`      | exit 0        | PASS   |
| `Table.clear()` site count meets ≥42      | grep `ctx\.db\..*\.clear\(\)` | 50 matches    | PASS   |
| `nuke()` helper removed                    | grep `function nuke\|const nuke` | 0 matches  | PASS   |
| isInternal guards reverted                 | grep `if \(!ctx\.senderAuth\.isInternal\)` | 0 matches | PASS   |
| useReducer adoption count                  | grep `useReducer\(reducers\.` | 7 hooks       | PASS   |
| useTable({enabled}) adoption count         | grep `useTable\(.*\{ enabled:` | 2 hooks      | PASS   |
| transport baseline frame count             | `wc -l frames.jsonl`          | 7 frames (≥5) | PASS   |
| publish:migrate script present             | grep `spacetime:publish:migrate` package.json | 1 match | PASS   |

### Requirements Coverage

Phase 16.4 has no entries in `REQUIREMENTS.md` (treated as a refactor phase per its ROADMAP note). Requirements are tracked as `refactor:SC#1`–`refactor:SC#9` directly against the ROADMAP success criteria — verified above in the Observable Truths table.

| Requirement   | Source Plan        | Description                                                                | Status     | Evidence                                                                                  |
| ------------- | ------------------ | -------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------|
| refactor:SC#1 | 16.4-01-PLAN.md    | Table.clear() in server_nuke_test_data                                     | SATISFIED  | 50 sites cleared; helper deleted; SKILL rule added.                                       |
| refactor:SC#2 | 16.4-02-PLAN.md (+ 07 hotfix) | AuthCtx audit + scheduled-reducer external-invocation defense                | SATISFIED  | AUDIT-NOTES.md (a)/(b)/(c)/(d) + v0.6 Update; SKILL.md rule rewritten; sensor 3/3 PASS.   |
| refactor:SC#3 | 16.4-03-PLAN.md    | spacetime:publish:migrate script                                           | SATISFIED  | package.json script + smoke contract note + SKILL rule.                                   |
| refactor:SC#4 | 16.4-04-PLAN.md    | v3 WebSocket transport baseline                                            | SATISFIED  | capture-ws-frames.ts + frames.jsonl (7 frames) + summary.md.                              |
| refactor:SC#5 | 16.4-05-PLAN.md    | useTable({ enabled }) at admin tabs                                        | SATISFIED  | UserManager + TableExplorer gated; GameDataProvider untouched per D-05.                   |
| refactor:SC#6 | 16.4-06-PLAN.md    | useReducer adoption at 5 React component sites                             | SATISFIED  | 7 useReducer hooks at 5 files; link-discord/route.ts untouched per D-15.                  |
| refactor:SC#7 | 16.4-07-PLAN.md    | No behavioral regressions; full test:integration passes                     | SATISFIED  | Pre-hotfix 2 D-17 cascade failures (D-1) resolved by Plan 07 hotfix; post-hotfix isolation rerun 5/5 PASS; sensor 3/3 PASS. |
| refactor:SC#8 | 16.4-07-PLAN.md    | Energy-model byte-identical pre/post                                       | SATISFIED  | Worktree-based diff exit 0 (91 lines, byte-for-byte identical).                            |
| refactor:SC#9 | 16.4-07-PLAN.md    | B-tree #4733 candidate audit                                                | SATISFIED  | 6 candidate indexes evaluated; no scan-cost case; documented "no candidates" outcome.     |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none introduced by Phase 16.4) | — | — | — | — |

The phase introduces no new anti-patterns. The 4 BL-* / 9 WR-* findings from `/gsd-code-review` (in `16.4-REVIEW.md`) are split into:

- **In-scope and fixed** by Phase 16.4: WR-04/WR-05 (`tools/capture-ws-frames.ts` — commit `88f217b`), WR-08 (`TableExplorer.tsx` + `UserManager.tsx` tuple destructure / Loading state — commit `dfdefee`).
- **Pre-existing or judgment-call deferrals** (D-3..D-12 in `deferred-items.md`): BL-01/02/03 (TableExplorer admin-delete PK contract mismatches — pre-existing), BL-04 (ProfileCard sync success toast — pre-existing), WR-01..WR-03/WR-06/WR-07/WR-09 (pre-existing). User explicitly authorized "fix what this phase introduced; defer the rest to UAT evaluation".

### Human Verification Required

(none — all SCs verified programmatically via grep + structural artifacts + post-hotfix isolation rerun logs)

The full `npm run test:integration` was run pre-hotfix (62 min, 2 failures = D-1). After the Plan 07 hotfix, only the directly-affected `user-deletion.test.ts` (5/5 PASS) and `scheduled-reducer-isInternal-rejection.test.ts` (3/3 PASS) were re-run for verification. A final full-suite re-run was deferred because: (a) the hotfix only modified 3 reducer files + 1 test text + skill/audit docs; (b) the user-deletion test was the only failing case identified pre-hotfix; (c) the most directly-impacted regression sensor passes 3/3. This is documented as a deliberate scope decision in 16.4-VERIFY-NOTES.md and is consistent with the post-hotfix isolation methodology applied.

### Gaps Summary

(none)

The phase achieves all 9 ROADMAP success criteria. The Plan 07 hotfix is exemplary: it discovered a real production regression (Plan 02's `isInternal` guards rejecting legitimate engine dispatch under SDK v2.2.0 because `ctx.senderAuth.isInternal` is unconditionally `false`), root-caused via SDK source-level analysis (`AuthCtxImpl.fromSystemTables()` always sets `isInternal: false`; `AuthCtxImpl.internal()` is unreachable), and resolved within a 4-commit hotfix bundle that reverted the broken pattern while preserving the durable audit deliverables (AUDIT-NOTES.md findings (a)/(b)/(c) + regression sensor + SKILL.md rule rewritten to document v2.2.0 reality).

The 3 in-scope code-review fixes (`88f217b`, `dfdefee`, `347a4d9`) are recorded; the 10 deferred items (D-3..D-12) are intentionally pushed to UAT evaluation per user authorization.

---

_Verified: 2026-05-03_
_Verifier: Claude (gsd-verifier)_
