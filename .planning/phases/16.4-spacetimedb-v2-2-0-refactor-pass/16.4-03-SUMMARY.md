---
phase: 16.4-spacetimedb-v2-2-0-refactor-pass
plan: 03
subsystem: infra
tags: [spacetimedb, publish, npm-scripts, cli, v2.2.0]

# Dependency graph
requires:
  - phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
    provides: "v2.2.0 SDK shipped to maincloud — exposes --yes=migrate granular flag (PR #4885)"
provides:
  - "spacetime:publish:migrate npm script — auto-confirms ONLY the destructive-migration prompt"
  - "Operational note in docs/smoke/contract.md disambiguating :migrate vs default publish"
  - "SKILL.md publishing rule capturing when to use :migrate (already-reviewed schema delta)"
affects: [16.4-04, 16.4-05, 16.4-06, 16.4-07, 17+]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sibling npm script pattern for opt-in granular CLI flags (default stays interactive; sibling skips ONE specific prompt)"

key-files:
  created: []
  modified:
    - "package.json"
    - "docs/smoke/contract.md"
    - ".claude/skills/spacetimedb/SKILL.md"

key-decisions:
  - "Separate :migrate sibling script (not a flag added to default publish) — destructive-migration confirm preserved as safety feature for the default workflow per D-06"
  - "--yes=migrate value attached with '=' (not space-separated) per PR #4885 CLI surface"
  - "Other --yes values (break-clients, delete-data, remote, skip-login) intentionally NOT added — only :migrate opted in"

patterns-established:
  - "Granular CLI confirm-skip via sibling npm scripts: default = full safety; named sibling = skip ONE named prompt"
  - "Operational doc updates in docs/{feature}/contract.md non-Phase-History sections are scoped to the same plan that ships the script (avoids drift)"

requirements-completed:
  - "refactor:SC#3"

# Metrics
duration: 2min
completed: 2026-05-03
---

# Phase 16.4 Plan 03: spacetime:publish:migrate sibling script Summary

**Adds `npm run spacetime:publish:migrate` (appends `--yes=migrate`) — surfaces v2.2.0 PR #4885's granular migration auto-confirm while keeping the default `spacetime:publish` interactive.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-03T13:42:21Z
- **Completed:** 2026-05-03T13:44:44Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- New `spacetime:publish:migrate` npm script with correct `--yes=migrate` syntax (=-attached value) — auto-confirms ONLY the destructive-migration prompt; all other destructive prompts (break-clients, delete-data, remote, skip-login) remain interactive.
- Default `npm run spacetime:publish` UNCHANGED — destructive-migration confirm dialog still fires per D-06.
- Operational note in `docs/smoke/contract.md` line 13 area disambiguates the variant from the full-bootstrap `--clear-database` workflow without replacing the existing text.
- SKILL.md publishing rule appended near the CLI commands section, including informational notes per D-14 (brotli compression not adopted, PR #4593 empty-table drop note).

## Task Commits

Each task was committed atomically:

1. **Task 1: Add spacetime:publish:migrate script to package.json** - `9ffae91` (feat)
2. **Task 2: Note the :migrate variant in docs/smoke/contract.md** - `2a79b0a` (docs)
3. **Task 3: Append the publishing :migrate rule to SKILL.md** - `cc895fa` (docs)

**Plan metadata commit:** [pending — added in final commit step alongside SUMMARY.md, STATE.md, ROADMAP.md updates]

## Files Created/Modified
- `package.json` — Added `"spacetime:publish:migrate": "spacetime publish --module-path spacetimedb --server maincloud --yes=migrate"` directly below the existing `spacetime:publish` entry. Single-line insertion (+1/-0).
- `docs/smoke/contract.md` — Added one operational paragraph under the `### scripts/post-publish.ts (Full Bootstrap)` heading explaining the `:migrate` variant. Two-line insertion (+2/-0).
- `.claude/skills/spacetimedb/SKILL.md` — Appended `### Publishing: use :migrate for reviewed schema deltas` rule between the CLI `Common commands` block and the existing `### spacetime sql column name gotcha` rule, including bash example, syntax note, and v2.2.0 informational notes (brotli, PR #4593). Seventeen-line insertion (+17/-0).

## Decisions Made
- **Insertion site for SKILL.md rule (Claude's Discretion per Plan PATTERNS.md):** Placed right after the `### Common commands` `spacetime publish` reference (line 506 area) and before `### spacetime sql column name gotcha`. This keeps publishing-related CLI guidance topically grouped, matching the PATTERNS.md guardrail "Place new rules in topical order: server-side rules near existing server sections."
- All other implementation choices followed the plan exactly — exact text strings, exact command shapes, exact insertion points (line 13 area for contract.md, after `spacetime:publish` line for package.json).

## Deviations from Plan

None — plan executed exactly as written. All three tasks completed with the verbatim text strings the plan specified; only the SKILL.md insertion location was a Plan-flagged Claude's Discretion call (chose CLI commands section per PATTERNS.md topical-order guardrail).

## Issues Encountered

None. All acceptance-criteria greps and node-eval checks passed first time:
- `node -e "...spacetime:publish:migrate..."` exits 0 with the expected output string.
- `grep -c "spacetime:publish:migrate" package.json` returns 1.
- `grep -c "--yes=migrate" package.json` returns 1.
- `grep -c "--yes break-clients\|--yes delete-data" package.json` returns 0 (only :migrate variant uses --yes).
- `grep -c "spacetime:publish:migrate" docs/smoke/contract.md` returns 1; `spacetime publish --clear-database` still present (existing text preserved).
- SKILL.md: `### Publishing: use :migrate for reviewed schema deltas` count 1; `spacetime:publish:migrate` count 2 (canonical bash example + reference); `PR #4885`, `Phase 16.4 Plan 03`, `withCompression('brotli')`, `PR #4593` all match.
- `npm run` lists `spacetime:publish:migrate` among available scripts.
- Pre-existing Plan 01 (`### Whole-table wipes — use Table.clear()`) and Plan 02 (`### AuthCtx applicability + isInternal hardening`) sections in SKILL.md remain at count 1 — not disturbed.

## User Setup Required

None — no external service configuration required. The new script is fully self-contained within `package.json`; running `npm run spacetime:publish:migrate` will invoke the spacetime CLI with the same auth credentials as `npm run spacetime:publish` (no new env vars, no new tokens).

## Next Phase Readiness

- **For Plan 04 (transport verification):** No dependency on this plan; Plan 04 can proceed in parallel with this on-disk artifact.
- **For Plan 05 / Plan 06 (client refactors):** Independent.
- **For Plan 07 (cross-cutting verify + maincloud republish):** When Plan 07's republish step runs, the user (or executor with explicit go-ahead) can invoke `npm run spacetime:publish:migrate` IF the schema delta has been reviewed in a prior diff. Plan 07's PLAN.md should reference the new script in its republish notes — but this is a downstream addition, not a Plan 03 responsibility.

## Self-Check: PASSED

Verification commands run after writing this SUMMARY.md:
- `package.json` exists with `spacetime:publish:migrate` script — FOUND
- `docs/smoke/contract.md` exists with `spacetime:publish:migrate` reference — FOUND
- `.claude/skills/spacetimedb/SKILL.md` exists with `### Publishing: use :migrate for reviewed schema deltas` heading — FOUND
- Commit `9ffae91` exists in git log — FOUND
- Commit `2a79b0a` exists in git log — FOUND
- Commit `cc895fa` exists in git log — FOUND

---
*Phase: 16.4-spacetimedb-v2-2-0-refactor-pass*
*Completed: 2026-05-03*
