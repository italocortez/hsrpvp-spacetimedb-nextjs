---
phase: 16.4-spacetimedb-v2-2-0-refactor-pass
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/smoke/contract.md
  - .claude/skills/spacetimedb/SKILL.md
autonomous: true
requirements:
  - refactor:SC#3
threat_model_summary: "none — refactor preserves existing security envelope (default spacetime:publish stays interactive; only :migrate variant skips the migration confirm prompt; other destructive prompts remain interactive)"

must_haves:
  truths:
    - "Running `npm run spacetime:publish:migrate` invokes spacetime publish against maincloud with --yes=migrate appended"
    - "Default `npm run spacetime:publish` is UNCHANGED — destructive-migration confirm dialog still fires"
    - "docs/smoke/contract.md mentions the :migrate variant near the existing publish reference (line 13 area)"
    - "SKILL.md gains a publishing rule that captures when to use :migrate (already-reviewed schema delta)"
  artifacts:
    - path: package.json
      provides: "spacetime:publish:migrate script"
      contains: "spacetime:publish:migrate"
    - path: docs/smoke/contract.md
      provides: ":migrate variant note near line 13"
      contains: "spacetime:publish:migrate"
    - path: .claude/skills/spacetimedb/SKILL.md
      provides: "Publishing :migrate convention rule"
      contains: "### Publishing: use :migrate for reviewed schema deltas"
  key_links:
    - from: package.json
      to: spacetimedb (module-path argument)
      via: "spacetime publish CLI"
      pattern: "--module-path spacetimedb"
---

<objective>
Add a sibling `spacetime:publish:migrate` script to `package.json` that invokes `spacetime publish ... --yes=migrate`. Default `spacetime:publish` stays interactive. Note the variant in `docs/smoke/contract.md` near line 13 and append a SKILL.md rule.

Purpose: Closes SC#3 — surfaces v2.2.0's granular `--yes=migrate` flag (PR #4885) for re-publishes where the schema delta has already been reviewed in a prior diff. Destructive-migration confirm is preserved as a safety feature for the default workflow.

Output: New script in package.json; one-line note in docs/smoke/contract.md; SKILL.md rule.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-CONTEXT.md
@.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-RESEARCH.md
@.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-PATTERNS.md
@package.json
@docs/smoke/contract.md
@.claude/skills/spacetimedb/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spacetime:publish:migrate script to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (lines 10-25 — see existing `spacetime:publish` at line 17 and `spacetime:publish:local` at line 16)
    - .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-PATTERNS.md (lines 228-251 — Pattern Assignment for package.json with exact BEFORE/AFTER excerpts and the `--yes=migrate` `=` syntax requirement)
    - .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-CONTEXT.md (D-06 — separate-script rationale)
  </read_first>
  <action>
    Edit `package.json` and add a single new script line directly below the existing `spacetime:publish` line (currently line 17). The exact BEFORE/AFTER:

    BEFORE (lines 14-17):
    ```json
        "generate": "npm --prefix spacetimedb install && cargo run -p gen-bindings -- --out-dir src/module_bindings --module-path spacetimedb && prettier --write src/module_bindings",
        "spacetime:generate": "spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb",
        "spacetime:publish:local": "spacetime publish --module-path spacetimedb --server local",
        "spacetime:publish": "spacetime publish --module-path spacetimedb --server maincloud",
    ```

    AFTER:
    ```json
        "generate": "npm --prefix spacetimedb install && cargo run -p gen-bindings -- --out-dir src/module_bindings --module-path spacetimedb && prettier --write src/module_bindings",
        "spacetime:generate": "spacetime generate --lang typescript --out-dir src/module_bindings --module-path spacetimedb",
        "spacetime:publish:local": "spacetime publish --module-path spacetimedb --server local",
        "spacetime:publish": "spacetime publish --module-path spacetimedb --server maincloud",
        "spacetime:publish:migrate": "spacetime publish --module-path spacetimedb --server maincloud --yes=migrate",
    ```

    **Critical:**
    - The `--yes=migrate` value MUST be attached with `=` (not space-separated) per RESEARCH.md Example 5: "the value must be attached with `=`."
    - Default `spacetime:publish` MUST remain UNCHANGED (no `--yes=*` flags added) — destructive-migration confirm is the safety feature per D-06.
    - Do NOT touch `spacetime:publish:local` (stale; deferred for separate cleanup task per CONTEXT.md `<deferred>`).
    - Other `--yes` values (`break-clients`, `delete-data`, `remote`, `skip-login`) MUST NOT be added — only `migrate` is opted-in for this variant.
    - Preserve trailing comma + indentation style (4 spaces, double quotes around keys+values, comma after every script except the last).
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (!p.scripts['spacetime:publish:migrate']) process.exit(1); if (!/--yes=migrate/.test(p.scripts['spacetime:publish:migrate'])) process.exit(2); if (!/--server maincloud/.test(p.scripts['spacetime:publish:migrate'])) process.exit(3); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').scripts['spacetime:publish:migrate'])"` prints `spacetime publish --module-path spacetimedb --server maincloud --yes=migrate`
    - `node -e "console.log(require('./package.json').scripts['spacetime:publish'])"` prints `spacetime publish --module-path spacetimedb --server maincloud` (UNCHANGED)
    - `grep -c '"spacetime:publish:migrate":' package.json` returns 1
    - `grep -c "--yes=migrate" package.json` returns 1
    - `grep -c "--yes break-clients\\|--yes delete-data\\|--yes=break-clients\\|--yes=delete-data" package.json` returns 0 (only :migrate variant uses --yes)
    - `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` exits 0 (valid JSON)
    - `npm run` (no script name) lists `spacetime:publish:migrate` among available scripts
  </acceptance_criteria>
  <done>
    package.json valid; new script present with correct `--yes=migrate` syntax; default publish UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Note the :migrate variant in docs/smoke/contract.md</name>
  <files>docs/smoke/contract.md</files>
  <read_first>
    - docs/smoke/contract.md (full file — line 13 area shows existing `spacetime publish --clear-database` mention; verify prose voice is terse + operational)
    - .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-PATTERNS.md (lines 254-271 — Pattern Assignment for docs/smoke/contract.md with BEFORE excerpt + AFTER intent + guardrails)
  </read_first>
  <action>
    Add a single sentence near line 13 of `docs/smoke/contract.md` noting the `:migrate` variant. The current text at lines 11-13 is:

    ```markdown
    ### `scripts/post-publish.ts` (Full Bootstrap)

    **Purpose:** End-to-end database initialization after `spacetime publish --clear-database`
    ```

    Insert a single follow-up paragraph immediately after line 13 (after the "Purpose" line), preserving the existing Purpose text verbatim:

    ```markdown
    ### `scripts/post-publish.ts` (Full Bootstrap)

    **Purpose:** End-to-end database initialization after `spacetime publish --clear-database`

    For re-publishes that include schema deltas already reviewed in a prior diff, use `npm run spacetime:publish:migrate` (appends `--yes=migrate`, skips ONLY the destructive-migration confirm prompt). Default `npm run spacetime:publish` stays interactive.
    ```

    **Guardrails:**
    - Single sentence (one paragraph). Do NOT enumerate other `--yes=*` flags here — defer to SKILL.md.
    - Match the existing prose voice (terse, operational, second-person implicit).
    - Do NOT replace the existing `--clear-database` text — `:migrate` is a re-publish alternative, NOT a replacement for the full-bootstrap publish.
    - Do NOT add cross-references to PRs (#4885 etc.) — surface area for `docs/` is operational, not changelog.
  </action>
  <verify>
    <automated>grep -c "spacetime:publish:migrate" docs/smoke/contract.md && grep -c "spacetime publish --clear-database" docs/smoke/contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spacetime:publish:migrate" docs/smoke/contract.md` returns 1
    - `grep -c "spacetime publish --clear-database" docs/smoke/contract.md` returns at least 1 (existing text PRESERVED)
    - `grep "skips ONLY the destructive-migration confirm prompt" docs/smoke/contract.md` matches (the disambiguating phrase is present)
    - `grep -c "Default \`npm run spacetime:publish\` stays interactive." docs/smoke/contract.md` returns 1
    - File still parses as markdown (no broken heading hierarchy — verify by visual inspection that the new paragraph lives under the existing `### scripts/post-publish.ts` heading, not breaking it)
  </acceptance_criteria>
  <done>
    docs/smoke/contract.md notes the :migrate variant near line 13; existing publish text UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append the publishing :migrate rule to SKILL.md</name>
  <files>.claude/skills/spacetimedb/SKILL.md</files>
  <read_first>
    - .claude/skills/spacetimedb/SKILL.md (full file — find an appropriate insertion point near other operations / publishing content)
    - .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-PATTERNS.md (line 723 — exact rule body summary for Plan 03)
    - .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-CONTEXT.md (D-14 — informational items for this rule: brotli compression note, #4593 empty-table drop note)
  </read_first>
  <action>
    Append a new `###` rule to `.claude/skills/spacetimedb/SKILL.md` near other publishing / deployment operations. Use the EXACT text below:

    ```markdown
    ### Publishing: use :migrate for reviewed schema deltas

    `npm run spacetime:publish` stays interactive — the destructive-migration confirm dialog is a safety feature, not friction. Use `npm run spacetime:publish:migrate` (v2.2.0+, PR #4885) when you have already reviewed the schema delta in a prior diff.

    ```bash
    npm run spacetime:publish           # Interactive: confirms destructive migrations.
    npm run spacetime:publish:migrate   # Auto-confirms ONLY the migration prompt (--yes=migrate).
    ```

    The `--yes=migrate` value must attach with `=` (per `spacetime publish --help`). Other `--yes` values (`break-clients`, `delete-data`, `remote`, `skip-login`) remain interactive in this script — opt them in explicitly only when the situation warrants.

    Related v2.2.0 informational notes:
    - `.withCompression('brotli')` exists on `DbConnection.builder()` as an alternative to gzip; project has not adopted it (browser-support floor too high for current users; realistic ratio gain on BSATN binary frames is 5-15% — not worth the failure mode on older browsers).
    - v2.2.0 also allows dropping empty tables during auto-migration (PR #4593) — relevant when refactoring schemas; no project-side action needed.

    Reference: Phase 16.4 Plan 03; `package.json` script line; `docs/smoke/contract.md` operational note.
    ```

    Before inserting, grep the file for `spacetime:publish:migrate` to ensure the rule does not already exist (must return 0 hits pre-edit).
  </action>
  <verify>
    <automated>grep -c "### Publishing: use :migrate for reviewed schema deltas" .claude/skills/spacetimedb/SKILL.md && grep -c "spacetime:publish:migrate" .claude/skills/spacetimedb/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Publishing: use :migrate for reviewed schema deltas" .claude/skills/spacetimedb/SKILL.md` returns 1
    - `grep -c "spacetime:publish:migrate" .claude/skills/spacetimedb/SKILL.md` returns at least 2 (canonical bash example + reference)
    - `grep "PR #4885" .claude/skills/spacetimedb/SKILL.md` matches (v2.2.0 attribution)
    - `grep "Phase 16.4 Plan 03" .claude/skills/spacetimedb/SKILL.md` matches (cross-reference)
    - `grep "withCompression('brotli')" .claude/skills/spacetimedb/SKILL.md` matches (informational note per D-14)
    - `grep "PR #4593" .claude/skills/spacetimedb/SKILL.md` matches (empty-table drop informational note per D-14)
  </acceptance_criteria>
  <done>
    SKILL.md has the publishing :migrate rule + brotli + #4593 informational notes appended.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → spacetime CLI | Local-only operation; auth enforced by `~/.spacetimedb/credentials.json` and the spacetime CLI's interactive prompts. Default `spacetime:publish` retains all destructive prompts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | — | — | — | Refactor preserves existing security envelope. The default `spacetime:publish` script is UNCHANGED. The new `:migrate` sibling auto-confirms ONLY the migration prompt; `break-clients`, `delete-data`, `remote`, and `skip-login` prompts remain interactive in BOTH scripts. Operational risk is bounded by spacetime CLI's per-prompt safety design (PR #4885 made `--yes` granular). |
</threat_model>

<verification>
After all 3 tasks:
- `npm run` lists `spacetime:publish:migrate`
- `npm run spacetime:publish:migrate -- --help` does not error (script line is well-formed)
- All `<acceptance_criteria>` greps pass
- `git diff package.json` shows ONLY a single inserted line (the new script)
</verification>

<success_criteria>
SC#3 closes: package.json gains the `:migrate` sibling; default publish UNCHANGED; docs note added; SKILL rule landed.
</success_criteria>

<output>
After completion, create `.planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-03-SUMMARY.md`.
</output>
