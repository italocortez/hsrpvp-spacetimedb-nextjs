---
phase: 16.4-spacetimedb-v2-2-0-refactor-pass
plan: 05
subsystem: ui
tags: [spacetimedb, useTable, enabled, react, admin-view, subscription-gating, v2.2.0]

# Dependency graph
requires:
  - phase: 16.3-spacetimedb-v2-1-0-realign-v2-2-0-upgrade-inserted
    provides: "v2.2.0 SDK shipped to maincloud — useTable({ enabled }) typing available at spacetimedb/dist/react/useTable.d.ts"
provides:
  - "useTable({ enabled }) adopted at 2 admin-route panels (UserManager, TableExplorer)"
  - "isActive prop convention for gated admin tabs"
  - "SKILL.md rule: layer-0 always-on, gated panels use useTable enabled"
  - "v09-frontend-subscription-strategy.md Decision 9 (committed on disk; gitignored — see deviations)"
affects: [phase 16.4 plan 06, phase 17, future-admin-panels, useReducer-adoption]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useTable({ enabled: <boolean> }) for visibility-gated panels"
    - "isActive: boolean prop threaded from tab-state parent"

key-files:
  created:
    - .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-05-SUMMARY.md
  modified:
    - app/(authed)/admin-view/page.tsx
    - components/features/admin-view/components/UserManager.tsx
    - components/features/admin-view/components/TableExplorer.tsx
    - .claude/skills/spacetimedb/SKILL.md
    - notes/v09-frontend-subscription-strategy.md (gitignored — disk-only edit)

key-decisions:
  - "Gating layer-0 explicitly forbidden (D-05 hard constraint) — GameDataProvider's 7 layer-0 subs MUST stay always-on; service worker portrait/Spine prefetch + anonymous-page hydration depend on this"
  - "isActive prop threaded from AdminViewPage based on activeTab — passed only to subs-gated children (UserManager, TableExplorer); BulkUpsert untouched (no useTable)"
  - "{ enabled: isActive } lives in 2nd-arg callback object alongside onInsert/onDelete/onUpdate (verified at dist/react/useTable.d.ts:1-10) — NOT a third arg"
  - "Reducer-call pattern (getConnection().reducers.X → useReducer) is OUT of scope for Plan 05 — reserved for Plan 06 per CONTEXT.md D-15"

patterns-established:
  - "Gated admin panels: useTable(table, { enabled: isActive }) with isActive prop derived from tab visibility"
  - "Layer-0 reference data subs always-on, no enabled prop"
  - "Decision 9 in v09-frontend-subscription-strategy.md codifies layer-0-always-on / gated-panels-use-enabled rule"

requirements-completed:
  - refactor:SC#5

# Metrics
duration: 8min
completed: 2026-05-03
---

# Phase 16.4 Plan 05: useTable({ enabled }) for admin tabs Summary

**Adopted v2.2.0's `useTable({ enabled })` (PR #4721) at 2 admin-route panels (UserManager, TableExplorer); GameDataProvider's 7 layer-0 subs stay always-on per D-05 hard constraint; SKILL rule + Decision 9 landed**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-03T14:34:41Z
- **Completed:** 2026-05-03T14:42:27Z
- **Tasks:** 3 (all auto)
- **Files modified:** 5 (4 committed + 1 gitignored on-disk)

## Accomplishments

- `app/(authed)/admin-view/page.tsx` threads `isActive={activeTab === 'users'}` to UserManager and `isActive={activeTab === 'tables'}` to TableExplorer.
- `UserManager` accepts `{ isActive }: { isActive: boolean }` and gates `useTable(tables.User, { enabled: isActive })`.
- `TableExplorer` accepts `{ isActive }: { isActive: boolean }` and gates `useTable(TABLE_MAP[selectedTable], { enabled: isActive })`.
- `BulkUpsert` untouched (no useTable subscription — Plan 06 owns its `useReducer` adoption).
- `GameDataProvider.tsx` has zero changes across all 3 commits (D-05 hard constraint upheld — verified via `git diff HEAD~3 HEAD --stat`).
- `notes/v09-frontend-subscription-strategy.md` Decision 9 appended on disk between Decision 8 and "Out of scope for v0.9" with full Scope/Rationale/Implementation notes/Cross-references shape (gitignored — see deviations).
- `.claude/skills/spacetimedb/SKILL.md` gains `### Subscription gating: layer-0 always-on, gated panels use useTable enabled` rule with GOOD/BAD code comparison and cross-link to Decision 9.

Closes refactor:SC#5 — real measurable egress drop on `/admin-view` (User table + dynamic table sub no longer fire on mount when their tab is not the active one; the `display: none` pattern that previously kept the components mounted no longer carries an active sub).

## Task Commits

Each task was committed atomically (single-repo, no sub_repos):

1. **Task 1: Thread isActive prop + gate UserManager + TableExplorer useTable** — `9905409` (refactor)
2. **Task 2: Append Decision 9 to v09-frontend-subscription-strategy.md** — *not committed* (file is gitignored — see Deviations §1; disk-only edit verified via grep)
3. **Task 3: Append subscription-gating rule to SKILL.md** — `646eabd` (docs)

_Note: 2 commits made; Task 2 disk edit is verified live via grep but did not produce a git commit because `notes/` is in `.gitignore` (line 52-54). Per CLAUDE.md "Gitignore Guardrail" — `git add -f` is forbidden under any circumstance._

## Files Created/Modified

- `app/(authed)/admin-view/page.tsx` — threads `isActive` prop to UserManager + TableExplorer based on `activeTab`. (committed `9905409`)
- `components/features/admin-view/components/UserManager.tsx` — accepts `isActive` prop; gates `useTable(tables.User, { enabled: isActive })`. (committed `9905409`)
- `components/features/admin-view/components/TableExplorer.tsx` — accepts `isActive` prop; gates `useTable(TABLE_MAP[selectedTable], { enabled: isActive })`. (committed `9905409`)
- `.claude/skills/spacetimedb/SKILL.md` — appends `### Subscription gating: layer-0 always-on, gated panels use useTable enabled` rule between `### Subscription pitfalls` and `### Handling reducer errors on the client`. (committed `646eabd`)
- `notes/v09-frontend-subscription-strategy.md` — Decision 9 appended on disk between Decision 8 and "Out of scope for v0.9" sections. **NOT committed (file is gitignored).** Disk content verified via `grep -c "## Decision 9 — Layer-0 subscriptions are always-on" notes/v09-frontend-subscription-strategy.md` → 1.

## Decisions Made

- **Disk-only update for `notes/v09-frontend-subscription-strategy.md`.** The plan's `files_modified` frontmatter included `notes/v09-frontend-subscription-strategy.md`, but `notes/` is in `.gitignore` (line 52-54) and the file has never been tracked in git. Per project CLAUDE.md "Gitignore Guardrail" (no `git add -f`, ever, for any reason), the update was applied on disk only and is NOT committed. The disk edit is durable for the developer but invisible to git history. Future readers of git history will see only the SKILL.md commit referencing Decision 9.
- **No reducer-call refactor in Plan 05.** Both UserManager and TableExplorer still use `getConnection().reducers.X(...)` patterns; Plan 06 owns the `useReducer` adoption per CONTEXT.md D-15.
- **Used `display: none` pattern preserved.** Plan kept the existing parent layout (all 3 children mounted, visibility toggled via `display: none`); the gating mechanism is the `enabled` prop on `useTable`, not unmount/remount.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking + Gitignore Guardrail] `notes/v09-frontend-subscription-strategy.md` is gitignored, cannot be committed**

- **Found during:** Task 2 (`git add notes/v09-frontend-subscription-strategy.md`)
- **Issue:** The plan's `files_modified` frontmatter listed `notes/v09-frontend-subscription-strategy.md` and Task 2 mandated appending Decision 9 to it. `git add` failed: "The following paths are ignored by one of your .gitignore files: notes". `git check-ignore` confirmed `.gitignore:54:/notes`. `git ls-files notes/` returns empty — the file has never been tracked. The plan's pre-execution gitignore audit appears to have missed this entry.
- **Fix:** Followed the project's CLAUDE.md "Gitignore Guardrail" rule (no `git add -f` ever). Applied the disk edit (verified: `grep -c "## Decision 9 — Layer-0 subscriptions are always-on" notes/v09-frontend-subscription-strategy.md` → 1; full Scope/Rationale/Implementation notes/Cross-references shape matches existing decisions). Skipped the commit. Documented this deviation here so the user can decide whether to:
  - (a) accept the disk-only edit as-is (the doc is a local working strategy reference; gitignored is intentional design),
  - (b) re-track the file by removing `notes/` from `.gitignore` (separate hygiene task — not Plan 05 scope),
  - (c) move the decision text into a tracked location during `/gsd-verify-work`.
- **Files modified:** `notes/v09-frontend-subscription-strategy.md` (disk-only)
- **Verification:** All 7 Task 2 acceptance grep checks pass against the on-disk file. Decision 9 heading present, Decision 8 still present, Decision 10 absent (Plan 06 owns it), `### Scope` count went from 2 to 3 (one new section), "Phase 16.4 Plan 05" referenced 3x, "first frame missing portraits" rationale present, `useTable.d.ts:1-10` SDK citation present.
- **Committed in:** N/A (gitignored — see issue above). The SKILL.md cross-reference to Decision 9 IS committed in `646eabd`, providing a tracked breadcrumb for future readers.

---

**Total deviations:** 1 auto-fixed (1 blocking via gitignore guardrail)
**Impact on plan:** Functional outcome (Decision 9 appended to v09 strategy doc on disk) is delivered. Git-history visibility of Decision 9 is reduced — only the SKILL.md cross-reference is committed. No scope creep. No code-correctness impact (the doc is reference material; runtime behavior is determined by the SKILL rule + the actual `useTable({ enabled })` calls in UserManager/TableExplorer, both of which ARE committed).

## Issues Encountered

- **`npm run lint` is interactively blocked.** `next lint` is deprecated and prompts ESLint config setup; this is a pre-existing repo issue (not introduced by Plan 05). Per executor SCOPE BOUNDARY, out-of-scope; not auto-fixed. `npm run test:typecheck` passed cleanly twice (after Task 1, and final post-Task-3) — sufficient build gate for a 6-line refactor with no new logic.
- **Frontend self-check (Playwright CLI) — partial.** Started `npm run dev` (HTTP 200 on `/`) and ran `playwright-cli open http://localhost:3000/admin-view` + `screenshot`. The route redirected to landing page because `/admin-view` lives under the `(authed)` route group and the playwright session is unauthenticated. Could not capture an authed admin-view screenshot without a real Discord login flow, which is not feasible in this executor session. Per CLAUDE.md: "pure refactors with no visual delta" exception applies — when `isActive===true` (the only state where the user can see the panel), `useTable(t, { enabled: true })` behaves identically to the pre-refactor `useTable(t)` (same data, same rendering). When `isActive===false`, the panel is `display: none` so no user-visible delta exists in any observable state. Saved unauthed-redirect screenshot to `tmp/pw/16.4-05-admin-view-tables.png` (gitignored — for executor self-verify only). Closed browser at session end.

[Note: "Deviations from Plan" documents unplanned work that was handled automatically. "Issues Encountered" documents problems during planned work.]

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 06 ready to start.** Plan 05 deliberately did NOT touch reducer-call patterns in UserManager/TableExplorer (still on `getConnection().reducers.X(...)`). Plan 06 owns those + BulkUpsert + ProfileCard + useAuth.ts adopting `useReducer(reducers.X)` per CONTEXT.md D-15.
- **GameDataProvider untouched** as required by D-05. Plan 06 should NOT touch it either (same hard constraint — service worker prefetch dependency).
- **`isActive` prop is now a stable convention.** Future gated admin panels added in 17+ phases should follow the same pattern: take `isActive: boolean`, pass `{ enabled: isActive }` to `useTable`. SKILL.md rule + Decision 9 codify this.
- **Decision-9 disk-vs-git discrepancy.** Recommend `/gsd-verify-work` reviewer decide on resolution path (a/b/c above). The SKILL.md rule (which IS in git) cross-references the disk-only Decision 9 by name, so a reader looking up "subscription gating" finds the rule in the tracked SKILL but is pointed at the disk-only strategy doc for the longer rationale.

## Threat Flags

None — no new security-relevant surface introduced. The `enabled` prop only gates whether the SDK establishes the subscription; it does NOT change subscription scope (the query string is unchanged), so threat T-16.4-05-01 is mitigated by construction. When `isActive === true`, the data set rendered is identical to pre-refactor.

## Self-Check: PASSED

- `app/(authed)/admin-view/page.tsx` exists and contains `isActive={activeTab === 'tables'}` (1) + `isActive={activeTab === 'users'}` (1) — verified.
- `components/features/admin-view/components/UserManager.tsx` exists and contains `function UserManager({ isActive }: { isActive: boolean })` (1) + `useTable(tables.User, { enabled: isActive })` (1) — verified.
- `components/features/admin-view/components/TableExplorer.tsx` exists and contains `function TableExplorer({ isActive }: { isActive: boolean })` (1) + `useTable(TABLE_MAP[selectedTable], { enabled: isActive })` (1) — verified.
- `.claude/skills/spacetimedb/SKILL.md` exists and contains `### Subscription gating: layer-0 always-on, gated panels use useTable enabled` (1) — verified.
- `notes/v09-frontend-subscription-strategy.md` exists on disk (gitignored) and contains `## Decision 9 — Layer-0 subscriptions are always-on` (1) — verified.
- `git diff HEAD~3 HEAD --stat -- components/features/game-data/components/GameDataProvider.tsx` is empty — D-05 hard constraint upheld.
- Task 1 commit `9905409` present in `git log` — verified.
- Task 3 commit `646eabd` present in `git log` — verified.
- `npm run test:typecheck` exits 0 — verified twice.

---
*Phase: 16.4-spacetimedb-v2-2-0-refactor-pass*
*Completed: 2026-05-03*
