---
phase: 16.4
slug: spacetimedb-v2-2-0-refactor-pass
status: verified
threats_open: 0
asvs_level: 1
created: 2026-05-06
---

# Phase 16.4 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

Pure refactor pass for spacetimedb v2.2.0 — no new external-facing behavior beyond the dev-only `tools/capture-ws-frames.ts` script and the dev `:migrate` npm script. All 4 dispositioned threats verified closed; 3 plans (01, 03, 07) declared no-new-threats and confirmed the existing security envelope is unchanged.

Detailed audit evidence: see `16.4-SECURITY-AUDIT.md`.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| client → reducer (`server_nuke_test_data`) | Plan 01 — refactor preserves two-gate defense | `requireServer(ctx)` + `confirmation === 'NUKE_TEST_DATA'` |
| external WS client → scheduled reducer | Plan 02 — engine-level "no such reducer" rejection (defense in depth) | scheduled-reducer dispatch frames |
| developer → spacetime CLI | Plan 03 — default `:publish` retains all destructive prompts; `:migrate` auto-confirms ONLY migration prompt | per-prompt CLI confirmation flow |
| developer machine → maincloud (read-only WS) | Plan 04 — `tools/capture-ws-frames.ts`; subscribe + minimal reducer call + unsub | low-privilege guest token + frame size/direction only (no payload) |
| client → SpacetimeDB subscription (gated `useTable`) | Plan 05 — `enabled` prop only gates establishment, not query scope | unchanged subscription query strings |
| client → reducer (via `useReducer` hook) | Plan 06 — typed callback queues until ready; same `ctx.sender` auth path | per-call reducer invocations across 5 declared + 1 unregistered React sites |
| developer machine → maincloud (publish) + read-only SQL probe | Plan 07 — republish only; admin-gated `admin_gc_lobbies` smoke probe | `~/.spacetimedb/credentials.json` + `spacetime sql` against `st_table` / `gc_result` |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-16.4-02-01 | Tampering / Elevation of Privilege | `run_user_deletion`, `run_lobby_gc`, `run_identity_gc` | mitigate | Engine-level "no such reducer" rejection (structural); regression sensor `test/backend/scheduled-reducer-isInternal-rejection.test.ts` exercises external invocation of all 3 reducers (Plan 07 hotfix removed app-layer `isInternal` guards after confirming engine layer was already rejecting external dispatch — UAT 3/3 pass) | closed |
| T-16.4-04-01 | Information Disclosure | `tools/capture-ws-frames.ts` reading `.env.local` `CAPTURE_WS_FIXTURE_TOKEN` | mitigate | Script exits if token missing OR equals `SPACETIMEDB_SERVER_TOKEN` (admin-token guard at lines 47-71); frame log emits `{ t, dir, size }` only — `data` access limited to `byteLength` / `length`, no payload field reads (lines 170-180) | closed |
| T-16.4-05-01 | Information Disclosure (regression) | `UserManager` + `TableExplorer` (gated `useTable` adoption) | mitigate | `enabled` prop gates SDK establishment only; query strings unchanged from pre-refactor → subscription scope unchanged. `enabled: false` produces NO data (no scope leak vector). Verified at `UserManager.tsx:35-36`, `TableExplorer.tsx:95`, `app/(authed)/admin-view/page.tsx:17,23` | closed |
| T-16.4-06-01 | Tampering (regression) | 5 React reducer-call sites adopting `useReducer` (`useAuth`, `UserManager`, `TableExplorer`, `BulkUpsert`, `ProfileCard`) | mitigate | Typed callback queues until connection-ready (no scope/permission change at SDK or server). Per-site `.catch()` error UX preserved; `setIsSubmitting` lifecycle preserved in BulkUpsert via `.finally()` (`:276`, `:362`). `ctx.sender` auth path unchanged at server | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

**No-new-threats plans:** 01, 03, 07 — auditor confirmed existing security envelope unchanged (see audit notes in `16.4-SECURITY-AUDIT.md`).

**Unregistered flag (informational, covered):** `BulkUpsert.tsx:209` `useReducer(reducers.adminAssignCharacterArchetypes)` is a 6th `useReducer` adoption site beyond Plan 06's declared 5-site list. Same mitigation pattern (typed Promise queue + `.catch()` + `.finally()`). Risk profile identical to T-16.4-06-01; treated as covered. Recommend next phase touching `BulkUpsert` map this site explicitly in its threat register.

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|

*No accepted risks.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-06 | 4 | 4 | 0 | gsd-security-auditor (sonnet) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-05-06
