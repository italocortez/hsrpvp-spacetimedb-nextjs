---
phase: 16.4
slug: spacetimedb-v2-2-0-refactor-pass
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-03
audited: 2026-05-06
---

# Phase 16.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Refactor phase — primary regression sensor is the existing `npm run test:integration` suite per RESEARCH.md `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.0 (devDep) |
| **Config file** | `vitest.config.ts` (root); integration suite per `npm run test:integration` |
| **Quick run command** | `npm run test:integration -- --run <focus>` (per touched-file path) |
| **Full suite command** | `npm run test:integration` |
| **Estimated runtime** | ~60–180s for full integration suite (varies per maincloud RTT) |

---

## Sampling Rate

- **After every task commit:** Run focused integration test relevant to the touched file (or `npm run typecheck` for skill/notes-only edits).
- **After every plan wave:** Run `npm run test:integration` (full integration suite).
- **Before `/gsd-verify-work`:** Full suite must be green AND Plan 07 republish + smoke probes must pass.
- **Max feedback latency:** ~180 seconds (full integration suite).

---

## Per-Task Verification Map

> Backfilled per checker W-1. Each task row carries the `<automated>` verify command from its plan, plus requirement / threat refs and file-existence status. Threat refs cite the plan's STRIDE register; "—" means the plan has no new threat surface for the task.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 16.4-01-01 | 01 | 1 | refactor:SC#1 | — | Two-gate defense (`requireServer` + confirmation) preserved on `server_nuke_test_data` | integration | `grep -c "\.clear();" spacetimedb/src/reducers/server.ts && grep -c "requireServer(ctx)" spacetimedb/src/reducers/server.ts && grep -c "NUKE_TEST_DATA" spacetimedb/src/reducers/server.ts` | ✅ existing | ✅ green |
| 16.4-01-02 | 01 | 1 | refactor:SC#1 | — | SKILL rule references caveat against gating layer-0 wipes | unit (grep) | `grep -c "### Whole-table wipes — use Table.clear()" .claude/skills/spacetimedb/SKILL.md` | ✅ existing | ✅ green |
| 16.4-02-01 | 02 | 1 | refactor:SC#2 | T-16.4-02-01 | `run_lobby_gc` + `run_identity_gc` external invocation rejected by engine layer (Plan 07 hotfix removed app-layer guards; rationale documented in source) | unit (grep) + delegated to 02-05 | `grep -c "Plan 07 hotfix" spacetimedb/src/reducers/lobbyGc.ts && grep -c "Plan 07 hotfix" spacetimedb/src/reducers/identityGc.ts` *(behavior verified by 02-05 regression test)* | ✅ existing | ✅ green (refreshed 2026-05-06) |
| 16.4-02-02 | 02 | 1 | refactor:SC#2 | T-16.4-02-01 | `run_user_deletion` external invocation rejected by engine layer; helper `performUserDeletion` UNCHANGED (Plan 07 hotfix rationale in source) | unit (grep) + delegated to 02-05 | `grep -c "Plan 07 hotfix" spacetimedb/src/reducers/userDeletion.ts && grep -c "performUserDeletion" spacetimedb/src/reducers/userDeletion.ts` *(behavior verified by 02-05 regression test)* | ✅ existing | ✅ green (refreshed 2026-05-06) |
| 16.4-02-03 | 02 | 1 | refactor:SC#2 | T-16.4-02-01 | AUDIT-NOTES documents AuthCtx applicability + isInternal hardening rationale | unit (grep) | `test -f .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-AUDIT-NOTES.md && grep -c "## AuthCtx applicability audit — D-13" .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-AUDIT-NOTES.md` | ❌ created | ✅ green |
| 16.4-02-04 | 02 | 1 | refactor:SC#2 | T-16.4-02-01 | SKILL rule documents AuthCtx vs requireServer / getAuthenticatedUser (Plan 07 renamed heading) | unit (grep) | `grep -c "### AuthCtx applicability + scheduled-reducer external-invocation defense" .claude/skills/spacetimedb/SKILL.md` | ✅ existing | ✅ green (refreshed 2026-05-06) |
| 16.4-02-05 | 02 | 1 | refactor:SC#2 | T-16.4-02-01 | External WS callers receive engine-level "no such reducer" rejection from each of the 3 scheduled reducers | integration | `test -f test/backend/scheduled-reducer-isInternal-rejection.test.ts && grep -c "run_user_deletion\|runUserDeletion" test/backend/scheduled-reducer-isInternal-rejection.test.ts && grep -c "run_lobby_gc\|runLobbyGc" test/backend/scheduled-reducer-isInternal-rejection.test.ts && grep -c "run_identity_gc\|runIdentityGc" test/backend/scheduled-reducer-isInternal-rejection.test.ts` *(integration assertion: 3/3 PASS per 16.4-VERIFY-NOTES SC#7 + commits `9270294`, `39a750d`, `e66f8a9`)* | ❌ created | ✅ green |
| 16.4-03-01 | 03 | 1 | refactor:SC#3 | — | `:migrate` script auto-confirms ONLY the migration prompt; default publish stays interactive | unit (script) | `node -e "const p = require('./package.json'); if (!p.scripts['spacetime:publish:migrate']) process.exit(1); if (!/--yes=migrate/.test(p.scripts['spacetime:publish:migrate'])) process.exit(2); if (!/--server maincloud/.test(p.scripts['spacetime:publish:migrate'])) process.exit(3); console.log('OK');"` | ✅ existing | ✅ green |
| 16.4-03-02 | 03 | 1 | refactor:SC#3 | — | docs/smoke/contract.md notes the `:migrate` variant near operational publish text | unit (grep) | `grep -c "spacetime:publish:migrate" docs/smoke/contract.md && grep -c "spacetime publish --clear-database" docs/smoke/contract.md` | ✅ existing | ✅ green |
| 16.4-03-03 | 03 | 1 | refactor:SC#3 | — | SKILL rule captures `:migrate` convention + brotli + #4593 informational notes | unit (grep) | `grep -c "### Publishing: use :migrate for reviewed schema deltas" .claude/skills/spacetimedb/SKILL.md && grep -c "spacetime:publish:migrate" .claude/skills/spacetimedb/SKILL.md` | ✅ existing | ✅ green |
| 16.4-04-01 | 04 | 1 | refactor:SC#4 | T-16.4-04-01 | `ws` + `@types/ws` installed as devDeps; CAPTURE_WS_FIXTURE_TOKEN env entry surfaced (manual step) | unit (script) | `node -e "console.log(require('./package.json').devDependencies.ws)"` | ✅ existing | ✅ green |
| 16.4-04-02 | 04 | 1 | refactor:SC#4 | T-16.4-04-01 | Capture script source enforces fixture-token-only (admin-token guard); records frame size + direction only | unit (typecheck + grep) | `npm run test:typecheck` | ❌ created | ✅ green (typecheck exit 0 — 2026-05-06) |
| 16.4-04-03 | 04 | 1 | refactor:SC#4 | T-16.4-04-01 | Capture artifact (`frames.jsonl` + `summary.md`) committed; "maincloud-as-shipped" baseline narrative present | unit (file + line count) | `test -f .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/transport-evidence/frames.jsonl && test -f .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/transport-evidence/summary.md && [ "$(wc -l < .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/transport-evidence/frames.jsonl)" -ge 5 ]` | ❌ created | ✅ green |
| 16.4-05-01 | 05 | 1 | refactor:SC#5 | T-16.4-05-01 | Admin-tab gating produces IDENTICAL data when `isActive === true`; GameDataProvider 7 layer-0 subs UNCHANGED | typecheck + manual | `npm run test:typecheck && grep -c "{ enabled: isActive }" components/features/admin-view/components/UserManager.tsx && grep -c "{ enabled: isActive }" components/features/admin-view/components/TableExplorer.tsx` | ✅ existing | ✅ green |
| 16.4-05-02 | 05 | 1 | refactor:SC#5 | T-16.4-05-01 | Decision 9 documents layer-0 always-on / gated panels rule | unit (grep) | `grep -c "## Decision 9 — Layer-0 subscriptions are always-on" notes/v09-frontend-subscription-strategy.md` | ✅ existing | ✅ green |
| 16.4-05-03 | 05 | 1 | refactor:SC#5 | T-16.4-05-01 | SKILL rule + GOOD/BAD code comparison + cross-link to Decision 9 | unit (grep) | `grep -c "### Subscription gating: layer-0 always-on, gated panels use useTable enabled" .claude/skills/spacetimedb/SKILL.md && grep -c "PR #4721" .claude/skills/spacetimedb/SKILL.md` | ✅ existing | ✅ green |
| 16.4-06-01 | 06 | 1 | refactor:SC#6 | T-16.4-06-01 | useReducer adoption preserves observable reducer-call semantics in useAuth (3 sites) | typecheck + grep | `npm run test:typecheck && grep -c "useReducer(reducers.loginAsGuest)" components/features/auth/hooks/useAuth.ts && grep -c "useReducer(reducers.deleteGuestAccount)" components/features/auth/hooks/useAuth.ts` | ✅ existing | ✅ green |
| 16.4-06-02 | 06 | 1 | refactor:SC#6 | T-16.4-06-01 | useReducer adoption in UserManager + TableExplorer; Plan 05 gating preserved | typecheck + grep | `npm run test:typecheck && grep -c "useReducer(reducers.adminUpdateUser)" components/features/admin-view/components/UserManager.tsx && grep -c "useReducer(reducers.adminDeleteRow)" components/features/admin-view/components/TableExplorer.tsx` | ✅ existing | ✅ green |
| 16.4-06-03 | 06 | 1 | refactor:SC#6 | T-16.4-06-01 | useReducer in BulkUpsert + ProfileCard; try/finally → then/catch/finally lifecycle preserved; link-discord/route.ts UNCHANGED | typecheck + grep | `npm run test:typecheck && grep -c "useReducer(reducers.adminBulkUpsert)" components/features/admin-view/components/BulkUpsert.tsx && grep -c "useReducer(reducers.updateUsername)" components/features/profile/components/ProfileCard.tsx` | ✅ existing | ✅ green |
| 16.4-06-04 | 06 | 1 | refactor:SC#6 | T-16.4-06-01 | Decision 10 + SKILL rule document `useReducer(reducers.X)` convention; D-15 exclusions cited | unit (grep) | `grep -c "## Decision 10 — Reducer calls in React components use useReducer" notes/v09-frontend-subscription-strategy.md` | ✅ existing | ✅ green |
| 16.4-07-01 | 07 | 2 | refactor:SC#7 / SC#8 | — | Full integration suite green AND `tools/energy-model.js` stdout identical pre/post via worktree diff | integration + diff | `npm run test:integration` *(verified at execution time per 16.4-VERIFY-NOTES SC#7 PASS + commits `9c634bc`/`f24fd86`/`ead065d`/`6515a59` post-hotfix isolation rerun 5/5; energy-model diff exit 0 per SC#8)* | ❌ created | ✅ green (covered by execution + UAT closure) |
| 16.4-07-02 | 07 | 2 | refactor:SC#9 | — | All 5 SKILL rules + 2 Decisions present (cross-cut audit per D-10) | unit (grep) | `grep -c "### Whole-table wipes — use Table.clear()" .claude/skills/spacetimedb/SKILL.md && grep -c "## Decision 10 — Reducer calls in React components use useReducer" notes/v09-frontend-subscription-strategy.md` | ✅ existing | ✅ green |
| 16.4-07-03 | 07 | 2 | refactor:SC#7 | — | Maincloud republish via plain `spacetime:publish` (no `--clear-database`, no `--yes=migrate`); st_table schema-delta probe + admin_gc_lobbies + GcResult round-trip | checkpoint:human-verify | manual gate per CLAUDE.md Autonomous Execution `<DO pause for>` cross-boundary actions; verification embedded in 16.4-VERIFY-NOTES.md | manual | ✅ green (UAT closed 13/13) |
| 16.4-07-04 | 07 | 2 | refactor:SC#7 / SC#8 / SC#9 | — | Rollback path documented + final verdict table for all 9 SCs | unit (grep) | `grep -c "## Final verdict" .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-VERIFY-NOTES.md && grep -c "## Rollback path" .planning/phases/16.4-spacetimedb-v2-2-0-refactor-pass/16.4-VERIFY-NOTES.md` | ❌ created | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · File Exists: ✅ existing (modify) · ❌ created (new file in this plan)*

---

## Wave 0 Requirements

- [ ] No new framework install — vitest 4.1.0 already in devDeps.
- [x] Plan 04 may need `ws` (Node WebSocket lib) for `tools/capture-ws-frames.ts`; verify with `npm ls ws` and add as devDep if missing — Plan 04 Task 1 owns the install.
- [x] `.env.local` extension for `CAPTURE_WS_FIXTURE_TOKEN` (Plan 04, per RESEARCH.md OQ-2).
- [x] Plan 02 Task 5 — automated regression test for `isInternal` rejection (per checker B-2; closes the Wave 0 Gap flagged at RESEARCH.md `## Validation Architecture` line 847).

*Existing integration test infrastructure covers regression sensing for all 9 success criteria; Plan 02 Task 5 + Plan 04 + Plan 07 add bespoke probes (isInternal regression test + WS frame capture + maincloud SQL probes) which are committed under `test/backend/` and `.planning/phases/16.4-.../transport-evidence/` and embedded in PLAN.md `<acceptance_criteria>` per the project UAT format.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| v3 transport multi-message batching observable | SC#4 | Requires DevTools Network → WS frame inspection on a live maincloud connection; not stubbable. | Open the running app against maincloud; open DevTools Network → WS; trigger a subscribe-heavy interaction (admin page or lobby join); capture frames. Cross-reference against `tools/capture-ws-frames.ts` log for batch counts. |
| Maincloud republish smoke gate | D-08 | Republish is a one-shot operation against the live module; cannot be unit-tested. | Plan 07: `npm run spacetime:publish` from the post-refactor commit; then `spacetime sql` queries embedded in 16.4-07-PLAN.md `<acceptance_criteria>` (e.g., `st_table` schema-delta probe; trigger `admin_gc_lobbies` and verify a `GcResult` row appears). |
| Service worker portrait/Spine prefetch unbroken (GameDataProvider layer-0 stays always-on) | SC#5 (negative — must NOT be broken) | Service worker behavior is browser-state dependent; covered by app smoke (load anon page → portraits/Spine present). | Run dev server, hard-refresh anon page in incognito; verify character portraits render in first frame (not after a delay). Plan 05 acceptance criterion. |
| Energy-model.js sanity-check output identical pre/post | SC#8 | Static estimator; not part of vitest suite. | Plan 07 Task 1: capture `node tools/energy-model.js` output before Plan 01 starts (via worktree on `PRE_REFACTOR_SHA`); rerun post-refactor; diff must be empty. Worktree-based diff command embedded in 16.4-07-PLAN.md Task 1 `<automated>`. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (planner filled the per-task table above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task ID has either `<automated>` or is a `checkpoint:human-verify`)
- [x] Wave 0 covers all MISSING references (Plan 04 `ws` dep + `CAPTURE_WS_FIXTURE_TOKEN` env entry + Plan 02 Task 5 isInternal regression test)
- [x] No watch-mode flags
- [x] Feedback latency < 180s (full integration suite worst case)
- [x] `nyquist_compliant: true` set in frontmatter (after planner populates per-task map)

**Approval:** signed-off by gsd-planner revision pass (checker W-1 closed); audit-verified 2026-05-06 (gsd-validate-phase, run by italo.cortezv).

---

## Validation Audit 2026-05-06

| Metric | Count |
|--------|-------|
| Tasks audited | 22 |
| ✅ Green (verified) | 22 |
| ⚠️ Stale-refreshed | 3 (16.4-02-01, 02-02, 02-04 — recorded commands updated to point at current evidence post-Plan 07 hotfix) |
| ❌ Red | 0 |
| ⬜ Pending | 0 |
| Manual-only | 1 (16.4-07-03 — UAT-closed) |
| New tests generated | 0 |

### Stale-command refresh rationale

Plan 07 hotfix (`9c634bc` → `6515a59`) REMOVED the application-layer `ctx.senderAuth.isInternal` guards from `userDeletion.ts`, `lobbyGc.ts`, and `identityGc.ts` after isolation rerun proved the SDK v2.2.0 `senderAuth` factory always sets `isInternal: false` — making the engine-level "no such reducer" defense the actual mitigation for T-16.4-02-01. The original validation commands greppinng for `ctx.senderAuth.isInternal` in those files no longer match (only docstring references remain). Refreshed commands now grep for the `Plan 07 hotfix` rationale comment + delegate behavior verification to task 16.4-02-05's regression test (`scheduled-reducer-isInternal-rejection.test.ts`), which is the surviving sensor for the threat. SKILL.md heading was also renamed from `### AuthCtx applicability + isInternal hardening for scheduled reducers` → `### AuthCtx applicability + scheduled-reducer external-invocation defense (v2.2.0+)` (task 16.4-02-04 command updated accordingly).

### Wave 0 closure

Wave 0 was sequenced and closed during execution (Plan 02 Task 5 added the regression test, Plan 04 Task 1 installed `ws` devDep + surfaced the env entry). All Wave 0 dependencies are satisfied; `wave_0_complete: true` in frontmatter.

### Why integration suite was not re-run during this audit

The integration suite was started (background id `bqxbd6rgs`) but exceeded 15 minutes with no output progression — likely a wrapper buffering issue. SC#7 and SC#8 are already PASS per `16.4-VERIFY-NOTES.md` line 342–344 (post-hotfix isolation rerun 5/5; regression sensor 3/3; energy-model diff exit 0); UAT closed 13/13 per `16.4-UAT.md`. Re-running here would produce redundant evidence. If a future audit needs a fresh suite run, invoke directly: `npm run test:integration` (no `tail` pipe — output streams to terminal).
