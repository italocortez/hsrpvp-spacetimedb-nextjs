---
phase: 15.1
slug: auction-cost-template-extension
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-14
---

# Phase 15.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npm run test:integration -- test/backend/reducers/admin/seed-cost-extraction.test.ts` |
| **Full suite command** | `npm run test:integration` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `{quick run command}`
- **After every plan wave:** Run `{full suite command}`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.1-01-T1 | 01 | 1 | D-01, D-02, D-03, D-04, D-05, D-09, D-10 | T-15.1-01 | fixture data only, no runtime surface | template-shape | `node -e "const j=JSON.parse(require('fs').readFileSync('test/data-templates/characters_template.json','utf8')); ... assert P1/P2/P3/P4"` (see Plan 01 Task 1 `<verify>`) | ✅ | ⬜ pending |
| 15.1-01-T2 | 01 | 1 | D-01, D-09, D-10 | T-15.1-01 | fixture data only | template-shape | `node -e "const j=JSON.parse(require('fs').readFileSync('test/data-templates/lightcones_template.json','utf8')); ... assert P1/P2/P3/P4"` (see Plan 01 Task 2 `<verify>`) | ✅ | ⬜ pending |
| 15.1-01-T3 | 01 | 1 | D-02, D-03, D-04, D-05 (gitignore boundary) | T-15.1-01 | commit boundary | git-check | `git log -1 --stat && git check-ignore test/data-templates/*.json; [ $? -eq 1 ]` | ✅ | ⬜ pending |
| 15.1-02-T1 | 02 | 1 | D-02, D-13, D-17 | T-15.1-02 | docs only | grep | `node -e "..."` asserts L84 placeholder removed + sibling-block rows present + pairing section intact (see Plan 02 Task 1 `<verify>`) | ✅ | ⬜ pending |
| 15.1-02-T2 | 02 | 1 | D-17 (gitignore boundary) | T-15.1-02 | commit boundary | git-check | `git log -1 --name-only` shows only `test/data-templates/README.md` | ✅ | ⬜ pending |
| 15.1-03-T1 | 03 | 2 | D-06, D-07, D-08, D-09, D-10 | T-15.1-03 | extractor guards on malformed JSON; D-09 skip rule; no placeholder survives | grep + typecheck | `grep -n "auctionBaseBid:\s*{\s*\.\.\." scripts/seed-data.ts ... && npx tsc --noEmit` (see Plan 03 Task 1 `<verify>`) | ✅ | ⬜ pending |
| 15.1-03-T2 | 03 | 2 | D-06, D-07, D-08, D-09, D-10 | T-15.1-03 | mirror extractor contract in test harness | grep + typecheck | `grep -n "auctionBaseBid:\s*eidolonCost" test/shared/seed-data.ts ... && npx tsc --noEmit -p .` (see Plan 03 Task 2 `<verify>`) | ✅ | ⬜ pending |
| 15.1-03-T3 | 03 | 2 | D-11, D-12, D-13, P-01 (gitignore boundary + force-track residue) | T-15.1-03 | local-only data migration; no commit | git-check | `git check-ignore tmp/... test/data/... && git ls-files test/data/ = empty && git status --short clean of tmp/ or test/data/` (see Plan 03 Task 3 `<verify>`) | ✅ | ⬜ pending |
| 15.1-03-T4 | 03 | 2 | D-03, D-14, D-15, D-16 (commit scope + reducer/schema guard) | T-15.1-04, T-15.1-05 | only 2 .ts files in commit; no reducer/table/validation files touched phase-wide | git-diff | `git log -1 --name-only = scripts/seed-data.ts + test/shared/seed-data.ts AND git diff ${PHASE_BASE}..HEAD` matches no `spacetimedb/src/(reducers/(admin\|costSetManagement\|draftClassic\|draftAuction\|postDraft)\.ts\|tables/)` (see Plan 03 Task 4 `<verify>`) | ✅ | ⬜ pending |
| 15.1-04-T1 | 04 | 3 | D-18 scenarios 1, 2, 3, 5 | T-15.1-06 | admin-only reducer surface; no new auth surface | integration | `test -f test/backend/reducers/admin/seed-cost-extraction.test.ts && npx vitest run test/backend/reducers/admin/seed-cost-extraction.test.ts` exits 0 (4 passed or cleanly skipped) | ❌ W0 | ⬜ pending |
| 15.1-04-T2 | 04 | 3 | D-18 scenario 4, D-06 | T-15.1-06 | key-absent → preserve contract (NOT null) | integration | `grep -c "D-18-4" test/backend/reducers/admin/partial-update.test.ts ≥ 1 && npx vitest run test/backend/reducers/admin/partial-update.test.ts` exits 0 | ✅ | ⬜ pending |
| 15.1-04-T3 | 04 | 3 | D-18 (all 5), D-19 (guardrail), P-01, P-02, P-05 | T-15.1-06 | full-suite green + D-19 immutable files untouched + gitignore boundary | integration | `npm run test:integration` → 0 failures AND `git log -1 --name-only` = 2 test files only (see Plan 04 Task 3 `<verify>`) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*File Exists: ✅ already present · ❌ W0 means Wave 0 task must create it first*

*Populated by planner during step 8 — see RESEARCH.md §Validation Architecture for contract.*

---

## Wave 0 Requirements

- [ ] `test/backend/reducers/admin/seed-cost-extraction.test.ts` — NEW file created by Plan 04 Task 1. Contains D-18 scenarios 1, 2, 3, 5 (classic-only insert → zero auction; auction-only insert → zero classic; both sub-blocks absent → no row; default-seed regression 83×3 = 249 rows with real classic + zero auction).
- [ ] `test/backend/reducers/admin/partial-update.test.ts` — EXISTING file MODIFIED by Plan 04 Task 2. Adds D-18 scenario 4 (partial update with only `classicCostsJson` set, `auctionBaseBidJson` key absent → preserves existing auction struct column — NOT zeroed, NOT copied from classic). Existing scalar-preservation tests in this file remain untouched and serve as D-19 guardrail companions.

*Existing `test/backend/cost-sets/cost-set-lifecycle.test.ts` and `test/backend/reducers/admin/cost-set-pk.test.ts` remain unchanged (D-19) — they continue to provide coverage for the fully-populated path. Any edit to these files by Plan 04 is a phase-blocker.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Default-seed round-trip after `--clear-database` | D-09, D-11, D-12 | Touches real maincloud database via `post-publish.ts` — automated suite uses test-harness seed | After publish with `--clear-database`, run `spacetime sql 'SELECT COUNT(*) FROM HsrCharacterCost'` → expect 249 (83×3); same for lightcones → 468 (156×3); spot-check that `auctionBaseBid` structs are all-zero |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
