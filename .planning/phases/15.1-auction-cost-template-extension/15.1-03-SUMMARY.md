---
phase: 15.1-auction-cost-template-extension
plan: 03
subsystem: testing
tags: [seed-pipeline, cost-tables, sibling-block, auction, spacetimedb]

# Dependency graph
requires:
  - phase: 15.1-auction-cost-template-extension plan 01
    provides: sibling-block template JSON shape (classic/auction sub-blocks) for char and lc templates
  - phase: 15.1-auction-cost-template-extension plan 02
    provides: README transform tables updated to sibling-block shape
provides:
  - Production seed pipeline (scripts/seed-data.ts) reading sibling-block shape with D-07 zero-padding
  - Test-harness seed pipeline (test/shared/seed-data.ts) identically rewritten
  - Local test/data/*.json files re-migrated to sibling-block shape (on-disk, not committed — D-11/D-12)
affects: [15.1-plan-04, 15.4-cost-table-restructure, Phase 17 cost tables frontend]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sibling-block cost extractor: cost.<mode>.classic + cost.<mode>.auction parsed independently; absent sub-block → zero-pad on insert (D-07); both absent → skip row (D-09/D-10)"
    - "ZERO_EIDOLON / ZERO_SUPERPOSITION constants for zero-pad structs; extractEidolonCost / extractSuperpositionCost helpers shared across both seed files"
    - "No classic→auction copy: removed auctionBaseBid:{...costs} placeholder (D-08)"

key-files:
  created: []
  modified:
    - scripts/seed-data.ts
    - test/shared/seed-data.ts

key-decisions:
  - "D-06 honored: partial-update semantics are upstream in admin_bulk_upsert — seed always inserts with both structs fully populated"
  - "D-07 honored: zero-pad struct literals used on insert when classic or auction sub-block is absent"
  - "D-08 honored: auctionBaseBid:{...costs} placeholder removed from both seed files"
  - "D-09 honored: per-mode fan-out reads classic and auction sub-blocks independently; skip row if both absent"
  - "D-10 honored: absent cost.<mode> block → no row written for that mode"
  - "D-11/D-12 honored: local test/data/*.json re-migrated to sibling-block shape (on-disk only; gitignored path never committed)"
  - "D-13 honored: pairing loop unchanged in both files"
  - "D-14 deferred: _old backup cleanup is Claude's discretion — not actioned in this plan"
  - "D-15 honored: no reader source files (draftClassic.ts, draftAuction.ts, postDraft.ts) touched"
  - "D-16 honored: no validation tightening in admin_bulk_upsert"

patterns-established:
  - "Sibling-block extractor pattern: use optional classic?/auction? sub-block keys; zero-pad missing side on insert only"
  - "Zero-pad constants: ZERO_EIDOLON + ZERO_SUPERPOSITION defined once, reused across both extract helpers"

requirements-completed: []

# Metrics
duration: ~35min (split across two agent spawns with orchestrator verification gate)
completed: 2026-04-14
---

# Phase 15.1 Plan 03: Seed Pipeline Sibling-Block Rewrite Summary

**Both seed pipelines rewritten to parse classic/auction sub-blocks independently with D-07 zero-padding; auctionBaseBid:{...costs} placeholder removed; local data files re-migrated to sibling-block shape on disk.**

## Performance

- **Duration:** ~35 min (Tasks 1-2 in first agent spawn; D-11/D-12 gate confirmed by orchestrator; Task 4 in this spawn)
- **Started:** 2026-04-14T13:28:18Z
- **Completed:** 2026-04-14
- **Tasks:** 4
- **Files modified:** 2 (scripts/seed-data.ts, test/shared/seed-data.ts)

## Accomplishments
- Rewrote `scripts/seed-data.ts` cost extractors: replaced flat `RawModeEidolonBlock`/`RawModeSuperpositionBlock` types with sibling-block shape; added `ZERO_EIDOLON`/`ZERO_SUPERPOSITION` constants and `extractEidolonCost`/`extractSuperpositionCost` helpers; removed `auctionBaseBid: { ...costs }` placeholder
- Mirrored the same rewrite in `test/shared/seed-data.ts`: same helper pattern, widened inline types to sibling-block shape, replaced both cost loops; pairing loop untouched per D-02/D-13
- D-11/D-12 gate satisfied: `test/data/characters_table.json` (249 cost-mode blocks) and `test/data/lightcones_table.json` (468 cost-mode blocks) verified by orchestrator to be 100% sibling-block shape with zero flat blocks; local-only on disk per gitignore boundary

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite scripts/seed-data.ts cost extractors** - `5657738` (feat)
2. **Task 2: Mirror rewrite in test/shared/seed-data.ts** - `3235ff1` (feat)
3. **Task 3: D-11/D-12 data re-migration gate** - CONFIRMED COMPLETE by orchestrator verification (no commit — gitignored path; on-disk only)
4. **Task 4: SUMMARY.md + state tracking** - see Plan metadata commit below

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `scripts/seed-data.ts` — Production seed pipeline: sibling-block extractor, zero-pad constants, helper functions, placeholder removed (60 insertions, 25 deletions)
- `test/shared/seed-data.ts` — Test-harness seed pipeline: identical sibling-block rewrite, placeholder removed (53 insertions, 24 deletions)

## Files Audit: Stranger-Path Check

Commit `5657738` diff: `scripts/seed-data.ts` only — 1 file, no gitignored paths, no spacetimedb/**, no module_bindings/**.

Commit `3235ff1` diff: `test/shared/seed-data.ts` only — 1 file, no gitignored paths, no spacetimedb/**, no module_bindings/**.

D-15/D-16 guard: neither commit touches any reducer, validation, or reader source file.

## D-11/D-12 Migration Note

The `test/data/characters_table.json` and `test/data/lightcones_table.json` files were re-migrated to sibling-block shape locally (via a `tmp/` script that is gitignored per RESEARCH §Gitignore Boundary and PLAN P-01). These files are never committed. The orchestrator verified the on-disk state after the prior agent spawn:
- `characters_table.json`: 249 cost-mode blocks — all sibling-block (`classic`/`auction` keys), 0 flat, 0 empty
- `lightcones_table.json`: 468 cost-mode blocks — all sibling-block, 0 flat, 0 empty

## Decisions Made

All decisions followed plan specification exactly:
- D-06: partial-update semantics are upstream (admin_bulk_upsert untouched)
- D-07: zero-pad with `{e0:0,...e6:0}` / `{s1:0,...s5:0}` on insert when sub-block absent
- D-08: classic→auction copy removed from both seed files
- D-09/D-10: per-mode fan-out + absent-mode skip implemented
- D-11/D-12: local-only migration; gitignored path never committed
- D-13: pairing loop unchanged
- D-15/D-16: no reader or validator source touched

## Deviations from Plan

None - plan executed exactly as written. The D-11/D-12 human-action checkpoint behaved as designed: prior agent paused, orchestrator verified on-disk state, this agent resumed at Task 4.

## Issues Encountered

None. The two-spawn execution (prior agent: Tasks 1-3; this agent: Task 4) was the intended flow per the checkpoint protocol.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 complete. Both seed pipelines now consume the sibling-block template shape from Plan 01.
- Plan 04 (regression tests for partial-ingestion — D-18 test suite) can proceed immediately; the seed pipelines it tests are now in final shape.
- Default seed (scripts/post-publish.ts --clear-database) will correctly produce: all 3 modes per character/lightcone with real classic values and zero auction values.

## Self-Check: PASSED

- FOUND: `.planning/phases/15.1-auction-cost-template-extension/15.1-03-SUMMARY.md`
- FOUND: commit `5657738` (feat(15.1-03): rewrite scripts/seed-data.ts cost extractors)
- FOUND: commit `3235ff1` (feat(15.1-03): mirror sibling-block extractor rewrite in test/shared/seed-data.ts)

---
*Phase: 15.1-auction-cost-template-extension*
*Completed: 2026-04-14*
