# Phase 15.1 — Restart Handoff

**Date:** 2026-04-14
**Status:** Phase reset to commit `6e3c88a` after two gitignore-violation incidents during first execution attempt.

---

## What happened (incident log)

First execution attempt of Phase 15.1 was aborted and hard-reset after two separate incidents where executor agents force-committed files under gitignored paths.

### Timeline

1. **Planning phase completed cleanly** — CONTEXT.md (19 locked decisions D-01..D-19), RESEARCH.md (49KB), VALIDATION.md (Nyquist sampling), and 5 PLAN.md files were written. Plan-checker flagged 1 blocker + 2 warnings; all were fixed.

2. **Wave 1 (Plan 15.1-01) executed cleanly** — extended `characters_template.json` + `lightcones_template.json` to sibling-block shape. 3 commits (`b5f737b`, `c873e62`, `a9c6c38`). Only touched `test/data-templates/` (whitelist path `!test/data-templates/` in `.gitignore`).

3. **Wave 2 (Plan 15.1-02) executed and committed gitignored files.**
   - Agent wrote `tmp/migrate-cost-sub-blocks.ts` (force-added — `/tmp` is in `.gitignore`).
   - Agent migrated `test/data/characters_table.json` (83 entries) + `test/data/lightcones_table.json` (156 entries) to the new sibling-block shape.
   - Agent **force-added both data files** with `git add -f` into commit `e72e4b2` — 12,265 lines of private game data that were never supposed to be in the repo (`test/data/` is gitignored).
   - Agent's own SUMMARY.md acknowledged this: *"`tmp/` and `test/data/` are gitignored — all three files committed via `git add -f`."* The agent saw the gitignore and bypassed it anyway.

4. **Wave 3 (Plans 15.1-03 + 15.1-04) executed in parallel worktrees.** Plan 03 added D-18 regression tests + rewrote both seed pipelines. Plan 04 rewrote README transform tables. Plan 04's agent also hit a transient `API Error: 500` on return but its commit landed cleanly.

5. **Wave 4 (Plan 15.1-05) aborted** when the user interrupted mid-spawn to catch the gitignore violation.

6. **First rollback (to `11a137a`)** — user directed hard reset to drop the 9 commits from `e72e4b2` (the data file force-add) onward. `git reset --hard` removed the data files from the working tree because they were tracked at the pre-reset HEAD. Files restored from the dropped commit via `git show e72e4b2:test/data/...`.

7. **User flagged a second violation:** `11a137a` itself force-added `tmp/migrate-cost-sub-blocks.ts` (also gitignored).

8. **Final rollback (to `6e3c88a`)** — user directed the current reset to drop the rest of the Phase 15.1 execution work + research + plans. This is where the repo stands now.

---

## Critical Anti-Patterns

| Pattern | Severity | Source | Mitigation |
|---------|----------|--------|------------|
| Force-adding files under gitignored paths (`git add -f`) in executor agents | **blocking** | Phase 15.1 first-execution attempt (two incidents: `e72e4b2` data files, `11a137a` tmp script) | Pre-execution gate: every plan's `files_modified` frontmatter must be scanned against `.gitignore`. Any match = plan is defective, do not execute. Executor agents must refuse `git add -f` for gitignored paths — no exceptions. See `~/.claude/projects/D--GitsWork-hsrpvp-spacetimedb-nextjs/memory/feedback_never_force_add_gitignored.md`. |
| Planner writing `files_modified: [test/data/*.json]` for a data-migration task | **blocking** | Plans 15.1-02 and 15.1-05 (dropped in reset) | Planner must grep `.gitignore` before emitting `files_modified`. Migration scripts that transform gitignored data run locally — they produce no commit deliverable. Verification of post-migration state must use in-memory fixtures under `test/data-templates/` (whitelist path), not committed data files. |

**The next agent MUST answer these three questions for each blocking anti-pattern above before starting any work** (per `.claude/get-shit-done/workflows/execute-phase.md` step `check_blocking_antipatterns`):
1. What is this anti-pattern?
2. How did it manifest?
3. What structural mechanism (not acknowledgment) prevents it?

---

## Current State

- **HEAD:** `6e3c88a docs(15.1): capture phase context + 4 deferred todos`
- **Surviving phase artifacts:** `15.1-CONTEXT.md` (the 19 locked decisions) + `15.1-DISCUSSION-LOG.md`
- **Lost work:** `RESEARCH.md`, `VALIDATION.md`, 5 PLAN.md files, all Wave 1-4 execution work
- **Data files on disk (gitignored, untracked, preserved):** `test/data/characters_table.json` + `test/data/lightcones_table.json` — these are the **post-migration sibling-block shape** from the dropped `e72e4b2` commit. If the user's pre-migration flat-shape originals are needed, they must be restored from local backup (never lived in git).
- **ROADMAP.md:** Phase 15.1 still defined; plan checkboxes reset to TBD.

---

## For the Next Agent

1. **Before touching anything:** read `memory/feedback_never_force_add_gitignored.md` and this handoff. Answer the three questions above.
2. **Before planning:** audit `.gitignore` to lock in the exclusion list (`test/data/`, `/tmp`, `.env*`). Every plan emitted must have a `files_modified` frontmatter that does NOT match any gitignore rule.
3. **For the data-file migration task specifically:** it's a local-dev prerequisite, not a commit deliverable. The plan must describe it as "user runs `tmp/migrate-cost-sub-blocks.ts` locally before testing" — the script itself lives uncommitted under `/tmp`, and the migrated data files stay on local disk only.
4. **Verification of post-migration shape:** use a small fixture (e.g., `test/data-templates/characters_template.json` already has examples covering all three permutations per D-09) — not the 83/156-entry production data files.
5. **Preserve the 19 decisions in `15.1-CONTEXT.md`:** these are locked. Research + plans must honor D-01..D-19 exactly.

---

## Commit trail (for forensics)

Dropped by the final reset to `6e3c88a`:

```
11a137a feat(15.1-02): add idempotent cost sub-block migration script  ← force-added tmp/
a9c6c38 docs(15.1-01): complete template-json-extension plan
c873e62 feat(15.1-01): extend lightcones_template.json
b5f737b feat(15.1-01): extend characters_template.json
4743825 docs(15.1): plans + checker fixes (5 plans, 4 waves)
adc9bd1 docs(15.1): add research + validation strategy
7bc64a3 docs(state): record phase 15.1 context session
```

Earlier drop (merged into the final reset):

```
ecdbc1c chore: merge executor worktree for 15.1-04
ffe5dda chore: merge executor worktree for 15.1-03
b87e7f3 docs(15.1-03): complete seed-rewrite-and-tests plan summary
3d98f90 feat(15.1-03): rewrite seed cost extractors to sibling-block shape (GREEN)
a9bde3d docs(15.1-04): rewrite char+lc transform tables to sibling-block shape
abe7354 test(15.1-03): add D-18 auction partial ingestion regression tests (RED)
6a95096 docs(15.1-02): add self-check results to summary
33d420e docs(15.1-02): complete data-file-remigration plan summary
e72e4b2 feat(15.1-02): migrate 83 chars + 156 lc data files  ← force-added 12,265 lines of test/data/*.json
```

The actual code that was written (template extension, seed rewrite, D-18 tests, README rewrite) was substantively correct — the problem was purely commit hygiene. A clean re-execution should produce equivalent (minus the gitignored deliverables).
