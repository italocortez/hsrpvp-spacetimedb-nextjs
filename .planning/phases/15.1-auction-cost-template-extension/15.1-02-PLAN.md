---
phase: 15.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/data-templates/README.md
autonomous: true
requirements: []
decisions_addressed: [D-02, D-13, D-17]

must_haves:
  truths:
    - "README character + lightcone transform tables describe the sibling-block shape (D-17)."
    - "The L84 placeholder note `auctionBaseBid uses the same values as classicCosts for now (placeholder; the auction format may diverge in a later phase)` is deleted (D-17)."
    - "The pairing section is unchanged (D-02, D-13)."
  artifacts:
    - path: "test/data-templates/README.md"
      provides: "Onboarding doc describing sibling-block cost shape"
      contains: "cost.memory_of_chaos.classic"
  key_links:
    - from: "test/data-templates/README.md"
      to: "test/data-templates/characters_template.json (Plan 01)"
      via: "transform table references the new sibling-block keys"
      pattern: "cost\\.memory_of_chaos\\.classic"
---

<objective>
Rewrite the character and lightcone transform tables in `test/data-templates/README.md` to describe the sibling-block shape (D-17). Delete the L84 placeholder note. Leave the pairing section untouched (D-02, D-13).

Purpose: Documentation must match the templates. Phase 15 D-22 "templates are the source of truth" implies the README also has to reflect the canonical shape.
Output: One modified README.md file.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-auction-cost-template-extension/15.1-CONTEXT.md
@.planning/phases/15.1-auction-cost-template-extension/15.1-RESEARCH.md
@.planning/phases/15.1-auction-cost-template-extension/15.1-VALIDATION.md

<interfaces>
<!-- Current README rows to replace (verified from repo read at HEAD 6e3c88a) -->

Character rows at L37-39 (current flat shape):
```
| `cost.memory_of_chaos.E0..E6` | HsrCharacterCost row | gameMode: `"MemoryOfChaos"`, classicCosts+auctionBaseBid: `{e0..e6}`, costSetId: `cost.cost_set_id` |
| `cost.apocalyptic_shadow.E0..E6` | HsrCharacterCost row | gameMode: `"ApocalypticShadow"` |
| `cost.anomaly_arbitration.E0..E6` | HsrCharacterCost row | gameMode: `"AnomalyArbitration"` |
```

Lightcone rows at L58-60 (current flat shape):
```
| `cost.memory_of_chaos.S1..S5` | HsrLightconeCost row | gameMode: `"MemoryOfChaos"`, classicCosts+auctionBaseBid: `{s1..s5}`, costSetId: `cost.cost_set_id` |
| `cost.apocalyptic_shadow.S1..S5` | HsrLightconeCost row | gameMode: `"ApocalypticShadow"` |
| `cost.anomaly_arbitration.S1..S5` | HsrLightconeCost row | gameMode: `"AnomalyArbitration"` |
```

Placeholder note to DELETE at L84 (currently reads verbatim):
```
- `auctionBaseBid` uses the same values as `classicCosts` for now (placeholder; the auction format may diverge in a later phase).
```

Pairing section at L65-76 (DO NOT TOUCH — D-02, D-13).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite character + lightcone transform tables to sibling-block shape; delete L84 placeholder note</name>
  <files>test/data-templates/README.md</files>
  <read_first>
    - test/data-templates/README.md (current state, all 86 lines)
    - .planning/phases/15.1-auction-cost-template-extension/15.1-CONTEXT.md (D-02, D-13, D-17)
    - .planning/phases/15.1-auction-cost-template-extension/15.1-RESEARCH.md §Code Examples §4
  </read_first>
  <action>
Apply three surgical edits to `test/data-templates/README.md`:

**Edit 1 (characters table, replace L37-39):** Replace the three rows matching `cost.memory_of_chaos.E0..E6` / `cost.apocalyptic_shadow.E0..E6` / `cost.anomaly_arbitration.E0..E6` with EXACTLY these rows:

```md
| `cost.memory_of_chaos.classic.E0..E6` | HsrCharacterCost.classicCosts | gameMode: `"MemoryOfChaos"`; missing sub-block → zero-pad on insert (D-07); present → apply values |
| `cost.memory_of_chaos.auction.E0..E6` | HsrCharacterCost.auctionBaseBid | gameMode: `"MemoryOfChaos"`; missing sub-block → zero-pad on insert (D-07); present → apply values |
| `cost.apocalyptic_shadow.classic.E0..E6` | HsrCharacterCost.classicCosts | gameMode: `"ApocalypticShadow"`; same rules |
| `cost.apocalyptic_shadow.auction.E0..E6` | HsrCharacterCost.auctionBaseBid | gameMode: `"ApocalypticShadow"`; same rules |
| `cost.anomaly_arbitration.classic.E0..E6` | HsrCharacterCost.classicCosts | gameMode: `"AnomalyArbitration"`; same rules |
| `cost.anomaly_arbitration.auction.E0..E6` | HsrCharacterCost.auctionBaseBid | gameMode: `"AnomalyArbitration"`; same rules |
| (mode block present but both `classic` + `auction` sub-blocks absent) | (no row written) | Skip the mode entirely (D-09/D-10) |
```

Update the **Fan-out count** line immediately after the character table to read:
```md
**Fan-out count:** Up to 7 eidolons × 2 draft modes (classic/auction) × 3 game modes = **42 HsrCharacterCost writable fields per character** (2 struct columns × 3 game modes = 6 row-level slots). A row is written for a game mode only if at least one of `classic` / `auction` is present in the template; the missing side is zero-padded on insert (D-07) and preserved on update (D-06).
```

**Edit 2 (lightcones table, replace L58-60):** Replace the three rows matching `cost.memory_of_chaos.S1..S5` / etc. with EXACTLY these rows:

```md
| `cost.memory_of_chaos.classic.S1..S5` | HsrLightconeCost.classicCosts | gameMode: `"MemoryOfChaos"`; missing sub-block → zero-pad on insert (D-07); present → apply values |
| `cost.memory_of_chaos.auction.S1..S5` | HsrLightconeCost.auctionBaseBid | gameMode: `"MemoryOfChaos"`; missing sub-block → zero-pad on insert (D-07); present → apply values |
| `cost.apocalyptic_shadow.classic.S1..S5` | HsrLightconeCost.classicCosts | gameMode: `"ApocalypticShadow"`; same rules |
| `cost.apocalyptic_shadow.auction.S1..S5` | HsrLightconeCost.auctionBaseBid | gameMode: `"ApocalypticShadow"`; same rules |
| `cost.anomaly_arbitration.classic.S1..S5` | HsrLightconeCost.classicCosts | gameMode: `"AnomalyArbitration"`; same rules |
| `cost.anomaly_arbitration.auction.S1..S5` | HsrLightconeCost.auctionBaseBid | gameMode: `"AnomalyArbitration"`; same rules |
| (mode block present but both `classic` + `auction` sub-blocks absent) | (no row written) | Skip the mode entirely (D-09/D-10) |
```

Update the **Fan-out count** line immediately after the lightcone table to read:
```md
**Fan-out count:** Up to 5 superimpositions × 2 draft modes (classic/auction) × 3 game modes = **30 HsrLightconeCost writable fields per lightcone** (2 struct columns × 3 game modes = 6 row-level slots). A row is written for a game mode only if at least one of `classic` / `auction` is present; the missing side is zero-padded on insert (D-07) and preserved on update (D-06).
```

**Edit 3 (delete L84 placeholder note):** Delete the entire bullet line reading:
```
- `auctionBaseBid` uses the same values as `classicCosts` for now (placeholder; the auction format may diverge in a later phase).
```

Immediately replace it with a new bullet:
```
- `auctionBaseBid` is populated from `cost.<mode>.auction` sub-blocks (D-01). Missing on insert → zero-padded per D-07. Missing on update → existing value preserved per D-06. There is no silent classic → auction copy at the seed layer (D-08).
```

**DO NOT TOUCH:**
- The pairing table and its fan-out line (L65-76 region) — D-02, D-13.
- The header (L1-13) and "Notes" heading.
- Other bullets under Notes (L81-85 except L84 itself).
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('test/data-templates/README.md','utf8'); if(r.includes('placeholder; the auction format may diverge')) throw new Error('P-05: placeholder note still present'); if(!r.includes('cost.memory_of_chaos.classic.E0..E6')) throw new Error('char classic row missing'); if(!r.includes('cost.memory_of_chaos.auction.E0..E6')) throw new Error('char auction row missing'); if(!r.includes('cost.memory_of_chaos.classic.S1..S5')) throw new Error('lc classic row missing'); if(!r.includes('cost.memory_of_chaos.auction.S1..S5')) throw new Error('lc auction row missing'); if(!r.includes('source_name')||!r.includes('target_name')) throw new Error('D-02 violation: pairing section damaged'); if(!r.includes('HsrSynergyCost row')) throw new Error('D-02 violation: pairing rows missing'); console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "placeholder; the auction format may diverge" test/data-templates/README.md` returns 0 (D-17 P-05).
    - `grep -c "cost.memory_of_chaos.classic.E0..E6" test/data-templates/README.md` returns 1.
    - `grep -c "cost.memory_of_chaos.auction.E0..E6" test/data-templates/README.md` returns 1.
    - `grep -c "cost.memory_of_chaos.classic.S1..S5" test/data-templates/README.md` returns 1.
    - `grep -c "cost.memory_of_chaos.auction.S1..S5" test/data-templates/README.md` returns 1.
    - Pairing section intact: `grep -c "source_name" test/data-templates/README.md` returns ≥1, and `grep -c "HsrSynergyCost row" test/data-templates/README.md` returns 3 (D-02, D-13 unchanged).
    - File is still valid Markdown (renders without errors).
  </acceptance_criteria>
  <done>Character and lightcone transform tables rewritten to sibling-block shape; L84 placeholder deleted; pairing section and Notes structure intact.</done>
</task>

<task type="auto">
  <name>Task 2: Commit README rewrite atomically</name>
  <files>test/data-templates/README.md</files>
  <read_first>
    - test/data-templates/README.md (post-edit)
  </read_first>
  <action>
Verify the file is NOT gitignored (whitelist `!test/data-templates/` applies), then commit atomically with explicit file name.

```
git check-ignore test/data-templates/README.md
```
Expected exit code: 1 (not ignored).

```
git add test/data-templates/README.md
git status --short
```
Expected: exactly one line `M  test/data-templates/README.md`. If anything else is staged, `git reset` and retry.

```
git commit -m "docs(15.1): rewrite README transform tables to sibling-block shape (D-17)"
```

Forbidden: `git add -A`, `git add .`, `git add -f`, any path under `test/data/**` or `tmp/**` in the commit.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format:"%s" | grep -E "^(docs\(15.1\)|test/data-templates/README.md)" && git log -1 --name-only --pretty=format:"" | grep -v "^$" | grep -v "^test/data-templates/README.md" | wc -l | grep -qE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` lists ONLY `test/data-templates/README.md`.
    - Commit subject starts with `docs(15.1):`.
    - `git check-ignore test/data-templates/README.md` exits 1 (not ignored).
  </acceptance_criteria>
  <done>README commit landed atomically; only the one file in the diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation file only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-02 | (none) | test/data-templates/README.md | accept | Documentation change; no runtime surface; no user input deserialization. |
</threat_model>

<verification>
- README has no occurrence of the L84 placeholder string (P-05 guard).
- Sibling-block keys appear in both character and lightcone tables.
- Pairing section intact (D-02, D-13).
- Commit diff shows exactly 1 file.
</verification>

<success_criteria>
- D-17 satisfied: transform tables describe sibling-block shape, L84 note gone.
- D-02, D-13 satisfied: pairing section untouched.
- Gitignore boundary respected.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-auction-cost-template-extension/15.1-02-SUMMARY.md` documenting the 3 edits applied and confirming pairing section is pristine.
</output>
