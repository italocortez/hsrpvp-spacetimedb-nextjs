---
phase: 15.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/data-templates/characters_template.json
  - test/data-templates/lightcones_template.json
autonomous: true
requirements: []
decisions_addressed: [D-01, D-02, D-03, D-04, D-05, D-09, D-10]

must_haves:
  truths:
    - "Every template cost.<mode> block is a dict with optional classic and/or auction sub-blocks (D-01)."
    - "At least one template entry per file exercises a classic-only mode, at least one exercises classic+auction both, at least one exercises auction-only, and at least one exercises both-sub-blocks-absent (so D-09 fan-out permutations are exercised downstream)."
    - "pairing_template.json is not touched (D-02)."
    - "No schema files, reducer files, or module bindings are modified (D-03, D-04, D-05)."
  artifacts:
    - path: "test/data-templates/characters_template.json"
      provides: "Sibling-block character template covering D-09 permutations"
      contains: "\"classic\""
    - path: "test/data-templates/lightcones_template.json"
      provides: "Sibling-block lightcone template covering D-09 permutations"
      contains: "\"classic\""
  key_links:
    - from: "test/data-templates/characters_template.json"
      to: "scripts/seed-data.ts + test/shared/seed-data.ts (Plan 03)"
      via: "sibling-block cost.<mode>.{classic,auction} shape"
      pattern: "\"classic\":\\s*\\{\\s*\"E0\""
    - from: "test/data-templates/lightcones_template.json"
      to: "scripts/seed-data.ts + test/shared/seed-data.ts (Plan 03)"
      via: "sibling-block cost.<mode>.{classic,auction} shape"
      pattern: "\"classic\":\\s*\\{\\s*\"S1\""
---

<objective>
Extend the two committed templates (`test/data-templates/characters_template.json` + `lightcones_template.json`) to the parallel sibling-block shape per D-01: each `cost.<mode>` block becomes a dict with optional `classic` and `auction` sub-blocks. Cover every D-09 permutation across the 4 template entries so Plan 03's extractor rewrite and Plan 04's D-18 tests exercise real fixture data.

Purpose: Close Phase 15 IN-10 at the template layer. These templates are the public onboarding shape — Phase 15 D-22 "templates are the source of truth" applies.
Output: Two modified JSON files. No schema changes, no reducer changes, no bindings regeneration.
</objective>

<execution_context>
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/workflows/execute-plan.md
@D:/GitsWork/hsrpvp-spacetimedb-nextjs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-auction-cost-template-extension/15.1-CONTEXT.md
@.planning/phases/15.1-auction-cost-template-extension/15.1-RESEARCH.md
@.planning/phases/15.1-auction-cost-template-extension/15.1-VALIDATION.md

<interfaces>
<!-- Sibling-block shape per D-01 and RESEARCH §Pattern 1 -->

Characters — new cost.<mode> block shape (both sub-blocks optional):
```json
{
  "cost": {
    "cost_set_id": 0,
    "memory_of_chaos": {
      "classic": { "E0": 7, "E1": 8, "E2": 11, "E3": 11.5, "E4": 12.5, "E5": 13, "E6": 15 },
      "auction": { "E0": 70, "E1": 80, "E2": 110, "E3": 115, "E4": 125, "E5": 130, "E6": 150 }
    },
    "apocalyptic_shadow": {
      "classic": { "E0": 7, "E1": 8, "E2": 11, "E3": 11.5, "E4": 12.5, "E5": 13, "E6": 15 }
    }
  }
}
```

Lightcones — new cost.<mode> block shape (both sub-blocks optional):
```json
{
  "cost": {
    "cost_set_id": 0,
    "memory_of_chaos": {
      "classic": { "S1": 1.5, "S2": 1.5, "S3": 1.5, "S4": 1.5, "S5": 1.5 }
    }
  }
}
```

D-09 permutations this plan must cover across the 4 template entries (acheron, aglaea, adreamscentedinwheat, cornucopia):
- P1: classic only (seed path → auctionBaseBid zero-padded on insert)
- P2: classic + auction both present
- P3: auction only (seed path → classicCosts zero-padded on insert)
- P4: mode block present but BOTH sub-blocks absent — `"anomaly_arbitration": {}` (seed must skip the mode entirely)

Gitignore boundary (RESEARCH §Gitignore Boundary):
- `test/data-templates/*.json` ARE committable (whitelist override `!test/data-templates/` in .gitignore).
- `test/data/**` is gitignored — NOT touched by this plan.
- `pairing_template.json` is NOT touched (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend characters_template.json to sibling-block shape covering all D-09 permutations</name>
  <files>test/data-templates/characters_template.json</files>
  <read_first>
    - test/data-templates/characters_template.json (current 2-entry flat shape)
    - .planning/phases/15.1-auction-cost-template-extension/15.1-CONTEXT.md (D-01, D-02, D-03, D-09, D-10)
    - .planning/phases/15.1-auction-cost-template-extension/15.1-RESEARCH.md §Code Examples §1 + §Pattern 1
  </read_first>
  <action>
Rewrite every `cost.<mode>` block in the 2 existing character entries (acheron, aglaea) from the current flat `{ E0..E6 }` shape to the sibling-block `{ classic?: {E0..E6}, auction?: {E0..E6} }` shape per D-01.

Distribute the 4 D-09 permutations across the 6 mode blocks (2 entries × 3 modes each) as follows — this shape is LOCKED, do not deviate:

**acheron** — classic-only across all modes (P1, the default-seed case):
```json
"cost": {
  "cost_set_id": 0,
  "memory_of_chaos": {
    "classic": { "E0": 7, "E1": 8, "E2": 11, "E3": 11.5, "E4": 12.5, "E5": 13, "E6": 15 }
  },
  "apocalyptic_shadow": {
    "classic": { "E0": 7, "E1": 8, "E2": 11, "E3": 11.5, "E4": 12.5, "E5": 13, "E6": 15 }
  },
  "anomaly_arbitration": {
    "classic": { "E0": 7, "E1": 8, "E2": 11, "E3": 11.5, "E4": 12.5, "E5": 13, "E6": 15 }
  }
}
```

**aglaea** — mix P2/P3/P4 to exercise the remaining permutations:
```json
"cost": {
  "cost_set_id": 0,
  "memory_of_chaos": {
    "classic": { "E0": 6, "E1": 10, "E2": 12, "E3": 12.5, "E4": 13.5, "E5": 14, "E6": 17 },
    "auction": { "E0": 60, "E1": 100, "E2": 120, "E3": 125, "E4": 135, "E5": 140, "E6": 170 }
  },
  "apocalyptic_shadow": {
    "auction": { "E0": 60, "E1": 100, "E2": 120, "E3": 125, "E4": 135, "E5": 140, "E6": 170 }
  },
  "anomaly_arbitration": {}
}
```

Preserve every non-cost field exactly (name, display_name, aliases, rarity, path, element, role, archetype, version_released, treat_as_version, image_url, skel_url, atlas_url, atlas_img_url, positioning). Only the `cost` block changes.

Keep the JSON valid (pretty-printed, 2-space indent — match the current file's style). No trailing commas.
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('test/data-templates/characters_template.json','utf8')); if(j.length!==2) throw new Error('expected 2 entries'); const ach=j.find(c=>c.name==='acheron'); const agl=j.find(c=>c.name==='aglaea'); ['memory_of_chaos','apocalyptic_shadow','anomaly_arbitration'].forEach(m=>{ if(!ach.cost[m].classic||ach.cost[m].auction) throw new Error('acheron '+m+' must be classic-only'); }); if(!agl.cost.memory_of_chaos.classic||!agl.cost.memory_of_chaos.auction) throw new Error('aglaea MoC must have both'); if(agl.cost.apocalyptic_shadow.classic||!agl.cost.apocalyptic_shadow.auction) throw new Error('aglaea AS must be auction-only'); if(Object.keys(agl.cost.anomaly_arbitration).length!==0) throw new Error('aglaea AA must be empty block'); console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid JSON with exactly 2 entries.
    - acheron has `classic` sub-block in all 3 modes; NO `auction` sub-block anywhere (P1 coverage).
    - aglaea memory_of_chaos has BOTH `classic` AND `auction` sub-blocks (P2 coverage).
    - aglaea apocalyptic_shadow has ONLY `auction` sub-block (P3 coverage).
    - aglaea anomaly_arbitration is present but empty `{}` (P4 coverage).
    - `grep -c '"E0":' test/data-templates/characters_template.json` returns 5 (3 classic acheron + 1 classic aglaea + 1 auction aglaea MoC + 1 auction aglaea AS = 6; exact count is secondary — shape is primary).
    - No other fields touched (`git diff --stat` shows one file changed, cost blocks only).
  </acceptance_criteria>
  <done>Template file contains all 4 D-09 permutations across the 2 entries; JSON is valid; no non-cost fields were modified.</done>
</task>

<task type="auto">
  <name>Task 2: Extend lightcones_template.json to sibling-block shape covering all D-09 permutations</name>
  <files>test/data-templates/lightcones_template.json</files>
  <read_first>
    - test/data-templates/lightcones_template.json (current 2-entry flat shape)
    - .planning/phases/15.1-auction-cost-template-extension/15.1-CONTEXT.md (D-01, D-09, D-10)
    - .planning/phases/15.1-auction-cost-template-extension/15.1-RESEARCH.md §Code Examples §1 + §Pattern 1
  </read_first>
  <action>
Rewrite every `cost.<mode>` block in the 2 existing lightcone entries (adreamscentedinwheat, cornucopia) from flat `{ S1..S5 }` to sibling-block `{ classic?: {S1..S5}, auction?: {S1..S5} }` per D-01.

Distribute D-09 permutations — LOCKED shape:

**adreamscentedinwheat** — classic-only across all modes (P1, the default-seed case):
```json
"cost": {
  "cost_set_id": 0,
  "memory_of_chaos": {
    "classic": { "S1": 1.5, "S2": 1.5, "S3": 1.5, "S4": 1.5, "S5": 1.5 }
  },
  "apocalyptic_shadow": {
    "classic": { "S1": 1.5, "S2": 1.5, "S3": 1.5, "S4": 1.5, "S5": 1.5 }
  },
  "anomaly_arbitration": {
    "classic": { "S1": 1.5, "S2": 1.5, "S3": 1.5, "S4": 1.5, "S5": 1.5 }
  }
}
```

**cornucopia** — mix P2/P3/P4:
```json
"cost": {
  "cost_set_id": 0,
  "memory_of_chaos": {
    "classic": { "S1": 0.5, "S2": 0.5, "S3": 0.5, "S4": 0.5, "S5": 0.5 },
    "auction": { "S1": 5, "S2": 5, "S3": 5, "S4": 5, "S5": 5 }
  },
  "apocalyptic_shadow": {
    "auction": { "S1": 5, "S2": 5, "S3": 5, "S4": 5, "S5": 5 }
  },
  "anomaly_arbitration": {}
}
```

Preserve every non-cost field exactly (name, display_name, aliases, rarity, path, image_url, positioning). Only the `cost` block changes.

Keep JSON valid with 2-space indent matching the current file.
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('test/data-templates/lightcones_template.json','utf8')); if(j.length!==2) throw new Error('expected 2 entries'); const a=j.find(c=>c.name==='adreamscentedinwheat'); const c=j.find(c=>c.name==='cornucopia'); ['memory_of_chaos','apocalyptic_shadow','anomaly_arbitration'].forEach(m=>{ if(!a.cost[m].classic||a.cost[m].auction) throw new Error('adream '+m+' must be classic-only'); }); if(!c.cost.memory_of_chaos.classic||!c.cost.memory_of_chaos.auction) throw new Error('cornucopia MoC must have both'); if(c.cost.apocalyptic_shadow.classic||!c.cost.apocalyptic_shadow.auction) throw new Error('cornucopia AS must be auction-only'); if(Object.keys(c.cost.anomaly_arbitration).length!==0) throw new Error('cornucopia AA must be empty block'); console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid JSON with exactly 2 entries.
    - adreamscentedinwheat has `classic` sub-block in all 3 modes; NO `auction` sub-block (P1 coverage).
    - cornucopia memory_of_chaos has BOTH `classic` and `auction` (P2 coverage).
    - cornucopia apocalyptic_shadow has ONLY `auction` (P3 coverage).
    - cornucopia anomaly_arbitration is empty `{}` (P4 coverage).
    - No non-cost fields were modified (`git diff --stat` shows cost blocks only).
  </acceptance_criteria>
  <done>Template file contains all 4 D-09 permutations across the 2 entries; JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 3: Commit template extension atomically</name>
  <files>test/data-templates/characters_template.json, test/data-templates/lightcones_template.json</files>
  <read_first>
    - test/data-templates/characters_template.json (post-edit)
    - test/data-templates/lightcones_template.json (post-edit)
  </read_first>
  <action>
Verify the two files are NOT gitignored, then commit them atomically with an explicit file list (NEVER `git add -A` or `git add .` or `git add -f`).

Run:
```
git check-ignore test/data-templates/characters_template.json test/data-templates/lightcones_template.json
```
Expected exit code: 1 (neither is ignored — the `!test/data-templates/` whitelist applies).

Then:
```
git add test/data-templates/characters_template.json test/data-templates/lightcones_template.json
git status --short
```
Expected output: exactly 2 lines, both `M ` entries for the two template files. If anything else appears staged, `git reset` and start this task over.

Commit:
```
git commit -m "docs(15.1): extend char + lc templates to sibling-block shape (D-01)"
```

Do NOT touch `test/data/`, `tmp/`, `pairing_template.json`, seed files, or README in this task.
  </action>
  <verify>
    <automated>git log -1 --stat --pretty=format:"%s" && git check-ignore test/data-templates/characters_template.json test/data-templates/lightcones_template.json; [ $? -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore` exits with code 1 for both files (they are NOT gitignored).
    - `git log -1 --stat` shows exactly 2 files modified (both templates) and commit message begins with `docs(15.1): extend char + lc templates`.
    - `git diff HEAD~1 HEAD --name-only` lists exactly `test/data-templates/characters_template.json` and `test/data-templates/lightcones_template.json`.
    - No files under `test/data/`, `tmp/`, `spacetimedb/`, `scripts/`, or `test/shared/` appear in the diff.
  </acceptance_criteria>
  <done>Atomic commit landed; only the 2 template files in the diff; no gitignored paths touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Template JSON is developer-authored fixture data; not user input; not deserialized at runtime by any reducer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-01 | (none) | test/data-templates/*.json | accept | No new user-facing surface, no new reducer entry, no new egress. Fixture data is consumed only at developer seed time and in test fixtures, guarded by existing `JSON.parse` + existing `validateEnum`/`validateKeys` from Phase 15 (D-16 preserves this). |
</threat_model>

<verification>
- Both template files parse as valid JSON.
- All 4 D-09 permutations (P1/P2/P3/P4) are represented across the 4 entries.
- `git log -1 --stat` shows exactly the 2 template files — nothing else.
- No modifications to `test/data/**`, `tmp/**`, `pairing_template.json`, `spacetimedb/**`, `scripts/**`, `test/shared/**`, README.md.
</verification>

<success_criteria>
- D-01 satisfied: sibling-block shape in both templates.
- D-02 satisfied: pairing template untouched.
- D-03/D-04/D-05 satisfied: no schema/reducer/synergy files touched.
- D-09/D-10 permutations exercisable by downstream seed + test plans.
- Gitignore boundary respected (no `-f`, no gitignored path in commit).
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-auction-cost-template-extension/15.1-01-SUMMARY.md` documenting the permutation distribution table and confirming no gitignored paths were staged.
</output>
