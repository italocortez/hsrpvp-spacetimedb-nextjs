---
phase: 15.1-auction-cost-template-extension
plan: "01"
subsystem: testing
tags: [json, templates, auction, cost, sibling-block]

requires:
  - phase: 15-backend-pre-work
    provides: "HsrCharacterCost + HsrLightconeCost auction columns (auctionBaseBid) and partial-update seed pipeline"

provides:
  - "characters_template.json extended to sibling-block shape covering all 4 D-09 permutations"
  - "lightcones_template.json extended to sibling-block shape covering all 4 D-09 permutations"

affects: [15.1-02, 15.1-03, 15.1-04, 15.1-05]

tech-stack:
  added: []
  patterns:
    - "Sibling-block cost shape: cost.<mode>.{classic?, auction?} — both sub-blocks optional per D-01"
    - "D-09 permutation coverage: P1=classic-only, P2=both, P3=auction-only, P4=mode-present-empty"

key-files:
  created: []
  modified:
    - test/data-templates/characters_template.json
    - test/data-templates/lightcones_template.json

key-decisions:
  - "D-01 honored: flat cost.<mode>.{E0..E6} shape replaced with sibling-block cost.<mode>.{classic?, auction?}"
  - "D-09 permutations distributed: acheron=P1 across all modes; aglaea=P2(MoC)+P3(AS)+P4(AA)"
  - "D-09 permutations distributed: adreamscentedinwheat=P1 across all modes; cornucopia=P2(MoC)+P3(AS)+P4(AA)"
  - "No gitignored paths staged — files committed via explicit path list, no -f flag"

patterns-established:
  - "Template entries as D-09 fixture coverage: first entry = P1 (classic-only, default-seed case); second entry = P2/P3/P4 mix"

requirements-completed: []

duration: 10min
completed: "2026-04-14"
---

# Phase 15.1 Plan 01: Template Extension Summary

**characters_template.json and lightcones_template.json rewritten from flat cost shape to sibling-block `cost.<mode>.{classic?, auction?}` covering all 4 D-09 permutations across 4 entries**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-14T18:00:00Z
- **Completed:** 2026-04-14T18:11:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Extended `characters_template.json` (2 entries) from flat `{E0..E6}` to sibling-block `{classic?, auction?}` shape per D-01
- Extended `lightcones_template.json` (2 entries) from flat `{S1..S5}` to sibling-block shape per D-01
- All 4 D-09 permutations covered across each file's 2 entries (P1/P2/P3/P4)
- Atomic commit of exactly 2 files; gitignore boundary respected; no `-f` used

## D-09 Permutation Distribution

| Entry | File | MoC | AS | AA | Permutations |
|---|---|---|---|---|---|
| acheron | characters_template.json | classic-only | classic-only | classic-only | P1 (all modes) |
| aglaea | characters_template.json | classic+auction | auction-only | empty `{}` | P2 + P3 + P4 |
| adreamscentedinwheat | lightcones_template.json | classic-only | classic-only | classic-only | P1 (all modes) |
| cornucopia | lightcones_template.json | classic+auction | auction-only | empty `{}` | P2 + P3 + P4 |

## Task Commits

Tasks 1 and 2 were combined into a single atomic commit per Task 3's instruction:

1. **Task 1: Extend characters_template.json** - `a7a4253` (docs)
2. **Task 2: Extend lightcones_template.json** - `a7a4253` (docs, same commit)
3. **Task 3: Atomic commit** - `a7a4253` (docs: extend char + lc templates to sibling-block shape (D-01))

## Files Created/Modified

- `test/data-templates/characters_template.json` - Rewritten cost blocks to sibling-block shape; acheron P1, aglaea P2/P3/P4
- `test/data-templates/lightcones_template.json` - Rewritten cost blocks to sibling-block shape; adreamscentedinwheat P1, cornucopia P2/P3/P4

## Decisions Made

None beyond plan specification — executed exactly as written. D-01 shape and D-09 permutation distribution were locked in the plan.

## Deviations from Plan

None - plan executed exactly as written.

## Gitignore Boundary Audit

- `git check-ignore test/data-templates/characters_template.json test/data-templates/lightcones_template.json` exited with code 1 (neither file is ignored)
- Staged via explicit path list: `git add test/data-templates/characters_template.json test/data-templates/lightcones_template.json`
- No `-f`/`--force` flag used at any point
- `git diff HEAD~1 HEAD --name-only` confirms exactly 2 files: both templates only
- No `test/data/`, `tmp/`, `spacetimedb/`, `scripts/`, or `test/shared/` paths in commit

## Issues Encountered

None.

## Known Stubs

None — the template files contain real fixture values, not placeholder data. The `auction` values (e.g., E0:60..E6:170 for aglaea) are example figures suitable for downstream seed + test use.

## Next Phase Readiness

- Plan 15.1-02 (data file migration) can proceed: templates now define the authoritative sibling-block shape
- Plan 15.1-03 (seed rewrite) has real D-09 fixture data to validate against
- Plan 15.1-04 (README rewrite) has the correct shape to document
- `pairing_template.json` is untouched (D-02 satisfied)
- No schema, reducer, or binding files were modified (D-03/D-04/D-05 satisfied)

---
*Phase: 15.1-auction-cost-template-extension*
*Completed: 2026-04-14*
