// # Determines: player | referee | spectator
// May need have some conflict with GameDataProvider, depending on which tables we subscribe here or if we just wrap
// an existing subscriber from game data provider and derive role from that.